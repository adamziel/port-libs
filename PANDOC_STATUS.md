# Pandoc Status

Updated: 2026-07-01 UTC

Current registry audit: 51 upstream input formats tracked; 33 have partial native PHP readers; 18 remain unsupported. Output registry audit: 75 upstream output formats tracked; 19 have partial native PHP writers; 56 remain unsupported. Project-local inputs `pdf` and `doc` are tracked separately from upstream Pandoc inputs. The latest format-dispatch work keeps bounded `xml`, `jats`, and `bits` readers on `PortLibs\Pandoc\XmlReader`, keeps `docbook` on direct `PortLibs\Pandoc\DocBookReader` dispatch, moves `html` onto dedicated `PortLibs\Pandoc\HtmlReader` dispatch while preserving the current HTML-capable reader bridge, adds bounded `opml` dispatch through `PortLibs\Pandoc\OpmlReader` plus `PortLibs\Pandoc\OpmlWriter`, adds bounded DOCX output through `PortLibs\Pandoc\DocxWriter`, adds bounded EPUB/EPUB3 output through `PortLibs\Pandoc\EpubWriter`, adds bounded PPTX output through `PortLibs\Pandoc\PptxWriter` including Courier code inline/block runs, preserved inline runs in title/list output, ordered list style/delimiter autonumbering, ordered-list margins and list continuation paragraphs, small-caps and adjacent-text inline run properties, standalone image-only and content-with-caption slide grouping with figure captions, shape-free blank slides for empty or NBSP-only headings/bodies, table-of-contents slides with internal slide links, slide-level-aware heading breaks, background images from heading attributes, slide-level-0 first-heading title promotion, speaker-note package parts, public endnote slides, custom document properties from Pandoc metadata, raw OpenXML inline/block passthrough, and upstream-style omission of non-OpenXML raw inline plus empty-alt unresolved image content, closes the pinned upstream CSV/TSV parser evidence through `PortLibs\Pandoc\DelimitedTextReader`, closes the pinned upstream XLSX reader fixture through `PortLibs\Pandoc\XlsxReader`, closes the pinned upstream PPTX reader fixture through `PortLibs\Pandoc\PptxReader`, adds bounded Jira wiki reader unit semantics through `PortLibs\Pandoc\JiraReader`, exposes wiki-family registry metadata for 7 upstream input tokens and 5 upstream output tokens without claiming direct wiki parity, and covers the six pinned upstream FB2 golden fixture semantics through `PortLibs\Pandoc\Fb2Reader`; full Pandoc parity remains open for the larger unsupported/partial format surface.

Rule: a format is complete only when repo passing tests equals upstream tests.

Current DOCX/OpenXML audit: full upstream DOCX parity is not defensible. The pinned Pandoc `test/docx` inventory at `0640c4c9859aa5a3ede082c190fcd5883c24ac83` has 233 artifacts: 112 `.native` expected artifacts, 121 `.docx` package artifacts, and 38 golden `.docx` outputs. Current upstream `612e143fbe6d735b612c4800d21e61b7d44e4dca` adds three DOCX drift artifacts now checked in under `lanes/pandoc/fixtures/upstream-current-docx`: `lists_restart_8367.docx`, `lists_restart_8367.native`, and `ns0-reference.docx`. This repo currently has 36 checked-in `upstream-native-docx-*` fixtures, 23 exact normalized matches to pinned upstream native fixture names, 2 checked-in current-upstream `.docx` package fixtures, 0 checked-in pinned upstream `.docx` package fixtures, and a core `PortLibs\Pandoc\DocxWriter` implementation registered as partial output support. The writer-golden generation harness is wired for all 38 upstream writer cases; using the sibling hydrated cache at `.upstream-cache/pandoc-current/test/docx`, current generated stable package comparison coverage is 38/38 generated, 38/38 compared, 38/38 matched, 0 missing, 0 unexpected, and 0 mismatched. This closes the checked writer-golden stable package gate without recording an upstream Haskell/Cabal/Tasty runner result or full DOCX/OpenXML parity. Broader package coverage is synthetic ZIP/XML plus bounded package-provenance tests.

Optional DOCX/native smoke: `tools/pandoc-docx-native-smoke.php` compares same-basename upstream `.docx` and `.native` files when `.upstream-cache/pandoc-current/test/docx` or `PANDOC_UPSTREAM_DOCX_DIR` is available. It reports artifact/pair counts, parsed counts, same text/top-type counts, and known semantic gap categories; missing upstream cache is reported as skipped with exit 0 so PR CI does not claim or require full upstream DOCX parity.

Optional DOCX parser coverage audit: `tools/pandoc-docx-parity-audit.php` reports root-level paired upstream `.docx`/`.native` parser acceptance when the local upstream cache is available, includes writer-golden inventory/support status, and explicitly avoids asserting AST equality, upstream Haskell runner parity, writer package semantic parity, or full DOCX/OpenXML parity. Its stable JSON includes a `parserAcceptanceBaseline`, a `parserAcceptanceRegression` guard, and `writerGoldenEvidence` showing generation/comparison status; `--fail-on-regression` exits nonzero only when a present cache regresses below the current 74/74 paired-fixture parser-acceptance baseline.

Optional DOCX writer-golden package manifest: `tools/pandoc-docx-writer-golden-audit.php` inventories upstream `test/docx/golden/*.docx` packages when the local cache is available, records expected upstream writer references (`src/Text/Pandoc/Writers/Docx.hs`, `test/Tests/Writers/Docx.hs`, `data/docx`), and emits package part names plus SHA-256 hashes. With `--generate-supported-dir`, it uses the bounded `PortLibs\Pandoc\DocxWriter` to generate DOCX packages for the 38 pinned upstream writer golden native inputs it can read; with `--generated-dir` or `--generate-supported-dir`, it compares generated DOCX packages to the golden packages by stable package semantics: part sets, content types, relationships, XML document semantics, and binary payload digests. The current generated comparison evidence came from `tools/pandoc-docx-writer-golden-audit.php --json --docx-dir .upstream-cache/pandoc-current/test/docx --generate-supported-dir .port-libs/pandoc-docx-writer-golden/status-truth --require-generated-stable-matches 38`; it generated all 38 packages, compared all 38, and matched all 38 by stable package semantics. This is writer-golden stable package evidence only; no upstream Haskell/Cabal/Tasty runner result or full DOCX/OpenXML parity is claimed.

Optional upstream DOCX cache manifest: `lanes/pandoc/UPSTREAM_DOCX_CACHE_MANIFEST.json` is generated by `tools/pandoc-docx-cache-manifest.php` from Pandoc commit `612e143fbe6d735b612c4800d21e61b7d44e4dca`. It records metadata-only stems, byte counts, and SHA-256 hashes for 236 optional cache artifacts: 85 root `.docx` packages, 113 root `.native` expectations, and 38 golden `.docx` packages. This strengthens reproducible provenance and hydration review without checking in DOCX package bytes; the checked-in pinned DOCX package corpus gap remains open.

Optional upstream DOCX Haskell inventory: `lanes/pandoc/UPSTREAM_DOCX_HASKELL_INVENTORY.json` maps current upstream `test/Tests/Readers/Docx.hs` and `test/Tests/Writers/Docx.hs` static DOCX cases to local gates. It records that the local reader/native parser-acceptance gate covers 74 paired root fixtures without AST equality, keeps all 36 reader cases not semantically covered by that local gate checked as explicit rows, records that the writer-golden stable package gate generated/compared/matched 38/38 packages, and keeps all 7 direct Haskell writer assertions outside that local writer-golden gate checked as explicit rows. The direct writer mirror evidence covers no leading first-chapter section break, chapter section breaks using `writerTopLevelDivision=TopLevelChapter`, media content types emitted as concrete `/word/media/rId*.jpg` part overrides without a `/word/media/` directory override, reference-docx language preservation, non-`w`-prefix reference section properties, metadata language overriding reference styles, and `FirstParagraph after heading with footnote (#11573)`. This is an inventory boundary artifact only, not an upstream Haskell/Cabal/Tasty runner result.

DOCX runner evidence plan records `test:test-pandoc` as the upstream Tasty runner target, `test/test-pandoc.hs` as the entry file, `Tests.Readers.Docx.tests` and `Tests.Writers.Docx.tests` as the DOCX groups, the `test/docx/*.docx`, `test/docx/*.native`, and `test/docx/golden/*.docx` fixture closure, and the descriptor-only Cabal dry-run command `cabal v2-build --offline --project-dir=. --builddir=.port-libs/pandoc-runner/cabal-build/docx-targeted-run --dry-run --only-dependencies --enable-tests --disable-benchmarks test:test-pandoc test:test-pandoc-lua-engine`. `tools/pandoc-docx-upstream-runner-plan.php --json --upstream-root .upstream-cache/pandoc-current` emits a local readiness gate with machine-readable blocker codes for missing source, unverified pinned source, missing Cabal, missing GHC, and insufficient disk. `tools/pandoc-docx-upstream-runner-plan.php --json --upstream-root .upstream-cache/pandoc-current --write-selected-inventory .port-libs/pandoc-runner/artifacts/docx-targeted-run/selected-test-inventory.json` emits the guarded preflight and, when the checkout is hydrated, writes a static selected DOCX source/fixture inventory artifact with candidate Haskell source labels plus root/golden fixture names, stems, and byte sizes. `tools/pandoc-docx-upstream-runner-plan.php --json --upstream-root .upstream-cache/pandoc-current --validate-result-artifacts --artifact-root .port-libs/pandoc-runner/artifacts/docx-targeted-run --log-root .port-libs/pandoc-runner/logs` now validates the targeted-runner artifact gate: required transcripts, selected inventory, `result.json` fields, pinned commit/target/pattern, pass/fail/skip count consistency, exact Cabal command lines plus exit-code markers in transcripts, DOCX Tasty `--list-tests` labels, targeted DOCX Tasty run output, and SHA-256 binding across artifacts before any future result can be admitted. `tools/pandoc-docx-upstream-runner-plan.php --json --upstream-root .upstream-cache/pandoc-docx-runner-pinned --artifact-root .port-libs/pandoc-runner/artifacts/docx-targeted-run --log-root .port-libs/pandoc-runner/logs --write-result-artifact .port-libs/pandoc-runner/artifacts/docx-targeted-run/result.json --result-started-at-utc <started> --result-finished-at-utc <finished>` assembles `result.json` from captured transcripts only; it does not execute Cabal/Tasty. The manual `Pandoc DOCX parity` workflow path with `run_upstream_docx_tasty=true` hydrates the pinned runner checkout, captures the exact DOCX Tasty pattern commands, writes `result.json`, and requires the artifact gate to report `admissible-targeted-runner-result-artifacts-no-parity-claim`. Missing or placeholder targeted runner transcripts are a hard evidence gap, not pass evidence. This runner evidence plan is preflight/artifact-gating only unless that manual workflow is run: no upstream Haskell/Cabal DOCX runner result, Tasty `--list-tests` output, or full DOCX parity is recorded by this static status.

Focused DOCX parity CI now wires the non-executing runner evidence path directly: it lints the DOCX parity support files it tests, emits the selected-inventory preflight command, runs the result-artifact gate, and uses `tools/pandoc-docx-parity-audit.php --fail-on-regression` instead of a capped parser sample. Focused DOCX parity CI now validates `UPSTREAM_DOCX_HASKELL_INVENTORY.json`, validates `UPSTREAM_DOCX_HASKELL_FOCUSED_READER_EVIDENCE.json`, runs `DocxUpstreamHaskellInventoryTest.php` so the 36 reader uncovered cases and 7 writer direct HUnit uncovered cases cannot be silently dropped, runs `DocxUpstreamFocusedReaderEvidenceTest.php` so the focused reader denominator map cannot drift, and runs the bounded `DocxWriterTest.php` writer-core package test. CI invokes `tools/pandoc-docx-focused-reader-evidence.php --require-covered 36 --require-targeted-checks 32 --require-no-targeted-failures` when the upstream cache is hydrated; checked-in focused reader evidence is guarded as 36 covered / 0 remaining open with hydrated-cache targeted DOCX checks at 32 passed / 0 failed / 4 mapped-only. CI invokes the writer-golden audit with `--generate-supported-dir --require-generated-stable-matches 38` so hydrated cache jobs generate, compare, and gate all supported writer golden packages; local default-cache validation on 2026-06-30 still reported that audit as `skipped_missing_writer_golden_directory` because this worktree lacks `.upstream-cache`. The checked-in generated writer-golden evidence is guarded as 38 generated / 38 compared / 38 matched / 0 mismatched. Local validation also reported the optional cache and parser audit as `skipped_missing_upstream_docx_directory`, runner preflight as `blocked-missing-docx-upstream-source`, selected-inventory artifact writing as successful but blocked by missing upstream source, the result-artifact gate as `blocked-missing-targeted-runner-result-artifacts`, and `DocxWriterTest.php` as 433 assertions with 0 failures across all seven upstream direct writer HUnit mirrors. These outcomes are evidence-status checks only and do not change the upstream runner blocker counts above.

ODF/ODT is marked ship-ready: 91 local mapped ODF/ODT cases / 20 upstream ODF/ODT cases, with 0 critical ODF/ODT gaps.

| Format / path | Repo passing tests | Upstream tests |
| --- | ---: | --- |
| ODF / ODT / OpenDocument (`odt`) | 20 | 20 |
| Markdown / CommonMark / GFM | 3,867 | 1,096 |
| JSON / native AST | 87 | 252 |
| Typst input | 0 | 17 |
| PPTX / XLSX | 2 | 2 pinned PPTX/XLSX reader fixtures covered; bounded PPTX writer package output registered; full upstream PPTX writer corpus remains open |
| FB2 / FictionBook | 6 | 6 pinned FB2 reader golden fixture semantics covered |
| Wiki / roff / text markup readers | 1 | 20; Jira reader unit semantics covered, larger Jira fixture still open |
| OPML | 7 | 2 upstream old-suite tests; reader fixture canonical-native exact, writer fixture exact |
| HTML / XML / JATS / BITS DOM | 30 | 54 |
| DOCX / OpenXML | bounded synthetic/native slices only | 233 pinned artifacts; upstream DOCX runner missing; core `DocxWriter` package support present; writer golden stable package comparison generated/compared/matched 38/38 |
| EPUB / EPUB3 | 105 | 11 |
| CSV / TSV | 4 | 4 pinned CSV reader/parser evidence; RST csv-table integration remains tracked with RST |
| CSL / BibTeX / BibLaTeX / csljson / RIS / EndNote XML | 80 | 227 |
| LaTeX / TeX / math | 21 | 36 |
| DocBook | 28 | 17 |
| RTF | 4 | 27 |
| Shared ZIP / OPC package | 110 | 578 |
| PDF import (adjacent; not upstream Pandoc input) | 52 | N/A - Pandoc output/engine boundary only |
| Legacy DOC / CFB (adjacent; not a current upstream Pandoc input token) | 7 | N/A - not a current upstream Pandoc input token |

Latest package/core-format evidence: DOCX/OpenXML aFChunk alternative-format imports preserve safe versus unsafe external-target policy without fetching targets or exposing chunk bytes. Bundled ZIP/OPC and ODF/ODT package coverage preserves ODF manifest control-byte path preflight, ZIP package comment layout, ZIP manifest preflight, selected ZIP zero-byte handoff buckets, ODF package comment provenance, ODT encrypted image manifest provenance, ODT ZIP timestamp provenance, ODF package identity preflight, rich ODT reader package identity provenance, ODT layout-cache sidecar metadata-only review policy, and ODF package inventory role/byte-exposure byte buckets.

Recent checked gates are slice-scoped rather than a full-suite completion claim. The latest EPUB slices add `PortLibs\Pandoc\EpubWriter` for bounded EPUB3 package output with stored `mimetype`, `container.xml`, OPF DC/refinement/schema accessibility metadata, OPF manifest/spine with `toc="ncx"` and optional `page-progression-direction`, manifest `mathml`/`svg` properties derived from generated XHTML spine content, `nav.xhtml`, `toc.ncx`, default and configured `css`/`stylesheet` package resources linked from every generated XHTML surface, default `writerEpubTitlePage` XHTML generation with hidden EPUB3 landmarks and explicit opt-out, configured cover-image resource packaging through `cover-image`/`epub-cover-image` metadata or writer options with `text/cover.xhtml`, first-spine cover placement, cover landmarks, and OPF guide references, and one or more XHTML body spine documents rendered through the shared HTML writer; package media resources through `MediaBag`, rewrite generated XHTML image `src` targets to package-local media paths, include data URI images as package entries, mark configured cover images with OPF `cover-image`, split EPUB spine documents at `writerSplitLevel` with matching OPF manifest/spine entries and nav/NCX targets, rewrite cross-chapter internal fragment links, and render nested EPUB nav lists from heading levels. They also tighten `PortLibs\Pandoc\EpubReader` so normal XHTML `head` content is excluded from spine AST parsing, both EPUB3 nav and NCX TOC resources are recorded while EPUB3 nav entries are preferred over duplicate NCX fallback entries, and update the rich-package status registry so `epub`/`epub3` output are bounded native outputs while `epub2` remains unsupported. The latest HTML reader-dispatch slice adds `PortLibs\Pandoc\HtmlReader` as the registered `html` input implementation, stamps HTML provenance metadata (`sourceFormat`, `reader`, `readerScope`, delegate, and source hash), and preserves existing DOM/raw-HTML/native-div/table/link/list behavior through the current HTML-capable bridge. The latest OPML slices add `PortLibs\Pandoc\OpmlReader` for document title, owner, modified date, nested outlines, link outlines, Markdown notes, and bounded HTML inline markup inside outline text, including canonical native semantic parity for Pandoc's upstream `opml-reader.opml`/`opml-reader.native` fixture, plus `PortLibs\Pandoc\OpmlWriter` for nested outline output, escaped HTML heading text, Markdown `_note` bodies, default OPML metadata, and Pandoc-compatible OPML note markers for tight lists, definition lists, horizontal rules, empty Div attributes, raw TeX inline nodes, empty links, link titles with quotes, OPML note wrapping, display-math softbreaks, footnote definition wrapping, and nested empty-attribute Div fence width. Direct pinned `testsuite.native` to `writer.opml` comparison with `columns => 80` is byte-for-byte equal: `actual_bytes=14075`, `expected_bytes=14075`, `same=yes`, Levenshtein `0`. The latest CSV/TSV slice ports the pinned Pandoc `Text.Pandoc.CSV`/`Text.Pandoc.Readers.CSV` behavior for direct CSV tables and available parser options. The latest XLSX slice ports the pinned Pandoc `Text.Pandoc.Readers.Xlsx` behavior for workbook relationship discovery, workbook sheet order, shared strings, direct font bold/italic style indexes, dense sheet grids, first-row table heads, numeric cells rendered with decimal notation, empty cells, and trailing empty-row trimming. The latest PPTX slices port the pinned Pandoc `Text.Pandoc.Readers.Pptx` reader behavior for presentation relationship discovery, slide order, title placeholders, text boxes, Wingdings/explicit bullet grouping, simple tables, image references, and SmartArt hierarchy, and add `PortLibs\Pandoc\PptxWriter` for deterministic PresentationML package output with slide relationships, core/app/custom properties from Pandoc metadata, master/layout/theme parts, standalone image-only and content-with-caption slide grouping with figure captions, shape-free blank slides for empty or NBSP-only headings/bodies, table-of-contents slides with internal slide links, slide-level-aware heading breaks, bullet and ordered list paragraphs preserving inline runs plus Pandoc style/delimiter autonumbering, ordered-list margins, list continuation paragraphs with explicit `buNone`, DrawingML tables, hyperlink relationships, local image media resources, Courier code inline/block runs, small-caps and adjacent-text inline run properties, background images from heading attributes, horizontal-rule slide splitting, slide-level-0 first-heading title promotion, speaker-note notesSlides/notesMaster packaging from upstream-style notes Divs and metadata notes, public endnote slide generation from inline notes, raw OpenXML inline/block passthrough, and upstream-style omission of non-OpenXML raw inline plus empty-alt unresolved image content. The latest Jira slice ports the pinned Pandoc `Text.Pandoc.Readers.Jira` unit-module semantics for paragraphs, headings, lists, block quotes, tables, panels, core inline styles, links, images, and entities; the larger `jira-reader.jira/native` fixture remains non-exact (`same=no`, `blocks=51`, `native_bytes=14556`, `expected_bytes=20887`). The latest wiki registry slice tracks wiki input/output direction buckets, `.dokuwiki` and `.wiki` extension inference, Jira's partial input implementation, unsupported wiki reader/writer gaps, and explicit no-direct-parity booleans without invoking Pandoc or wiki converters. The latest FB2 slice maps FictionBook XML sections, titles, epigraphs, poems, metadata, notes, and inline formatting for all six pinned upstream FB2 golden fixture categories; direct upstream-file smoke parses all six fixtures, with compact local native output not byte-identical to upstream pretty native output. The latest PDF reader slice preserves background fills on inferred empty table cells by keeping full cell boxes separate from text boxes, without adding invoice-specific fixture strings. Verification: EPUB writer/reader/registry/converter/rich-package focused gate (`5` files, `568` assertions, `0` failures), wiki registry focused gate (`1` file, `238` assertions, `0` failures), CSV/TSV focused gate (`1` file, `419` assertions, `0` failures), XLSX/registry focused gate (`4` files, `367` assertions, `0` failures), latest PPTX/registry focused gate (`4` files, `907` assertions, `0` failures), Jira/registry focused gate (`3` files, `279` assertions, `0` failures), FB2/registry focused gate (`3` files, `262` assertions, `0` failures), broad reader/writer smoke including FB2/Jira/PPTX/XLSX (`27` files, `18,566` assertions, `0` failures), PDF reader/converter focused gate (`2` files, `1,556` assertions, `0` failures), PDF/markerPDF guard (`2` files, `3,071` assertions, `0` failures), local problematic-PDF smoke `reconstruction=geometry tables=10 geometry=10 cells=104 rects=896 gray_attrs=38 collapsed_guard=clear spaced_guard=hit`, and exact-string guard for the problematic PDF path/content terms (`0` hits).
