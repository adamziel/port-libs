# rclone Upstream Inventory

## 2026-05-25 OneDrive Upload Bracketing Slice

- Upstream evidence: reuses the existing focused static read of `lib/oauthutil/renew.go` and `backend/onedrive/onedrive.go` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`, specifically the OneDrive upload call sites that wrap upload work with `tokenRenewer.Start()` and deferred `Stop()`.
- Native behavior added: deterministic upload bracketing via `OneDriveTokenRenewer::duringUpload()`, including Start before upload work, Stop after successful upload work, Stop after thrown upload errors, Stop when shutdown happens inside bracketed work, and expiry refresh eligibility while bracketed work is active.
- Focused evidence: `php tools/run-tests.php lanes/rclone/tests/OneDriveTokenRenewerTest.php` passed with 9 behavior tests, 61 assertions, and 0 failures.
- Blocker: no rclone-local PHP blocker. Live OneDrive provider/OAuth tests remain excluded for isolated lane work.
- Dependency closure: no new support component is needed; this slice reuses the existing bounded native OneDrive token-renewer component and does not require OAuth, timers, credentials, or live provider access.
- Next task: map another credential-free OneDrive provider lifecycle cluster, such as shutdown/feature cleanup or a narrow no-versions cleanup behavior.

- Upstream: `https://github.com/rclone/rclone`
- Commit: `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`
- Cache: `.upstream-cache/rclone`
- Method: shallow clone with `--filter=blob:none --depth=1`; denominator counted from `git ls-tree -r --name-only HEAD` plus targeted reads of `Makefile`, `COPYING`, `fs/filter/glob.go`, `fs/filter/filter.go`, `fs/filter/*_test.go`, `fs/hash/hash_test.go`, `fs/open_options.go`, `fs/object/object_test.go`, `fs/operations/check.go`, `fs/operations/check_test.go`, `fs/operations/reopen.go`, `fs/operations/reopen_test.go`, `fs/operations/operations.go`, `fs/operations/operations_test.go`, `fs/operations/copy_test.go`, `fs/sync/sync.go`, `fs/sync/sync_test.go`, `fstest/fstests/fstests.go`, `fs/accounting/stats.go`, `fs/deletemode.go`, `docs/content/docs.md`, `lib/readers/readfill.go`, `lib/readers/readfill_test.go`, `lib/readers/error.go`, `lib/readers/error_test.go`, `lib/readers/repeatable.go`, `lib/readers/repeatable_test.go`, `lib/readers/fakeseeker.go`, `lib/readers/fakeseeker_test.go`, `lib/readers/noseeker.go`, `lib/readers/noseeker_test.go`, `lib/readers/pattern_reader.go`, `lib/readers/pattern_reader_test.go`, `lib/readers/limited.go`, `lib/readers/noclose.go`, `lib/readers/noclose_test.go`, `lib/readers/gzip.go`, `lib/readers/gzip_test.go`, `lib/readers/context.go`, `lib/readers/context_test.go`, `lib/readers/counting_reader.go`, and selected command files.
- Focused listing reads: `cmd/lsjson/lsjson.go`, `fs/operations/lsjson.go`, and `fs/operations/lsjson_test.go`, including the `StatJSON` branch that compares directory entries with `strings.EqualFold` when the provider advertises `CaseInsensitive`.
- Focused checksum/reader reads: `cmd/checksum/checksum.go`, `cmd/hashsum/hashsum.go`, `fs/operations/check.go`, `fs/operations/check_test.go`, `lib/readers/readfill.go`, `lib/readers/readfill_test.go`, `lib/readers/error.go`, `lib/readers/error_test.go`, `lib/readers/repeatable.go`, `lib/readers/repeatable_test.go`, `lib/readers/fakeseeker.go`, `lib/readers/fakeseeker_test.go`, `lib/readers/noseeker.go`, `lib/readers/noseeker_test.go`, `lib/readers/pattern_reader.go`, `lib/readers/pattern_reader_test.go`, `lib/readers/limited.go`, `lib/readers/noclose.go`, `lib/readers/noclose_test.go`, `lib/readers/gzip.go`, `lib/readers/gzip_test.go`, `lib/readers/context.go`, `lib/readers/context_test.go`, and `lib/readers/counting_reader.go`, including `TestCheckDownload`, `TestCheckEqualReaders`, `TestCheckSumDownload`, `TestReadFill`, `TestErrorReader`, `TestRepeatableReader`, `TestFakeSeeker`, `TestFakeSeekerError`, `TestNoSeeker`, `TestPatternReader`, `TestPatternReaderSeek`, `TestNoCloser`, `TestGzipReader`, `TestContextReader`, the `NewRepeatableReaderSized`, `NewRepeatableLimitReader`, `NewRepeatableReaderBuffer`, and `NewRepeatableLimitReaderBuffer` source constructors, the `NewLimitedReadCloser`/`LimitedReadCloser.Close` branches, the `NoCloser` close-hiding wrapper, the `NewGzipReader` close wrapper, the `NewContextReader` cancellation wrapper, and the `NewCountingReader` byte counter source. No dedicated upstream `counting_reader_test.go` exists at this commit.
- Focused reopen reads: `fs/operations/reopen.go` and `fs/operations/reopen_test.go`, including retry-at-offset behavior, range and seek options, unknown-size range streams, `SeekEnd` rejection for unknown-sized objects, `ReadAt`, close-after-error state, and delayed accounting.
- Focused low-level retry reads: `fs/fserrors/error.go` and the `fserrors.IsNoLowLevelRetryError` branch in `fs/operations/reopen.go`, where no-low-level-retry read errors are made sticky and are not reopened.
- Focused sync/delete/backup-dir reads: `fs/deletemode.go`, `fs/sync/sync.go`, `fs/sync/sync_test.go`, `fs/operations/operations.go`, `fs/operations/operations_test.go`, `fs/operations/copy_test.go`, `fs/accounting/stats.go`, `cmd/delete/delete.go`, `cmd/purge/purge.go`, and `cmd/cleanup/cleanup.go`, including destination-only delete planning, delete mode constants, copy-not-delete behavior, filtered destination behavior, `--delete-excluded`, `--max-delete`, `--max-delete-size`, `BackupDir`, `MoveBackupDir`, `SuffixName`, suffix-only backups, suffix-keep-extension naming, and destructive command boundaries.
- Focused compare/copy-dest reads: targeted `fs/sync/sync.go`, `fs/sync/sync_test.go`, `fs/operations/operations.go`, and `fs/operations/copy_test.go` reads covering `CompareOrCopyDest`, `compareDest`, `copyDest`, `GetCompareDest`, `GetCopyDest`, `BackupDir` validation, `TestSyncCompareDest`, `TestSyncMultipleCompareDest`, `TestSyncCopyDest`, `TestCopyFileCompareDest`, and `TestCopyFileCopyDest`.
- Focused transfer-decision reads: targeted `fs/sync/sync.go`, `fs/sync/sync_test.go`, `fs/operations/operations.go`, `fs/operations/operations_test.go`, `fs/march/march.go`, `fs/fs.go`, and `docs/content/docs.md` reads covering `--ignore-existing`, `--immutable`, `--no-check-dest`, `--ignore-times`, `--update`, `--no-update-modtime`, `NeedTransfer`, `Equal`, `ErrorImmutableModified`, destination listing bypasses, `TestSyncIgnoreExisting`, `TestMoveWithIgnoreExisting`, `TestMoveFileWithIgnoreExisting`, `TestSyncImmutable`, `TestSyncIgnoreTimes`, `TestSyncAfterChangingModtimeOnly`, `TestSyncAfterChangingModtimeOnlyWithNoUpdateModTime`, and `TestSyncWithUpdateOlder`.
- Focused refresh-times reads: targeted `fs/operations/operations.go`, `fs/sync/sync.go`, `fs/config.go`, and `docs/content/docs.md` reads covering `--refresh-times`, the `hash.None && !ci.RefreshTimes` branch in `Equal`, destination timestamp update with absent hashes, common-hash mismatch transfer behavior, and the documented no-effect boundary for size-only/checksum sync modes.
- Focused directory modtime/metadata reads: targeted `fs/operations/operations.go`, `fs/operations/operations_test.go`, `fs/sync/sync.go`, `fs/sync/sync_test.go`, `fs/operations/lsjson.go`, and `fs/operations/lsjson_test.go` reads covering `MkdirModTime`, `SetDirModTime`, `DirsEqual`, `--no-update-dir-modtime`, directory modtime replacement, empty directory handling, and ListJSON directory modtime/metadata output.
- Focused delayed directory modtime reads: targeted `fs/sync/sync.go` and `fs/sync/sync_test.go` reads covering `setDelayedDirModTimes`, `markDirModified`, `markDirModifiedObject`, delayed queueing from `copyDirMetadata`, `TestNothingToTransferWithEmptyDirs`, and `TestNothingToTransferWithoutEmptyDirs`, including deepest-first updates after child object writes, parent propagation, unchanged-directory suppression, empty-directory skipping, `--create-empty-src-dirs`, and `--no-update-dir-modtime`.
- Focused IgnoreCaseSync reads: targeted `docs/content/docs.md`, `fs/config.go`, `fs/march/march.go`, `fs/sync/sync.go`, and `fs/sync/sync_test.go` reads covering `TestSyncIgnoreCase`, the `ci.IgnoreCaseSync` lower-case march transform on otherwise case-sensitive source/destination pairs, first duplicate match-key retention, no-rename equal object matching, matched destination path updates, and destination-only delete suppression for differently-cased matches.
- Focused FixCase reads: targeted `fs/sync/sync.go`, `fs/sync/sync_test.go`, `fs/march/march.go`, and `fs/operations/operations.go` reads covering `TestFixCase`, the case-insensitive destination march key transform, file fix-case rename after equality checks, directory fix-case rename before recursion, immutable suppression, and `MoveCaseInsensitive` / `DirMoveCaseInsensitive` two-step rename boundaries.
- Focused TrackRenames reads: targeted `docs/content/docs.md`, `fs/config.go`, `fs/sync/sync.go`, and `fs/sync/sync_test.go` reads covering `--track-renames`, `--track-renames-strategy`, destination server-side move/copy gating, hash commonality gating, delete-after coercion, `parseTrackRenamesStrategy`, `renameID`, `makeRenameMap`, `popRenameMap`, `tryRename`, `TestSyncWithTrackRenames`, `TestParseRenamesStrategyModtime`, `TestRenamesStrategyModtime`, `TestSyncWithTrackRenamesStrategyModtime`, and `TestSyncWithTrackRenamesStrategyLeaf`.
- Focused provider move/copy reads: targeted `fs/operations/copy.go`, `fs/operations/operations.go`, `fs/operations/operations_test.go`, and `fs/sync/sync.go` reads covering `CanServerSideMove`, `Move` fallback from `ErrorCantMove` to copy plus delete, copy-only server-side move simulation, `MoveBackupDir`, `DirMove` direct provider calls, `ErrorCantDirMove` / `ErrorDirExists` fallback to object moves, fatal `DirMove` errors, and `tryRename` fallback to upload/delete-after when `operations.Move` fails.
- Focused single-file MoveFile/CopyFile reads: targeted `fs/operations/operations.go`, `fs/operations/operations_test.go`, `fs/operations/copy.go`, and `fs/operations/copy_test.go` reads covering `moveOrCopyFile`, `CopyFile`, `MoveFile`, `TestCopyFile`, `TestMoveFile`, `TestMoveFileWithIgnoreExisting`, `TestCaseInsensitiveMoveFile`, `TestCaseInsensitiveMoveFileDryRun`, `TestCopyFileBackupDir`, `TestMoveFileBackupDir`, `needsMoveCaseInsensitive`, `MoveCaseInsensitive`, `copy.checkPartial`, `copy.removeFailedPartialCopy`, partial copy verification cleanup, and the same-object no-op branch.
- Focused `RemoveExisting` / provider-copy reads: targeted `fs/operations/operations.go`, `fs/operations/operations_test.go`, and backend callers in Dropbox, OneDrive, Yandex, and SugarSync. This covers missing-object no-op cleanup, direct `Move` requirement for existing destinations, temporary rename before overwrite, cleanup deletion after success, restore on caller failure, long leaf-name truncation before appending the temporary suffix, cleanup delete errors, provider server-side copy callers that use the helper before replacing existing objects, SugarSync same-lowercase source/destination guards before temporary destination creation, OneDrive same-lowercase source/destination guards after `RemoveExisting`, and createObject destination handles that are not visible provider objects until the copy succeeds.
- Focused provider server-side copy result reads: targeted `backend/dropbox/dropbox.go`, `backend/onedrive/onedrive.go`, `backend/onedrive/metadata.go`, `backend/yandex/yandex.go`, `backend/yandex/api/types.go`, `backend/sugarsync/sugarsync.go`, and `fs/fs.go`. This covers Dropbox `RelocationResult` FileMetadata-only handling, content hash and client-modified metadata refresh, OneDrive async copy `Location` handling, `errAsyncJobAccessDenied` to `ErrorCantCopy`, set-source-modtime and add-only permission metadata refresh, Yandex async copy failure wrapping plus `rclone_modified` custom-property/MD5 reads, and SugarSync HTML error extraction plus Location/read-after-copy metadata.
- Focused OneDrive QuickXor reads: targeted `backend/onedrive/quickxorhash/quickxorhash.go`, `backend/onedrive/quickxorhash/quickxorhash_test.go`, `backend/onedrive/onedrive.go`, and `fs/hash/hash.go`. This covers the circular XOR/length checksum, upstream QuickXor base64 vectors, rclone's hex encoding of hash bytes, OneDrive `QuickXorHash` base64 decoding in object metadata, and server-side copy metadata refresh for OneDrive Business-style hashes.
- Focused dedupe reads: targeted `fs/operations/dedupe.go`, `fs/operations/dedupe_test.go`, `cmd/dedupe/dedupe.go`, and `docs/content/commands/rclone_dedupe.md`. This covers `DeduplicateMode` strings and unknown-mode errors, `TestDeduplicateInteractive`, `TestDeduplicateNewestByHash`, `TestMergeDirs`, by-hash hash support requirements, grouping duplicate content by provider `Hashes().GetOne()`, non-interactive `first`/`newest`/`oldest`/`largest`/`smallest`/`skip`/`list` modes, interactive `skip`/`keep`/`rename`/`quit` decisions, by-name duplicate cleanup before prompts or skip/keep/rename modes, size-only duplicate identity, repeated provider-ID suppression, provider `MergeDirs` behavior, duplicate-directory largest-first merge ordering, provider-ID/ParentID duplicate directory discovery, list-mode duplicate-directory reporting, provider-ID non-list duplicate-directory merge child rewiring, and the remaining boundary that full live-provider duplicate-directory merge validation is beyond the native in-memory provider.
- Focused fstest object contract reads: targeted `fstest/fstests/fstests.go`, `fs/object/object_test.go`, and `fs/open_options.go`. This covers `ObjectOpenSeek`, `ObjectOpenRange`, `ObjectUpdate`, `FsPutStream`, and `FsUploadUnknownSize` boundaries: seek reads from an offset, inclusive ranges cap at object length, negative range starts fetch the final N bytes, object update ignores the source remote name while preserving the existing remote path, and unknown-size source uploads/updates must not panic and report the stored byte length after success.
- Focused fstest list/root/purge reads: targeted `fstest/fstests/fstests.go`, `fs/operations/operations.go`, and `fs/operations/operations_test.go`. This covers `walk.GetAll` direct-depth listings, `FsListDirFile2`, `FsListDirRoot`, `FsListSubdir`, `FsListLevel2`, `FsPurge`, `FsPurgeRoot`, `FsListRootedSubdir`, `FsRmdirNotFound`, `FsRmdirEmpty`, `FsRmdirFull`, and `operations.Purge` fallback behavior without running live provider remotes.
- Focused PublicLink reads: targeted `fstest/fstests/fstests.go` and `fs/operations/operations.go`. This covers missing-path errors, non-empty file links, distinct links for different files, repeated share calls, directory links, and sharing the root of a rooted subremote.
- Focused SetTier/GetTier reads: targeted `fstest/fstests/fstests.go`, `fs/types.go`, `fs/operations/operations.go`, `cmd/settier/settier.go`, `cmd/lsf/lsf.go`, and `fs/operations/lsjson.go`. This covers the optional object `SetTierer`/`GetTierer` interfaces, `operations.SetTier` over listed objects, `operations.SetTierFile`, the command-level unsupported-remote boundary, `lsf` format `T`, and `lsjson` `Tier` output when the provider advertises `GetTier`.
- Focused chunksize reads: targeted `fs/chunksize/chunksize.go` and `fs/chunksize/chunksize_test.go`. This covers streaming upload fallback to the configured default chunk size, default-size retention when a file fits the provider part limit, MiB rounding when a larger chunk is required, the exact-boundary and one-byte-over boundaries, and the upstream forum regression case for large uploads.
- Focused cleanup command reads: targeted `cmd/cleanup/cleanup.go`, `fs/operations/operations.go` `CleanUp`, `fs/features.go` `CleanUpper` feature wiring and masking, `fs/operations/rc.go`, `fs/operations/rc_test.go` `TestRcCleanup`, plus a static backend scan of exact `CleanUp(ctx)` implementations. This covers optional provider cleanup support checks before dry-run, supported dry-run suppression before provider calls, provider cleanup error propagation, and the fact that cleanup clears backend trash or old versions outside the ordinary visible listing.
- Focused chunkedreader reads: targeted `fs/chunkedreader/chunkedreader.go`, `fs/chunkedreader/sequential.go`, `fs/chunkedreader/parallel.go`, `fs/chunkedreader/chunkedreader_test.go`, `fs/chunkedreader/sequential_test.go`, and `fs/chunkedreader/parallel_test.go`. This covers factory boundaries, sequential lazy `RangeSeek`, one-shot custom range lengths, initial chunk reset, chunk doubling with max caps, disabled whole-object chunking, invalid seek/closed-reader errors, parallel fixed chunk-size rounding to `multipart.BufferSize`, configured stream prefetch, seek reuse inside prefetched stream ranges, lazy restart outside prefetched ranges, `RangeSeek` length ignoring in parallel mode, known-size requirement, closed-reader errors, prefetch read-error cleanup, close-all first-error reporting, and seek-discarded stream close-error suppression.
- Focused chunkedreader factory reads: `fs/chunkedreader/chunkedreader.go` and `fs/chunkedreader/chunkedreader_test.go` cover the upstream `New` table: nonpositive chunks disable chunking, max chunks below the initial value clamp up to the initial value, negative streams become zero, zero/one stream selects sequential, known-size objects with multiple streams select parallel, and unknown-size objects select sequential even when multiple streams are requested.
- Focused metadata-set reads: targeted `fstest/fstests/fstests.go` around `FsCopy/Metadata`, `FsMove/Metadata`, `ObjectSetMetadata`, `FsMkdirMetadata`, and `FsDirectory/WriteDirMetadata`. This covers `--metadata-set` replacement metadata on copied/moved destination objects, system `mtime` propagation into visible modtime, source-object preservation on copy, object `SetMetadata`, directory metadata replacement, and `MkdirMetadata` updating an existing directory.
- Focused fs/list helper reads: targeted `fs/list/helpers.go` and `fs/list/helpers_test.go`. This covers `NewHelper`, `Helper.Add` ignoring nil entries, the 100-entry callback threshold, `Flush`, and `WithListP` preserving entries returned before a provider error.
- Focused OneDrive ListP error propagation reads: targeted `backend/onedrive/onedrive.go` `_listAll`, `ListP`, `List`, and `ListR` shared-folder `listFolder`; `fs/list/helpers.go` `Add`, `Flush`, and `WithListP`; and `fs/walk/walk.go` direct ListR callback forwarding. This covers 100-entry helper batches delivered before later provider errors, suppression of pending final batches after provider/callback errors, shared-folder fallback discarding child `ListP` partials when `f.List` returns an error, and caller callback errors stopping recursive ListR immediately.
- Focused fs/list filter/sort reads: targeted `fs/list/list.go` and `fs/list/list_test.go`. This covers `filterAndSortDir`, `filterDir`, `TestFilterAndSortIncludeAll`, `TestFilterAndSortCheckDir`, `TestFilterAndSortCheckDirRoot`, and `TestFilterAndSortUnknown`: includeAll bypasses, object/directory include callbacks, direct directory membership pruning, root slash-only bucket entries, unknown entry errors, and stable duplicate-remote sorting.
- Focused fs/list sorter reads: targeted `fs/list/sorter.go` and `fs/list/sorter_test.go`. This covers `NewSorter`, nil key-function identity ordering, `Add`, `Send`, `CleanUp`, caller key functions, cutoff-triggered external-sort mode, 100-entry callback batching while in cutoff mode, and `TestSorterExtTempFileError`'s upstream `sorter:` error boundary when temp setup fails.
- Focused fs/list DirSortedFn reads: targeted `fs/list/list.go`, `fs/operations/listdirsorted_test.go`, and `fs/filter/filter.go`. This covers `DirSortedFn`, its `listP` fallback/callback shape, raw listed-entry accounting before filtering, `ListContainsExcludeFile` page short-circuiting when `includeAll` is false, reuse of `filterDir`, global sorter delivery, caller key functions, cleanup after send/error, and the shared `testListDirSorted` includeAll/filter/exclude-file cases exercised by upstream `TestListDirSortedFn`.
- Focused fs/list DirSorted reads: targeted `fs/list/list.go`, `fs/operations/listdirsorted_test.go`, and `fs/filter/filter.go`. This covers the object-returning `DirSorted` provider `List` path, one provider `List` call per directory, raw listed-entry counting before filtering, whole-directory exclude-if-present short-circuiting, reuse of `filterAndSortDir`, provider `List` error propagation, unknown entry errors, directory-filter errors, and the shared `testListDirSorted` matrix exercised by upstream `TestListDirSorted`.
- Focused fs/walk reads: targeted `fs/walk/walk.go` and `fs/walk/walk_test.go`. This covers `Walk` over `DirSorted`, parent-before-child callback order, maxLevel recursion, `ErrorSkipDir`, provider/list errors passed to callbacks for masking or propagation, exclude-if-present child directory pruning, `ListR` fallback through `Walk`, `ListObjects`/`ListDirs`/`ListAll` filtering, delayed listing-error return, and callback-error propagation.
- Focused direct fs/walk ListR/dirMap reads: targeted `fs/walk/walk.go` and `fs/walk/walk_test.go` around `listR`, `dirMap`, `TestListR`, `TestDirMapAdd`, `TestDirMapAddEntries`, and `TestDirMapSendEntries`. This covers provider batch order, ListObjects/ListDirs/ListAll filtering, include callback filtering, bucket-based synthesized parent directories, root-boundary suppression, explicit-directory sent-state overrides, sorted missing-parent sends, the upstream 100-entry helper threshold, provider errors before synthetic flush, and callback-error propagation.

## Counted Test-Related Inventory

- Total upstream repository files at the commit: 2,553
- Go test files: 327
- Backend Go test files: 124
- `fs/` Go test files: 91
- `cmd/` Go test files: 47
- `lib/` Go test files: 43
- `vfs/` Go test files: 19
- `cmdtest/` Go test files: 2
- `fstest` paths: 61
- `fstest` Go test files: 1
- Focused fstest object contract inventory: 3 targeted file reads (`fstest/fstests/fstests.go`, `fs/object/object_test.go`, `fs/open_options.go`), 4 `ObjectOpenRange` table runs, 1 `ObjectOpenSeek` run, 1 `ObjectUpdate` test function, 1 `FsPutStream` test function with 2 size cases, 2 `FsUploadUnknownSize` subtests, 4 `RangeOption.Decode`/`FixRangeOption` branches, and 4 native boundary cases (range/seek reads, update path preservation, unknown-size put stream, unknown-size update).
- Focused fstest list/root/purge inventory: 3 targeted file reads (`fstest/fstests/fstests.go`, `fs/operations/operations.go`, `fs/operations/operations_test.go`), 9 focused upstream test functions/subtest families (`FsListDirFile2`, `FsListDirRoot`, `FsListSubdir`, `FsListLevel2`, `FsPurge`, `FsPurgeRoot`, `FsListRootedSubdir`, `FsRmdirNotFound`/`FsRmdirEmpty`/`FsRmdirFull`, and operations purge/rmdir tests), 4 native list/purge boundary cases, and 3 rooted provider boundary cases.
- Focused PublicLink inventory: 2 targeted file reads (`fstest/fstests/fstests.go`, `fs/operations/operations.go`), 1 upstream `PublicLink` subtest family, 1 implementation wrapper, 3 native provider boundary cases, and 1 rooted provider boundary case.
- Focused metadata-set inventory: 1 targeted upstream file (`fstest/fstests/fstests.go`) with 4 focused upstream subtest families (`FsCopy/Metadata`, `FsMove/Metadata`, `ObjectSetMetadata`, and directory `FsMkdirMetadata`/`WriteDirMetadata`), plus 4 native boundary cases for object metadata replacement, directory metadata replacement, server-side copy metadata-set, server-side move metadata-set, and a WordPress handoff example.
- Focused SetTier/GetTier inventory: 6 targeted upstream reads (`fstest/fstests/fstests.go`, `fs/types.go`, `fs/operations/operations.go`, `cmd/settier/settier.go`, `cmd/lsf/lsf.go`, `fs/operations/lsjson.go`), 3 upstream behavior entry points (`fstest` `SetTier`, `operations.SetTier`, and `operations.SetTierFile`), 1 command file, 2 list-output branches (`lsf` `T` and `lsjson` `Tier`), and 4 native boundary cases for object tier round trips, unsupported SetTier/GetTier feature gates, filtered multi-object tier updates, and WordPress archive-tier selection.
- Focused chunksize inventory: 2 targeted upstream reads (`fs/chunksize/chunksize.go` and `fs/chunksize/chunksize_test.go`), 1 upstream Go test function, 9 named upstream table cases, and 10 native boundary cases including a WordPress archive upload part-range scenario.
- Focused chunkedreader inventory: 6 targeted upstream reads (`fs/chunkedreader/chunkedreader.go`, `sequential.go`, `parallel.go`, `chunkedreader_test.go`, `sequential_test.go`, and `parallel_test.go`), 6 focused upstream Go test functions (`TestChunkedReader`, `TestSequential`, `TestSequentialErrorAfterClose`, `TestParallel`, `TestParallelErrorAfterClose`, and `TestParallelLarge`), 3 upstream mock seek modes, 10 shared chunk-size inputs, 12 native boundary tests, and 2 WordPress restore scenarios. The cleanup slice adds 3 native parallel boundaries for failed prefetch cleanup, close-all first-error reporting, and ignored close errors while seeking past abandoned streams. The factory slice adds the 6 upstream `TestChunkedReader` selection cases as 5 native behavior tests plus 1 WordPress restore strategy example.
- Focused fs/list helper inventory: 2 targeted upstream reads (`fs/list/helpers.go` and `fs/list/helpers_test.go`), 5 upstream Go test functions (`TestNewListRHelper`, `TestListRHelperAdd`, `TestListRHelperSend`, `TestListRHelperFlush`, and `TestListWithListP`), 2 `WithListP` subtests, 6 native boundary tests, and 1 WordPress batched-manifest scenario.
- Focused fs/list filter/sort inventory: 2 targeted upstream reads (`fs/list/list.go` and `fs/list/list_test.go`), 4 upstream Go test functions (`TestFilterAndSortIncludeAll`, `TestFilterAndSortCheckDir`, `TestFilterAndSortCheckDirRoot`, and `TestFilterAndSortUnknown`), 5 native boundary tests, and 1 WordPress direct backup-manifest scenario.
- Focused fs/list sorter inventory: 2 targeted upstream reads (`fs/list/sorter.go` and `fs/list/sorter_test.go`), 5 upstream Go test functions (`TestSorter`, `TestSorterIdentity`, `TestSorterKeyFn`, `TestSorterExt`, and `TestSorterExtTempFileError`), 6 `TestSorterExt` table cases, 6 native boundary tests, and 1 WordPress restore-manifest ordering scenario.
- Focused fs/list DirSortedFn inventory: 3 targeted upstream reads (`fs/list/list.go`, `fs/operations/listdirsorted_test.go`, and `fs/filter/filter.go`), 1 upstream test function (`TestListDirSortedFn`) using the shared 7-call `testListDirSorted` matrix, 5 native boundary tests, and 1 WordPress paged restore-manifest scenario.
- Focused fs/list DirSorted inventory: 3 targeted upstream reads (`fs/list/list.go`, `fs/operations/listdirsorted_test.go`, and `fs/filter/filter.go`), 1 upstream test function (`TestListDirSorted`) using the shared 7-call `testListDirSorted` matrix, 4 native boundary tests, and 1 WordPress provider-List restore-manifest scenario.
- Focused fs/walk inventory: 2 targeted upstream reads (`fs/walk/walk.go` and `fs/walk/walk_test.go`), 18 walk-focused upstream test functions across `Walk`, `WalkR`, `walkRDirTree`, `ListType`, `ListR`, and `dirMap`, 18 `walkRDirTree` table cases, 1 `TestListR` matrix, 6 native walk boundary tests, 6 native ListR fallback/list-type boundaries, and 1 WordPress recursive restore-manifest scenario.
- Focused direct fs/walk ListR/dirMap inventory: same 2 targeted upstream reads, 1 upstream `TestListR` matrix with 12 direct ListR cases, 3 dirMap-focused upstream test functions, 7 native direct ListR/dirMap boundary cases, and 1 WordPress bucket-manifest scenario.
- Focused direct fs/walk `walkRDirTree` / `NewDirTree` inventory: same 2 targeted upstream reads plus `fs/dirtree/dirtree.go`; mapped representative upstream `TestWalkRDirTree` and `TestWalkRDirTreeExclude` cases for arbitrary recursive order normalization, parent synthesis, non-root roots, maxLevel truncation, ordinary filtered-object parent preservation, exclude-file post-list prune, includeAll prune bypass, and provider/entry error propagation. Native PHP now has 5 walkRDirTree boundary tests and 1 WordPress direct restore-tree scenario.
- Focused direct fs/walk `walkR` / `NewDirTree` selection inventory: same targeted `fs/walk/walk.go` and `fs/walk/walk_test.go` reads; mapped sorted `walkR` directory callback order over the direct ListR DirTree, `ErrorSkipDir` descendant-prefix suppression with same-prefix sibling preservation, and the `NewDirTree` branch that falls back to provider `List` at `maxLevel == 1`. Native PHP adds 3 focused boundary tests and 1 WordPress direct walkR restore-manifest scenario.
- Focused fs/walk `GetAll` inventory: targeted `fs/walk/walk.go` plus the upstream `walk.GetAll` callsites in `fstest/fstests/fstests.go`, `fstest/fstest.go`, and `fs/operations/dedupe_test.go`; mapped `ListAll` collection into separate object and directory arrays, direct unbounded ListR selection, Walk fallback for bounded `maxLevel`, delayed fallback listing errors, direct provider error propagation, and 1 WordPress direct GetAll restore-catalog scenario.
- Focused fs/walk no-traverse/files-from inventory: targeted the `NewDirTree` branch in `fs/walk/walk.go` plus `fs/filter.Filter.MakeListR` and `TestNewFilterMakeListR` in `fs/filter/filter.go` / `filter_test.go`; mapped the `ci.NoTraverse && fi.HaveFilesFrom()` selection before provider ListR, explicit `NewObject` lookups from files-from remotes, missing-object skipping, lookup/type error propagation, no provider List/ListR traversal, and 1 WordPress selected-restore manifest scenario.
- Focused fs/march no-traverse inventory: targeted `fs/march/march.go` `processJob` plus upstream `TestCopyNoTraverse`, `TestCopyNoTraverseDeadlock`, and `TestSyncNoTraverse` in `fs/sync/sync_test.go`; mapped per-source-object destination `NewObject` probes, directory source-only recursion boundaries, no target listing, `--no-check-dest` probe bypass, source-order match/miss accounting, and 1 WordPress filtered no-traverse copy scenario.
- Focused fs/march `matchListings` duplicate inventory: targeted `fs/march/march.go` `matchListings` plus the no-traverse/track-renames option boundary in `fs/sync/sync.go`; mapped the source and destination duplicate-entry branches, the out-of-order guards, first-listed source object retention, duplicate source object suppression in changed-path and copy planning, and 1 WordPress duplicate WXR provider-listing scenario.
- Filter Go test files: 2
- Hash Go test files: 1
- Testdata paths: 836
- Test directory paths: 35
- Integration-related paths counted by path: 3
- Script paths and shell helpers: 43
- Focused `lsjson` static inventory: 2 upstream Go test functions (`TestListJSON`, `TestStatJSON`), 24 named/subtest table cases counted from `fs/operations/lsjson_test.go`, and 11 command flags/options declared in `cmd/lsjson/lsjson.go`.
- Focused case-insensitive `StatJSON` rule: 1 implementation branch in `fs/operations/lsjson.go` maps provider `Features().CaseInsensitive` to case-folded directory-entry matching.
- Focused checksum/check static inventory: 9 upstream Go test functions in `fs/operations/check_test.go`, including 9 `ParseSumFile` line samples over LF/CRLF, 14 `CheckSum` named runs across normal and download modes, 7 `CheckDownload` table runs, 12 `CheckEqualReaders` byte/error fixtures, 10 `ApplyTransforms` path-normalization scenarios, and checksum/hashsum command boundaries in `cmd/checksum/checksum.go` and `cmd/hashsum/hashsum.go`.
- Focused `lib/readers` static inventory: 20 reader helper paths, 9 reader Go test files, 1 `TestReadFill` function with 3 count/error scenarios, 1 `TestErrorReader` function confirming sentinel read-error propagation, 1 `TestRepeatableReader` function with 10 read/cache/seek scenarios, 5 `RepeatableReader` constructor functions including 4 limit/buffer variants, 2 `FakeSeeker` test functions covering pass-through read-seeker wrapping, pre-read `SeekStart`/`SeekCurrent`/`SeekEnd`, non-start read rejection, post-read seek rejection, and sticky EOF/read errors, 1 `NoSeeker` test function covering delegated reads and the upstream `can't Seek` error, 2 `PatternReader` test functions covering zero/ten-byte lengths, modulo-251 byte emission, `SeekStart`/`SeekCurrent`/`SeekEnd`, invalid whence, and negative-position errors, 1 `LimitedReadCloser` source file covering negative-limit passthrough and the close-error suppression branch after complete limited reads, 1 `TestNoCloser` function covering nil passthrough, read-only passthrough, closable-reader wrapping, hidden close, and delegated read errors, 1 `TestGzipReader` function covering gzip decompression and underlying stream close behavior, 1 `TestContextReader` function covering a successful first read followed by `context.Canceled` without additional underlying reads, plus 1 untested `CountingReader` source helper that wraps `io.Reader` and increments `BytesRead` by the returned byte count.
- Focused ReOpen static inventory: 1 upstream Go test function in `fs/operations/reopen_test.go`, 4 mode runs (`Normal`, `WithRangeOption`, `WithSeekOption`, `UnknownSize`), 36 subtest executions across basics, immediate open failure, transient read failures, too many retries, `ReadAt`, `Seek`, unknown-size positive seek and `SeekEnd` error behavior, accounting, delayed accounting, and accounting-error behavior, plus 3 interface assertions for read/seek/close, reader-at, and delay-accounting contracts.
- Focused no-low-level-retry static inventory: 1 upstream error marker constructor (`NoLowLevelRetryError`), 1 marker detection function (`IsNoLowLevelRetryError`), and 1 ReOpen read branch that suppresses low-level reopen attempts when the marker is present.
- Focused sync/delete static inventory: 5 delete mode constants in `fs/deletemode.go`; 10 focused sync delete/filter test functions in `fs/sync/sync_test.go` (`TestSyncAfterRemovingAFileAndAddingAFileDryRun`, `TestSyncAfterRemovingAFileAndAddingAFile`, `TestSyncAfterRemovingAFileAndAddingAFileSubDir`, `TestSyncAfterRemovingAFileAndAddingAFileSubDirWithErrors`, `TestSyncDeleteAfter`, `TestSyncDeleteDuring`, `TestSyncDeleteBefore`, `TestCopyDeleteBefore`, `TestSyncWithExclude`, and `TestSyncWithExcludeAndDeleteExcluded`); 6 focused operations delete/purge/rmdirs test functions in `fs/operations/operations_test.go`; and 3 destructive command files (`delete`, `purge`, `cleanup`) read for command boundaries.
- Focused cleanup command inventory: 5 targeted source/test reads (`cmd/cleanup/cleanup.go`, `fs/operations/operations.go`, `fs/features.go`, `fs/operations/rc.go`, and `fs/operations/rc_test.go`), 1 upstream rc test function (`TestRcCleanup`), 28 exact backend `CleanUp(ctx)` implementations, 29 backend `fs.CleanUpper` compile assertions, 4 native cleanup boundary cases, and 1 WordPress empty-trash scenario.
- Focused max-delete static inventory: 3 upstream operations tests (`TestMaxDelete`, `TestMaxDeleteSizeLargeFile`, `TestMaxDeleteSize`), 2 guard branches in `fs/accounting.StatsInfo.DeleteFile`, and 2 documented flags in `docs/content/docs.md`. The guard checks happen before deletion, `--max-delete-size` is cumulative, and negative object sizes are counted as zero.
- Focused backup-dir static inventory: 8 upstream test functions across `fs/sync/sync_test.go`, `fs/operations/copy_test.go`, and `fs/operations/operations_test.go` (`TestSyncBackupDir`, `TestSyncBackupDirWithSuffix`, `TestSyncBackupDirWithSuffixKeepExtension`, `TestSyncBackupDirSuffixOnly`, `TestSyncSuffix`, `TestSyncSuffixKeepExtension`, `TestCopyFileBackupDir`, and `TestMoveFileBackupDir`); 5 implementation functions/branches (`BackupDir`, `MoveBackupDir`, `SuffixName`, `DeleteFileWithBackupDir`, and `DeleteFilesWithBackupDir`); and 2 suffix-keep-extension modes including multi-extension handling.
- Focused compare/copy-dest static inventory: 5 upstream test functions (`TestSyncCompareDest`, `TestSyncMultipleCompareDest`, `TestSyncCopyDest`, `TestCopyFileCompareDest`, and `TestCopyFileCopyDest`); 5 implementation functions (`CompareOrCopyDest`, `compareDest`, `copyDest`, `GetCompareDest`, and `GetCopyDest`); 6 `BackupDir` validation branches for same remote, destination/source overlap, same-dir single-file backups, suffix-only destination backups, internal empty config, and missing server-side move/copy support; and 2 copy-dest/backup-dir interaction points where overwritten destination objects are moved before the reference object is copied into place.
- Focused `--ignore-existing` / `--immutable` / `--no-check-dest` / `--ignore-times` / `--update` / `--no-update-modtime` static inventory: 8 upstream test functions across `fs/sync/sync_test.go` and `fs/operations/operations_test.go` (`TestSyncIgnoreExisting`, `TestMoveWithIgnoreExisting`, `TestMoveFileWithIgnoreExisting`, `TestSyncImmutable`, `TestSyncIgnoreTimes`, `TestSyncAfterChangingModtimeOnly`, `TestSyncAfterChangingModtimeOnlyWithNoUpdateModTime`, and `TestSyncWithUpdateOlder`); 5 transfer-decision implementation points (`NeedTransfer`, `Equal`, `syncCopyMove.pairChecker`, `CopyFile` / `MoveFile` destination lookup, and `fs/march` destination bypass); 2 no-check-dest destination bypass branches in `fs/march/march.go`; 1 exported immutable error sentinel (`ErrorImmutableModified`); 4 modtime/update branches in `NeedTransfer`/`Equal`; and 6 documented flag sections in `docs/content/docs.md`.
- Focused `--refresh-times` static inventory: 1 config flag in `fs/config.go`, 1 documented flag section in `docs/content/docs.md`, 1 `hash.None && !ci.RefreshTimes` decision branch in `operations.Equal`, and 2 native boundary cases mapped here: absent common hashes update destination modtime without transfer when `refreshTimes` is enabled, while a common hash mismatch still transfers source bytes.
- Focused directory modtime/metadata static inventory: 3 operations test functions (`TestMkdirModTime`, `TestSetDirModTime`, and `TestDirsEqual`), 4 sync directory-modtime test functions (`TestSyncReplaceDirModTime`, `TestSyncReplaceDirModTimeWithEmptyDirs`, `TestNothingToTransferWithEmptyDirs`, and `TestNothingToTransferWithoutEmptyDirs`), 4 directory metadata-focused tests/branches (`TestCopyMetadata`, `TestCopyMetadataNoEmptyDirs`, `TestMkdirMetadata`, and `TestCopyDirMetadata`), and 2 ListJSON directory metadata/modtime branches for directory entries and metadata output.
- Focused delayed directory modtime inventory: 4 implementation functions/branches (`setDelayedDirModTimes`, `copyDirMetadata` delayed queueing, `markDirModified`, and `markDirModifiedObject`), 2 ordering rules (deepest-first update order and parent marking after child updates), and 2 native boundary cases (changed child object propagation and empty-dir/no-update suppression).
- Focused IgnoreCaseSync inventory: 1 upstream `TestSyncIgnoreCase` function, 1 config flag in `fs/config.go`, 1 documented flag section in `docs/content/docs.md`, 2 implementation branches/boundaries (case-folded march keys and duplicate key first-entry retention), and 7 native boundary cases (equal differently-cased object skip without rename, changed differently-cased object update without source-case rename, destination-only delete suppression, filtered WordPress artifact recopy suppression on a case-sensitive provider, compare-dest reference lookup at the matched destination casing, copy-dest plus backup-dir archival at the matched destination casing, and WordPress copy-dest hydration from a warm mirror while retaining remote casing).
- Focused FixCase inventory: 1 upstream `TestFixCase` function, 4 implementation branches/boundaries (case-folded destination match keys, file fix-case rename, directory fix-case rename before recursion, and immutable suppression), and 4 native boundary cases (equal object rename without transfer, changed nested directory/object casing before copy, immutable no-rename, and filtered WordPress backup casing repair).
- Focused TrackRenames inventory: 5 upstream sync test functions (`TestSyncWithTrackRenames`, `TestParseRenamesStrategyModtime`, `TestRenamesStrategyModtime`, `TestSyncWithTrackRenamesStrategyModtime`, and `TestSyncWithTrackRenamesStrategyLeaf`), 7 strategy parse cases, 3 strategy bits (`hash`, `modtime`, `leaf`) plus always-on size, 5 implementation functions/boundaries (`parseTrackRenamesStrategy`, `renameID`, `makeRenameMap`, `popRenameMap`, and `tryRename`), and 6 native boundary cases (hash rename before delete-after, no-common-hash fallback, no-server-side-move fallback, modtime candidate selection, leaf-only no-hash rename, and WordPress renamed upload plus backup-dir stale WXR archival).
- Focused provider move/copy inventory: 4 upstream test functions or test families (`TestDirMove`, `TestMoveFileBackupDir`, `TestSyncWithTrackRenames`, and server-side move/copy fallback checks in `TestServerSideMove*`), 5 implementation functions/boundaries (`CanServerSideMove`, `Move`, `Copy.serverSideCopy`, `MoveBackupDir`, and `DirMove`), and 6 native boundary cases (copy-only track-renames, `ErrorCantMove` copy/delete fallback, fatal tryRename upload/delete fallback, direct provider directory move, `ErrorCantDirMove` object-move fallback, and fatal directory move no-fallback).
- Focused single-file MoveFile/CopyFile inventory: 6 upstream test functions (`TestCopyFile`, `TestMoveFile`, `TestMoveFileWithIgnoreExisting`, `TestCaseInsensitiveMoveFile`, `TestCopyFileBackupDir`, and `TestMoveFileBackupDir`), 1 dry-run case-insensitive test boundary now ported in the command dry-run slice, 6 implementation functions/boundaries (`moveOrCopyFile`, `CopyFile`, `MoveFile`, `needsMoveCaseInsensitive`, `MoveCaseInsensitive`, and `copy.checkPartial`), 2 partial-cleanup branches (`removeFailedPartialCopy` and `removeFailedCopy`), and 7 native boundary cases (same-object no-op, idempotent copy, move source deletion, ignore-existing move preservation, case-insensitive temporary rename, backup-dir archival before replacement, and partial-upload temporary cleanup).
- Focused `RemoveExisting` inventory: 1 upstream test function (`TestRemoveExisting`), 4 backend server-side copy callers (Dropbox, OneDrive, Yandex, SugarSync), 5 implementation boundaries (missing-object no-op, direct move feature gate, temporary rename, success cleanup delete, and failure restore), 1 long-name truncation rule from `copy.TruncateString`, and 4 native boundary cases (missing cleanup no-op plus direct-move rejection, successful replacement cleanup, long-name restore after failure, and cleanup delete error reporting).
- Focused provider server-side copy replacement inventory: 4 backend callers, 2 same-lowercase guard branches in OneDrive/SugarSync, 2 createObject destination-handle call sites in OneDrive/SugarSync, 1 OneDrive guard-order boundary where `RemoveExisting` runs before the same-lowercase guard, and 7 native boundary cases (successful RemoveExisting cleanup after copy, failed copy restores the saved destination and preserves the provider error, same-lowercase paths are rejected without deleting the source, OneDrive-style same-lowercase rejection restores a temporarily saved destination, a direct-move blocker is reported before the OneDrive guard, a pre-created destination handle does not create a visible object when the provider copy fails, and a WordPress WXR casefold guard example keeps the portable export intact).
- Focused provider server-side copy metadata/error inventory: 4 provider result profiles (Dropbox, OneDrive, Yandex, SugarSync), 11 error/result branches (Dropbox not-a-file result; OneDrive missing location, async access denied to `ErrorCantCopy`, async status decode/status/metadata-read/timeout errors; Yandex async info/status decode, failure, and timeout wrapping; and SugarSync HTML plus missing copied-ID extraction), and 11 native boundary cases across metadata refresh and restore-after-provider-error behavior.
- Focused OneDrive QuickXor inventory: 4 targeted upstream source/test reads, 5 upstream quickxor test functions (`TestQuickXorHash`, `TestQuickXorHashByBlock`, `TestSize`, `TestBlockSize`, and `TestReset`), 1 upstream object metadata branch that decodes Graph `QuickXorHash` base64 into hex, 4 native boundary cases (three QuickXor vectors, hashsum base64 formatting, OneDrive copy-result decode, and WordPress provider-copy metadata refresh).
- Focused dedupe inventory: 11 upstream dedupe test functions including `TestDeduplicateInteractive` and `TestDeduplicateNewestByHash`, 1 upstream `TestMergeDirs` provider feature test, 9 command mode strings, 1 by-hash upstream test function, 8 by-name upstream test functions, 4 duplicate-directory implementation functions/branches (`dedupeDir`, `dedupeFindDuplicateDirs`, `dedupeMergeDuplicateDirs`, and provider `MergeDirs`), 6 native by-hash boundary cases (mode parsing, newest keep/delete, first keep/delete, skip/list no-delete reporting, interactive keep/quit, and no-hash/rename boundary errors), 7 native by-name boundary cases (identical same-name cleanup before skip, newest keep-one, extension-preserving rename around existing numbered names, size-only cleanup without hashes, repeated provider-ID suppression, interactive keep after identical cleanup, and interactive rename), 4 interactive prompt commands (`skip`, `keep`, `rename`, `quit`), 3 native merge-dir boundary cases (later directory contents move into the first directory, duplicate-directory groups are ordered largest-first before merge, and same-path provider-ID source children are rewired to the kept directory), 1 native provider-ID/ParentID list-mode discovery case, and 1 native provider-ID non-list merge case.
- Focused native fstest object contract inventory: 4 new native behavior tests map upstream object open/update/unknown-size source boundaries, plus 1 WordPress WXR example test maps range preview/tail reads, in-place update, and streamed unknown-size import storage.

## Runner Status

The prior missing Go/module/tooling blocker is resolved for a bounded upstream runner. On 2026-05-22, this lane installed Go 1.25.0 under `/home/claude/.local/go-toolchains/go1.25.0`, rebuilt the upstream binary at `28d6b0b`, supplied `rsync` 3.4.1 through a local Fedora RPM extraction and wrapper, and used a temporary mount-namespace overlay for `/etc/mime.types`.

The focused cleanup command passed for the packages that previously exposed local environment gaps:

```text
go test -skip '^TestIntegration' ./backend/huaweidrive ./cmd/gitannex ./cmd/serve/http ./fs/accounting ./fs/logger
```

The broad bounded command then passed for 299 upstream Go packages:

```text
GOFLAGS='-p=1' go test -timeout 20m -skip '^TestIntegration' $(go list ./... | grep -Ev '^github.com/rclone/rclone/cmd/(mount|mount2|serve/docker)$')
```

Environment notes for the passing run:

- `go version go1.25.0 linux/amd64`
- `rclone v1.75.0-beta.1.28d6b0b`, statically linked from the upstream checkout
- `rsync 3.4.1`
- `TZ=UTC`, `CI=true`, `RCLONE_CONFIG=/notfound`
- UID 1001 after dropping namespace capabilities, so permission-denial tests still run as an ordinary user
- temporary `/etc/mime.types` overlay inside the mount namespace only, without modifying the host `/etc`

This is bounded upstream runner evidence, not full provider/mount parity. Live-service provider tests were skipped with `-skip '^TestIntegration'`. `cmd/mount`, `cmd/mount2`, and `cmd/serve/docker` were excluded because they require FUSE/container mount permissions. Full `make quicktest` / `go test ./...` parity and `make test` / `fstest/test_all` provider parity remain open for a later environment that can run the live-service and mount fixtures.

## Mapped Native Slice

The PHP slice maps selected semantics from `fs/filter/glob_test.go`, `fs/filter/filter_test.go`, and existing lane checksum planning:

- Path glob conversion for `*`, `**`, `?`, leading `/`, brace alternation, regexp escapes, and invalid glob detection.
- First-match include/exclude rule ordering with `!` rule reset.
- Case-insensitive matching via the upstream `ignore_case` option.
- Sync planning that skips objects excluded by rclone-style filters.
- WordPress backup planning that includes uploads, WXR exports, and SQL dumps while excluding cache/log/source files.
- Destination-only delete planning for rclone sync modes `off`, `before`, `during`, `after`/default, and `only`, with the same final candidate set for before/during/after/only and no deletion in off mode.
- Filtered destination deletion: excluded destination objects are not deleted by default, but `deleteExcluded` includes destination entries ignored by the normal filter, matching the upstream `SyncWithExclude` and `SyncWithExcludeAndDeleteExcluded` boundary.
- Copy remains copy-only: changed/missing source objects are copied without pruning destination-only files until the explicit delete planning pass, matching the upstream `CopyDeleteBefore` boundary.
- WordPress backup pruning copies included uploads/WXR/SQL artifacts and deletes stale included remote artifacts while leaving excluded cache/log/source artifacts untouched.
- `fs/accounting DeleteFile` max-delete safeguards: deletion stops before the next object when `deletes+1` would exceed `--max-delete`, and the upstream threshold message is surfaced.
- `fs/accounting DeleteFile` max-delete-size safeguards: deletion stops before the next object when cumulative deleted bytes plus the candidate size would exceed `--max-delete-size`; unknown/negative sizes count as zero like upstream.
- WordPress backup cleanup can now enforce a max-delete guard, removing the first stale included artifact while leaving the next stale backup artifact and excluded cache files in place for review.
- `fs/operations MoveBackupDir` semantics: overwritten destination objects and destination-only delete candidates are moved into a backup directory before replacement/removal, preserving the old bytes and overwriting prior backup objects at the same path.
- `fs/operations SuffixName` semantics: backup names can append a suffix after the whole path, or before the kept extension when suffix-keep-extension is enabled; the PHP slice includes the upstream `file.tar.gz` versus `file.badextension.gz` distinction.
- Suffix-only backup behavior: when no backup directory is configured but a suffix is present, the destination acts as the backup location, so caller filters must exclude the suffix to prevent backup files from being selected for deletion in a later sync.
- WordPress backup cleanup can archive overwritten uploads and stale WXR exports under a dated backup prefix with suffix-keep-extension naming while still respecting max-delete before the next destructive move.
- `fs/operations BackupDir` validation: configured backup roots must be on the same remote as the destination, must not overlap source or destination roots for sync, cannot be the same source/destination directory for single-file backup without a suffix, suffix-only backups use the destination root, and backup remotes must support server-side move or copy.
- `fs/operations CompareOrCopyDest` compare-dest behavior: matching reference objects suppress source transfers while leaving an old or missing destination unchanged.
- Multiple `--compare-dest` references are consulted in order until one equal reference object is found.
- `fs/operations CompareOrCopyDest` copy-dest behavior: matching reference objects are copied to the destination instead of uploading source bytes, and stale references fall back to the source transfer path.
- `--copy-dest` plus `--backup-dir` moves an overwritten destination object to the backup root before copying the equal reference object into place.
- WordPress backup sync can hydrate missing or replaced portable artifacts from a warm mirror while archiving the previous target upload and preserving excluded cache objects.
- `--ignore-existing` transfer decisions skip any source object that already exists at the destination, even when the destination bytes differ, while still copying missing source objects.
- `--immutable` transfer decisions create missing destination objects but refuse to replace a differing existing destination object with the upstream `immutable file modified` error, leaving the destination bytes unchanged.
- `--no-check-dest` copy-style decisions do not consult the destination before transfer, so every included source object is sent and existing destination bytes are overwritten in the in-memory provider without backup-dir archival because no destination object was checked.
- WordPress append-only archive sync can create a missing dated SQL artifact while preserving an existing WXR backup and surfacing `immutable file modified` if the source later rewrites that backup path.
- `--ignore-times` transfer decisions send existing destination objects unconditionally, even when size, hash, and modtime already match.
- Default modtime-only differences with equal size and hash update the destination timestamp without transferring bytes; `--no-update-modtime` suppresses that timestamp repair.
- `--update` / `--update-older` skips destination objects newer than source, transfers older destination objects when size/hash differ, and treats near-equal mtimes as size-only unless `--checksum` is set.
- WordPress update-older archive sync can refresh stale SQL and same-window changed upload artifacts while preserving a newer remote WXR recovery export and excluded cache files.
- `--refresh-times` repairs destination timestamps without transferring when source and destination have equal size, different modtimes, and no common hashes; without the flag, the same no-hash condition transfers because upstream cannot prove equality safely.
- `--refresh-times` still transfers when a common provider hash exists and differs, matching the upstream branch that only accepts absent hashes when refresh-times is enabled.
- WordPress no-hash archive sync can repair WXR and SQL artifact timestamps while preserving existing bytes and still copying a missing upload artifact.
- `fs/operations MkdirModTime`-style explicit directories store modtimes independently from objects, while synthetic parent directories remain visible for list/stat behavior.
- `fs/operations SetDirModTime`-style updates reject missing directories, obey the `--no-update-dir-modtime` skip boundary, and can set timestamps on an existing synthetic directory without creating unrelated missing paths.
- `fs/operations DirsEqual`-style directory comparisons honor modtime tolerance, `--ignore-times`, `--immutable`, `--ignore-existing`, `--update`, missing destination directories, and disabled directory modtime/metadata options.
- `fs/operations ListJSON`-style directory entries can expose explicit directory modtime, metadata, and ID values, while synthetic parent directories keep blank modtimes.
- WordPress uploads and incremental export directories can repair directory timestamps and publish `lsjson` directory metadata without replacing portable backup objects.
- `fs/sync setDelayedDirModTimes`-style repairs run after changed child object writes, update modified directories deepest-first, then mark parent directories modified for the next level.
- Delayed directory timestamp repair skips empty source directories unless `copyEmptySourceDirs` is enabled and is suppressed entirely by `--no-update-dir-modtime`.
- WordPress upload-directory metadata repair now propagates from changed media month directories up through year/uploads/content roots while leaving unrelated cache directories stale.
- `fs/sync --ignore-case-sync` case-folds source and destination match keys on case-sensitive providers, so equal differently-cased objects are not recopied or renamed.
- `fs/sync --ignore-case-sync` treats differently-cased destination matches as present during destination-only delete planning and updates the matched destination path when bytes differ instead of forcing source casing.
- WordPress ignore-case sync can skip recopying upload and WXR artifacts whose names differ only by case on a case-sensitive provider while keeping the existing remote casing and excluded cache artifacts.
- `fs/operations CompareOrCopyDest` under `--ignore-case-sync` probes compare-dest and copy-dest references at the matched destination remote path when one exists, matching upstream `remote = dst.Remote()` behavior.
- Copy-dest plus backup-dir under `--ignore-case-sync` moves the overwritten differently-cased destination object to backup under its current remote casing, then writes the matching reference object back to that same destination path.
- WordPress ignore-case copy-dest sync can hydrate differently-cased upload and WXR export artifacts from a warm mirror, archive the previous remote-cased versions, copy missing portable artifacts at source casing, and keep excluded cache artifacts outside delete planning.
- `fs/sync --fix-case` case repair for case-insensitive providers: equal destination objects are renamed to source casing without transfer, changed destination objects/directories are case-repaired before copying source bytes, directory casing is fixed before recursively checking child paths, and immutable mode suppresses the repair.
- WordPress backup fix-case sync repairs upload and export artifact casing on a case-insensitive remote while excluded cache leaf paths remain untouched except for shared parent directory case changes.
- `fs/sync --track-renames` strategy parsing accepts `hash`, `modtime`, `leaf`, and ignored `size` tokens like upstream while rejecting unknown tokens.
- `fs/sync --track-renames` default hash strategy collects source-only and destination-only candidates, builds size plus common-hash IDs, renames the destination object to the source path before uploads, then runs delete-after for remaining destination-only objects.
- Hash-based `--track-renames` falls back to ordinary upload/delete behavior when source and destination providers do not share a common hash or the destination does not advertise server-side move/copy support.
- `--track-renames-strategy modtime` matches same-size candidates only when source and destination modtimes are inside the modify window, skipping stale candidates with the same size.
- `--track-renames-strategy leaf` matches basename plus size without hashes, modeling the upstream encrypted/no-hash provider boundary.
- WordPress track-renames sync can move a renamed upload artifact server-side while archiving an unmatched stale WXR export through backup-dir suffix handling and leaving excluded cache artifacts untouched.
- `fs/operations CanServerSideMove`-style gating treats either direct `Move` or server-side `Copy` as enough provider support for track-renames and backup-dir move flows.
- `fs/operations Move`-style `ErrorCantMove` handling falls back to copy plus delete, while fatal move errors make `tryRename` fall back to ordinary source upload and delete-after cleanup of the stale destination object.
- `fs/operations DirMove` uses the provider directory-move feature when available, falls back to per-object moves on `ErrorCantDirMove` or `ErrorDirExists`, and does not hide fatal provider directory-move errors.
- WordPress provider move-gate sync can repair a renamed upload on a copy-only provider, then archive an upload month directory through object-move fallback when direct directory moves are unavailable.
- `fs/operations CopyFile` and `MoveFile` single-file calls now map same-object no-op, idempotent destination copies, moving via copy/delete with source removal, `--ignore-existing` source preservation, `--backup-dir` archival of overwritten destinations, and case-insensitive two-step file casing repair.
- `fs/operations Copy` partial-upload cleanup is modeled for failed single-file copies: the temporary partial object is removed, the prior destination object is preserved, and the later successful partial copy is renamed into place.
- WordPress single-file backup repair can change only the casing of an uploaded media object, preserve a local WXR recovery export under `--ignore-existing`, and clean failed partial upload artifacts before retry.
- `fs/operations RemoveExisting` safe overwrite behavior is modeled: existing destination objects are direct-moved to temporary names before provider overwrites, missing destinations return a no-op cleanup, success cleanup deletes the saved object, operation failure restores it, and long leaf names are truncated before the temporary suffix is appended.
- Provider server-side copy replacement behavior is modeled for RemoveExisting callers: successful copies delete the saved destination, failed copies restore it while preserving the provider error, and providers without server-side copy support reject the helper.
- SugarSync-style same-remote case-fold guards reject source and destination paths that differ only by case before provider copy can overwrite the source object, while the OneDrive-style branch runs after `RemoveExisting` and restores the temporarily saved destination on guard failure.
- OneDrive/SugarSync-style pre-created destination handles remain non-visible until the server-side copy succeeds, so failed provider copies do not leave empty placeholder objects.
- WordPress long-name WXR overwrite workflows can replace an export through the safe temporary rename path while preserving the old export if the provider copy fails.
- WordPress server-side WXR copy replacement can publish a fresh export, preserve same-lowercase provider guard errors, restore OneDrive-style temporarily moved casefold destinations, and leave failed pre-created destination handles out of listings.
- `cmd/dedupe` / `fs/operations DeduplicateMode` mode parsing accepts upstream non-interactive mode strings and rejects unknown modes with an upstream-shaped error.
- `fs/operations Deduplicate --by-hash` groups provider objects by the provider's first supported hash, requires at least one supported hash, and implements newest/oldest/first/largest/smallest keep-one behavior plus skip/list no-delete reporting.
- `fs/operations Deduplicate` interactive mode is represented by deterministic caller-supplied choices matching upstream prompt commands: `skip` reports without mutation, `keep` deletes all but the 1-based selected object, by-name `rename` uses numbered extension-preserving names, by-hash `rename` is rejected because upstream does not offer it, and `quit` stops before later duplicate groups.
- WordPress duplicate WXR archive copies can be deduped by hash while keeping the newest export and preserving unrelated SQL backups.
- `fs/operations Deduplicate` by duplicate name is represented with unchecked same-path provider entries: identical same-name objects are deleted before skip/keep/rename modes, size-only mode can collapse same-size objects without hashes, repeated provider IDs are ignored to avoid data loss, newest keeps the latest modtime, and rename mode picks numbered extension-preserving paths while skipping existing targets.
- WordPress interactive WXR duplicate review can remove identical same-name exports before keeping a reviewer-selected recovered draft export.
- WordPress duplicate-name WXR exports can collapse identical same-name artifacts first, then rename remaining conflicting exports around an existing numbered export for manual review.
- `fs/operations MergeDirs` provider behavior is modeled for dedupe's duplicate-directory pre-pass: later directory contents move into the first directory, emptied source directories are removed, and colliding file paths remain as duplicate same-name objects for the file dedupe pass.
- `fs/operations dedupeMergeDuplicateDirs` largest-first ordering is modeled for explicit duplicate-directory groups, so the directory with the highest recursive entry count is retained before merging other candidates into it.
- `fs/operations dedupeFindDuplicateDirs` provider-ID/ParentID discovery is modeled for duplicate directory list mode: duplicate same-remote directory entries are grouped by path, recursive counts are built through ParentID links, and `list` mode reports the duplicate directory group without mutating provider entries.
- `fs/operations dedupeMergeDuplicateDirs` non-list provider-ID merging is modeled for same-path duplicate directories: the largest recursive directory entry is kept, source child directories and objects are reparented to the kept provider ID, the removed source directory ID disappears from discovery, and colliding object paths remain duplicated for the file dedupe pass.
- WordPress duplicate upload-directory recovery can merge a smaller restored media directory into the larger published upload month and then rename colliding media files for manual review.
- `fs/operations` / `cmd/lsjson` list and stat JSON shapes for paths, names, sizes, directory entries, recursive listings, file-only and dir-only modes, omitted modtime/mimetype fields, selected hash output, and metadata.
- Case-insensitive provider `StatJSON` lookup: differently-cased file and directory requests resolve to the provider's canonical object or directory path while still honoring file-only and dir-only filters.
- `fs/operations CheckSum`-style verification of parsed SUM files against provider objects, including match, differ, file-only-in-provider, file-only-in-sum, one-way, filter, duplicate-sum, mixed-case hash, and case-insensitive path transform behavior.
- `fs/operations CheckSum --download` behavior where ordinary checksum mode rejects a provider that does not advertise the requested hash, while download mode hashes object bytes locally and preserves the same match/differ/missing reports.
- `fs/operations CheckEqualReaders` plus `lib/readers ReadFill` behavior for 64 KiB chunk filling, equal byte streams, byte differences, length differences, and read-error precedence before byte-difference reporting.
- `fs/operations CheckDownload` provider-to-provider verification: size differences become ordinary `*` differences before download, equal-size objects are compared byte-for-byte, and open/read failures are reported through `!` error lines with failed-download wrapping.
- `fs/operations ReOpen` retry/range reader behavior: initial open failures, reopen after transient read failures at the already-read offset, inclusive range end handling, seek-offset starts, `ReadAt` without changing the current position, seek bounds, closed-reader errors, and delayed accounting.
- `fs/operations ReOpen` no-low-level-retry and accounting-error behavior: no-low-level-retry read errors are returned without opening a new ranged reader, remain sticky for subsequent reads, and accounting callback errors are propagated without retrying the underlying object stream.
- `fs/operations ReOpen` unknown-size mode: objects reporting size `-1` can stream from a range start without a bounded length, reopen retries keep using unbounded range reads, positive seeks past the actual byte length are accepted, and `SeekEnd` returns the upstream unknown-size seek error.
- `lib/readers RepeatableReader` cache-backed read/seek behavior: bytes read from the underlying reader are cached, cached prefixes can be replayed after seeking, seeking beyond the cached prefix is rejected, and `SeekCurrent`/`SeekEnd` are resolved against the cached data.
- `lib/readers RepeatableReader` limit/buffer constructor behavior: limit constructors stop reading at the upstream `io.LimitReader` byte count, buffer constructors treat the supplied byte slice as zero-length capacity rather than preloaded cache, and the combined limit-buffer constructor maps both behaviors.
- `lib/readers FakeSeeker` and `NoSeeker` provider-contract behavior: existing read-seekers are passed through, non-seekable readers can be seeked before any bytes are read to discover known length, reads after a non-zero pre-read seek fail until rewound, all seeks after data is read fail, EOF/read errors remain sticky, and `NoSeeker` delegates reads while rejecting every seek with the upstream `can't Seek` error.
- `lib/readers PatternReader` deterministic fixture behavior: byte `i` is emitted as `i % 251`, the next byte resets after `SeekStart`, `SeekCurrent`, and `SeekEnd`, past-end seeks are accepted and read as EOF, and invalid whence or negative positions raise upstream-shaped errors.
- `lib/readers LimitedReadCloser` byte-limit and close behavior: non-negative limits cap reads to the upstream `io.LimitedReader` byte count, negative limits pass the original reader through, close errors propagate while bytes remain unread, and close errors are ignored after the limited body is fully consumed.
- `lib/readers NoCloser` close-hiding behavior: nil is returned unchanged, read-only readers are returned unchanged, readers that also expose `Close` are wrapped so `Close` is no longer visible, and read errors still propagate from the underlying reader.
- `lib/readers GzipReader` close behavior: gzip streams are decompressed with native PHP zlib, `Close` closes the underlying provider body because the gzip layer alone would not, and provider close errors take precedence.
- `lib/readers ContextReader` cancellation behavior: the context error is checked before each read, successful reads delegate unchanged before cancellation, and canceled reads return the context error without advancing the underlying stream.
- `lib/readers CountingReader` byte-accounting behavior: each successful read delegates to the underlying reader and increments `BytesRead` by the number of bytes actually returned; failed reads that return no bytes do not advance the count in the PHP exception model.
- `fstest walk.GetAll` direct-depth listing behavior: depth 1 returns only immediate files and directories, depth 2 includes second-level entries without deeper descendants, subdirectory walks are scoped under the requested path, and missing directories raise an upstream-shaped not-found error.
- `fs/operations Purge` behavior: subtree purge deletes all listed objects and explicit directories under a target path, preserves unrelated paths, and reports an error when a non-root subtree is purged a second time after removal.
- `FsListRootedSubdir` / `FsPurgeRoot` behavior: a rooted provider view rebases object and directory paths relative to its root while mutating the same backing provider for puts and purges.
- WordPress rooted upload-month maintenance can list direct media files and generated thumbnail directories, purge generated thumbnails from the rooted monthly view, and preserve adjacent upload months plus WXR exports in the backing provider.
- `fstest PublicLink` provider behavior: missing paths raise an error, existing files return non-empty stable links, different files produce distinct links, existing directories can be shared, and rooted subremote roots can be shared through the same provider feature.
- WordPress public-link sharing can publish a WXR export and a rooted upload-month directory for migration review, then explicitly unlink the WXR share without exposing unrelated provider paths.
- `fstest` metadata-set behavior is modeled for objects and directories: object `SetMetadata` replaces metadata, `mtime` updates visible modtime, `content-type` updates mimetype, directory metadata can be created or replaced, and server-side copy/move can apply `--metadata-set` to the destination while preserving copy sources.
- WordPress migration handoff copy/move workflows can stamp WXR exports and upload artifacts with review metadata, MIME type, and handoff mtimes without shelling out to rclone.
- `fstest` SetTier/GetTier behavior is modeled for objects: supported tiers round-trip through provider objects, unsupported providers report upstream-shaped errors, filtered `SetTier` updates only listed matching objects, `SetTierFile` changes one object, `lsf --format T` emits object tiers, and `lsjson` includes `Tier` only when the provider exposes GetTier.
- WordPress portable backup artifacts can be moved to an archive storage tier through filtered settier-style planning while cache, logs, and design-source artifacts retain their original provider tier.
- `fs/list DirSortedFn` integration is modeled over ListP-style provider pages: each raw page is counted before filtering, page-local exclude-if-present markers skip that page when `includeAll` is false, direct-directory object/directory filters are reused, caller key functions are passed through `ListSorter`, and cleanup happens even when later sends or callbacks fail.
- WordPress paged restore manifests can publish SQL first, primary WXR before supplemental WXR, then upload directories from provider pages while pruning cache/debug entries and short-circuiting a cache page marked by `.rclone-ignore`.
- `fs/list DirSorted` object-returning integration is modeled over provider `List`: a single direct listing is counted before filters, exclude-if-present markers suppress the whole listed directory when `includeAll` is false, direct-directory filtering and stable Remote sorting are reused, and provider/type/filter errors propagate.
- WordPress provider-List restore manifests can use the object-returning `DirSorted` path when a backend lacks `ListP`, then apply a restore-priority sorter to publish SQL, WXR, users, and uploads while excluding cache pages marked by `.rclone-ignore`.
- `fs/walk Walk` over `DirSorted` is modeled for parent-before-child callbacks, maxLevel traversal, non-fatal `ErrorSkipDir`, provider/list error masking or propagation through the callback, and exclude-if-present child directory pruning.
- `fs/walk ListR` fallback through `Walk` is modeled for `ListObjects`/`ListDirs`/`ListAll` batch filtering, delayed provider listing errors after traversal, and immediate callback-error propagation.
- WordPress recursive restore manifests can traverse provider `List` fallback directories to a bounded depth, publish WXR/SQL/uploads in restore-priority order, prune cache contents marked by `.rclone-ignore`, and stop before generated thumbnail leaves beyond the configured max depth.
- `fs/walk` direct `ListR` is modeled for provider callback batch order, `ListObjects`/`ListDirs`/`ListAll` filtering, object and directory include callbacks, provider errors returned after callbacks, and callback errors that stop before synthetic parent flush.
- `fs/walk` `dirMap` bucket synthesis is modeled for raw-entry parent tracking before filters, explicit directories marked as sent, root-boundary suppression, sorted unsent parent directories, and upstream-shaped 100-entry callback batches for synthesized parents.
- WordPress direct ListR bucket manifests can consume arbitrary recursive provider batches from remotes that do not list placeholder directories, synthesize missing upload parents, preserve provider order before publish sorting, and emit SQL, WXR, users WXR, and uploads in restore-priority order.
- `fs/march --no-traverse` transfer matching is modeled for copy/sync transfer phases: destination `NewObject` probes are issued only for source objects, source directories remain source-only recursion entries, `--no-check-dest` skips probes entirely, and no destination listing is used for matching.
- WordPress no-traverse backup copies can refresh WXR/SQL/upload artifacts by probing only those filtered destination object paths, leaving cache artifacts unlisted and untouched.
- `fs/march.matchListings` duplicate-destination behavior is now exposed as native diagnostics: after sorted matching, the first destination object for a path remains the comparison entry, later same-key destination objects are reported with the upstream-shaped `Duplicate object found in destination - ignoring` message, and those ignored objects do not become destination-only delete candidates in the diagnostic partition.
- WordPress migration review can now surface a duplicate destination WXR export before sync: the canonical WXR stays matched, the interrupted stale duplicate is reported for reviewer attention, the missing SQL dump remains source-only and copied, and excluded cache artifacts stay outside diagnostics.
- `fs/walk NewDirTree` no-traverse plus files-from construction is modeled as a synthetic ListR: explicit files-from remotes are normalized and de-duplicated, `NewObject` is called for each selected remote, missing objects are skipped, ordinary lookup/type errors propagate, and provider `List` / provider `ListR` traversal is bypassed.
- WordPress no-traverse files-from restore manifests can lookup only selected WXR/SQL/media remotes, skip missing entries, synthesize upload parents through the DirTree builder, and avoid listing unrelated cache artifacts.

## Current PHP Verification

- Rclone-only PHP lane check on 2026-05-22 before the no-low-level-retry/accounting-error slice: 38 tests, 280 assertions, 0 failures.
- Rclone-only PHP lane check on 2026-05-22 after the no-low-level-retry/accounting-error slice: 40 tests, 289 assertions, 0 failures.
- Rclone-only PHP lane check on 2026-05-22 after the RepeatableReader limit/buffer constructor slice: 43 tests, 310 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the RepeatableReader limit/buffer constructor slice: 73 test files, 4,155 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the FakeSeeker/NoSeeker slice: 5 rclone test files, 48 tests, 331 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the FakeSeeker/NoSeeker slice: 75 test files, 4,264 assertions, 2 failures in unrelated `lanes/esbuild/tests/TypeScriptModuleLowererTest.php` const-enum formatting tests; the rclone lane tests in that run all passed.
- Rclone-only PHP lane check on 2026-05-22 after the PatternReader slice: 6 rclone test files, 52 tests, 1,373 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the PatternReader slice: 78 test files, 5,431 assertions, 1 failure in unrelated `lanes/readability/tests/ArticleExtractorTest.php` nested wrapper simplification; the rclone lane tests in that run all passed.
- Rclone-only PHP lane check on 2026-05-22 after the LimitedReadCloser slice: 7 rclone test files, 57 tests, 1,386 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the LimitedReadCloser slice: 80 test files, 5,390 assertions, 22 failures in unrelated `lanes/readability/tests/ArticleExtractorTest.php`, all reporting missing `PortLibs\Readability\ArticleExtractor::effectiveBaseUri()`; the rclone lane tests in that run all passed.
- Rclone-only PHP lane check on 2026-05-22 after the NoCloseReader slice: 7 rclone test files, 60 tests, 1,394 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the NoCloseReader slice: 82 test files, 5,679 assertions, 2 failures in unrelated `lanes/dolt/tests/StatusTableTest.php` status row ordering/ignored-row key order checks; the rclone lane tests in that run all passed.
- Rclone-only PHP lane check on 2026-05-22 after the GzipReader slice: 8 rclone test files, 64 tests, 1,404 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the GzipReader slice: 86 test files, 5,779 assertions, 7 failures in unrelated `lanes/esbuild/tests/TypeScriptNamespaceLowererTest.php`, all reporting missing `PortLibs\Esbuild\TypeScriptNamespaceLowerer::namespaceStatementAt()`; the rclone lane tests in that run all passed.
- Rclone-only PHP lane check on 2026-05-22 after the ContextReader slice: 9 rclone test files, 67 tests, 1,412 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the ContextReader slice: 89 test files, 6,056 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the CountingReader slice: 10 rclone test files, 71 tests, 1,432 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the CountingReader slice: 93 test files, 6,257 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the sync delete-planning slice: 11 rclone test files, 75 tests, 1,452 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the sync delete-planning slice: 95 test files, 6,292 assertions, 1 failure in unrelated `lanes/pandoc/tests/MarkdownReaderTest.php` (`PortLibs\Pandoc\MarkdownReader::parseLinkDestinationAndTitle()` missing); all rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-22 after the max-delete/max-delete-size slice: 11 rclone test files, 78 tests, 1,467 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the max-delete/max-delete-size slice: 98 test files, 6,487 assertions, 7 failures in unrelated lanes: missing Dolt fixture `lanes/dolt/fixtures/wp-procedure-history.php` and six `lanes/esbuild/tests/TypeScriptNamespaceLowererTest.php` namespace lowering expectation failures. All rclone tests in that root run passed.
- Required root `php tools/run-tests.php` rerun on 2026-05-22 after concurrent Dolt/Esbuild lane updates landed: 98 test files, 6,532 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the backup-dir/suffix slice: 11 rclone test files, 82 tests, 1,491 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the backup-dir/suffix slice: 100 test files, 6,747 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the compare-dest/copy-dest/backup validation slice: 11 rclone test files, 88 tests, 1,518 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the compare-dest/copy-dest/backup validation slice: 103 test files, 7,052 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the no-check-dest/ignore-existing/immutable slice: 11 rclone test files, 91 tests, 1,534 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the no-check-dest/ignore-existing/immutable slice: 104 test files, 7,219 assertions, 0 failures in the current shared dirty worktree.
- Required root `php tools/run-tests.php` reruns later on 2026-05-22 after unrelated lane changes landed found non-rclone failures while the rclone tests still passed: one run reported 106 test files, 7,449 assertions, 2 failures in unrelated `lanes/lightningcss/tests/CssMinifierTest.php` animation shorthand expectations; the latest filtered rerun reported 106 test files, 7,476 assertions, 1 failure in unrelated `lanes/pandoc/tests/MarkdownReaderTest.php` pipe-table classification.
- Rclone-only PHP lane check on 2026-05-22 after the ignore-times/update/modtime slice: 11 rclone test files, 96 tests, 1,556 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the ignore-times/update/modtime slice: 108 test files, 7,657 assertions, 5 failures in unrelated LightningCSS tests, all caused by missing `PortLibs\LightningCSS\CssMinifier::rewriteAnimationGroup()`; all rclone tests in that root run passed.
- Required root `php tools/run-tests.php` rerun on 2026-05-22 after concurrent fixes landed: 108 test files, 7,734 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the refresh-times slice: 11 rclone test files, 98 tests, 1,571 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the refresh-times slice first found 1 unrelated esbuild fixture failure, then passed after concurrent non-rclone lane updates landed: 110 test files, 7,961 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the directory modtime/metadata slice: 11 rclone test files, 102 tests, 1,596 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the directory modtime/metadata slice: 112 test files, 8,231 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the delayed directory modtime slice: 11 rclone test files, 104 tests, 1,609 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the delayed directory modtime slice: 113 test files, 8,396 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the FixCase slice: 12 rclone test files, 108 tests, 1,625 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the first FixCase slice attempt: 115 test files, 8,530 assertions, 2 failures: the new rclone WordPress fix-case expectation was corrected, and one unrelated markerPDF benchmark excerpt threshold failure remained outside this lane.
- Required root `php tools/run-tests.php` rerun on 2026-05-22 after the rclone expectation correction and concurrent non-rclone updates: 115 test files, 8,614 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the ignore-case-sync slice: 12 rclone test files, 112 tests, 1,646 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the ignore-case-sync slice: 115 test files, 8,748 assertions, 1 failure in unrelated `lanes/dolt/tests/CommitLogTableTest.php` (`wordpress commit log fixture surfaces import branch refs and merge parents`); all rclone tests in that root run passed.
- Required root `php tools/run-tests.php` rerun on 2026-05-22 after concurrent non-rclone changes: 116 test files, 8,825 assertions, 1 failure in unrelated `lanes/libsqlite/tests/SQLiteHeaderTest.php` (`parses sqlite substr expression index metadata without treating it as a column index`); all rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-22 after the ignore-case-sync compare/copy-dest slice: 12 rclone test files, 115 tests, 1,663 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the ignore-case-sync compare/copy-dest slice: 116 test files, 8,955 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the track-renames slice: 13 rclone test files, 122 behavior tests, 1,711 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the track-renames slice: 120 test files, 9,267 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the provider move-gates slice: 14 rclone test files, 128 behavior tests, 1,746 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the provider move-gates slice: 122 test files, 9,501 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the single-file MoveFile/CopyFile slice: 14 rclone test files, 135 behavior tests, 1,789 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the single-file MoveFile/CopyFile slice: 123 test files, 10,737 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the RemoveExisting slice: 14 rclone test files, 139 behavior tests, 1,810 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the RemoveExisting slice: 124 test files, 10,969 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the provider server-side copy replacement slice: 14 rclone test files, 143 behavior tests, 1,829 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 initially passed after the provider server-side copy replacement slice with 125 test files, 11,107 assertions, 0 failures.
- Required root `php tools/run-tests.php` rerun on 2026-05-22 after concurrent non-rclone changes now exits 1 with 126 test files, 11,229 assertions, and 1 unrelated failure in `lanes/difftastic/tests/TokenDifferTest.php` (`maps upstream tsx whitespace sample as formatting only`). All rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-22 after the provider copy metadata/error slice: 14 rclone test files, 151 behavior tests, 1,866 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the provider copy metadata/error slice exits 1 with 128 test files, 11,515 assertions, and 3 unrelated LightningCSS list-style failures in `lanes/lightningcss/tests/CssMinifierTest.php` and `lanes/lightningcss/tests/TransitionPrefixerTest.php`. All rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-22 after the by-hash dedupe slice: 15 rclone test files, 156 behavior tests, 1,891 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the by-hash dedupe slice: 130 test files, 11,689 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the duplicate-name dedupe slice: 15 rclone test files, 161 behavior tests, 1,909 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the duplicate-name dedupe slice: 135 test files, 12,036 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the merge-dir dedupe slice: 15 rclone test files, 163 behavior tests, 1,922 assertions, 0 failures.
- Required root `TMPDIR=/home/claude/port-runner-cache/port-rclone-root-tests/tmp php tools/run-tests.php` on 2026-05-22 after the merge-dir dedupe slice: 138 test files, 12,193 assertions, 0 failures in the current shared dirty worktree. A default-`/tmp` root run immediately before this hit unrelated Syncthing temp-file `errno=122 Disk quota exceeded`; moving `TMPDIR` off crowded `/tmp` cleared that environment blocker without code changes outside rclone.
- Rclone-only PHP lane check on 2026-05-22 after the duplicate-directory list-mode discovery slice: 15 rclone test files, 164 behavior tests, 1,931 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the duplicate-directory list-mode discovery slice: 140 test files, 12,377 assertions, 0 failures in the current shared dirty worktree using the default temp directory; no repo-local `TMPDIR` rerun was needed.
- Rclone-only PHP lane check on 2026-05-22 after the interactive dedupe prompt-choice slice: 15 rclone test files, 167 behavior tests, 1,947 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the interactive dedupe prompt-choice slice: 142 test files, 12,668 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the provider-ID duplicate-directory merge slice: 15 rclone test files, 168 behavior tests, 1,956 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the provider-ID duplicate-directory merge slice: 143 test files, 12,863 assertions, 1 failure in unrelated `lanes/readability/tests/ArticleExtractorTest.php` (`maps Mozilla data-url-image fixture media retention boundaries`, expected 3 editorial paragraphs and got 4). All rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-22 after the fstest object open/update slice: 15 rclone test files, 172 behavior tests, 1,980 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the fstest object open/update slice: 145 test files, 12,959 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the fstest list/root/purge slice: 15 rclone test files, 176 behavior tests, 2,006 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the fstest list/root/purge slice: 148 test files, 13,193 assertions, 1 unrelated failure in `lanes/syncthing/tests/IndexHandlerRegistryTest.php` (`updates attached folder index state from received Index and IndexUpdate batches`, expected `3`, actual `4`). All rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-22 after the PublicLink slice: 15 rclone test files, 179 behavior tests, 2,025 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the PublicLink slice: 148 test files, 13,358 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the metadata-set copy/move slice: 15 rclone test files, 183 behavior tests, 2,054 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the metadata-set copy/move slice: 149 test files, 13,543 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-22 after the SetTier/GetTier slice: 15 rclone test files, 187 behavior tests, 2,085 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the SetTier/GetTier slice in a clean temporary worktree containing only the rclone lane changes: 151 test files, 13,689 assertions, 0 failures.
- Concurrent shared dirty-worktree root `php tools/run-tests.php` on 2026-05-22 exited 1 with 152 test files, 13,851 assertions, and 1 unrelated failure in `lanes/readability/tests/ArticleExtractorTest.php` (`preserves named Medium section wrappers for oracle output while flattening WordPress blocks`). All rclone tests in that shared root run passed.
- Rclone-only PHP lane check on 2026-05-22 after the chunksize calculator slice: 16 rclone test files, 197 behavior tests, 2,114 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-22 after the chunksize calculator slice in the shared dirty worktree: 155 test files, 14,074 assertions, 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the sequential chunkedreader slice: 17 rclone test files, 202 behavior tests, 2,147 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the sequential chunkedreader slice in the shared dirty worktree: 159 test files, 14,502 assertions, 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the parallel chunkedreader slice: 18 rclone test files, 206 behavior tests, 2,182 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the parallel chunkedreader slice was executed in the shared dirty worktree and exited 1; latest observed rerun reported 161 test files, 14,679 assertions, and 1 unrelated failure in `lanes/esbuild/tests/TypeScriptModuleLowererTest.php` (`lowers wordpress default exported using asset controller without trapping exports`). All rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-23 after the parallel chunkedreader cleanup slice: 18 rclone test files, 209 behavior tests, 2,190 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the parallel chunkedreader cleanup slice: 162 test files, 14,874 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-23 after the chunkedreader factory slice: 19 rclone test files, 214 behavior tests, 2,209 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the chunkedreader factory slice: first unfiltered shared-worktree run exited 1 with 163 test files, 14,948 assertions, and 4 unrelated concurrent LightningCSS failures; a diagnostic rerun passed with 163 test files, 14,952 assertions, and 0 failures; the final unfiltered rerun passed with 163 test files, 14,952 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the fs/list helper slice: 20 rclone test files, 221 behavior tests, 2,230 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/list helper slice: first streamed shared-worktree run exited 1 with 164 test files, 15,051 assertions, and 1 failure; a captured rerun immediately after passed with 164 test files, 15,058 assertions, and 0 failures.
- Required final root `php tools/run-tests.php` on 2026-05-23 after lane notes/status were updated: 164 test files, 15,116 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-23 after the fs/list filter/sort slice: 21 rclone test files, 227 behavior tests, 2,240 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/list filter/sort slice: 165 test files, 15,307 assertions, 1 failure in unrelated `lanes/esbuild/tests/TypeScriptNamespaceLowererTest.php` (`lowers wordpress namespace scoped disposable preview asset without node`). All rclone tests in that root run passed.
- Required final root `php tools/run-tests.php` rerun on 2026-05-23 after the fs/list filter/sort status update: 165 test files, 15,329 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-23 after the fs/list sorter slice: 22 rclone test files, 234 behavior tests, 2,261 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/list sorter slice first exited 1 in the shared dirty worktree with 167 test files, 15,447 assertions, and 1 uncaptured failure; an immediate captured rerun passed with 167 test files, 15,456 assertions, and 0 failures.
- Latest required root `php tools/run-tests.php` rerun after concurrent non-rclone HEAD/worktree changes exited 1 with 168 test files, 15,482 assertions, and 1 unrelated `lanes/markerpdf/tests/BlockStructureTest.php` failure (`<h2>## Migration Checklist</h2>` still retained markdown heading markers).
- Rclone-only PHP lane check on 2026-05-23 after the fs/list DirSortedFn slice: 22 rclone test files, 239 behavior tests, 2,273 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/list DirSortedFn slice: 169 test files, 15,599 assertions, 0 failures in the current shared dirty worktree.
- Required final root `php tools/run-tests.php` rerun on 2026-05-23 after concurrent non-rclone changes: 170 test files, 15,646 assertions, 1 failure in unrelated `lanes/syncthing/tests/PullTemporaryFileTest.php` (`performFinish schedules a scan instead of deleting directory with unknown children`, expected only `wp-content/uploads/2026/generated-gallery/thumbs/local-only.jpg` but also reported the parent directory). All rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-23 after the fs/list DirSorted slice: 22 rclone test files, 243 behavior tests, 2,293 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/list DirSorted slice: 170 test files, 15,764 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-23 after the fs/walk Walk/ListR fallback slice: 22 rclone test files, 250 behavior tests, 2,315 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/walk slice exited 1 in the current shared dirty worktree with 171 test files, 15,925 assertions, and 2 unrelated failures; a failure-only rerun reported 171 test files, 15,937 assertions, and the same unrelated failures in `lanes/esbuild/tests/TypeScriptModuleLowererTest.php` (`lowers wordpress async generator asset queue class cleanup without node`) and `lanes/readability/tests/ArticleExtractorTest.php` (`honors upstream keepClasses extraction option`). All rclone tests in those runs passed.
- Rclone-only PHP lane check on 2026-05-23 after the direct fs/walk ListR/dirMap slice: 22 rclone test files, 255 behavior tests, 2,340 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the direct fs/walk ListR/dirMap slice: 172 test files, 16,079 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-23 after the direct fs/walk walkRDirTree/NewDirTree slice: 22 rclone test files, 261 behavior tests, 2,357 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the direct fs/walk walkRDirTree/NewDirTree slice: 174 test files, 16,248 assertions, 0 failures in the current shared dirty worktree.
- Rclone-only PHP lane check on 2026-05-23 after the direct fs/walk walkR/NewDirTree selection slice: 22 rclone test files, 265 behavior tests, 2,376 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the direct fs/walk walkR/NewDirTree selection slice: 174 test files, 16,366 assertions, 0 failures in the current shared dirty worktree.
- Later required root `php tools/run-tests.php` reruns on 2026-05-23 after unrelated shared-worktree movement ended red. An intermediate captured rerun reported 174 test files, 16,394 assertions, and 5 unrelated failures across `lanes/esbuild/tests/TypeScriptModuleLowererTest.php` and `lanes/quadrable/tests/QuadbStoreTest.php`; the latest captured rerun reported 174 test files, 16,443 assertions, and 4 unrelated `lanes/esbuild/tests/TypeScriptModuleLowererTest.php` async-generator lowering failures. All rclone tests in those reruns passed.
- Current required root `php tools/run-tests.php` rerun on 2026-05-23 after the direct fs/walk walkR/NewDirTree selection slice: 174 test files, 16,519 assertions, 0 failures in the current shared dirty worktree. Rclone-only verification in this run remains 22 rclone test files, 2,376 assertions, 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the fs/walk GetAll slice: 22 rclone test files, 270 behavior tests, 2,396 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/walk GetAll slice: 176 test files, 16,637 assertions, 0 failures in the current shared dirty worktree.
- Final required root `php tools/run-tests.php` rerun on 2026-05-23 after metadata updates and shared-worktree movement exited 1 with 176 test files, 16,713 assertions, and 2 unrelated failures in `lanes/dolt/tests/PatchFunctionCallTest.php` (`dolt patch function call materializes default generated and on update column snapshot deltas` and `wordpress patch generated default review example exposes import queue ddl`). All rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-23 after the fs/walk no-traverse/files-from NewDirTree slice: 22 rclone test files, 273 behavior tests, 2,414 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-files-from-no-traverse-restore.php`: source `filesFrom`, 4 explicit lookups, 0 provider List calls, 0 provider ListR calls, 3 listed objects, and the missing WXR entry skipped.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/walk no-traverse/files-from NewDirTree slice: 176 test files, 16,753 assertions, 0 failures in the current shared dirty worktree.
- Final captured root `php tools/run-tests.php` rerun on 2026-05-23 after metadata updates exited 1 with 177 test files, 16,873 assertions, and 2 unrelated failures in `lanes/syncthing/tests/PullFinisherTest.php` (`finisherRoutine promotes successful pulls completes queue and emits item finished` and `finisherRoutine leaves receive-encrypted progress emitter registrations alone`). All rclone tests in that root run passed.
- Rclone-only PHP lane check on 2026-05-23 after the fs/march no-traverse destination lookup slice: 22 rclone test files, 276 behavior tests, 2,440 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-no-traverse-copy.php`: 4 destination object probes, 2 matches, 2 misses, targetListUsed=false, copied `database/site.sql`, `wp-content/uploads/2026/05/hero.jpg`, and `wp-content/uploads/2026/05/hero.webp`, with cache left untouched.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/march no-traverse destination lookup slice exited 1 outside rclone. The full-output run reported 178 test files, 17,126 assertions, and 2 failures; a filtered rerun reported 178 test files, 17,135 assertions, and the same 2 failures in `lanes/libsqlite/tests/SQLiteHeaderTest.php` (`decodes focused sqlite jsonb blobs for json expression verification`) and `lanes/markerpdf/tests/SuppliedDocumentConverterTest.php` (`rejects malformed supplied document options before benchmark import`). All rclone tests in the root runs passed.
- Current required root `php tools/run-tests.php` rerun on 2026-05-23 after shared-worktree movement and the fs/march no-traverse destination lookup slice: 178 test files, 17,211 assertions, 0 failures.
- Rclone slice hygiene checks on 2026-05-23: `lanes/rclone/UPSTREAM_TEST_MANIFEST.json` and `lanes/rclone/lane-status.json` parse as JSON; `git diff --check -- lanes/rclone` passes.
- Rclone-only PHP lane check on 2026-05-23 after the fs/march matchListings duplicate-source slice: 22 rclone test files, 278 behavior tests, 2,449 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-duplicate-source-listing.php`: changedBeforeCopy contains only `database/site.sql`, copied contains only `database/site.sql`, `exports/site.wxr` remains the first listed export bytes, and `database/site.sql` is copied.
- Required root `php tools/run-tests.php` on 2026-05-23 after the fs/march matchListings duplicate-source slice: 180 test files, 17,327 assertions, 0 failures in the current shared dirty worktree.
- Focused upstream no-traverse/delete-mode read on 2026-05-23 used targeted `git show` reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/sync/sync.go`, `fs/march/march.go`, and `fs/sync/sync_test.go`. Counted boundaries: sync delete modes disable `--no-traverse` before transfer matching, no-traverse destination `NewObject` probes are not used after disablement, track-renames keeps destination traversal for candidate discovery, and `--delete-before` plus `--track-renames` is fatal before the two-pass sync starts.
- Rclone-only PHP lane check on 2026-05-23 after the no-traverse sync delete-mode slice: 22 rclone test files, 281 behavior tests, 2,474 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the no-traverse sync delete-mode slice first streamed an uncaptured single failure during shared-worktree movement; an immediate captured rerun passed with 182 test files, 17,593 assertions, 0 failures in the current shared dirty worktree.
- Focused upstream delete-before/no-traverse follow-up on 2026-05-23 used targeted `git show` reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/sync/sync.go` `runSyncCopyMove`, `newSyncCopyMove`, and `run`, plus `fs/sync/sync_test.go` `TestSyncDeleteBefore` and `TestCopyDeleteBefore`. Counted boundaries: `--delete-before` runs an initial delete-only pass with destination traversal, rejects `--track-renames` before starting, then runs a copy-only pass where delete mode is off and `--no-traverse` object probes can be used.
- Rclone-only PHP lane check on 2026-05-23 after the delete-before no-traverse two-pass slice: 22 rclone test files, 283 behavior tests, 2,495 assertions, 0 failures.
- Required root `php tools/run-tests.php` on 2026-05-23 after the delete-before no-traverse two-pass slice and lane metadata updates: 183 test files, 17,804 assertions, 0 failures in the current shared dirty worktree.
- Focused upstream matchListings duplicate-destination follow-up on 2026-05-23 used targeted `git show` reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/march/march.go` `matchListings` and `processJob`, plus `fs/march/march_test.go` `TestMatchListings`. Counted boundaries: duplicate source and destination entries are ignored after the first same-key object, upstream logs `Duplicate object found in destination - ignoring`, file/directory pairs are not duplicates, and transformed duplicate keys keep the first stable-sort entry for matching.
- Rclone-only PHP lane check on 2026-05-23 after the matchListings duplicate-destination diagnostics slice: 22 rclone test files, 285 behavior tests, 2,513 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-duplicate-destination-diagnostics.php`: matched `exports/site.wxr`, reported `database/site.sql` as source-only/copied, reported one duplicate destination path `exports/site.wxr` with message `Duplicate object found in destination - ignoring`, kept the canonical WXR bytes, and preserved excluded cache.
- Required root `php tools/run-tests.php` on 2026-05-23 after the matchListings duplicate-destination diagnostics slice first exposed one unrelated transient `lanes/markerpdf/tests/SuppliedDocumentConverterTest.php` failure, then passed once with 183 test files, 17,934 assertions, and 0 failures. The final required same-snapshot root rerun exited 1 outside rclone with 183 test files, 17,911 assertions, and 2 failures in `lanes/quadrable/tests/QuadbStoreTest.php` (`native quadb store restores full-head raw LMDB cursor dumps without portable state` and `native quadb store restores upstream full-head LMDB cursor slices and rejects proof witnesses`), both caused by `QuadbStore::__construct()` receiving null `partialProofHeads` from currently dirty Quadrable files. A separate root run was active after that, so no further root rerun was started.
- Focused upstream delete-before backup/max-delete follow-up on 2026-05-23 used targeted `git show` reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/sync/sync.go` `runSyncCopyMove`, `newSyncCopyMove`, `run`, `startDeleters`, and `deleteFiles`; `fs/sync/sync_test.go` `TestSyncDeleteBefore` and the `TestSyncBackupDir*` family; `fs/operations/operations.go` `DeleteFileWithBackupDir` and `DeleteFilesWithBackupDir`; `fs/operations/operations_test.go` `TestMaxDelete*`; and `fs/accounting/stats.go` max-delete guard branches. Counted boundaries: delete-before's delete-only pass uses backup-dir moves, max-delete/max-delete-size failures stop before the copy-only pass starts, and the subsequent copy-only pass still moves overwritten destination objects into backup-dir before replacement.
- Rclone-only PHP lane check on 2026-05-23 after the delete-before backup/max-delete slice: 22 rclone test files, 288 behavior tests, 2,537 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-delete-before-backup-limit.php`: max-delete reported `--max-delete threshold reached`, no copy-pass stats were produced, the first stale WXR was archived to `archive/2026-05-22/exports/old-site-previous.wxr`, the stale upload and stale published WXR remained in place, the missing SQL dump was not copied, and excluded cache remained untouched.
- Required root `php tools/run-tests.php` on 2026-05-23 after the delete-before backup/max-delete slice: 183 test files, 18,085 assertions, 0 failures in the current shared dirty worktree.
- Focused upstream matchListings directory/file partition follow-up on 2026-05-23 used targeted `git show` reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/march/march.go` `matchListings`, `fs/march/march_test.go` `TestMatchListings` file/directory cases, and `fs/direntries.go` `DirEntryType` / `CompareDirEntries`. Counted boundaries: same-remote directories and objects sort as separate entries, duplicate detection is scoped to the same transformed remote and same DirEntry type, later duplicate directories are ignored after the first listed directory, and upstream logs `Duplicate directory found in destination - ignoring`.
- Rclone-only PHP lane check on 2026-05-23 after the matchListings directory/file partition slice: 22 rclone test files, 291 behavior tests, 2,554 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-duplicate-directory-file-diagnostics.php`: `wp-content/uploads/2026/05` is reported as both a directory match and object match, the interrupted restore directory is reported as a duplicate destination directory, the canonical directory ID remains `published-month`, the ignored directory ID is `interrupted-restore-month`, and the same-path marker object's SHA-256 is preserved.
- Required root `php tools/run-tests.php` on 2026-05-23 after the matchListings directory/file partition slice: 183 test files, 18,316 assertions, 0 failures in the current shared dirty worktree.
- Focused upstream matchListings order-guard follow-up on 2026-05-23 used targeted `git show` reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/march/march.go` `matchListings` and `fs/direntries.go` `DirEntryType` ordering. Counted boundaries: decreasing source keys panic upstream as `Out of order listing in source`, decreasing destination keys panic upstream as `Out of order listing in destination`, and same-remote objects listed before directories violate the directory-before-object sort order.
- Rclone-only PHP lane check on 2026-05-23 after the matchListings order-guard slice: 22 rclone test files, 294 behavior tests, 2,561 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-matchlisting-order-guard.php`: the unsorted source order `wp-content/uploads/2026/05/hero.jpg`, `database/site.sql`, `exports/site.wxr` reports `Out of order listing in source`, produces no trusted matches, and leaves excluded cache untouched.
- Required root `php tools/run-tests.php` on 2026-05-23 after checking no active root harness exited 1 with 183 test files, 18,524 assertions, and 1 unrelated failure in `lanes/difftastic/tests/TokenDifferTest.php` (`wordpress plugin directory json diff includes hidden tooling and filters unchanged files`; missing `WordPress/WordPress#6.6`). All rclone tests in that root run passed. The rclone order-guard batch remains uncommitted because the required root harness is red outside this lane.
- Focused upstream delete-before empty-directory prune follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/sync/sync.go` `DstOnly`, `deleteEmptyDirectories`, and `runSyncCopyMove`; `fs/sync/sync_test.go` `TestSyncEmptyDirectories`, `TestSyncNoEmptyDirectories`, `TestSyncDeleteBefore`, and related empty-directory tests; plus `fs/operations/operations.go` `Rmdirs` and `fs/operations/operations_test.go` `TestRmdirsNoLeaveRoot`, `TestRmdirsLeaveRoot`, and `TestRmdirsWithFilter`. Counted boundaries: sync records destination-only directory candidates before deleting files, delete-before prunes those candidates after the delete-only pass, directory cleanup runs deepest-first, TryRmdir-style non-empty/missing errors are ignored by sync cleanup, and backup-dir paths created by file moves are not pruned.
- Rclone-only PHP lane check on 2026-05-23 after the delete-before empty-directory prune slice: 22 rclone test files, 296 behavior tests, 2,577 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-delete-before-empty-dir-prune.php`: stale WXR and upload artifacts are moved into `archive/2026-05-22`, empty `exports/retired` and `wp-content/uploads/2024` destination directories are pruned, backup archive directories remain visible, and excluded cache remains untouched.
- Accidental aggregate root `php tools/run-tests.php` on 2026-05-23 exited 1 with 183 test files, 18,661 assertions, and 3 failures; detailed failure lines were truncated by tool output. Subsequent required root preflights found active root harnesses, first PID 454065 and later PID 467834 (`php tools/run-tests.php`, parent 467833, elapsed 02:06 when sampled), so no duplicate root run was started and the root result is pending for the supervisor/integrator.
- Focused upstream standalone Rmdirs follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/operations/operations.go` `Rmdirs` and `TryRmdir`; `fs/operations/operations_test.go` `TestRmdirsNoLeaveRoot`, `TestRmdirsLeaveRoot`, and `TestRmdirsWithFilter`; `fs/list/list.go` filtered `DirSorted` behavior; `fs/filter/filter.go` `IncludeDirectory` and trailing-slash `IncludeRemote` candidate behavior; and `lib/errcount/errcount.go`. Counted boundaries: filtered-walk emptiness maps, deepest-first deletion groups, root removal after all children are empty, leave-root preservation, filter includeRemote candidate matching, and errcount-shaped reporting when filtered-out provider entries make Rmdir fail.
- Rclone-only PHP lane check on 2026-05-23 after the standalone Rmdirs slice: 22 rclone test files, 301 behavior tests, 2,598 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-rmdirs-upload-prune.php`: empty `wp-content/uploads/2024/01` and `wp-content/uploads/2024` directories are pruned, `wp-content/uploads` is preserved as the leave-root directory, `wp-content/uploads/2026/05/hero.jpg` remains present, and excluded cache remains untouched.
- Required root preflight on 2026-05-23 found no active `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 183 test files, 18,888 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream standalone Rmdir/TryRmdir follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/operations/operations.go` `TryRmdir`, `Rmdir`, and `SkipDestructive`; `fs/accounting/stats.go` `DeletedDirs` and error accounting; `fs/accounting/accounting.go` `DryRun`; and `fstest/fstests/fstests.go` `FsRmdirNotFound`, `FsRmdirEmpty`, and `FsMkdirRmdirSubdir`. Counted boundaries: deleted-directory attempts are accounted before dry-run checks, TryRmdir returns provider errors without counting them, Rmdir counts provider errors, dry-run skips provider Rmdir calls even for missing directories, and normal empty-directory removal mutates the provider.
- Rclone-only PHP lane check on 2026-05-23 after the standalone Rmdir/TryRmdir slice: 22 rclone test files, 306 behavior tests, 2,622 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-rmdir-dry-run-preflight.php`: dry-run records two deleted-directory attempts and skips both provider calls, stale `wp-content/uploads/2024/01` remains after dry-run, the later real Rmdir removes it, a missing `wp-content/uploads/2023/12` Rmdir records one counted error, and current media bytes remain intact.
- Required root preflight on 2026-05-23 found no active `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 183 test files, 19,032 assertions, and 0 failures in the current shared dirty worktree. An earlier accidental root run before the example fix exited with 1 failure from this lane's new WordPress example; the focused rclone rerun and final root rerun both passed after the fix.
- Focused upstream standalone operations.Purge follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/operations/operations.go` `Purge`, `listToChan`, `DeleteFiles`, `Rmdirs`, and `SkipDestructive`; `fs/operations/operations_test.go` `TestPurge`; `fs/fs.go` `ErrorCantPurge`; `cmd/purge/purge.go`; and `fstest/fstests/fstests.go` `FsPurge`/`FsPurgeRoot`. Counted boundaries: direct provider purge increments deleted-directory accounting once, direct dry-run skips the provider call and does not fallback, `ErrorCantPurge` falls back to DeleteFiles plus Rmdirs, fallback dry-run accounts listed object delete attempts and empty-directory removal attempts without mutation, and fatal direct purge errors are counted without fallback.
- Rclone-only PHP lane check on 2026-05-23 after the standalone operations.Purge slice: 22 rclone test files, 312 behavior tests, 2,661 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-purge-fallback-preflight.php`: direct purge dry-run records one deleted-directory attempt without falling back or mutating thumbnails, the later real purge falls back from `can't purge directory`, removes the generated thumbnail objects and `thumbs` directory, and preserves current media plus the WXR export.
- Required root preflight on 2026-05-23 found no active `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 183 test files, 19,288 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream operations.Delete command follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `cmd/delete/delete.go`, `fs/operations/operations.go` `Delete`, `ListFn`, `DeleteFiles`, and `DeleteFileWithBackupDir`; `fs/operations/operations_test.go` `TestDelete`, `TestMaxDelete`, `TestMaxDeleteSizeLargeFile`, and `TestMaxDeleteSize`; `fs/accounting/stats.go` `DeleteFile`; and `fs/filter/filter.go` filter size boundaries. Counted boundaries: ListFn applies filters before delete candidates reach DeleteFiles, dry-run records object delete attempts and bytes without provider mutation, ordinary provider delete errors aggregate as `failed to delete N files`, and max-delete threshold failures surface as an aggregate command error after counted successful deletes.
- Rclone-only PHP lane check on 2026-05-23 after the operations.Delete command slice: 22 rclone test files, 316 behavior tests, 2,689 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-delete-command-preflight.php`: dry-run lists 4 provider objects, accounts one 120-byte large cache delete without mutation, the apply pass deletes only `wp-content/cache/page/rendered-block-fragment.bin`, and small cache, current media, and WXR export bytes are preserved.
- Required root preflight on 2026-05-23 found active root harness PID 787347 (`php tools/run-tests.php`) and focused PID 787841 (`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`). The processes ended before owner sampling with `ps`, so no duplicate root run was started; root verification for this lane batch is pending for the supervisor/integrator.
- Focused upstream operations.CleanUp/cmd cleanup follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `cmd/cleanup/cleanup.go`, `fs/operations/operations.go` `CleanUp`, `fs/features.go` `CleanUpper` feature wiring/masking, `fs/operations/rc.go`, `fs/operations/rc_test.go` `TestRcCleanup`, and a static backend scan. Counted boundaries: unsupported providers error before dry-run is checked, supported dry-run suppresses provider cleanup calls, provider cleanup clears hidden trash/old-version entries outside ordinary listings, and provider cleanup errors propagate without clearing hidden entries.
- Rclone-only PHP lane check on 2026-05-23 after the operations.CleanUp command slice: 22 rclone test files, 321 behavior tests, 2,724 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-cleanup-empty-trash.php`: dry-run records one cleanup skip without calling the provider and leaves trashed WXR/upload entries intact, the apply pass clears two hidden trash objects and two hidden trash directories, and current WXR plus upload bytes remain visible.
- Required root preflight on 2026-05-23 found active root harness PID 861697 (`php tools/run-tests.php`). The process ended before `ps` owner sampling, so no duplicate root run was started; root verification for this lane batch is pending for the supervisor/integrator.
- Focused upstream operations.Cat/Rcat follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `cmd/cat/cat.go`, `cmd/rcat/rcat.go`, `fs/operations/operations.go` `Cat`, `Rcat`, and `RcatSize`, plus `fs/operations/operations_test.go` `TestCat`, `TestRcat`, `TestRcatMetadata`, `TestRcatSize`, `TestRcatSizeMetadata`, and `TestRcatSizeUploadHeaders`. Counted boundaries: cat offset/count and negative tail reads, trailing separators, command head/tail/offset conflict handling, discard output while still opening objects, filter-before-open behavior, rcat small Put vs streaming PutStream selection, metadata preservation, checksum-ignore behavior, known-size RcatSize Put, unknown-size RcatSize delegation, and input close behavior.
- Rclone-only PHP lane check on 2026-05-23 after the Cat/Rcat slice: 23 rclone test files, 329 behavior tests, 2,777 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-cat-rcat-wxr-stream.php`: native rcat uploads `exports/site.wxr` through the streaming path, the filtered cat manifest emits SQL plus WXR artifacts with trailing newlines while excluding cache, and a negative-offset cat tail returns the closing `</rss>` marker.
- Required root preflight on 2026-05-23 found active root harness PID 1076399 owned by `claude` (`php tools/run-tests.php`, parent 895902, elapsed 19 seconds when sampled). No duplicate root run was started; root verification for this Cat/Rcat lane batch is pending for the supervisor/integrator.
- Focused upstream operations.CopyURL/cmd copyurl follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `cmd/copyurl/copyurl.go`, `cmd/copyurl/copyurl_test.go`, `fs/operations/operations.go` `copyURLFn`, `CopyURL`, and `CopyURLToWriter`, plus `fs/operations/operations_test.go` `TestCopyURL`, `TestCopyURLDownloadHeaders`, and `TestCopyURLToWriter`. Counted boundaries: explicit destination uploads, no-clobber existing-file errors, auto filename from final URL after redirects, Content-Disposition filename parsing with folder/backslash cleanup, missing URL/header filename errors, non-2xx status errors, DownloadHeaders request propagation, Last-Modified modtime propagation, stdout writer mode, command missing-destination/stdout/print-filename handling, and `--urls` CSV auto-name plus aggregate-error behavior.
- Rclone-only PHP lane check on 2026-05-23 after the CopyURL/copyurl slice: 24 rclone test files, 336 behavior tests, 2,817 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-copyurl-remote-media-import.php`: mocked remote-media import sends WordPress download headers, resolves `hero.jpg` from Content-Disposition, imports to `wp-content/uploads/2026/05/hero.jpg`, preserves Last-Modified as `2026-05-23T13:00:00Z`, prints the final destination filename, and confirms `--no-clobber` preserves an existing image.
- Required root preflight on 2026-05-23 found active root harness PID 1209965 owned by `claude` (`php tools/run-tests.php`, parent 1209964, elapsed 00:14 when sampled). No duplicate root run was started; root verification for this CopyURL lane batch is pending for the supervisor/integrator.
- Focused upstream cmd/touch and operations.TouchDir follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `cmd/touch/touch.go`, `cmd/touch/touch_test.go`, `fs/operations/operations.go` `TouchDir` and `SkipDestructive`, `fs/operations/operations_test.go` `TestTouchDir`, and `fs/accounting/stats.go` error accounting. Counted boundaries: YYMMDD/ISO timestamp parsing, creation of missing empty files with metadata, `--no-create`, recursive missing-path no-create behavior, empty/root directory touches, existing-file SetModTime, nonrecursive directory touches, recursive filtered TouchDir, dry-run skip accounting, directory SetModTime errors counted without aborting later objects, and single-file SetModTime errors returned to the caller.
- Rclone-only PHP lane check on 2026-05-23 after the cmd/touch/TouchDir slice: 25 rclone test files, 345 behavior tests, 2,870 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-touch-media-timestamps.php`: the dry-run recursive upload timestamp repair records two skipped media updates without mutating objects, the apply pass touches only `wp-content/uploads/2026/05/gallery.jpg` and `wp-content/uploads/2026/05/hero.jpg`, WXR and cache modtimes remain unchanged, and recursive missing upload paths do not create files.
- Required root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 189 test files, 20,443 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream cmd/move and cmd/moveto follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `cmd/move/move.go`, `cmd/moveto/moveto.go`, `fs/operations/operations.go` `moveOrCopyFile`, `MoveFile`, and `MoveBackupDir`, plus `fs/sync/sync.go` `MoveDir`, `runSyncCopyMove`, and `deleteEmptyDirectories`. Counted boundaries: `move` file sources keep the source leaf under the destination directory, `moveto` file sources use the exact destination path, directory sources share MoveDir dispatch, provider DirMove is tried before object-by-object fallback, fallback can create empty destination directories, and `--delete-empty-src-dirs` prunes source dirs while ignoring TryRmdir-style missing/non-empty errors.
- Rclone-only PHP lane check on 2026-05-23 after the cmd/move/cmd/moveto slice: 26 rclone test files, 350 behavior tests, 2,915 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-move-command-media-relocation.php`: `move` sends `hero.jpg` to `archive/media/hero.jpg`, `moveto` sends a renamed media file to exact `archive/media/hero-renamed.jpg`, the overwritten archive object is preserved as `archive/media/hero-renamed.bak.jpg`, a filtered uploads subtree is moved to `archive/uploads/2026/05/hero.jpg`, and cache remains unmoved.
- Required root preflight on 2026-05-23 found active exact root harness PID 1326734 (`php tools/run-tests.php`), and a later final check found active exact root harness PID 1331413. Both processes ended before `ps` owner sampling, so no duplicate root run was started; root verification for this lane batch remains pending for the supervisor/integrator.
- Focused upstream cmd/copy and cmd/copyto follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `cmd/copy/copy.go`, `cmd/copyto/copyto.go`, `fs/sync/sync.go` `CopyDir` and `runSyncCopyMove`, and `fs/sync/sync_test.go` `TestCopy`, `TestCopyEmptyDirectories`, and `TestCopyNoEmptyDirectories`. Counted boundaries: `copy` file sources keep the source leaf under the destination directory, `copyto` file sources use the exact destination path, directory sources copy source directory contents rather than the source directory name, `copy --create-empty-src-dirs` creates explicit empty destination directories with metadata, and `copyto` directory mode leaves empty source directories uncreated.
- Rclone-only PHP lane check on 2026-05-23 after the cmd/copy/cmd/copyto slice: 27 rclone test files, 355 behavior tests, 2,960 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-copy-command-media-promotion.php`: `copy` sends `hero.jpg` to `publish/media/hero.jpg`, `copyto` sends a renamed media file to exact `publish/media/hero-renamed.jpg`, the overwritten publish object is preserved as `publish/media/hero-renamed.bak.jpg`, a filtered uploads subtree is copied to `publish/uploads/2026/05/hero.jpg`, local source artifacts remain present, and cache remains uncopied.
- Required root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process before this lane's first root run. That root run passed with 191 test files, 20,759 assertions, and 0 failures in the current shared dirty worktree. A post-status root rerun then exited 1 with 192 test files, 20,789 assertions, and 4 failures; the visible failures are outside this lane in `lanes/difftastic/tests/TokenDifferTest.php` ANSI color expectations where bright `91`/`92`/`93` color codes were produced instead of expected `31`/`32`/`33` codes. A final duplicate-root sample found active root harness PID 1445402 owned by `claude` (`php tools/run-tests.php`, parent 1352779), so no further duplicate run was started.
- Focused upstream command/file dry-run follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/operations/operations.go` `SkipDestructive`, `move`, `moveOrCopyFile`, `MoveFile`, `CopyFile`, and `MoveCaseInsensitive`; `fs/operations/operations_test.go` `TestCaseInsensitiveMoveFileDryRun`; `cmd/copy/copy.go`, `cmd/copyto/copyto.go`, `cmd/move/move.go`, `cmd/moveto/moveto.go`; and `fs/sync/sync.go` `CopyDir`/`MoveDir` mutation boundaries. Counted boundaries: dry-run records backup-dir plus transfer intent without moving existing destinations, skips copy/move provider mutations, suppresses case-insensitive temporary renames, skips copy `--create-empty-src-dirs` placeholders, and prevents move source deletion/source-directory pruning.
- Rclone-only PHP lane check on 2026-05-23 after the copy/move dry-run slice: 27 rclone test files, 360 behavior tests, 3,003 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-copy-move-dry-run-preflight.php`: copyto dry-run records backup plus copy intent for `publish/media/hero-renamed.jpg`, moveto dry-run records backup plus move intent for `archive/media/hero-renamed.jpg`, directory-copy dry-run records copy intent for `publish/uploads/hero.jpg`, the existing remote publish/archive objects remain unchanged, no dry-run backup object is created, and local staging artifacts remain present.
- Required root preflight on 2026-05-23 found active exact root harness PID 1476747 owned by `claude` (`php tools/run-tests.php`, parent 1470444, elapsed 00:08 when sampled). No duplicate root run was started; root verification for this lane batch remains pending for the supervisor/integrator.
- Focused upstream command/file interactive follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/operations/operations.go` `SkipDestructive`, `skipDestructiveChoose`, `move`, `MoveBackupDir`, `moveOrCopyFile`, `MoveFile`, and `CopyFile`; `fs/operations/operations_test.go` `TestCaseInsensitiveMoveFileDryRun` as the closest upstream dry-run guard; and `cmd/copy/copy.go`, `cmd/copyto/copyto.go`, `cmd/move/move.go`, and `cmd/moveto/moveto.go` command help boundaries. Counted boundaries: interactive `n` skips one transfer without mutation, `s` caches skip-all for repeated action strings such as `move into backup dir`, `!` caches do-all, `q` maps to an exception instead of `os.Exit`, a skipped backup-dir move still permits a later copy/move overwrite like upstream's `MoveBackupDir` call site, and a skipped post-backup move leaves the source plus archived destination state visible for review.
- Rclone-only PHP lane check on 2026-05-23 after the command/file interactive slice: 27 rclone test files, 365 behavior tests, 3,049 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-interactive-copy-move-preflight.php`: the interactive copy choice preserves `publish/media/hero-renamed.jpg`, the interactive backup-dir skip-all choice prevents `archive/media/hero-renamed.bak.jpg` creation, the archive move proceeds to `archive/media/hero-renamed.jpg`, the skipped copy source remains in local staging, and the moved archive source is removed.
- Required root preflight on 2026-05-23 found active exact root harness PID 1536245 owned by `claude` (`php tools/run-tests.php`, parent 1470705, elapsed 00:14 when sampled). No duplicate root run was started; root verification for this lane batch remains pending for the supervisor/integrator.
- Focused upstream OneDrive/SugarSync provider-copy guard-order follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `Copy`, `backend/sugarsync/sugarsync.go` `Copy`, and `fs/operations/operations.go` `RemoveExisting`. Counted boundaries: OneDrive calls `RemoveExisting` before rejecting same-lowercase source/destination copies, cleanup restores the temporarily saved destination when the guard fails, direct-move unavailability is reported before the OneDrive guard, and SugarSync keeps the existing guard-before-remove branch.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive/SugarSync provider-copy guard-order slice: 27 rclone test files, 368 behavior tests, 3,065 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-casefold-copy-guard.php`: a case-insensitive provider rejects `exports/site.wxr` to `EXPORTS/SITE.WXR`, restores the temporarily moved WXR export with its metadata, removes the `.wpcopy` temporary object, and reports the direct-move blocker before the guard when the provider cannot save the existing destination.
- Required root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then exited 1 with 196 test files, 21,450 assertions, and 1 unrelated failure in `lanes/difftastic/tests/TokenDifferTest.php` (`maps upstream side by side novel spans with ansi colors`, expected bright ANSI color sequence drift). All rclone tests in that root run passed.
- Focused upstream MoveBackupDir collision follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/operations/operations.go` `MoveBackupDir`, `Move`, `DeleteFile`, and `moveOrCopyFile`; `fs/operations/copy_test.go` `TestCopyFileBackupDir`; `fs/sync/sync.go` backup-dir replacement call sites; and `fs/sync/sync_test.go` `TestSyncBackupDir*`. Counted boundaries: MoveBackupDir computes the suffix path, looks up any existing backup object, deletes that object through Move before moving the overwritten destination there, preserves the overwritten destination object's metadata/modtime/MIME data, and stops before moving when the delete fails.
- Rclone-only PHP lane check on 2026-05-23 after the MoveBackupDir collision slice: 27 rclone test files, 371 behavior tests, 3,083 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-backup-dir-collision.php`: a stale previous media archive path is replaced with the current published upload bytes, published modtime and metadata are preserved in the archive, fresh staged media bytes are published to the live path, and the local staged source remains available.
- Required root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process before the first root run. The streamed root run exited 1 with 196 test files, 21,579 assertions, and 1 failure. After a later active root PID 1908018 owned by `claude` cleared, a captured rerun in `/tmp/port-libs-root-rclone-collision.out` exited 1 with 196 test files, 21,611 assertions, and 1 unrelated failure in `lanes/readability/tests/ArticleExtractorTest.php` (`maps Mozilla engadget fixture with review gallery and buy chrome cleanup`; missing spaces before `Hardware` and `Wrap-up`). All rclone tests passed in the captured root run.
- Focused upstream CopyFile max-transfer follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/operations/copy.go` `copy.checkLimits`, `fs/operations/copy_test.go` `TestCopyFileMaxTransfer`, `fs/accounting/accounting.go` max-transfer errors, `fs/cutoffmode.go`, `fs/cutoffmode_test.go`, and `fs/config.go` `MaxTransfer`/`CutoffMode` wiring. Counted boundaries: hard cutoff reports `max transfer limit reached as set by --max-transfer` and leaves no destination for oversized copies, cautious cutoff refuses transfers that would reach the limit before starting, soft cutoff permits the current oversized copy, and soft cutoff refuses later copies once completed bytes are already at the limit.
- Rclone-only PHP lane check on 2026-05-23 after the CopyFile max-transfer slice: 27 rclone test files, 373 behavior tests, 3,100 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-copy-max-transfer.php`: the small WXR copy succeeds, hard and cautious large WXR copies fail without creating destination objects, soft mode publishes the same large WXR, a later soft transfer at the completed-byte limit fails, and the staged large WXR bytes remain available.
- Required root preflight on 2026-05-23 found active exact root harness PIDs 2013608 and 2013655 owned by `claude` (`php tools/run-tests.php`), so this lane did not start a duplicate aggregate root run. Root verification for the CopyFile max-transfer batch remains pending for the supervisor/integrator.
- Focused upstream MoveBackupDir accounting/no-op follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/operations/operations.go` `SameObject`, `Move`, `DeleteFileWithBackupDir`, `MoveBackupDir`, and `moveOrCopyFile`; `fs/operations/copy_test.go` `TestCopyFile` and `TestCopyFileBackupDir`; and `fs/operations/operations_test.go` `TestMoveFile` and `TestMoveFileBackupDir`. Counted boundaries: same provider/root/path copy-or-move returns without source lookup and emits match diagnostics, MoveBackupDir deletes an existing backup object through the same Move/DeleteFile accounting path before moving the overwritten destination, the backup move is counted as a server-side rename/checking transfer, and the moveOrCopyFile backup-dir call records the MissingOnDst logger event after the destination is moved aside.
- Rclone-only PHP lane check on 2026-05-23 after the MoveBackupDir accounting/no-op slice: 27 rclone test files, 377 behavior tests, 3,128 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-backup-accounting-noop.php`: stale media backup deletion is accounted with one deleted file and byte count, the overwritten published upload is logged as moved into the backup path, the backup-dir logger emits `missing-on-dst`, a same-target publish preflight emits `match` with zero accounting, and the fresh published media bytes remain intact.
- Required root gate on 2026-05-23 initially found no exact `php tools/run-tests.php` process, but the root harness lock revealed an active aggregate run. This lane stopped its queued duplicate root process. The concurrent captured root log at `.upstream-cache/dolt/tmp/root-php-after-runner-0804-recorded-final.log` then completed green with 198 test files, 21,824 assertions, and 0 failures.
- Final duplicate-root sample on 2026-05-23 found active exact root harness PID 2090077 owned by `claude` (`php tools/run-tests.php`, parent 2038154, elapsed 00:48 when sampled), so this lane did not start another aggregate root run after the observed green root log.
- Focused upstream OneDrive QuickXor/provider-copy metadata follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/quickxorhash/quickxorhash.go`, `backend/onedrive/quickxorhash/quickxorhash_test.go`, `backend/onedrive/onedrive.go` `setMetaData`/`Copy`, and `fs/hash/hash.go`. Counted boundaries: QuickXor's circular XOR plus file-length checksum, upstream base64 vectors represented as rclone hex hash strings, hashsum base64 formatting, and OneDrive Graph `quickXorHash` base64 decoding during server-side copy metadata refresh.
- Affected focused PHP lane check on 2026-05-23 after the OneDrive QuickXor slice: `HashAndCheckTest.php` plus `ProviderMoveFeatureTest.php` passed with 2 test files, 447 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive QuickXor/provider-copy metadata slice: 27 rclone test files, 379 behavior tests, 3,144 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: OneDrive copied WXR metadata exposes copied ID, source modtime, add-only permission metadata, SHA-1, and QuickXor hash `7d9eb7fc27ebe7034f98f473570308ca800738bc`; Yandex copy metadata exposes `rclone_modified` modtime plus MD5; and OneDrive access-denied copy restores the previous WXR export.
- Required root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 198 test files, 22,048 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream provider-copy async/error-result follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `Copy`/`waitForJob`, `backend/yandex/yandex.go` `copyOrMove`/`waitForJob`, and `backend/sugarsync/sugarsync.go` `Copy`/`readMetaData`/`setMetaData`. Counted boundaries: OneDrive missing `Location`, async status JSON decode errors, `failed`/`deleteFailed` statuses, completed-job metadata-read failures, and timeout errors; Yandex async-info and async-status decode errors plus timeout wrapping under `couldn't copy file`; and SugarSync copied-object ID recovery from either the copy `Location` header or metadata `Ref`, with `no ID found in response` when neither is available.
- Affected focused PHP lane check on 2026-05-23 after the provider-copy async/error-result slice: `HashAndCheckTest.php` plus `ProviderMoveFeatureTest.php` passed with 2 test files, 478 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the provider-copy async/error-result slice: 27 rclone test files, 382 behavior tests, 3,175 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: the provider-copy metadata scenario still exposes OneDrive SHA-1 plus QuickXor and Yandex MD5/rclone_modified refreshes, and now records OneDrive bad async status body errors plus SugarSync missing copied-object-ID restoration while preserving the previous WXR export after each provider failure.
- Required root preflight on 2026-05-23 found active exact root harness PID 2298082 owned by `claude` (`php tools/run-tests.php`, parent 2200064, elapsed 00:05, state `Rs` when sampled), so this lane did not start a duplicate aggregate root run. Root verification for this provider-copy async/error-result batch remains pending for the supervisor/integrator.
- Focused upstream provider-copy metadata validation follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/dropbox/dropbox.go` `Copy`, `backend/onedrive/onedrive.go` `Copy` plus `backend/onedrive/metadata.go` `WritePermissions`/`needsUpdatePermissions`, and `backend/yandex/yandex.go` `Copy`/`readMetaData`/`setMetaData` plus `backend/yandex/api/types.go`. Counted boundaries: Dropbox relocation API errors are wrapped as `copy failed`, OneDrive permission-write errors restore the saved destination unless metadata permissions `failok` is enabled, OneDrive `failok` logs/suppresses permission write errors while completing the copied object, Yandex post-copy metadata rejects directory and unknown resource types, and Yandex rejects invalid `rclone_modified`/`modified` RFC3339Nano timestamps.
- Affected focused PHP lane check on 2026-05-23 after the provider-copy metadata validation slice: `ProviderMoveFeatureTest.php` passed with 1 test file, 309 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the provider-copy metadata validation slice: 27 rclone test files, 386 behavior tests, 3,197 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: the provider-copy metadata scenario now exposes OneDrive permissions `failok` completion, Yandex invalid modtime restoration, Dropbox relocation API error wrapping, OneDrive bad async status body errors, SugarSync missing copied-object-ID restoration, and preserved previous WXR bytes after each provider failure.
- Required root preflight on 2026-05-23 found active exact root harness PID 2449282 owned by `claude` (`php tools/run-tests.php`, parent 2306892, elapsed 00:04, state `R` when sampled), so this lane did not start a duplicate aggregate root run. Root verification for this provider-copy metadata validation batch remains pending for the supervisor/integrator.
- Final duplicate-root sample on 2026-05-23 found active exact root harness PID 2476360 owned by `claude` (`php tools/run-tests.php`, parent 2419198, elapsed 00:51, state `R` when sampled), so this lane still did not start a duplicate aggregate root run.
- Focused upstream provider-copy support-gate/metadata-write follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `Copy`, `backend/dropbox/dropbox.go` `Copy`/`setMetadataForExport`, and `backend/yandex/yandex.go` `SetModTime`/`setCustomProperty`. Counted boundaries: OneDrive rejects personal-to-business/business-to-personal and different-business-drive server-side copies before `RemoveExisting`, Dropbox non-downloadable files expose exportable or list-only metadata with unknown size and no copied hash, and Yandex writes the `rclone_modified` custom property while preserving the previous modtime/metadata if the provider write fails.
- Affected focused PHP lane check on 2026-05-23 after the provider-copy support-gate/metadata-write slice: `ProviderMoveFeatureTest.php` passed with 1 test file, 346 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the provider-copy support-gate/metadata-write slice: 27 rclone test files, 389 behavior tests, 3,234 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: the scenario now also exposes OneDrive unsupported cross-drive rejection before destination removal, Dropbox Paper-style non-downloadable export metadata at `exports/site.html`, Yandex `rclone_modified` custom-property writes, and Yandex custom-property write failure preservation without contacting live providers.
- Hygiene checks on 2026-05-23 after the provider-copy support-gate/metadata-write slice: PHP lint passed for `lanes/rclone/src/SyncPlan.php`, `lanes/rclone/src/MemoryProvider.php`, `lanes/rclone/tests/ProviderMoveFeatureTest.php`, and `lanes/rclone/examples/wordpress-provider-copy-metadata.php`; `git diff --check -- lanes/rclone` passed.
- Required root preflight on 2026-05-23 found active exact root harness PIDs 2521537 and 2522248 owned by `claude` (`php tools/run-tests.php`, parents 2492443 and 2484500, elapsed 00:28/00:25, states `Rs`/`Ss` when sampled), so this lane did not start a duplicate aggregate root run. Root verification for this provider-copy support-gate/metadata-write batch remains pending for the supervisor/integrator.
- Final duplicate-root sample on 2026-05-23 found active exact root harness PID 2536367 owned by `claude` (`php tools/run-tests.php`, parent 2536365, elapsed 00:15, state `R` when sampled), so this lane still did not start a duplicate aggregate root run.
- Focused upstream Dropbox `skip_exports` follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/dropbox/dropbox.go` option help for `skip_exports`/`show_all_exports`, provider `List` listability filtering, `possibleMetadatas`/`NewObject` exact lookup boundaries, `Copy` relocation result metadata handling, `setMetadataForExport`, `exportType.listable`/`exportable`, and `Object.Open`, plus `backend/dropbox/dropbox_internal_test.go` Paper export coverage. Counted boundaries: `skip_exports` marks non-downloadable export objects hidden from ordinary listings, `show_all_exports` keeps non-downloadable objects list-only without adding export extensions, exportable objects add the preferred export extension, and list-only exports cannot be opened as downloads.
- Affected focused PHP lane check on 2026-05-23 after the Dropbox `skip_exports` slice: `ProviderMoveFeatureTest.php` passed with 1 test file, 354 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the Dropbox `skip_exports` slice: 27 rclone test files, 390 behavior tests, 3,242 assertions, 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: the provider-copy metadata scenario now records a Dropbox Paper export copied under `skip_exports`, marks it as hidden, and verifies it is absent from the published `exports/` listing while visible exportable/list-only provider-copy metadata still remains testable without live credentials.
- Hygiene checks on 2026-05-23 after the Dropbox `skip_exports` slice: PHP lint passed for `lanes/rclone/src/MemoryProvider.php`, `lanes/rclone/tests/ProviderMoveFeatureTest.php`, and `lanes/rclone/examples/wordpress-provider-copy-metadata.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 at 09:04:26 UTC found no active exact `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 199 test files, 22,695 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream OneDrive shared-copy fallback follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `fs/operations/copy.go` `serverSideCopy`, `manualCopy`, and `copy` fallback branches, plus `backend/onedrive/onedrive.go` `Copy` and `waitForJob`. Counted boundaries: same-remote-type server-side-across-configs attempts, OneDrive Personal-to-Personal shared-copy attempts that pass preflight, async `AccessDenied_` job status mapping to `ErrorCantCopy`, `RemoveExisting` cleanup restoration, server-side accounting reset before manual fallback, and ordinary streamed copy completion after the fallback.
- Affected focused PHP lane check on 2026-05-23 after the OneDrive shared-copy fallback slice: `ProviderMoveFeatureTest.php` passed with 1 test file, 374 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive shared-copy fallback slice: 27 rclone test files, 392 behavior tests, 3,262 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-shared-copy-fallback.php`: the scenario reports `ErrorCantCopy` fallback from an async access-denied provider copy, streams the shared WXR bytes to `exports/site.wxr`, preserves source bytes and metadata, and leaves no `.wpcopy` temporary object behind.
- Required duplicate-root preflight on 2026-05-23 found active exact root harness PID 2612277 owned by `claude` (`php tools/run-tests.php`, parent 2569759, elapsed 00:10, state `R` when sampled), so this lane did not start a duplicate aggregate root run. Root verification for this OneDrive shared-copy fallback batch remains pending for the supervisor/integrator.
- Final duplicate-root sample on 2026-05-23 found active exact root harness PID 2613382 owned by `claude` (`php tools/run-tests.php`, parent 2613380, elapsed 00:19, state `R` when sampled), so this lane still did not start a duplicate aggregate root run.
- Focused upstream Dropbox list-only open follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/dropbox/dropbox.go` option help for `show_all_exports`, `setMetadataForExport`, `exportType.exportable`, `Object.export`, and `Object.Open`. Counted boundary: `show_all_exports` keeps non-downloadable Paper objects visible as list-only provider entries, but `Open` attempts return `fs.ErrorObjectNotFound` instead of downloading bytes because no export format is selected.
- Affected focused PHP lane check on 2026-05-23 after the Dropbox list-only open slice: `ProviderMoveFeatureTest.php` passed with 1 test file, 385 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the Dropbox list-only open slice: 27 rclone test files, 393 behavior tests, 3,273 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: the provider-copy metadata scenario now records a Dropbox list-only Paper export at `exports/list-only.paper`, confirms it remains listed in `exports/`, and confirms `openReader` returns `Object not found: exports/list-only.paper` without live Dropbox credentials.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 200 test files, 23,006 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream OneDrive remoteItem/shared metadata follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `Copy`, `setMetaData`, and `setSystemMetadata`; `backend/onedrive/metadata.go` `Metadata.Get`; and `backend/onedrive/api/types.go` `RemoteItemFacet`, `SharedType`, `GetID`, `GetFile`, `GetCreatedBy`, and `GetLastModifiedBy`. Counted boundaries: remoteItem `driveId#id` normalization, remoteItem file MIME/hash metadata, created-by and last-modified-by user metadata, shared owner/by/scope/time metadata, and the existing set-source-modtime plus add-only permission copy flow.
- Affected focused PHP lane check on 2026-05-23 after the OneDrive remoteItem/shared metadata slice: `ProviderMoveFeatureTest.php` passed with 1 test file, 412 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive remoteItem/shared metadata slice: 27 rclone test files, 394 behavior tests, 3,300 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: the provider-copy metadata scenario now records a shared OneDrive WXR copy with normalized `shared-drive#shared-export-copy`, remoteItem MIME/hash metadata, created/modified actor metadata, and shared owner/by/scope/time fields without live credentials.
- Required duplicate-root preflight on 2026-05-23 found active exact root harness PID 2671978 owned by `claude` (`php tools/run-tests.php`, parent 2656932, elapsed 00:23, state `R` when sampled), so this lane did not start a duplicate aggregate root run. Root verification for this OneDrive remoteItem/shared metadata batch remains pending for the supervisor/integrator.
- Focused upstream Dropbox export-format validation follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/dropbox/dropbox.go` `exportKnownAPIFormats`, `exportKnownExtensions`, `export_formats` option help, NewFs export-format validation, `chooseExportFormat`, and `setMetadataForExport`. Counted boundaries: known dotless export extensions `html` and `md`, unknown configured format rejection before destination save/restore handling, first configured valid extension winning when Dropbox advertises it, and fallback to Dropbox's first known advertised format when configured valid formats do not match.
- Affected focused PHP lane check on 2026-05-23 after the Dropbox export-format validation slice: `ProviderMoveFeatureTest.php` passed with 1 test file, 428 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the Dropbox export-format validation slice: 27 rclone test files, 395 behavior tests, 3,316 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: the provider-copy metadata scenario now records a Dropbox Paper export where configured `md` wins and the copied remote becomes `exports/markdown.md`, plus an invalid configured `pdf` export format that fails before a `.wpcopy` temporary object is created and preserves the previous Paper export bytes.
- Hygiene checks on 2026-05-23 after the Dropbox export-format validation slice: PHP lint passed for `lanes/rclone/src/SyncPlan.php`, `lanes/rclone/tests/ProviderMoveFeatureTest.php`, and `lanes/rclone/examples/wordpress-provider-copy-metadata.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 203 test files, 23,474 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream OneDrive remoteItem package follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/api/types.go` `GetPackage`/`GetPackageType`, `backend/onedrive/metadata.go` `Metadata.Get`, and `backend/onedrive/onedrive.go` `Object.Open`/`Object.Update` OneNote package guards. Counted boundaries: remoteItem `package.type` normalizes into `package-type` metadata, copied OneNote packages remain listable provider objects, object reads fail with `can't open a OneNote file`, and content updates fail with `can't upload content to a OneNote file`.
- Affected focused PHP lane check on 2026-05-23 after the OneDrive package metadata slice: `ProviderMoveFeatureTest.php` passed with 1 test file, 441 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive package metadata slice: 27 rclone test files, 396 behavior tests, 3,329 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: the provider-copy metadata scenario now records a copied OneDrive OneNote migration notebook with normalized `shared-drive#shared-notes-copy`, `package-type` metadata, a native open error, and a native content-update error without live credentials.
- Hygiene checks on 2026-05-23 after the OneDrive package metadata slice: PHP lint passed for `lanes/rclone/src/SyncPlan.php`, `lanes/rclone/src/MemoryProvider.php`, `lanes/rclone/tests/ProviderMoveFeatureTest.php`, and `lanes/rclone/examples/wordpress-provider-copy-metadata.php`.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 204 test files, 23,583 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream OneDrive SharePoint/documentLibrary copy-gate follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `Copy`, `backend/onedrive/api/types.go` `Drive`/`ItemReference`/`CopyItemRequest`, and `backend/onedrive/onedrive_internal_test.go` copy metadata coverage. Counted boundaries: Personal-to-non-Personal copies return `ErrorCantCopy` before `RemoveExisting`, Business-to-Business copies reject differing drive IDs before `RemoveExisting`, and Business/documentLibrary or documentLibrary/Business copies are allowed even when drive/library IDs differ.
- Affected focused PHP lane check on 2026-05-23 after the OneDrive SharePoint copy-gate slice: `ProviderMoveFeatureTest.php` passed with 1 test file, 458 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive SharePoint copy-gate slice: 27 rclone test files, 397 behavior tests, 3,346 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-provider-copy-metadata.php`: the provider-copy metadata scenario now records a WordPress WXR export copied from a Business drive into a SharePoint document library, verifies the saved `.wpcopy` destination is removed after success, and confirms a documentLibrary-to-Personal copy fails before temporary destination removal while preserving the previous personal export.
- Hygiene checks on 2026-05-23 after the OneDrive SharePoint copy-gate slice: PHP lint passed for `lanes/rclone/src/SyncPlan.php`, `lanes/rclone/tests/ProviderMoveFeatureTest.php`, and `lanes/rclone/examples/wordpress-provider-copy-metadata.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. Required root `php tools/run-tests.php` then passed with 205 test files, 23,780 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream OneDrive direct ListR/shared-folder follow-up on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `readMetaDataForPath`, `ListP`, `ListR`, `itemToDirEntry`, and the shared-folder `listFolder` fallback; `backend/onedrive/api/types.go` `RemoteItemFacet`, `ItemReference.GetID`, `Item.GetID`, `GetName`, `GetFolder`, `GetPackageType`, and `GetParentReference`; and the existing `fs/list.Helper` batching behavior. Counted boundaries: delta ListR skips duplicate IDs, skips deleted items, skips root-directory entries, skips items whose parent ID is not in the directory cache, relies on directories being cached before children, filters scoped subdirectory calls by path prefix, normalizes OneDrive/SharePoint `driveId#id` values, hides OneNote packages unless explicitly exposed, and conventionally recurses into `remoteItem.folder` shared folders.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive direct ListR/shared-folder slice: 28 rclone test files, 402 behavior tests, 3,371 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-shared-listr-restore.php`: the scenario builds a restore manifest from a OneDrive delta listing rooted at `site-backups`, skips a duplicate WXR export, skips a deleted cache object, skips an outside-root SQL object, preserves the shared folder's normalized `owner-drive#shared-review-root` ID and shared metadata, and lists the shared review folder conventionally to include users WXR and upload media without live credentials.
- Required duplicate-root preflight on 2026-05-23 found active exact root harness PID 2972851 owned by `claude` (`php tools/run-tests.php`, parent 2887644, elapsed 00:27, state `R` when sampled), so this lane did not start a duplicate aggregate root run. Root verification for the OneDrive direct ListR/shared-folder slice remains pending for the supervisor/integrator.
- Focused upstream OneDrive delta-token/resume read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `ChangeNotify`, `changeNotifyStartPageToken`, `changeNotifyNextChange`, `buildDriveDeltaOpts`, `changeNotifyRunner`, `getItemFullPath`, and `getRelativePathInsideBase`; plus `backend/onedrive/api/types.go` `DeltaResponse`, `ItemReference`, `Item.GetName`, `Item.GetFile`, `Item.GetFolder`, and `Item.GetParentReference`. Counted boundaries: next resume token parsing from `@odata.deltaLink`, delta request shape `/driveID/root/delta?token=...`, tenant-root URL selection, root-folder skip by empty parent ID, configured-root skip, outside-root suppression, invalid parent-path non-fatal skip, file/directory notification type selection, empty-root inclusion, and remoteItem name normalization.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDriveDeltaCursorTest.php`: 4 behavior tests, 19 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive delta-token/resume slice: 29 rclone test files, 406 behavior tests, 3,390 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-delta-resume-cache.php`: the scenario extracts a seed delta token, builds the next OneDrive delta request, reports changed WXR/upload artifacts under `site-backups`, skips the configured root and outside-site changes, and records invalid parent-path skips without live credentials.
- Hygiene checks on 2026-05-23 after the OneDrive delta-token/resume slice: PHP lint passed for `lanes/rclone/src/OneDriveDeltaCursor.php`, `lanes/rclone/tests/OneDriveDeltaCursorTest.php`, and `lanes/rclone/examples/wordpress-onedrive-delta-resume-cache.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 found active exact root harness PID 2992617 owned by `claude` (`php tools/run-tests.php`, parent 2992616, elapsed 00:30, state `R` when sampled), so this lane did not start a duplicate aggregate root run. Root verification for the OneDrive delta-token/resume slice remains pending for the supervisor/integrator.
- After PID 2992617 exited, a fresh required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. This lane then ran aggregate `php tools/run-tests.php`, which passed with 211 test files, 24,237 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream OneDrive disabled-delta/ListR fallback read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` option `delta`, `Features().ListR` clearing when delta is false, and `ListR`; plus `fs/walk/walk.go` `ListR` and `NewDirTree` selection gates. Counted boundaries: OneDrive does not advertise provider ListR by default when delta is disabled; `fs/walk.ListR` falls back through Walk/List when ListR is unavailable or disabled, files-from is active, `maxLevel` is bounded, exclude-if-present markers are configured, or directory filters are active; and `NewDirTree` avoids provider ListR when files-from filters are active without no-traverse.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/ListDirectoryTest.php`: 49 behavior tests, 184 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive disabled-delta/ListR fallback slice: 29 rclone test files, 410 behavior tests, 3,411 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-disabled-delta-fallback.php`: the scenario records ListR as not advertised, uses ordinary provider List calls for `site-backups` and upload subdirectories, publishes WXR/users WXR/SQL/upload media, prunes cache and generated thumbnail paths, and performs zero delta ListR calls without live credentials.
- Hygiene checks on 2026-05-23 after the OneDrive disabled-delta/ListR fallback slice: PHP lint passed for `lanes/rclone/src/ListDirectory.php`, `lanes/rclone/tests/ListDirectoryTest.php`, and `lanes/rclone/examples/wordpress-onedrive-disabled-delta-fallback.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. This lane then ran aggregate `php tools/run-tests.php`, which failed with 213 test files, 24,459 assertions, and 3 unrelated failures in `lanes/readability/tests/ArticleExtractorTest.php`: two Wikipedia paragraph-count expectations and one TMZ legacy byline whitespace expectation. All rclone tests in the aggregate run passed, so the rclone slice remains uncommitted only because the required root gate is not green.
- Focused upstream OneDrive delta-pagination read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `_listAll`, `listAll`, `ListP`, `ListR`, and `itemToDirEntry`; plus `backend/onedrive/api/types.go` `DeltaResponse` and `ListChildrenResponse` next-link fields. Counted boundaries: first delta request uses `/root/delta` with `$top` equal to list chunk, continuation clears path and parameters while using `@odata.nextLink` as RootURL, page values are filtered for deleted/file/directory modes before item callbacks, helper batching can cross Graph pages, and provider page errors return before final `ListHelper::flush()`.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDriveListRTest.php`: 8 behavior tests, 42 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive delta-pagination slice: 29 rclone test files, 413 behavior tests, 3,428 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-delta-paginated-restore.php`: the scenario follows `@odata.nextLink` to a second Graph page, publishes a 105-entry WXR/SQL/uploads restore manifest, records helper batch sizes `[100, 5]`, and requires no live credentials or provider config.
- Hygiene checks on 2026-05-23 after the OneDrive delta-pagination slice: PHP lint passed for `lanes/rclone/src/OneDriveListR.php`, `lanes/rclone/tests/OneDriveListRTest.php`, `lanes/rclone/examples/wordpress-onedrive-delta-paginated-restore.php`, `lanes/rclone/src/ListDirectory.php`, `lanes/rclone/tests/ListDirectoryTest.php`, and `lanes/rclone/examples/wordpress-onedrive-disabled-delta-fallback.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. This lane then ran aggregate `php tools/run-tests.php`, which passed with 213 test files, 24,589 assertions, and 0 failures in the current shared dirty worktree.
- Focused upstream OneDrive child ListP/listAll pagination read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `_listAll`, `listAll`, `ListP`, `ListR` shared-folder `listFolder`, and `itemToDirEntry`; `backend/onedrive/api/types.go` `ListChildrenResponse` and normalized item accessors; and `fs/list/helpers.go`. Counted boundaries: first child-list request uses `/children` with `$top=listChunk`, `@odata.nextLink` continuations clear path and parameters while using the next link as RootURL, deleted items are skipped before callbacks, `directoriesOnly`/`filesOnly` filters run before callbacks, empty pages stop before following a nextLink, and recursive shared-folder fallback reuses the same ListP provider callable for child directories.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDriveListRTest.php`: 12 behavior tests, 59 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive child ListP pagination slice: 29 rclone test files, 417 behavior tests, 3,445 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-shared-listp-pagination.php`: the scenario follows a shared-folder child `@odata.nextLink`, conventionally recurses into a nested uploads directory with the same ListP callable, skips a deleted cache artifact, and publishes a WXR/SQL/uploads restore manifest without live credentials.
- Hygiene checks on 2026-05-23 after the OneDrive child ListP pagination slice: PHP lint passed for `lanes/rclone/src/OneDriveListR.php`, `lanes/rclone/tests/OneDriveListRTest.php`, and `lanes/rclone/examples/wordpress-onedrive-shared-listp-pagination.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json` passed; `git diff --check -- lanes/rclone` passed.
- Focused upstream OneDrive ListP error propagation read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `_listAll`, `ListP`, `List`, and `ListR` shared-folder `listFolder`; `fs/list/helpers.go` `Add`, `Flush`, and `WithListP`; and `fs/walk/walk.go` direct ListR callback forwarding. Counted boundaries: child `ListP` sends 100-entry helper batches before later provider errors, suppresses pending entries because final `Flush` is skipped after errors, shared-folder fallback discards child `ListP` partials when `f.List` returns an error, and caller callback errors stop the outer recursive ListR immediately.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDriveListRTest.php`: 16 behavior tests, 85 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-shared-listp-error-preflight.php`: the scenario preserves a flushed 100-entry WXR manifest batch, propagates `couldn't list files: Graph shared review page failed`, suppresses the pending shared-folder directory and child partials, and follows the failed child continuation link without OAuth or provider credentials.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive ListP error propagation slice: 29 rclone test files, 421 behavior tests, 3,471 assertions, and 0 failures.
- Hygiene checks on 2026-05-23 after the OneDrive ListP error propagation slice: PHP lint passed for `lanes/rclone/src/OneDriveListR.php`, `lanes/rclone/tests/OneDriveListRTest.php`, and `lanes/rclone/examples/wordpress-onedrive-shared-listp-error-preflight.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 found no active exact root harness, so this lane ran `php tools/run-tests.php`; the aggregate PHP run passed with 217 test files, 25,053 assertions, and 0 failures.
- Focused upstream OneDrive ListP conversion/cache read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `itemToDirEntry`, `ListP`, `List`, and `ListR` shared-folder `listFolder`; `backend/onedrive/api/types.go` normalized `Item`/`RemoteItem` accessors; `backend/onedrive/metadata.go` `Metadata.Get`; and `fs/list/helpers.go` batching. Counted boundaries: `itemToDirEntry` errors return before final `Flush`, OneNote packages are skipped unless exposed, permission metadata errors are deferred until metadata is requested, folder entries update the directory cache for later scoped calls, and shared-folder recursion can discover child directory IDs without pre-seeding.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDriveListRTest.php`: 21 behavior tests, 105 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive ListP conversion/cache slice: 29 rclone test files, 426 behavior tests, 3,491 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-listp-cache-metadata-preflight.php`: the scenario lists a shared review WXR export and upload folder, hides a OneNote migration notebook, reuses the cached upload-directory ID for a later scoped `ListP`, and reports `failed to get permissions: Graph permissions denied` only when metadata is read.
- Hygiene checks on 2026-05-23 after the OneDrive ListP conversion/cache slice: PHP lint passed for `lanes/rclone/src/ObjectInfo.php`, `lanes/rclone/src/OneDriveListR.php`, `lanes/rclone/tests/OneDriveListRTest.php`, and `lanes/rclone/examples/wordpress-onedrive-listp-cache-metadata-preflight.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root gate on 2026-05-23 first found active focused run PID 3341307 owned by `claude` (`php tools/run-tests.php` with rclone and syncthing test-file arguments), so this lane did not start an aggregate run until that process cleared. A later exact preflight was clear and aggregate `php tools/run-tests.php` passed with 219 test files, 25,261 assertions, and 0 failures.
- Focused upstream OneDrive metadata-permissions read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/metadata.go` `Metadata.Get`, the `metadata_permissions` option help, and permission marshal/get error branches; `backend/onedrive/metadata_test.go` `TestOrderPermissions` and `TestOrderPermissionsJSON`; `backend/onedrive/api/types.go` `PermissionsType`, `IdentitySet`, `SharingLinkType`, and `GetGrantedTo`/`GetGrantedToIdentities`; and `backend/onedrive/onedrive.go` `setSystemMetadata`, `readMetaData`, and update-permission refresh boundaries. Static inventory for this focused area is 9 tracked OneDrive paths, 4 OneDrive Go test files, 3 metadata-named paths, and 1 main `onedrive.go` path.
- Bounded upstream check on 2026-05-23 for this slice: `go test ./backend/onedrive -run 'TestOrderPermissions'` passed without live provider credentials.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDriveListRTest.php`: 24 behavior tests, 117 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive metadata-permissions slice: 29 rclone test files, 429 behavior tests, 3,503 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-permissions-metadata-preflight.php`: the scenario lists WXR artifacts, materializes permissions JSON in read mode, suppresses permission metadata in off mode, and reports a deferred permission marshal error without live credentials or OAuth state.
- Hygiene checks on 2026-05-23 after the OneDrive metadata-permissions slice: PHP lint passed for `lanes/rclone/src/OneDriveListR.php`, `lanes/rclone/tests/OneDriveListRTest.php`, and `lanes/rclone/examples/wordpress-onedrive-permissions-metadata-preflight.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. This lane then ran aggregate `php tools/run-tests.php`, which passed with 221 test files, 25,463 assertions, and 0 failures.
- Focused upstream OneDrive permission write/update/remove planning read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/metadata.go` `Metadata.Set`, `WritePermissions`, `sortPermissions`, `processPermissions`, `fillRecipients`, `addPermission`, `updatePermission`, `removePermission`, and errcount aggregation; `backend/onedrive/metadata.md` permission write documentation; `backend/onedrive/api/types.go` permission, identity, link, add-request, and update-request structures; `backend/onedrive/onedrive.go` server-side copy `permsAddOnly`; and `backend/onedrive/onedrive_internal_test.go` `TestWritePermissions`, `TestReadPermissions`, and `TestMetadataMapper` live-provider scenarios. Counted boundaries: write-mode gating, JSON permission metadata unmarshal errors, add-only server-side copy planning, Business sharing-link update remove-plus-add fallback, invalid same-role update skips, owner-role remove suppression, invite recipient extraction/order, missing-recipient and owner-add skips, anonymous public-link adds, and `failok` aggregate error suppression.
- Bounded upstream check on 2026-05-23 for this slice: `go test ./backend/onedrive -run 'TestOrderPermissions'` passed without live provider credentials. The upstream write/update/remove scenarios in `onedrive_internal_test.go` require live OneDrive provider credentials and remain static-read evidence only.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`: 5 behavior tests, 34 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive permission write-planner slice: 30 rclone test files, 434 behavior tests, 3,537 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-permission-write-plan.php`: the scenario upgrades a review WXR export's direct reviewer permission, replaces a Business sharing-link permission through remove-plus-add, removes a stale contractor permission, preserves an owner role, and suppresses a simulated Graph invite error under `failok` without live credentials or OAuth state.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. This lane then ran aggregate `php tools/run-tests.php`, which passed with 224 test files, 25,662 assertions, and 0 failures.
- Focused upstream OneDrive permission write execution/directory metadata read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/metadata.go` `WritePermissions`, `RefreshPermissions`, `processPermissions`, `MkdirMetadata`, `createDir`, and `updateDir`; `backend/onedrive/onedrive.go` `setSystemMetadata`, `itemToDirEntry`, and metadata read helpers; `fs/operations/operations.go` `MkdirMetadata`; `fs/operations/operations_test.go` `TestMkdirMetadata`; and `backend/onedrive/onedrive_internal_test.go` `TestWritePermissions`, `TestReadPermissions`, and directory metadata coverage. Counted boundaries: write-mode and normalizedID preflight errors are not `failok`-suppressed, process errors stop before refresh while `failok` returns nil without clearing queued permissions, refresh errors are wrapped as `failed to get permissions: failed to refresh permissions`, successful writes replace cached permissions with freshly fetched permissions and clear queued permissions, newly-created directories refresh permissions before writing them and then perform the upstream extra directory metadata write, existing directories patch mtime/btime before permission writes, and existing-directory permissions-only metadata hits the no-API-metadata write boundary.
- Bounded upstream check on 2026-05-23 for this slice: `go test ./backend/onedrive -run 'TestOrderPermissions'` passed without live provider credentials. The richer OneDrive permission write and directory metadata scenarios in `onedrive_internal_test.go` remain live-provider/internal coverage and were mapped by static inspection only.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`: 10 behavior tests, 70 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive permission refresh/directory metadata slice: 30 rclone test files, 439 behavior tests, 3,573 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-permission-refresh-dir-metadata.php`: the scenario creates a WordPress `site-backups/review` directory with reviewer permissions, records pre-write and post-write permission refresh sequencing, shows the existing-directory permissions-only metadata boundary, and updates the reviewer permission after mtime/btime metadata is patched, all without live OneDrive credentials.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. This lane then ran aggregate `php tools/run-tests.php`, which passed with 224 test files, 25,874 assertions, and 0 failures.
- Focused upstream OneDrive object `updateMetadata` read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/metadata.go` `Metadata.Get`, `Metadata.Set`, `Metadata.Write`, `WritePermissions`, `needsUpdatePermissions`, and `Object.updateMetadata`; `backend/onedrive/onedrive.go` `uploadSinglepart`, `uploadMultipart`, `Update`, and `setModTime` NoVersions branches; and `backend/onedrive/onedrive_internal_test.go` `TestWritePermissions`/`TestUploadSinglePart` live-provider scenarios. Counted boundaries: object metadata refreshes permissions before `Set` when read mode is active, permissions-only object metadata fails at the no-API-metadata boundary before permission writing, unsupported metadata with zero writeable fields is an object no-op, successful object metadata writes update the object metadata cache before `NoVersions` cleanup, and `NoVersions` failures are reported after metadata has been applied.
- Bounded upstream check on 2026-05-23 for this slice: `go test ./backend/onedrive -run 'TestOrderPermissions'` passed from cache without live provider credentials. The object `updateMetadata` permission/write/version cleanup cases in `onedrive_internal_test.go` remain live-provider/internal coverage and were mapped by static inspection only.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`: 15 behavior tests, 109 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive object metadata update slice: 30 rclone test files, 444 behavior tests, 3,612 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-object-metadata-update.php`: the scenario updates a WXR review export's modtime and reviewer permissions, records permission refresh before `Set`, exposes the object permissions-only no-API-metadata boundary, and shows `NoVersions` cleanup running after object metadata has already been cached, all without live OneDrive credentials.
- Hygiene checks on 2026-05-23 after the OneDrive object metadata update slice: PHP lint passed for `lanes/rclone/src/OneDrivePermissionPlanner.php`, `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-object-metadata-update.php`; `git diff --check -- lanes/rclone` passed. The required duplicate-root gate found active exact root harness PID 3663662 owned by `claude` (`php tools/run-tests.php`), so this lane did not start a duplicate aggregate run and leaves root evidence pending for the supervisor/integrator.
- Focused upstream OneDrive `fetchAndUpdateMetadata`/`uploadSinglepart` read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/metadata.go` `fetchAndUpdateMetadata` and `Object.updateMetadata`; `backend/onedrive/onedrive.go` `uploadSinglepart` and `setModTime`; `fs/metadata.go` `GetMetadataOptions`; and `backend/onedrive/onedrive_internal_test.go` `TestUploadSinglePart` live-provider coverage. Counted boundaries: source metadata read errors are wrapped as `failed to read metadata from source object`, `uploadSinglepart` wraps fetch/update errors as `failed to fetch and update metadata`, nil metadata falls back to `setModTime`, a successful setModTime returns info for a final metadata cache update, and `NoVersions` cleanup errors are logged/suppressed on plain setModTime updates.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`: 19 behavior tests, 130 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive upload metadata fallback slice: 30 rclone test files, 448 behavior tests, 3,633 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-upload-metadata-fallback.php`: the scenario models a zero-byte WXR upload whose absent source metadata falls back to `setModTime`, records suppressed `NoVersions` cleanup failure on plain modtime repair, and surfaces an uploadSinglepart-wrapped source metadata mapper failure without live OneDrive credentials.
- Hygiene checks on 2026-05-23 after the OneDrive upload metadata fallback slice: PHP lint passed for `lanes/rclone/src/OneDrivePermissionPlanner.php`, `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-upload-metadata-fallback.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 found active exact root harness PID 3782892 owned by `claude` (`php tools/run-tests.php`, parent 3758230, elapsed 00:10, state `Rs` when sampled), so this lane did not start a duplicate aggregate run. Root verification for the OneDrive upload metadata fallback slice remains pending for the supervisor/integrator.
- Focused upstream OneDrive `uploadMultipart` metadata read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `createUploadSession`, `uploadMultipart`, `uploadFragment`, and `setMetaData`; `backend/onedrive/metadata.go` `fetchMetadataForCreate`, `needsUpdatePermissions`, `Object.updateMetadata`, `Metadata.Set`, `Metadata.Write`, and `WritePermissions`; and `backend/onedrive/onedrive_internal_test.go` `TestUploadSinglePart` plus metadata mapper coverage as the nearest live-provider upload metadata evidence. Counted boundaries: multipart rejects nonpositive/unknown source sizes before session creation, create-session metadata reads wrap source metadata errors, metadata without writable permissions returns after the initial upload item cache update, permission metadata triggers the post-upload `updateMetadata` path only when write mode is enabled, permissions-only metadata returns the no-API-metadata error with `info == nil` before the final cache update, and update errors that still return item info are followed by a final `setMetaData(info)` that masks the update error like upstream.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`: 24 behavior tests, 172 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive multipart upload metadata slice: 30 rclone test files, 453 behavior tests, 3,675 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-multipart-upload-metadata.php`: the scenario models a large WordPress WXR upload that sets reviewer permissions after chunk upload, exposes the permissions-only no-API-metadata failure before the final cache update, and records the upstream multipart behavior where a permission write error is ignored when `updateMetadata` still returns item info, all without live OneDrive credentials.
- Hygiene checks on 2026-05-23 after the OneDrive multipart upload metadata slice: PHP lint passed for `lanes/rclone/src/OneDrivePermissionPlanner.php`, `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-multipart-upload-metadata.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 found no active exact `php tools/run-tests.php` process. This lane then ran the serialized aggregate `php tools/run-tests.php`; no lock wait message was observed, and the root harness passed with 226 test files, 26,371 assertions, and 0 failures in the current shared dirty worktree.
- Static upstream test inventory strengthened on 2026-05-23 using `git ls-tree -r --name-only HEAD` only, avoiding broad blob hydration in the filtered clone. Counts at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: 2,553 repository files, 1,107 Go files, 327 Go test files, 836 `testdata` paths, and 849 `testdata`/`testfiles`/`fixtures` paths. Go test files are distributed across backend 124, fs 91, cmd 47, lib 43, vfs 19, fstest 1, and cmdtest 2. Full provider/mount parity remains excluded; the bounded local upstream runner evidence remains the prior 299-package pass with provider `TestIntegration`, FUSE/mount, Docker-backed, and live remote suites excluded.
- Focused upstream OneDrive `Object.Update` upload-selection read on 2026-05-23 used targeted reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `Object.Update`, `uploadSinglepart`, `uploadMultipart`, `Fs.Put`, `createObject`, and OneNote package guards; `backend/onedrive/metadata.go` `fetchMetadataForCreate`, `fetchAndUpdateMetadata`, and `Object.updateMetadata`; `fs/object/object_test.go` object `Update`; `fstest/fstests/fstests.go` `FsUploadUnknownSize`; and `backend/onedrive/onedrive_internal_test.go` `TestUploadSinglePart`/`TestMetadataMapper` as nearest live-provider upload metadata evidence. Counted boundaries: `Object.Update` rejects existing OneNote package objects before upload, unknown-sized sources return `unknown-sized upload not supported`, zero-size sources choose `uploadSinglepart`, positive sizes at or above `upload_cutoff` choose `uploadMultipart`, selected upload paths receive source metadata, and update-level `NoVersions` delete failures are logged/suppressed rather than returned.
- Focused PHP check on 2026-05-23 for `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`: 29 behavior tests, 204 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-23 after the OneDrive Object.Update upload-selection slice: 30 rclone test files, 458 behavior tests, 3,707 assertions, and 0 failures.
- Touched example check on 2026-05-23 for `lanes/rclone/examples/wordpress-onedrive-update-upload-selection.php`: the scenario reports zero-byte WXR marker updates through `singlepart`, large WXR reviewer-permission handoff through `multipart`, OneNote migration notebook update rejection, and unknown-size WXR stream rejection without OAuth or live provider credentials.
- Hygiene checks on 2026-05-23 after the OneDrive Object.Update upload-selection slice: PHP lint passed for `lanes/rclone/src/OneDrivePermissionPlanner.php`, `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-update-upload-selection.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed before lane-status edits and was rerun after edits; `git diff --check -- lanes/rclone` passed.
- Required duplicate-root preflight on 2026-05-23 initially found exact root harness PID `4144739` (`php tools/run-tests.php`), but it exited before owner sampling; the immediate exact recheck was clear. This lane then ran the normal serialized aggregate `php tools/run-tests.php`; no lock wait message was observed, and the root harness passed with 226 test files, 26,546 assertions, and 0 failures.
- Focused upstream OneDrive `Fs.Put`/`createObject` read on 2026-05-24 used targeted static reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `Fs.Put`, `createObject`, `uploadSinglepart`, `uploadMultipart`, and singlepart `nameAlreadyExists` OneNote hint handling, plus `backend/onedrive/metadata.go` `fetchMetadataForCreate`, `fetchAndUpdateMetadata`, and `Object.updateMetadata` as the reused metadata evidence. Counted boundaries: parent directory ID lookup before upload, parent lookup error wrapping, temporary destination object handles with no existing metadata, upload-selection reuse for singlepart and multipart creates, and singlepart nameAlreadyExists errors annotated with the upstream OneNote overwrite hint.
- Focused PHP check on 2026-05-24 for `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`: 32 behavior tests, 227 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-24 after the OneDrive Put/createObject slice: 30 rclone test files, 461 behavior tests, 3,730 assertions, and 0 failures.
- Touched example check on 2026-05-24 for `lanes/rclone/examples/wordpress-onedrive-put-create-object.php`: the scenario reports a zero-byte WXR create through a parent ID and temporary no-metadata object, a large WXR reviewer-permission multipart create, a missing parent lookup failure, and a OneNote `nameAlreadyExists` overwrite hint without OAuth or live provider credentials.
- Hygiene checks on 2026-05-24 after the OneDrive Put/createObject slice: PHP lint passed for `lanes/rclone/src/OneDrivePermissionPlanner.php`, `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-put-create-object.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded OneDrive upload metadata planner path and no live provider credentials.
- Rework verification on 2026-05-25 recovered the OneDrive `Fs.Put`/`createObject` upload-creation planner slice from current `refs/heads/main` without live provider tests. Focused checks passed for PHP lint on the planner, focused permission planner test, local Put/createObject example smoke, lane JSON validation, and `git diff --check -- lanes/rclone`; the no-argument root harness was not run.
- Focused upstream OneDrive `MkdirMetadata`/`createDir` read on 2026-05-24 used targeted static reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/metadata.go` `MkdirMetadata`, `createDir`, and `updateDir`; plus `backend/onedrive/onedrive.go` `itemToDirEntry` and `Mkdir`. Counted boundaries: `FindDir` existing-vs-missing branching, parent `FindPath` errors before Graph create, `createDir` `conflictBehavior=fail`/`nameAlreadyExists` behavior, the extra post-create metadata write, existing-directory `updateDir` metadata refresh, Graph item-to-directory conversion, directory ID cache updates, and wrong-type conversion errors before returning `fs.Directory`.
- Focused PHP check on 2026-05-24 for `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`: 34 behavior tests, 250 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-24 after the OneDrive MkdirMetadata/createDir slice: 30 rclone test files, 463 behavior tests, 3,753 assertions, and 0 failures.
- Touched example check on 2026-05-24 for `lanes/rclone/examples/wordpress-onedrive-permission-refresh-dir-metadata.php`: the scenario reports a review-folder create through a parent ID, `conflictBehavior=fail`, Graph item-to-directory cache handoff, create `nameAlreadyExists` conflict, existing-directory metadata refresh, and permissions-only metadata boundaries without OAuth or provider credentials.
- Hygiene checks on 2026-05-24 after the OneDrive MkdirMetadata/createDir slice: PHP lint passed for `lanes/rclone/src/OneDrivePermissionPlanner.php`, `lanes/rclone/tests/OneDrivePermissionPlannerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-permission-refresh-dir-metadata.php`; focused example smoke passed; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded OneDrive permission/directory metadata planner path and no live provider credentials.
- Focused upstream OneDrive `ChangeNotify` runner read on 2026-05-25 used targeted static reads of `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `ChangeNotify`, `changeNotifyStartPageToken`, `changeNotifyNextChange`, `buildDriveDeltaOpts`, and `changeNotifyRunner`; plus `fs/features.go` `ChangeNotifier`. Counted boundaries: initial `latest` delta token retrieval, missing start deltaLink shutdown, nonzero interval requests, zero-duration pause, closed-channel shutdown, provider page failure logging, invalid parent path logging, deltaLink token advancement, and callback cancellation/error propagation in the deterministic PHP harness.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveDeltaCursorTest.php`: 7 behavior tests, 39 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-change-notify-media-watch.php`: the scenario models a WordPress `wp-content` OneDrive watch that pauses polling, queues changed media/WXR objects, logs an invalid parent path, suppresses outside-root SQL changes, and stops cleanly when the interval channel closes, all without OAuth or provider credentials.
- Hygiene checks on 2026-05-25 after the OneDrive ChangeNotify runner slice: PHP lint passed for `lanes/rclone/src/OneDriveDeltaCursor.php`, `lanes/rclone/tests/OneDriveDeltaCursorTest.php`, and `lanes/rclone/examples/wordpress-onedrive-change-notify-media-watch.php`; focused example smoke passed; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded OneDrive delta cursor model and no live provider credentials.
- Focused upstream OneDrive OAuth config read on 2026-05-25 used targeted static reads from upstream `backend/onedrive/onedrive.go` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `commonPathPrefix`, `authPath`, `tokenPath`, `scopeAccess`, `scopeAccessWithoutSites`, `scopeAccessClientCred`, `graphAPIEndpoint`, `authEndpoint`, and `makeOauthConfig`. Counted boundaries: default `/common` auth/token URLs, tenant-specific URLs, `disable_site_permission` scope replacement, global/US/DE/CN national cloud endpoint selection, `client_credentials` `.default` scope override, and missing-tenant rejection.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveOAuthConfigTest.php`: 5 behavior tests, 25 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-oauth-preflight.php`: the scenario reports common-tenant browser auth URL construction, Sites.Read.All scope removal, US national-cloud client-credentials token URL construction, `.default` scope override, missing-tenant rejection, and `secretInputsRead=false` without OAuth or provider credentials.
- Hygiene checks on 2026-05-25 after the OneDrive OAuth config slice: PHP lint passed for `lanes/rclone/src/OneDriveOAuthConfig.php`, `lanes/rclone/tests/OneDriveOAuthConfigTest.php`, and `lanes/rclone/examples/wordpress-onedrive-oauth-preflight.php`; focused example smoke passed; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this is a bounded native PHP config-derivation helper and deliberately does not read process environments, token stores, OAuth browser state, cloud remotes, or provider credentials.
- Focused upstream OneDrive token-renewer read on 2026-05-25 used targeted static reads of `/home/claude/port-libs/.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `lib/oauthutil/renew.go` `NewRenew`, `renewOnExpiry`, `Start`, `Stop`, and `Shutdown`; plus `backend/onedrive/onedrive.go` `NewFs` tokenRenewer construction and `Object.Update` upload bracketing. Counted boundaries: expiry does nothing when no upload is active, active uploads trigger the root metadata refresh callback, callback errors are reported/loggable without changing upload accounting, concurrent upload Start/Stop accounting keeps refresh active until the final Stop, exact atomic Stop decrement behavior can underflow and still leaves nonzero active accounting for expiry refresh decisions, handled expiry signals rearm the loop for later expiry events, and Shutdown suppresses/disarms later expiry refreshes.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveTokenRenewerTest.php`: 6 behavior tests, 48 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-25 after the OneDrive token-renewer rearmer slice: 32 test files, 3,849 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-token-renewer-preflight.php`: the scenario reports idle expiry suppression, active-upload root metadata refresh, post-upload idle suppression, explicit rearmed-state reporting, expiry rearm accounting, shutdown disarm/suppression, balanced upload accounting, and `secretInputsRead=false` without OAuth token stores, process environments, provider remotes, or live credentials.
- Hygiene checks on 2026-05-25 after the OneDrive token-renewer slice: PHP lint passed for `lanes/rclone/src/OneDriveTokenRenewer.php`, `lanes/rclone/tests/OneDriveTokenRenewerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-token-renewer-preflight.php`; focused example smoke passed; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded OneDrive/OAuth lifecycle model and deliberately avoids timers, token-source internals, process environments, OAuth browser state, cloud remotes, and provider credentials.
- Focused upstream OneDrive no-versions cleanup read on 2026-05-25 used targeted static reads of `/home/claude/port-libs/.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `deleteVersions`, `deleteVersion`, `cleanUpNoVersions`, and the existing `NoVersions` call sites in metadata/update/upload paths. Counted boundaries: Graph version listing before destructive work, fewer-than-two version no-op behavior, first/current version preservation, old-version deletion in listed order, `SkipDestructive` dry-run suppression, and first delete-error propagation.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveVersionCleanerTest.php`: 6 behavior tests, 33 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-25 after the OneDrive no-versions cleanup slice: 33 test files, 3,879 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-no-versions-cleanup.php`: the scenario reports current WXR version preservation, old-version deletion, dry-run destructive skip reporting, first Graph delete-error propagation, and `secretInputsRead=false` without OAuth token stores, process environments, provider remotes, or live credentials.
- Hygiene checks on 2026-05-25 after the OneDrive no-versions cleanup slice: PHP lint passed for `lanes/rclone/src/OneDriveVersionCleaner.php`, `lanes/rclone/tests/OneDriveVersionCleanerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-no-versions-cleanup.php`; focused example smoke passed; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this is a bounded native PHP OneDrive lifecycle helper and deliberately avoids live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, and provider credentials.
- Focused upstream OneDrive token-renewer watchdog read on 2026-05-25 reused the existing targeted evidence for `.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `lib/oauthutil/renew.go` `NewRenew`, `renewOnExpiry`, `Start`, `Stop`, and `Shutdown`; plus `backend/onedrive/onedrive.go` tokenRenewer construction and upload bracketing. Counted boundaries: no watchdog arming when no token expiry source exists, active-upload refresh from a watchdog receive, closed expiry-channel termination, shutdown/disarm state preservation, and no timers/OAuth/live Graph calls in the native model.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveTokenRenewerTest.php`: 11 behavior tests, 79 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-25 after the OneDrive token-renewer watchdog slice: 33 rclone test files, 3,910 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-token-renewer-preflight.php`: the scenario records idle expiry suppression, bracketed WXR upload refresh, active watchdog refresh, no-expiry-source watchdog suppression, closed-channel watchdog shutdown, and `secretInputsRead=false` without OAuth token stores, process environments, provider remotes, cloud config, or live credentials.
- Hygiene checks on 2026-05-25 after the OneDrive token-renewer watchdog slice: PHP lint passed for `lanes/rclone/src/OneDriveTokenRenewer.php`, `lanes/rclone/tests/OneDriveTokenRenewerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-token-renewer-preflight.php`; focused example smoke passed; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this is a bounded native PHP OneDrive lifecycle helper and deliberately avoids live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, and provider credentials.
- Focused upstream OneDrive token-renewer watchdog underflow read on 2026-05-25 reused the existing targeted evidence for `lib/oauthutil/renew.go` `renewOnExpiry`, `Start`, and `Stop`: upstream gates refresh work on an upload counter greater than zero, so a Stop underflow is visible accounting state but must not count as active upload work.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveTokenRenewerTest.php`: 12 behavior tests, 86 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-token-renewer-preflight.php`: local smoke loaded the scenario array and verified `source=onedrive-token-renewer-preflight`, `watchdogUnderflowRefreshed=false`, `watchdogUnderflowActiveUploads=-1`, and `secretInputsRead=false`.
- Hygiene checks on 2026-05-25 after the OneDrive token-renewer watchdog underflow slice: PHP lint passed for `lanes/rclone/src/OneDriveTokenRenewer.php`, `lanes/rclone/tests/OneDriveTokenRenewerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-token-renewer-preflight.php`; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the bounded native OneDrive token-renewer lifecycle helper and deliberately avoids live Graph calls, timers, token stores, process environments, OAuth browser state, cloud remotes, and provider credentials.
- Focused upstream OneDrive provider shutdown lifecycle read on 2026-05-25 reused the recorded static evidence for upstream `lib/oauthutil/renew.go` `Shutdown`/`Start`/`Stop`, `backend/onedrive/onedrive.go` tokenRenewer construction and ChangeNotify wiring, and `fs/features.go` ChangeNotifier boundaries. Counted boundaries: provider shutdown stops change notification state, token-renewer shutdown/disarm is invoked once, repeated shutdown is idempotent, change-notify starts after shutdown are ignored, and post-shutdown token refreshes are suppressed; the accepted token-renewer watchdog underflow evidence remains intact.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveProviderLifecycleTest.php`: 4 behavior tests, 22 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-25 after the OneDrive provider shutdown repair slice: 34 rclone test files, 3,939 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-provider-shutdown-preflight.php`: the scenario records an active WXR upload at provider shutdown, stopped change notification state, token-renewer shutdown, idempotent second shutdown, post-shutdown refresh suppression, and `secretInputsRead=false` without OAuth token stores, process environments, provider remotes, cloud config, or live credentials.
- Hygiene checks on 2026-05-25 after the OneDrive provider shutdown repair slice: PHP lint passed for `lanes/rclone/src/OneDriveProviderLifecycle.php`, `lanes/rclone/tests/OneDriveProviderLifecycleTest.php`, and `lanes/rclone/examples/wordpress-onedrive-provider-shutdown-preflight.php`; focused example smoke passed; focused lane tests passed; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check` passed. Dependency closure: no new support component is needed; this reuses the existing bounded OneDrive token-renewer and change-notify lifecycle models and deliberately avoids live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, and provider credentials.
- Focused upstream OneDrive token-renewer closed-watchdog read on 2026-05-25 reused the recorded static evidence for upstream `lib/oauthutil/renew.go` `renewOnExpiry`, `Start`, `Stop`, and `Shutdown`, plus OneDrive upload bracketing call sites. Counted boundaries: an expiry-channel close during active upload work disarms the watchdog without a metadata refresh, reports the active-upload diagnostic count at close time, still allows deferred Stop accounting when the upload bracket unwinds, and suppresses later Start attempts.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveTokenRenewerTest.php`: 13 behavior tests, 97 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-25 after the OneDrive token-renewer closed-watchdog slice: 34 rclone test files, 3,950 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-token-renewer-preflight.php`: the scenario records a WXR upload whose expiry channel closes mid-upload, deferred upload Stop accounting returning to zero, later start suppression, and `secretInputsRead=false` without OAuth token stores, process environments, provider remotes, cloud config, or live credentials.
- Hygiene checks on 2026-05-25 after the OneDrive token-renewer closed-watchdog slice: PHP lint passed for `lanes/rclone/src/OneDriveTokenRenewer.php`, `lanes/rclone/tests/OneDriveTokenRenewerTest.php`, and `lanes/rclone/examples/wordpress-onedrive-token-renewer-preflight.php`; focused example smoke passed; focused lane tests passed; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded OneDrive token-renewer lifecycle model and deliberately avoids live Graph calls, timers, token stores, process environments, OAuth browser state, cloud remotes, and provider credentials.
- Focused upstream OneDrive provider lifecycle feature-mask read on 2026-05-25 reused recorded static evidence for upstream `fs/features.go` ChangeNotifier feature masking plus `backend/onedrive/onedrive.go` ChangeNotify wiring and `lib/oauthutil/renew.go` token-renewer Start/Shutdown. Counted boundary: a masked ChangeNotify feature suppresses notification start without marking the provider shut down or disabling token renewal, so later unmasked provider activity can still refresh active uploads before ordinary shutdown.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveProviderLifecycleTest.php`: 5 behavior tests, 29 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-25 after the OneDrive provider feature-mask lifecycle slice: 34 rclone test files, 3,957 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-provider-shutdown-preflight.php`: the scenario records masked ChangeNotify suppression before a WXR upload shutdown, later unmasked notification shutdown, token-renewer shutdown, idempotent second shutdown, and `secretInputsRead=false` without OAuth token stores, process environments, provider remotes, cloud config, or live credentials.
- Hygiene checks on 2026-05-25 after the OneDrive provider feature-mask lifecycle slice: PHP lint passed for `lanes/rclone/src/OneDriveProviderLifecycle.php`, `lanes/rclone/tests/OneDriveProviderLifecycleTest.php`, and `lanes/rclone/examples/wordpress-onedrive-provider-shutdown-preflight.php`; focused example smoke passed; focused lane tests passed; `jq empty lanes/rclone/UPSTREAM_TEST_MANIFEST.json lanes/rclone/lane-status.json` passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded OneDrive token-renewer and change-notify lifecycle models and deliberately avoids live Graph calls, timers, token stores, process environments, OAuth browser state, cloud remotes, and provider credentials.
- Focused upstream OneDrive CleanUp/no-versions command wiring rework on 2026-05-25 used targeted static reads of `/home/claude/port-libs/.upstream-cache/rclone` at `28d6b0b7b906da70afdc036ba5bb21f3c86613b8`: `backend/onedrive/onedrive.go` `CleanUp`, `deleteVersions`, `deleteVersion`, and `NoVersions` call sites; `cmd/cleanup/cleanup.go` command wiring; `fs/operations/operations.go` `CleanUp`; and `fs/features.go` `CleanUpper` feature wiring. Counted boundaries: cleanup command remote-argument arity rejects missing, extra, and empty remotes before feature/traversal/provider work, cleanup disabled by the no-versions option exits before feature/traversal/provider work, cleanup checks feature availability before dry-run/traversal/type-error/provider work, walks object entries after feature gating, preserves the first/current Graph version, deletes old versions in listed order, records dry-run destructive skips, logs per-object delete/list failures while continuing to later objects, fails traversal/type errors after feature gating, and explicitly proves type errors stop before provider/version cleanup.
- Focused PHP check on 2026-05-25 for `lanes/rclone/tests/OneDriveCleanupCommandTest.php`: 14 behavior tests, 93 assertions, and 0 failures.
- Touched example check on 2026-05-25 for `lanes/rclone/examples/wordpress-onedrive-cleanup-command-preflight.php`: the scenario records missing, extra, and empty remote argument rejection before feature/traversal/provider work, disabled no-versions cleanup bypassing feature/traversal/provider work, disabled cleanup ignoring dry-run/traversal/object-type state, feature-masked cleanup rejection before provider work, enabled type errors stopping before provider/version cleanup, stale WXR version cleanup, dry-run skip reporting, per-object Graph delete and version-list failure logging, continued media-version cleanup after each failure, and `secretInputsRead=false` without OAuth token stores, process environments, provider remotes, cloud config, or live credentials.
- Hygiene checks on 2026-05-25 after the OneDrive cleanup command rework slice: PHP lint passed for `lanes/rclone/src/OneDriveCleanupCommand.php`, `lanes/rclone/tests/OneDriveCleanupCommandTest.php`, and `lanes/rclone/examples/wordpress-onedrive-cleanup-command-preflight.php`; focused example smoke passed; focused rclone lane tests passed with 35 files, 4,050 assertions, and 0 failures; JSON validation passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded native OneDrive version cleanup model and deliberately avoids live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, and provider credentials.
- Priority keeper rework refresh on 2026-05-25: this isolated worktree's accepted HEAD already contains the OneDrive cleanup command behavior from the stale handoffs, including command remote-argument arity validation before feature/traversal/provider work, disabled no-versions bypasses, feature-before-traversal/type-error ordering, explicit type-error-before-provider assertions, version-list failure continuation, and the WordPress cleanup command preflight. The fresh patch is intentionally metadata-only and additive so the integrator can accept it without replaying stale manifest/status context.
- Priority finisher rework refresh on 2026-05-25 for `priority-finisher-20260525T114549Z`: current accepted HEAD `de55720729b64a0f26c6f242b03966dbaccae2fe` already contains the stale OneDrive cleanup-command behavior and tests, so this patch preserves accepted manifest/status evidence and only refreshes lane-local rework status. Focused verification reruns the cleanup command unit test, the WordPress cleanup command smoke, JSON validation, and `git diff --check -- lanes/rclone`; root harness remains not run for the isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded native OneDrive version cleanup model without live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, or provider credentials.
- Priority finisher rework refresh on 2026-05-25 for `priority-finisher-20260525T152408Z`: current accepted HEAD `e4d2aae4e1502930375db9078137bbd330c41680` already contains the stale OneDrive cleanup-command behavior and tests, so this additive slice maps the remaining local RC cleanup boundary from `fs/operations/rc.go` / `rc_test.go` `TestRcCleanup`. Counted boundaries: `operations/cleanup` uses the RC `fs` parameter before feature/traversal/provider work, does not apply command remote-argument arity, reports unsupported cleanup before provider work when the feature is masked, and reuses the accepted OneDrive version cleanup behavior once `fs` is present. No live OneDrive provider, OAuth, token store, cloud remote, process environment, or credential-bearing input is used.
- Hygiene checks on 2026-05-25 after the RC cleanup rework slice: PHP lint passed for `lanes/rclone/src/OneDriveCleanupCommand.php`, `lanes/rclone/tests/OneDriveCleanupCommandTest.php`, and `lanes/rclone/examples/wordpress-onedrive-cleanup-command-preflight.php`; focused `OneDriveCleanupCommandTest.php` passed with 15 behavior tests, 106 assertions, and 0 failures; full focused rclone lane tests passed with 35 files, 4,063 assertions, and 0 failures; local cleanup-command example smoke passed; JSON validation passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded native OneDrive version cleanup model and RC parameter model without live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, or provider credentials.
- Priority refill rework refresh on 2026-05-25 for `priority-refill-20260525T163734Z`: current accepted HEAD `ef96a451a573a2fdac1ca4eef8f6a2c40571969a` already contains the stale cleanup-command behavior and RC dispatch model, so this additive patch preserves accepted manifest/status evidence and tightens the remaining RC boundary. Counted boundary: `operations/cleanup` requires the RC `fs` parameter before even disabled no-versions cleanup can bypass provider/version work, while still ignoring command-style `remoteArgs` after `fs` is present. No live OneDrive provider, OAuth, token store, cloud remote, process environment, or credential-bearing input is used.
- Hygiene checks on 2026-05-25 after the priority-refill RC cleanup rework slice: PHP lint passed for `lanes/rclone/src/OneDriveCleanupCommand.php`, `lanes/rclone/tests/OneDriveCleanupCommandTest.php`, and `lanes/rclone/examples/wordpress-onedrive-cleanup-command-preflight.php`; focused `OneDriveCleanupCommandTest.php` passed with 15 behavior tests, 110 assertions, and 0 failures; full focused rclone lane tests passed with 35 files, 4,067 assertions, and 0 failures; local cleanup-command example smoke passed; JSON validation passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded native OneDrive version cleanup model and RC parameter model without live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, or provider credentials.
- Priority finisher rework refresh on 2026-05-25 for `priority-finisher-20260525T164508Z`: current accepted HEAD already contains the stale OneDrive cleanup-command behavior and RC dispatch model, so this additive patch preserves accepted manifest/status evidence and tightens the RC disabled-cleanup boundary. Counted boundary: `operations/cleanup` ignores command-style `remoteArgs` once the RC `fs` parameter is present even when no-versions cleanup is disabled, while missing or empty `fs` still fails before the disabled no-versions bypass. No live OneDrive provider, OAuth, token store, cloud remote, process environment, or credential-bearing input is used.
- Hygiene checks on 2026-05-25 after the priority-finisher RC cleanup rework slice: PHP lint passed for `lanes/rclone/src/OneDriveCleanupCommand.php`, `lanes/rclone/tests/OneDriveCleanupCommandTest.php`, and `lanes/rclone/examples/wordpress-onedrive-cleanup-command-preflight.php`; focused `OneDriveCleanupCommandTest.php` passed with 15 behavior tests, 115 assertions, and 0 failures; full focused rclone lane tests passed with 35 files, 4,072 assertions, and 0 failures; local cleanup-command example smoke passed; JSON validation passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded native OneDrive version cleanup model and RC parameter model without live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, or provider credentials.
- Closure refill rework refresh on 2026-05-25 for `closure-refill-20260525T165345Z`: current accepted HEAD already contains the stale OneDrive cleanup-command behavior and RC dispatch model, so this additive patch preserves accepted manifest/status evidence and tightens command-vs-RC disabled-cleanup ordering. Counted boundary: command-mode `cleanup` validates exactly one non-empty remote argument before the disabled no-versions bypass, feature checks, traversal, type checks, or provider/version work; RC `operations/cleanup` remains fs-parameter driven and keeps the accepted command-arity bypass once `fs` is present. No live OneDrive provider, OAuth, token store, cloud remote, process environment, or credential-bearing input is used.
- Hygiene checks on 2026-05-25 after the closure-refill cleanup-command rework slice: PHP lint passed for `lanes/rclone/src/OneDriveCleanupCommand.php`, `lanes/rclone/tests/OneDriveCleanupCommandTest.php`, and `lanes/rclone/examples/wordpress-onedrive-cleanup-command-preflight.php`; focused `OneDriveCleanupCommandTest.php` passed with 15 behavior tests, 121 assertions, and 0 failures; full focused rclone lane tests passed with 35 files, 4,078 assertions, and 0 failures; local cleanup-command example smoke passed; JSON validation passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded native OneDrive version cleanup model and RC parameter model without live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, or provider credentials.
- Priority refill rework refresh on 2026-05-25 for `priority-refill-20260525T170103Z`: current accepted HEAD already contains the stale OneDrive cleanup-command behavior and RC dispatch model, so this additive patch preserves accepted manifest/status evidence and fills the remaining command-mode disabled-cleanup arity evidence. Counted boundary: missing, extra, and empty command remote arguments are all rejected before the disabled no-versions bypass, feature checks, traversal, type checks, or provider/version work; RC `operations/cleanup` remains fs-parameter driven and keeps the accepted command-arity bypass once `fs` is present. No live OneDrive provider, OAuth, token store, cloud remote, process environment, or credential-bearing input is used.
- Hygiene checks on 2026-05-25 after the priority-refill cleanup-command disabled-arity rework slice: PHP lint passed for `lanes/rclone/src/OneDriveCleanupCommand.php`, `lanes/rclone/tests/OneDriveCleanupCommandTest.php`, and `lanes/rclone/examples/wordpress-onedrive-cleanup-command-preflight.php`; focused `OneDriveCleanupCommandTest.php` passed with 15 behavior tests, 133 assertions, and 0 failures; full focused rclone lane tests passed with 35 files, 4,090 assertions, and 0 failures; local cleanup-command example smoke passed; JSON validation passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded native OneDrive version cleanup model and RC parameter model without live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, or provider credentials.
- Continuous-dev cleanup-command rework refresh on 2026-05-25 for `continuous-dev-20260525T232216Z`: current accepted HEAD already contains the stale OneDrive cleanup-command behavior and RC dispatch model, so this additive patch preserves accepted manifest/status evidence and fills the valid command-remote acceptance side of the command-arity boundary. Counted boundary: a single non-empty command remote reaches the disabled no-versions short-circuit before feature/traversal/provider work, while the same valid remote reaches the existing cleanup feature gate when version cleanup is enabled; missing, extra, and empty remotes still fail before both paths. No live OneDrive provider, OAuth, token store, cloud remote, process environment, or credential-bearing input is used.
- Hygiene checks on 2026-05-25 after the continuous-dev cleanup-command valid-remote rework slice: PHP lint passed for `lanes/rclone/src/OneDriveCleanupCommand.php`, `lanes/rclone/tests/OneDriveCleanupCommandTest.php`, and `lanes/rclone/examples/wordpress-onedrive-cleanup-command-preflight.php`; focused `OneDriveCleanupCommandTest.php` passed with 16 behavior tests, 145 assertions, and 0 failures; full focused rclone lane tests passed with 35 files, 4,102 assertions, and 0 failures; local cleanup-command example smoke passed; JSON validation passed; `git diff --check -- lanes/rclone` passed. Dependency closure: no new support component is needed; this reuses the existing bounded native OneDrive version cleanup model and RC parameter model without live Graph calls, token stores, process environments, OAuth browser state, cloud remotes, or provider credentials.
- Continuous-dev cleanup-command rework refresh on 2026-05-25 for `continuous-dev-20260525T233252Z`: current accepted HEAD already contains the stale OneDrive cleanup-command behavior and RC dispatch model, so this additive patch preserves accepted manifest/status evidence and makes the conflict-prone ordering machine-readable in the native result. Counted boundary: cleanup returns explicit `stoppedAt` diagnostics for command-arity, disabled-no-versions, feature-gate, walk, type-check, rc-fs, and complete outcomes, proving that command arity still wins before disabled cleanup, RC `fs` still wins before disabled RC cleanup, feature gating still wins before traversal/type/provider work, and type errors still stop before provider/version cleanup. No live OneDrive provider, OAuth, token store, cloud remote, process environment, or credential-bearing input is used.
- Focused test check on 2026-05-25 for `lanes/rclone/tests/OneDriveCleanupCommandTest.php`: 16 behavior tests, 174 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-25 after the cleanup-command stoppedAt rework slice: 35 rclone test files, 4,131 assertions, and 0 failures.
- Continuous-dev cleanup-command rework refresh on 2026-05-25 for `continuous-dev-20260525T235434Z`: current accepted HEAD already contains the stale OneDrive cleanup-command behavior and RC dispatch model, so this additive patch preserves accepted manifest/status evidence and fills the enabled RC command-arity bypass boundary. Counted boundary: RC `operations/cleanup` ignores command-style extra `remoteArgs` once the `fs` parameter is present even when cleanup is enabled and version cleanup actually runs; missing or empty `fs` still fails before disabled cleanup, and command-mode cleanup still validates exactly one non-empty remote before disabled cleanup or provider work. No live OneDrive provider, OAuth, token store, cloud remote, process environment, or credential-bearing input is used.
- Focused test check on 2026-05-25 for `lanes/rclone/tests/OneDriveCleanupCommandTest.php`: 16 behavior tests, 180 assertions, and 0 failures.
- Rclone-only PHP lane check on 2026-05-25 after the cleanup-command RC command-args rework slice: 35 rclone test files, 4,137 assertions, and 0 failures.
