{
  "schemaVersion": 1,
  "library": "syncthing",
  "estimatedProgress": 71,
  "suiteProgress": "verified cloned static upstream inventory counted 658 Go test/benchmark entry points across 141 upstream `_test.go` files plus 264 focused behavior/test paths from 940 tracked paths; bounded upstream package runners include focused block-pull reorderer, blockDiff, handleFile/copier/sparse zero-block pull planning, job queue, device activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index RemoteIndexUpdated, folder completion, request max-size, sqlite global/need, sqlite global drop/recalculation, sqlite needed-file pull-order, sqlite global-prefix/drop-device, versioner Archive/tagging, directory deletion scan/error precedence including ignored-child precedence, `performFinish` existing-file scan guard/case-only slices, and finisher deregistration failure tests; native PHP now maps 217 focused tests including unavailable-peer block availability/temporary-state cleanup, post-promotion dbUpdaterRoutine fsync/updateLocals/RemoteChangeDetected/ReceivedFile batching, finisherRoutine queue completion/progress deregistration/ItemFinished/temp pull error handling, end-of-pull tempPullErrors promotion into FolderErrors, and pullScannerRoutine-style deferred post-pull scan aggregation; full upstream runner not executed",
  "phpPass": 217,
  "phpFail": 0,
  "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders. Current examples include BEP request/response/cluster/progress/close frames, Index/IndexUpdate handling, FolderIndexState global/need state, pullBlock retry handling, temporary peer disconnect fallback, temporary-file finalization, receive-encrypted finalization, temp-file permission reuse, conflict copies, scan guards, case-only conflict preservation, directory replacement guards, version archive replacement, post-promotion DB update batching in `wordpress-pull-db-updater.php`, `wordpress-pull-finisher.php` where a finalized Playground media pull completes the queue, emits an ItemFinished-style payload, deregisters progress, records block stats, and hands off the database update, `wordpress-folder-errors.php` where a failed media pull promotes `syncing: finishing:` into persistent FolderErrors while retaining the temporary file for retry, and `wordpress-post-pull-scan-scheduler.php` where local-first media and stale Playground folder scan requests are de-duplicated and deferred until the pull scanner closes.",
  "phase": "verified cloned static upstream test inventory plus bounded focused upstream runner evidence and native scanner, protocol vector, DeviceID/Luhn, FileInfo conflict/equivalence, FolderCompletion, FolderIndexState, FileInfoBatch, protocol validation/wire-path, BEP wire/session/callbacks, request/response exchange, Index/IndexUpdate receive and send paths, DownloadProgress, ProgressEmitter, temporary-block planning, connection-aware unavailable-peer cleanup, least-busy activity, pullBlock retry/verification/zero-block/receive-encrypted trust, temporary-file block writes/final close/final promotion/performFinish conflict and directory guards/post-promotion dbUpdaterRoutine side effects/finisherRoutine lifecycle/folder-error promotion handling/pullScannerRoutine deferred scan scheduling/receive-encrypted trailer append, block-pull ordering, blockDiff, PullWorkPlan, pull job queue, service-map, request-serving, ignore, receive-encrypted request/envelope/data/FileInfo/trailer/finalization, encrypted DownloadProgress, encryption consistency, and PasswordToken slices",
  "audit": "audited 2026-05-23: lane-local Syncthing PHP tests pass 217/0 across 36 test files with 1766 assertions after adding PullScanner deferred scan aggregation and PullFinisher finalization handoff. Focused upstream evidence for this batch is static targeted inspection of `lib/model/folder_sendrecv.go` `pullScannerRoutine`, scanChan close semantics, `finisherRoutine`, `performFinish`, `scanIfItemChanged`, `checkToBeDeleted`, and `deleteDirOnDiskHandleChildren`; no full upstream runner parity was attempted. Required root `php tools/run-tests.php` exits 0 with 179 test files, 17287 assertions, and 0 failures. Static upstream inventory remains 610 Test functions, 48 Benchmark functions, and 0 Fuzz functions across 141 `_test.go` files.",
  "currentWork": "Native `PullScanner` now maps Syncthing pullScannerRoutine-style behavior: paths queued while finalization or deletion is still in progress are de-duplicated, file and directory scan candidates are tracked for PHP bookkeeping, no scan callback fires before the pull scanner closes, close emits one post-pull scan batch, repeated close is idempotent, failed finalization with `file modified but not rescanned` still queues the media path, and PullFinisher can hand finalization scan names to the deferred scanner.",
  "blocker": "No Syncthing lane-local or root PHP blocker: lane-only tests pass 217/0 across 36 files with 1766 assertions, and the required root harness passes 179 files, 17287 assertions, 0 failures. Full Syncthing upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building the full Go module graph, and running 141 Go test files plus integration test paths under `test/`. Go is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`.",
  "latestCommit": "Map Syncthing pull scanner scheduling; root harness green",
  "nextTask": "Target handleDir/handleSymlink directory and symlink update lifecycle, including ItemStarted/ItemFinished payloads and dbUpdate scheduling boundaries."
}
