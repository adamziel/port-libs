{
    "schemaVersion": 1,
    "library": "syncthing",
    "estimatedProgress": 23,
    "suiteProgress": "cloned static upstream inventory counted: 264 unique behavior/test paths from 940 upstream tracked paths plus 658 static Go test/benchmark entry points; scanner, protocol vector, FileInfo conflict/equivalence, protocol filename/request/index consistency, wire-path normalization, BEP Hello/Request/Response/ClusterConfig wire-frame semantics, native LZ4 compressed-frame behavior, Index/IndexUpdate FileInfo protobuf payloads, DownloadProgress append/forget plus remote temporary-download state, sent-download outgoing diff computation, ProgressEmitter connection-boundary grouping/cleanup/event summaries, timer-driven scheduler plus wire connection adapters, inbound remote DownloadProgress temporary-block availability/request planning, inbound Request.fromTemporary serving, ignored request path rejection, include/escape-aware ignore parsing with glob ranges/alternatives and skip-dir boundaries, focused ignore match results, receive-encrypted final-file hash-token bypass, receive-encrypted request envelope geometry, deterministic encrypted-name and offset-bound block-hash token transforms, encrypted path parent detection, receive-encrypted FileInfo trailers, cluster-config encryption consistency, and exact PasswordToken scrypt/AES-SIV derivation mapped; upstream runner not executed",
    "phpPass": 84,
    "phpFail": 0,
    "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders; examples cover continuing a partially synchronized media upload from the first untrusted byte offset, detecting concurrent media/caption edits from separate devices, resolving media tombstone/conflict winner decisions, treating scanner-only metadata noise as equivalent while content changes still force sync, validating normalized WordPress media index updates while rejecting traversal requests, emitting a native BEP Request frame for a missing media block, advertising a WordPress media folder/device ClusterConfig frame, compressing repeated WordPress folder/device metadata with native LZ4 frames, sending a WordPress media IndexUpdate frame with FileInfo block hashes/version counters/sequence metadata/normalized wire paths, advertising temporary media blocks with a native DownloadProgress append frame, computing outgoing new-block-only DownloadProgress deltas plus cleanup forgets for WordPress media pulls, grouping those deltas per subscribed WordPress peer with completed-byte progress summaries, scheduling real BEP DownloadProgress frames for a WordPress media peer only when the puller registry changes, choosing a native Request.fromTemporary plan when a WordPress media block is available only from a peer's temporary file, serving a WordPress media restore request by rejecting stale temporary bytes before returning a native BEP Response frame from the finalized file, serving a receive-encrypted WordPress media restore while an ignored private export path remains blocked, loading included WordPress ignore snippets with escaped literal glob filenames before serving request bytes, shaping a receive-encrypted WordPress media request envelope with deterministic encrypted names and offset-bound block-hash tokens plus a FileInfo trailer without exposing the plaintext filename to the untrusted peer, deriving exact Syncthing receive-encrypted password tokens for WordPress media peers, and rejecting untrusted WordPress media peers unless the cluster config advertises compatible receive-encrypted tokens.",
    "phase": "cloned static upstream inventory plus scanner block, protocol vector, FileInfo conflict/equivalence, protocol validation/wire-path, BEP wire-frame/cluster-config, native LZ4 compressed-frame, Index/IndexUpdate FileInfo protobuf, DownloadProgress temporary-block state, sent-download outgoing diff, ProgressEmitter connection-boundary, timer-driven scheduler/connection-adapter, inbound remote temporary-block availability, inbound request-serving, include/escape-aware ignore-match, receive-encrypted request envelope/trailer, deterministic encrypted-name and block-hash token transforms, cluster-config encryption consistency, and exact PasswordToken scrypt/AES-SIV slices",
    "audit": "audited 2026-05-22: lane-local Syncthing PHP tests pass 84/0 with 672 assertions across 16 test files; `php lanes/syncthing/examples/wordpress-receive-encrypted-envelope.php` runs and returns a deterministic encrypted WordPress media name, decrypted plaintext name, offset-bound encrypted hash token, decrypted SHA-256 block hash, padded request size, and FileInfo trailer metadata; required root `php tools/run-tests.php` passes with 104 test files, 7203 assertions, and 0 failures in the current shared worktree; no upstream runner parity. During prior KDF probing this lane installed `php-sodium`/`libsodium` via `sudo -n dnf install -y php-sodium`, but PHP exposes only high-level scrypt here, so exact N/r/p scrypt is implemented in lane PHP.",
    "currentWork": "Native scanner-style block hashing, version vectors, FileInfo conflict/equivalence behavior, protocol validation/wire-path normalization, BEP Hello/Request/Response/ClusterConfig proto3 payload serialization, uncompressed plus LZ4-compressed post-auth frame handling, Index/IndexUpdate FileInfo protobuf payloads, DownloadProgress append/forget payloads and message type 5 frames, legacy FDPU fixture decoding, remote temporary-download append/forget state, outgoing sent-download state, ProgressEmitter subscription grouping/cleanup/event summary behavior, timer-gated scheduler plus BEP wire connection adapters from upstream `progressemitter.go`, `progressemitter_test.go`, and `protocol.go`, inbound model.DownloadProgress sharing guards, RemoteDownloadProgress summaries, blockAvailabilityFromTemporary, Request.fromTemporary planning from upstream `model.go` and `folder_sendrecv.go`, inbound Request.fromTemporary serving from upstream `model.Request`, `readOffsetIntoBuf`, `scanner.Validate`, `fs.IsInternal`, and `fs.TempName`, request ignored-file, include/escape-aware ignore parsing, receive-encrypted hash-token guards from `model.Request` and `lib/ignore`, receive-encrypted request envelope/path/trailer helpers plus deterministic `encryptName`, `decryptName`, `KeyGenerator.FileKey`, and `encryptBlockHash` behavior from `lib/protocol/encryption.go` and `lib/protocol/encryption_test.go`, cluster-config encryption consistency decisions from `model.ccCheckEncryption` and `TestCcCheckEncryption`, plus exact `PasswordToken` derivation from `KeyFromPassword`, `knownBytes`, and AES-SIV fixtures in `TestKeyDerivation`.",
    "blocker": "Full upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building Go dependencies, and running 141 Go test files plus integration test paths under `test/`. Go itself is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`. Lane-local Syncthing tests and the required root PHP suite are green. Remaining native crypto gaps include encrypted FileInfo metadata wrapping/decryption and XChaCha20-Poly1305 data encryption/decryption.",
    "latestCommit": "dcec92b (deterministic encrypted name/hash token slice)",
    "nextTask": "Port encrypted FileInfo metadata wrapping/decryption from `encryptFileInfo` and `DecryptFileInfo`, then map XChaCha20-Poly1305 data encryption/decryption boundaries."
}
