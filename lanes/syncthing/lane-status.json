{
  "schemaVersion": 1,
  "library": "syncthing",
  "estimatedProgress": 63,
  "suiteProgress": "verified cloned static upstream inventory counted 658 Go test/benchmark entry points across 141 upstream `_test.go` files plus 264 focused behavior/test paths from 940 tracked paths; bounded upstream package runners passed focused block-pull reorderer, blockDiff, handleFile/copier/sparse zero-block pull planning, job queue, device activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index RemoteIndexUpdated, folder completion, request max-size, sqlite global/need, sqlite global drop/recalculation, sqlite needed-file pull-order, sqlite global-prefix/drop-device, versioner Archive/tagging, directory deletion scan/error precedence including ignored-child precedence, and `performFinish` existing-file scan guard slices; native PHP now maps 195 focused tests including upstream `performFinish` version-archive replacement, `scanIfItemChanged` existing-file guards, and `deleteDirOnDiskHandleChildren` unknown-child/temp-child/ignored-child boundaries through static targeted reads plus bounded upstream `go test` runs; full upstream runner not executed",
  "phpPass": 195,
  "phpFail": 0,
  "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders. Current examples include media resume, FileInfo equivalence, BEP request/response/cluster/progress/close frames, Index/IndexUpdate handling, FolderIndexState global/need state, pullBlock retry handling, temporary-file finalization, receive-encrypted finalization, temp-file permission reuse, `wordpress-pull-conflict-replacement.php` for concurrent media edit conflict copies, `wordpress-pull-unscanned-local-edit.php` where a local editor crop changed after scanning blocks remote replacement and schedules a scan while retaining the pulled temp file, `wordpress-pull-directory-replacement.php` where a stale generated media directory is removed before a Playground archive file is promoted, `wordpress-pull-version-archive.php` where a non-conflicting prior media version is archived under `.stversions` before replacement, `wordpress-pull-directory-scan-guard.php` where an unknown local thumbnail prevents destructive directory replacement and schedules a follow-up scan, and `wordpress-pull-ignored-directory.php` where an ignored local review cache blocks destructive directory replacement without scheduling scans for ignored paths.",
  "phase": "verified cloned static upstream test inventory plus bounded focused upstream runner evidence and native scanner, protocol vector, DeviceID/Luhn, FileInfo conflict/equivalence, FolderCompletion, FolderIndexState, FileInfoBatch, protocol validation/wire-path, BEP wire/session/callbacks, request/response exchange, Index/IndexUpdate receive and send paths, DownloadProgress, ProgressEmitter, temporary-block planning, least-busy activity, pullBlock retry/verification/zero-block/receive-encrypted trust, temporary-file block writes/final close/final promotion/provisional permissions/read-only reuse/performFinish existing-file scan guard/conflict-copy replacement/tracked-directory replacement/conflict-retention pruning/version-archive replacement/directory deletion guard/ignored-directory deletion precedence/receive-encrypted trailer append, block-pull ordering, blockDiff, PullWorkPlan, pull job queue, service-map, request-serving, ignore, receive-encrypted request/envelope/data/FileInfo/trailer/finalization, encrypted DownloadProgress, encryption consistency, and PasswordToken slices",
  "audit": "audited 2026-05-23: lane-local Syncthing PHP tests pass 195/0 across 32 test files with 1605 assertions. Required root `php tools/run-tests.php` exits 0 with 172 test files, 16025 assertions, and 0 failures. Static upstream inventory remains 610 Test functions, 48 Benchmark functions, and 0 Fuzz functions across 141 `_test.go` files. Focused upstream evidence for this batch includes static targeted inspection of `lib/model/folder_sendrecv.go` `performFinish`/`scanIfItemChanged` and `deleteDirOnDiskHandleChildren` ignored-child precedence plus bounded upstream `go test ./lib/model -run 'TestPullCaseOnlyPerformFinish|TestDeleteIgnorePerms' -count=1` in `.upstream-cache/port-go-local-capacity-20260523T0034Z/syncthing`; prior focused evidence also includes versioner Archive/tagging and directory deletion scan/error precedence runs. No full upstream runner parity was attempted.",
  "currentWork": "Native `PullTemporaryFile` now maps Syncthing `performFinish` existing-file safety before replacement and `deleteDirOnDiskHandleChildren` ignored-child precedence: an on-disk file must still match the current scanned `FileInfo` before PHP moves it to a conflict copy, archives it, unlinks it, or promotes the pulled temporary file; nondeletable ignored directory children preserve the local directory, keep the pulled `.syncthing.<name>.tmp` file reusable, and fail with the upstream ignored-files error without scan names.",
  "blocker": "No Syncthing lane-local PHP blocker: lane-only tests pass 195/0 and the required root PHP harness passes. Full Syncthing upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building the full Go module graph, and running 141 Go test files plus integration test paths under `test/`. Go is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`.",
  "latestCommit": "eeb84d0 (Syncthing performFinish existing-file and ignored-directory deletion guards; root harness green before commit)",
  "nextTask": "Broaden upstream `folder_sendrecv` behavior around unavailable peers, case-conflict replacement, receive-only changed children, and post-promotion database update/fsync/event side effects."
}
