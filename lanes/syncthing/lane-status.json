{
  "schemaVersion": 1,
  "library": "syncthing",
  "estimatedProgress": 60,
  "suiteProgress": "verified cloned static upstream inventory counted 658 Go test/benchmark entry points across 141 upstream `_test.go` files plus 264 focused behavior/test paths from 940 tracked paths; bounded upstream package runners previously passed focused block-pull reorderer, blockDiff, handleFile/copier/sparse zero-block pull planning, job queue, device activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index RemoteIndexUpdated, folder completion, request max-size, sqlite global/need, sqlite global drop/recalculation, sqlite needed-file pull-order, and sqlite global-prefix/drop-device slices; native PHP now maps 189 focused tests including upstream `performFinish` tracked-directory replacement and `MaxConflicts` retention pruning through static targeted reads of `performFinish`, `deleteItemOnDisk`, `deleteDirOnDisk`, `moveForConflict`, `existingConflicts`, and `FileInfo.InConflictWith`; full upstream runner not executed",
  "phpPass": 189,
  "phpFail": 0,
  "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders. Current examples include media resume, FileInfo equivalence, BEP request/response/cluster/progress/close frames, Index/IndexUpdate handling, FolderIndexState global/need state, pullBlock retry handling, temporary-file finalization, receive-encrypted finalization, temp-file permission reuse, `wordpress-pull-conflict-replacement.php` for concurrent media edit conflict copies, and the new `wordpress-pull-directory-replacement.php` scenario where a stale generated media directory is removed before a Playground archive file is promoted.",
  "phase": "verified cloned static upstream test inventory plus bounded focused upstream runner evidence and native scanner, protocol vector, DeviceID/Luhn, FileInfo conflict/equivalence, FolderCompletion, FolderIndexState, FileInfoBatch, protocol validation/wire-path, BEP wire/session/callbacks, request/response exchange, Index/IndexUpdate receive and send paths, DownloadProgress, ProgressEmitter, temporary-block planning, least-busy activity, pullBlock retry/verification/zero-block/receive-encrypted trust, temporary-file block writes/final close/final promotion/provisional permissions/read-only reuse/performFinish conflict-copy replacement/tracked-directory replacement/conflict-retention pruning/receive-encrypted trailer append, block-pull ordering, blockDiff, PullWorkPlan, pull job queue, service-map, request-serving, ignore, receive-encrypted request/envelope/data/FileInfo/trailer/finalization, encrypted DownloadProgress, encryption consistency, and PasswordToken slices",
  "audit": "audited 2026-05-23: lane-local Syncthing PHP tests pass 189/0 across 32 test files with 1564 assertions. Required root `php tools/run-tests.php` was rerun after the implementation batch and passes with 166 test files, 15382 assertions, and 0 failures in the current shared worktree. Static upstream inventory remains 610 Test functions, 48 Benchmark functions, and 0 Fuzz functions across 141 `_test.go` files. Focused upstream evidence for this slice is static targeted inspection of `lib/model/folder_sendrecv.go`, `lib/model/folder_sendrecv_test.go`, and `lib/protocol/bep_fileinfo.go`, especially `performFinish`, `deleteItemOnDisk`, `deleteDirOnDisk`, `moveForConflict`, `conflictName`, `existingConflicts`, `FileInfo.InConflictWith`, `TestSRConflictReplaceFileByDir`, `TestSRConflictReplaceFileByLink`, and `TestPullCaseOnlyPerformFinish`; no new full upstream runner parity was attempted.",
  "currentWork": "Native `PullTemporaryFile` now maps two additional upstream `performFinish` boundaries: a tracked existing directory or symlink can be deleted before a pulled regular file is promoted, and `MaxConflicts` pruning keeps only the newest conflict copies after `moveForConflict`. The directory replacement path avoids creating a conflict copy for WordPress media archive files, while regular concurrent file edits still retain `.sync-conflict-YYYYMMDD-HHMMSS-device` siblings.",
  "blocker": "No Syncthing lane-local or required root PHP harness blocker in this run. Full Syncthing upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building the full Go module graph, and running 141 Go test files plus integration test paths under `test/`. Go is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`. This tracked-directory replacement and conflict-retention slice used targeted static upstream reads only.",
  "latestCommit": "implementation commit 0fec3b5: native Syncthing performFinish tracked-directory replacement and conflict retention after green root PHP with 166 test files, 15382 assertions, 0 failures",
  "nextTask": "Broaden upstream `folder_sendrecv` behavior around versioner/archive replacement during `performFinish`, case-conflict replacement, receive-only changed children, unavailable peers, and database update side effects after native final file promotion succeeds."
}
