{
  "schemaVersion": 1,
  "library": "syncthing",
  "estimatedProgress": 76,
  "suiteProgress": "verified cloned static upstream inventory counted 658 Go test/benchmark entry points across 141 upstream `_test.go` files plus 264 focused behavior/test paths from 940 tracked paths; bounded upstream package runners include focused block-pull reorderer, blockDiff, handleFile/copier/sparse zero-block pull planning, job queue, device activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index RemoteIndexUpdated, folder completion, request max-size, sqlite global/need, sqlite global drop/recalculation, sqlite needed-file pull-order, sqlite global-prefix/drop-device, versioner Archive/tagging, directory deletion scan/error precedence including ignored-child precedence, `performFinish` existing-file scan guard/case-only slices, finisher deregistration failure tests, handleDir/handleSymlink lifecycle tests, deleteFile/deleteDir lifecycle tests, static `processDeletions` batch-order mapping, and focused `TestPullCaseOnlyRename` rename-shortcut evidence; native PHP now maps 235 focused tests including unavailable-peer block availability/temporary-state cleanup, post-promotion dbUpdaterRoutine fsync/updateLocals/RemoteChangeDetected/ReceivedFile batching, finisherRoutine queue completion/progress deregistration/ItemFinished/temp pull error handling, end-of-pull tempPullErrors promotion into FolderErrors, pullScannerRoutine-style deferred post-pull scan aggregation, native handleDir/handleSymlink ItemStarted/ItemFinished/dbUpdate scheduling, native deleteFile/deleteDir dbUpdateDeleteFile/dbUpdateDeleteDir scheduling, native processDeletions file-before-dir/reverse-dir scheduling, native processNeeded/renameFile same-block rename shortcut scheduling, and native processNeeded/shortcutFile metadata-only dbUpdateShortcutFile scheduling; full upstream runner not executed",
  "phpPass": 235,
  "phpFail": 0,
  "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders. Current examples include BEP request/response/cluster/progress/close frames, Index/IndexUpdate handling, FolderIndexState global/need state, pullBlock retry handling, temporary peer disconnect fallback, temporary-file finalization, receive-encrypted finalization, temp-file permission reuse, conflict copies, scan guards, case-only conflict preservation, directory replacement guards, version archive replacement, post-promotion DB update batching in `wordpress-pull-db-updater.php`, `wordpress-pull-finisher.php` where a finalized Playground media pull completes the queue, emits an ItemFinished-style payload, deregisters progress, records block stats, and hands off the database update, `wordpress-folder-errors.php` where a failed media pull promotes `syncing: finishing:` into persistent FolderErrors while retaining the temporary file for retry, `wordpress-post-pull-scan-scheduler.php` where local-first media and stale Playground folder scan requests are de-duplicated and deferred until the pull scanner closes, `wordpress-directory-symlink-lifecycle.php` where a Playground peer creates a dated upload directory plus `current/latest` symlink with ItemStarted/ItemFinished, remote-change, and fsync/db-update boundaries, `wordpress-delete-lifecycle.php` where a Playground peer deletes an old upload plus a deletable private cache directory with dbUpdateDeleteFile/dbUpdateDeleteDir boundaries, `wordpress-process-deletions.php` where stale media-file tombstones are applied before reverse-ordered month/year directory cleanup, `wordpress-rename-shortcut.php` where a staged media import is renamed into the dated uploads folder without re-downloading the same block content, and `wordpress-metadata-shortcut.php` where a peer updates media permissions/mtime without re-downloading unchanged bytes.",
  "phase": "verified cloned static upstream test inventory plus bounded focused upstream runner evidence and native scanner, protocol vector, DeviceID/Luhn, FileInfo conflict/equivalence, FolderCompletion, FolderIndexState, FileInfoBatch, protocol validation/wire-path, BEP wire/session/callbacks, request/response exchange, Index/IndexUpdate receive and send paths, DownloadProgress, ProgressEmitter, temporary-block planning, connection-aware unavailable-peer cleanup, least-busy activity, pullBlock retry/verification/zero-block/receive-encrypted trust, temporary-file block writes/final close/final promotion/performFinish conflict and directory guards/post-promotion dbUpdaterRoutine side effects/finisherRoutine lifecycle/folder-error promotion handling/pullScannerRoutine deferred scan scheduling/handleDir and handleSymlink item lifecycle/deleteFile and deleteDir item lifecycle/processDeletions batch ordering/renameFile shortcut batching/metadata-only shortcutFile dbUpdateShortcutFile scheduling/receive-encrypted trailer append, block-pull ordering, blockDiff, PullWorkPlan, pull job queue, service-map, request-serving, ignore, receive-encrypted request/envelope/data/FileInfo/trailer/finalization, encrypted DownloadProgress, encryption consistency, and PasswordToken slices",
  "audit": "audited 2026-05-23: lane-local Syncthing PHP tests pass 235/0 across 37 test files with 1892 assertions after adding native PullItemUpdater metadata-shortcut coverage. Focused upstream evidence for this batch is static targeted reads of `lib/model/folder_sendrecv.go` around `processNeeded`, `shortcutFile`, and `dbUpdaterRoutine`; no direct upstream package test was found for this exact branch. `wordpress-metadata-shortcut.php` runs and shows dbUpdateShortcutFile with unchanged media bytes and no ReceivedFile marker. Exact root `php tools/run-tests.php` completed after briefly waiting on the test lock and exits 0 with 183 test files, 18316 assertions, and 0 failures. Static upstream inventory remains 610 Test functions, 48 Benchmark functions, and 0 Fuzz functions across 141 `_test.go` files.",
  "currentWork": "Native `PullItemUpdater` now maps Syncthing processNeeded/shortcutFile metadata behavior: needed regular files whose current local FileInfo has identical block content skip full handle-file work, emit ItemStarted/ItemFinished metadata lifecycle payloads, update permissions unless ignored, apply the upstream mtime, schedule dbUpdateShortcutFile, leave nonmatching files for full pull work, and report missing on-disk files without creating placeholders.",
  "blocker": "No Syncthing lane-local or root PHP blocker: lane-only tests pass 235/0 across 37 files with 1892 assertions, `wordpress-metadata-shortcut.php` runs, and exact `php tools/run-tests.php` exits 0 with 183 files, 18316 assertions, and 0 failures. Full Syncthing upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building the full Go module graph, and running 141 Go test files plus integration test paths under `test/`. Go is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`.",
  "latestCommit": "HEAD Map Syncthing metadata shortcut",
  "nextTask": "Target the receive-encrypted shortcutFile branch that rewrites the encrypted FileInfo trailer and adjusts advertised size before scheduling dbUpdateShortcutFile."
}
