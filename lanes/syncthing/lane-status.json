{
    "schemaVersion": 1,
    "library": "syncthing",
    "estimatedProgress": 90,
    "suiteProgress": "verified cloned static upstream inventory counted 658 Go test/benchmark entry points across 141 upstream `_test.go` files, 50 upstream test package directories, 94 Go package directories, and 264 focused behavior/test paths from 940 tracked paths; bounded upstream package runners now include scanner `TestVerify`/`TestWalk` byte-counter/walk evidence plus prior scanner normalization, block-pull reorderer, blockDiff, pull planning, job queue, device activity, FileInfoBatch, service-map, indexhandler, receive-index, folder completion, request max-size, sqlite global/need/drop/pull-order/prefix, versioner, directory deletion, scanner walk, scanner ownership/xattr, scanner block-size hysteresis, scanner unchanged-file, scanner IgnorePerms/mod-time-window, and scanner symlink evidence; this batch adds static host-specific scanner Windows executable-bit inventory from `updateFileInfo` plus a skipped Linux-host probe of upstream `TestScanOwnershipWindows`; native PHP now maps 276 focused tests including scanner Windows executable-bit preservation; full upstream runner not executed",
    "phpPass": 276,
    "phpFail": 0,
    "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders. Current examples include BEP request/response/cluster/progress/close frames, Index/IndexUpdate handling, FolderIndexState global/need state, pullBlock retry handling, temporary-file finalization, receive-encrypted finalization, conflict copies, scan guards, directory/symlink/delete lifecycle examples, process-deletions, rename and metadata shortcuts, scanner platform metadata, scanner walk includes, scanner block hysteresis, scanner unchanged shortcut, scanner IgnorePerms/mod-time-window, scanner normalization in `wordpress-scanner-normalization.php`, scanner progress events in `wordpress-scanner-progress-events.php`, scanner Windows executable-bit preservation in `wordpress-scanner-windows-exec-bits.php`, xattr replacement/unsupported/hard-error scenarios, and receive-encrypted shortcut/retry-loop scenarios.",
    "phase": "verified cloned static upstream test inventory plus bounded focused upstream runner evidence and native scanner Windows executable-bit preservation, FolderScanProgress byte totals, scanner UTF-8 normalization, scanner walk, scanner block-size hysteresis, scanner unchanged-file short-circuiting, scanner IgnorePerms/mod-time-window/symlink equivalence, protocol vector, DeviceID/Luhn, FileInfo conflict/equivalence/platform metadata, FolderCompletion, FolderIndexState, FileInfoBatch, BEP wire/session/callbacks, request/response exchange, DownloadProgress, ProgressEmitter, pull planning, service-map, request-serving, ignore, receive-encrypted request/envelope/data/FileInfo/trailer/finalization, encryption consistency, and PasswordToken slices",
    "audit": "audited 2026-05-23: targeted upstream static reads counted the Windows-only scanner `updateFileInfo` executable-bit branch and one Windows-only `TestScanOwnershipWindows`; `go test ./lib/scanner -run '^TestScanOwnershipWindows$' -count=1 -v` passed by skipping on this Linux host with `ok github.com/syncthing/syncthing/lib/scanner 0.007s`; focused `php tools/run-tests.php lanes/syncthing/tests/FileInfoScannerTest.php` passed 1 file, 101 assertions, 0 failures; full lane `php tools/run-tests.php lanes/syncthing/tests` passed 39 files, 2103 assertions, 0 failures; `php lanes/syncthing/examples/wordpress-scanner-windows-exec-bits.php` ran with `posixPermissionChangeItems=1`, `windowsPermissionChangeItems=0`, and `windowsAdvertisedPermissions=0755`; required pre-root `pgrep -af '^php tools/run-tests\\.php( |$)'` returned no active root harness, and `php tools/run-tests.php` passed 196 files, 21507 assertions, 0 failures.",
    "currentWork": "Native FileInfoScanner now maps the host-specific Windows regular-file permission branch from upstream `updateFileInfo`: an explicit `platformFamily` option lets tests exercise Windows semantics on this Linux worker, executable bits from the current indexed FileInfo are preserved before equivalence checks, and unchanged WordPress plugin assets do not rescan only because the host disk mode lacks executable bits.",
    "blocker": "No Syncthing lane-local PHP blocker: lane-only tests pass 276/0 across 39 files with 2103 assertions, the scanner Windows executable-bit WordPress example runs, and the required root PHP harness passed 196 files with 21507 assertions. Full Syncthing upstream runner remains unexecuted because .upstream-cache/syncthing is blob-filtered/no-checkout and broad go test ./... would require hydrating the full checkout, downloading/building the Go module graph, and running 141 Go test files plus integration paths under test/. The new Windows-specific upstream runner evidence is static/skipped on this Linux host, not executed Windows parity.",
    "latestCommit": "pending lane-local commit after green root harness",
    "nextTask": "Extend scanner progress toward cancellation/error event boundaries, or map another host-specific scanner branch with executable upstream runner evidence on a matching platform."
}
