{
  "schemaVersion": 1,
  "library": "syncthing",
  "estimatedProgress": 61,
  "suiteProgress": "verified cloned static upstream inventory counted 658 Go test/benchmark entry points across 141 upstream `_test.go` files plus 264 focused behavior/test paths from 940 tracked paths; bounded upstream package runners passed focused block-pull reorderer, blockDiff, handleFile/copier/sparse zero-block pull planning, job queue, device activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index RemoteIndexUpdated, folder completion, request max-size, sqlite global/need, sqlite global drop/recalculation, sqlite needed-file pull-order, sqlite global-prefix/drop-device, versioner Archive/tagging, and directory deletion scan/error precedence slices; native PHP now maps 193 focused tests including upstream `performFinish` version-archive replacement and `deleteDirOnDiskHandleChildren` unknown-child/temp-child boundaries through static targeted reads plus bounded upstream `go test` runs; full upstream runner not executed",
  "phpPass": 193,
  "phpFail": 0,
  "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders. Current examples include media resume, FileInfo equivalence, BEP request/response/cluster/progress/close frames, Index/IndexUpdate handling, FolderIndexState global/need state, pullBlock retry handling, temporary-file finalization, receive-encrypted finalization, temp-file permission reuse, `wordpress-pull-conflict-replacement.php` for concurrent media edit conflict copies, `wordpress-pull-directory-replacement.php` where a stale generated media directory is removed before a Playground archive file is promoted, `wordpress-pull-version-archive.php` where a non-conflicting prior media version is archived under `.stversions` before replacement, and `wordpress-pull-directory-scan-guard.php` where an unknown local thumbnail prevents destructive directory replacement and schedules a follow-up scan.",
  "phase": "verified cloned static upstream test inventory plus bounded focused upstream runner evidence and native scanner, protocol vector, DeviceID/Luhn, FileInfo conflict/equivalence, FolderCompletion, FolderIndexState, FileInfoBatch, protocol validation/wire-path, BEP wire/session/callbacks, request/response exchange, Index/IndexUpdate receive and send paths, DownloadProgress, ProgressEmitter, temporary-block planning, least-busy activity, pullBlock retry/verification/zero-block/receive-encrypted trust, temporary-file block writes/final close/final promotion/provisional permissions/read-only reuse/performFinish conflict-copy replacement/tracked-directory replacement/conflict-retention pruning/version-archive replacement/directory deletion guard/receive-encrypted trailer append, block-pull ordering, blockDiff, PullWorkPlan, pull job queue, service-map, request-serving, ignore, receive-encrypted request/envelope/data/FileInfo/trailer/finalization, encrypted DownloadProgress, encryption consistency, and PasswordToken slices",
  "audit": "audited 2026-05-23: lane-local Syncthing PHP tests pass 193/0 across 32 test files with 1590 assertions. Required root `php tools/run-tests.php` exits 0 with 170 test files, 15660 assertions, and 0 failures. Static upstream inventory remains 610 Test functions, 48 Benchmark functions, and 0 Fuzz functions across 141 `_test.go` files. Focused upstream evidence for this batch includes static targeted inspection of `lib/model/folder_sendrecv.go`, `lib/versioner/versioner.go`, `lib/versioner/simple.go`, `lib/versioner/trashcan.go`, and `lib/versioner/util.go`, plus bounded upstream `go test ./lib/versioner -run 'TestTaggedFilename|TestTrashcanArchiveRestoreSwitcharoo|TestTrashcanRestoreDeletedFile' -count=1` and `go test ./lib/model -run 'TestPullDeleteUnscannedDir|TestPullDeleteIgnoreChildDir' -count=1` passes. No full upstream runner parity was attempted.",
  "currentWork": "Native `PullTemporaryFile` now maps Syncthing `performFinish`/`deleteItemOnDisk` replacement behavior for regular files and tracked directories: descendant non-conflicting updates can archive the old file under `.stversions`, conflicting replacements still use `.sync-conflict` copies, and guarded directory replacement now walks existing children, preserves unknown or changed children with upstream scan names and the `contains changed files` error, while allowing abandoned Syncthing temporary children to be removed before promotion.",
  "blocker": "No Syncthing lane-local PHP blocker: lane-only tests pass 193/0 and the required root PHP harness passes. Full Syncthing upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building the full Go module graph, and running 141 Go test files plus integration test paths under `test/`. Go is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`.",
  "latestCommit": "pending current Syncthing lane commit after green root harness; previous accepted Syncthing commit remains 0fec3b5",
  "nextTask": "Broaden upstream `folder_sendrecv` behavior around case-conflict replacement, ignored-directory deletion precedence, receive-only changed children, unavailable peers, and database update side effects after native final file promotion succeeds."
}
