{
  "schemaVersion": 1,
  "library": "syncthing",
  "estimatedProgress": 68,
  "suiteProgress": "verified cloned static upstream inventory counted 658 Go test/benchmark entry points across 141 upstream `_test.go` files plus 264 focused behavior/test paths from 940 tracked paths; bounded upstream package runners passed focused block-pull reorderer, blockDiff, handleFile/copier/sparse zero-block pull planning, job queue, device activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index RemoteIndexUpdated, folder completion, request max-size, sqlite global/need, sqlite global drop/recalculation, sqlite needed-file pull-order, sqlite global-prefix/drop-device, versioner Archive/tagging, directory deletion scan/error precedence including ignored-child precedence, and `performFinish` existing-file scan guard/case-only slices; native PHP now maps 204 focused tests including unavailable-peer block availability/temporary-state cleanup, upstream `performFinish` version-archive replacement, `scanIfItemChanged` existing-file guards, case-only promotion versus case-conflict no-scan behavior, `deleteDirOnDiskHandleChildren` unknown-child/temp-child/ignored-child/receive-only-child boundaries, and post-promotion `dbUpdaterRoutine` fsync/updateLocals/ReceivedFile batching through static targeted reads plus bounded upstream `go test` runs; full upstream runner not executed",
  "phpPass": 204,
  "phpFail": 0,
  "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders. Current examples include media resume, FileInfo equivalence, BEP request/response/cluster/progress/close frames, Index/IndexUpdate handling, FolderIndexState global/need state, pullBlock retry handling, temporary peer disconnect fallback in `wordpress-temporary-peer-disconnect.php`, temporary-file finalization, receive-encrypted finalization, temp-file permission reuse, `wordpress-pull-conflict-replacement.php` for concurrent media edit conflict copies, `wordpress-pull-unscanned-local-edit.php` where a local editor crop changed after scanning blocks remote replacement and schedules a scan while retaining the pulled temp file, `wordpress-pull-case-only-conflict.php` where a case-detecting local-first media/plugin sync keeps the existing lowercase asset and temp file while surfacing Syncthing's no-scan case-conflict error, `wordpress-pull-directory-replacement.php` where a stale generated media directory is removed before a Playground archive file is promoted, `wordpress-pull-version-archive.php` where a non-conflicting prior media version is archived under `.stversions` before replacement, `wordpress-pull-directory-scan-guard.php` where an unknown local thumbnail prevents destructive directory replacement and schedules a follow-up scan, `wordpress-pull-ignored-directory.php` where an ignored local review cache blocks destructive directory replacement without scheduling scans for ignored paths, `wordpress-pull-receive-only-directory.php` where a receive-only media directory is replaced while scheduling a scan to resurrect local-only editor changes, and `wordpress-pull-db-updater.php` where a finalized Playground media pull is batched into the local index with parent-directory fsync and ReceivedFile-style markers.",
  "phase": "verified cloned static upstream test inventory plus bounded focused upstream runner evidence and native scanner, protocol vector, DeviceID/Luhn, FileInfo conflict/equivalence, FolderCompletion, FolderIndexState, FileInfoBatch, protocol validation/wire-path, BEP wire/session/callbacks, request/response exchange, Index/IndexUpdate receive and send paths, DownloadProgress, ProgressEmitter, temporary-block planning, connection-aware unavailable-peer cleanup, least-busy activity, pullBlock retry/verification/zero-block/receive-encrypted trust, temporary-file block writes/final close/final promotion/provisional permissions/read-only reuse/performFinish existing-file scan guard/case-only case-conflict behavior/conflict-copy replacement/tracked-directory replacement/conflict-retention pruning/version-archive replacement/directory deletion guard/ignored-directory deletion precedence/receive-only directory scan resurrection/post-promotion dbUpdaterRoutine fsync updateLocals and ReceivedFile batching/receive-encrypted trailer append, block-pull ordering, blockDiff, PullWorkPlan, pull job queue, service-map, request-serving, ignore, receive-encrypted request/envelope/data/FileInfo/trailer/finalization, encrypted DownloadProgress, encryption consistency, and PasswordToken slices",
  "audit": "audited 2026-05-23: lane-local Syncthing PHP tests pass 204/0 across 33 test files with 1659 assertions. Required root `php tools/run-tests.php` exits 0 with 176 test files, 16637 assertions, and 0 failures. Static upstream inventory remains 610 Test functions, 48 Benchmark functions, and 0 Fuzz functions across 141 `_test.go` files. Focused upstream evidence for this batch is static targeted inspection of `lib/model/folder_sendrecv.go` `dbUpdaterRoutine`/`dbUpdateJob`, `lib/model/fileinfobatch.go`, and `lib/model/folder.go` `updateLocalsFromPulling`; prior focused runner evidence includes the bounded `go test ./lib/model -run 'TestPullCaseOnlyPerformFinish|TestDeleteIgnorePerms' -count=1` pass plus block-pull, blockDiff, handleFile, queue, device activity, FileInfoBatch, service-map, indexhandler, receive-index, folder completion, request-size, sqlite global/need, versioner, directory deletion, and performFinish runs. No full upstream runner parity was attempted.",
  "currentWork": "Native `PullDbUpdater` now maps Syncthing's post-promotion `dbUpdaterRoutine` boundary: finalized pull update jobs are batched through FileInfoBatch, changed file/shortcut/dir jobs expose upstream fsync candidates, local sequence numbers are reset before updateLocals-style callbacks, tick/close flushes work like the upstream timer/channel close boundaries, and each flushed batch emits only the last non-invalid received file/delete marker.",
  "blocker": "No Syncthing lane-local or root PHP blocker: lane-only tests pass 204/0 and the required root PHP harness passes. Full Syncthing upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building the full Go module graph, and running 141 Go test files plus integration test paths under `test/`. Go is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`.",
  "latestCommit": "pending lane commit for Syncthing case-conflict, receive-only directory, and dbUpdaterRoutine slices; final SHA reported by worker",
  "nextTask": "Target `finisherRoutine` item lifecycle boundaries: ItemFinished events, progress-emitter deregistration, queue completion, and temp pull error recording after successful and failed final close paths."
}
