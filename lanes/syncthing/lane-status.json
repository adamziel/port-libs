{
    "schemaVersion": 1,
    "library": "syncthing",
    "estimatedProgress": 29,
    "suiteProgress": "cloned static upstream inventory counted: 264 unique behavior/test paths from 940 upstream tracked paths plus 658 static Go test/benchmark entry points; scanner, protocol vector, FileInfo conflict/equivalence, protocol filename/request/index consistency, wire-path normalization, BEP Hello/Request/Response/ClusterConfig wire-frame semantics, native LZ4 compressed-frame behavior, Index/IndexUpdate FileInfo protobuf payloads including encrypted metadata field 19, encrypted Index/IndexUpdate collection wrappers, DownloadProgress append/forget plus remote temporary-download state, sent-download outgoing diff computation, ProgressEmitter connection-boundary grouping/cleanup/event summaries, timer-driven scheduler plus wire connection adapters, inbound remote DownloadProgress temporary-block availability/request planning, inbound Request.fromTemporary serving, ignored request path rejection, include/escape-aware ignore parsing with glob ranges/alternatives and skip-dir boundaries, focused ignore match results, receive-encrypted final-file hash-token bypass, receive-encrypted request envelope geometry, encrypted request/response data padding and trimming, deterministic encrypted-name and offset-bound block-hash token transforms, encrypted path parent detection, XChaCha20-Poly1305 encrypted byte payloads, encrypted FileInfo metadata wrapping/decryption, encrypted DownloadProgress no-op routing, receive-encrypted FileInfo trailers plus finalized-file trailer verification, cluster-config encryption consistency, and exact PasswordToken scrypt/AES-SIV derivation mapped; upstream runner not executed",
    "phpPass": 93,
    "phpFail": 0,
    "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders; examples cover continuing a partially synchronized media upload from the first untrusted byte offset, detecting concurrent media/caption edits from separate devices, resolving media tombstone/conflict winner decisions, treating scanner-only metadata noise as equivalent while content changes still force sync, validating normalized WordPress media index updates while rejecting traversal requests, emitting a native BEP Request frame for a missing media block, advertising a WordPress media folder/device ClusterConfig frame, compressing repeated WordPress folder/device metadata with native LZ4 frames, sending a WordPress media IndexUpdate frame with FileInfo block hashes/version counters/sequence metadata/normalized wire paths, advertising temporary media blocks with a native DownloadProgress append frame, computing outgoing new-block-only DownloadProgress deltas plus cleanup forgets for WordPress media pulls, grouping those deltas per subscribed WordPress peer with completed-byte progress summaries, scheduling real BEP DownloadProgress frames for a WordPress media peer only when the puller registry changes, choosing a native Request.fromTemporary plan when a WordPress media block is available only from a peer's temporary file, serving a WordPress media restore request by rejecting stale temporary bytes before returning a native BEP Response frame from the finalized file, serving a receive-encrypted WordPress media restore while an ignored private export path remains blocked, suppressing receive-encrypted private-media DownloadProgress before BEP framing or remote temporary-state mutation, loading included WordPress ignore snippets with escaped literal glob filenames before serving request bytes, shaping a receive-encrypted WordPress media request envelope with deterministic encrypted names, offset-bound block-hash tokens, encrypted response padding, trusted-side plaintext trimming, and a FileInfo trailer without exposing the plaintext filename to the untrusted peer, finalizing a private receive-encrypted WordPress export with a native encrypted FileInfo trailer, stripping host-local trailer bytes before remote index advertising, and verifying encrypted block data before plaintext restoration, wrapping full WordPress media IndexUpdate collections into Syncthing encrypted FileInfo metadata for untrusted index exchange, deriving exact Syncthing receive-encrypted password tokens for WordPress media peers, and rejecting untrusted WordPress media peers unless the cluster config advertises compatible receive-encrypted tokens.",
    "phase": "cloned static upstream inventory plus scanner block, protocol vector, FileInfo conflict/equivalence, protocol validation/wire-path, BEP wire-frame/cluster-config, native LZ4 compressed-frame, Index/IndexUpdate FileInfo protobuf including encrypted field 19, encrypted Index/IndexUpdate collection wrappers, DownloadProgress temporary-block state, sent-download outgoing diff, ProgressEmitter connection-boundary, timer-driven scheduler/connection-adapter, inbound remote temporary-block availability, inbound request-serving, include/escape-aware ignore-match, receive-encrypted request envelope/trailer/finalization, encrypted request/response padding/trim, deterministic encrypted-name and block-hash token transforms, XChaCha20-Poly1305 encrypted byte payloads, encrypted FileInfo metadata wrapping/decryption, encrypted DownloadProgress no-op routing, cluster-config encryption consistency, and exact PasswordToken scrypt/AES-SIV slices",
    "audit": "audited 2026-05-22: lane-local Syncthing PHP tests pass 93/0 with 793 assertions across 16 test files; `php lanes/syncthing/examples/wordpress-receive-encrypted-finalization.php` runs and shows a private WordPress export finalized with a native encrypted FileInfo trailer, local index size including the trailer, remote index size stripping it, plaintext/block-hash verification, and extra-data rejection. Required root `php tools/run-tests.php` passed with 113 test files, 8391 assertions, and 0 failures in the current shared worktree. No upstream runner parity. During prior KDF probing this lane installed `php-sodium`/`libsodium` via `sudo -n dnf install -y php-sodium`, and this slice continues to use sodium XChaCha20-Poly1305 for Syncthing encrypted byte payloads while keeping exact N/r/p scrypt in lane PHP.",
    "currentWork": "Native scanner-style block hashing, version vectors, FileInfo conflict/equivalence behavior, protocol validation/wire-path normalization, BEP Hello/Request/Response/ClusterConfig proto3 payload serialization, uncompressed plus LZ4-compressed post-auth frame handling, Index/IndexUpdate FileInfo protobuf payloads including encrypted metadata field 19, encrypted Index/IndexUpdate collection wrappers from upstream `encryptedConnection.Index`, `encryptedConnection.IndexUpdate`, `encryptedModel.Index`, `encryptedModel.IndexUpdate`, `encryptFileInfos`, and `decryptFileInfos`, DownloadProgress append/forget payloads and message type 5 frames, legacy FDPU fixture decoding, remote temporary-download append/forget state, outgoing sent-download state, ProgressEmitter subscription grouping/cleanup/event summary behavior, timer-gated scheduler plus BEP wire connection adapters from upstream `progressemitter.go`, `progressemitter_test.go`, and `protocol.go`, inbound model.DownloadProgress sharing guards, RemoteDownloadProgress summaries, blockAvailabilityFromTemporary, Request.fromTemporary planning from upstream `model.go` and `folder_sendrecv.go`, inbound Request.fromTemporary serving from upstream `model.Request`, `readOffsetIntoBuf`, `scanner.Validate`, `fs.IsInternal`, and `fs.TempName`, request ignored-file, include/escape-aware ignore parsing, receive-encrypted hash-token guards from `model.Request` and `lib/ignore`, receive-encrypted request envelope/path/trailer helpers plus finalized-file trailer writing, local trailer-size accounting, outbound index size stripping, and decrypt-tool-style trailer/block verification from upstream `sharedPullerState.finalizeEncrypted`, `writeEncryptionTrailer`, `prepareFileInfoForIndex`, and `cmd/syncthing/decrypt`, deterministic `encryptName`, `decryptName`, `KeyGenerator.FileKey`, and `encryptBlockHash` behavior from `lib/protocol/encryption.go` and `lib/protocol/encryption_test.go`, encrypted request/response data handling from upstream `encryptedConnection.Request` and `encryptedModel.Request`, XChaCha20-Poly1305 `encryptBytes`/`DecryptBytes`, `encryptFileInfo`/`DecryptFileInfo` encrypted metadata wrapping from upstream `encryption.go`, encryptedConnection.DownloadProgress and encryptedModel.DownloadProgress no-op routing for encrypted folders, cluster-config encryption consistency decisions from `model.ccCheckEncryption` and `TestCcCheckEncryption`, plus exact `PasswordToken` derivation from `KeyFromPassword`, `knownBytes`, and AES-SIV fixtures in `TestKeyDerivation`.",
    "blocker": "Full upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building Go dependencies, and running 141 Go test files plus integration test paths under `test/`. Go itself is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`. Lane-local Syncthing tests are green and the required root PHP suite passed. Remaining native gaps include receive-encrypted synthetic parent directory scan/index cleanup, broader encrypted request serving integration, and full upstream runner parity.",
    "latestCommit": "05abd65",
    "nextTask": "Map receive-encrypted folder scanning/index cleanup for synthetic encrypted parent directories from `folder.go`, then broaden encrypted request serving integration beyond the focused finalization and trailer verification path."
}
