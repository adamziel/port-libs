{
    "schemaVersion": 1,
    "library": "syncthing",
    "estimatedProgress": 48,
    "suiteProgress": "verified cloned static upstream inventory counted 658 Go test/benchmark entry points across 141 upstream `_test.go` files plus 264 focused behavior/test paths from 940 tracked paths; bounded upstream package runners passed focused block-pull reorderer, job queue, device activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index RemoteIndexUpdated, folder completion, request max-size, and sqlite global/need slices; native PHP now maps 159 focused tests covering scanner blocks, vectors, DeviceID base32/Luhn identity parsing, FileInfo conflict/equivalence, FolderCompletion progress math, in-memory FolderIndexState global/need selection, FileInfoBatch lifecycle, protocol validation, BEP wire/session/callbacks, request/response exchange, Index/IndexUpdate send and receive behavior, DownloadProgress, sent and remote progress state, least-busy device activity, block-pull ordering, pull job queue, service-map lifecycle, indexhandler registry lifecycle, request serving including direct max-size guards, upstream temporary filename generation and basename-prefix detection, ignore matching, receive-encrypted helpers, encrypted DownloadProgress no-op routing, cluster-config encryption consistency, and exact PasswordToken scrypt/AES-SIV derivation; full upstream runner not executed",
    "phpPass": 159,
    "phpFail": 0,
    "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders; examples include media resume, FileInfo equivalence, normalized index validation, BEP request/response/cluster/progress/close frames, stream-backed BEP frame IO and model callbacks, bounded FileInfoBatch flushing for WordPress media IndexUpdate frames, sequence-aware IndexHandler deltas, receive-side WordPress media Index handling that clears stale temporary-block progress and schedules a pull, in-memory global/need state for deciding which WordPress media files a local or remote peer still needs after edits, deletes, ignored paths, and full index resets, FolderCompletion reporting for media sync progress and delete-only work, IndexHandler registry pause/resume gating for receive-encrypted shares, raw request/response exchange and stale media retry handling, DeviceID validation for Playground pairing, temporary-source request planning and serving, rejecting zero-length or oversized WordPress media block requests before RequestServer disk reads, filtering `.syncthing.` and `~syncthing~` temporary upload files out of WordPress media publish scans, least-busy peer selection, standard block-pull ordering, pull job queue pagination and priority bumping, service-map lifecycle handling, ignore include/escape handling, receive-encrypted request envelopes and request serving, encrypted metadata/index exchange, encrypted finalization and verification, and cluster-config password-token checks for untrusted media peers.",
    "phase": "verified cloned static upstream test inventory plus bounded upstream block-pull, job-queue, device-activity, FileInfoBatch, service-map, indexhandler, receive-index, folder-completion, request max-size, and sqlite global/need runner evidence and native scanner, protocol vector, DeviceID/Luhn, FileInfo conflict/equivalence, FolderCompletion, FolderIndexState global/need, FileInfoBatch lifecycle, protocol validation/wire-path, BEP wire-frame/cluster-config/compression/control-frame, stream-backed post-auth frame IO, bounded BEP session state, stream-backed model callbacks, raw request/response exchange, Index/IndexUpdate send and receive paths, IndexHandler registry lifecycle, DownloadProgress, ProgressEmitter, inbound temporary-block planning, least-busy activity selection, block-pull ordering, pull job queue, service-map folder-worker lifecycle, request-serving max-size/temporary/final-file/ignore boundaries, upstream temporary-name generation/detection, include/escape-aware ignore, receive-encrypted request/envelope/data/FileInfo/trailer/finalization, encrypted DownloadProgress, encryption consistency, and PasswordToken slices",
    "audit": "audited 2026-05-22: lane-local Syncthing PHP tests pass 159/0 with 1370 assertions across 29 test files; required root `php tools/run-tests.php` passes with 147 test files, 13043 assertions, and 0 failures. Static upstream inventory remains 610 Test functions, 48 Benchmark functions, and 0 Fuzz functions across 141 `_test.go` files. Focused upstream `go test ./internal/db/sqlite -run '^(TestNeed|TestNeedDeleted|TestDontNeedIgnored|TestLocalDontNeedDeletedMissing|TestRemoteDontNeedDeletedMissing|TestNeedRemoteSymlinkAndDir|TestNeedPagination)$' -count=1` passed with `ok github.com/syncthing/syncthing/internal/db/sqlite 0.041s`; prior focused block-pull, job-queue, device-activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index, folder-completion, and request max-size runners also pass in throwaway worktrees. No full upstream runner parity.",
    "currentWork": "Native Syncthing FolderIndexState now maps focused upstream sqlite global/need semantics from `internal/db/sqlite/db_global_test.go`, `folderdb_global.go`, `folderdb_counts.go`, and `folderdb_update.go`: local and remote needed-file lists follow version-vector winner selection, ignored and remote-invalid entries do not create false downloads, deleted globals are needed only by devices with a valid non-deleted copy, directory/symlink/file counts and alphabetic pagination are exposed, and full remote Index resets preserve another peer's need for a re-added WordPress media file. Existing current slices remain RequestServer max-size guards, FolderCompletion, ReceiveIndex behavior, IndexHandler send/registry behavior, ServiceMap, FileInfoBatch, device activity, block-pull ordering, pull job queue, stream-backed BEP frame IO/session dispatch, native scanner, BEP wire/protobuf, DeviceID, request/progress, ignore, and receive-encrypted primitives.",
    "blocker": "Full upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building the full Go module graph, and running 141 Go test files plus integration test paths under `test/`. Go itself is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`. Bounded focused upstream runners cover block-pull, job-queue, device-activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index `TestNeedFolderFiles`, folder completion `TestAddFolderCompletion|TestCompletionEmptyGlobal`, protocol request max-size `TestRequestMaxSize`, and sqlite global/need tests `TestNeed`, `TestNeedDeleted`, `TestDontNeedIgnored`, `TestLocalDontNeedDeletedMissing`, `TestRemoteDontNeedDeletedMissing`, `TestNeedRemoteSymlinkAndDir`, and `TestNeedPagination`; lane-local PHP tests and the required root PHP harness are green as of this run.",
    "latestCommit": "current lane commit: native FolderIndexState global/need slice after green root PHP (147 test files, 13043 assertions, 0 failures)",
    "nextTask": "Connect FolderIndexState to receive-side IndexHandlerRegistry updates, or map upstream sqlite global availability/drop recalculation before broadening protocol/model behavior."
}
