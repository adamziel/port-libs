{
  "schemaVersion": 1,
  "library": "syncthing",
  "estimatedProgress": 70,
  "suiteProgress": "verified cloned static upstream inventory counted 658 Go test/benchmark entry points across 141 upstream `_test.go` files plus 264 focused behavior/test paths from 940 tracked paths; bounded upstream package runners include focused block-pull reorderer, blockDiff, handleFile/copier/sparse zero-block pull planning, job queue, device activity, FileInfoBatch, service-map, indexhandler concurrency, receive-index RemoteIndexUpdated, folder completion, request max-size, sqlite global/need, sqlite global drop/recalculation, sqlite needed-file pull-order, sqlite global-prefix/drop-device, versioner Archive/tagging, directory deletion scan/error precedence including ignored-child precedence, `performFinish` existing-file scan guard/case-only slices, and finisher deregistration failure tests; native PHP now maps 213 focused tests including unavailable-peer block availability/temporary-state cleanup, post-promotion dbUpdaterRoutine fsync/updateLocals/RemoteChangeDetected/ReceivedFile batching, finisherRoutine queue completion/progress deregistration/ItemFinished/temp pull error handling, and end-of-pull tempPullErrors promotion into FolderErrors; full upstream runner not executed",
  "phpPass": 213,
  "phpFail": 0,
  "wordpressScenarios": "Resumable media/content synchronization for local-first WordPress and Playground folders. Current examples include BEP request/response/cluster/progress/close frames, Index/IndexUpdate handling, FolderIndexState global/need state, pullBlock retry handling, temporary peer disconnect fallback, temporary-file finalization, receive-encrypted finalization, temp-file permission reuse, conflict copies, scan guards, case-only conflict preservation, directory replacement guards, version archive replacement, post-promotion DB update batching in `wordpress-pull-db-updater.php`, `wordpress-pull-finisher.php` where a finalized Playground media pull completes the queue, emits an ItemFinished-style payload, deregisters progress, records block stats, and hands off the database update, and `wordpress-folder-errors.php` where a failed media pull promotes `syncing: finishing:` into persistent FolderErrors while retaining the temporary file for retry.",
  "phase": "verified cloned static upstream test inventory plus bounded focused upstream runner evidence and native scanner, protocol vector, DeviceID/Luhn, FileInfo conflict/equivalence, FolderCompletion, FolderIndexState, FileInfoBatch, protocol validation/wire-path, BEP wire/session/callbacks, request/response exchange, Index/IndexUpdate receive and send paths, DownloadProgress, ProgressEmitter, temporary-block planning, connection-aware unavailable-peer cleanup, least-busy activity, pullBlock retry/verification/zero-block/receive-encrypted trust, temporary-file block writes/final close/final promotion/performFinish conflict and directory guards/post-promotion dbUpdaterRoutine side effects/finisherRoutine lifecycle/folder-error promotion handling/receive-encrypted trailer append, block-pull ordering, blockDiff, PullWorkPlan, pull job queue, service-map, request-serving, ignore, receive-encrypted request/envelope/data/FileInfo/trailer/finalization, encrypted DownloadProgress, encryption consistency, and PasswordToken slices",
  "audit": "audited 2026-05-23: lane-local Syncthing PHP tests pass 213/0 across 35 test files with 1731 assertions after adding FolderErrorTracker and PullFinisher integration for tempPullErrors promotion. Focused upstream evidence for this batch includes static targeted inspection of `lib/model/folder_sendrecv.go` `sendReceiveFolder.pull`, `pullerIteration`, `newPullError`, `maxPullerIterations`, and `lib/model/folder.go` `Errors`/`clearScanErrors`, plus the previous focused upstream `go test ./lib/model -run 'TestDeregisterOnFailInCopy|TestDeregisterOnFailInPull' -count=1` evidence for the finisher error source. Required root `php tools/run-tests.php` exits 0 with 178 test files, 17109 assertions, and 0 failures. Static upstream inventory remains 610 Test functions, 48 Benchmark functions, and 0 Fuzz functions across 141 `_test.go` files. No full upstream runner parity was attempted.",
  "currentWork": "Native `FolderErrorTracker` now maps Syncthing's end-of-pull error lifecycle: each pull clears persistent pull errors, each puller iteration resets transient tempPullErrors, duplicate syncing errors keep the first error per path, context cancellation is ignored, only the final iteration's temporary errors promote to persistent pullErrors, sorted `Errors()` combines scan and pull errors, FolderErrors events are emitted with the upstream-shaped folder/errors payload, and PullFinisher can feed failed final-close errors as `syncing: finishing:` folder errors.",
  "blocker": "No Syncthing lane-local or root PHP blocker: lane-only tests pass 213/0 and the required root harness passes 178 files, 17109 assertions, 0 failures. Full Syncthing upstream runner has not executed: `.upstream-cache/syncthing` is blob-filtered/no-checkout with mass tracked deletions, and `go test ./...` would require hydrating the full checkout, downloading/building the full Go module graph, and running 141 Go test files plus integration test paths under `test/`. Go is available as `/usr/bin/go` version `go1.26.3-X:nodwarf5 linux/amd64`.",
  "latestCommit": "Map Syncthing folder error promotion; root harness green",
  "nextTask": "Target pullScannerRoutine scan aggregation and deferred post-pull scan scheduling for files and directories queued during finalization/deletion."
}
