{
    "schemaVersion": 1,
    "library": "libsqlite",
    "estimatedProgress": 99,
    "suiteProgress": "Integrated a current-base libsqlite corpus follow-up batch on top of source 3f92ca2579. Focused verification passed 15 files / 13060 assertions / 0 failures / 6064 selected PASS lines. Accepted-base comparison at 3c71f3e7 over selected files that already existed passed 6 files / 7497 assertions / 0 failures / 3853 PASS lines, so the honest new PASS-line delta is +2211. The batch adds date2 deterministic behavior, trigger/FK recursive/deferred behavior, SELECT derived-table behavior, JSON array-insert behavior, PRAGMA schema follow-ups, expression numcast behavior, WAL persistence and lock-race behavior, UPSERT/RETURNING dynamic arms, CREATE INDEX schema behavior, VFS I/O follow-up, window dynamic frames, and JSONB CHECK source-neutral cleanup. PHP lint for 23 changed/new PHP files, git diff --check -- lanes/libsqlite, changed-file domain-neutral scans, no numbered production-source suffix scan, and the focused no-domain API guard passed. Public PASS estimate moves from 216146 to 218357; mapped coverage remains 958 / 1589 because no new denominator rows are claimed.",
    "phpPass": 218357,
    "phpFail": 0,
    "wordpressScenarios": "Generic application/self-test coverage now includes date2 deterministic behavior, trigger/FK recursive/deferred behavior, SELECT derived-table behavior, JSON array-insert behavior, PRAGMA schema follow-ups, expression numcast behavior, WAL persistence and lock-race behavior, UPSERT/RETURNING dynamic arms, CREATE INDEX schema behavior, VFS I/O follow-up, window dynamic frames, and JSONB CHECK source-neutral cleanup. No domain-specific libsqlite APIs are accepted.",
    "phase": "real upstream corpus admission, source-neutral cleanup, blocker burn-down, behavior fixes, numbered suffix consolidation, and strict generic libsqlite API enforcement",
    "audit": "Accepted current-base follow-up batch after applying 13 compatible handoffs. Verification passed focused combined gate 15 files / 13060 assertions / 0 failures / 6064 PASS lines, accepted-base comparison 6 files / 7497 assertions / 0 failures / 3853 PASS lines, PHP lint for 23 changed/new PHP files, git diff --check -- lanes/libsqlite, source-neutral changed-file scans, no numbered production-source suffix scan, and the focused no-domain API guard.",
    "currentWork": "Published source 0bcc6fa83f with a +2211 focused PASS-line libsqlite batch. Continue polling fresh current-head workers for larger expression/date/json/btree/pager/select handoffs; low-yield stale markers should only ride with a useful batch.",
    "blocker": "The active speed limiter is still handoff yield and freshness, not worker idleness. The supervisor is bypassing conflicting stale markers and preserving full-lane replay for periodic checkpoints.",
    "nextTask": "Consume this accepted batch, then integrate the next fresh current-head high-yield bundle from the visible 10-11 worker pool. Prefer batches above +2500 PASS lines, but publish smaller verified batches when they unblock behavior and keep cadence.",
    "wordpressScenarioLatest": "Latest generic application coverage adds date2 deterministic behavior, trigger/FK recursive/deferred behavior, SELECT derived-table behavior, JSON array-insert behavior, PRAGMA schema follow-ups, expression numcast behavior, WAL persistence and lock-race behavior, UPSERT/RETURNING dynamic arms, CREATE INDEX schema behavior, VFS I/O follow-up, window dynamic frames, and JSONB CHECK source-neutral cleanup. No domain-specific libsqlite APIs are accepted.",
    "latestCommit": "0bcc6fa83f libsqlite: add current-base corpus followups",
    "commit": "0bcc6fa83fa332e744dc066bb925cf71024517b7"
}
