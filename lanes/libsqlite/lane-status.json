{
    "schemaVersion": 1,
    "library": "libsqlite",
    "estimatedProgress": 99.6,
    "suiteProgress": "Supervisor-integrated libsqlite real upstream JSON102 inspection SELECT corpus, insert4 RETURNING transfer corpus, and UTF-16 NOCASE LIKE/RTRIM source-neutral cleanup on current base. Verification passed changed PHP lint, git diff --check, exact SQLitePDO invalid-column/file reproduction, 8 libsqlite example smokes, and focused libsqlite batch of 13 files / 76650 assertions / 0 failures. Broad release/all still has 7 known failures and the full lane memory blocker remains.",
    "phpPass": 5895014,
    "phpFail": 7,
    "wordpressScenarios": "Generic application/self-test coverage now includes source-neutral key-value example filenames/API guards, source-neutral STAT4 planner key-field metadata, triggerC recursive insert depth-cutoff and UPDATE conflict rollback behavior, row-value savepoint default cleanup, upstream window aggregate/window NULL-row behavior, source-neutral UTF-16 NOCASE LIKE/RTRIM default rows, B-tree/index skipscan STAT4 planner behavior, capi3 PRAGMA prepare-tail behavior, expression-affinity modulo casts plus implies_nonnull_row row-dependency coverage, selectC compound-derived wildcard ordinal SELECT coverage, subselect scalar WHERE subqueries, recursive trigger/upsert defaults, JSON path diagnostics, row-value DML LIMIT behavior, pager/WAL checksum/savepoint recovery including pager1 invalid page and rollback max-page real-corpus coverage, FK diagnostics, and app-WAL post-checkpoint recovery. No domain-specific libsqlite API is accepted.",
    "phase": "focused source integration accepted; broad full-lane/release parity remains open and default-memory full lane currently exhausts the runner memory limit before summary",
    "audit": "2026-06-01 supervisor batch: exact SQLitePDO INSERT typo reproduction throws PDOException, leaves zero rows, and creates the backing sqlite file; focused libsqlite batch passed 13 files / 76650 assertions / 0 failures; 8 changed examples passed self-tests. Added +2005 real upstream PASS cases from JSON102 inspection and insert4 RETURNING transfer corpora. Broad release/all was not rerun because the known enforced 1.5G memory exhaustion blocker remains.",
    "currentWork": "Real upstream JSON102 inspection SELECT corpus, insert4 RETURNING transfer corpus, and UTF-16 NOCASE LIKE/RTRIM source-neutral cleanup integrated on current base; SQLitePDO invalid-column behavior verified on the integrated worktree.",
    "blocker": "Broad libsqlite full-lane/release parity is not green: seven known failures remain in prior broad gates, and current full-lane attempts exhausted the enforced 1.5G PHP memory limit before producing a new broad summary.",
    "nextTask": "Prioritize broad full-lane memory exhaustion in SQLiteJsonImportSavepointPlan/rollback-WAL dynamic tests, keep PDO parity audit worker running, then continue non-overlapping real-corpus JSON, VFS, WAL, pager, PRAGMA, B-tree/index, STAT4, expression, trigger/FK, row-value, date/time, cast/like, and upsert coverage.",
    "wordpressScenarioLatest": "Latest libsqlite batch adds generic real upstream JSON102 inspection and insert4 RETURNING transfer coverage, keeps source-neutral LIKE/RTRIM cleanup moving, and confirms SQLitePDO rejects unknown INSERT columns without mutation; no domain-specific libsqlite API is accepted.",
    "latestCommit": "306226e89096a89e478a2188b8fe6c2cebd2d445",
    "commit": "306226e89096a89e478a2188b8fe6c2cebd2d445",
    "latestFocusedSlice": "2026-06-01 supervisor batch: JSON102 inspection SELECT corpus (+1002 PASS cases), insert4 RETURNING transfer corpus (+1003 PASS cases), and UTF-16 NOCASE LIKE/RTRIM source-neutral cleanup. Focused libsqlite batch passed 13 files / 76650 assertions / 0 failures; phpPass moves to 5895014 with broad release/all still at 7 known failures; mapped coverage remains 1589 / 1589."
}
