{
    "schemaVersion": 1,
    "library": "libsqlite",
    "estimatedProgress": 99,
    "suiteProgress": "Source commit d25443ab5 integrates the ninety-fifth current corpus sweep across app-WAL rollback JSON parity, B-tree index expression/bestindex behavior, PRAGMA schema boolean state, SELECT alias/order/parenthesized behavior, UPSERT RETURNING histograms, VFS journal isolation, expression affinity int/real behavior, JSON102/JSONB cases, pager WAL readonly restart, trigger/FK diagnostics/program restrictions/schema lifecycle, and windowB JSON object inverse behavior. Verification passed PHP lint for 28 changed/new PHP files, git diff --check, focused selected tests 20 files / 306436 assertions / 0 failures / 52851 PASS lines, accepted-base overlap 2 files / 71799 assertions / 0 failures / 10556 PASS lines, exact related guard 32 files / 471071 assertions / 0 failures / 65124 PASS lines, changed app-WAL example, changed-source WordPress/numeric-suffix guards, and a default-memory app-WAL comparison proving the OOM is pre-existing on accepted base c30488a9b rather than a batch95 regression.",
    "phpPass": 2831909,
    "phpFail": 0,
    "wordpressScenarios": "Generic application/self-test coverage now also includes app-WAL rollback JSON parity, B-tree index expression and bestindex declaration error behavior, PRAGMA schema boolean state, SELECT alias/order/parenthesized behavior, UPSERT RETURNING histograms and repeated fooval cases, VFS journal isolation, expression affinity int/real behavior, JSON102 string/numeric and JSONB ordered remove behavior, pager WAL readonly restart, trigger/FK create diagnostics/program restrictions/schema lifecycle behavior, and windowB JSON object inverse cases. No new WordPress-specific libsqlite API is accepted.",
    "phase": "blocker burn-down, upstream corpus closure, source-neutral API enforcement, release/all-runner parity, and parallel Gitoxide porting",
    "audit": "Accepted source commit d25443ab5 after batch95. Net selected PASS increase is +42295 after subtracting 10556 accepted-base overlap PASS lines from 52851 focused PASS lines. Mapped coverage remains 1589 / 1589. Default-memory app-WAL OOM was reproduced on accepted base c30488a9b and is tracked as pre-existing memory pressure. A full-directory libsqlite sweep was intentionally stopped as too slow after the focused and exact-related gates passed, so it is not cited as full-lane evidence.",
    "currentWork": "Committed batch95 with 19 accepted source/test-moving current-base handoffs across app-WAL, B-tree, PRAGMA, SELECT, UPSERT, VFS, expression, JSON/JSONB, pager/WAL, trigger/FK, and window behavior. Public selected evidence moves to 2831909 pass / 0 fail with mapped coverage 1589 / 1589. Worker pool is now split 6 libsqlite / 6 gitoxide in tmux main.",
    "blocker": "Remaining libsqlite blockers: pre-existing default-memory app-WAL/pager pressure, baseline PRAGMA/SELECT known-red clusters, rejected/conflicting VFS/JSON/WAL/app-WAL/row-value slices, release/all-runner parity, old accepted WordPress-shaped source debt outside changed batch files, and proof that the full runnable SQLite corpus is green enough for closure. Gitoxide is now active in parallel rather than waiting for perfect libsqlite closure.",
    "nextTask": "Keep the 6 libsqlite / 6 gitoxide split active. Continue integrating fresh current-base libsqlite handoffs with accepted-base PASS deltas while Gitoxide workers produce bounded upstream-backed protocol, transport, object, pack/index, merge/tree, and reference-transaction patches. Publish only verified status/dashboard snapshots.",
    "wordpressScenarioLatest": "No WordPress-specific libsqlite API changes were accepted. Batch95 focuses on generic SQLite corpus behavior and changed-source neutral/numeric-suffix guards.",
    "latestCommit": "d25443ab5c libsqlite: add ninety-fifth current corpus sweep",
    "commit": "d25443ab5cdb5337338c1ac2374cdcf3c839b803"
}
