{
    "schemaVersion": 1,
    "library": "libsqlite",
    "estimatedProgress": 98,
    "suiteProgress": "Cloned static upstream inventory plus clean upstream SQLite veryquick runner parity. The strengthened static denominator counts 1589 hydrated upstream Tcl/C/harness test inventory units; full veryquick remains the strongest bounded runner evidence: 1235 scripts / 329670 upstream tests / 0 errors. Accepted JSON scalar/path/JSONB, aggregate, WAL, and LIKE/GLOB evidence is preserved. Latest isolated refill adds json_tree(X, root) selected-root key/path row parity on top of checkpoint-style WAL database image overlay, checksum validation, bounded WAL header/frame parsing, and committed page-image extraction, reusing the static focused JSON table inventory because this worktree has no hydrated upstream cache.",
    "phpPass": 279,
    "phpFail": 0,
    "wordpressScenarios": "Accepted JSON preflight scenarios remain preserved. Accepted LIKE/GLOB option-name diagnostics cover escaped LIKE \\_transient\\_% matching with SQLite default ASCII case folding and case-sensitive GLOB patterns. The latest isolated json_tree smoke reports selected-root key/fullkey/path/root shape for copied wp_options option_value subtrees before import.",
    "phase": "upstream veryquick runner parity plus accepted native JSON/WAL evidence, bounded LIKE/GLOB option-name matching, WAL checksum/checkpoint-image handling, and json_tree selected-root row parity",
    "audit": "isolated json_tree selected-root refill 2026-05-26 adds SQLite-compatible key/path shape for json_tree(X, root) selected root rows; php -l for changed PHP files passed; php lanes/libsqlite/examples/wordpress-json-tree-option-settings.php passed and reported selectedRootShape; php tools/run-tests.php lanes/libsqlite/tests/SQLiteHeaderTest.php passed with 1 file / 2319 assertions / 0 failures; manifest/status JSON validation passed; git diff --check -- lanes/libsqlite passed; root no-argument harness pending integrator.",
    "currentWork": "Latest isolated refill aligns json_tree(X, root) selected-root rows with SQLite table-valued output by deriving the selected node key and parent path from the supplied root path while preserving hidden json/root columns. Dependency closure: no new support component is needed; this reuses lane-local JSON path lookup, JSON5/JSONB decoding, table row shaping, and WordPress option-value smoke coverage and counts no shared support-library progress.",
    "blocker": "Full SQLite all/release runner parity remains pending; this isolated worktree did not include the hydrated upstream cache, so no new upstream testfixture run was performed. This slice is bounded to json_tree selected-root row shape; virtual-table planner constraint pushdown, SQL query integration, window-frame aggregate scheduling, malformed JSONB edge parity, WAL-index/shared-memory behavior, rollback journals, savepoints, and broader B-tree delete/rebalance remain inactive separate slices.",
    "latestCommit": "isolated marker pending for libsqlite-json-tree-selected-root; php -l passed, WordPress json_tree smoke passed with selectedRootShape key/fullkey/path/root output, focused lane tests passed with 1 file / 2319 assertions / 0 failures, manifest/status JSON validation passed, git diff --check -- lanes/libsqlite passed, root harness pending integrator.",
    "nextTask": "After integrator acceptance, map bounded JSON table planner constraints, malformed JSONB edge parity, or rollback-journal/savepoint behavior as a separate libsqlite slice."
}
