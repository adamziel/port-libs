{
    "schemaVersion": 1,
    "library": "libsqlite",
    "estimatedProgress": 99.6,
    "suiteProgress": "Supervisor-integrated libsqlite app-WAL JSON rollback tail, row-value UPDATE/DELETE LIMIT COLLATE, and real upstream date hexio REAL boundary follow-ups. Verification passed changed PHP lint, git diff --check, focused libsqlite gate 5 files / 7911 assertions / 0 failures, and application-WAL example smoke; exact SQLitePDO invalid-column repro still throws PDOException and leaves zero rows.",
    "phpPass": 5897230,
    "phpFail": 7,
    "wordpressScenarios": "Generic application/self-test coverage now includes source-neutral key-value example filenames/API guards, source-neutral STAT4 planner key-field metadata, triggerC recursive insert depth-cutoff and UPDATE conflict rollback behavior, row-value savepoint default cleanup, row-value DML LIMIT behavior including COLLATE postfix dynamic LIMIT/OFFSET parity, upstream date hexio REAL boundary behavior, upstream window aggregate/window NULL-row behavior, source-neutral UTF-16 NOCASE LIKE/RTRIM default rows, B-tree/index skipscan STAT4 planner behavior, capi3 PRAGMA prepare-tail behavior, expression-affinity modulo casts plus implies_nonnull_row row-dependency coverage, selectC compound-derived wildcard ordinal SELECT coverage, subselect scalar WHERE subqueries, recursive trigger/upsert defaults, JSON path diagnostics, pager/WAL checksum/savepoint recovery including app-WAL final recovery-checkpoint follow-up tail rollback, FK diagnostics, and app-WAL post-checkpoint recovery. No domain-specific libsqlite API is accepted.",
    "phase": "focused source integration accepted; broad full-lane/release parity remains open and default-memory full lane currently exhausts the runner memory limit before summary",
    "audit": "2026-06-01 supervisor source 1a0db522af74: PHP lint on changed/new PHP passed; git diff --check passed; focused libsqlite gate passed 5 files / 7911 assertions / 0 failures; application-wal rollback JSON example smoke passed; exact SQLitePDO INSERT typo repro throws PDOException and leaves zero rows. Broad release/all was not rerun because seven known failures and the full-lane memory blocker remain.",
    "currentWork": "App-WAL JSON rollback tail, row-value LIMIT/COLLATE dynamic parity, and upstream date hexio REAL boundary coverage integrated on current base.",
    "blocker": "Broad libsqlite full-lane/release parity is not green: seven known failures remain in prior broad gates, and current full-lane attempts exhausted the enforced 1.5G PHP memory limit before producing a new broad summary.",
    "nextTask": "Keep half the workers on libsqlite until the seven broad failures and full-lane memory exhaustion are eliminated; focus on JSON/WAL rollback, pager, VFS, date/time, row-value, PRAGMA/schema, trigger/FK, expression, and PDO native-parity gaps.",
    "wordpressScenarioLatest": "Latest libsqlite batch adds 1213 upstream PASS cases across app-WAL JSON rollback tails, row-value LIMIT/COLLATE, and date hexio REAL boundary behavior; no domain-specific API is accepted.",
    "latestCommit": "1a0db522af743befb547f81889cff3320e672e68",
    "commit": "1a0db522af743befb547f81889cff3320e672e68",
    "latestFocusedSlice": "2026-06-01 supervisor batch: app-WAL JSON rollback tail (+877 PASS), row-value UPDATE/DELETE LIMIT COLLATE (+77 PASS), and real upstream date hexio REAL boundary (+259 PASS). Focused libsqlite gate passed 5 files / 7911 assertions / 0 failures; phpPass moves 5896017 -> 5897230; mapped coverage remains 1589 / 1589."
}
