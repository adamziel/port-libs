{
    "schemaVersion": 1,
    "library": "libsqlite",
    "estimatedProgress": 99.6,
    "suiteProgress": "Supervisor-integrated JSON subtype lazy if()/iif() and UPSERT RETURNING quote()/IS NULL parity. Focused upstream-backed gates passed; broad release parity remains at 16 known failures.",
    "phpPass": 6247535,
    "phpFail": 16,
    "wordpressScenarios": "Libsqlite source remains guarded against WordPress-shaped declarations/text; latest accepted source adds generic SQLite JSON subtype lazy branch behavior and UPSERT RETURNING quote()/IS NULL parity while PDO invalid-column native parity remains green.",
    "phase": "current-base source integration accepted by supervisor with focused JSON subtype and UPSERT RETURNING evidence; broad full-lane/release parity still has known failures",
    "audit": "2026-06-01 20:13 UTC supervisor integration: source commit 287e36260b651a5e9c2cbd5a6899c6dc389dbbb4 accepted JSON subtype lazy if()/iif() evaluation plus UPSERT RETURNING quote()/IS NULL parity. PHP lint passed for changed files; git diff --check passed; rg WordPress|wordpress|wp_ over lanes/libsqlite/src returned no matches; focused new gates passed 2 files / 37013 assertions / 0 failures; regression bundle passed 6 files / 284388 assertions / 0 failures; mapped coverage remains 1589 / 1589; broad full-lane/release parity remains open with 16 observed failures. PDO invalid target-column audit on current main still throws table test has no column named namedd and leaves rows empty.",
    "currentWork": "JSON subtype lazy if()/iif() semantics, UPSERT RETURNING quote()/IS NULL projection parity, source-neutral UTF-16 cleanup, PDO invalid DML native parity, and remaining broad failure triage integrated on current main.",
    "blocker": "Broad libsqlite full-lane/release parity is not green: latest selected evidence still shows 16 failures, so workers continue reducing the failing set and PDO/native behavior gaps.",
    "nextTask": "Run/triage the 16 broad libsqlite failures, continue focused upstream-corpus slices for WITHOUT ROWID, window functions, trigger/FK, PRAGMA/schema, row-value, WAL/VFS, JSON subtype, UPSERT/RETURNING, and PDO parity, and keep the no-domain source guard green.",
    "wordpressScenarioLatest": "Latest libsqlite batch adds source-neutral JSON subtype lazy branch and UPSERT RETURNING quote()/IS NULL parity; PDO invalid INSERT target-column parity remains green.",
    "latestCommit": "287e36260b651a5e9c2cbd5a6899c6dc389dbbb4",
    "commit": "287e36260b651a5e9c2cbd5a6899c6dc389dbbb4",
    "latestFocusedSlice": "2026-06-01 supervisor batch: JSON subtype lazy if()/iif() and UPSERT RETURNING quote()/IS NULL parity. Focused new gates passed 2 files / 37013 assertions / 0 failures; regression bundle passed 6 files / 284388 assertions / 0 failures; phpPass moves 6210522 -> 6247535; mapped coverage remains 1589 / 1589; broad full-lane/release parity remains open with 16 failures. PDO invalid target-column audit on current main remains green."
}
