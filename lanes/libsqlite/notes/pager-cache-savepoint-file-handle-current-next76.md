# pager-cache-savepoint-file-handle-current-next76

- Behavior: added `SQLitePagerCacheSavepointFileHandlePlan::currentNext()` to bridge pager cache dirty-page savepoints to the existing native `SQLiteVfsFileHandle`. It captures before-images from the current database file, writes current dirty pages, restores those pages on `ROLLBACK TO`, then captures and writes the next retry pages from the restored image.
- Application smoke: `examples/application-pager-cache-savepoint-file-handle-current-next76.php` applies a copied `wp_options` plugin-settings import where an autoload index page and plugin row are dirtied, rolled back, and retried through file handles without ext/sqlite.
- Focused evidence: `php tools/run-tests.php lanes/libsqlite/tests/SQLitePagerCacheSavepointFileHandleCurrentNext76Test.php` passed with `1 test files, 50 assertions, 0 failures`.
- Example evidence: `php lanes/libsqlite/examples/application-pager-cache-savepoint-file-handle-current-next76.php` emitted status `applied`, rollback pages `[2,3]`, next pages `[2,4]`, final page 2 `autoload-index-retry`, final page 3 `plugin-row-before`, and final page 4 `plugin-row-next`.
- Status delta: `lane-status.json` `phpPass` moves from `28917` to `28967` by the 50 new focused PASS lines. Mapped upstream denominator is unchanged because this is native behavior coverage, not a new upstream inventory row.
- Non-overlap: avoids accepted VFS savepoint rollback apply, WAL byte truncation, rollback-journal commit/apply, VFS file writer/locked writer/sync/lock clusters, pager dirty-page cache spill planning, pager statement-journal rollback, hot-journal/super-journal recovery, B-tree freeblock/freelist/page-move/root-collapse, JSON table source/cursor/constraint work, SELECT SQL text/subquery/group/order clusters, and Unicode GLOB behavior.
- Dependency closure: reuses existing bounded `SQLiteVfsFileHandle` and `SQLiteSavepointStack`; no new support component is required.
