# Libsqlite merged-head smoke 2026-05-29T05:33:17Z

Worktree:

- Path: `.tmux-team/worktrees/merged-smoke-batch-20260529T053317Z`
- Base/head checked: `c776c477de561db02c3ab4fc358f5229d58a9497`
- Scope: bounded smoke for merged libsqlite VFS current-source `next266-273`, plus adjacent `next258-265` integration continuity.

Files inspected from merged head:

- `lanes/libsqlite/src/SQLiteVfsCurrentSourceNext266273Plan.php`
- `lanes/libsqlite/tests/SQLiteVfsCurrentSourceNext266273Test.php`
- `lanes/libsqlite/examples/wordpress-vfs-current-source-next266-273.php`
- `lanes/libsqlite/notes/vfs-current-source-next266-273.md`

Commands and results:

```sh
php -l lanes/libsqlite/src/SQLiteVfsCurrentSourceNext266273Plan.php
```

Result: pass, no syntax errors.

```sh
php -l lanes/libsqlite/tests/SQLiteVfsCurrentSourceNext266273Test.php
```

Result: pass, no syntax errors.

```sh
php -l lanes/libsqlite/examples/wordpress-vfs-current-source-next266-273.php
```

Result: pass, no syntax errors.

```sh
php tools/run-tests.php lanes/libsqlite/tests/SQLiteVfsCurrentSourceNext266273Test.php
```

Result: pass. Exact summary: `1 test files, 20 assertions, 0 failures`.

Assertions covered:

- Dependency marker includes `vfs-current-source-snapshot-reuse-publish-next266-273`.
- Prior prerequisites include `next234-237`, `next242-245`, and `next258-265`.
- Reusable snapshot claim records `reader-reuse-next273` with no blockers.
- Publish uses the claim, preserves `shared-cache-next265`, and advances published count to 7.
- Dirty pages, stale ack, missing claim, stale claim, empty operations, bad claim token, and unsupported operation are rejected.
- Non-overlap note references the prior `next258-265` window.

```sh
php lanes/libsqlite/examples/wordpress-vfs-current-source-next266-273.php
```

Result: pass, exit 0. The example printed the plan array showing `status => published-reused-current-source`, `reader-reuse-next273`, `shared-cache-next273`, and dependencies through `next266-273`.

```sh
php lanes/libsqlite/examples/wordpress-vfs-current-source-next266-273.php --self-test
```

Result: pass. Exact output: `wordpress-vfs-current-source-next266-273 self-test passed`.

```sh
php tools/run-tests.php lanes/libsqlite/tests/SQLiteVfsCurrentSourceNext258265Test.php lanes/libsqlite/tests/SQLiteVfsCurrentSourceNext266273Test.php
```

Result: pass. Exact summary: `2 test files, 40 assertions, 0 failures`.

Integration assertions covered:

- `next258-265` and `next266-273` both claim reusable current-source snapshots.
- Both publish only after matching claims.
- Both preserve the prior reuse ack and advance publish counts.
- Both reject dirty, stale, missing-claim, stale-claim, malformed, and unsupported operation paths.
- `next266-273` records `next258-265` as its direct prior window.

Blockers/failures:

- None found.

Next slice:

- Continue with the next libsqlite VFS current-source window after `next266-273`, preserving the reuse claim/publish continuity and the focused test plus example `--self-test` pattern.
