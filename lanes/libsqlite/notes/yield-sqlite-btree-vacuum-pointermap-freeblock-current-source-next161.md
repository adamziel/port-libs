# B-tree vacuum pointer-map freeblock current-source next161

- Behavior: adds `SQLiteBTreeVacuumPointerMapFreeblockCurrentSourceNext161Plan`, a current-source B-tree plan for deleting an overflow-backed copied `wp_options` transient, partially vacuuming the overflow tail, then allocating a larger replacement overflow chain that reuses the surviving released page and appends formerly truncated page numbers with fresh overflow next-pointers and auto-vacuum pointer-map parents.
- Focused test: `php tools/run-tests.php lanes/libsqlite/tests/SQLiteBTreeVacuumPointerMapFreeblockCurrentSourceNext161Test.php` -> `1 test files / 369 assertions / 0 failures` with 75 PASS lines.
- Application smoke: `php lanes/libsqlite/examples/application-btree-vacuum-pointermap-freeblock-current-source-next161.php --self-test` -> self-test passed.
- Non-overlap: avoids accepted next156 single-survivor reuse, next159 multi-page surviving-chain reuse, next157/next158 variants, next144 pointer-map vacuum rows, overflow freelist release, bulk overflow freeblocks, page relocation, root collapse, index-interior merge, and freelist trunk pointer-map reuse. This slice specifically covers appending back into page numbers that were truncated by partial incremental vacuum, ensuring stale pointer-map ownership is replaced before the larger overflow chain is materialized.
- Dependency closure: no new support component is needed; this composes existing native PHP SQLite page-image, freeblock, freelist allocation/append, overflow chain encoding, incremental vacuum, and auto-vacuum pointer-map primitives.
