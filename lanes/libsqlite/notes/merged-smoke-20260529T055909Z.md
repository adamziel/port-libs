# Merged-head smoke 2026-05-29T05:59:09Z

Objective: bounded smoke check of exact shared head `80e24c408cf582ad42acc74d4c320f4829fee54b` after latest integrations.

Worktree:

- Created with `git worktree add .tmux-team/worktrees/prep-merge-smoke5-20260529T055909Z 80e24c408cf582ad42acc74d4c320f4829fee54b`.
- Verified `git -C .tmux-team/worktrees/prep-merge-smoke5-20260529T055909Z rev-parse HEAD` returned `80e24c408cf582ad42acc74d4c320f4829fee54b`.
- No cherry-picks or alternate detached heads inspected.

Smoke tests:

- `php tools/run-tests.php lanes/libsqlite/tests/SQLiteWalHotJournalSavepointCheckpointCurrentSourceNext356363Test.php lanes/libsqlite/tests/SQLiteJsonTableGeneratedPathRowidCostCurrentSourceNext377384Test.php`
- Result: `2 test files, 97 assertions, 0 failures`.
- Coverage notes: representative latest merged WAL/savepoint/checkpoint current-source slice and JSON table generated-path rowid cost current-source slice at the exact shared head.

Examples:

- `php lanes/libsqlite/examples/application-json-table-generated-path-rowid-cost-current-source-next377-384.php`
  - Exit: 0.
  - Emitted scenario `application-json-table-generated-path-rowid-cost-current-source-next377-384` with dependency `sqlite-json-table-generated-path-rowid-cost-current-source-next384`.
- `php lanes/libsqlite/examples/application-wal-hot-journal-savepoint-checkpoint-current-source-next363.php`
  - Exit: 0.
  - Silent success.

Syntax and hygiene:

- Changed PHP files before note: none.
- `git diff --name-only -- '*.php'`: no output; `php -l` not applicable.
- `find lanes/libsqlite -maxdepth 4 -path '*self-tests*' -print`: no output; no libsqlite `examples/self-tests` path present.
- `git diff --check`: exit 0, no output before note creation.
- Autoload changes: none; `composer dump-autoload` not required.

Blockers:

- None for this bounded smoke slice.

Next slice:

- If more confidence is needed, run a scheduled/full libsqlite lane suite separately from the bounded merge smoke path and capture the final assertion count.
