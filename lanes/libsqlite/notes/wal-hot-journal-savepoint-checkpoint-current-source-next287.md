# WAL hot-journal savepoint checkpoint current-source next287

Seals the next284-286 after-ready WAL hot-journal savepoint checkpoint receipt window after next286. It rejects duplicate checkpoint receipts and stays scoped away from suite, dashboard, SQL, JSON, B-tree, VFS, and planner work.
