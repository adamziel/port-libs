# SQLite upstream veryquick shard current-source next917-932

This slice extends `SQLiteUpstreamSuiteEvidence` with direct next917 through
next932 veryquick-shard admission methods that reuse the existing shared
current-source helper.

The slice continues directly from integrated next901 through next916 evidence.
It preserves the accepted next901-916 anchor as already countable, admits
exactly one newly mapped shard row per next917-932 method, and keeps release
and all-suite parity explicitly unclaimed.

- Scope: focused current-source upstream veryquick shard evidence only.
- Countability: each next917-932 record admits exactly one newly mapped shard
  row when provenance, runner metadata, and focused PHP PASS-line evidence are
  valid.
- Non-overlap: avoids integrated next901-916 suite evidence, exact-shard
  next148, queued runner106/jsonvt104 rebase work, and live behavior lanes.
