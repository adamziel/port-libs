# real-upstream-corpus-date-affinity-dynamic-20260530T215024Z-0

Base accepted HEAD: `ced5e7f57ac3f6a7cf8403098b6a9b4f0ee89285`.

Owned upstream source:

- `/home/claude/port-libs/.upstream-cache/libsqlite/test/date2.test`
- `date2-300` setup: `t3(a INTEGER PRIMARY KEY,b)` populated with `julianday('2017-07-01')+x` for `x=1..1000`, then row `500` changed to text `now`.
- `date2-320` partial index admission on `datetime(b)` where `typeof(b)='real'`.
- `date2-331` selected rowids for `datetime(b) BETWEEN '2017-07-04' AND '2017-07-08'`.

Patch summary:

- Added `SQLiteRealUpstreamDate2RealAffinityFullTableDynamicTest.php`.
- Expands the real-affinity generated `t3` setup into 1,000 distinct row-level TestRunner cases plus source-citation and selected-rowset assertions.
- Non-overlap: existing date-affinity files already cover fractional unixepoch, date4/date5 calendar loops, modifier matrices, deterministic schema guards, and small `date2-331` samples. This slice owns the full `date2-300` 1,000-row real-affinity table generated by the upstream CTE and the `date2-331` predicate rowset over that full corpus.

Dependency closure:

- No new support component is needed. The test reuses `SQLiteCoreScalarFunction` date/time and `typeof` behavior already present in the libsqlite port.

Expected dashboard movement:

- Count as PASS-line growth only: 1,002 new focused TestRunner cases if accepted.
- No mapped denominator movement; the manifest already reports complete mapped coverage.

Focused verification:

- `php -l lanes/libsqlite/tests/SQLiteRealUpstreamDate2RealAffinityFullTableDynamicTest.php`
  - `No syntax errors detected in lanes/libsqlite/tests/SQLiteRealUpstreamDate2RealAffinityFullTableDynamicTest.php`
- `php tools/run-tests.php lanes/libsqlite/tests/SQLiteRealUpstreamDate2RealAffinityFullTableDynamicTest.php`
  - `1 test files, 3005 assertions, 0 failures`
- `php tools/run-tests.php lanes/libsqlite/tests/SQLiteRealUpstreamDate2RealAffinityFullTableDynamicTest.php lanes/libsqlite/tests/SQLiteRealUpstreamDate2DeterministicDynamicCorpusTest.php lanes/libsqlite/tests/SQLiteRealUpstreamDateAffinityDynamicDate2ModifierRowsTest.php lanes/libsqlite/tests/SQLiteRealUpstreamDate2DeterministicSchemaGuardCorpusTest.php`
  - `4 test files, 12269 assertions, 0 failures`
- `php tools/run-tests.php lanes/libsqlite/tests/SQLiteNoDomainSpecificApiTest.php`
  - `1 test files, 3 assertions, 0 failures`
- `git diff --check -- lanes/libsqlite`
  - passed with no output
