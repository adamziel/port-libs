# real-upstream-corpus-btree-index-dynamic-20260530T224848Z-0

Base accepted HEAD: `dc9a740fd34e07dba61e9143b3604d183ad170bf`.

Added focused PHP TestRunner coverage in
`SQLiteRealUpstreamBTreeIndexDynamicCorpusTest.php` for existing real upstream
corpus rows generated by `SQLiteBTreeIndexDynamicCorpusPlan`:

- `indexexpr1.test` sections `indexexpr1-110` through `indexexpr1-260`:
  expression-index lookup, commuted expression predicates, expression ORDER BY,
  and WITHOUT ROWID replay.
- `indexexpr2.test` sections `indexexpr2-3.4.5`, `indexexpr2-3.4.6`,
  `indexexpr2-4.120`, and `indexexpr2-4.130`: expression-index collation and
  UPDATE recomputation/refcount behavior.
- `indexexpr1.test` sections `indexexpr1-300` through `indexexpr1-410`: DDL
  expression-index guard errors and UNIQUE expression-index duplicate rollback.

Focused verification:

- `php tools/run-tests.php lanes/libsqlite/tests/SQLiteRealUpstreamBTreeIndexDynamicCorpusTest.php`
  passed: `1 test files, 284017 assertions, 0 failures`.

Expected movement: focused PASS-line growth only in the B-tree/index dynamic
corpus file. Mapped denominator coverage remains complete at `1589 / 1589`.

Dependency closure: no new support component is needed; this reuses the
existing native PHP corpus plan and TestRunner.
