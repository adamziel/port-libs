# Merged-head smoke 2026-05-29T05:56:46Z

Objective: bounded smoke check of exact shared head `7a8a99fe8cdc8f4fabcd9abe0ae699d89132ee51` after latest integrations.

Worktree:

- Created with `git worktree add .tmux-team/worktrees/prep-merge-smoke4-20260529T055646Z 7a8a99fe8cdc8f4fabcd9abe0ae699d89132ee51`.
- Verified `git rev-parse HEAD` returned `7a8a99fe8cdc8f4fabcd9abe0ae699d89132ee51`.
- No cherry-picks or alternate detached heads inspected.

Smoke tests:

- `php tools/run-tests.php lanes/libsqlite/tests/SQLiteWalHotJournalSavepointCheckpointCurrentSourceNext356363Test.php lanes/libsqlite/tests/SQLiteJsonTableGeneratedPathRowidCostCurrentSourceNext377384Test.php`
- Result: `2 test files, 97 assertions, 0 failures`.
- Coverage notes: representative latest merged WAL/savepoint/checkpoint current-source slice and JSON table generated-path rowid cost current-source slice at the exact shared head.

Examples:

- `php lanes/libsqlite/examples/wordpress-json-table-generated-path-rowid-cost-current-source-next377-384.php`
  - Exit: 0.
  - Emitted scenario `wordpress-json-table-generated-path-rowid-cost-current-source-next377-384` with dependency `sqlite-json-table-generated-path-rowid-cost-current-source-next384`.
- `php lanes/libsqlite/examples/wordpress-wal-hot-journal-savepoint-checkpoint-current-source-next363.php`
  - Exit: 0.
  - Silent success.

Syntax and hygiene:

- Changed PHP files before note: none.
- `git diff --name-only -- '*.php'`: no output; `php -l` not applicable.
- `find lanes/libsqlite -path '*self-tests*' -maxdepth 4 -print`: no output; no libsqlite `examples/self-tests` path present.
- `git diff --check`: exit 0, no output before note creation.
- Autoload changes: none; `composer dump-autoload` not required.

Blockers:

- None for this bounded smoke slice.

Next slice:

- If more confidence is needed, run a scheduled/full libsqlite lane suite separately from the bounded merge smoke path and capture the final assertion count.
