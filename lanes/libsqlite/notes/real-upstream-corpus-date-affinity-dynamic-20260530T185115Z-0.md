# real-upstream-corpus-date-affinity-dynamic-20260530T185115Z-0

Base accepted HEAD: `0eff666a68d9fc5c2de0693a82870643615fd7c5`

## Upstream Source

- `/home/claude/port-libs/.upstream-cache/libsqlite/test/date3.test`
- Ported scenario:
  - `date3-1.7`: randomized `unixepoch(time-value,'unixepoch') == time-value`
    identity behavior.

## Behavior

Added a non-overlapping 1,000-value deterministic bulk shard for the upstream
`date3-1.7` randomized unixepoch identity loop. Existing coverage in
`SQLiteRealUpstreamDateAutoUnixepochModifierCorpusTest.php` already includes
100 deterministic values for this upstream loop; this slice adds a fresh
1,000-value range generated by a fixed LCG and bounded to SQLite's supported
unix timestamp range.

No implementation change was needed. The current `SQLiteCoreScalarFunction`
date/time implementation already preserves integer unixepoch identity with the
immediate `unixepoch` modifier.

## Verification

- `php -l lanes/libsqlite/tests/SQLiteRealUpstreamDate3UnixepochRoundtripBulkTest.php`
  - `No syntax errors detected in lanes/libsqlite/tests/SQLiteRealUpstreamDate3UnixepochRoundtripBulkTest.php`
- `php tools/run-tests.php lanes/libsqlite/tests/SQLiteRealUpstreamDate3UnixepochRoundtripBulkTest.php`
  - `1 test files, 2005 assertions, 0 failures`
  - `1002` focused PASS lines
- `SQLiteNoWordPressSpecificApiTest.php` was absent in this worktree.

## Non-overlap

This avoids the accepted `date2.test` deterministic schema guard and modifier
coverage, existing `date.test`/`date4.test`/`date5.test` date-affinity dynamic
coverage, and the previous 100-value `date3-1.7` shard. It only extends the
real upstream `date3-1.7` randomized unixepoch identity loop with new values.

## Dependency Closure

No new support component is needed. The slice reuses existing
`SQLiteCoreScalarFunction` unixepoch/date-time behavior.
