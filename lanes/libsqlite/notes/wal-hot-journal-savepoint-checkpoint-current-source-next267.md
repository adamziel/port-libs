# WAL hot-journal savepoint checkpoint current-source next267

Seals the after-current WAL hot-journal savepoint checkpoint receipt chain after next266. It rejects duplicate checkpoint receipts and keeps the scope separate from suite, status, dashboard, SQL, JSON, B-tree, VFS, and planner work.
