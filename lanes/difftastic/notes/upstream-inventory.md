# Difftastic Upstream Inventory

Upstream: `https://github.com/Wilfred/difftastic.git`

Commit: `7ccfcb315f7e46fd015809416c7d7dffa5be7078`

Cache: `.upstream-cache/difftastic`

The cache is a sparse blob-filtered clone (`remote.origin.partialclonefilter=blob:none`, `core.sparsecheckout=true`). Counts below use `git ls-tree` and targeted `git show` reads to avoid broad checkout hydration.

## Counted Denominator

- 786 tracked files in the upstream tree.
- 68 Rust files total, including 32 under `src/` and 1 integration test file under `tests/`.
- 144 Rust `#[test]` or `#[test_case]` attributes in `src/` and `tests/`, across 20 files.
- 23 CLI integration `#[test]` functions in `tests/cli.rs`.
- 30 display-layer test attributes, 32 diff-layer test attributes, and 24 parse-layer test attributes in targeted source families.
- 248 `sample_files/` artifacts.
- 114 numbered sample fixture pairs, 4 directory fixture pairs, and 111 `sample_files/compare.expected` golden output pairs generated by `sample_files/compare_all.sh`.
- 382 vendored parser files, including 35 parser corpus files, 5 parser example files, 1 parser bench file, and 42 binding files.
- The upstream JSON display highlight boundary is inspected through `src/display/json.rs`'s 7 serialized `Highlight` enum variants: `delimiter`, `normal`, `string`, `type`, `comment`, `keyword`, and `tree_sitter_error`.
- The upstream parse-error fallback boundary is inspected through `DEFAULT_PARSE_ERROR_LIMIT`, `to_syntax_with_limit`, `line_parser::change_positions`, and the `tests/cli.rs` `yaml_parse_errors` test with its two `sample_files/cli_tests/bad_yaml_*.yml` fixtures.
- The upstream byte-limit fallback boundary is inspected through `DEFAULT_BYTE_LIMIT`, `to_tree_with_limit`, `ExceededByteLimit`, and `main.rs` `TextFallback` handling before line-parser fallback positions.
- The upstream graph-limit fallback boundary is inspected through `DEFAULT_GRAPH_LIMIT`, `--graph-limit`/`DFT_GRAPH_LIMIT`, `dijkstra::mark_syntax`, `ExceededGraphLimit`, and `main.rs` `TextFallback` handling before line-parser fallback positions.
- The upstream UTF-16 text/binary boundary is inspected through `src/files.rs` `has_utf16_byte_order_mark`, `u16_from_bytes`, and `guess_content`, plus `sample_files/utf16_1.py` / `sample_files/utf16_2.py`.
- The upstream mostly-valid UTF-8 text/binary boundary is inspected through `src/files.rs` `guess_content` and the `tests/cli.rs` `slightly_invalid_utf8` CLI test, which keeps content with at most two invalid UTF-8/null characters as text before considering Windows-1252 fallback.
- The upstream strip-CR boundary is inspected through `src/options.rs` `--strip-cr` / `DFT_STRIP_CR`, `src/main.rs` `strip_cr` preprocessing for two-file and conflict-file diffs, and `src/lines.rs` CRLF splitting behavior.
- The upstream trailing EOF line boundary is inspected through `src/lines.rs` `split_on_newlines` tests, which keep a trailing empty line for text ending in `\n`, plus `sample_files/repeated_line_no_eol_*.txt`.
- The upstream Makefile text-atom boundary is inspected through `tests/cli.rs` `makefile_text_as_atom`, the `src/parse/tree_sitter_parser.rs` `Make` configuration, and `sample_files/cli_tests/makefile_1.mk` / `sample_files/cli_tests/makefile_2.mk`.
- The upstream changes-at-end text boundary is inspected through `tests/cli.rs` `text_changes_at_end_doesnt_crash` and `sample_files/cli_tests/changes_at_end_1.txt` / `sample_files/cli_tests/changes_at_end_2.txt`.
- The upstream JSON display hunk grouping boundary is inspected through `src/display/hunks.rs` `MAX_DISTANCE` and `sample_files/text_1.txt` / `sample_files/text_2.txt`, where nearby text changes separated by short retained context are displayed as one hunk.
- The Python trailing-comma boundary is inspected through `src/parse/tree_sitter_parser.rs` `Python` `ignore_trailing_tokens`: dictionaries, lists, sets, argument lists, and parameters ignore trailing commas, but tuple is deliberately excluded.
- The Python block-header boundary is inspected through the upstream `Python` tree-sitter configuration and a targeted directory fixture pair, `sample_files/dir_1/has_many_hunk.py` / `sample_files/dir_2/has_many_hunk.py`, where `function041` changes only its signature while neighboring functions stay stable.
- The Python compound-clause boundary is inspected through the upstream `Python` tree-sitter configuration and mapped locally for `elif`, `else`, `try`, `except`, and `finally` clauses, with continuation clauses attached to the preceding compound statement path.

This gives the lane 415 inspected behavior artifacts for the current cloned static denominator: 144 Rust test/test-case attributes, 111 golden output pairs, 114 numbered sample fixture pairs, 4 directory fixture pairs, 35 parser corpus files, 3 targeted strip-CR source files, 2 targeted Makefile CLI fixture files, and 2 targeted changes-at-end CLI fixture files.

## Runner Status

Rust tooling was installed for this lane on 2026-05-22 with:

```sh
sudo -n dnf install -y cargo rust
```

Installed versions:

```text
cargo 1.95.0 (f2d3ce0bd 2026-03-21) (Fedora 1.95.0-5.fc44)
rustc 1.95.0 (59807616e 2026-04-14) (Fedora 1.95.0-5.fc44)
```

The full upstream runner was not executed in this slice. The cache is still sparse/blob-filtered, and a full `cargo test` would require hydrating the checkout, downloading the Cargo dependency graph, and compiling difftastic plus many tree-sitter parser/native parser crates. A bounded offline probe was run after installing Rust:

```sh
cargo test --no-run --locked --offline
```

It failed before compilation because the local Cargo cache cannot resolve `clap` from crates.io in offline mode. Running the same probe online or running the full upstream suite would exceed this worker's modest network/CPU budget.

## Newly Mapped Fixture Pairs

- `sample_files/added_line_1.txt` / `sample_files/added_line_2.txt`: copied locally and mapped through explicit plain-text line-parser output. Native `text`/`plain` modes now report inserted lines under `$text.line[...]` without emitting a fallback marker or returning an empty syntax-list diff just because no structural delimiters are present.
- `sample_files/insert_blank_1.txt` / `sample_files/insert_blank_2.txt`: copied locally and mapped through plain-text JSON display output. Native text display now keeps blank-line-only changes as `changed` files with zero-based aligned lines and a deletion-side blank line chunk instead of incorrectly returning `unchanged` because the token stream is equal.
- `sample_files/align_footer_1.txt` / `sample_files/align_footer_2.txt`: copied locally from the upstream `compare.expected` golden set and mapped through text JSON display alignment. Native text display keeps the retained `after` footer aligned after a changed line plus a deleted line, and the unchanged opposite side of ` foo x` to ` x` now emits an empty JSON `changes` array instead of fabricating novel RHS content.
- The WordPress readme footer fixture applies the same display slice to `wp-content/plugins/acme-review-tools/readme.txt`, keeping a stable FAQ footer heading aligned as context while reporting the description wording change and a deleted beta-only note.
- `tests/cli.rs` `text_changes_at_end_doesnt_crash` and `sample_files/cli_tests/changes_at_end_1.txt` / `sample_files/cli_tests/changes_at_end_2.txt`: copied locally and mapped through text syntax-list and JSON display output. Native text display reports the large final changed block and inserted final RHS lines while keeping the terminal EOF context line aligned outside the changed chunk.
- The WordPress readme end-changes fixture applies the same terminal text display slice to `wp-content/plugins/acme-end-review/readme.txt`, reporting stable-tag and final changelog edits plus an inserted audit note without rendering the retained EOF line as a changed chunk.
- `sample_files/text_1.txt` / `sample_files/text_2.txt`: copied locally and mapped through the upstream display hunk grouping boundary from `src/display/hunks.rs`. Native JSON display now keeps nearby text changes in one chunk when the changed groups are separated by short retained context, while the retained context lines remain aligned and do not appear as novel JSON content.
- The WordPress readme nearby-hunks fixture applies the same grouping slice to `wp-content/plugins/acme-review-tools/readme.txt`, reporting an inserted `Requires PHP` line and a nearby `legacy` to `modern` description update in one review chunk without marking the stable tag line as changed.
- `sample_files/big_text_hunk_1.txt` / `sample_files/big_text_hunk_2.txt`: copied locally and mapped through dense inserted text display. Native text mode reports 42 inserted dependency lines under `$text.line[7]` through `$text.line[48]`, and compact JSON keeps them as one RHS-only changed chunk without inventing LHS entries.
- `sample_files/many_newlines_1.txt` / `sample_files/many_newlines_2.txt`: inspected as the upstream empty-LHS created-file text shape. Compact JSON display returns lowercase `created` status with no chunks, while native syntax-list text output now emits pure `$text.line[...]` insertions instead of pairing the first inserted line with a synthetic empty old line.
- The WordPress created import-report fixture applies that empty-LHS slice to `wp-content/uploads/migration/import-report.csv`, returning a created file envelope for JSON display and pure inserted CSV rows for syntax-list review surfaces.
- `src/lines.rs` `split_on_newlines` and `sample_files/repeated_line_no_eol_1.txt` / `sample_files/repeated_line_no_eol_2.txt`: copied locally as hex fixtures for exact no-final-newline bytes and mapped through plain-text line-parser plus compact JSON display output. Native `text`/`plain` modes now preserve trailing EOF empty lines and report appended repeated final lines under `$text.line[...]` paths instead of collapsing them with the retained preceding line.
- The WordPress import-log no-EOL fixture applies the same text display slice to `wp-content/uploads/migration/import.log`, preserving appended final migration records as JSON chunks even when neither side has a final newline.
- `tests/cli.rs` `makefile_text_as_atom` and `sample_files/cli_tests/makefile_1.mk` / `sample_files/cli_tests/makefile_2.mk`: copied locally and mapped through native Makefile text atoms. Makefile syntax-list output now reports changed assignment lines under `$make.text[...]`, including the upstream `CCFLAGS` line, instead of returning an empty delimiter-list diff.
- The WordPress plugin build Makefile fixture applies the same slice to native block build review. It reports `CCFLAGS` hardening changes and inserted `build/view.js` assets under Makefile-specific atom paths so shared-hosting review UIs can inspect build metadata without shelling out to difftastic.
- `sample_files/trailing_commas_1.js` / `sample_files/trailing_commas_2.js`: copied locally and mapped as a no-change formatting/trailing-comma diff under the token differ's default JavaScript-like trailing-comma normalization.
- `sample_files/javascript_simple_1.js` / `sample_files/javascript_simple_2.js`: copied locally and mapped through standalone JavaScript statement syntax. The inserted `if (true)` body wrapper is reported separately from retained `foo`/`bar`/`baz` calls, `bar(1)` to `bar(2)` remains a focused call-argument update, and the `people` array reports only the inserted `"yvonne"` item while retaining following entries.
- `sample_files/javascript_1.js` / `sample_files/javascript_2.js`: copied locally and mapped through named JavaScript callback-context matching. Repeated Jest-style `describe(...)` and `test(...)` calls now include their first string labels plus enclosing named callback labels in match signatures, so stable tests under `describe("Editing", ...)` are not paired with newly inserted tests under `describe("Authentication", ...)` by callee name alone.
- `sample_files/load_1.js` / `sample_files/load_2.js`: a targeted local excerpt maps JavaScript function declarations as call scopes. Named `function createResource(...) { ... }` declarations no longer appear as fake `functioncreateResource()` calls, and calls inside removed helpers such as `createNodeModuleResource` do not pair with retained helper calls by callee name alone.
- The WordPress block registration function fixture applies the same function-scoped call matching to repeated `wp.blocks.registerBlockType(...)` calls, so a newly inserted `registerQueryBlock()` function is reported separately while the retained gallery registration function stays aligned.
- `sample_files/typescript_1.ts` / `sample_files/typescript_2.ts`: copied locally and mapped through TypeScript type-literal member alignment. The inserted `name: string` member is reported under `$ts.type["Symbol"][1]` while the retained following `items: string[]` member stays matched.
- Targeted TypeScript parser config in `src/parse/tree_sitter_parser.rs`: inspected the upstream `TypeScript | TypeScriptTsx` branch, which uses tree-sitter TypeScript, treats `{}`, `()`, `[]`, and `< >` as delimiter lists, and ignores trailing tokens in object/array/argument/formal-parameter lists. The local `typescript-module-declarations-*.ts` fixture maps that tree-shaped module-declaration behavior by aligning named import/export specifier lists under `$ts.import[...]`, `$ts.import.type[...]`, `$ts.export.local`, and `$ts.export.type[...]` paths instead of replacing whole import/export statements when specifiers are inserted.
- The same targeted TypeScript parser config now maps default import, namespace import, and re-export source-change shapes in `typescript-module-import-shapes-*.ts`. Retained default imports stay aligned when a named specifier is added, namespace alias changes report under `$ts.import.namespace[...]`, and retained named re-exports report `from` source changes under `$ts.export.source[...]` or `$ts.export.type.source[...]` instead of delete/add whole declarations.
- The same targeted TypeScript parser config now maps export-star, namespace re-export, and import assertion/attribute shapes in `typescript-module-attributes-*.ts`. Unchanged `export *` declarations stay out of the change stream, `export type * from` source moves report under `$ts.export.type.source["*"]`, `export * as ...` alias changes report under `$ts.export.namespace[...]`, and `assert`/`with` attribute lists report under `$ts.import.attributes[...]` without being mistaken for named import specifier lists.
- The TypeScript parser boundary now also maps a bounded dynamic `import()` metadata fixture in `wordpress-block-dynamic-metadata-*.ts`. Dynamic `assert`/`with` option objects report under `$ts.import.dynamic.attributes[...]`, retained dynamic imports do not only appear as generic `$js.call["import"]` argument churn, and the same fixture drives compact JSON display chunks for WordPress TypeScript asset review.
- `sample_files/jsx_1.jsx` / `sample_files/jsx_2.jsx`: copied locally and mapped through JSX/TSX angle-delimiter tag-list alignment. The `h1` class change, inserted `span`, and `div` to `p` wrapper change are reported as structural tag changes instead of one broad `ReactDOM.render(...)` argument replacement.
- `sample_files/whitespace_1.tsx` / `sample_files/whitespace_2.tsx`: copied locally and mapped through JSX/TSX whitespace-only string expression normalization. Moved `{" "}` spacer expressions and formatter-driven tag indentation no longer produce syntax-list churn when retained tags and text are unchanged.
- `sample_files/html_simple_1.html` / `sample_files/html_simple_2.html`: copied locally and mapped through HTML-mode angle delimiters so tag attribute changes and inserted tags are reported as syntax-list changes.
- `sample_files/xml_1.xml` / `sample_files/xml_2.xml`: copied locally and mapped through XML-mode angle delimiters so a newly inserted self-closing tag is reported without replacing the stable surrounding tags.
- `sample_files/if_1.py` / `sample_files/if_2.py`: copied locally and mapped through a narrow Python indentation-block slice. The stable `if x:` header and retained `foo` body item remain matched while `bar` moving out of the indented body is reported as a deletion under `$py.if["x"][1]` and an insertion under `$py.root[0]`.
- Targeted Python parser config in `src/parse/tree_sitter_parser.rs`: inspected the upstream `Python` branch's `ignore_trailing_tokens` list and mapped its tuple exception. Native Python token diffs now treat list, dict, set, argument-list, and parameter trailing commas as formatting-only while preserving tuple comma changes such as `(value,)` versus `(value)`.
- `sample_files/dir_1/has_many_hunk.py` / `sample_files/dir_2/has_many_hunk.py`: a targeted local excerpt around `function040` through `function042` maps Python `def` block header alignment. The `def function041()` to `def function041(**args)` signature change reports as `$py.def["function041"]/header` without deleting stable neighboring function blocks.
- Targeted Python block-header handling now also recognizes `for`, `while`, and `with` indentation blocks. The WordPress loop migration fixture applies this to a top-level `for post in posts` migration loop where `hydrate_featured_media(post)` moves out of the loop body and appears separately at `$py.for[...]` and `$py.root[...]` paths.
- `sample_files/dir_1/has_many_hunk.py` / `sample_files/dir_2/has_many_hunk.py`: a second targeted excerpt around `function080` through `function082` maps nested Python block extraction inside a retained `def`. The upstream after-side adds `if True:` inside `function081`; the PHP port now reports that as `$py.def["function081"].if["True"]` without deleting the retained function header, final direct `pass`, or neighboring functions.
- Recursive Python indentation-suite parsing now also drives a WordPress nested migration fixture where an inserted featured-media guard inside `migrate_posts()` / `for post in posts` is reported under `$py.def["migrate_posts"].for["post in posts"].if[...]`, while the retained function, loop, `normalize_blocks(post)`, and `save_post(post)` stay aligned.
- Targeted Python compound-clause handling now recognizes `elif`, `else`, `try`, `except`, and `finally` headers from the same upstream tree-sitter Python boundary. The local `python-compound-clauses-*.py` fixture reports an inserted `elif` under `$py.if[...].elif[...]` and an inserted `finally` under `$py.try[...].finally[...]` while retaining the original `if`, `else`, `try`, and `except` blocks.
- The WordPress compound migration fixture applies that clause slice to a migration helper: an inserted raw-HTML sanitization branch reports under `$py.def["migrate_post"].if[...].elif[...]`, and cleanup work reports under `$py.def["migrate_post"].try[...].finally[...]` without deleting the retained exception handler.
- `sample_files/json_1.json` / `sample_files/json_2.json`: copied locally and mapped through JSON object-key item alignment. The retained `"foo"` property is diffed inside its array value (`1` deleted and `5` inserted), while renamed/new keys (`"bar"`, `"zab"`, `"woo"`) remain property-level changes.
- `sample_files/slider_at_end_1.json` / `sample_files/slider_at_end_2.json`: copied locally and mapped through JSON list-item alignment so repeated stable items remain matched while intervening `"novel-*"` entries are reported as focused deletions.
- `sample_files/slider_1.rs` / `sample_files/slider_2.rs`: inspected as the larger upstream slider pair, with a targeted local excerpt around the inserted `container_sequence_header` method, extracted `context` setup, and added `codec_data` field. Rust mode now splits block items on method boundaries and semicolon-terminated statements so retained methods/statements stay matched while the inserted setup is reported as focused additions.
- `sample_files/nested_slider_1.rs` / `sample_files/nested_slider_2.rs`: copied locally and mapped through nested slider wrapper correction from `src/diff/sliders.rs`. The retained inner `x` expression is kept stable while inserted wrappers are reported as `ifpad_last{...}` and `bar(...)`.
- `sample_files/nested_slider_1.el` / `sample_files/nested_slider_2.el`: copied locally and mapped through the Lisp-family opposite nested-slider preference from `src/diff/sliders.rs`. The deleted outer `-when-let(...)` wrapper is reported separately while the retained `setq` form is diffed inside it.
- `sample_files/change_outer_1.el` / `sample_files/change_outer_2.el`: copied locally and mapped through changed-outer-delimiter handling. When the flattened child atoms are retained, the PHP differ now reports the delimiter pair change separately from newly introduced inner wrappers instead of replacing the whole list body.
- `sample_files/strings_1.el` / `sample_files/strings_2.el`: targeted upstream excerpts around `hack--keyword-regex` are copied locally and mapped through Emacs Lisp reader-quote and semicolon-comment tokenization plus flat literal-list item splitting. The changed `regexp-opt` keyword list now reports focused quoted-string/comment additions and deletions instead of replacing the whole form.
- `sample_files/hack_1.php` / `sample_files/hack_2.php`: copied locally and mapped through PHP/Hack function return-type extraction plus existing syntax-list alignment. The changed `foo(): vec<int>` signature is reported as a focused `$php.function.foo.return_type` update while the inserted `null` in `return vec[1, null]` remains a nested list insertion.
- `sample_files/string_subwords_1.el` / `sample_files/string_subwords_2.el`: copied locally and mapped through changed string atom word splitting from `src/parse/syntax.rs`. The JSON renderer now reports word spans inside paired changed string atoms when enough words are shared.
- `sample_files/comments_1.rs` / `sample_files/comments_2.rs`: copied locally and mapped through changed comment atom word splitting from `src/parse/syntax.rs`. Inserted comment words and changed quoted subwords inside comments are emitted as comment-highlighted word spans.
- `sample_files/multiline_string_1.ml` / `sample_files/multiline_string_2.ml`: copied locally and mapped through multiline atom byte spans. The JSON renderer now keeps changed words inside a multiline string highlighted as `string` instead of falling back to per-line identifier highlighting.
- `sample_files/multiline_string_eof_1.yml` / `sample_files/multiline_string_eof_2.yml`: copied locally and mapped through YAML block-scalar string atom spans. The JSON renderer now keeps removed words such as `set -x` string-highlighted even when the opposite block scalar becomes a single content line.
- `sample_files/trailling_newline_1.yaml` / `sample_files/trailling_newline_2.yaml`: copied locally and mapped through one-line YAML block-scalar fallback spans. When the old and new scalar bodies do not share enough words for subword pairing, the JSON renderer still keeps `${{ BAR }}` and `bar` highlighted as string content instead of splitting expression braces into delimiters.
- `sample_files/yaml_1.yaml` / `sample_files/yaml_2.yaml`: copied locally and mapped through a narrow YAML structural slice. Flow lists still use delimiter-list alignment, while line-based YAML block sequences report the inserted `'item'` under `$yaml.hello[1]` and the deleted `stuff: |` body is reported as a block-scalar deletion without deleting the retained `"world"` or `other` sequence items.
- Targeted upstream parse-error fallback semantics now map the default `DFT_PARSE_ERROR_LIMIT=0` behavior: when native delimiter parse errors exceed the limit, syntax-list output switches to line-oriented `$text.line[...]` changes and emits a `Text (... exceeded DFT_PARSE_ERROR_LIMIT)` fallback marker. The WordPress `wordpress-block-editor-syntax-error-*.js` fixture applies this to a partial `registerPlugin(...)` edit with an unclosed object literal, so the PHP port avoids misleading `$js.call[...]` matching and labels compact JSON display as text fallback.
- Targeted upstream byte-limit fallback semantics now map the default `DFT_BYTE_LIMIT=1000000` boundary with an overrideable `byteLimit` option. When either supported-language input exceeds the limit, the native differ emits line-oriented `$text.line[...]` changes and a `Text (... exceeded DFT_BYTE_LIMIT)` fallback marker before structural matching. The WordPress render metadata example exercises this with a bounded 127-byte PHP fixture and an 80-byte limit.
- Targeted upstream graph-limit fallback semantics now map `DEFAULT_GRAPH_LIMIT=3000000`, `--graph-limit`/`DFT_GRAPH_LIMIT`, `ExceededGraphLimit`, and `main.rs` `TextFallback` behavior with an overrideable `graphLimit` option. The native PHP port uses a conservative syntax-node graph bound rather than constructing the Rust arena graph; when that bound exceeds the configured limit, it emits line-oriented `$text.line[...]` changes and a `Text (exceeded DFT_GRAPH_LIMIT)` marker before structural matching. The WordPress block variation example exercises this with a low graph limit so large `registerBlockVariation` diffs remain reviewable without partial `$js.array[...]` matches.
- Targeted upstream strip-CR semantics now map the default `--strip-cr=on` path from `src/options.rs` and `src/main.rs`: carriage returns are removed before parsing/diffing, including multiline comment/string atoms, and callers can pass `stripCr => false` to preserve CR-only line-ending changes. The WordPress CRLF render example compares a Windows-edited plugin render file against LF output and returns an unchanged JSON file envelope by default.
- `sample_files/utf16_1.py` / `sample_files/utf16_2.py`: copied as hex-encoded upstream fixture bytes and mapped through targeted `src/files.rs` content guessing. The PHP port decodes UTF-16LE/BE only when an explicit byte order mark is present, preserves the decoded text for normal JSON display chunks, and reports the upstream snowman/Japanese Python sample as `Python` text instead of `Binary`.
- `sample_files/multibyte_1.py` / `sample_files/multibyte_2.py`: copied locally and mapped through upstream `src/display/json.rs` byte-column display semantics. The JSON renderer reports changed `foo`/`bar` words and the retained `€` suffix with byte offsets, so UTF-8 display content is not split by character counts.
- The WordPress i18n block-copy fixture applies that display slice to translated block metadata. `legacy` to `modern` changes after a Japanese prefix report at byte offset 28 while the surrounding UTF-8 copy stays valid and string-highlighted.
- `sample_files/windows1251_1.txt` / `sample_files/windows1251_2.txt`: copied as hex-encoded upstream fixture bytes and mapped through the targeted `src/files.rs` Windows-1252 fallback branch. The PHP port now decodes legacy Latin-1/Windows-1252 bytes such as `Muß`, `können`, and `ähnlich` into UTF-8 text before JSON display instead of reporting a binary envelope.
- Targeted `src/files.rs` mostly-valid UTF-8 decoding and `tests/cli.rs` `slightly_invalid_utf8` are mapped with a bounded local hex fixture containing one invalid byte. The PHP port now returns text with `U+FFFD` replacement before trying Windows-1252, matching upstream's `String::from_utf8_lossy` boundary for files that are almost valid UTF-8.
- The WordPress slightly-invalid WXR fixture applies that decoding to export XML bytes with one corrupt byte, keeping `Legacy` to `Modern` title changes and inserted `_wp_page_template` metadata reviewable as XML chunks rather than binary output or Windows-1252 punctuation.
- `sample_files/css_1.css` / `sample_files/css_2.css`: copied locally and mapped through CSS selector-block alignment plus declaration property signatures. Reordered stable selectors such as `.bar` remain matched, while `.foo1` gets a focused `color: green` declaration addition, `.baz` property values update in place, `.another` keeps the `margin-left` property aligned, and the new `p` rule is reported as a rule insertion.
- `sample_files/tailwind_1.css` / `sample_files/tailwind_2.css`: copied locally and mapped through CSS at-rule item signatures. The changed `@apply rounded-md ...` utility list is retained as a focused update under `select` instead of delete/add churn around the upstream tree-sitter ERROR-shaped atom.
- `sample_files/simple_1.scss` / `sample_files/simple_2.scss`: copied locally and mapped through SCSS mixin selector/header matching plus nested declaration alignment. The changed mixin default argument, nested `border`, `font-size`, `.primary` border, and `.disabled` opacity edits are reported without replacing the whole `@mixin buttons(...)` body.
- `sample_files/html_1.html` / `sample_files/html_2.html`: targeted CSS excerpts from the upstream `<style>` blocks are copied locally as `upstream-html-style-media-*.css` and mapped through nested CSS at-rule matching. The stable `@media (max-width: 700px)` child rule stays matched while surrounding style rules change.
- `sample_files/html_1.html` / `sample_files/html_2.html`: the full upstream HTML pair is now copied locally and mapped through the upstream `src/parse/tree_sitter_parser.rs` `style_element` sub-language rule. HTML mode extracts `<style>` raw text, parses it as CSS, and reports selector/declaration changes under `$html.style.css[...]` paths while keeping the stable nested `@media` rule matched.
- `sample_files/html_1.html` / `sample_files/html_2.html`: the same full upstream HTML pair now maps multiple `style_element` raw-text captures separately. When the new upstream HTML inserts a first `<style>` block before the retained changed block, the native diff reports the inserted `p` rule under `$html.style[0].css[...]` and the retained `body`/`#main` changes under `$html.style[1].css[...]` instead of concatenating every style body into one aggregate CSS stream.
- `sample_files/html_1.html` / `sample_files/html_2.html`: the same full upstream HTML pair now maps the adjacent upstream `script_element` sub-language rule. HTML mode extracts `<script>` raw text, parses focused JavaScript call arguments, and reports the `alert('welcome!')` to `alert("goodbye!")` change under `$html.script.js.call["alert"][0]`.
- `sample_files/html_1.html` / `sample_files/html_2.html`: HTML root-list comparison now strips raw `<style>` and `<script>` bodies before generic tag/list diffing. CSS and JavaScript body changes are still reported through `$html.style.css[...]` and `$html.script.js.call[...]`, but no longer appear a second time as root `$[...]` list churn.
- Nested CSS `@media` / `@supports` containers now recurse into direct child rules. This is a native behavior slice against the same tree-shaped at-rule semantics exercised by the upstream HTML style sample, while keeping SCSS mixin bodies on the existing declaration-aware path so direct declarations are still reported.

## JSON Display Slice

Targeted upstream source: `src/display/json.rs`.

Mapped native behavior:

- File JSON uses lowercase `unchanged`, `changed`, `created`, and `deleted` statuses.
- `language`, `path`, and `status` are always serialized; `aligned_lines` and `chunks` are emitted only for changed text files.
- Line numbers and aligned line pairs are zero-based, matching the upstream display source's `LineNumber.0` serialization.
- Changed chunks include `lhs`/`rhs` sides with `line_number` and novel token `changes`.
- Plain-text JSON display treats line-level text inequality as a display change even when token streams are equal, mapping the upstream `insert_blank_*.txt` golden shard.
- JSON display merges nearby text change groups into one chunk when fewer than difftastic's `MAX_DISTANCE` retained lines separate them, mapping the upstream `text_*.txt` display hunk shape while keeping retained context out of the `changes` arrays.
- Plain-text line splitting keeps trailing empty EOF lines like upstream `split_on_newlines`, and JSON display preserves no-final-newline append chunks from `repeated_line_no_eol_*.txt`.
- Token changes carry `start`, `end`, `content`, and `highlight` fields; native highlights now map all seven upstream enum variants: delimiter, normal, string, comment, keyword, type, and tree_sitter_error. The tree-sitter-error slice is mapped through native delimiter error spans when the parse-error limit allows structural display instead of the default text fallback.
- JSON display spans are constructed from byte ranges, matching upstream's `start_col`/`end_col` serialization and Rust string slicing. The upstream `multibyte_*.py` sample now asserts `"foo€"`/`"bar€"` offsets as `foo`/`bar` at `1..4` and `€` at `4..7`.
- Directory JSON output is represented as an array of file objects and can skip unchanged files, matching upstream `print_directory`.
- TypeScript dynamic metadata imports are now covered by JSON display review chunks for `assets.ts`, so block metadata `assert` to `with`, `javascript` to `module`, and inserted `supports.json` import spans remain machine-readable.
- TypeScript/PHP/Python/Rust keyword and primitive-type atoms now keep upstream-style `keyword` and `type` highlight labels in compact JSON display. The WordPress TypeScript metadata example uses this for inserted `type`, `const`, `string`, `number`, and `boolean` spans in block asset review data.
- Parser-error delimiters now keep upstream-style `tree_sitter_error` highlight labels in compact JSON display when `parseErrorLimit` is high enough to continue structural display. The WordPress block editor parser-error example exposes an extra `}` in block registration JavaScript as a machine-readable error span instead of reducing it to a normal punctuation change.
- Graph-limit fallback is labeled as `Text (exceeded DFT_GRAPH_LIMIT)` in JSON display output, matching upstream `TextFallback` language labeling for safety fallbacks.
- Strip-CR normalization runs before JSON display status and chunk construction, so CRLF-only WordPress plugin edits return `unchanged` by default while `stripCr => false` exposes the preserved line-ending changes.
- Paired changed string/comment atoms now use upstream-style `split_words_and_numbers` plus the `has_common_words` threshold from `src/parse/syntax.rs`. When enough words are shared, JSON changes are emitted as word-level spans instead of whole string/comment replacements.
- YAML block scalar atoms that do not share enough words now fall back to per-line string spans instead of generic token highlighting, mapping upstream `trailling_newline_*.yaml`.
- Token byte spans are now preserved by the tokenizer and used to project paired multiline string/comment/YAML block-scalar atom word diffs back to zero-based line/column spans. This maps the upstream `multiline_string_*.ml` and `multiline_string_eof_*.yml` samples plus WordPress PHP render doc-comment and plugin workflow YAML changes.
- File bytes now map the upstream `src/files.rs` Windows-1252 fallback branch. The PHP decoder keeps valid UTF-8 and UTF-16-BOM handling first, then conservatively accepts printable Windows-1252/Latin-1 text with high bytes so legacy encoded WordPress readme/export bytes produce normal text chunks instead of binary status.
- File bytes now also map the upstream mostly-valid UTF-8 branch. One or two invalid UTF-8/null characters are decoded with replacement characters before Windows-1252 fallback, so slightly corrupted source/export text stays readable and does not silently become Latin-1 punctuation.

The focused PHP lane test now passes 129 tests and 694 assertions, including the mapped upstream XML sample, upstream added-line, insert-blank, align-footer, changes-at-end, text hunk grouping, big-text-hunk dense insertion, many-newlines created-file status, repeated-line-no-EOL text samples, targeted upstream Makefile text-atom CLI fixture, targeted upstream trailing EOF line splitting from `src/lines.rs`, upstream UTF-16 Python sample, upstream multibyte Python display sample, upstream Windows-1252/windows1251 text sample, targeted upstream mostly-valid UTF-8 decoding from `src/files.rs` plus `tests/cli.rs`, targeted upstream strip-CR preprocessing from `src/options.rs`/`src/main.rs`/`src/lines.rs`, upstream Python if indentation sample, targeted upstream Python def/function directory excerpt, targeted upstream Python nested-def directory excerpt, targeted Python compound `elif`/`else`/`try`/`except`/`finally` clause handling, targeted Python trailing-comma tuple exception, CSS sample, upstream tailwind CSS sample, upstream simple SCSS sample, upstream JavaScript simple sample, upstream larger JavaScript named-callback sample, targeted upstream load JavaScript function-scope excerpt, upstream TypeScript type-literal sample, targeted TypeScript module import/export declaration slice, targeted TypeScript default import/namespace/re-export source-change slice, targeted TypeScript export-star/namespace re-export/import-attribute slice, dynamic TypeScript import attribute metadata slice, upstream JSX sample, upstream TSX whitespace sample, upstream HTML style `@media` CSS extraction, full upstream HTML style sub-language sample, full upstream HTML indexed style-block sample, full upstream HTML script sub-language sample, full upstream HTML raw-text de-duplication sample, nested slider Rust sample, nested slider Emacs Lisp sample, upstream change-outer Emacs Lisp sample, upstream strings Emacs Lisp excerpt, upstream Hack sample, upstream string-subwords Emacs Lisp sample, upstream comments Rust sample, upstream multiline string OCaml sample, upstream multiline string EOF YAML sample, upstream trailling-newline YAML sample, upstream YAML sample, targeted JSON display keyword/type/tree_sitter_error highlight behavior from `src/display/json.rs`, targeted parse-error fallback behavior, targeted byte-limit fallback behavior, targeted graph-limit fallback behavior, targeted slider Rust excerpt, and WordPress created import-report display, plugin readme text line output, WordPress plugin readme blank-line JSON display, WordPress readme footer alignment display, WordPress readme end-changes display, WordPress readme nearby-hunk JSON grouping, WordPress no-final-newline import-log JSON display, WordPress plugin build Makefile text-atom review, WordPress CRLF render-comment JSON status, WordPress legacy encoded readme JSON display, WordPress UTF-16 WXR JSON display, WordPress slightly-invalid UTF-8 WXR JSON display, WordPress i18n block-copy multibyte JSON display, WordPress Python migration guard alignment, WordPress Python migration loop alignment, WordPress Python nested migration guard alignment, WordPress Python compound migration clause alignment, WordPress Python trailing-comma review, template-wrapper, render-callback return-type, block editor TypeScript props, block editor module import/export alignment, block module asset import/source alignment, block import assertion/attribute alignment, dynamic block metadata import JSON display, block editor JavaScript function-scoped block registration, block editor JavaScript syntax-error fallback, block editor JavaScript parser-error span display, block render metadata byte-limit fallback, block variation graph-limit fallback, block editor TSX controls, block editor TSX whitespace, WXR XML, inline HTML style sub-language, inline HTML script sub-language, multi-inline asset HTML sub-language, standalone block view-script JavaScript, hook-registration JavaScript, block allow-list array syntax, block-style CSS, block-editor SCSS, nested at-rule CSS, block-copy JSON display, TypeScript metadata keyword/type JSON display, multiline render doc-comment display, plugin workflow YAML display, and plugin workflow step YAML syntax-list scenarios.

The required root test runner was run after this dense inserted text and created-file behavior slice:

```text
php tools/run-tests.php
```

The difftastic-focused test file remains green with 129 tests, 694 assertions, and 0 failures via a direct `TestRunner` invocation.

The latest exact required root run for this batch is green:

```text
172 test files, 16023 assertions, 0 failures
```
