name: Release
on: workflow_dispatch
jobs:
  release:
    steps:
      - uses: actions/checkout@v4
      - name: Build block assets
        run: npm run build
      - name: Make block metadata
        run: wp i18n make-json languages --no-purge
