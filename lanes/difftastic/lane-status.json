{
    "schemaVersion": 1,
    "library": "difftastic",
    "estimatedProgress": 8,
    "suiteProgress": "cloned static inventory: 404 upstream behavior artifacts inspected; upstream contiguous, hyphen-subword, trailing-comma, html_simple, JSON, slider_at_end, targeted slider Rust excerpt, nested_slider Rust, and nested_slider Emacs Lisp samples mapped; recursive syntax-list, Rust method/statement item splitting, nested slider wrapper correction with inner-delimiter and Lisp outer-delimiter preferences, JSON key/list alignment, word-splitting, HTML tag-list, escaped HTML display, and compact JSON display slices mapped; upstream runner not executed",
    "phpPass": 30,
    "phpFail": 0,
    "wordpressScenarios": "Readable diffs for block render callbacks, block-style slugs, templates, theme.json, block.json metadata, saved block markup, theme variation cleanup, code snippets, and structured documents; current examples filter PHP comment churn while preserving an escaping API change, recurse into nested block registration arrays, report hyphenated slug/version changes at subword granularity, emit escaped HTML for block-style, template-wrapper, theme.json, block.json, block-markup, and theme-variation review UIs, and emit machine-readable JSON display chunks for block.json review pipelines.",
    "phase": "cloned static inventory plus comment/delimiter normalization, recursive syntax-list, Rust slider method/statement item splitting, nested slider inner/outer wrapper correction, JSON key/list alignment, subword diff, HTML tag-list, HTML display, and JSON display slices",
    "audit": "audited 2026-05-22: local PHP smoke coverage only; no upstream runner parity",
    "currentWork": "Native tokenizer/differ with comment classification, delimiter anchors, ignore-comments filtering, JavaScript-like trailing-comma normalization, recursive bracketed-list diffs, Rust block item splitting for method and semicolon-terminated statement slider shapes, nested slider wrapper correction for the upstream Rust inner-delimiter fixture shape and Emacs Lisp outer-delimiter fixture shape, JSON object-key item alignment, JSON list deletion alignment for slider_at_end-shaped arrays, upstream-style word/subword diffs, optional HTML angle delimiters for tag-list changes, root tag-list alignment, escaped HTML rendering for token, subword, and syntax-list changes, and compact JSON display output with upstream-style status, aligned_lines, chunks, line_number, and highlighted novel spans.",
    "blocker": "Lane blocker: full upstream runner parity is not available. Rust tooling was installed with `sudo -n dnf install -y cargo rust`, but the sparse blob-filtered clone would need broad hydration plus online Cargo dependency downloads and many tree-sitter parser/native builds; `cargo test --no-run --locked --offline` fails before compilation because the local Cargo cache cannot resolve `clap`. No difftastic-local PHP blocker is present; the focused difftastic test file passes 30 tests and 133 assertions. A final required root `php tools/run-tests.php` run now reports 73 test files, 4068 assertions, and 2 failures outside this lane: `lanes/dolt/tests/TableDiffTest.php` (`wordpress ignore summary fixture hides generated migration tables`) and `lanes/esbuild/tests/TypeScriptModuleLowererTest.php` (`rejects malformed upstream typescript enum members`).",
    "latestCommit": "de9bff9",
    "nextTask": "Map another focused upstream sample_files display pair such as change_outer_*.el, outer_delimiter_*.el, or html_*.html without hydrating the full runner."
}
