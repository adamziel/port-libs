{
    "schemaVersion": 1,
    "library": "difftastic",
    "estimatedProgress": 8,
    "suiteProgress": "cloned static inventory: 404 upstream behavior artifacts inspected; upstream contiguous, hyphen-subword, trailing-comma, html_simple, JSON, and slider_at_end samples mapped; recursive syntax-list, JSON key/list alignment, word-splitting, HTML tag-list, escaped HTML display, and compact JSON display slices mapped; upstream runner not executed",
    "phpPass": 26,
    "phpFail": 0,
    "wordpressScenarios": "Readable diffs for block render callbacks, block-style slugs, templates, theme.json, block.json metadata, saved block markup, theme variation cleanup, code snippets, and structured documents; current examples filter PHP comment churn while preserving an escaping API change, recurse into nested block registration arrays, report hyphenated slug/version changes at subword granularity, emit escaped HTML for block-style, theme.json, block.json, block-markup, and theme-variation review UIs, and emit machine-readable JSON display chunks for block.json review pipelines.",
    "phase": "cloned static inventory plus comment/delimiter normalization, recursive syntax-list, JSON key/list alignment, subword diff, HTML tag-list, HTML display, and JSON display slices",
    "audit": "audited 2026-05-22: local PHP smoke coverage only; no upstream runner parity",
    "currentWork": "Native tokenizer/differ with comment classification, delimiter anchors, ignore-comments filtering, JavaScript-like trailing-comma normalization, recursive bracketed-list diffs, JSON object-key item alignment, JSON list deletion alignment for slider_at_end-shaped arrays, upstream-style word/subword diffs, optional HTML angle delimiters for tag-list changes, root tag-list alignment, escaped HTML rendering for token, subword, and syntax-list changes, and compact JSON display output with upstream-style status, aligned_lines, chunks, line_number, and highlighted novel spans.",
    "blocker": "Lane blocker: full upstream runner parity is not available. Rust tooling was installed with `sudo -n dnf install -y cargo rust`, but the sparse blob-filtered clone would need broad hydration plus online Cargo dependency downloads and many tree-sitter parser/native builds; `cargo test --no-run --locked --offline` now fails before compilation because the local Cargo cache cannot resolve `clap`. No lane-local PHP blocker is present; the focused difftastic test file passes 26 tests and 116 assertions, and `php tools/run-tests.php` passes 61 test files, 3428 assertions, 0 failures in this lane run.",
    "latestCommit": "e6a8ae9",
    "nextTask": "Map the upstream nested_slider fixture pair to exercise delimiter-sliding behavior, then connect that shape to a WordPress nested block/template array example."
}
