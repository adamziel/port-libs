# DOCX relationship source basename stems

Added package provenance summary fields for relationship source part basename stems, matching the target-side stem review surface.

The review remains metadata-only: it buckets source stems, existing and non-existing source counts, duplicate stems, source extensions, content type sources, relationship source kinds, directories, relationship sidecars, and largest existing source digests without exposing package bytes.
