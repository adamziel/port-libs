# pandoc Upstream Test Inventory

Inventory source: blob-filtered shallow clone at `.upstream-cache/pandoc`.

- Upstream commit: `0640c4c9859aa5a3ede082c190fcd5883c24ac83`
- Main suite declared by `pandoc.cabal`: `test-suite test-pandoc`
- Runner shape: Haskell Tasty executable `test/test-pandoc.hs`
- License: GPL-2.0-or-later, with GPL-compatible exceptions documented in
  `COPYRIGHT`

## EPUB3 Scope Boundary

- The EPUB lane is scoped to data import, conversion, preservation, and
  diagnostics. Authenticity and reading-system runtime behavior are not product
  goals for this port.
- JavaScript execution is out of scope. The supported behavior is to preserve
  script resources and metadata where applicable, record `scripted` package
  properties, expose relevant diagnostics, and avoid executing EPUB scripts,
  reading-system APIs, `epub:trigger` behavior, or event-handler code.
- Cryptographic XML signature validation is out of scope. The supported
  behavior is to preserve and record signature/encryption sidecar structures
  for import fidelity and structural diagnostics, without validating digest
  transforms, certificate chains, trust stores, timestamps, revocation, or
  publisher authenticity.
- These exclusions should not count as blockers for EPUB data-import support.
  Future EPUB3 parity notes should continue to distinguish preservation and
  diagnostics from script execution or cryptographic trust validation.

## 2026-06-25 EPUB3 OPF Unique-Identifier Reconcile Slice

- Focused PHP coverage is now 1,145 behavior tests and 21,703 full-suite
  assertions across the Pandoc lane. This slice closes a bounded OPF
  `unique-identifier` sanitation gap without duplicating the separate
  metadata-property refines preservation slice.
- `EpubWriter` now chooses the package `unique-identifier` after generated
  manifest and itemref IDs are known. Configured IDs that already belong to
  package elements such as generated manifest items are skipped, surviving
  `dc:identifier` IDs are preferred, and fallback IDs use an available
  `book-id` variant so generated package metadata does not create package-wide
  duplicate IDs.
- The new reader regression imports OPFs whose `unique-identifier` points at a
  manifest item and at an OPF metadata property with a valid local `refines`.
  Both cases report `invalid-unique-identifier-target` without also reporting
  `missing-unique-identifier`; the metadata-property case also proves valid
  `refines="#book-id"` does not become a missing-refines diagnostic.
- The paired writer regression requests a generated manifest item ID as the
  package unique identifier. Generated EPUB3 output falls back to `book-id`,
  keeps the manifest item and metadata identifier IDs unique, round-trips
  `epubPackageUniqueIdentifierId`, and avoids `duplicate-package-id`,
  `invalid-unique-identifier-target`, `missing-unique-identifier`, and
  `empty-unique-identifier` self diagnostics.
- Verification passed `php -l` for `lanes/pandoc/src/EpubWriter.php`,
  `lanes/pandoc/tests/EpubReaderTest.php`, and
  `lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 15,145 assertions, and 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 21,703 assertions, and 0 failures with
  pass-count verification `pass=1145 fail=0`.
- This remains native PHP EPUB3 OPF identifier/refines sanitation coverage. It
  does not claim EPUBCheck validation, full upstream Haskell runner parity,
  full fixture corpus parity, arbitrary multi-rendition graph validation beyond
  covered generated-package ID collisions, DRM decryption, XML signature
  cryptographic validation, broader OPF vocabulary validation, or full EPUB2
  output parity beyond existing bounded tests.

## 2026-06-25 EPUB2 Guide-Derived Landmark Reader Closure Slice

- Focused PHP coverage is now 1,140 behavior tests and 21,551 full-suite
  assertions across the Pandoc lane. This closure slice derives sanitized EPUB
  landmark metadata from OPF2 guide references when a package has no EPUB3
  landmarks nav.
- `EpubReader` now promotes only valid XML-name guide types into derived
  landmark entries, maps guide `text` to landmark `bodymatter`, requires local
  package-relative guide hrefs to target readable linear spine XHTML resources,
  verifies guide fragments against well-formed target XML, and suppresses
  duplicate type/target pairs.
- Invalid guide types, empty or malformed hrefs, remote guide references,
  missing fragments, non-linear spine targets, NCX/nav targets, and duplicate
  guide references remain out of derived landmarks while source
  `epubGuideReferences` stay available for review and diagnostics.
- Focused reader coverage builds primary and alternate OPF2 rootfiles with
  valid, duplicate, remote, missing-fragment, non-linear, and invalid guide
  references, then verifies primary and alternate `epubLandmarkEntries` plus
  clean landmark self-diagnostics. Existing writer-side EPUB3 guide-derived
  landmark generation remains covered by the paired reader/writer gate.
- Verification: `php -l` passed for `EpubReader.php`, `EpubReaderTest.php`,
  and `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,818 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,993 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,551 assertions, 0 failures with pass-count verification `pass=1140
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB2 guide-derived
  landmark import for primary and alternate OPF2 packages. It does not claim
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  covered rootfile summaries, DRM decryption, XML signature cryptographic
  validation, or broad EPUB2/EPUB3 navigation parity beyond existing focused
  OPF guide, NCX, and landmark cases.

## 2026-06-25 EPUB3 Package-Document Link Closure Slice

- Focused PHP coverage is now 1,139 behavior tests and 21,547
  full-suite assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-package-link-package-document-reference`
  when OPF package links target the package document itself without a fragment,
  not only when they use `package.opf#id` or fragment-only hrefs.
- Bare package-document diagnostics preserve the offending `href`, resolved OPF
  path, and package/collection parent context, while omitting fragment context
  for hrefs that do not contain one.
- `EpubWriter` now treats bare package-document package links as invalid for
  top-level metadata links, collection metadata links, direct collection member
  links, and nested collection links. Generated OPF output omits those members
  rather than accepting `package.opf` as a valid local linked resource.
- Dependency-closure checkpoint: this is limited to native PHP OPF package-link
  handling for references back to the package document itself. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary cross-rendition graph validation, browser
  rendering behavior, DRM decryption, XML signature cryptographic validation,
  or broader package metadata vocabulary parity.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,804 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 10,185 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,989 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 21,547 assertions, 0 failures, with
  1,139 counted `PASS` lines in
  `/tmp/pandoc-epub-package-doc-link-full-rebased-20260625.log`.

## 2026-06-25 EPUB NCX Direct-Child Navigation Import Slice

- Focused PHP coverage is now 1,139 behavior tests and 21,527 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility for
  EPUB3 packages that still carry NCX `navMap`, `pageList`, and `navList`
  navigation.
- `EpubReader` now imports NCX `navPoint`, `pageTarget`, and `navTarget` text,
  language, and `content src` only from direct `navLabel`/`content` children.
  That aligns import with the diagnostics path, so malformed parent navigation
  entries no longer borrow descendant labels or targets while valid nested
  `navPoint` children continue to import.
- The new reader regression builds valid nested `navPoint` entries, page-list
  and nav-list entries, and a malformed parent `navPoint` whose only label and
  target live on a child. Read-back reports missing direct `navLabel` and
  `content` diagnostics for the parent, imports the valid child instead of
  synthesizing the parent, normalizes relative NCX content sources, preserves
  label language metadata, and keeps `pageList`/`navList` metadata intact.
- The paired writer regression proves generated EPUB3 NCX output normalizes
  duplicate/backward explicit `playOrder` metadata into a monotonic sequence
  across `navMap`, `pageList`, and `navList` output without self-diagnosing
  duplicate/out-of-order play order, missing content, or target errors.
- Verification passed `php -l` for `lanes/pandoc/src/EpubReader.php`,
  `lanes/pandoc/tests/EpubReaderTest.php`, and
  `lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,794 assertions, and
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 10,175 assertions, and 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,969 assertions, and 0 failures; and `php
  tools/run-tests.php lanes/pandoc/tests` passed 11 files, 21,527 assertions,
  and 0 failures.
- This remains native PHP EPUB3 NCX compatibility coverage. It does not claim
  EPUBCheck validation, full upstream Haskell runner parity, full fixture corpus
  parity, DRM decryption, XML signature cryptographic validation, arbitrary
  multi-rendition graph validation beyond covered paths, broader TeX/MathML
  parser parity, or full EPUB2 output parity beyond existing bounded NCX tests.
## 2026-06-25 EPUB3 Split Itemref Rendition Conflict Slice

- Focused PHP coverage is now 21,644 full-suite assertions across the Pandoc
  lane. This implementation slice closes a generated EPUB3 fixed-layout
  self-conflict path for alternate rootfiles with split spine metadata.
- `EpubWriter` now suppresses per-spine `rendition:*` refinements whose
  structured metadata conflicts with explicit itemref rendition properties for
  the same spine entry. Explicit itemref properties remain the deterministic
  source for layout, orientation, spread, and flow in that conflict case, while
  non-conflicting viewport refinements and same-value scoped rendition metadata
  are still emitted.
- Focused coverage writes an alternate EPUB3 rootfile with split fixed-layout
  spine pages, explicit `rendition:layout-*`, `rendition:orientation-*`,
  `rendition:spread-*`, and `rendition:flow-*` itemref properties, plus
  conflicting structured metadata fields. The regression verifies generated OPF
  output omits only the self-conflicting refinements, preserves viewport
  fallback metadata in the OPF and XHTML heads, and reads back through
  `EpubReader` without conflicting itemref/refinement or cross-target
  diagnostics.
- Verification: `php -l lanes/pandoc/src/EpubWriter.php` and `php -l
  lanes/pandoc/tests/EpubWriterTest.php` passed; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,254 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 15,086 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,644 assertions, 0 failures.
- Dependency-closure checkpoint: this is limited to generated EPUB3
  itemref/refinement conflict sanitation for the covered split-spine
  alternate-rootfile writer path and the shared per-spine metadata helper. It
  does not claim reading-system rendering, EPUBCheck validation, arbitrary
  multi-rendition graph validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, DRM decryption, XML signature cryptographic
  validation, or broader OPF vocabulary validation beyond existing focused
  diagnostics.
## 2026-06-24 EPUB3 Samp/Var Source Handoff Slice

- Focused PHP coverage remains 1,137 behavior tests and is now 21,494
  full-suite assertions across the Pandoc lane. This implementation slice
  tightens bounded EPUB/HTML phrase source fidelity for bare `samp` and `var`
  inline code roles.
- `WordPressBlockWriter` now lowers inline code nodes that carry only the
  generated `sample` or `variable` role back to native `<samp>` and `<var>`
  markup. The path is deliberately narrow: code with IDs, extra classes, custom
  attributes, or highlighted source markup remains on the existing `<code>`
  renderer.
- Focused coverage now expects direct HTML-reader source `<samp>` and `<var>`
  imports to hand off to native WordPress tags, and the EPUB3 phrase-source
  spine fixture includes `samp` and `var` alongside `b`, `i`, `tt`, and `code`.
  It asserts structured code AST role classes, semanticElements metadata
  retention, and native WordPress output.
- Verification: `php -l` passed for `WordPressBlockWriter.php`,
  `MarkdownReaderTest.php`, and `EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,733 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,776 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,936 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,494 assertions, 0 failures with pass-count verification `pass=1137
  fail=0`.
- Dependency-closure checkpoint: this closes bare `samp`/`var` WordPress
  handoff for the covered source-reader and EPUB phrase-source paths. It does
  not claim arbitrary HTML5 tree-construction parity, browser validation
  behavior, EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader code-role
  styling parity beyond existing focused cases.

## 2026-06-24 EPUB3 Iframe Source Import Slice

- Focused PHP coverage is now 1,137 behavior tests and 21,486 full-suite
  assertions across the Pandoc lane. This implementation slice upgrades bounded
  PHP `epub` spine conversion for source XHTML `iframe` fallback content from
  literal text/raw fallback handling into structured source AST import when no
  local iframe resource can be expanded.
- `MarkdownReader` now recognizes standalone source `iframe` inline HTML
  fragments and maps real source `iframe` tags to classed `span` nodes with
  parsed fallback text, retained IDs/classes, `src`, title/loading, and data
  attributes. The existing package/local iframe resource expansion path remains
  intact for dereferenceable iframe resources.
- `WordPressBlockWriter` now lowers metadata-proven `iframe` spans back to
  native `iframe` HTML through a dedicated iframe path with a narrow
  `src`/`srcdoc`-oriented attribute allowlist. Ordinary CSS-only classes named
  `iframe` still remain spans unless native iframe source attributes prove
  native element intent.
- Focused coverage adds a standalone HTML-reader `iframe` fixture and
  strengthens the EPUB3 media/math/ruby source spine fixture with a remote
  iframe and required `remote-resources` manifest property. It asserts
  structured span AST nodes, retained iframe metadata, native WordPress handoff,
  disabled-raw-HTML behavior, semantic metadata retention, and generated local
  iframe round-trip WordPress handoff as native iframe output.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`,
  `EpubReaderTest.php`, and `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,733 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,768 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,928 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,486 assertions, 0 failures with pass-count verification `pass=1137
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for non-expanded `iframe` elements in the covered source-reader paths. It
  does not claim reading-system runtime iframe behavior, JavaScript execution,
  arbitrary iframe sandbox enforcement, arbitrary HTML5 tree-construction
  parity, browser validation behavior, EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader interactive media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 Canvas Source Import Slice

- Focused PHP coverage is now 1,136 behavior tests and 21,458 full-suite
  assertions across the Pandoc lane. This implementation slice upgrades bounded
  PHP `epub` spine conversion for source XHTML `canvas` fallback content from
  raw inline fallback boundaries into structured source AST import.
- `MarkdownReader` now recognizes standalone source `canvas` inline HTML
  fragments and maps real source `canvas` tags to classed `span` nodes with
  parsed fallback text, retained IDs/classes, dimensions, titles, and data
  attributes.
- `WordPressBlockWriter` now lowers metadata-proven `canvas` spans back to
  native `canvas` HTML through a dedicated canvas path with a narrow width/height
  attribute allowlist. Ordinary CSS-only classes named `canvas` still remain
  spans unless native dimensions prove native element intent.
- Focused coverage adds a standalone HTML-reader `canvas` fixture and
  strengthens the EPUB3 media/math/ruby source spine fixture. It asserts
  structured span AST nodes, retained canvas metadata, native WordPress handoff,
  disabled-raw-HTML behavior, and existing generated EPUB3 canvas round-trip
  metadata coverage.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,718 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,755 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,915 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,458 assertions, 0 failures with pass-count verification `pass=1136
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for `canvas` in the covered source-reader paths. It does not claim scripted
  canvas behavior, arbitrary HTML5 tree-construction parity, browser validation
  behavior, EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader interactive
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Picture Source Import Slice

- Focused PHP coverage is now 1,135 behavior tests and 21,429 full-suite
  assertions across the Pandoc lane. This implementation slice upgrades bounded
  PHP `epub` spine conversion for source XHTML `picture` and `source` fallback
  image content from raw inline fallback boundaries into structured source AST
  import.
- `MarkdownReader` now recognizes standalone source `picture` inline HTML
  fragments and maps real source `picture` tags to classed `span` nodes with
  parsed child content, retained IDs/classes, titles, and data attributes.
  Structured `source` void children use the existing generated-writer span
  convention and fallback `img` content remains a native image node.
- `WordPressBlockWriter` now lowers metadata-proven `picture` spans back to
  native `picture` HTML through a dedicated picture path with a narrow picture
  attribute allowlist, rendering structured `source` children and fallback
  images as native picture contents. Ordinary CSS-only classes named `picture`
  still remain spans unless source/image children prove native element intent.
- Focused coverage adds a standalone HTML-reader `picture`/`source`/fallback
  image fixture and strengthens the EPUB3 media/math/ruby source spine fixture.
  It asserts structured span AST nodes, retained picture/source/image metadata,
  native WordPress handoff, disabled-raw-HTML behavior, package-relative
  resource tracking, and existing generated EPUB3 picture round-trip metadata
  coverage.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,703 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,741 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,901 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,429 assertions, 0 failures with pass-count verification `pass=1135
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for `picture`, `source`, and fallback image content in the covered
  source-reader paths. It does not claim arbitrary HTML5 tree-construction
  parity, browser validation behavior, EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader responsive-image/media edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 Object/Param/Embed Source Import Slice

- Focused PHP coverage is now 1,134 behavior tests and 21,380 full-suite
  assertions across the Pandoc lane. This implementation slice upgrades bounded
  PHP `epub` spine conversion for source XHTML `object`, `param`, and `embed`
  elements from raw inline fallback boundaries into structured source AST
  import.
- `MarkdownReader` now maps real source `object` tags to classed `span` nodes
  with parsed child content, and maps source `param` and `embed` void elements
  to structured classed spans. As with the prior void-element slices, any
  following text nested by libxml's HTML parser is appended back into normal
  inline flow.
- `WordPressBlockWriter` now lowers metadata-proven `object` spans back to
  native object-family HTML, including structured `param` and `embed` child
  spans, through a dedicated object path with a narrow object/param/embed
  attribute allowlist. Ordinary CSS-only classes named `object`, `param`, or
  `embed` still remain spans unless native attributes or children prove native
  element intent.
- Focused coverage updates the standalone HTML-reader object/embed fixture and
  strengthens the EPUB3 media/math/ruby source spine fixture. It asserts
  structured span AST nodes, retained object/param/embed metadata, native
  WordPress handoff, disabled-raw-HTML behavior, package-relative resource
  tracking, and existing generated EPUB3 object/embed round-trip metadata
  coverage.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,679 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,716 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,876 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,380 assertions, 0 failures with pass-count verification `pass=1134
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for `object`, `param`, and `embed` in the covered source-reader paths. It
  does not claim arbitrary HTML5 tree-construction parity, browser validation
  behavior, EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader object/media
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Audio/Video Source/Track Source Import Slice

- Focused PHP coverage is now 1,134 behavior tests and 21,329 full-suite
  assertions across the Pandoc lane. This implementation slice upgrades bounded
  PHP `epub` spine conversion for source XHTML `audio`, `video`, `source`, and
  `track` media elements from raw inline fallback boundaries into structured
  source AST import.
- `MarkdownReader` now maps real source `audio` and `video` tags to classed
  `span` nodes with parsed child content, and maps source `source` and `track`
  void elements to structured classed spans. As with the prior void-element
  slices, any following text nested by libxml's HTML parser is appended back
  into normal inline flow.
- `WordPressBlockWriter` now lowers metadata-proven `audio` and `video` spans
  back to native media HTML, including structured `source` and `track` child
  spans, through a dedicated media path with a narrow media attribute allowlist.
  Ordinary CSS-only classes named `audio` or `video` still remain spans unless
  media attributes or `source`/`track` children prove native element intent.
- Focused coverage updates the standalone HTML-reader audio/video fixtures and
  strengthens the EPUB3 media/math/ruby source spine fixture. It asserts
  structured span AST nodes, retained media/source/track metadata, native
  WordPress handoff, disabled-raw-HTML behavior, and existing generated EPUB3
  media round-trip metadata coverage.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`,
  `EpubReaderTest.php`, and `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,659 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,685 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,845 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,329 assertions, 0 failures with pass-count verification `pass=1134
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for `audio`, `video`, `source`, and `track` in the covered source-reader
  paths. It does not claim arbitrary HTML5 tree-construction parity, browser
  validation behavior, EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Map/Area Source Import Slice

- Focused PHP coverage is now 1,134 behavior tests and 21,288 full-suite
  assertions across the Pandoc lane. This implementation slice upgrades bounded
  PHP `epub` spine conversion for source XHTML `map` and `area` image-map
  elements from raw inline fallback boundaries into structured source AST
  import.
- `MarkdownReader` now maps real source `map` tags to classed `span` nodes with
  parsed child content and maps source `area` void elements to structured
  classed spans, while any following text nested by libxml's HTML parser is
  appended back into normal inline flow.
- `WordPressBlockWriter` now lowers metadata-proven `map` and `area` classed
  spans to native image-map HTML through a dedicated map/area path with a narrow
  image-map attribute allowlist for `name`, `href`, `alt`, `coords`, `shape`,
  and related safe attributes. CSS-only classes named `map` or `area` still
  remain ordinary spans unless image-map metadata proves native element intent.
- Focused coverage updates the standalone HTML-reader map/area fixture and
  strengthens the EPUB3 media/math/ruby source spine fixture. It asserts
  structured span AST nodes, retained map/area metadata, native WordPress
  handoff, disabled-raw-HTML behavior, and existing generated EPUB3 image-map
  round-trip metadata coverage.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`,
  `EpubReaderTest.php`, and `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,635 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,668 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,828 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,288 assertions, 0 failures with pass-count verification `pass=1134
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for `map` and `area` in the covered source-reader paths. It does not claim
  arbitrary HTML5 tree-construction parity, browser validation behavior,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Ruby Source Import Slice

- Focused PHP coverage is now 1,134 behavior tests and 21,255 full-suite
  assertions across the Pandoc lane. This implementation slice upgrades bounded
  PHP `epub` spine conversion for source XHTML `ruby`, `rt`, and `rp` inline
  elements from raw inline fallback boundaries into structured source AST
  import.
- `MarkdownReader` now maps real source ruby annotation trees to classed
  `span` nodes using the same generated-writer convention already exercised by
  EPUB3 output. The import preserves retained classes, source IDs, `xml:lang`,
  `data-*` attributes, ruby base text, ruby text, and fallback parentheses as
  parsed child nodes instead of raw open/close tags.
- `WordPressBlockWriter` now lowers `ruby`, `rt`, and `rp` classed spans to
  native HTML tags through the span-like output path, matching existing
  `HtmlWriter` behavior and generated EPUB3 ruby output.
- Focused coverage updates the direct HTML-reader ruby case and strengthens the
  EPUB3 media/math/ruby source spine fixture. It asserts structured span AST
  nodes, retained ruby metadata, native WordPress handoff, and existing
  generated EPUB3 ruby round-trip metadata coverage.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`,
  `EpubReaderTest.php`, and `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,625 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,645 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,805 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,255 assertions, 0 failures with pass-count verification `pass=1134
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for `ruby`, `rt`, and `rp` in the covered source-reader paths. It does not
  claim arbitrary HTML5 tree-construction parity, browser validation behavior,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Cite/WBR Source Import Slice

- Focused PHP coverage is now 1,134 behavior tests and 21,235 full-suite
  assertions across the Pandoc lane. This implementation slice upgrades bounded
  PHP `epub` spine conversion for source XHTML `cite` and `wbr` inline
  elements from raw inline fallback boundaries into structured source AST
  import.
- `MarkdownReader` now maps real source `cite` tags to classed `span` nodes
  with retained IDs, classes, `data-*` attributes, and parsed children. Source
  `wbr` now becomes an empty classed `span` node so it preserves void-element
  behavior, while any following text nested by libxml's HTML parser is appended
  back into normal inline flow.
- `WordPressBlockWriter` now lowers metadata-proven `wbr` spans to native
  `<wbr>` output after stripping the marker class and preserving retained
  source attributes. `cite` joins the semantic span-like native output path,
  matching existing EPUB writer behavior for generated cite spans.
- Focused coverage updates the direct HTML-reader standalone and fixture cases
  plus the EPUB3 HTML5 source spine fixture. It asserts structured span AST
  nodes, retained source metadata, native WordPress handoff for `cite` and
  `wbr`, and continued generated EPUB3 cite/wbr round-trip metadata coverage.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`,
  `EpubReaderTest.php`, and `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,623 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,627 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,787 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,235 assertions, 0 failures with pass-count verification `pass=1134
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for `cite` and `wbr` in the covered source-reader paths. It does not claim
  arbitrary HTML5 tree-construction parity, browser validation behavior,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 HTML5 Semantic Inline Source Import Slice

- Focused PHP coverage is now 1,134 behavior tests and 21,208 full-suite
  assertions across the Pandoc lane. This implementation slice upgrades bounded
  PHP `epub` spine conversion for source XHTML `bdi`, `data`, `meter`,
  `output`, `progress`, and `time` inline elements from raw inline boundary
  preservation into structured source AST import.
- `MarkdownReader` now maps real source `bdi`, `data`, `meter`, `output`,
  `progress`, and `time` tags to classed `span` nodes, preserving source IDs,
  retained classes, per-element attributes, `data-*` attributes, and parsed
  inline children. This makes source EPUB3 spine read-back align with the
  structured span-like AST shape already used by the generated EPUB3 writer
  paths for these elements.
- `WordPressBlockWriter` now renders metadata-proven semantic spans back to
  native `bdi`, `data`, `meter`, `output`, `progress`, and `time` HTML using
  per-tag proving attributes such as `dir`, `value`, `min`/`max`, `for`,
  `name`, and `datetime`. Ordinary CSS-only classes with those names still fall
  back to spans unless the proving attributes are present.
- Focused coverage updates direct HTML-reader fixtures, the EPUB3 source spine
  fixture, generated raw-inline EPUB read-back, and alternate-rootfile body
  summaries. It asserts structured span AST nodes, retained semantic metadata,
  native WordPress handoff, and continued XML-valid generated XHTML.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`,
  `EpubReaderTest.php`, and `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,611 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,612 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,160 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,772 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,208 assertions, 0 failures with pass-count verification `pass=1134
  fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for the covered HTML5 semantic inline elements. It does not claim arbitrary
  HTML5 tree-construction parity, browser validation behavior, EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 Button Source Import And URL Boundary Slice

- Focused PHP coverage is now 1,134 behavior tests and 21,145 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` spine conversion from form/control source import into structured
  source XHTML AST import for metadata-proven `button` controls.
- `MarkdownReader` now maps preserved source `button` elements to classed
  `span` nodes when `preserveHtmlInputControls` is enabled, matching the
  existing structured form-control convention for `label`, `select`,
  `optgroup`, `option`, and `textarea`. `WordPressBlockWriter` renders those
  metadata-proven button spans back to native `button` HTML with button/form
  attributes such as `formaction`, `formmethod`, `formenctype`, `formtarget`,
  `formnovalidate`, `disabled`, and `autofocus`, while avoiding generic CSS
  class `button` promotion without button-specific evidence.
- `EpubReader` still rewrites package-relative `formaction` values before XHTML
  spine parsing, but URL-bearing attribute matching no longer rewrites
  hyphenated `data-*` suffixes such as `data-action`. The related reader and
  writer remote/scripted/resource scanners now use the same hyphen-aware
  boundary so `data-action` and `data-srcset` values are not treated as URL
  resources, while namespaced resource attributes such as `epub:textref` remain
  covered.
- Focused coverage extends the direct HTML form fixture and EPUB3 interactive
  spine fixture; it asserts structured button AST nodes, native WordPress/HTML
  button handoff, retained semantic metadata, package-relative `formaction`
  rewriting, and literal `data-action` preservation.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `EpubReader.php`, `EpubWriter.php`,
  `MarkdownReaderTest.php`, `EpubReaderTest.php`, and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 4,601 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,589 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 10,130 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,719 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 21,145 assertions, 0 failures with
  pass-count verification `pass=1134 fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for metadata-proven button controls and a URL-matcher boundary bug affecting
  `data-*` attributes. It does not claim arbitrary imported form-control body
  reconstruction, browser form submission or validation behavior,
  script-trigger behavior, EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Interactive Form Source Import Slice

- Focused PHP coverage is now 1,134 behavior tests and 21,137 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` spine conversion from form/control metadata retention into
  structured source XHTML AST import for interactive form content.
- `MarkdownReader` now maps DOM-parsed source `form` and `fieldset` blocks to
  the existing structured classed-`div` convention already used by `HtmlWriter`,
  maps contextual `legend` blocks to classed div blocks, and maps preserved
  `label`, `select`, `optgroup`, `option`, and `textarea` controls to classed
  `span` nodes. Standalone raw `<textarea>` Markdown import and the default
  non-EPUB behavior for non-checkbox input controls remain unchanged.
- `EpubReader` already enables `htmlNativeDivs` and `preserveHtmlInputControls`
  for XHTML spine conversion, so source EPUB3 form chapters now retain
  structured form/control AST nodes while preserving semantic spine metadata and
  package-relative action/formaction rewriting. `WordPressBlockWriter` now
  renders the structured form div/span nodes back to native `form`, `fieldset`,
  `legend`, `label`, `select`, `optgroup`, `option`, and `textarea` HTML using
  form-control-specific attribute filtering; this avoids broad inline allowlist
  leakage such as link MIME `type` attributes appearing on ordinary anchors.
- Focused coverage imports the same form/control structures through direct HTML
  parsing and EPUB3 spine parsing; asserts structured form, fieldset, legend,
  label, select, optgroup, option, and textarea AST nodes, native WordPress/HTML
  handoff, semantic metadata retention, and package-relative form
  action/formaction rewriting.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, `EpubReaderTest.php`,
  and `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,597 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,585 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,130 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,715 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,137 assertions, 0 failures with pass-count verification `pass=1134 fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for the covered interactive form/control structures, but it does not claim
  arbitrary imported form-control body reconstruction, browser form submission
  or validation behavior, script-trigger behavior, EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 Native HTML5 Container Source Import Slice

- Focused PHP coverage is now 1,133 behavior tests and 21,092 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` spine conversion from metadata-only residual HTML5 container
  recognition into structured source XHTML AST import for native container
  wrappers.
- `MarkdownReader` now maps source `article`, `nav`, `footer`, `address`,
  `hgroup`, `menu`, `search`, and `dialog` elements through the existing
  `htmlNativeDivs` path, adding to the previously covered `section`, `aside`,
  `header`, and `main` wrappers. These elements import as classed `div` blocks
  with retained IDs, classes, ARIA/role/title/lang metadata, and `data-*`
  attributes. Source `menu` elements with `li` children import as a classed
  wrapper containing a parsed `bullet_list`, and the existing wrapper/heading ID
  deduplication still clears a first heading ID that matches its wrapper.
- `EpubReader` already enables `htmlNativeDivs` for XHTML spine conversion, so
  source EPUB3 chapters using `address`, `hgroup`, `menu`, `search`, and
  `dialog` now produce structured `div`/list AST output while retaining the
  residual container `semanticElements` metadata added in the prior slice.
  `WordPressBlockWriter` now allows `open` on block-level HTML attributes so
  `dialog open` survives the PHP AST to WordPress HTML handoff.
- Focused coverage imports the same residual HTML5 containers through both
  direct Markdown HTML parsing and EPUB3 spine parsing; asserts classed div
  wrappers, nested native container wrappers, menu list conversion, matching
  heading ID deduplication, semantic metadata retention, WordPress output, and
  `dialog open` preservation.
- Verification: `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, `EpubReaderTest.php`,
  and `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,572 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 4,565 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,130 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,695 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,092 assertions, 0 failures with pass-count verification `pass=1133 fail=0`.
- Dependency-closure checkpoint: this closes bounded EPUB3 source XHTML import
  for the covered native HTML5 container wrappers, but it does not claim full
  HTML5 tree-construction parity, browser layout or dialog behavior, EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 Residual HTML5 Source Container Reader Metadata Slice

- Focused PHP coverage is now 1,132 behavior tests and 21,052 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` reader metadata coverage for remaining source XHTML HTML5
  container tags in EPUB spine chapters.
- `EpubReader` now recognizes source `address`, `hgroup`, `menu`, `search`, and
  `dialog` elements in spine `semanticElements` when they carry source
  metadata. The reader preserves retained IDs, roles, classes, data attributes,
  text, and `dialog open` state. Existing HTML reader AST conversion is
  unchanged, so these importer-generic source containers continue to normalize
  through the current paragraph flow unless future parser work adds native block
  handling.
- Focused source EPUB coverage builds a package with address contact text,
  hgroup title/deck text, menu action items, search text, and an open dialog;
  asserts current read-back paragraph normalization, normalized WordPress block
  output, residual container `semanticElements` metadata, and no
  `malformed-spine-xhtml` diagnostics.
- Verification: `php -l` passed for `EpubReader.php`, `EpubReaderTest.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,555 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 10,130 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,685 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,052 assertions, 0 failures with pass-count verification `pass=1132 fail=0`.
- Dependency-closure checkpoint: this closes bounded source EPUB spine semantic
  metadata coverage for residual HTML5 container tags, but it does not claim
  native AST reconstruction for those containers, full HTML5 tree-construction
  parity, browser CSS/layout/dialog behavior, EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 Structural Source Block Reader Metadata Slice

- Focused PHP coverage is now 1,131 behavior tests and 21,029 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` reader metadata coverage for source XHTML structural block tags in
  EPUB spine chapters.
- `EpubReader` now recognizes source `h1`-`h6`, `p`, `blockquote`, `pre`,
  `ol`, `ul`, `li`, `dl`, `dt`, `dd`, and `hr` elements in spine
  `semanticElements` when they carry source metadata. The reader preserves
  retained IDs, classes, data attributes, element text, `blockquote cite`,
  ordered-list `start`/`type`/`reversed`, unordered-list `type`, and list-item
  `value` metadata. Empty `hr` elements are retained as marker records, while
  bare paragraphs without metadata still do not create semantic metadata noise.
- Plain HTML attributes now use direct attribute lookup during semantic metadata
  collection, so namespaced `epub:type` is not duplicated as an HTML `type`
  value on elements such as `ol`. The `trigger` path still accepts namespaced XML
  Events local attributes such as `ev:observer` and `ev:event`.
- Focused source EPUB coverage builds a package with heading, paragraph,
  blockquote, pre/code, ordered and unordered lists, definition list, and
  horizontal-rule structures; asserts read-back AST block shapes, normalized
  WordPress block output, structural `semanticElements` metadata, and no
  `malformed-spine-xhtml` diagnostics.
- Verification: `php -l` passed for `EpubReader.php`, `EpubReaderTest.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,532 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 10,130 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,662 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  21,029 assertions, 0 failures with pass-count verification `pass=1131 fail=0`.
- Dependency-closure checkpoint: this closes bounded source EPUB spine semantic
  metadata coverage for common structural block tags, but it does not claim full
  XHTML5 element coverage, browser CSS/layout behavior, EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 Body Style/Script Reader Metadata Slice

- Focused PHP coverage is now 1,130 behavior tests and 20,979 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` reader metadata coverage for source XHTML body `style` and
  `script` tags in EPUB spine chapters.
- `MarkdownReader` already preserves body `style` and generic `script` tags as
  raw HTML, and converts `script type="math/tex"` payloads to math nodes.
  `EpubReader` now recognizes body `style` and `script` in spine
  `semanticElements`, while leaving AST conversion unchanged.
- Focused source EPUB coverage builds a package with a block style, inline
  style, `math/tex` script, src-backed generic script, and bare generic script;
  asserts read-back raw block/inline/math shapes, normalized WordPress block
  output with package-qualified CSS/script resource references, style
  type/media/data/text metadata, script type/src/defer/data/text metadata, bare
  style/script text-only metadata, and no malformed-spine or
  missing-manifest-required-property diagnostics.
- Verification: `php -l` passed for `EpubReader.php`, `EpubReaderTest.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,482 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 10,130 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,612 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,979 assertions, 0 failures with pass-count verification `pass=1130 fail=0`.
- Dependency-closure checkpoint: this closes bounded source EPUB spine semantic
  metadata coverage for body style/script tags, but it does not claim browser
  CSS/script execution, complete JavaScript/CSS semantics, CSS cascade/layout
  validation, full XHTML5 element coverage, EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 Raw Fallback Source Tag Reader Metadata Slice

- Focused PHP coverage is now 1,129 behavior tests and 20,951 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` reader metadata coverage for source XHTML `applet`, `svg`, and
  `noscript` fallback/raw tags in EPUB spine chapters.
- `MarkdownReader` already preserves source `applet`, SVG, and `noscript`
  markup as raw inline content during HTML body conversion. `EpubReader` now
  recognizes `applet`, `svg`, and `noscript` in spine `semanticElements`, while
  leaving AST conversion unchanged.
- Focused source EPUB coverage builds a package with a legacy applet, an SVG
  chapter manifest property, and noscript fallback text; asserts read-back
  paragraph import, normalized WordPress block output, applet
  code/archive/width/height/text metadata, SVG id/classes/data/viewBox/title
  text metadata, noscript text-only metadata, and no malformed-spine or
  missing-manifest-required-property diagnostics.
- Verification: `php -l` passed for `EpubReader.php`, `EpubReaderTest.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,454 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 10,130 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,584 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,951 assertions, 0 failures with pass-count verification `pass=1129 fail=0`.
- Dependency-closure checkpoint: this closes bounded source EPUB spine semantic
  metadata coverage for raw fallback tags, but it does not claim full SVG DOM
  parity, browser/script/applet execution, CSS/browser rendering behavior
  validation, full XHTML5 element coverage, EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 Line-Break Reader Metadata Slice

- Focused PHP coverage is now 1,128 behavior tests and 20,933 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` reader metadata coverage for generated and source XHTML `br`
  line-break tags in EPUB spine chapters.
- `HtmlWriter` already renders `linebreak` AST nodes as native XHTML `<br />`.
  `MarkdownReader` already maps source `<br>` variants to `linebreak` AST
  nodes during HTML body conversion. `EpubReader` now recognizes `br` in spine
  `semanticElements`, and keeps empty `br` records so void line-break tags
  preserve element identity even when they have no text payload.
- Focused generated EPUB3 coverage writes a line-break paragraph, asserts exact
  generated XHTML, XML-valid spine output, read-back `text`/`linebreak`/`text`
  AST nodes, a bare `br` semantic spine metadata record, and no malformed-spine
  diagnostics. Focused source EPUB coverage builds a package with bare and
  class/data-attributed `br` tags; asserts read-back
  `text`/`linebreak`/`text`/`linebreak`/`text` AST nodes, normalized WordPress
  block output, and semantic spine metadata for both source `br` elements
  including retained class and data metadata.
- Verification: `php -l` passed for `EpubReader.php`, `EpubReaderTest.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,436 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 10,130 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,566 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,933 assertions, 0 failures with pass-count verification `pass=1128 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated/source EPUB
  spine semantic metadata coverage for line-break tags, but it does not claim
  CSS/browser rendering behavior validation, full XHTML5 element coverage,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Phrase Inline Reader Metadata Slice

- Focused PHP coverage is now 1,126 behavior tests and 20,912 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` reader metadata coverage for generated and source XHTML phrase
  inline tags in EPUB spine chapters.
- `HtmlWriter` already renders generated `strong`, `emph`, and bare `code` AST
  nodes as native XHTML `<strong>`, `<em>`, and `<code>`. `MarkdownReader`
  already maps source `<strong>/<b>` to `strong`, `<em>/<i>` to `emph`, and
  `<code>/<tt>` to `code` nodes during HTML body conversion. `EpubReader` now
  recognizes `strong`, `b`, `em`, `i`, `code`, and `tt` in spine
  `semanticElements`, and keeps text-only records for those tags so generated
  and source EPUB packages preserve element identity and text in metadata.
- Focused generated EPUB3 coverage writes `strong` with nested `emph`,
  standalone `emph`, and bare `code` output; asserts exact generated XHTML,
  XML-valid spine output, read-back AST phrase nodes, semantic spine metadata
  for `strong`, `em`, and `code`, and no malformed-spine diagnostics. Focused
  source EPUB coverage builds a package with `b`, `i`, `tt`, and `code` aliases;
  asserts read-back AST nodes, normalized WordPress block output, and source
  element identities in semantic spine metadata.
- Verification: `php -l` passed for `EpubReader.php`, `EpubReaderTest.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 4,424 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 10,121 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,545 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,912 assertions, 0 failures with pass-count verification `pass=1126 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated/source EPUB
  spine semantic metadata coverage for basic phrase inline tags, but it does not
  claim CSS/browser rendering behavior validation, full XHTML5 element coverage,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Source Text-Decoration Reader Metadata Slice

- Focused PHP coverage is now 1,124 behavior tests and 20,877 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub` reader metadata coverage for plain source XHTML text-decoration
  tags in EPUB spine chapters.
- `MarkdownReader` already maps source `<ins>` to `underline` and source
  `<s>`, `<strike>`, and `<del>` to `strikeout` during HTML body conversion.
  `EpubReader` now recognizes `s` and `strike` in spine `semanticElements`, and
  keeps text-only records for `ins`, `s`, `strike`, and `del` so source EPUB
  packages preserve the original element identity and text in metadata.
- Focused EPUB3 reader coverage builds a source EPUB package with plain `ins`,
  `s`, `strike`, and `del` in a spine chapter; asserts read-back
  `underline`/`strikeout` AST nodes, normalized WordPress block output through
  `<u>` and `<del>`, and semantic spine metadata for all four source element
  identities and text. Existing generated EPUB3 writer decoration coverage
  remains green.
- Verification: `php -l` passed for `EpubReader.php` and `EpubReaderTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php` passed 1 file,
  4,408 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,510 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,877 assertions, 0 failures with pass-count verification `pass=1124 fail=0`.
- Dependency-closure checkpoint: this closes bounded source EPUB spine semantic
  metadata coverage for plain text-decoration tags, but it does not claim
  CSS/browser rendering behavior validation, full XHTML5 element coverage,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Underline/Strikeout Inline Reader Metadata Slice

- Focused PHP coverage is now 1,123 behavior tests and 20,861 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub3` reader metadata coverage for native underline and strikeout inline
  elements already emitted by the generated writer path.
- `HtmlWriter` already renders `underline` and `strikeout` AST nodes as native
  XHTML `<u>` and `<del>` in generated EPUB spine output. `EpubReader` now
  recognizes `u` in spine `semanticElements`, and keeps text-only `u` and
  plain `del` records so generated decoration output round-trips element
  identity and text.
- Focused EPUB3 coverage writes generated `underline` with nested emphasis and
  generated `strikeout` text, asserts exact generated XHTML and XML-valid spine
  output, round-trips through `EpubReader` into `underline` and `strikeout` AST
  nodes, records text-only `u` and `del` semantic spine metadata, and avoids
  malformed-spine diagnostics. Existing Markdown HTML reader decoration handling
  remains green.
- Verification: `php -l` passed for `EpubReader.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  10,102 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,494 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,861 assertions, 0 failures with pass-count verification `pass=1123 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 reader
  semantic metadata coverage for underline and plain strikeout inlines, but it
  does not claim CSS/browser rendering behavior validation, full XHTML5 element
  coverage, EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Sup/Sub Inline Reader Metadata Slice

- Focused PHP coverage is now 1,122 behavior tests and 20,848 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub3` reader metadata coverage for native superscript and subscript
  inline elements already emitted by the generated writer path.
- `HtmlWriter` already renders `superscript` and `subscript` AST nodes as native
  XHTML `<sup>` and `<sub>` in generated EPUB spine output. `EpubReader` now
  recognizes `sup` and `sub` in spine `semanticElements`, and keeps text-only
  records for both tags so generated script output round-trips element identity
  and text.
- Focused EPUB3 coverage writes generated `subscript` and `superscript` inlines,
  asserts exact generated XHTML and XML-valid spine output, round-trips through
  `EpubReader` into `subscript` and `superscript` AST nodes, records text-only
  `sub` and `sup` semantic spine metadata, and avoids malformed-spine
  diagnostics. Existing Markdown HTML reader script handling remains green.
- Verification: `php -l` passed for `EpubReader.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  10,089 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,481 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,848 assertions, 0 failures with pass-count verification `pass=1122 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 reader
  semantic metadata coverage for superscript and subscript inlines, but it does
  not claim full XHTML5 element coverage, browser rendering behavior validation,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Q-Tag Writer Option Metadata Slice

- Focused PHP coverage is now 1,121 behavior tests and 20,837 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub3` writer and reader metadata coverage for opt-in HTML quote tag
  output.
- `EpubWriter` now passes the `htmlQTags` writer option through to its internal
  `HtmlWriter` while still forcing EPUB spine math output to MathML. Default
  EPUB quote output remains unchanged unless callers opt in to q-tag output.
- `EpubReader` now recognizes `q` in spine `semanticElements`, records `cite`
  attributes on generated XHTML quotes, and keeps text-only `q` records so bare
  generated quote tags round-trip element identity and text.
- Focused EPUB3 coverage writes an opt-in `htmlQTags` package with a cited
  quote and a bare quote; asserts exact generated XHTML `<q>` output, XML-valid
  spine output, no fallback cite span output, no literal curly quote wrappers in
  the generated q-tag XHTML, and no malformed-spine diagnostics; and round-trips
  through `EpubReader` to quoted AST nodes plus semantic spine metadata for
  cited and bare `q` elements. The coverage records the real HTML round-trip
  boundary: standalone `<q>` reconstructs quote kind from nesting depth, not from
  the source AST's single/double quote kind.
- Verification: `php -l` passed for `EpubWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,078 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,470 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,837 assertions, 0 failures with
  pass-count verification `pass=1121 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer and
  reader metadata coverage for opt-in HTML q-tag quote output, but it does not
  claim default q-tag output, preservation of source single/double quote kind
  through standalone HTML `<q>`, full XHTML5 element coverage, browser quote
  rendering validation, EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Small Inline Writer Metadata Slice

- Focused PHP coverage is now 1,120 behavior tests and 20,815 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub3` writer and reader metadata coverage for small-print inline
  structures.
- `HtmlWriter` now renders span class `small` as native XHTML `<small>` through
  the established span-like lowering path, preserving retained classes and
  structured data/title metadata on generated EPUB3 output.
- `EpubReader` now recognizes `small` in spine `semanticElements`, and keeps
  text-only `small` records so bare generated small-print output round-trips
  element identity and text instead of being dropped before text is attached.
- Focused EPUB3 coverage writes an attributed `small` with nested strong text
  and a bare text-only `small`; asserts exact generated XHTML, XML-valid spine
  output, no fallback span serialization for generated `small`, no duplicate
  `source` attributes, and no malformed-spine diagnostics; and round-trips
  through `EpubReader` semantic spine metadata for classes, title, data
  attributes, text, and bare text-only `small`. Existing Markdown HTML reader
  coverage for imported `<small>` still maps source HTML to span class `small`.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,056 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,448 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,815 assertions, 0 failures with
  pass-count verification `pass=1120 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer and
  reader metadata coverage for small-print inline elements, but it does not
  claim full XHTML5 element coverage, browser rendering behavior validation,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Cite/WBR Inline Writer Metadata Slice

- Focused PHP coverage is now 1,119 behavior tests and 20,802 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub3` writer and reader metadata coverage for citation-title and
  word-break inline structures.
- `HtmlWriter` now renders span class `cite` as native XHTML `<cite>` through
  the established span-like lowering path, and renders structured span class
  `wbr` as native XHTML `<wbr />` only when data, title, or ARIA metadata proves
  word-break intent. Ordinary CSS-only classes named `wbr` remain fallback span
  output.
- `EpubReader` now recognizes `cite` and `wbr` in spine `semanticElements`, and
  keeps text-only `cite` records so bare generated citation-title output
  round-trips element identity and text instead of being dropped before text is
  attached.
- Focused EPUB3 coverage writes an attributed `cite` with nested emphasis, a
  bare text-only `cite`, a metadata-proven `wbr`, and a CSS-only `wbr` fallback
  span; asserts exact generated XHTML, XML-valid spine output, no fallback span
  serialization for native `cite`/`wbr` elements, no duplicate `source`
  attributes, and no malformed-spine diagnostics; and round-trips through
  `EpubReader` semantic spine metadata for attributed `cite`, bare `cite`,
  `wbr`, and the fallback span. Existing Markdown HTML reader raw fallback
  coverage for imported `<cite>`/`<wbr>` remains green.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,043 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,435 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,802 assertions, 0 failures with
  pass-count verification `pass=1119 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer and
  reader metadata coverage for citation-title and metadata-proven word-break
  inline elements, but it does not claim full XHTML5 element coverage, browser
  line-breaking behavior validation, EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Object Param Void Inline Writer Metadata Slice

- Focused PHP coverage is now 1,118 behavior tests and 20,780 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub3` writer and reader metadata coverage for object parameter
  structures.
- `HtmlWriter` now renders structured span class `param` as native XHTML
  `<param />` when `name` or `value` metadata proves object-parameter intent.
  Ordinary CSS-only classes named `param` remain fallback span output.
- `EpubReader` now recognizes `param` in spine `semanticElements`, preserving
  element identity, retained classes, `name`, `value`, and data attributes
  through generated EPUB3 round trips.
- Focused EPUB3 coverage strengthens the generated `object` round-trip to write
  a native child `param` inside native `object` output, preserve a CSS-only
  `param` fallback span, package and extract the object data resource, assert
  exact generated XHTML and XML-valid spine output, avoid malformed/missing
  resource diagnostics, avoid fallback span serialization for semantically-proven
  object/param elements, and avoid duplicate `source` attributes.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,021 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,413 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,780 assertions, 0 failures with
  pass-count verification `pass=1118 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer and
  reader metadata coverage for semantically-proven object parameters, but it does
  not claim full XHTML5 element coverage, plugin/runtime behavior validation,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Map/Area Image Map Writer Metadata Slice

- Focused PHP coverage is now 1,118 behavior tests and 20,771 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub3` writer and reader metadata coverage for HTML image-map structures.
- `HtmlWriter` now renders structured span class `map` as native XHTML `<map>`
  when `name` metadata or a proving `area` child indicates image-map intent, and
  renders span class `area` as native self-closing XHTML `<area />` when `href`,
  `alt`, `coords`, `shape`, `target`, `download`, or `rel` proves image-map area
  intent. Ordinary CSS-only classes named `map` or `area` remain fallback span
  output.
- `EpubReader` now recognizes `map` and `area` in spine `semanticElements`, and
  records image `usemap`, map `name`, area `href`, `alt`, `coords`, `shape`,
  link-ish attributes, data attributes, and referenced resource metadata.
- Focused EPUB3 coverage writes a generated image with `usemap`, a native
  `map` containing a native `area`, a CSS-only `area` fallback span, and a
  referenced package resource; asserts exact generated XHTML, XML-valid spine
  output, packaged resource payload, no malformed/missing-resource diagnostics,
  no fallback span serialization for semantically-proven image-map elements, and
  no duplicate `source` attributes; and round-trips through `EpubReader`
  semantic spine metadata for the image, map, area, and fallback span.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 10,012 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,404 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,771 assertions, 0 failures with
  pass-count verification `pass=1118 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer and
  reader metadata coverage for semantically-proven image maps, but it does not
  claim full XHTML5 element coverage, image-map browser behavior validation,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Samp/Var Code Role Reader Metadata Slice

- Focused PHP coverage is now 1,117 behavior tests and 20,738 full-suite
  assertions across the Pandoc lane. This implementation slice extends bounded
  PHP `epub3` reader metadata for native HTML5 code-role phrase elements already
  emitted by `HtmlWriter`.
- `EpubReader` now recognizes `samp` and `var` in spine `semanticElements` and
  keeps text-only records for those two elements, so generated bare
  `sample`/`variable` code-role output round-trips through EPUB read-back
  metadata instead of being dropped before text is attached.
- `HtmlWriter` already emitted bare `code` inlines with class `sample` or
  `variable` as native `<samp>` and `<var>` through its established code-role
  path; this slice closes the reader metadata half without changing that writer
  behavior.
- Focused EPUB3 coverage writes generated native `samp` and `var` inline tags
  from code-role AST nodes; asserts exact generated XHTML, XML-valid spine
  output, and no fallback classed `code` serialization for those bare roles; and
  round-trips through `EpubReader` semantic spine metadata for element identity
  and text.
- Verification: `php -l` passed for `EpubReader.php`, `HtmlWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,979 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,371 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,738 assertions, 0 failures with
  pass-count verification `pass=1117 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 reader
  metadata coverage for native `samp`/`var` code-role output, but it does not
  claim full XHTML5 element coverage, browser behavior validation, EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 Kbd/Mark/Dfn/Abbr Spanlike Inline Reader Metadata Slice

- Focused PHP coverage is now 1,116 behavior tests and 20,729 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` reader's structured inline metadata for native HTML5
  phrase elements already emitted by `HtmlWriter`.
- `EpubReader` now recognizes `kbd`, `mark`, `dfn`, and `abbr` in spine
  `semanticElements`, preserving element identity, IDs, retained classes, title
  metadata, text, and data attributes through generated EPUB3 round trips.
- `HtmlWriter` already emitted these elements via the established span-like
  lowering path; this slice closes the reader metadata half without changing
  that writer behavior.
- Focused EPUB3 coverage writes generated native `kbd`, `mark`, `dfn`, and
  nested `abbr` inline tags; asserts exact generated XHTML, XML-valid spine
  output, no fallback span serialization for those established span-like tags,
  and no duplicate `source` attributes; and round-trips through `EpubReader`
  semantic spine metadata for each element and its classes, title, text, and
  data attributes.
- Verification: `php -l` passed for `EpubReader.php`, `HtmlWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,970 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,362 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,729 assertions, 0 failures with
  pass-count verification `pass=1116 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 reader
  metadata coverage for established phrase-level span-like elements, but it does
  not claim full XHTML5 element coverage, browser behavior validation, EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 BDO Spanlike Inline Writer Metadata Slice

- Focused PHP coverage is now 1,115 behavior tests and 20,699 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for HTML5 `bdo`
  bidirectional override metadata.
- `HtmlWriter` now renders structured span class `bdo` as native XHTML `<bdo>`
  only when `dir="ltr"` or `dir="rtl"` proves bidirectional override intent.
  Ordinary CSS-only classes named `bdo` remain fallback span output, and plain
  span `dir` metadata remains unchanged.
- `EpubReader` now recognizes `bdo` in spine `semanticElements`, preserving
  element identity, `id`, retained classes, valid direction, text, and data
  attributes.
- Focused EPUB3 coverage writes a generated `bdo` inline with retained class,
  `dir="rtl"`, nested strong text, data metadata, and a CSS-only `bdo` span;
  asserts exact generated XHTML, XML-valid spine output, no fallback span
  serialization for semantically-proven `bdo`, no duplicate `source`
  attributes, and CSS-only fallback span output; and round-trips through
  `EpubReader` semantic spine metadata for `bdo` and fallback span text,
  classes, direction, and data attributes.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,940 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,332 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,699 assertions, 0 failures with
  pass-count verification `pass=1115 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer and
  reader metadata coverage for semantically-proven `bdo` direction overrides,
  but it does not claim full bidirectional text rendering validation, browser
  behavior validation, EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, full
  XHTML5 element coverage, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 Ins/Del Spanlike Inline Writer Metadata Slice

- Focused PHP coverage is now 1,114 behavior tests and 20,686 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for HTML5 revision
  metadata.
- `HtmlWriter` now renders structured span class `ins` or `del` as native XHTML
  `<ins>` or `<del>` only when `cite` or `datetime` attributes prove revision
  intent. Ordinary CSS-only classes named `ins` or `del` remain fallback span
  output, and existing native strikeout output remains unchanged.
- `EpubReader` now records `ins` and `del` in spine `semanticElements`,
  including `cite` and `datetime` attributes alongside retained classes, text,
  and data attributes.
- Focused EPUB3 coverage writes generated insertion and deletion inlines with
  retained classes, `cite`/`datetime`/data metadata, fallback text, and CSS-only
  `ins`/`del` spans; asserts exact generated XHTML, XML-valid spine output, no
  fallback span serialization for semantically-proven revision elements, no
  duplicate `source` attributes, and CSS-only fallback span output; and
  round-trips through `EpubReader` semantic spine metadata for `ins`, `del`, and
  fallback span text, classes, citation/date metadata, and data attributes.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,927 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,319 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,686 assertions, 0 failures with
  pass-count verification `pass=1114 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer and
  reader metadata coverage for semantically-proven revision elements, but it
  does not claim full change-tracking semantics, DOCX revision acceptance or
  rejection behavior in EPUB output, browser behavior validation, EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Progress Spanlike Inline Writer Metadata Slice

- Focused PHP coverage is now 1,113 behavior tests and 20,656 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for HTML5 `progress`
  metadata.
- `HtmlWriter` now renders structured span class `progress` as native XHTML
  `<progress>` only when `value` or `max` attributes prove element intent.
  Ordinary CSS-only classes named `progress` remain fallback span output.
- `EpubReader` now records `progress` in spine `semanticElements`, including
  `value` and `max` attributes alongside retained classes, text, and data
  attributes.
- Focused EPUB3 coverage writes a generated progress inline with retained class,
  `value`/`max`/data metadata, fallback text, and a CSS-only progress span;
  asserts exact generated XHTML, XML-valid spine output, no fallback span
  serialization for semantically-proven progress, no duplicate `source`
  attributes, and CSS-only fallback span output; and round-trips through
  `EpubReader` semantic spine metadata for progress and fallback span text,
  classes, value/max metadata, and data attributes.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,897 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,289 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,656 assertions, 0 failures with
  pass-count verification `pass=1113 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer and
  reader metadata coverage for semantically-proven `progress` elements, but it
  does not claim full XHTML5 element coverage, browser behavior validation,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Switch Case/Default Block Writer Metadata Slice

- Focused PHP coverage is now 1,112 behavior tests and 20,642 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured block output for EPUB3 `switch`,
  `case`, and `default` branch metadata.
- `HtmlWriter` now renders structured div class `switch` as native XHTML
  `<epub:switch>` when a direct child case with `required-namespace` or
  `required-modules` proves element intent. Direct `case` and `default` child
  divs are promoted only inside that native switch path; case attributes are
  normalized to `required-namespace` and `required-modules`. Ordinary CSS-only
  classes named `switch`, `case`, or `default` remain fallback div output.
- `EpubWriter` now detects structured AST switches for OPF `switch` manifest
  property inference, complementing existing raw XHTML switch detection.
- Focused EPUB3 coverage writes a generated switch with unsupported case,
  supported XHTML case, default branch, and a packaged local resource referenced
  from the selected branch; asserts exact generated XHTML, XML-valid spine
  output, `switch` manifest property inference, no fallback div serialization for
  the semantically-proven switch, no duplicate `source` attributes, CSS-only
  fallback div output, packaged resource output, and read-back selection of only
  the supported branch after `EpubReader` switch normalization.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,883 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,275 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,642 assertions, 0 failures with
  pass-count verification `pass=1112 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for semantically-proven `epub:switch` branch metadata and manifest
  property inference, but it does not claim arbitrary switch feature execution,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM decryption,
  XML signature cryptographic validation, full XHTML5 element coverage, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Trigger Inline Writer Metadata Slice

- Focused PHP coverage is now 1,111 behavior tests and 20,622 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured void inline output for EPUB3
  `epub:trigger` metadata.
- `HtmlWriter` now renders structured span class `trigger` as native
  self-closing XHTML `<epub:trigger />` when trigger-specific metadata proves
  element intent. `observer` and `event` are serialized as XML Events
  `ev:observer` and `ev:event` attributes, while `action`, `ref`, retained
  classes, and data attributes remain round-trippable. Ordinary CSS-only
  classes named `trigger` remain fallback span output.
- `EpubWriter` now declares `xmlns:ev` on generated spine roots only when the
  rendered chapter body contains XML Events attributes, and treats structured or
  raw trigger content as `scripted` for OPF manifest property inference.
- Focused EPUB3 coverage writes a trigger bound to a generated button; asserts
  exact generated XHTML, XML-valid spine output, `xmlns:ev` root output,
  `scripted` manifest property inference, no fallback span serialization for the
  semantically-proven trigger, no duplicate `source` attributes, and CSS-only
  fallback span output for unrelated `trigger` classes; and round-trips through
  `EpubReader` semantic spine metadata for trigger, retained class, observer,
  event, action, ref, data attributes, button metadata, and fallback span text.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,863 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,255 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,622 assertions, 0 failures with
  pass-count verification `pass=1111 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for semantically-proven `epub:trigger` metadata and XML Events
  namespace emission, but it does not claim JavaScript execution, EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Details Summary Block Tags Writer Metadata Slice

- Focused PHP coverage is now 1,110 behavior tests and 20,598 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured block output for HTML5 `details` and
  contextual `summary` blocks.
- `HtmlWriter` now renders structured div class `details` as native XHTML
  `<details>` when the block carries `open`, semantic attributes, or a direct
  summary child that proves element intent. Direct div children with class
  `summary` are promoted only inside that native details path. Ordinary
  CSS-only classes named `details` or `summary` remain fallback div output.
- The existing `EpubReader` semantic spine metadata path already records
  `details`, `summary`, and the details `open` state. This slice closes the
  generated-writer half for retained classes, `epub:type`, ARIA labels, text,
  open state, and data attributes.
- Focused EPUB3 coverage writes a structured open details block with contextual
  summary and body paragraph; asserts exact generated XHTML, XML-valid spine
  output, no fallback div serialization for semantically-proven details/summary,
  no duplicate `source` attributes, and CSS-only fallback div output for
  unrelated `details`/`summary` classes; and round-trips through `EpubReader`
  semantic spine metadata for details, summary, retained classes, `epub:type`,
  ARIA label, open state, text, and data attributes. Existing raw HTML details
  passthrough coverage remains green.
- Verification: `php -l` passed for `HtmlWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1
  file, 9,839 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,231 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,598 assertions, 0 failures with pass-count verification `pass=1110
  fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for structured `details`/contextual-`summary` block metadata, but it
  does not claim arbitrary imported details body reconstruction beyond existing
  raw HTML preservation, full HTML5 tree-construction parity, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Semantic Block Tags Writer Metadata Slice

- Focused PHP coverage is now 1,109 behavior tests and 20,572 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured block output for semantic XHTML5
  `article`, `aside`, `nav`, `main`, `header`, and `footer` blocks.
- `HtmlWriter` now renders structured div classes for those semantic block tags
  as native XHTML block elements only when semantic attributes prove element
  intent. Proving metadata includes `role`, `epub:type`, language/direction,
  title/hidden, ARIA attributes, or non-`data-source` data attributes, so
  ordinary CSS-only classes named `article`, `aside`, `nav`, `main`, `header`,
  or `footer` remain fallback div output.
- The existing `EpubReader` semantic spine metadata path already recognizes
  these native tags. This slice closes the generated-writer half so retained
  classes, EPUB/ARIA roles, text, and data attributes can round-trip through
  generated spine XHTML instead of collapsing to generic divs.
- Focused EPUB3 coverage writes a native article containing header, main,
  aside, nav, and footer children; asserts exact generated XHTML, XML-valid
  spine output, no fallback div serialization for semantically-proven blocks,
  no duplicate `source` attributes, and CSS-only fallback div output for all six
  class names; and round-trips through `EpubReader` semantic spine metadata for
  element names, retained classes, roles, `epub:type`, ARIA labels, text, and
  data attributes.
- Verification: `php -l` passed for `HtmlWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1
  file, 9,813 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 14,205 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,572 assertions, 0 failures with pass-count verification `pass=1109
  fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for semantically-proven `article`/`aside`/`nav`/`main`/`header`/
  `footer` block metadata, but it does not claim arbitrary imported native
  block reconstruction without semantic markers, full HTML5 tree-construction
  parity, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation,
  DRM decryption, XML signature cryptographic validation, full XHTML5 element
  coverage, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-24 EPUB3 Form Fieldset Legend Block Tags Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,100 behavior tests and 20,439 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured block output for reader-recorded
  HTML5 `form`, `fieldset`, and contextual `legend` metadata.
- `HtmlWriter` now renders structured div class `form` and `fieldset` as native
  XHTML block tags only when semantic form/fieldset attributes prove element
  intent. `legend` is promoted only as a direct rendered child inside a native
  fieldset path. Ordinary CSS classes named `form`, `fieldset`, or `legend`
  remain fallback div output when the proving metadata is absent.
- `EpubReader` now records `fieldset` `name` and `form` attributes alongside
  the existing disabled-state metadata, preserving generated fieldset linkage
  across read-back.
- Focused EPUB3 coverage writes a structured form containing a disabled
  fieldset, contextual legend, and structured input; asserts exact generated
  XHTML, remote-action `remote-resources` manifest inference, XML-valid spine
  output, no fallback div serialization for native blocks, no duplicate
  `source` attributes, and CSS-only fallback div output for unrelated
  `form`/`fieldset`/`legend` classes; and round-trips through `EpubReader`
  semantic spine metadata for form, fieldset, legend, input, retained classes,
  attributes, booleans, text, and data attributes.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,739 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,085 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,439 assertions, 0 failures with
  pass-count verification `pass=1100 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for structured `form`/`fieldset`/contextual-`legend` block metadata,
  plus reader metadata for fieldset `name`/`form`, but it does not claim
  arbitrary imported form-control body reconstruction, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Optgroup Spanlike Inline Tag Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,099 behavior tests and 20,398 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for reader-recorded
  HTML5 `optgroup` metadata inside generated select controls.
- `HtmlWriter` now renders structured span class `optgroup` through the
  contextual form-control path when label-backed optgroup metadata proves
  element intent, and structured select spans can be promoted by nested
  option or optgroup children. Ordinary CSS classes named `optgroup` remain
  fallback spans when no optgroup metadata is present.
- `EpubReader` now records `disabled` boolean state for semantic `optgroup`
  elements, matching the existing label metadata path and preserving generated
  optgroup state across read-back.
- Focused EPUB3 coverage writes a structured inline select whose native output
  is driven by a nested labeled optgroup containing a selected option, with
  retained classes, disabled state, fallback option text, and `data-source`
  metadata; asserts exact generated XHTML; rejects fallback span serialization
  and duplicate `source` attributes; confirms a plain CSS `optgroup` class
  still emits as a span; parses generated spine XHTML as XML; and round-trips
  through `EpubReader` semantic spine metadata for select, optgroup, option,
  classes, label, disabled/selected state, text, and data attributes.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,698 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,044 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,398 assertions, 0 failures with
  pass-count verification `pass=1099 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for label-backed structured inline `optgroup` metadata inside
  select controls, plus reader metadata for optgroup disabled state, but it
  does not claim generated `form`/`fieldset` writer coverage, arbitrary
  imported form-control body reconstruction, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Textarea Spanlike Inline Tag Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,098 behavior tests and 20,373 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for reader-recorded
  HTML5 `textarea` form-control metadata.
- `HtmlWriter` now renders structured span class `textarea` through the
  contextual form-control path instead of widening the generic span-like class
  table. Native `<textarea>` output is emitted only when textarea form-control
  attributes prove element intent, so ordinary CSS classes named `textarea`
  remain fallback spans.
- Focused EPUB3 coverage writes a structured inline `textarea` with retained
  class, name/placeholder/rows/cols/required/wrap metadata, fallback text, and
  `data-source`; asserts exact generated XHTML; rejects fallback span
  serialization and duplicate `source` attributes; confirms a plain CSS
  `textarea` class still emits as a span; parses generated spine XHTML as XML;
  and round-trips through `EpubReader` semantic spine metadata for textarea
  classes, form attributes, boolean required state, text, and data attributes.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,673 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,019 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,373 assertions, 0 failures with
  pass-count verification `pass=1098 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for structured inline `textarea` form controls, but it does not
  claim generated `form`/`optgroup`/`fieldset` writer coverage, arbitrary
  imported form-control body reconstruction, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Select And Option Spanlike Inline Tags Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,097 behavior tests and 20,355 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for reader-recorded
  HTML5 `select` and `option` form-control metadata.
- `HtmlWriter` now renders structured span class `select` and `option` through
  a contextual form-control path instead of widening the generic span-like
  class table. Native `<select>`/`<option>` output is emitted only when
  form-control attributes or structured option children prove element intent,
  so ordinary CSS classes named `select` or `option` remain fallback spans.
- Focused EPUB3 coverage writes a structured inline `select` containing two
  structured `option` children with retained classes, name/form metadata,
  boolean required/selected/disabled state, values, fallback text, and
  `data-source`; asserts exact generated XHTML; rejects fallback span
  serialization and duplicate `source` attributes; confirms plain CSS
  `select` and `option` classes still emit as spans; parses generated spine
  XHTML as XML; and round-trips through `EpubReader` semantic spine metadata
  for select/option classes, form attributes, boolean states, values, text,
  and data attributes.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,655 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 14,001 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,355 assertions, 0 failures with
  pass-count verification `pass=1097 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for structured inline `select` and `option` form controls, but it
  does not claim generated `form`/`textarea`/`optgroup`/`fieldset` writer
  coverage, arbitrary imported form-control body reconstruction, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, full XHTML5 element
  coverage, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-24 EPUB3 Input Spanlike Void Inline Tag Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,096 behavior tests and 20,328 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for reader-recorded
  HTML5 `input` form-control metadata.
- `HtmlWriter` now treats structured span class `input` as a contextual void
  HTML element only when form-control attributes such as `type`, `name`,
  `value`, `placeholder`, `required`, `checked`, `disabled`, or related input
  attributes prove element intent. This keeps ordinary CSS classes named
  `input` as fallback `<span class="input ...">` output when no form-control
  attributes are present.
- Focused EPUB3 coverage writes a structured inline `input` with retained
  class, type/name/value/placeholder/autocomplete metadata, boolean required
  state, and `data-source`; asserts exact native self-closing XHTML; rejects
  fallback span serialization and duplicate `source` attributes; confirms a
  plain CSS class `input` span still emits as `<span>`; parses the generated
  spine XHTML as XML; and round-trips through `EpubReader` semantic spine
  metadata for input classes, plain attributes, required state, and data
  attributes.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,628 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,974 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,542 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,328 assertions, 0 failures with
  pass-count verification `pass=1096 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for structured inline `input` form controls, but it does not claim
  full form reconstruction, generated `form`/`select`/`option`/`textarea`
  writer coverage, arbitrary imported form-control body reconstruction, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, full XHTML5 element
  coverage, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-24 EPUB3 Local Iframe Spanlike Writer Packaged Resource Tracking Slice

- Focused PHP coverage is now 1,095 behavior tests and 20,311 full-suite
  assertions across the Pandoc lane. This coverage slice locks the generated
  EPUB3 writer contract for structured inline local `iframe` spans that point
  at packaged XHTML resources.
- Focused EPUB3 coverage writes an inline structured `iframe` with retained
  class/title/data attributes, fallback text, a packaged local XHTML frame, a
  nested XHTML iframe resource, and a PNG referenced from inside the frame.
  The test asserts exact generated XHTML with spine-relative local `src`,
  rejects fallback span serialization and duplicate `source` attributes,
  confirms OPF manifest entries for both XHTML iframe documents and the PNG,
  verifies ZIP payload bytes for the packaged local iframe resources, and
  parses the generated spine XHTML as XML.
- Read-back coverage now confirms generated local inline iframe EPUBs avoid
  malformed-spine and missing-resource diagnostics, track the local and nested
  iframe documents in `epubIframeResources`, track all local iframe-side
  resources in `epubReferencedResources`, preserve the PNG payload through
  `epubResourcePayloads`, keep inline fallback text in WordPress block output,
  and round-trip iframe semantic spine metadata for classes, `src`, title,
  `data-source`, and fallback text. The existing reader-side block-level local
  iframe test continues to cover importing iframe document bodies into nested
  `div.iframe` blocks.
- Verification: `php -l` passed for `EpubWriterTest.php`; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  9,611 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,957 assertions, 0 failures; and `php
  tools/run-tests.php lanes/pandoc/tests` passed 11 files, 20,311 assertions,
  0 failures with pass-count verification `pass=1095 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 writer
  coverage for local packaged inline iframe resources and their metadata
  tracking, but it does not claim inline iframe document-body expansion inside
  paragraphs, arbitrary imported iframe-to-AST reconstruction beyond existing
  block-level coverage, arbitrary embedded-resource serialization, scripted
  media behavior, full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, full
  XHTML5 element coverage, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 Picture Spanlike Inline Tag Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,094 behavior tests and 20,286 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for reader-recorded
  HTML5 `picture` wrappers.
- `HtmlWriter` now renders structured span class `picture` through a
  context-sensitive picture path instead of the generic span-like class path.
  The mapping only fires when the span contains picture-like children, such as
  a structured `source` child with `src`/`srcset` metadata or an image child,
  so ordinary CSS classes named `picture` remain fallback spans. The generated
  XHTML preserves retained classes, wrapper title/data attributes, nested
  self-closing `source` elements, and the fallback `img` element.
- `EpubReader` now records `picture` in spine `semanticElements`, allowing
  generated and imported XHTML picture wrappers to round-trip wrapper IDs,
  retained classes, title, data attributes, and child source/image resource
  metadata through the existing semantic spine metadata channel.
- Focused EPUB3 coverage writes a structured responsive `picture` inline with
  WebP `source srcset` candidates and PNG fallback `img srcset` candidates,
  all backed by local packaged resources. The test asserts exact generated
  XHTML with spine-relative URLs, rejects fallback span serialization and
  duplicate `source` attributes, confirms OPF manifest entries for WebP and PNG
  resources, parses the generated spine XHTML as XML, extracts packaged
  payloads on read-back, and round-trips through `EpubReader` semantic spine
  metadata for picture wrapper classes/title/htmlAttributes plus source and
  image srcset/resource metadata.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubReader.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 9,586 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,932 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,286 assertions, 0 failures with
  pass-count verification `pass=1094 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 XHTML
  writer coverage for structured `picture` wrappers with `source` and `img`
  children, but it does not claim arbitrary imported picture-to-AST
  reconstruction, local iframe resource import or packaging, arbitrary
  embedded-resource serialization, scripted media behavior, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Source Track And Embed Spanlike Void Media Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,093 behavior tests and 20,249 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for the reader-recorded
  HTML5 void media helper elements `source`, `track`, and `embed`.
- `HtmlWriter` now renders those three element aliases through a
  context-sensitive void-element path instead of the generic span-like class
  path: `source` requires a real `src` or `srcset` attribute, while `track` and
  `embed` require `src`. That preserves ordinary CSS classes named `source`
  as fallback `<span class="source">` output while still emitting native
  self-closing XHTML `<source />`, `<track />`, and `<embed />` for structured
  media metadata.
- Focused EPUB3 coverage writes a structured `video` inline containing
  self-closing `source` and `track` children plus a standalone `embed` inline,
  all backed by local packaged resources. The test asserts exact generated
  XHTML with spine-relative URLs, rejects fallback span serialization and
  duplicate `source` attributes, confirms OPF manifest entries for MP4, VTT,
  and binary resources, parses the generated spine XHTML as XML, extracts the
  packaged payloads on read-back, and round-trips through `EpubReader`
  semantic spine metadata for classes, media attributes, boolean track default
  state, dimensions, titles, `htmlAttributes`, referenced resources, and
  payload extraction.
- Regression coverage re-ran the Markdown reader/writer file that caught the
  generic `.source` class collision, confirming CSS class lowering still emits
  the expected `<span class="source">` output when no media-resource attribute
  is present.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,542 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 9,549 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,895 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,249 assertions, 0 failures with
  pass-count verification `pass=1093 fail=0`.
- Dependency-closure checkpoint: this closes bounded generated EPUB3 XHTML
  writer coverage for structured `source`, `track`, and `embed` void media
  helpers, but it does not claim full `picture` reconstruction, arbitrary
  embedded-resource serialization, local iframe resource import or packaging,
  scripted media behavior, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB graph validation, DRM decryption, XML signature cryptographic
  validation, full XHTML5 element coverage, or broader media/layout edge-case
  parity beyond existing focused cases.

## 2026-06-24 EPUB3 Object Spanlike Inline Tag Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,092 behavior tests and 20,202 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for the reader-recorded
  HTML5 `object` element.
- `HtmlWriter` now treats structured span class `object` as a span-like XHTML
  element alias alongside the existing keyboard, mark, definition,
  abbreviation, ruby-family, HTML5 phrasing, form-control, canvas, iframe, and
  audio/video mappings. Structured AST spans can now emit native `<object>`
  elements instead of fallback `<span>` wrappers while preserving retained
  classes, `data`, `type`, `name`, title, dimensions, data attributes, and
  fallback text.
- Focused EPUB3 coverage writes a structured `object` inline with a local
  packaged SVG resource. The test asserts exact generated XHTML with `data`
  rewritten relative to the spine document, rejects fallback span serialization
  and duplicate `source` attributes, confirms OPF manifest output for the SVG
  resource with `properties="svg"`, parses the generated spine XHTML as XML,
  extracts the packaged SVG payload on read-back, and round-trips through
  `EpubReader` semantic spine metadata for classes, object data, type, name,
  title, dimensions, `htmlAttributes`, referenced resources, and fallback text.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,502 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,848 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,202 assertions, 0 failures with pass-count verification `pass=1092
  fail=0`.
- Dependency-closure checkpoint: this improves generated EPUB3 XHTML semantic
  embedded-object coverage for `object`, but it does not claim structured
  void-element writer parity for `embed`/`source`/`track`, full `picture`
  reconstruction, local iframe resource import or packaging, arbitrary embedded
  resource serialization, scripted object behavior, full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 Audio And Video Spanlike Inline Tags Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,091 behavior tests and 20,182 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for the reader-recorded
  HTML5 `audio` and `video` elements.
- `HtmlWriter` now treats structured span classes `audio` and `video` as
  span-like XHTML element aliases alongside the existing keyboard, mark,
  definition, abbreviation, ruby-family, HTML5 phrasing, form-control, canvas,
  and iframe mappings. Structured AST spans can now emit native `<audio>` and
  `<video>` elements instead of fallback `<span>` wrappers while preserving
  retained classes, media source attributes, poster references, controls,
  dimensions, preload metadata, data attributes, and fallback text.
- Focused EPUB3 coverage writes structured `video` and `audio` inlines with
  local packaged media resources. The test asserts exact generated XHTML with
  resource URLs rewritten relative to the spine document, rejects fallback span
  serialization and duplicate `source` attributes, confirms OPF manifest
  entries for poster/video/audio resources, parses the generated spine XHTML as
  XML, extracts packaged payloads on read-back, and round-trips through
  `EpubReader` semantic spine metadata for classes, `src`, `poster`, title,
  controls, dimensions, preload, `htmlAttributes`, referenced resources, and
  fallback text.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,482 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,828 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,182 assertions, 0 failures with pass-count verification `pass=1091
  fail=0`.
- Dependency-closure checkpoint: this improves generated EPUB3 XHTML semantic
  media inline coverage for `audio` and `video`, but it does not claim
  structured `source`/`track`/`picture` writer parity, local iframe resource
  import or packaging, arbitrary embedded resource serialization, scripted
  media behavior, full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, full
  XHTML5 element coverage, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-25 EPUB3 Iframe Spanlike Inline Tag Writer Remote Resource Metadata Slice

- Focused PHP coverage is now 1,090 behavior tests and 20,144 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for the
  reader-recorded HTML5 `iframe` element and closes remote-resource manifest
  inference for structured XHTML attributes.
- `HtmlWriter` now treats structured span class `iframe` as a span-like XHTML
  element alias alongside the existing keyboard, mark, definition,
  abbreviation, ruby-family, HTML5 phrasing, form-control, and canvas mappings.
  Structured AST spans can now emit native `<iframe>` elements instead of
  fallback `<span>` wrappers while preserving retained classes, remote `src`,
  title, data attributes, and fallback text on the generated element.
- `EpubWriter` now checks structured AST `attributes` and `htmlAttributes` for
  remote-resource-bearing attributes such as `src`, `srcset`, `poster`,
  `data`, `action`, `formaction`, `background`, `textref`, and CSS `url(...)`
  references. Generated XHTML spine items now receive the EPUB3
  `remote-resources` manifest property when structured inline attributes refer
  to remote resources, matching the reader diagnostic path for generated OPF
  packages.
- Focused EPUB3 coverage writes a structured `iframe` inline with retained
  class, remote `src`, `title`, `data-source`, and fallback text. The test
  asserts exact generated XHTML, rejects fallback span serialization and
  duplicate `source` attributes, confirms the OPF item has
  `properties="remote-resources"`, parses the generated spine XHTML as XML, and
  round-trips the package through `EpubReader` semantic spine metadata for
  classes, source URL, title, `htmlAttributes`, manifest properties, and text.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,444 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,790 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,144 assertions, 0 failures with pass-count verification `pass=1090
  fail=0`.
- Dependency-closure checkpoint: this improves generated EPUB3 XHTML semantic
  inline coverage and structured remote-resource manifest inference, but it
  does not claim local iframe resource import or packaging, arbitrary embedded
  resource serialization, scripted iframe behavior, full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-25 EPUB3 Canvas Spanlike Inline Tag Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,089 behavior tests and 20,131 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for the reader-recorded
  HTML5 `canvas` element.
- `HtmlWriter` now treats structured span class `canvas` as a span-like XHTML
  element alias alongside the existing keyboard, mark, definition,
  abbreviation, ruby-family, HTML5 phrasing, and form-control mappings.
  Structured AST spans can now emit native `<canvas>` elements instead of
  fallback `<span>` wrappers while preserving retained classes and valid HTML
  attributes on the generated element.
- Focused EPUB3 coverage writes a structured `canvas` inline with retained
  class, `width`, `height`, `data-source`, and fallback text. The test asserts
  exact generated XHTML, rejects fallback span serialization and duplicate
  `source` attributes, parses the generated spine XHTML as XML, and round-trips
  the package through `EpubReader` semantic spine metadata for classes,
  dimensions, `htmlAttributes`, and text.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,431 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,777 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,131 assertions, 0 failures with pass-count verification `pass=1089
  fail=0`.
- Dependency-closure checkpoint: this improves generated EPUB3 XHTML semantic
  inline coverage for the selected HTML5 canvas element, but it does not claim
  scripted canvas behavior, embedded resource serialization, full form
  serialization, void input/select/textarea writer parity, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-25 EPUB3 Label And Button Spanlike Inline Tags Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,088 behavior tests and 20,121 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for reader-recognized
  HTML5 `label` and `button` elements that carry form-control metadata.
- `HtmlWriter` now treats structured span classes `label` and `button` as
  span-like XHTML element aliases alongside the existing keyboard, mark,
  definition, abbreviation, ruby-family, `bdi`, `data`, `meter`, `output`, and
  `time` mappings. Structured AST spans can now emit native `<label>` and
  `<button>` elements instead of fallback `<span>` wrappers while preserving
  retained classes and valid HTML attributes on the generated element.
- Focused EPUB3 coverage writes structured `label` and `button` inlines with
  retained classes, `for`, `form`, `type`, `name`, `value`, `formaction`,
  `formmethod`, `formenctype`, `formtarget`, `disabled`, and `data-source`
  attributes. The test asserts exact generated XHTML, rejects fallback span
  serialization and duplicate `source` attributes, parses the generated spine
  XHTML as XML, and round-trips the package through `EpubReader` semantic spine
  metadata for classes, label target/form bindings, button form metadata,
  boolean disabled state, `htmlAttributes`, and text.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,421 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,767 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,121 assertions, 0 failures with pass-count verification `pass=1088
  fail=0`.
- Dependency-closure checkpoint: this improves generated EPUB3 XHTML semantic
  inline coverage for selected HTML5 form-control phrasing elements, but it
  does not claim full form serialization, void input/select/textarea writer
  parity, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation,
  DRM decryption, XML signature cryptographic validation, full XHTML5 element
  coverage, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-25 EPUB3 Meter And Output Spanlike Inline Tags Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,087 behavior tests and 20,096 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for the remaining
  HTML5 `meter`/`output` inline elements already covered by raw XHTML import
  and reader semantic metadata.
- `HtmlWriter` now treats structured span classes `meter` and `output` as
  span-like XHTML element aliases alongside the existing keyboard, mark,
  definition, abbreviation, ruby-family, `bdi`, `data`, and `time` mappings.
  Structured AST spans can now emit native `<meter>` and `<output>` elements
  instead of fallback `<span>` wrappers while preserving retained classes and
  valid HTML attributes on the generated element.
- Focused EPUB3 coverage writes structured `meter` and `output` inlines with
  retained classes, `value`, `min`, `max`, `low`, `high`, `optimum`, `for`,
  `name`, and `data-source` attributes. The test asserts exact generated
  XHTML, rejects fallback span serialization and duplicate `source`
  attributes, parses the generated spine XHTML as XML, and round-trips the
  package through `EpubReader` semantic spine metadata for classes, range
  values, output bindings, `htmlAttributes`, and text.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,396 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,742 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,096 assertions, 0 failures with pass-count verification `pass=1087
  fail=0`.
- Dependency-closure checkpoint: this improves generated EPUB3 XHTML semantic
  inline coverage for selected HTML5 phrasing elements, but it does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, full XHTML5 element
  coverage, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-25 EPUB3 Bdi Data And Time Spanlike Inline Tags Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,086 behavior tests and 20,074 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for additional HTML5
  phrasing elements already recorded by the EPUB reader.
- `HtmlWriter` now treats structured span classes `bdi`, `data`, and `time` as
  span-like XHTML element aliases alongside the existing keyboard, mark,
  definition, abbreviation, and ruby-family mappings. Structured AST spans can
  now emit native `<bdi>`, `<data>`, and `<time>` elements instead of fallback
  `<span>` wrappers while preserving retained classes and valid HTML/XML
  attributes on the generated element.
- Focused EPUB3 coverage writes structured `bdi`, `data`, and `time` inlines
  with retained classes, `dir`, `xml:lang`, `value`, `datetime`, and
  `data-source` attributes. The test asserts exact generated XHTML, rejects
  fallback span serialization and duplicate `source` attributes, parses the
  generated spine XHTML as XML, and round-trips the package through
  `EpubReader` semantic spine metadata for classes, language/direction,
  `data` values, `time` datetimes, `htmlAttributes`, and text.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,374 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,720 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,074 assertions, 0 failures with pass-count verification `pass=1086
  fail=0`.
- Dependency-closure checkpoint: this improves generated EPUB3 XHTML semantic
  inline coverage for selected HTML5 phrasing elements, but it does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, full XHTML5 element
  coverage, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-25 EPUB3 Ruby Spanlike Inline Tags Writer Semantic Metadata Slice

- Focused PHP coverage is now 1,085 behavior tests and 20,049 full-suite
  assertions across the Pandoc lane. This implementation slice extends the
  bounded PHP `epub3` writer's structured inline output for EPUB3 ruby
  annotation markup.
- `HtmlWriter` now treats structured span classes `ruby`, `rt`, and `rp` as
  span-like XHTML element aliases alongside the existing `kbd`, `mark`, `dfn`,
  and `abbr` mappings. Structured AST spans can now emit native `<ruby>`,
  `<rt>`, and `<rp>` elements instead of fallback `<span>` wrappers while
  preserving retained classes and valid HTML/XML attributes on the outer
  generated element.
- Focused EPUB3 coverage writes a ruby annotation with a retained class,
  `xml:lang`, `data-source`, `rt` language metadata, and `rp` fallback
  parentheses. The test asserts exact generated XHTML, rejects fallback span
  serialization and duplicate `source` attributes, parses the generated spine
  XHTML as XML, and round-trips the package through `EpubReader` semantic
  spine metadata for `ruby`, `rt`, and `rp` ids, classes, languages,
  `htmlAttributes`, and text.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriter.php`, and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,349 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,695 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,049 assertions, 0 failures with pass-count verification `pass=1085
  fail=0`.
- Dependency-closure checkpoint: this improves generated EPUB3 XHTML semantic
  inline coverage for ruby annotations, but it does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, full XHTML5 element coverage, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-25 EPUB3 TeX Remaining Unicode Math Layout Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,084 behavior tests and 20,034 full-suite
  assertions across the Pandoc lane. This implementation slice closes the
  remaining local upstream `unicode-math` command-table accounting for the
  bounded PHP `epub3` TeX-to-MathML writer path.
- `HtmlWriter` now maps the remaining parser-aware accent aliases `\ovhook`,
  `\candra`, `\oturnedcomma`, `\ocommatopright`, `\droang`,
  `\vertoverlay`, `\annuity`, and `\widebridgeabove` through native MathML
  `<mover accent="true">` output; maps `\notaccent` through a diagonal-strike
  `<menclose>`; and generalizes the over/under layout handler to cover
  `\overbracket`, `\underbracket`, `\overparen`, and `\underparen` alongside
  the already covered `\overbrace` and `\underbrace`. Existing parser-aware
  `\sqrt` coverage is included in the structural accounting.
- Structural upstream coverage confirmed the selected remaining layout alias
  set at 16/16 accounted for. The broader local `unicode-math` command key scan
  now reports 2,448/2,448 commands mapped with 0 remaining unmapped commands
  under the current literal-plus-semantic scan convention.
- Focused EPUB3 coverage writes the remaining accent, overlay, and over/under
  aliases across inline and display equations, asserts exact MathML mover,
  `menclose`, `mover`, and `munder` serialization, confirms command names do
  not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 16/16 selected
  remaining layout commands accounted for and 2,448/2,448 total upstream
  commands mapped; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 9,334 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,680 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  20,034 assertions, 0 failures with pass-count verification `pass=1084
  fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` parser-aware layout aliases
  and completes the local command-table scan, but it does not claim full TeX
  parser parity, full TeX math class/spacing layout parity, visual equivalence
  for every reader's rendering of obscure combining accents, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-25 EPUB3 TeX Enclosing Technical Delimiter Piece And Arabic Operator Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,083 behavior tests and 20,009 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 59 previously unmapped upstream
  `unicode-math` enclosing, technical, delimiter-piece, APL, and Arabic
  operator aliases.
- `HtmlWriter` now maps `\enclosecircle`, `\enclosesquare`,
  `\enclosediamond`, `\enclosetriangle`, `\ulcorner`, `\urcorner`,
  `\llcorner`, `\lrcorner`, `\inttop`, `\intbottom`,
  `\varhexagonlrbonds`, `\conictaper`, `\topbot`, `\APLnotslash`,
  `\APLnotbackslash`, `\APLboxupcaret`, `\APLboxquestion`,
  `\rangledownzigzagarrow`, `\hexagon`, delimiter construction pieces from
  `\lparenuend` through `\rvboxline`, `\varcarriagereturn`, `\obrbrak`,
  `\ubrbrak`, `\trapezium`, `\benzenr`, `\strns`, `\fltns`, `\accurrent`,
  `\elinters`, `\arabicmaj`, and `\arabichad` to native MathML operator
  output.
- Structural upstream coverage confirmed the selected enclosing/technical/
  delimiter-piece/APL/Arabic alias set at 59/59 mapped. The broader local
  `unicode-math` command key scan now reports 2,432/2,448 commands mapped,
  leaving 16 unmapped commands for later parser-aware layout slices: `\sqrt`,
  eight math accents, one overlay accent, three over constructs, and three
  under constructs.
- Focused EPUB3 coverage writes all 59 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 59/59 selected
  commands mapped and 2,432/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  9,309 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,655 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 20,009 assertions, 0 failures with
  pass-count verification `pass=1083 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` enclosing, technical,
  delimiter-piece, APL, and Arabic operator aliases without claiming
  parser-aware `\sqrt` layout parity, combining accent/enclosure layout
  parity, over/under bracket/parenthesis/brace layout parity, full TeX symbol
  table parity, full TeX math class/spacing layout parity, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-25 EPUB3 TeX Bag Relation Diamond Bracket And Gapped Arrow Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,082 behavior tests and 19,938 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 34 previously unmapped upstream
  `unicode-math` bag delimiter, subset/superset relation, division, diamond,
  tack, bracket, group, and gapped-arrow aliases from U+27C5 through U+27F4.
- `HtmlWriter` now maps `\lbag`, `\rbag`, `\bsolhsub`, `\suphsol`,
  `\longdivision`, `\diamondcdot`, `\upin`, `\pullback`, `\pushout`,
  `\DashVDash`, `\dashVdash`, `\multimapinv`, `\vlongdash`,
  `\longdashv`, `\cirbot`, `\lozengeminus`, `\concavediamond`,
  `\concavediamondtickleft`, `\concavediamondtickright`,
  `\whitesquaretickleft`, `\whitesquaretickright`, `\lBrack`, `\rBrack`,
  `\lAngle`, `\rAngle`, `\Lbrbrak`, `\Rbrbrak`, `\lgroup`, `\rgroup`,
  `\UUparrow`, `\DDownarrow`, `\acwgapcirclearrow`, `\cwgapcirclearrow`,
  and `\rightarrowonoplus` to native MathML operator output.
- Structural upstream coverage confirmed the selected bag/relation/diamond/
  bracket/gapped-arrow alias set at 34/34 mapped. The broader local
  `unicode-math` command key scan now reports 2,373/2,448 commands mapped,
  leaving 75 unmapped commands for later slices.
- Focused EPUB3 coverage writes all 34 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 34/34 selected
  commands mapped and 2,373/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  9,238 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,584 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,938 assertions, 0 failures with
  pass-count verification `pass=1082 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` bag delimiter,
  subset/superset relation, division, diamond, tack, bracket, group, and
  gapped-arrow aliases without claiming full TeX symbol table parity, full TeX
  math class/spacing layout parity, delimiter construction piece parity,
  combining accent/enclosure layout parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Check Ornament Bracket Arrow And Subset Symbol Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,081 behavior tests and 19,892 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 12 previously unmapped upstream
  `unicode-math` check, ornament, bracket, drafting-arrow, angle, triangle, and
  subset/superset aliases from U+2713 through U+27C4, with `\perp` already
  covered between the selected rows.
- `HtmlWriter` now maps `\checkmark`, `\maltese`, `\circledstar`,
  `\varstar`, `\dingasterisk`, `\lbrbrak`, `\rbrbrak`, `\draftingarrow`,
  `\threedangle`, `\whiteinwhitetriangle`, `\subsetcirc`, and `\supsetcirc`
  to native MathML operator output.
- Structural upstream coverage confirmed the selected check/ornament/bracket/
  arrow/subset alias set at 12/12 mapped. The broader local `unicode-math`
  command key scan now reports 2,339/2,448 commands mapped, leaving 109
  unmapped commands for later slices.
- Focused EPUB3 coverage writes all 12 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 12/12 selected
  commands mapped and 2,339/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  9,192 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,538 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,892 assertions, 0 failures with
  pass-count verification `pass=1081 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` check, ornament, bracket,
  drafting-arrow, angle, triangle, and subset/superset aliases without claiming
  full TeX symbol table parity, full TeX math class/spacing layout parity,
  delimiter construction piece parity, combining accent/enclosure layout parity,
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Star Sun Moon Card Music Dice And Gender Symbol Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,080 behavior tests and 19,868 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 32 previously unmapped upstream
  `unicode-math` open-star, sun, moon, gender, card-suit, music-note, dice,
  circled-dot, and circle aliases from U+2606 through U+26B2.
- `HtmlWriter` now maps `\bigwhitestar`, `\astrosun`, `\danger`,
  `\blacksmiley`, `\sun`, `\rightmoon`, `\leftmoon`, `\female`, `\male`,
  `\varspadesuit`, `\varheartsuit`, `\vardiamondsuit`, `\varclubsuit`,
  `\quarternote`, `\eighthnote`, `\twonotes`, `\acidfree`, `\dicei`,
  `\diceii`, `\diceiii`, `\diceiv`, `\dicev`, `\dicevi`,
  `\circledrightdot`, `\circledtwodots`, `\blackcircledrightdot`,
  `\blackcircledtwodots`, `\Hermaphrodite`, `\mdwhtcircle`, `\mdblkcircle`,
  `\mdsmwhtcircle`, and `\neuter` to native MathML operator output.
- Structural upstream coverage confirmed the selected star/sun/moon/card/music/
  dice/gender alias set at 32/32 mapped. The broader local `unicode-math`
  command key scan now reports 2,327/2,448 commands mapped, leaving 121
  unmapped commands for later slices.
- Focused EPUB3 coverage writes all 32 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 32/32 selected
  commands mapped and 2,327/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  9,168 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,514 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,868 assertions, 0 failures with
  pass-count verification `pass=1080 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` star, sun, moon, card-suit,
  music-note, dice, gender, circled-dot, and circle aliases without claiming
  full TeX symbol table parity, full TeX math class/spacing layout parity,
  delimiter construction piece parity, combining accent/enclosure layout parity,
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Square Circle And Triangle Quadrant Symbol Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,079 behavior tests and 19,824 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 24 previously unmapped upstream
  `unicode-math` square, circle, and triangle quadrant aliases from U+25E7
  through U+25FF, with the intervening `\boxbar` already covered.
- `HtmlWriter` now maps `\squareleftblack`, `\squarerightblack`,
  `\squareulblack`, `\squarelrblack`, `\trianglecdot`,
  `\triangleleftblack`, `\trianglerightblack`, `\lgwhtcircle`,
  `\squareulquad`, `\squarellquad`, `\squarelrquad`, `\squareurquad`,
  `\circleulquad`, `\circlellquad`, `\circlelrquad`, `\circleurquad`,
  `\ultriangle`, `\urtriangle`, `\lltriangle`, `\mdwhtsquare`,
  `\mdblksquare`, `\mdsmwhtsquare`, `\mdsmblksquare`, and `\lrtriangle`
  to native MathML operator output.
- Structural upstream coverage confirmed the selected square/circle/triangle
  quadrant alias set at 24/24 mapped. The broader local `unicode-math` command
  key scan now reports 2,295/2,448 commands mapped, leaving 153 unmapped
  commands for later slices.
- Focused EPUB3 coverage writes all 24 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 24/24 selected
  commands mapped and 2,295/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  9,124 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,470 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,824 assertions, 0 failures with
  pass-count verification `pass=1079 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` square, circle, and triangle
  quadrant aliases without claiming full TeX symbol table parity, full TeX math
  class/spacing layout parity, delimiter construction piece parity, combining
  accent/enclosure layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Pointer Diamond And Circle Symbol Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,078 behavior tests and 19,788 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 40 upstream `unicode-math`
  pointer, diamond, lozenge, circle, arc, semicircle, and triangle aliases from
  U+25BA through U+25E6 that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\blackpointerright`, `\whitepointerright`,
  `\bigblacktriangledown`, `\smallblacktriangleleft`, `\smalltriangleleft`,
  `\blackpointerleft`, `\whitepointerleft`, `\mdlgblkdiamond`,
  `\mdlgwhtdiamond`, `\blackinwhitediamond`, `\fisheye`,
  `\mdlgwhtlozenge`, `\mdlgwhtcircle`, `\dottedcircle`,
  `\circlevertfill`, `\bullseye`, `\mdlgblkcircle`,
  `\circlelefthalfblack`, `\circlerighthalfblack`,
  `\circlebottomhalfblack`, `\circletophalfblack`, `\circleurquadblack`,
  `\blackcircleulquadwhite`, `\blacklefthalfcircle`,
  `\blackrighthalfcircle`, `\inversebullet`, `\inversewhitecircle`,
  `\invwhiteupperhalfcircle`, `\invwhitelowerhalfcircle`, `\ularc`,
  `\urarc`, `\lrarc`, `\llarc`, `\topsemicircle`, `\botsemicircle`,
  `\lrblacktriangle`, `\llblacktriangle`, `\ulblacktriangle`,
  `\urblacktriangle`, and `\smwhtcircle` to native MathML operator output.
- Structural upstream coverage confirmed the selected pointer/diamond/circle
  alias set at 40/40 mapped. The broader local `unicode-math` command key scan
  now reports 2,271/2,448 commands mapped, leaving 177 unmapped commands for
  later slices.
- Focused EPUB3 coverage writes all 40 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 40/40 selected
  commands mapped and 2,271/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  9,088 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,434 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,788 assertions, 0 failures with
  pass-count verification `pass=1078 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` pointer, diamond, lozenge,
  circle, arc, semicircle, and triangle aliases without claiming full TeX symbol
  table parity, full TeX math class/spacing layout parity, delimiter
  construction piece parity, combining accent/enclosure layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Block Square And Triangle Symbol Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,077 behavior tests and 19,736 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 32 upstream `unicode-math`
  visible-space, block, square, rectangle, parallelogram, and triangle aliases
  from U+2422 through U+25B9 that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\blanksymbol`, `\mathvisiblespace`,
  `\bdtriplevdash`, `\blockuphalf`, `\blocklowhalf`, `\blockfull`,
  `\blocklefthalf`, `\blockrighthalf`, `\blockqtrshaded`,
  `\blockhalfshaded`, `\blockthreeqtrshaded`, `\mdlgblksquare`,
  `\mdlgwhtsquare`, `\squoval`, `\blackinwhitesquare`, `\squarehfill`,
  `\squarevfill`, `\squarehvfill`, `\squarenwsefill`, `\squareneswfill`,
  `\squarecrossfill`, `\smblksquare`, `\smwhtsquare`, `\hrectangleblack`,
  `\hrectangle`, `\vrectangleblack`, `\vrectangle`, `\parallelogramblack`,
  `\parallelogram`, `\bigblacktriangleup`, `\smallblacktriangleright`, and
  `\smalltriangleright` to native MathML operator output.
- Structural upstream coverage confirmed the selected block/square/triangle
  alias set at 32/32 mapped. The broader local `unicode-math` command key scan
  now reports 2,231/2,448 commands mapped, leaving 217 unmapped commands for
  later slices.
- Focused EPUB3 coverage writes all 32 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 32/32 selected
  commands mapped and 2,231/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  9,036 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,382 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,736 assertions, 0 failures with
  pass-count verification `pass=1077 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` block, square, and triangle
  symbol aliases without claiming full TeX symbol table parity, full TeX math
  class/spacing layout parity, delimiter construction piece parity, combining
  accent/enclosure layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Low Codepoint Membership And Technical Symbol Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,076 behavior tests and 19,692 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 30 upstream `unicode-math`
  single-glyph membership, geometric, and technical aliases from U+22B9 through
  U+2319 that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\hermitmatrix`, `\measuredrightangle`,
  `\varlrtriangle`, `\smwhtdiamond`, `\unicodecdots`, `\adots`, `\disin`,
  `\varisins`, `\isins`, `\isindot`, `\varisinobar`, `\isinobar`,
  `\isinvb`, `\isinE`, `\nisd`, `\varnis`, `\nis`, `\varniobar`,
  `\niobar`, `\bagmember`, `\diameter`, `\house`, `\varbarwedge`,
  `\vardoublebarwedge`, `\invnot`, `\sqlozenge`, `\profline`, `\profsurf`,
  `\viewdata`, and `\turnednot` to native MathML operator output.
- Structural upstream coverage confirmed the selected low-codepoint membership
  and technical symbol alias set at 30/30 mapped. The broader local
  `unicode-math` command key scan now reports 2,199/2,448 commands mapped,
  leaving 249 unmapped commands for later slices.
- Focused EPUB3 coverage writes all 30 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 30/30 selected
  commands mapped and 2,199/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,992 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,338 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,692 assertions, 0 failures with
  pass-count verification `pass=1076 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` membership and technical
  symbol aliases without claiming full TeX symbol table parity, full TeX math
  class/spacing layout parity, delimiter construction piece parity, combining
  accent/enclosure layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Low Codepoint Relation Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,075 behavior tests and 19,650 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 28 upstream `unicode-math`
  low-codepoint relation aliases from U+2241 through U+22B7 that were still
  falling back to literal identifiers.
- `HtmlWriter` now maps `\nsim`, `\sime`, `\nsime`, `\nsimeq`,
  `\simneqq`, `\ncong`, `\napprox`, `\approxident`, `\backcong`,
  `\coloneq`, `\eqcolon`, `\arceq`, `\wedgeq`, `\veeeq`, `\stareq`,
  `\eqdef`, `\measeq`, `\nequiv`, `\Equiv`, `\nasymp`, `\nlessgtr`,
  `\ngtrless`, `\assert`, `\VDash`, `\prurel`, `\scurel`, `\origof`, and
  `\imageof` to native MathML operator output.
- Structural upstream coverage confirmed the selected low-codepoint relation
  alias set at 28/28 mapped. The broader local `unicode-math` command key scan
  now reports 2,169/2,448 commands mapped, leaving 279 unmapped commands for
  later slices.
- Focused EPUB3 coverage writes all 28 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 28/28 selected
  commands mapped and 2,169/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,950 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,296 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,650 assertions, 0 failures with
  pass-count verification `pass=1075 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` relation aliases without
  claiming full TeX symbol table parity, full TeX math class/spacing layout
  parity, remaining relation/operator alias parity, combining accent/enclosure
  layout parity, full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Low Codepoint Core Symbol Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,074 behavior tests and 19,610 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 25 upstream `unicode-math`
  low-codepoint single-glyph aliases from U+2107 through U+223F that were still
  falling back to literal identifiers. It deliberately excludes `\sqrt`, which
  remains an argument-bearing root parser command rather than a simple token
  alias.
- `HtmlWriter` now maps `\Eulerconst` and `\Planckconst` to native MathML
  identifier output. It also maps `\increment`, `\smallin`, `\nni`,
  `\smallni`, `\QED`, `\minus`, `\vysmwhtcircle`, `\vysmblkcircle`, `\surd`,
  `\cuberoot`, `\fourthroot`, `\rightangle`, `\intclockwise`,
  `\varointclockwise`, `\ointctrclockwise`, `\mathratio`, `\Colon`,
  `\dotminus`, `\dashcolon`, `\dotsminusdots`, `\kernelcontraction`,
  `\invlazys`, and `\sinewave` to native MathML operator output.
- Structural upstream coverage confirmed the selected low-codepoint core symbol
  alias set at 25/25 mapped and confirmed `\sqrt` is still not counted as a
  simple token alias. The broader local `unicode-math` command key scan now
  reports 2,141/2,448 commands mapped, leaving 307 unmapped commands for later
  slices.
- Focused EPUB3 coverage writes all 25 aliases across inline and display
  equations, asserts exact MathML `<mi>` or `<mo>` serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 25/25 selected
  commands mapped, `\sqrt` not simple-token mapped, and 2,141/2,448 total
  upstream commands mapped; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 8,910 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,256 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  19,610 assertions, 0 failures with pass-count verification `pass=1074
  fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` low-codepoint symbol aliases
  without claiming full TeX symbol table parity, full root/radical command
  parity beyond existing focused root tests, full TeX math class/spacing layout
  parity, combining accent/enclosure layout parity, full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Unicode Arrow Ord Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,073 behavior tests and 19,573 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 18 upstream `unicode-math`
  arrow-like `mathord` aliases that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\updownarrowbar`, `\linefeed`, `\carriagereturn`,
  `\barovernorthwestarrow`, `\barleftarrowrightarrowbar`,
  `\acwopencirclearrow`, `\cwopencirclearrow`, `\nHuparrow`,
  `\nHdownarrow`, `\leftdasharrow`, `\updasharrow`, `\rightdasharrow`,
  `\downdasharrow`, `\leftwhitearrow`, `\upwhitearrow`, `\rightwhitearrow`,
  `\downwhitearrow`, and `\whitearrowupfrombar` to native MathML operator
  output.
- Structural upstream coverage confirmed the selected Unicode arrow `mathord`
  alias set at 18/18 mapped. The broader local `unicode-math` command key scan
  now reports 2,116/2,448 commands mapped, leaving 332 unmapped commands for
  later slices.
- Focused EPUB3 coverage writes all 18 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriter.php`, and
  `EpubWriterTest.php`; structural upstream coverage confirmed 18/18 selected
  commands mapped and 2,116/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,873 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,219 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,573 assertions, 0 failures with
  pass-count verification `pass=1073 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` arrow-like `mathord` aliases
  without claiming full TeX symbol table parity, full TeX math class/spacing
  layout parity, every arrow relation/operator alias, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Unicode Math Accent Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,072 behavior tests and 19,543 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 17 upstream `unicode-math`
  accent aliases that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\overbar`, `\wideoverbar`, `\widebreve`, `\ocirc`,
  `\widecheck`, `\leftharpoonaccent`, `\overleftharpoon`,
  `\rightharpoonaccent`, `\overrightharpoon`, `\dddot`, `\ddddot`, and
  `\asteraccent` through the existing native MathML `<mover accent="true">`
  path. It also maps `\wideutilde`, `\mathunderbar`, `\threeunderdot`,
  `\underrightharpoondown`, and `\underleftharpoondown` through the existing
  native MathML `<munder accentunder="true">` path.
- Structural upstream coverage confirmed the selected Unicode math accent alias
  set at 17/17 mapped. The broader local `unicode-math` command key scan now
  reports 2,098/2,448 commands mapped, leaving 350 unmapped commands for later
  slices.
- Focused EPUB3 coverage writes all 17 aliases across inline and display
  equations, asserts exact MathML mover/munder serialization, confirms command
  names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 17/17 selected
  commands mapped and 2,098/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,843 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,189 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,543 assertions, 0 failures with
  pass-count verification `pass=1072 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for selected upstream `unicode-math` accent aliases without
  claiming full TeX accent parser parity, every combining accent or overlay
  variant, full TeX symbol table parity, full TeX math class/spacing/layout
  parity, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation,
  DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Punctuation Prime And Currency Symbol Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,071 behavior tests and 19,514 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 18 upstream `unicode-math`
  punctuation, prime, leader, and currency aliases that were still falling back
  to literal identifiers.
- `HtmlWriter` now maps `\horizbar`, `\twolowline`, `\smblkcircle`,
  `\enleadertwodots`, `\unicodeellipsis`, `\dprime`, `\trprime`,
  `\backdprime`, `\backtrprime`, `\caretinsert`, `\Exclam`, `\tieconcat`,
  `\hyphenbullet`, `\fracslash`, `\Question`, `\closure`, `\qprime`, and
  `\euro` to native MathML operator output.
- Structural upstream coverage confirmed the selected punctuation/prime/currency
  alias set at 18/18 mapped. The broader local `unicode-math` command key scan
  now reports 2,081/2,448 commands mapped, leaving 367 unmapped commands for
  later slices.
- Focused EPUB3 coverage writes all 18 aliases across inline and display
  equations, asserts exact MathML `<mo>` serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 18/18 selected
  commands mapped and 2,081/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,814 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,160 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,514 assertions, 0 failures with
  pass-count verification `pass=1071 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for upstream `unicode-math` punctuation, prime, leader, and
  currency aliases without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB graph validation, DRM decryption, XML signature cryptographic
  validation, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-24 EPUB3 TeX Basic ASCII And Currency Symbol Aliases MathML Implementation Slice

- Focused PHP coverage is now 1,070 behavior tests and 19,484 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for 21 low-codepoint upstream
  `unicode-math` symbol aliases that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mathexclam`, `\mathoctothorpe`, `\mathdollar`,
  `\mathpercent`, `\mathampersand`, `\mathplus`, `\mathcomma`,
  `\mathperiod`, `\mathslash`, `\mathcolon`, `\mathsemicolon`, `\less`,
  `\equal`, `\greater`, `\mathquestion`, `\mathatsign`, `\mathsterling`,
  `\mathyen`, `\mathsection`, and `\mathparagraph` to native MathML operator
  output, and maps `\Zbar` to native MathML identifier output.
- Structural upstream coverage confirmed the selected basic ASCII/currency
  alias set at 21/21 mapped. The broader local `unicode-math` command key scan
  now reports 2,063/2,448 commands mapped, leaving 385 unmapped commands for
  later slices.
- Focused EPUB3 coverage writes all 21 aliases across inline and display
  equations, asserts exact MathML `<mo>` or `<mi>` serialization including XML
  escaping for `&`, `<`, and `>`, confirms command names do not leak as literal
  MathML identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 21/21 selected
  commands mapped and 2,063/2,448 total upstream commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,784 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,130 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,484 assertions, 0 failures with
  pass-count verification `pass=1070 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for basic upstream `unicode-math` symbol aliases without claiming
  full TeX symbol table parity, full TeX math class/spacing/layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Eth And Hyphen Alpha Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,069 behavior tests and 19,451 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the final two uncovered upstream
  `unicode-math` `mathalpha` commands, `\matheth` and `\mathhyphen`, which were
  still falling back to literal identifiers.
- `HtmlWriter` now maps `\matheth` and `\mathhyphen` to native MathML
  identifier output. These two newly added commands complete local coverage for
  upstream `unicode-math` `mathalpha` rows, now 1,043/1,043 mapped.
- Focused EPUB3 coverage writes `\matheth` and `\mathhyphen` across inline and
  display equations, asserts exact serialization as MathML `<mi>` nodes,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 1,043/1,043
  `mathalpha` commands mapped; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 8,751 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,097 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  19,451 assertions, 0 failures with pass-count verification `pass=1069
  fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for the final uncovered upstream `mathalpha` commands without
  claiming full TeX symbol table parity, full TeX math class/spacing/layout
  parity, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation,
  DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Upright Backepsilon Symbol MathML Implementation Slice

- Focused PHP coverage is now 1,068 behavior tests and 19,437 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the upstream `unicode-math`
  `\upbackepsilon` command that was still falling back to a literal identifier.
- `HtmlWriter` now maps `\upbackepsilon` to native MathML ordinary symbol
  output. This completes local coverage for the upstream `unicode-math`
  extended upright Greek rows U+0391-U+03F6, now 59/59 mapped.
- Focused EPUB3 coverage writes `\upbackepsilon` in inline and display
  equations, asserts exact serialization as MathML `<mo>϶</mo>`, confirms the
  command name does not leak as a literal MathML identifier, and round-trips
  both equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 59/59 extended
  upright Greek U+0391-U+03F6 commands mapped; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 8,737 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 13,083 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  19,437 assertions, 0 failures with pass-count verification `pass=1068
  fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for the Pandoc/TeX upright backepsilon symbol without claiming
  full TeX symbol table parity, full TeX math class/spacing/layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Digamma Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,067 behavior tests and 19,424 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the upstream `unicode-math`
  mathematical bold Digamma commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mbfDigamma` and `\mbfdigamma` to native MathML
  identifier output. These two newly added commands complete local coverage for
  the upstream `unicode-math` mathematical bold Digamma rows U+1D7CA-U+1D7CB,
  now 2/2 mapped.
- Focused EPUB3 coverage writes both added commands across inline and display
  equations, asserts exact mathematical bold Digamma serialization as MathML
  `<mi>` nodes, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 2/2
  mathematical bold Digamma U+1D7CA-U+1D7CB commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,724 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,070 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,424 assertions, 0 failures with
  pass-count verification `pass=1067 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold Digamma symbols
  without claiming full TeX symbol table parity, full TeX math class/spacing
  layout parity, full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Sans Serif Italic Greek Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,066 behavior tests and 19,410 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream `unicode-math`
  mathematical bold sans-serif italic Greek, nabla, partial, and variant
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\mbfitsansAlpha`, `\mbfitsansBeta`,
  `\mbfitsansGamma`, `\mbfitsansDelta`, `\mbfitsansEpsilon`,
  `\mbfitsansZeta`, `\mbfitsansEta`, `\mbfitsansTheta`,
  `\mbfitsansIota`, `\mbfitsansKappa`, `\mbfitsansLambda`,
  `\mbfitsansMu`, `\mbfitsansNu`, `\mbfitsansXi`,
  `\mbfitsansOmicron`, `\mbfitsansPi`, `\mbfitsansRho`,
  `\mbfitsansvarTheta`, `\mbfitsansSigma`, `\mbfitsansTau`,
  `\mbfitsansUpsilon`, `\mbfitsansPhi`, `\mbfitsansChi`,
  `\mbfitsansPsi`, `\mbfitsansOmega`, `\mbfitsansnabla`,
  `\mbfitsansalpha`, `\mbfitsansbeta`, `\mbfitsansgamma`,
  `\mbfitsansdelta`, `\mbfitsansvarepsilon`, `\mbfitsanszeta`,
  `\mbfitsanseta`, `\mbfitsanstheta`, `\mbfitsansiota`,
  `\mbfitsanskappa`, `\mbfitsanslambda`, `\mbfitsansmu`,
  `\mbfitsansnu`, `\mbfitsansxi`, `\mbfitsansomicron`,
  `\mbfitsanspi`, `\mbfitsansrho`, `\mbfitsansvarsigma`,
  `\mbfitsanssigma`, `\mbfitsanstau`, `\mbfitsansupsilon`,
  `\mbfitsansvarphi`, `\mbfitsanschi`, `\mbfitsanspsi`,
  `\mbfitsansomega`, `\mbfitsanspartial`, `\mbfitsansepsilon`,
  `\mbfitsansvartheta`, `\mbfitsansvarkappa`, `\mbfitsansphi`,
  `\mbfitsansvarrho`, and `\mbfitsansvarpi` to native MathML identifier
  output. These 58 newly added commands complete local coverage for the
  upstream `unicode-math` mathematical bold sans-serif italic Greek rows
  U+1D790-U+1D7C9, now 58/58 mapped.
- Focused EPUB3 coverage writes the full added command range across inline and
  display equations, asserts exact mathematical bold sans-serif italic Greek
  serialization as MathML `<mi>` nodes, confirms command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 58/58
  mathematical bold sans-serif italic Greek U+1D790-U+1D7C9 commands mapped;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1
  file, 8,710 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 13,056 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,410 assertions, 0 failures with
  pass-count verification `pass=1066 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold sans-serif italic
  Greek symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Sans Serif Greek Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,065 behavior tests and 19,340 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream `unicode-math`
  mathematical bold sans-serif Greek, nabla, partial, and variant commands that
  were still falling back to literal identifiers.
- `HtmlWriter` now maps `\mbfsansAlpha`, `\mbfsansBeta`, `\mbfsansGamma`,
  `\mbfsansDelta`, `\mbfsansEpsilon`, `\mbfsansZeta`, `\mbfsansEta`,
  `\mbfsansTheta`, `\mbfsansIota`, `\mbfsansKappa`, `\mbfsansLambda`,
  `\mbfsansMu`, `\mbfsansNu`, `\mbfsansXi`, `\mbfsansOmicron`,
  `\mbfsansPi`, `\mbfsansRho`, `\mbfsansvarTheta`, `\mbfsansSigma`,
  `\mbfsansTau`, `\mbfsansUpsilon`, `\mbfsansPhi`, `\mbfsansChi`,
  `\mbfsansPsi`, `\mbfsansOmega`, `\mbfsansnabla`, `\mbfsansalpha`,
  `\mbfsansbeta`, `\mbfsansgamma`, `\mbfsansdelta`,
  `\mbfsansvarepsilon`, `\mbfsanszeta`, `\mbfsanseta`, `\mbfsanstheta`,
  `\mbfsansiota`, `\mbfsanskappa`, `\mbfsanslambda`, `\mbfsansmu`,
  `\mbfsansnu`, `\mbfsansxi`, `\mbfsansomicron`, `\mbfsanspi`,
  `\mbfsansrho`, `\mbfsansvarsigma`, `\mbfsanssigma`, `\mbfsanstau`,
  `\mbfsansupsilon`, `\mbfsansvarphi`, `\mbfsanschi`, `\mbfsanspsi`,
  `\mbfsansomega`, `\mbfsanspartial`, `\mbfsansepsilon`,
  `\mbfsansvartheta`, `\mbfsansvarkappa`, `\mbfsansphi`,
  `\mbfsansvarrho`, and `\mbfsansvarpi` to native MathML identifier output.
  These 58 newly added commands complete local coverage for the upstream
  `unicode-math` mathematical bold sans-serif Greek rows U+1D756-U+1D78F, now
  58/58 mapped.
- Focused EPUB3 coverage writes the full added command range across inline and
  display equations, asserts exact mathematical bold sans-serif Greek
  serialization as MathML `<mi>` nodes, confirms command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and
  avoids missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 58/58
  mathematical bold sans-serif Greek U+1D756-U+1D78F commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,640 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 12,986 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,340 assertions, 0 failures with
  pass-count verification `pass=1065 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold sans-serif Greek
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Italic Greek Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,064 behavior tests and 19,270 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream `unicode-math`
  mathematical bold italic Greek, nabla, partial, and variant commands that
  were still falling back to literal identifiers.
- `HtmlWriter` now maps `\mbfitAlpha`, `\mbfitBeta`, `\mbfitGamma`,
  `\mbfitDelta`, `\mbfitEpsilon`, `\mbfitZeta`, `\mbfitEta`, `\mbfitTheta`,
  `\mbfitIota`, `\mbfitKappa`, `\mbfitLambda`, `\mbfitMu`, `\mbfitNu`,
  `\mbfitXi`, `\mbfitOmicron`, `\mbfitPi`, `\mbfitRho`, `\mbfitvarTheta`,
  `\mbfitSigma`, `\mbfitTau`, `\mbfitUpsilon`, `\mbfitPhi`, `\mbfitChi`,
  `\mbfitPsi`, `\mbfitOmega`, `\mbfitnabla`, `\mbfitalpha`, `\mbfitbeta`,
  `\mbfitgamma`, `\mbfitdelta`, `\mbfitvarepsilon`, `\mbfitzeta`,
  `\mbfiteta`, `\mbfittheta`, `\mbfitiota`, `\mbfitkappa`,
  `\mbfitlambda`, `\mbfitmu`, `\mbfitnu`, `\mbfitxi`, `\mbfitomicron`,
  `\mbfitpi`, `\mbfitrho`, `\mbfitvarsigma`, `\mbfitsigma`, `\mbfittau`,
  `\mbfitupsilon`, `\mbfitvarphi`, `\mbfitchi`, `\mbfitpsi`,
  `\mbfitomega`, `\mbfitpartial`, `\mbfitepsilon`, `\mbfitvartheta`,
  `\mbfitvarkappa`, `\mbfitphi`, `\mbfitvarrho`, and `\mbfitvarpi` to
  native MathML identifier output. These 58 newly added commands complete local
  coverage for the upstream `unicode-math` mathematical bold italic Greek rows
  U+1D71C-U+1D755, now 58/58 mapped.
- Focused EPUB3 coverage writes the full added command range across inline and
  display equations, asserts exact mathematical bold italic Greek serialization
  as MathML `<mi>` nodes, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 58/58
  mathematical bold italic Greek U+1D71C-U+1D755 commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,570 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 12,916 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,270 assertions, 0 failures with
  pass-count verification `pass=1064 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold italic Greek
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Italic Greek Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,063 behavior tests and 19,200 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream `unicode-math`
  mathematical italic Greek, nabla, partial, and variant commands that were
  still falling back to literal identifiers.
- `HtmlWriter` now maps `\mitAlpha`, `\mitBeta`, `\mitGamma`, `\mitDelta`,
  `\mitEpsilon`, `\mitZeta`, `\mitEta`, `\mitTheta`, `\mitIota`,
  `\mitKappa`, `\mitLambda`, `\mitMu`, `\mitNu`, `\mitXi`, `\mitOmicron`,
  `\mitPi`, `\mitRho`, `\mitvarTheta`, `\mitSigma`, `\mitTau`,
  `\mitUpsilon`, `\mitPhi`, `\mitChi`, `\mitPsi`, `\mitOmega`, `\mitnabla`,
  `\mitalpha`, `\mitbeta`, `\mitgamma`, `\mitdelta`, `\mitvarepsilon`,
  `\mitzeta`, `\miteta`, `\mittheta`, `\mitiota`, `\mitkappa`,
  `\mitlambda`, `\mitmu`, `\mitnu`, `\mitxi`, `\mitomicron`, `\mitpi`,
  `\mitrho`, `\mitvarsigma`, `\mitsigma`, `\mittau`, `\mitupsilon`,
  `\mitvarphi`, `\mitchi`, `\mitpsi`, `\mitomega`, `\mitpartial`,
  `\mitepsilon`, `\mitvartheta`, `\mitvarkappa`, `\mitphi`, `\mitvarrho`,
  and `\mitvarpi` to native MathML identifier output. These 58 newly added
  commands complete local coverage for the upstream `unicode-math` mathematical
  italic Greek rows U+1D6E2-U+1D71B, now 58/58 mapped.
- Focused EPUB3 coverage writes the full added command range across inline and
  display equations, asserts exact mathematical italic Greek serialization as
  MathML `<mi>` nodes, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 58/58
  mathematical italic Greek U+1D6E2-U+1D71B commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,500 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 12,846 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,200 assertions, 0 failures with
  pass-count verification `pass=1063 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical italic Greek symbols
  without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Greek Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,062 behavior tests and 19,130 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream `unicode-math`
  mathematical bold Greek, nabla, partial, and variant commands that were still
  falling back to literal identifiers.
- `HtmlWriter` now maps `\mbfAlpha`, `\mbfBeta`, `\mbfGamma`, `\mbfDelta`,
  `\mbfEpsilon`, `\mbfZeta`, `\mbfEta`, `\mbfTheta`, `\mbfIota`,
  `\mbfKappa`, `\mbfLambda`, `\mbfMu`, `\mbfNu`, `\mbfXi`, `\mbfOmicron`,
  `\mbfPi`, `\mbfRho`, `\mbfvarTheta`, `\mbfSigma`, `\mbfTau`,
  `\mbfUpsilon`, `\mbfPhi`, `\mbfChi`, `\mbfPsi`, `\mbfOmega`, `\mbfnabla`,
  `\mbfalpha`, `\mbfbeta`, `\mbfgamma`, `\mbfdelta`, `\mbfvarepsilon`,
  `\mbfzeta`, `\mbfeta`, `\mbftheta`, `\mbfiota`, `\mbfkappa`,
  `\mbflambda`, `\mbfmu`, `\mbfnu`, `\mbfxi`, `\mbfomicron`, `\mbfpi`,
  `\mbfrho`, `\mbfvarsigma`, `\mbfsigma`, `\mbftau`, `\mbfupsilon`,
  `\mbfvarphi`, `\mbfchi`, `\mbfpsi`, `\mbfomega`, `\mbfpartial`,
  `\mbfepsilon`, `\mbfvartheta`, `\mbfvarkappa`, `\mbfphi`, `\mbfvarrho`,
  and `\mbfvarpi` to native MathML identifier output. These 58 newly added
  commands complete local coverage for the upstream `unicode-math` mathematical
  bold Greek rows U+1D6A8-U+1D6E1, now 58/58 mapped.
- Focused EPUB3 coverage writes the full added command range across inline and
  display equations, asserts exact mathematical bold Greek serialization as
  MathML `<mi>` nodes, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 58/58
  mathematical bold Greek U+1D6A8-U+1D6E1 commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,430 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 12,776 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,130 assertions, 0 failures with
  pass-count verification `pass=1062 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold Greek symbols
  without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Script Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,061 behavior tests and 19,060 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream `unicode-math`
  mathematical script alphabet commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mscrA`, `\mscrC`, `\mscrD`, `\mscrG`, `\mscrJ`,
  `\mscrK`, `\mscrN`, `\mscrO`, `\mscrP`, `\mscrQ`, `\mscrS`, `\mscrT`,
  `\mscrU`, `\mscrV`, `\mscrW`, `\mscrX`, `\mscrY`, `\mscrZ`, `\mscra`,
  `\mscrb`, `\mscrc`, `\mscrd`, `\mscrf`, `\mscrh`, `\mscri`, `\mscrj`,
  `\mscrk`, `\mscrl`, `\mscrm`, `\mscrn`, `\mscrp`, `\mscrq`, `\mscrr`,
  `\mscrs`, `\mscrt`, `\mscru`, `\mscrv`, `\mscrw`, `\mscrx`, `\mscry`,
  and `\mscrz` to native MathML identifier output. These 41 newly added
  commands complete local coverage for the upstream `unicode-math` mathematical
  script alphabet rows U+1D49C-U+1D4CF, now 52/52 mapped with the existing
  letterlike script commands.
- Focused EPUB3 coverage writes the full added command range across inline and
  display equations, asserts exact mathematical script alphabet serialization
  as MathML `<mi>` nodes, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; structural upstream coverage confirmed 52/52
  mathematical script `\mscr*` commands mapped; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 8,360 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,706 assertions, 0
  failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  19,060 assertions, 0 failures with pass-count verification `pass=1061
  fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical script alphabet symbols
  without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Letterlike Double Struck Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,060 behavior tests and 19,007 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream `unicode-math`
  letterlike double-struck and turned symbol commands that were still falling
  back to literal identifiers.
- `HtmlWriter` now maps `\Bbbpi`, `\Bbbgamma`, `\BbbGamma`, `\BbbPi`,
  `\sansLturned`, `\sansLmirrored`, `\Yup`, `\mitBbbD`, `\mitBbbd`,
  `\mitBbbe`, `\mitBbbi`, `\mitBbbj`, and `\PropertyLine` to native MathML
  identifier output, and maps `\Bbbsum` and `\upand` to native MathML operator
  output. These 15 newly added commands complete local coverage for the
  upstream `unicode-math` rows U+213C-U+214B, now 16/16 mapped with the
  existing `\Game` command.
- Focused EPUB3 coverage writes the full added command range across inline and
  display equations, asserts exact mathematical letterlike double-struck
  serialization as MathML `<mi>` or `<mo>` nodes according to the renderer path,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; structural upstream coverage confirmed 16/16
  U+213C-U+214B letterlike double-struck/turned commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,307 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 12,653 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 19,007 assertions, 0 failures with
  pass-count verification `pass=1060 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode letterlike double-struck and turned
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Letterlike Alpha Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,059 behavior tests and 18,980 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream `unicode-math`
  letterlike `mathalpha` commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mscrg`, `\mscrH`, `\mscrI`, `\mscrL`, `\mscrR`,
  `\turnediota`, `\Angstrom`, `\mscrB`, `\mscre`, `\mscrE`, `\mscrF`,
  `\mscrM`, and `\mscro` to native MathML identifier output. These 13 newly
  added commands complete local coverage for the upstream `unicode-math`
  `mathalpha` rows U+210A-U+2134, now 27/27 mapped.
- Focused EPUB3 coverage writes the full added command range across inline and
  display equations, asserts exact mathematical letterlike alpha serialization
  as MathML `<mi>` nodes, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; structural upstream coverage confirmed 27/27
  U+210A-U+2134 letterlike `mathalpha` commands mapped; `php
  tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,280 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 12,626 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 18,980 assertions, 0 failures with
  pass-count verification `pass=1059 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode letterlike mathalpha symbols without
  claiming full TeX symbol table parity, full TeX math class/spacing/layout
  parity, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation,
  DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Upright Greek Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,058 behavior tests and 18,955 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream `unicode-math` upright
  Greek `mathalpha` commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mupAlpha` through `\mupOmega`, `\mupalpha` through
  `\mupomega`, and the upstream upright Greek variant commands `\mupvartheta`,
  `\mupphi`, `\mupvarpi`, `\upDigamma`, `\updigamma`, `\mupvarkappa`,
  `\mupvarrho`, `\mupvarTheta`, and `\mupepsilon` to native MathML identifier
  output. Focused coverage spans upstream `unicode-math` `mathalpha` rows
  U+0391-U+03F5.
- The U+03F6 `\upbackepsilon` row is intentionally outside this identifier
  slice because upstream classifies it as `mathord`, not `mathalpha`; it remains
  available for a later symbol/operator parity slice.
- Focused EPUB3 coverage writes the full selected command range across inline
  and display equations, asserts exact mathematical upright Greek serialization
  as MathML `<mi>` nodes, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; structural upstream coverage confirmed 58/58 upright
  Greek `mathalpha` commands mapped and `\upbackepsilon` unmapped in this slice;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  8,255 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 12,601 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 18,955 assertions, 0 failures with
  pass-count verification `pass=1058 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode upright Greek mathalpha symbols
  without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Styled Digit Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,057 behavior tests and 18,885 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for mathematical styled digit commands
  that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\mbfzero` through `\mbfnine`, `\Bbbzero` through
  `\Bbbnine`, `\msanszero` through `\msansnine`, `\mbfsanszero` through
  `\mbfsansnine`, and `\mttzero` through `\mttnine` to native MathML number
  output. Focused coverage spans upstream `unicode-math` rows
  U+1D7CE-U+1D7FF.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical styled digit serialization as
  MathML `<mn>` nodes, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 8,185 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,531 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,885 assertions, 0 failures with pass-count verification
  `pass=1057 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical styled digit symbols
  without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Monospace Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,056 behavior tests and 18,823 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the complete mathematical
  monospace Latin alphabet commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mttA` through `\mttZ` and `\mtta` through `\mttz`
  to native MathML identifier output. Focused coverage spans upstream
  `unicode-math` rows U+1D670-U+1D6A3.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical monospace alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 8,123 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,469 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,823 assertions, 0 failures with pass-count verification
  `pass=1056 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical monospace alphabet
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Sans Serif Italic Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,055 behavior tests and 18,759 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the complete mathematical bold
  sans-serif italic Latin alphabet commands that were still falling back to
  literal identifiers.
- `HtmlWriter` now maps `\mbfitsansA` through `\mbfitsansZ` and
  `\mbfitsansa` through `\mbfitsansz` to native MathML identifier output.
  Focused coverage spans upstream `unicode-math` rows U+1D63C-U+1D66F.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical bold sans-serif italic alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 8,059 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,405 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,759 assertions, 0 failures with pass-count verification
  `pass=1055 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold sans-serif italic
  alphabet symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Sans Serif Italic Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,054 behavior tests and 18,695 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the complete mathematical
  sans-serif italic Latin alphabet commands that were still falling back to
  literal identifiers.
- `HtmlWriter` now maps `\mitsansA` through `\mitsansZ` and `\mitsansa`
  through `\mitsansz` to native MathML identifier output. Focused coverage
  spans upstream `unicode-math` rows U+1D608-U+1D63B.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical sans-serif italic alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,995 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,341 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,695 assertions, 0 failures with pass-count verification
  `pass=1054 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical sans-serif italic
  alphabet symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Sans Serif Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,053 behavior tests and 18,631 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the complete mathematical bold
  sans-serif Latin alphabet commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mbfsansA` through `\mbfsansZ` and `\mbfsansa`
  through `\mbfsansz` to native MathML identifier output. Focused coverage
  spans upstream `unicode-math` rows U+1D5D4-U+1D607.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical bold sans-serif alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,931 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,277 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,631 assertions, 0 failures with pass-count verification
  `pass=1053 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold sans-serif
  alphabet symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Sans Serif Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,052 behavior tests and 18,567 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the complete mathematical
  sans-serif Latin alphabet commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\msansA` through `\msansZ` and `\msansa` through
  `\msansz` to native MathML identifier output. Focused coverage spans upstream
  `unicode-math` rows U+1D5A0-U+1D5D3.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical sans-serif alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,867 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,213 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,567 assertions, 0 failures with pass-count verification
  `pass=1052 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical sans-serif alphabet
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Double Struck Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,051 behavior tests and 18,503 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for direct mathematical double-struck
  Latin alphabet commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\BbbA` through `\BbbZ` and `\Bbba` through `\Bbbz`
  to native MathML identifier output, reusing the existing `\Bbbk` glyph while
  adding the missing direct commands. Focused coverage spans upstream
  `unicode-math` letterlike rows U+2102, U+210D, U+2115, U+2119-U+211A,
  U+211D, U+2124 and mathematical alphabet rows U+1D538-U+1D56B.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical double-struck alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,803 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,149 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,503 assertions, 0 failures with pass-count verification
  `pass=1051 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical double-struck alphabet
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Fraktur Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,050 behavior tests and 18,439 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the complete mathematical bold
  fraktur Latin alphabet commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mbffrakA` through `\mbffrakZ` and `\mbffraka`
  through `\mbffrakz` to native MathML identifier output. Focused coverage
  spans upstream `unicode-math` rows U+1D56C-U+1D59F.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical bold fraktur alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,739 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,085 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,439 assertions, 0 failures with pass-count verification
  `pass=1050 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold fraktur alphabet
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Fraktur Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,049 behavior tests and 18,375 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for upstream-declared mathematical
  fraktur Latin alphabet commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mfrakA`, `\mfrakB`, `\mfrakC`, `\mfrakD` through
  `\mfrakH`, `\mfrakJ` through `\mfrakQ`, `\mfrakS` through `\mfrakZ`, and
  `\mfraka` through `\mfrakz` to native MathML identifier output. Focused
  coverage spans upstream `unicode-math` letterlike rows U+210C, U+2128,
  U+212D and mathematical alphabet rows U+1D504-U+1D537, preserving the
  upstream absence of direct `\mfrakI` and `\mfrakR` commands.
- Focused EPUB3 coverage writes the full upstream-declared command range across
  inline and display equations, asserts exact mathematical fraktur alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, confirms non-upstream `\mfrakI` and `\mfrakR` identifiers are not
  invented, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,675 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 12,021 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,375 assertions, 0 failures with pass-count verification
  `pass=1049 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical fraktur alphabet
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Script Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,048 behavior tests and 18,309 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the complete mathematical bold
  script Latin alphabet commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mbfscrA` through `\mbfscrZ` and `\mbfscra` through
  `\mbfscrz` to native MathML identifier output. Focused coverage spans upstream
  `unicode-math` rows U+1D4D0-U+1D503.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical bold script alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,609 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,955 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,309 assertions, 0 failures with pass-count verification
  `pass=1048 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold script alphabet
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Italic Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,047 behavior tests and 18,245 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the complete mathematical bold
  italic Latin alphabet commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\mbfitA` through `\mbfitZ` and `\mbfita` through
  `\mbfitz` to native MathML identifier output. Focused coverage spans upstream
  `unicode-math` rows U+1D468-U+1D49B.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical bold italic alphabet
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,545 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,891 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,245 assertions, 0 failures with pass-count verification
  `pass=1047 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold italic alphabet
  symbols without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Italic Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,046 behavior tests and 18,181 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for the mathematical italic Latin
  alphabet commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\mitA` through `\mitZ`, plus the upstream small-letter
  rows `\mita` through `\mitg` and `\miti` through `\mitz`, to native MathML
  identifier output. Focused coverage spans upstream `unicode-math` rows
  U+1D434-U+1D467 and intentionally preserves the upstream gap at U+1D455,
  where no `\mith` row is declared.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical italic alphabet serialization,
  confirms command names do not leak as literal MathML identifiers, confirms no
  non-upstream `\mith` command is invented, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,481 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,827 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,181 assertions, 0 failures with pass-count verification
  `pass=1046 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical italic alphabet symbols
  without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Mathematical Bold Alphabet Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,045 behavior tests and 18,117 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed postal/zigzag
  symbols and the complete mathematical bold Latin alphabet that were still
  falling back to literal identifiers.
- `HtmlWriter` now maps `\postalmark`, `\hzigzag`, `\mbfA` through `\mbfZ`,
  and `\mbfa` through `\mbfz` to native MathML output. Focused coverage spans
  upstream `unicode-math` rows U+3012, U+3030, and U+1D400-U+1D433.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact mathematical bold alphabet serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,417 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,763 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,117 assertions, 0 failures with pass-count verification
  `pass=1045 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode mathematical bold alphabet and
  postal/zigzag symbols without claiming full TeX symbol table parity, full TeX
  math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Left Arrow And Star Shape Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,044 behavior tests and 18,051 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed circled, triple,
  circled-plus, squiggle, two-headed, dashed, dotted, tailed, curved, equality,
  similarity, approximation, greater/superset, quadruple arrow variants plus
  medium/small star and right-pentagon shape symbols that were still falling
  back to literal identifiers.
- `HtmlWriter` now maps `\circleonleftarrow`, `\leftthreearrows`,
  `\leftarrowonoplus`, `\longleftsquigarrow`, `\nvtwoheadleftarrow`,
  `\nVtwoheadleftarrow`, `\twoheadmapsfrom`, `\twoheadleftdbkarrow`,
  `\leftdotarrow`, `\nvleftarrowtail`, `\nVleftarrowtail`,
  `\twoheadleftarrowtail`, `\nvtwoheadleftarrowtail`,
  `\nVtwoheadleftarrowtail`, `\leftarrowx`, `\leftcurvedarrow`,
  `\equalleftarrow`, `\bsimilarleftarrow`, `\leftarrowbackapprox`,
  `\rightarrowgtr`, `\rightarrowsupset`, `\LLeftarrow`, `\RRightarrow`,
  `\bsimilarrightarrow`, `\rightarrowbackapprox`, `\similarleftarrow`,
  `\leftarrowapprox`, `\leftarrowbsimilar`, `\rightarrowbsimilar`,
  `\medwhitestar`, `\medblackstar`, `\smwhitestar`,
  `\rightpentagonblack`, and `\rightpentagon` to native MathML operator output.
  Focused coverage spans the upstream `unicode-math` rows U+2B30-U+2B4C and
  U+2B50-U+2B54.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact left-arrow/star shape symbol serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,351 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,697 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,051 assertions, 0 failures with pass-count verification
  `pass=1044 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode left-arrow and star/right-pentagon
  shape variants without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Black White Shape Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,043 behavior tests and 18,005 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed half-filled
  square/diamond, dotted square, large and very small squares, pentagon,
  hexagon, large circle, diamond/lozenge, and oval shape symbols that were
  still falling back to literal identifiers.
- `HtmlWriter` now maps `\squaretopblack`, `\squarebotblack`,
  `\squareurblack`, `\squarellblack`, `\diamondleftblack`,
  `\diamondrightblack`, `\diamondtopblack`, `\diamondbotblack`,
  `\dottedsquare`, `\lgblksquare`, `\lgwhtsquare`, `\vysmblksquare`,
  `\vysmwhtsquare`, `\pentagonblack`, `\pentagon`, `\varhexagon`,
  `\varhexagonblack`, `\hexagonblack`, `\lgblkcircle`, `\mdblkdiamond`,
  `\mdwhtdiamond`, `\mdblklozenge`, `\mdwhtlozenge`, `\smblkdiamond`,
  `\smblklozenge`, `\smwhtlozenge`, `\blkhorzoval`, `\whthorzoval`,
  `\blkvertoval`, and `\whtvertoval` to native MathML operator output.
  Focused coverage spans the full U+2B12-U+2B2F upstream `unicode-math` range.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact black/white shape symbol serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,305 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,651 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  18,005 assertions, 0 failures with pass-count verification
  `pass=1043 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode black/white geometric shape symbols
  without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Parallel And Triple Bar Relations MathML Implementation Slice

- Focused PHP coverage is now 1,042 behavior tests and 17,963 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed down-tack circle,
  stroked and tilded parallel, triple vertical bar, triple colon, nested
  less/greater, double-line slanted ordering, triple/double solidus, and tall
  oblong relation/operator symbols that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\topcir`, `\nhpar`, `\parsim`, `\nhVvert`,
  `\threedotcolon`, `\lllnest`, `\gggnest`, `\leqqslant`, `\geqqslant`,
  `\trslash`, `\biginterleave`, and `\bigtalloblong` to native MathML operator
  output. Focused coverage spans the full U+2AF1-U+2AFF upstream
  `unicode-math` range while preserving and reasserting the older exact
  `\interleave`, `\sslash`, and `\talloblong` mappings.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact parallel/triple-bar relation serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,263 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,609 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,963 assertions, 0 failures with pass-count verification
  `pass=1042 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode parallel, triple-bar, and related
  relation/operator variants without claiming full TeX symbol table parity,
  full TeX math class/spacing/layout parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Closed Subset And Tack Relations MathML Implementation Slice

- Focused PHP coverage is now 1,041 behavior tests and 17,936 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed closed
  subset/superset, fork, short tack, double tack, barred vertical, negated
  double-bar, reversed mid, and circular mid relation symbols that were still
  falling back to literal identifiers.
- `HtmlWriter` now maps `\csube`, `\csupe`, `\subsup`, `\supsub`,
  `\subsub`, `\supsup`, `\suphsub`, `\supdsub`, `\forkv`, `\topfork`,
  `\mlcp`, `\forks`, `\forksnot`, `\shortlefttack`, `\shortdowntack`,
  `\shortuptack`, `\perps`, `\vDdash`, `\dashV`, `\Dashv`, `\DashV`,
  `\varVdash`, `\Barv`, `\vBar`, `\vBarv`, `\barV`, `\Vbar`, `\Not`,
  `\bNot`, `\revnmid`, `\cirmid`, and `\midcir` to native MathML operator
  output. Focused coverage spans the full U+2AD1-U+2AF0 upstream
  `unicode-math` range.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact closed subset/tack relation serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,236 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,582 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,936 assertions, 0 failures with pass-count verification
  `pass=1041 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode closed subset/superset and tack
  relation variants without claiming full TeX symbol table parity, full TeX
  math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Precedence And Subset Relations MathML Implementation Slice

- Focused PHP coverage is now 1,040 behavior tests and 17,892 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed precedence
  not-equal, precedence double-equal, double precedence, dotted subset/superset,
  plus/multiply-marked subset/superset, dotted subset/superset equality,
  similarity subset/superset, square hooks, and closed subset/superset symbols
  that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\precneq`, `\succneq`, `\preceqq`, `\succeqq`,
  `\Prec`, `\Succ`, `\subsetdot`, `\supsetdot`, `\subsetplus`,
  `\supsetplus`, `\submult`, `\supmult`, `\subedot`, `\supedot`,
  `\subsim`, `\supsim`, `\lsqhook`, `\rsqhook`, `\csub`, and `\csup` to
  native MathML operator output. Focused coverage spans the full U+2AB1-U+2AD0
  upstream `unicode-math` range while preserving and reasserting the older exact
  `\precneqq`, `\succneqq`, `\precapprox`, `\succapprox`, `\precnapprox`,
  `\succnapprox`, `\subseteqq`, `\supseteqq`, `\subsetapprox`,
  `\supsetapprox`, `\subsetneqq`, and `\supsetneqq` mappings.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact precedence/subset relation serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,192 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,538 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,892 assertions, 0 failures with pass-count verification
  `pass=1040 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode precedence and subset/superset
  relation variants without claiming full TeX symbol table parity, full TeX
  math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Advanced Less Greater Relations MathML Implementation Slice

- Focused PHP coverage is now 1,039 behavior tests and 17,848 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed less/greater
  sandwich, slanted equality, dotted slanted equality, double-line equality,
  similarity-ordering, nested less/greater, curved less/greater, smaller/larger,
  and bumpy-equality relation symbols that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\lgE`, `\glE`, `\lesges`, `\gesles`, `\elsdot`,
  `\egsdot`, `\eqqless`, `\eqqgtr`, `\eqqslantless`, `\eqqslantgtr`,
  `\simless`, `\simgtr`, `\simlE`, `\simgE`, `\Lt`, `\Gt`,
  `\partialmeetcontraction`, `\glj`, `\gla`, `\ltcc`, `\gtcc`, `\lescc`,
  `\gescc`, `\smt`, `\lat`, `\smte`, `\late`, and `\bumpeqq` to native
  MathML operator output. Focused coverage also reasserts the already-correct
  `\eqslantless` and `\eqslantgtr` rows in the U+2A91-U+2AB0 upstream
  `unicode-math` range while preserving the existing conventional `\preceq`
  and `\succeq` mappings.
- Focused EPUB3 coverage writes the full command set across inline and display
  equations, asserts exact advanced less/greater relation serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,148 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,494 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,848 assertions, 0 failures with pass-count verification
  `pass=1039 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode advanced less/greater relation
  variants without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB graph validation, DRM decryption, XML signature cryptographic
  validation, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-24 EPUB3 TeX Slanted Equality And Ordering Relations MathML Implementation Slice

- Focused PHP coverage is now 1,038 behavior tests and 17,806 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed equality-plus,
  colon-equals, repeated-equals, dotted-equals, circled/questioned less/greater,
  slanted less/greater, dotted slanted ordering, approximation, not-equal,
  less/greater sandwich, and mixed similarity-ordering symbols that were still
  falling back to literal identifiers or approximate glyphs.
- `HtmlWriter` now maps `\eqqplus`, `\pluseqq`, `\eqqsim`, `\Coloneq`,
  `\eqeq`, `\eqeqeq`, `\ddotseq`, `\equivDD`, `\ltcir`, `\gtcir`,
  `\ltquest`, `\gtquest`, `\lesdot`, `\gesdot`, `\lesdoto`, `\gesdoto`,
  `\lesdotor`, `\gesdotol`, `\lsime`, `\gsime`, `\lsimg`, and `\gsiml`
  to native MathML operator output, and corrects `\leqslant` and `\geqslant`
  from the basic less/greater-equal glyphs to upstream slanted forms. Focused
  coverage spans the full U+2A71-U+2A90 upstream `unicode-math` range while
  preserving the older covered `\lessapprox`, `\gtrapprox`, `\lneq`, `\gneq`,
  `\lnapprox`, `\gnapprox`, `\lesseqqgtr`, and `\gtreqqless` aliases.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact slanted equality/ordering serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,106 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,452 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,806 assertions, 0 failures with pass-count verification
  `pass=1038 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode slanted equality and ordering
  variants without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB graph validation, DRM decryption, XML signature cryptographic
  validation, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-24 EPUB3 TeX Wedge Vee And Relation Operators MathML Implementation Slice

- Focused PHP coverage is now 1,037 behavior tests and 17,762 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed wedge/vee,
  sloped logical, middle-bar logical, Z antirestriction, dotted equality,
  equivalence, similarity, congruence, asterisk-equality, and approximate
  equality symbols that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\wedgeodot`, `\veeodot`, `\Wedge`, `\Vee`,
  `\wedgeonwedge`, `\veeonvee`, `\bigslopedvee`, `\bigslopedwedge`,
  `\veeonwedge`, `\wedgemidvert`, `\veemidvert`, `\midbarwedge`,
  `\midbarvee`, `\wedgebar`, `\wedgedoublebar`, `\doublebarvee`,
  `\veedoublebar`, `\dsub`, `\rsub`, `\eqdot`, `\dotequiv`,
  `\equivVert`, `\equivVvert`, `\dotsim`, `\simrdots`,
  `\simminussim`, `\congdot`, `\asteq`, `\hatapprox`, and `\approxeqq`
  to native MathML operator output. Focused coverage spans the full
  U+2A51-U+2A70 upstream `unicode-math` range while preserving the older
  covered `\doublebarwedge` and `\varveebar` aliases.
- Focused EPUB3 coverage writes the full command range across inline and
  display equations, asserts exact wedge/vee/relation operator serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,062 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,408 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,762 assertions, 0 failures with pass-count verification
  `pass=1037 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode wedge/vee and equality/relation
  variants without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB graph validation, DRM decryption, XML signature cryptographic
  validation, or broader media/layout edge-case parity beyond existing focused
  cases.

## 2026-06-24 EPUB3 TeX Times And Union Operators MathML Implementation Slice

- Focused PHP coverage is now 1,036 behavior tests and 17,718 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed multiplication,
  circled-times, triangle arithmetic, Z composition, cap/cup, double-cap/cup,
  and closed cap/cup symbols that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\timesbar`, `\btimes`, `\smashtimes`,
  `\otimeslhrim`, `\otimesrhrim`, `\otimeshat`, `\Otimes`, `\odiv`,
  `\triangleplus`, `\triangleminus`, `\triangletimes`, `\fcmp`, `\capdot`,
  `\uminus`, `\capwedge`, `\cupvee`, `\cupovercap`, `\capovercup`,
  `\cupbarcap`, `\capbarcup`, `\twocups`, `\twocaps`, `\closedvarcup`,
  `\closedvarcap`, `\Sqcap`, `\Sqcup`, and `\closedvarcupsmashprod` to
  native MathML operator output, closing the remaining missing U+2A31-U+2A50
  upstream `unicode-math` rows while preserving the older covered `\intprod`,
  `\intprodr`, `\amalg`, `\barcup`, and `\barcap` aliases.
- Focused EPUB3 coverage writes the full missing command set across inline and
  display equations, asserts exact times/union operator serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 7,018 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,364 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,718 assertions, 0 failures with pass-count verification
  `pass=1036 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode multiplication, cap/cup, and closed
  set-operator variants without claiming full TeX symbol table parity, full TeX
  math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Integral And Plus Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,035 behavior tests and 17,679 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed integral, Z
  notation, plus, minus, circled-plus, vector-times, and dotted-times symbols
  that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\awint`, `\rppolint`, `\scpolint`, `\npolint`,
  `\pointint`, `\sqint`, `\intlarhk`, `\intx`, `\intcap`, `\intcup`,
  `\upint`, `\lowint`, `\bigtriangleleft`, `\zcmp`, `\zpipe`,
  `\zproject`, `\ringplus`, `\plushat`, `\simplus`, `\plusdot`,
  `\plussim`, `\plussubtwo`, `\plustrif`, `\commaminus`, `\minusdot`,
  `\minusfdots`, `\minusrdots`, `\opluslhrim`, `\oplusrhrim`,
  `\vectimes`, and `\dottimes` to native MathML operator output, closing the
  remaining U+2A11-U+2A30 upstream `unicode-math` rows except the existing
  compatibility `\Join` mapping.
- Focused EPUB3 coverage writes the full command set across inline and display
  equations, asserts exact integral/plus symbol serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,979 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,325 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,679 assertions, 0 failures with pass-count verification
  `pass=1035 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode integral, plus, minus,
  circled-plus, vector-times, and Z notation symbol variants without claiming
  full TeX symbol table parity, full TeX math class/spacing/layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Solidus And N-Ary Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,034 behavior tests and 17,636 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed error-barred
  circle/diamond symbols, rule-delayed and solidus variants, double/triple plus
  signs, curved angle brackets, tiny plus/minus signs, and additional n-ary
  operator symbols that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\errbarblackdiamond`, `\errbarcircle`,
  `\errbarblackcircle`, `\ruledelayed`, `\reversesolidus`, `\dsol`,
  `\rsolbar`, `\xsol`, `\xbsol`, `\doubleplus`, `\tripleplus`,
  `\lcurvyangle`, `\rcurvyangle`, `\tplus`, `\tminus`, `\bigcupdot`,
  `\bigsqcap`, `\conjquant`, `\disjquant`, `\bigtimes`, `\modtwosum`,
  `\sumint`, `\intbar`, `\intBar`, `\fint`, and `\cirfnint` to native
  MathML operator output, closing the missing entries in the U+29F1-U+2A10
  upstream `unicode-math` range not already covered by earlier large-operator
  slices.
- Focused EPUB3 coverage writes the full command set across inline and display
  equations, asserts exact solidus/n-ary symbol serialization, confirms command
  names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,936 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,282 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,636 assertions, 0 failures with pass-count verification
  `pass=1034 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode solidus and n-ary operator variants
  without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB graph validation, DRM decryption, XML signature cryptographic validation,
  or broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Bowtie And Errorbar Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,033 behavior tests and 17,598 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed bowtie/times
  variants, wiggly fences, infinity variants, shuffle/parallel relations,
  thermodynamic/triangle markers, and error-barred shapes that were still
  falling back to literal identifiers.
- `HtmlWriter` now maps `\lfbowtie`, `\rfbowtie`, `\fbowtie`, `\lftimes`,
  `\rftimes`, `\hourglass`, `\blackhourglass`, `\lvzigzag`, `\rvzigzag`,
  `\Lvzigzag`, `\Rvzigzag`, `\iinfin`, `\tieinfty`, `\nvinfty`,
  `\dualmap`, `\laplac`, `\lrtriangleeq`, `\shuffle`, `\eparsl`,
  `\smeparsl`, `\eqvparsl`, `\gleichstark`, `\thermod`,
  `\downtriangleleftblack`, `\downtrianglerightblack`,
  `\blackdiamonddownarrow`, `\mdlgblklozenge`, `\circledownarrow`,
  `\blackcircledownarrow`, `\errbarsquare`, `\errbarblacksquare`, and
  `\errbardiamond` to native MathML operator output, closing the contiguous
  U+29D1-U+29F0 upstream `unicode-math` range.
- Focused EPUB3 coverage writes the full command set across inline and display
  equations, asserts exact bowtie/errorbar symbol serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,898 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,244 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,598 assertions, 0 failures with pass-count verification
  `pass=1033 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode bowtie, infinity, relation, and
  errorbar symbol variants without claiming full TeX symbol table parity, full
  TeX math class/spacing/layout parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Circled And Triangle Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,032 behavior tests and 17,554 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed reversed/marked
  empty-set symbols, extra circled operators, box/triangle variants, and
  triangle/bar relation symbols that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\revemptyset`, `\emptysetobar`, `\emptysetocirc`,
  `\emptysetoarr`, `\emptysetoarrl`, `\obot`, `\olcross`,
  `\odotslashdot`, `\uparrowoncircle`, `\circledwhitebullet`,
  `\circledbullet`, `\cirscir`, `\cirE`, `\boxonbox`, `\triangleodot`,
  `\triangleubar`, `\triangles`, `\triangleserifs`, `\rtriltri`,
  `\ltrivb`, and `\vbrtri` to native MathML operator output, closing the
  missing entries in the U+29B0-U+29D0 upstream `unicode-math` range not
  already covered by earlier operator slices.
- Focused EPUB3 coverage writes the full command set across inline and display
  equations, asserts exact circled/triangle symbol serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,854 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,200 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,554 assertions, 0 failures with pass-count verification
  `pass=1032 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode circled and triangle symbol
  variants without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB graph validation, DRM decryption, XML signature cryptographic validation,
  or broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Angle Variant Symbols MathML Implementation Slice

- Focused PHP coverage is now 1,031 behavior tests and 17,521 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed dotted fence,
  vertical zigzag, spherical/reversed/underbar angle, and measured-angle
  arrow-arm commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\fourvdots`, `\vzigzag`, `\measuredangleleft`,
  `\rightanglesqr`, `\rightanglemdot`, `\angles`, `\angdnr`, `\gtlpar`,
  `\sphericalangleup`, `\turnangle`, `\revangle`, `\angleubar`,
  `\revangleubar`, `\wideangledown`, `\wideangleup`, `\measanglerutone`,
  `\measanglelutonw`, `\measanglerdtose`, `\measangleldtosw`,
  `\measangleurtone`, `\measangleultonw`, `\measangledrtose`, and
  `\measangledltosw` to native MathML operator output, following the contiguous
  U+2999-U+29AF rows in the upstream `unicode-math` table.
- Focused EPUB3 coverage writes the full command set across inline and display
  equations, asserts exact angle-symbol serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,821 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,167 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,521 assertions, 0 failures with pass-count verification
  `pass=1031 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode angle-variant symbols without
  claiming full TeX symbol table parity, full TeX math class/spacing/layout
  parity, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation,
  DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Z Notation Extended Brackets MathML Implementation Slice

- Focused PHP coverage is now 1,030 behavior tests and 17,486 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed Z notation
  dotted angle, arc, and black tortoise-shell bracket commands that were still
  falling back to literal identifiers.
- `HtmlWriter` now maps `\langledot`, `\rangledot`, `\lparenless`,
  `\rparengtr`, `\Lparengtr`, `\Rparenless`, `\lblkbrbrak`, and
  `\rblkbrbrak` to native MathML operator output, following the contiguous
  U+2991-U+2998 rows in the upstream `unicode-math` table.
- Focused EPUB3 coverage writes `a \langledot b + c \rangledot d + e
  \lparenless f + g \rparengtr h` and `i \Lparengtr j + k \Rparenless l + m
  \lblkbrbrak n + o \rblkbrbrak p`, asserts exact bracket serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,786 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,132 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,486 assertions, 0 failures with pass-count verification
  `pass=1030 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode Z notation extended bracket
  variants without claiming full TeX delimiter table parity, full
  `\left`/`\right` extensible delimiter layout parity for every Unicode fence,
  full TeX math class/spacing/layout parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Z Notation Brackets MathML Implementation Slice

- Focused PHP coverage is now 1,029 behavior tests and 17,466 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed Z notation
  parenthesis, binding bracket, and square-bracket tick commands that were still
  falling back to literal identifiers.
- `HtmlWriter` now maps `\lParen`, `\rParen`, `\llparenthesis`,
  `\rrparenthesis`, `\llangle`, `\rrangle`, `\lbrackubar`,
  `\rbrackubar`, `\lbrackultick`, `\rbracklrtick`, `\lbracklltick`,
  and `\rbrackurtick` to native MathML operator output.
- Focused EPUB3 coverage writes `a \lParen b + c \rParen d + e
  \llparenthesis f + g \rrparenthesis h + i \llangle j + k \rrangle l`
  and `m \lbrackubar n + o \rbrackubar p + q \lbrackultick r + s
  \rbracklrtick t + u \lbracklltick v + w \rbrackurtick x`, asserts exact
  bracket serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,766 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,112 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,466 assertions, 0 failures with pass-count verification
  `pass=1029 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode Z notation bracket variants without
  claiming full TeX delimiter table parity, full `\left`/`\right` extensible
  delimiter layout parity for every Unicode fence, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB graph validation, DRM decryption, XML signature cryptographic validation,
  or broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Z Notation Delimiters MathML Implementation Slice

- Focused PHP coverage is now 1,028 behavior tests and 17,442 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed Z notation
  delimiter and marker commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\Vvert`, `\mdsmblkcircle`, `\typecolon`,
  `\lBrace`, and `\rBrace` to native MathML operator output.
- Focused EPUB3 coverage writes `a \Vvert b + c \mdsmblkcircle d + e
  \typecolon f` and `g \lBrace h + i \rBrace j`, asserts exact delimiter
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,742 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,088 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,442 assertions, 0 failures with pass-count verification
  `pass=1028 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode Z notation delimiter variants
  without claiming full TeX delimiter table parity, full `\left`/`\right`
  extensible delimiter layout parity for every Unicode fence, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB graph validation, DRM decryption, XML signature cryptographic validation,
  or broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Fishtail Arrows MathML Implementation Slice

- Focused PHP coverage is now 1,027 behavior tests and 17,425 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed fishtail arrow
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\leftfishtail`, `\rightfishtail`,
  `\upfishtail`, and `\downfishtail` to native MathML operator output.
- Focused EPUB3 coverage writes `a \leftfishtail b + c \rightfishtail d`
  and `e \upfishtail f + g \downfishtail h`, asserts exact arrow
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,725 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,071 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,425 assertions, 0 failures with pass-count verification
  `pass=1027 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode fishtail arrow variants without
  claiming full TeX arrow table parity, extensible arrow layout parity, full
  TeX math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Arrow Relation Overlays MathML Implementation Slice

- Focused PHP coverage is now 1,026 behavior tests and 17,409 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed arrow relation
  overlay commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\rightimply`, `\equalrightarrow`,
  `\similarrightarrow`, `\leftarrowsimilar`, `\rightarrowsimilar`,
  `\rightarrowapprox`, `\ltlarr`, `\leftarrowless`, `\gtrarr`,
  `\subrarr`, `\leftarrowsubset`, and `\suplarr` to native MathML
  operator output.
- Focused EPUB3 coverage writes `a \rightimply b + c \equalrightarrow d + e
  \similarrightarrow f + g \leftarrowsimilar h + i \rightarrowsimilar j + k
  \rightarrowapprox l` and `m \ltlarr n + o \leftarrowless p + q \gtrarr r
  + s \subrarr t + u \leftarrowsubset v + w \suplarr x`, asserts exact arrow
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,709 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,055 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,409 assertions, 0 failures with pass-count verification
  `pass=1026 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode arrow relation overlay variants
  without claiming full TeX arrow table parity, extensible arrow layout parity,
  full TeX math class/spacing/layout parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Dash Harpoons MathML Implementation Slice

- Focused PHP coverage is now 1,025 behavior tests and 17,385 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed dash harpoon and
  up/down paired harpoon commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\leftharpoonupdash`,
  `\dashleftharpoondown`, `\rightharpoonupdash`,
  `\dashrightharpoondown`, `\updownharpoonsleftright`, and
  `\downupharpoonsleftright` to native MathML operator output.
- Focused EPUB3 coverage writes `a \leftharpoonupdash b + c
  \dashleftharpoondown d + e \rightharpoonupdash f` and `g
  \dashrightharpoondown h + i \updownharpoonsleftright j + k
  \downupharpoonsleftright l`, asserts exact harpoon serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,685 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,031 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,385 assertions, 0 failures with pass-count verification
  `pass=1025 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode dash harpoon variants without
  claiming full TeX arrow table parity, extensible arrow layout parity, full
  TeX math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Parallel Harpoons MathML Implementation Slice

- Focused PHP coverage is now 1,024 behavior tests and 17,367 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed parallel harpoon
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\leftharpoonsupdown`, `\upharpoonsleftright`,
  `\rightharpoonsupdown`, `\downharpoonsleftright`,
  `\leftrightharpoonsup`, `\leftrightharpoonsdown`,
  `\rightleftharpoonsup`, and `\rightleftharpoonsdown` to native MathML
  operator output.
- Focused EPUB3 coverage writes `a \leftharpoonsupdown b + c
  \upharpoonsleftright d + e \rightharpoonsupdown f + g
  \downharpoonsleftright h` and `i \leftrightharpoonsup j + k
  \leftrightharpoonsdown l + m \rightleftharpoonsup n + o
  \rightleftharpoonsdown p`, asserts exact harpoon serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,667 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 11,013 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,367 assertions, 0 failures with pass-count verification
  `pass=1024 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode parallel harpoon variants without
  claiming full TeX arrow table parity, extensible arrow layout parity, full
  TeX math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Harpoon From Bar MathML Implementation Slice

- Focused PHP coverage is now 1,023 behavior tests and 17,347 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed harpoon-from-bar
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\leftharpoonupbar`, `\barrightharpoonup`,
  `\upharpoonrightbar`, `\bardownharpoonright`,
  `\leftharpoondownbar`, `\barrightharpoondown`,
  `\upharpoonleftbar`, and `\bardownharpoonleft` to native MathML
  operator output.
- Focused EPUB3 coverage writes `a \leftharpoonupbar b + c
  \barrightharpoonup d + e \upharpoonrightbar f + g
  \bardownharpoonright h` and `i \leftharpoondownbar j + k
  \barrightharpoondown l + m \upharpoonleftbar n + o
  \bardownharpoonleft p`, asserts exact harpoon serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips
  both equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,647 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,993 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,347 assertions, 0 failures with pass-count verification
  `pass=1023 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode harpoon-from-bar variants without
  claiming full TeX arrow table parity, extensible arrow layout parity, full
  TeX math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Harpoon To Bar MathML Implementation Slice

- Focused PHP coverage is now 1,022 behavior tests and 17,327 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed harpoon-to-bar
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\barleftharpoonup`, `\rightharpoonupbar`,
  `\barupharpoonright`, `\downharpoonrightbar`, `\barleftharpoondown`,
  `\rightharpoondownbar`, `\barupharpoonleft`, and
  `\downharpoonleftbar` to native MathML operator output.
- Focused EPUB3 coverage writes `a \barleftharpoonup b + c
  \rightharpoonupbar d + e \barupharpoonright f + g \downharpoonrightbar h`
  and `i \barleftharpoondown j + k \rightharpoondownbar l + m
  \barupharpoonleft n + o \downharpoonleftbar p`, asserts exact harpoon
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,627 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,973 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,327 assertions, 0 failures with pass-count verification
  `pass=1022 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode harpoon-to-bar variants without
  claiming full TeX arrow table parity, extensible arrow layout parity, full
  TeX math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Paired Harpoon Arrow MathML Implementation Slice

- Focused PHP coverage is now 1,021 behavior tests and 17,307 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed left-right and
  up-down paired harpoon commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\leftrightharpoonupdown`,
  `\leftrightharpoondownup`, `\updownharpoonrightleft`,
  `\updownharpoonleftright`, `\leftrightharpoonupup`,
  `\updownharpoonrightright`, `\leftrightharpoondowndown`, and
  `\updownharpoonleftleft` to native MathML operator output.
- Focused EPUB3 coverage writes `a \leftrightharpoonupdown b + c
  \leftrightharpoondownup d + e \updownharpoonrightleft f + g
  \updownharpoonleftright h` and `i \leftrightharpoonupup j + k
  \updownharpoonrightright l + m \leftrightharpoondowndown n + o
  \updownharpoonleftleft p`, asserts exact harpoon serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,607 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,953 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,307 assertions, 0 failures with pass-count verification
  `pass=1021 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode paired harpoon variants without
  claiming full TeX arrow table parity, extensible arrow layout parity, full
  TeX math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Short Plus Circle Arrow MathML Implementation Slice

- Focused PHP coverage is now 1,020 behavior tests and 17,287 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed short-overlaid,
  plus, x-through, left-right-circle, and two-headed-up-circle arrow commands
  that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\rightarrowshortleftarrow`,
  `\leftarrowshortrightarrow`, `\shortrightarrowleftarrow`,
  `\rightarrowplus`, `\leftarrowplus`, `\rightarrowx`,
  `\leftrightarrowcircle`, and `\twoheaduparrowcircle` to native MathML
  operator output.
- Focused EPUB3 coverage writes `a \rightarrowshortleftarrow b + c
  \leftarrowshortrightarrow d + e \shortrightarrowleftarrow f + g
  \rightarrowplus h` and `i \leftarrowplus j + k \rightarrowx l + m
  \leftrightarrowcircle n + o \twoheaduparrowcircle p`, asserts exact arrow
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,587 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,933 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,287 assertions, 0 failures with pass-count verification
  `pass=1020 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode short, plus, x-through, and circle
  arrow variants without claiming full TeX arrow table parity, extensible arrow
  layout parity, full TeX math class/spacing/layout parity, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Curved Arc Arrow MathML Implementation Slice

- Focused PHP coverage is now 1,019 behavior tests and 17,267 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed curved,
  clockwise/anticlockwise arc, under-curve, and circle-arrow commands that were
  still falling back to literal identifiers.
- `HtmlWriter` now maps `\rightcurvedarrow`, `\uprightcurvearrow`,
  `\downrightcurvedarrow`, `\leftdowncurvedarrow`, `\rightdowncurvedarrow`,
  `\cwrightarcarrow`, `\acwleftarcarrow`, `\acwoverarcarrow`,
  `\acwunderarcarrow`, `\curvearrowrightminus`, `\curvearrowleftplus`,
  `\cwundercurvearrow`, `\ccwundercurvearrow`, `\acwcirclearrow`, and
  `\cwcirclearrow` to native MathML operator output.
- Focused EPUB3 coverage writes `a \rightcurvedarrow b + c
  \uprightcurvearrow d + e \downrightcurvedarrow f + g \leftdowncurvedarrow h
  + i \rightdowncurvedarrow j + k \cwrightarcarrow l + m \acwleftarcarrow n
  + o \acwoverarcarrow p` and `q \acwunderarcarrow r + s
  \curvearrowrightminus t + u \curvearrowleftplus v + w \cwundercurvearrow x
  + y \ccwundercurvearrow z + A \acwcirclearrow B + C \cwcirclearrow D`,
  asserts exact arrow serialization, confirms command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,567 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,913 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,267 assertions, 0 failures with pass-count verification
  `pass=1019 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode curved and arc arrow variants
  without claiming full TeX arrow table parity, extensible arrow layout parity,
  full TeX math class/spacing/layout parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Diagonal Crossing Arrow MathML Implementation Slice

- Focused PHP coverage is now 1,018 behavior tests and 17,240 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for Unicode-backed rising/falling
  diagonal crossing and diagonal arrow-crossing commands that were still
  falling back to literal identifiers.
- `HtmlWriter` now maps `\rdiagovfdiag`, `\fdiagovrdiag`, `\seovnearrow`,
  `\neovsearrow`, `\fdiagovnearrow`, `\rdiagovsearrow`, `\neovnwarrow`, and
  `\nwovnearrow` to native MathML operator output.
- Focused EPUB3 coverage writes `a \rdiagovfdiag b + c \fdiagovrdiag d + e
  \seovnearrow f + g \neovsearrow h` and `i \fdiagovnearrow j + k
  \rdiagovsearrow l + m \neovnwarrow n + o \nwovnearrow p`, asserts exact
  arrow serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,540 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,886 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,240 assertions, 0 failures with pass-count verification
  `pass=1018 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode diagonal crossing arrow variants
  without claiming full TeX arrow table parity, extensible arrow layout parity,
  full TeX math class/spacing/layout parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Diamond Hook Arrow MathML Implementation Slice

- Focused PHP coverage is now 1,017 behavior tests and 17,220 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional Unicode-backed
  diamond-target, diagonal cross, hooked diagonal, and corner-combination arrow
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\diamondleftarrow`, `\rightarrowdiamond`,
  `\diamondleftarrowbar`, `\barrightarrowdiamond`, `\nwsearrow`,
  `\neswarrow`, `\hknwarrow`, `\hknearrow`, `\hksearrow`, `\hkswarrow`,
  `\tona`, `\toea`, `\tosa`, and `\towa` to native MathML operator output.
- Focused EPUB3 coverage writes `a \diamondleftarrow b + c \rightarrowdiamond
  d + e \diamondleftarrowbar f + g \barrightarrowdiamond h + i \nwsearrow j
  + k \neswarrow l + m \hknwarrow n` and `o \hknearrow p + q \hksearrow r
  + s \hkswarrow t + u \tona v + w \toea x + y \tosa z + A \towa B`,
  asserts exact arrow serialization, confirms command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,520 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,866 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,220 assertions, 0 failures with pass-count verification
  `pass=1017 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode diamond, hook, and corner arrow
  variants without claiming full TeX arrow table parity, extensible arrow layout
  parity, full TeX math class/spacing/layout parity, full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Map Tail Arrow MathML Implementation Slice

- Focused PHP coverage is now 1,016 behavior tests and 17,194 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional Unicode-backed long
  map, double map, long squiggle, vertical-stroke two-headed, double-arrow-from
  bar, barred vertical, triple-arrow, dashed-arrow, dotted-arrow, arrow-to-bar,
  tail, and double-tail arrow commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps `\longmapsfrom`, `\Longmapsfrom`, `\Longmapsto`,
  `\longrightsquigarrow`, `\nvtwoheadrightarrow`, `\nVtwoheadrightarrow`,
  `\nvLeftarrow`, `\nvRightarrow`, `\nvLeftrightarrow`, `\twoheadmapsto`,
  `\Mapsfrom`, `\Mapsto`, `\downarrowbarred`, `\uparrowbarred`,
  `\Uuparrow`, `\Ddownarrow`, `\leftbkarrow`, `\rightbkarrow`,
  `\leftdbkarrow`, `\dbkarrow`, `\drbkarrow`, `\rightdotarrow`,
  `\baruparrow`, `\downarrowbar`, `\nvrightarrowtail`,
  `\nVrightarrowtail`, `\twoheadrightarrowtail`,
  `\nvtwoheadrightarrowtail`, `\nVtwoheadrightarrowtail`, `\lefttail`,
  `\righttail`, `\leftdbltail`, and `\rightdbltail` to native MathML operator
  output.
- Focused EPUB3 coverage writes `a \longmapsfrom b + c \Longmapsfrom d + e
  \Longmapsto f + g \longrightsquigarrow h + i \nvtwoheadrightarrow j + k
  \nVtwoheadrightarrow l + m \nvLeftarrow n + o \nvRightarrow p + q
  \nvLeftrightarrow r + s \twoheadmapsto t + u \Mapsfrom v + w \Mapsto x +
  y \downarrowbarred z + A \uparrowbarred B + C \Uuparrow D + E \Ddownarrow
  F` and `G \leftbkarrow H + I \rightbkarrow J + K \leftdbkarrow L + M
  \dbkarrow N + O \drbkarrow P + Q \rightdotarrow R + S \baruparrow T + U
  \downarrowbar V + W \nvrightarrowtail X + Y \nVrightarrowtail Z + a
  \twoheadrightarrowtail b + c \nvtwoheadrightarrowtail d + e
  \nVtwoheadrightarrowtail f + g \lefttail h + i \righttail j + k
  \leftdbltail l + m \rightdbltail n`, asserts exact arrow serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,494 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,840 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,194 assertions, 0 failures with pass-count verification
  `pass=1016 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode map/tail arrow variants without
  claiming full TeX arrow table parity, extensible arrow layout parity, full
  TeX math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Unicode Arrow Variant MathML Implementation Slice

- Focused PHP coverage is now 1,015 behavior tests and 17,149 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional Unicode-backed wave,
  two-headed vertical, map, angled, paired vertical, double diagonal, squiggle,
  arrow-to-bar, circled, multi-arrow, vertical-stroke negated, and open-headed
  arrow commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\leftwavearrow`, `\rightwavearrow`,
  `\twoheaduparrow`, `\twoheaddownarrow`, `\mapsfrom`, `\mapsup`,
  `\mapsdown`, `\downzigzagarrow`, `\Ldsh`, `\Rdsh`, `\updownarrows`,
  `\Nwarrow`, `\Nearrow`, `\Searrow`, `\Swarrow`, `\leftsquigarrow`,
  `\barleftarrow`, `\rightarrowbar`, `\circleonrightarrow`,
  `\downuparrows`, `\rightthreearrows`, `\nvleftarrow`, `\nvrightarrow`,
  `\nvleftrightarrow`, `\nVleftarrow`, `\nVrightarrow`,
  `\nVleftrightarrow`, `\leftarrowtriangle`, `\rightarrowtriangle`, and
  `\leftrightarrowtriangle` to native MathML operator output.
- Focused EPUB3 coverage writes `a \leftwavearrow b + c \rightwavearrow d + e
  \twoheaduparrow f + g \twoheaddownarrow h + i \mapsfrom j + k \mapsup l +
  m \mapsdown n + o \downzigzagarrow p + q \Ldsh r + s \Rdsh t + u
  \updownarrows v + w \Nwarrow x + y \Nearrow z + A \Searrow B + C
  \Swarrow D` and `E \leftsquigarrow F + G \barleftarrow H + I
  \rightarrowbar J + K \circleonrightarrow L + M \downuparrows N + O
  \rightthreearrows P + Q \nvleftarrow R + S \nvrightarrow T + U
  \nvleftrightarrow V + W \nVleftarrow X + Y \nVrightarrow Z + a
  \nVleftrightarrow b + c \leftarrowtriangle d + e \rightarrowtriangle f + g
  \leftrightarrowtriangle h`, asserts exact arrow serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,449 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,795 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,149 assertions, 0 failures with pass-count verification
  `pass=1015 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode arrow variants without claiming
  full TeX arrow table parity, extensible arrow layout parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Unicode Binary Operator MathML Implementation Slice

- Focused PHP coverage is now 1,014 behavior tests and 17,107 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional Unicode-backed dot,
  slash, double cup/cap, wedge/vee-dot, barred cup/cap, interior-product,
  interleave, squared, circled, and outer-join binary operator commands that
  were still falling back to literal identifiers.
- `HtmlWriter` now maps `\dotplus`, `\divslash`, `\doublecup`,
  `\doublecap`, `\veedot`, `\wedgedot`, `\barcup`, `\barcap`,
  `\intprod`, `\intprodr`, `\varveebar`, `\interleave`, `\sslash`,
  `\talloblong`, `\boxbar`, `\boxdiag`, `\boxbslash`, `\boxast`,
  `\boxcircle`, `\boxbox`, `\circlehbar`, `\circledvert`,
  `\circledparallel`, `\obslash`, `\operp`, `\obar`, `\olessthan`,
  `\ogreaterthan`, `\leftouterjoin`, `\rightouterjoin`, `\fullouterjoin`,
  `\bigbot`, and `\bigtop` to native MathML operator output.
- Focused EPUB3 coverage writes `a \dotplus b + c \divslash d + e
  \doublecup f + g \doublecap h + i \veedot j + k \wedgedot l + m
  \barcup n + o \barcap p + q \intprod r + s \intprodr t + u \varveebar v
  + w \interleave x + y \sslash z + A \talloblong B` and `C \boxbar D + E
  \boxdiag F + G \boxbslash H + I \boxast J + K \boxcircle L + M \boxbox N
  + O \circlehbar P + Q \circledvert R + S \circledparallel T + U
  \obslash V + W \operp X + Y \obar Z + a \olessthan b + c \ogreaterthan d
  + e \leftouterjoin f + g \rightouterjoin h + i \fullouterjoin j + k
  \bigbot l + m \bigtop n`, asserts exact operator serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,407 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,753 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,107 assertions, 0 failures with pass-count verification
  `pass=1014 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode binary operator variants without
  claiming full TeX symbol table parity, full TeX math class/spacing/layout
  parity, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation,
  DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Unicode Relation Operator MathML Implementation Slice

- Focused PHP coverage is now 1,013 behavior tests and 17,062 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional Unicode-backed
  negated curly/square/triangle relations, equality/parallel relations, circled
  equality, and binary operator commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps negated relations `\npreccurlyeq`, `\nsucccurlyeq`,
  `\nsqsubseteq`, `\nsqsupseteq`, `\nvartriangleleft`,
  `\nvartriangleright`, `\ntrianglelefteq`, and `\ntrianglerighteq`; relation
  commands `\eqless`, `\eqgtr`, and `\equalparallel`; circled equality
  `\circledequal`; and binary operators `\cupleftarrow`, `\cupdot`,
  `\divideontimes`, and `\barvee` to native MathML operator output.
- Focused EPUB3 coverage writes `A \npreccurlyeq B + C \nsucccurlyeq D + E
  \nsqsubseteq F + G \nsqsupseteq H + I \nvartriangleleft J + K
  \nvartriangleright L + M \ntrianglelefteq N + O \ntrianglerighteq P` and
  `Q \eqless R + S \eqgtr T + U \equalparallel V + W \circledequal X + a
  \cupleftarrow b + c \cupdot d + e \divideontimes f + g \barvee h`, asserts
  exact relation and binary-operator serialization, confirms command names do
  not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,362 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,708 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,062 assertions, 0 failures with pass-count verification
  `pass=1013 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX Unicode relation and binary operator
  variants without claiming full TeX symbol table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Extended Arrow Harpoon MathML Implementation Slice

- Focused PHP coverage is now 1,012 behavior tests and 17,034 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for curved arrows, circular arrows,
  shaft-turn arrows, paired vertical arrows, vertical harpoons, squiggle/double
  arrows, restriction, and multimap commands that were still falling back to
  literal identifiers.
- `HtmlWriter` now maps curved/circular arrows `\curvearrowleft`,
  `\curvearrowright`, `\circlearrowleft`, and `\circlearrowright`; shaft-turn
  arrows `\Lsh` and `\Rsh`; paired vertical arrows `\upuparrows` and
  `\downdownarrows`; vertical harpoons `\upharpoonleft`, `\upharpoonright`,
  `\downharpoonleft`, and `\downharpoonright`; squiggle/double arrows
  `\leftrightsquigarrow`, `\Lleftarrow`, and `\Rrightarrow`; and relation-like
  arrow commands `\restriction` and `\multimap` to native MathML operator
  output.
- Focused EPUB3 coverage writes `a \curvearrowleft b + c \curvearrowright d +
  e \circlearrowleft f + g \circlearrowright h + i \Lsh j + k \Rsh l + m
  \upuparrows n + o \downdownarrows p` and `q \upharpoonleft r + s
  \upharpoonright t + u \downharpoonleft v + w \downharpoonright x + A
  \leftrightsquigarrow B + C \Lleftarrow D + E \Rrightarrow F + S
  \restriction T + U \multimap V`, asserts exact arrow/harpoon serialization,
  confirms command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,334 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,680 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,034 assertions, 0 failures with pass-count verification
  `pass=1012 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX extended arrow and harpoon variants without
  claiming full TeX arrow table parity, extensible arrow layout parity, full TeX
  math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Precedence Relation MathML Implementation Slice

- Focused PHP coverage is now 1,011 behavior tests and 17,005 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional precedence,
  succession, negated ordering, approximate ordering, and slanted-equality
  relation commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps precedence/succession relations `\preccurlyeq`,
  `\succcurlyeq`, `\curlyeqprec`, `\curlyeqsucc`, `\precneqq`, `\succneqq`,
  `\precnsim`, `\succnsim`, `\precnapprox`, `\succnapprox`, `\nprecsim`, and
  `\nsuccsim`; negated ordering relations `\nleqq`, `\ngeqq`,
  `\lvertneqq`, `\gvertneqq`, `\nlesssim`, `\ngtrsim`, `\nlessapprox`, and
  `\ngtrapprox`; and slanted-equality relations `\eqslantless` and
  `\eqslantgtr` to native MathML operator output.
- Focused EPUB3 coverage writes `a \preccurlyeq b + c \succcurlyeq d + e
  \curlyeqprec f + g \curlyeqsucc h + i \precneqq j + k \succneqq l + m
  \precnsim n + o \succnsim p + q \precnapprox r + s \succnapprox t` and `A
  \nleqq B + C \ngeqq D + E \lvertneqq F + G \gvertneqq H + I \nlesssim J +
  K \ngtrsim L + M \nlessapprox N + O \ngtrapprox P + Q \nprecsim R + S
  \nsuccsim T + U \eqslantless V + W \eqslantgtr X`, asserts exact precedence,
  succession, negated ordering, approximate ordering, and slanted-equality
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,305 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,651 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  17,005 assertions, 0 failures with pass-count verification
  `pass=1011 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX precedence and negated ordering relation
  variants without claiming full TeX relation table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Miscellaneous Symbol MathML Implementation Slice

- Focused PHP coverage is now 1,010 behavior tests and 16,971 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional letter-like, set,
  triangle/shape, diagonal, circled, and triangle-relation symbol commands that
  were still falling back to literal identifiers.
- `HtmlWriter` now maps letter-like symbols `\Finv`, `\Game`, `\mho`, `\eth`,
  and `\Bbbk`; the set symbol `\complement`; triangle/shape symbols
  `\vartriangle`, `\triangledown`, `\blacktriangle`, `\blacktriangledown`,
  `\blacklozenge`, and `\bigstar`; diagonal/circled/reversed symbols
  `\diagup`, `\diagdown`, `\circledS`, and `\backepsilon`; and
  triangle-relation variants `\vartriangleleft`, `\vartriangleright`,
  `\trianglelefteq`, `\trianglerighteq`, `\blacktriangleleft`, and
  `\blacktriangleright` to native MathML output.
- Focused EPUB3 coverage writes `A \complement B + C \Finv D + E \Game F + G
  \mho H + I \eth J + K \Bbbk L` and `M \vartriangle N + O \triangledown P +
  Q \blacktriangle R + S \blacktriangledown T + U \blacklozenge V + W \bigstar
  X + a \diagup b + c \diagdown d + e \circledS f + g \backepsilon h + i
  \vartriangleleft j + k \vartriangleright l + m \trianglelefteq n + o
  \trianglerighteq p + q \blacktriangleleft r + s \blacktriangleright t`,
  asserts exact symbol serialization, confirms command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,271 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,617 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,971 assertions, 0 failures with pass-count verification
  `pass=1010 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX miscellaneous symbol variants without
  claiming full TeX symbol table parity, full TeX math class/spacing/layout
  parity, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation,
  DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Subset Relation MathML Implementation Slice

- Focused PHP coverage is now 1,009 behavior tests and 16,937 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional subset, superset,
  approximation, negated subset/superset, and square-subset strict relation
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps subset/superset equality relations `\subseteqq` and
  `\supseteqq`; approximation relations `\subsetapprox` and `\supsetapprox`;
  high-fidelity strict unequal relations `\subsetneqq` and `\supsetneqq`;
  negated relations `\nsubset`, `\nsupset`, `\nsubseteqq`, and
  `\nsupseteqq`; and square-subset strict variants `\sqsubsetneq`,
  `\sqsupsetneq`, `\sqsubsetneqq`, and `\sqsupsetneqq` to native MathML
  operator output.
- Focused EPUB3 coverage writes `A \subseteqq B + C \supseteqq D + E
  \subsetapprox F + G \supsetapprox H + I \subsetneqq J + K \supsetneqq L`
  and `M \nsubset N + O \nsupset P + Q \nsubseteqq R + S \nsupseteqq T + U
  \sqsubsetneq V + W \sqsupsetneq X + Y \sqsubsetneqq Z + P \sqsupsetneqq
  Q`, asserts exact subset, superset, approximation, negated, and
  square-subset strict serialization, confirms command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,237 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,583 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,937 assertions, 0 failures with pass-count verification
  `pass=1009 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX subset and superset relation variants
  without claiming full TeX relation table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Equality Relation MathML Implementation Slice

- Focused PHP coverage is now 1,008 behavior tests and 16,911 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional equality-shape,
  similarity, reverse-similarity, proportionality, and short-divisibility
  relation commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps equality-shape relations `\bumpeq`, `\Bumpeq`,
  `\circeq`, `\triangleq`, `\eqcirc`, and `\questeq`; similarity relations
  `\thicksim` and `\thickapprox`; reverse-similarity relations `\backsim` and
  `\backsimeq`; proportionality alias `\varpropto`; and short-divisibility
  relations `\shortmid`, `\shortparallel`, `\nshortmid`, and
  `\nshortparallel` to native MathML operator output.
- Focused EPUB3 coverage writes `a \bumpeq b + c \Bumpeq d + e \circeq f + g
  \triangleq h + i \eqcirc j + k \questeq l` and `m \thicksim n + o
  \thickapprox p + q \backsim r + s \backsimeq t + u \varpropto v + w
  \shortmid x + y \shortparallel z + A \nshortmid B + C \nshortparallel D`,
  asserts exact equality-shape, similarity, reverse-similarity,
  proportionality, and short-divisibility serialization, confirms command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,211 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,557 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,911 assertions, 0 failures with pass-count verification
  `pass=1008 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX equality and similarity relation variants
  without claiming full TeX relation table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Ordering Relation MathML Implementation Slice

- Focused PHP coverage is now 1,007 behavior tests and 16,884 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional ordering, mixed
  comparison, dotted comparison, and equality-similarity relation commands that
  were still falling back to literal identifiers.
- `HtmlWriter` now maps ordering relations `\ll`, `\gg`, `\lll`, `\llless`,
  `\ggg`, and `\gggtr`; mixed comparison relations `\lessgtr`, `\gtrless`,
  `\lesseqgtr`, `\gtreqless`, `\lesseqqgtr`, and `\gtreqqless`; dotted
  comparison relations `\lessdot` and `\gtrdot`; and equality-similarity
  relations `\eqsim`, `\risingdotseq`, and `\fallingdotseq` to native MathML
  operator output.
- Focused EPUB3 coverage writes `a \ll b + c \gg d + e \lll f + g \ggg h + i
  \llless j + k \gggtr l + m \lessgtr n + o \gtrless p` and `m \lesseqgtr n
  + o \gtreqless p + q \lesseqqgtr r + s \gtreqqless t + u \lessdot v + w
  \gtrdot x + y \eqsim z + A \risingdotseq B + C \fallingdotseq D`, asserts
  exact ordering, mixed comparison, dotted comparison, and equality-similarity
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,184 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,530 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,884 assertions, 0 failures with pass-count verification
  `pass=1007 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX ordering and comparison relation variants
  without claiming full TeX relation table parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Arrow Alias MathML Implementation Slice

- Focused PHP coverage is now 1,006 behavior tests and 16,855 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional arrow aliases and
  negated/paired arrow commands that were still falling back to literal
  identifiers.
- `HtmlWriter` now maps alias arrows `\gets`, `\implies`, `\impliedby`, and
  `\iff`; negated arrows `\nleftarrow`, `\nrightarrow`, `\nleftrightarrow`,
  `\nLeftarrow`, `\nRightarrow`, and `\nLeftrightarrow`; dashed arrows
  `\dashleftarrow` and `\dashrightarrow`; paired arrows `\leftleftarrows`,
  `\rightrightarrows`, `\leftrightarrows`, and `\rightleftarrows`; and loop
  arrows `\looparrowleft` and `\looparrowright` to native MathML operator
  output.
- Focused EPUB3 coverage writes `a \gets b + c \implies d + e \impliedby f +
  g \iff h + p \dashleftarrow q + r \dashrightarrow s` and `p \nleftarrow q +
  r \nrightarrow s + t \nleftrightarrow u + v \nLeftarrow w + x \nRightarrow
  y + z \nLeftrightarrow A + B \leftleftarrows C + D \rightrightarrows E + F
  \leftrightarrows G + H \rightleftarrows I + J \looparrowleft K + L
  \looparrowright M`, asserts exact alias, negated, dashed, paired, and loop
  arrow serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,155 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,501 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,855 assertions, 0 failures with pass-count verification
  `pass=1006 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX arrow aliases and negated/paired arrows
  without claiming full TeX arrow table parity, extensible arrow layout parity,
  full TeX math class/spacing/layout parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Boxed Circled Binary Operator MathML Implementation Slice

- Focused PHP coverage is now 1,005 behavior tests and 16,825 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for boxed, cross-product,
  curly/bar-wedge, double cup/cap, intercal, and circled binary operator
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps boxed operators `\boxplus`, `\boxminus`, `\boxtimes`,
  and `\boxdot`; cross-product operators `\ltimes`, `\rtimes`,
  `\leftthreetimes`, and `\rightthreetimes`; curly/bar-wedge operators
  `\curlyvee`, `\curlywedge`, `\barwedge`, `\veebar`, and
  `\doublebarwedge`; double cup/cap and intercal operators `\Cup`, `\Cap`, and
  `\intercal`; and circled operators `\circledast`, `\circledcirc`, and
  `\circleddash` to native MathML operator output.
- Focused EPUB3 coverage writes `a \boxplus b + c \boxminus d + e \boxtimes f
  + g \boxdot h + p \ltimes q + r \rtimes s + u \leftthreetimes v + w
  \rightthreetimes z` and `A \curlywedge B + C \curlyvee D + E \barwedge F +
  G \veebar H + I \doublebarwedge J + x \Cup y + z \Cap w + m \intercal n +
  r \circledast s + t \circledcirc u + v \circleddash w`, asserts exact
  boxed, cross-product, curly/bar-wedge, double cup/cap, intercal, and circled
  operator serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,125 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,471 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,825 assertions, 0 failures with pass-count verification
  `pass=1005 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX boxed, circled, and related binary
  operators without claiming full TeX binary-operator table parity, full TeX
  math class/spacing/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Binary Shape Operator MathML Implementation Slice

- Focused PHP coverage is now 1,004 behavior tests and 16,794 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for common binary and shape operator
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\amalg`, `\wr`, `\dag`, `\dagger`, `\ddag`,
  `\ddagger`, `\diamond`, `\bigcirc`, `\triangleleft`, `\triangleright`,
  `\lhd`, `\rhd`, `\unlhd`, `\unrhd`, `\bigtriangleup`, and
  `\bigtriangledown` to native MathML operator output.
- Focused EPUB3 coverage writes `A \amalg B + C \wr D + E \dagger F + G
  \ddagger H + a \diamond b` and `x \bigcirc y + p \triangleleft q + r
  \triangleright s + t \bigtriangleup u + v \bigtriangledown w + c \unlhd d
  + e \unrhd f`, asserts exact amalgam, wreath, dagger, diamond, circle,
  triangle, and triangle-equality serialization, confirms command names do not
  leak as literal MathML identifiers, and round-trips both equations as native
  math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,094 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,440 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,794 assertions, 0 failures with pass-count verification
  `pass=1004 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX binary and shape operators without claiming
  full TeX binary-operator table parity, full TeX math class/spacing/layout
  parity, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation,
  DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Integral Variant MathML Implementation Slice

- Focused PHP coverage is now 1,003 behavior tests and 16,770 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for common integral variants that
  were still falling back to literal identifiers.
- `HtmlWriter` now maps `\intop`, `\smallint`, `\oint`, `\iint`, `\iiint`,
  `\iiiint`, `\idotsint`, `\oiint`, and `\oiiint` to native MathML operator
  output. The same integral operators now participate in the existing
  `\limits` handling path, so bounded uses such as `\oiint\limits_S` and
  `\idotsint\limits_R` emit under-script MathML instead of leaking limit
  commands as identifiers.
- Focused EPUB3 coverage writes `\oint_C f(z)\,dz + \iint_D g(x,y)\,dxdy +
  \iiint_E h\,dV + \smallint_a^b f` and `\oiint\limits_S F\cdot n +
  \oiiint_V h + \iiiint_Q k + \idotsint\limits_R m`, asserts exact contour,
  double, triple, quadruple, dotted, closed-surface, small-integral, spacing,
  and limit serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,070 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,416 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,770 assertions, 0 failures with pass-count verification
  `pass=1003 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX integral variants without claiming full TeX
  integral table parity, full TeX math class/spacing/layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Relation Variant MathML Implementation Slice

- Focused PHP coverage is now 1,002 behavior tests and 16,749 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional relation commands that
  were still falling back to literal identifiers.
- `HtmlWriter` now maps approximation and ordering relations including
  `\lesssim`, `\gtrsim`, `\lessapprox`, `\gtrapprox`, `\prec`, `\succ`,
  `\preceq`, `\succeq`, `\precsim`, `\succsim`, `\precapprox`,
  `\succapprox`, `\asymp`, `\approxeq`, `\doteq`, `\Doteq`, and
  `\doteqdot`; square/subset relations including `\Subset`, `\Supset`,
  `\sqsubset`, `\sqsupset`, `\sqsubseteq`, and `\sqsupseteq`; direct negated
  relation commands including `\nleq`, `\ngeq`, `\nless`, `\ngtr`,
  `\nleqslant`, `\ngeqslant`, `\lneq`, `\gneq`, `\lneqq`, `\gneqq`,
  `\lnsim`, `\gnsim`, `\lnapprox`, `\gnapprox`, `\npreceq`, `\nsucceq`,
  `\nprec`, and `\nsucc`; and relation operators including `\bowtie`, `\Join`,
  `\between`, `\pitchfork`, `\smile`, and `\frown`.
- The existing `\not` relation parser now emits single negated MathML operators
  for the same bounded family where standard negated symbols are available,
  including `\not\preceq` and `\not\sqsubseteq`.
- Focused EPUB3 coverage writes `a \lesssim b + c \gtrsim d + p \preceq q + r
  \succeq s` and `E \sqsubseteq F + G \sqsupseteq H + x \bowtie y + m \smile n
  + p \frown q + X \not\preceq Y + Z \nleq T`, asserts exact additional
  relation serialization, confirms command names do not leak as literal MathML
  identifiers, confirms negated variants are emitted as single operators, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,049 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,395 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,749 assertions, 0 failures with pass-count verification
  `pass=1002 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX relation variants without claiming full TeX
  relation table parity, full TeX math class/spacing/layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Miscellaneous Symbol MathML Implementation Slice

- Focused PHP coverage is now 1,001 behavior tests and 16,726 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for common miscellaneous symbols that
  were still falling back to literal identifiers.
- `HtmlWriter` now maps logic commands including `\neg`, `\lnot`, `\top`, and
  `\bot`; turnstile and model-relation commands including `\vdash`, `\dashv`,
  `\models`, `\vDash`, `\Vdash`, `\Vvdash`, `\nvdash`, `\nvDash`, `\nVdash`,
  and `\nVDash`; and suit, music, box, and diamond commands including
  `\clubsuit`, `\spadesuit`, `\heartsuit`, `\diamondsuit`, `\flat`,
  `\natural`, `\sharp`, `\Box`, `\square`, `\Diamond`, and `\lozenge` to
  native MathML operator output.
- Focused EPUB3 coverage writes `\neg P + \top \vdash Q + R \models S + \bot`
  and `\clubsuit + \spadesuit + \heartsuit + \diamondsuit + \flat + \natural
  + \sharp + \Box + \Diamond`, asserts exact logic, turnstile, model, suit,
  music, box, and diamond serialization, confirms command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,026 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,372 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,726 assertions, 0 failures with pass-count verification
  `pass=1001 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX miscellaneous symbols without claiming full
  TeX symbol table parity, full TeX math class/spacing/layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Arrow Variant MathML Implementation Slice

- Focused PHP coverage is now 1,000 behavior tests and 16,700 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for common arrow variant commands
  that were still falling back to literal identifiers.
- `HtmlWriter` now maps vertical and diagonal arrows including `\uparrow`,
  `\downarrow`, `\updownarrow`, `\Uparrow`, `\Downarrow`, `\Updownarrow`,
  `\nearrow`, `\searrow`, `\swarrow`, and `\nwarrow`; maps extended arrow
  aliases including `\longmapsto`, `\leadsto`, `\rightsquigarrow`,
  `\twoheadrightarrow`, `\twoheadleftarrow`, `\rightarrowtail`, and
  `\leftarrowtail`; and maps harpoon variants including `\rightharpoonup`,
  `\rightharpoondown`, `\leftharpoonup`, `\leftharpoondown`,
  `\rightleftharpoons`, and `\leftrightharpoons` to MathML `<mo>` output.
- Focused EPUB3 coverage writes `A \uparrow B + C \Downarrow D + E
  \updownarrow F + G \nearrow H` and `x \longmapsto y + p \rightsquigarrow q
  + r \twoheadrightarrow s + t \rightarrowtail u + v \leftharpoonup w`,
  asserts exact vertical, double, diagonal, long-mapsto, squiggle, two-headed,
  tail, and harpoon arrow serialization, confirms command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 6,000 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,346 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,700 assertions, 0 failures with pass-count verification
  `pass=1000 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX arrow variants without claiming full TeX
  arrow table parity, extensible arrow layout parity, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Ellipsis Punctuation MathML Implementation Slice

- Focused PHP coverage is now 999 behavior tests and 16,679 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for common ellipsis and punctuation
  commands that were still falling back to literal identifiers.
- `HtmlWriter` now maps `\dots`, `\ldots`, `\mathellipsis`, `\dotsc`, and
  `\dotso` to MathML horizontal ellipsis operators; maps `\cdots`, `\dotsb`,
  `\dotsm`, and `\dotsi` to centered ellipsis operators; maps `\vdots`,
  `\ddots`, and `\iddots` to vertical and diagonal ellipsis operators; and maps
  `\colon`, `\ldotp`, and `\cdotp` to punctuation/operator MathML output.
- Focused EPUB3 coverage writes `a_1 \ldots a_n + x \cdots y + f\colon A \to
  B` and `\begin{matrix} 1 & \dots & n \\\\ \vdots & \ddots & \vdots
  \end{matrix} + p\ldotp q + r\cdotp s`, asserts exact horizontal, centered,
  vertical, diagonal, colon, dot, and centered-dot serialization, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,979 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,325 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,679 assertions, 0 failures with pass-count verification `pass=999 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX ellipsis and punctuation commands without
  claiming full TeX context-sensitive `\dots` selection, full TeX math
  class/spacing/layout parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Symbol Variant MathML Implementation Slice

- Focused PHP coverage is now 998 behavior tests and 16,659 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for common symbol commands that were
  still falling back to literal identifiers.
- `HtmlWriter` now maps Greek variants including `\varpi`, `\varrho`,
  `\varsigma`, `\digamma`, and `\varkappa` to MathML `<mi>` symbol output,
  maps letter-like symbols including `\aleph`, `\beth`, `\gimel`, `\daleth`,
  `\ell`, `\hbar`, `\hslash`, `\imath`, `\jmath`, `\wp`, `\Re`, and `\Im` to
  MathML `<mi>` output, and maps set/logical/geometry symbols including
  `\emptyset`, `\varnothing`, `\forall`, `\exists`, `\angle`,
  `\measuredangle`, `\sphericalangle`, `\triangle`, `\therefore`, `\because`,
  `\prime`, and `\backprime` to MathML `<mo>` output.
- Focused EPUB3 coverage writes `\varpi + \varsigma + \digamma + \ell + \hbar`
  and `\emptyset \subseteq A \land \forall x \in B, \exists y \notin C
  \angle ABC + f\prime`, asserts exact Greek-variant, letter-symbol,
  set/logical, angle, and prime serialization, confirms symbol command names do
  not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,959 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,305 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,659 assertions, 0 failures with pass-count verification `pass=998 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc/TeX symbol commands without claiming full TeX
  symbol table parity, full TeX math class/spacing/layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Modulo MathML Implementation Slice

- Focused PHP coverage is now 997 behavior tests and 16,637 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded modulo commands.
- `HtmlWriter` now maps `\bmod` to MathML `<mo>mod</mo>`, parses bounded
  arguments for `\mod`, `\pmod`, and `\pod`, emits `\mod` as a normal `mod`
  label plus argument, emits `\pmod` as a parenthesized normal `mod` label plus
  argument, and emits `\pod` as the parenthesized argument without a `mod`
  label.
- Focused EPUB3 coverage writes `a \bmod n + x \pmod{m+1}` and
  `y \mod {k_2} + z \pod{r+s}`, asserts exact operator, normal-label,
  parenthesized modulo, and parenthesized pod serialization, confirms modulo
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,937 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,283 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,637 assertions, 0 failures with pass-count verification `pass=997 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common bounded Pandoc/TeX modulo constructs without claiming
  full TeX spacing/layout parity, full amsmath modulo spacing parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Enclosure MathML Implementation Slice

- Focused PHP coverage is now 996 behavior tests and 16,621 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded enclosure commands.
- `HtmlWriter` now maps `\boxed` and `\fbox` to MathML `<menclose
  notation="box">`, maps `\cancel` and `\bcancel` to diagonal strike
  enclosures, maps `\xcancel` to a two-diagonal strike enclosure, and treats
  `<menclose>` as a single MathML row element so standalone enclosure output is
  not redundantly wrapped.
- Focused EPUB3 coverage writes `\boxed{x+1} + \cancel{y_2} + \bcancel{z}` and
  `\fbox{\frac{a}{b}} + \xcancel{\sqrt{n}}`, asserts exact box,
  up-diagonal, down-diagonal, and crossed enclosure serialization, confirms
  enclosure command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,921 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,267 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,621 assertions, 0 failures with pass-count verification `pass=996 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common bounded Pandoc/TeX enclosure constructs without
  claiming full cancel-package parity, full enclosure layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX General Fraction MathML Implementation Slice

- Focused PHP coverage is now 995 behavior tests and 16,604 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded `\genfrac` commands.
- `HtmlWriter` now parses the six `\genfrac` arguments, converts raw delimiter
  groups such as `(`, `)`, `\langle`, `\rangle`, `\lbrace`, and `\rbrace` into
  MathML stretchy fence operators, maps empty or zero line-thickness groups to
  ordinary or no-line `<mfrac>` output, preserves non-zero thickness values as
  `linethickness`, and maps TeX style numbers `0`, `1`, `2`, and `3` to bounded
  MathML `<mstyle>` wrappers.
- Focused EPUB3 coverage writes `\genfrac{(}{)}{0pt}{}{n}{k} +
  \genfrac{\langle}{\rangle}{1px}{0}{a+b}{c}` and
  `\genfrac{[}{]}{}{1}{x_1}{y} +
  \genfrac{\lbrace}{\rbrace}{0pt}{2}{p}{q}`, asserts exact no-line,
  custom-thickness, displaystyle, scriptlevel, and fenced fraction
  serialization, confirms `genfrac` and delimiter command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,904 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,250 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,604 assertions, 0 failures with pass-count verification `pass=995 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common bounded Pandoc/TeX general fraction constructs without
  claiming full TeX fraction-layout parity, full arbitrary delimiter sizing
  parity, full TeX style propagation parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Substack Subarray MathML Implementation Slice

- Focused PHP coverage is now 994 behavior tests and 16,587 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded multi-line script
  arguments.
- `HtmlWriter` now maps `\substack{...}` to centered MathML `<mtable>` output
  and recognizes `\begin{subarray}{...}...\end{subarray}` through the existing
  table renderer with column alignment from the TeX column spec. Nested
  `array` and `subarray` environments are now tracked while splitting table
  rows/cells so their bodies do not corrupt surrounding MathML table parsing.
- Focused EPUB3 coverage writes `\sum\limits_{\substack{i=1\\j=2}} a_{ij} +
  x^{\substack{p\\q}}` and `\prod\limits_{\begin{subarray}{r}
  n>0\\m<n \end{subarray}} b_n + \lim\limits_{\begin{subarray}{c}
  x\to0\\x>0 \end{subarray}} f(x)`, asserts exact centered and right-aligned
  `<mtable>` serialization inside `<munder>` and `<msup>` scripts, confirms
  `substack` and `subarray` do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,887 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,233 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,587 assertions, 0 failures with pass-count verification `pass=994 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common multi-line Pandoc/TeX script arguments without
  claiming full TeX scriptstyle sizing parity, full subarray option/layout
  parity, arbitrary nested table environment parity, full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Operator MathML Implementation Slice

- Focused PHP coverage is now 993 behavior tests and 16,573 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional bounded operator
  commands.
- `HtmlWriter` now maps bounded binary/arithmetic commands such as `\div`,
  `\ast`, `\star`, `\circ`, and `\bullet`; set/logical/circled commands such
  as `\setminus`, `\smallsetminus`, `\land`, `\lor`, `\oplus`, `\ominus`,
  `\otimes`, `\oslash`, and `\odot`; and arrow aliases such as `\mapsto`,
  `\hookrightarrow`, `\hookleftarrow`, long arrow variants, and
  `\Leftrightarrow` to MathML `<mo>` operators instead of literal identifier
  fallback.
- Focused EPUB3 coverage writes `m \div n + a \ast b + c \star d + A
  \setminus B` and `p \land q + r \lor s + u \oplus v + w \otimes z + f
  \mapsto g + A \Leftrightarrow B`, asserts exact arithmetic, set, logical,
  circled, mapsto, and double-arrow operator serialization, confirms command
  names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,873 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,219 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,573 assertions, 0 failures with pass-count verification `pass=993 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common bounded Pandoc operator variants without claiming full
  TeX operator table parity, full TeX spacing/class/layout parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Additional Accent MathML Implementation Slice

- Focused PHP coverage is now 992 behavior tests and 16,551 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for additional bounded accent
  commands.
- `HtmlWriter` now maps `\dot`, `\ddot`, `\breve`, `\check`, `\acute`,
  `\grave`, and `\mathring` to MathML `<mover accent="true">` output with
  direct accent operators, and maps `\overrightarrow`, `\overleftarrow`, and
  `\overleftrightarrow` to over-arrow accent output.
- The parser now also maps `\underleftarrow`, `\underrightarrow`, and
  `\underleftrightarrow` to MathML `<munder accentunder="true">` output,
  complementing the existing `\underline` path.
- Focused EPUB3 coverage writes `\dot{x} + \ddot{y} + \breve{z} + \check{w}`
  and `\acute{\alpha} + \grave{\beta} + \mathring{A} +
  \overrightarrow{AB} + \underleftarrow{CD}`, asserts exact over- and
  under-accent serialization for symbol, Greek, grouped-letter, and arrow
  accents, confirms command names do not leak as literal MathML identifiers,
  and round-trips both equations as native math nodes with `mathml` spine
  manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,851 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,197 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,551 assertions, 0 failures with pass-count verification `pass=992 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common bounded Pandoc accent constructs without claiming full
  TeX accent parser parity, arbitrary stretch/layout parity, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Color Phantom Text MathML Implementation Slice

- Focused PHP coverage is now 991 behavior tests and 16,530 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded color, phantom, smash,
  and text-box commands.
- `HtmlWriter` now maps two-argument `\color{...}{...}` and
  `\textcolor{...}{...}` forms to MathML `<mstyle mathcolor="...">` wrappers,
  and maps `\colorbox{...}{...}` to `<mstyle mathbackground="...">`.
- `\phantom`, `\hphantom`, and `\vphantom` now emit MathML `<mphantom>` around
  a parsed bounded argument, while `\smash` emits `<mpadded height="0"
  depth="0">`. `\mbox` and `\hbox` now emit `<mtext>` content instead of
  falling back to literal command identifiers.
- Focused EPUB3 coverage writes `\textcolor{blue}{x_1} +
  \color{#cc0000}{\frac{a}{b}} + \phantom{z}` and
  `\colorbox{yellow}{\text{rate}} + \hphantom{y_2} +
  \vphantom{\frac{1}{2}} + \smash{x+y} + \mbox{units}`, asserts exact
  `mathcolor`, `mathbackground`, `mphantom`, `mpadded`, and `mtext`
  serialization, confirms command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with
  `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,830 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,176 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,530 assertions, 0 failures with pass-count verification `pass=991 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common bounded Pandoc color, phantom, smash, and text-box
  constructs without claiming full TeX color declaration scope parity, full
  `hphantom`/`vphantom` layout parity, full `smash` option parity, full TeX
  text-mode parsing parity, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation, DRM decryption, XML signature cryptographic validation, or
  broader media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Infix Fraction MathML Implementation Slice

- Focused PHP coverage is now 990 behavior tests and 16,510 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded infix fraction commands.
- `HtmlWriter` now recognizes row-level `\over`, `\atop`, `\choose`, `\brack`,
  and `\brace` after a parsed numerator and consumes the rest of the current
  bounded row as the denominator, instead of leaking those command names as
  literal MathML identifiers.
- The infix renderer emits lined MathML `<mfrac>` for `\over`, no-line
  `linethickness="0"` fractions for `\atop`, and parenthesized, bracketed, or
  braced no-line fractions for `\choose`, `\brack`, and `\brace`. The same
  detection is wired into the `\left...\right` row parser so fenced infix
  fractions can stop cleanly at the matching `\right`.
- Focused EPUB3 coverage writes `{a+b \over c+d} + {n \choose k}` and
  `\left( x+1 \over y_2 \right) + {p \atop q} + {r \brack s} + {u \brace v}`,
  asserts exact lined, no-line, parenthesized, bracketed, braced, and
  left/right-fenced serialization, confirms infix command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,810 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,156 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,510 assertions, 0 failures with pass-count verification `pass=990 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common bounded Pandoc infix fraction constructs without
  claiming full TeX math parser precedence, arbitrary nested infix-form parity,
  full math style/layout parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Fraction Family MathML Implementation Slice

- Focused PHP coverage is now 989 behavior tests and 16,493 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded fraction-style aliases
  and binomial commands.
- `HtmlWriter` now maps `\dfrac` and `\tfrac` to MathML `<mfrac>` output
  wrapped in `mstyle displaystyle="true"` or `mstyle displaystyle="false"`,
  preserving the existing `\frac` behavior while avoiding literal command-name
  fallback.
- `\binom`, `\dbinom`, and `\tbinom` now parse two bounded arguments and emit
  parenthesized no-line MathML fractions with `linethickness="0"`, with the
  display/text variants carrying the corresponding `mstyle` displaystyle
  wrapper.
- Focused EPUB3 coverage writes `\dfrac{x+1}{y_2} + \tfrac{1}{2}` and
  `\binom{n}{k} + \dbinom{a+b}{c} + \tbinom{p}{q_1}`, asserts exact
  displaystyle, no-line fraction, scripted denominator, and parenthesized
  binomial serialization, confirms fraction command names do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,793 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,139 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,493 assertions, 0 failures with pass-count verification `pass=989 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc fraction-family constructs without claiming
  full TeX infix `\over`, `\atop`, or `\choose` parser parity, full math style
  layout parity, full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Delimiter Family MathML Implementation Slice

- Focused PHP coverage is now 988 behavior tests and 16,476 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded delimiter-family
  commands in ordinary operator and `\left...\right` positions.
- `HtmlWriter` now exposes delimiter-family commands as MathML operators for
  `\lceil`, `\rceil`, `\lfloor`, `\rfloor`, `\lbrace`, `\rbrace`,
  `\lVert`, `\rVert`, `\vert`, `\backslash`, and related paired delimiter
  aliases instead of falling back to literal identifiers.
- The `\left...\right` delimiter reader now also recognizes `\lbrack`,
  `\rbrack`, `\lparen`, and `\rparen`, so command aliases can participate in
  stretchy fence serialization alongside existing angle, floor, ceiling, brace,
  vertical-bar, double-vertical-bar, and invisible delimiter handling.
- Focused EPUB3 coverage writes `\lceil x \rceil + \lVert v \rVert +
  \lbrace a,b \rbrace` and `\left\lfloor \frac{x}{2} \right\rfloor +
  \left\lbrack y \middle\vert z \right\rbrack + \backslash`, asserts exact
  direct and stretchy delimiter serialization, confirms delimiter command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,776 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,122 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,476 assertions, 0 failures with pass-count verification `pass=988 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc delimiter-family constructs without claiming
  full TeX delimiter parser parity, arbitrary delimiter sizing/layout parity,
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Relation Variant MathML Implementation Slice

- Focused PHP coverage is now 987 behavior tests and 16,453 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded relation variants and
  TeX `\not` relation prefixes.
- `HtmlWriter` now maps common relation commands including `\approx`, `\sim`,
  `\simeq`, `\equiv`, `\cong`, `\propto`, `\parallel`, `\perp`, `\mid`,
  `\nmid`, `\leqq`, `\geqq`, `\subsetneq`, `\subsetneqq`, `\supsetneq`,
  `\supsetneqq`, `\nsubseteq`, and `\nsupseteq` to MathML `<mo>` operators
  instead of falling back to literal identifiers.
- The parser now recognizes bounded `\not` prefix forms for common following
  relation commands and symbols, including `\not\in`, `\not=`, `\not\equiv`,
  `\not\approx`, `\not\subseteq`, `\not\parallel`, and `\not\mid`, emitting
  direct negated MathML relation operators when a safe mapping exists.
- Focused EPUB3 coverage writes `a \approx b + x \not\in S + m \equiv n` and
  `p \not= q + A \subsetneq B + u \parallel v + r \nmid s`, asserts exact
  relation and negated-relation serialization, confirms relation command names
  do not leak as literal MathML identifiers, and round-trips both equations as
  native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,753 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,099 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,453 assertions, 0 failures with pass-count verification `pass=987 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc relation constructs without claiming full TeX
  math parser parity, arbitrary negated-relation overlay parity, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Named Operator MathML Implementation Slice

- Focused PHP coverage is now 986 behavior tests and 16,435 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded named math operators and
  custom operator names.
- `HtmlWriter` now maps common TeX function/operator commands such as `\sin`,
  `\cos`, `\log`, `\ln`, `\exp`, `\max`, `\min`, `\lim`, `\liminf`,
  `\limsup`, `\det`, `\gcd`, and related trigonometric/hyperbolic names to
  normal MathML `<mi mathvariant="normal">...</mi>` identifiers instead of
  leaking command names as fallback identifiers.
- `\operatorname{...}` and `\operatorname*{...}` now read a bounded raw group
  and emit a normal MathML identifier, with simple TeX spacing-command cleanup
  inside the operator name. Explicit limit modifiers can now apply to normal
  named-operator identifiers through the existing large-operator limit path.
- Focused EPUB3 coverage writes `\sin x + \log_{2} n + \operatorname{rank} A`
  and `\max\limits_{x\in S} f(x) + \operatorname{erf}(t) +
  \cos^{2}\theta`, asserts exact normal-identifier, subscript, superscript, and
  under-limit serialization, confirms named operator commands do not leak as
  literal MathML identifiers, and round-trips both equations as native math
  nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,735 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,081 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,435 assertions, 0 failures with pass-count verification `pass=986 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc named-operator constructs without claiming full
  TeX math parser parity, full operator spacing/layout parity, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Large Operator Limits MathML Implementation Slice

- Focused PHP coverage is now 985 behavior tests and 16,418 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded large-operator limit
  modifiers.
- `HtmlWriter` now consumes `\limits`, `\nolimits`, and `\displaylimits`
  modifiers immediately after known large operator bases. Explicit `\limits`
  and `\displaylimits` use MathML `<munder>`, `<mover>`, or `<munderover>` for
  recognized large operators, while `\nolimits` preserves the existing
  `<msub>`, `<msup>`, and `<msubsup>` script shape without leaking the modifier
  as a literal identifier.
- The bounded operator map now includes additional large operators such as
  `\coprod`, `\bigcup`, `\bigcap`, `\bigsqcup`, `\bigvee`, `\bigwedge`,
  `\bigoplus`, `\bigotimes`, `\bigodot`, and `\biguplus`, with `\lim`, `\inf`,
  and `\sup` eligible for explicit under/over limits when parsed as identifier
  bases.
- Focused EPUB3 coverage writes `\sum\limits_{i=1}^{n} i +
  \prod\nolimits_{k=1}^{m} k` and `\int\limits_{0}^{1} x dx +
  \bigcup\limits_{j=1}^{p} A_j + \lim\limits_{x\to0} f(x)`, asserts exact
  `<munderover>`, `<munder>`, and `<msubsup>` serialization, confirms limit
  modifiers and new large-operator command names do not leak as literal MathML
  identifiers, and round-trips both equations as native math nodes with `mathml`
  spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,718 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,064 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,418 assertions, 0 failures with pass-count verification `pass=985 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc large-operator limit constructs without claiming
  full TeX math parser parity, automatic display-style limit placement parity,
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Over Under MathML Implementation Slice

- Focused PHP coverage is now 984 behavior tests and 16,402 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded over/under annotation
  commands.
- `HtmlWriter` now maps `\overbrace{...}` and `\underbrace{...}` to MathML
  `<mover>` and `<munder>` output with stretchy brace operators, and maps
  `\overset{...}{...}`, `\underset{...}{...}`, and `\stackrel{...}{...}` to
  general MathML over/under annotations while reusing the existing TeX row
  parser for both annotations and bases.
- Focused EPUB3 coverage writes `\overbrace{x+y} + \underbrace{a_b}` and
  `\overset{\text{top}}{A} + \underset{i=1}{\sum} + \stackrel{!}{=}`, asserts
  exact `<mover>`/`<munder>` serialization for compound rows, subscripted bases,
  text annotations, operator bases, and stacked relation markers, confirms
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,702 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,048 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,402 assertions, 0 failures with pass-count verification `pass=984 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc over/under annotation constructs without
  claiming full TeX math parser parity, large-operator limits parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Named Spacing MathML Implementation Slice

- Focused PHP coverage is now 983 behavior tests and 16,385 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for common named TeX spacing
  commands.
- `HtmlWriter` now maps `\thinspace`, `\medspace`, `\thickspace`,
  `\enspace`, `\quad`, `\qquad`, `\negthinspace`, `\negmedspace`, and
  `\negthickspace` to bounded MathML `<mspace width="..."/>` output. Existing
  short spacing commands such as `\,`, `\:`, `\;`, and `\!` remain covered and
  unchanged; negative spacing names follow the parser's existing zero-width
  fallback used for `\!`.
- Focused EPUB3 coverage writes `a\quad b\thinspace c` and
  `x\qquad y\enspace z\negthinspace w`, asserts exact `<mspace>`
  serialization for 1em, 2em, 0.5em, 0.167em, and zero-width spacing, confirms
  named spacing command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php`, `EpubWriterTest.php`,
  and `EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,685 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,031 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,385 assertions, 0 failures with pass-count verification `pass=983 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc spacing constructs without claiming full TeX
  math parser parity, negative spacing layout parity, full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Style Command MathML Implementation Slice

- Focused PHP coverage is now 982 behavior tests and 16,368 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for common math and text style
  commands.
- `HtmlWriter` now maps `\mathrm`, `\mathbf`, `\mathit`, `\mathsf`,
  `\mathtt`, `\mathbb`, `\mathcal`, `\mathfrak`, plus text variants such as
  `\textrm`, `\textbf`, `\textit`, `\textsf`, and `\texttt`, to MathML
  `<mstyle mathvariant="...">` output. Math-style commands parse their argument
  through the existing TeX MathML row parser, while text-style commands emit
  styled `<mtext>` content.
- Focused EPUB3 coverage writes `\mathrm{d}x + \mathbf{v}` and
  `\mathcal{F} = \textbf{rate} + \mathtt{code}`, asserts exact `<mstyle>`
  serialization for normal, bold, script, and monospace variants, confirms style
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,668 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 10,014 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,368 assertions, 0 failures with pass-count verification `pass=982 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc styled math constructs without claiming full TeX
  math parser parity, full TeX text-mode parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Array Column Spec MathML Implementation Slice

- Focused PHP coverage is now 981 behavior tests and 16,351 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded `array` environments with
  column specifications.
- `HtmlWriter` now consumes the required `\begin{array}{...}` column spec group,
  maps `l`, `c`, and `r` specifiers to MathML `columnalign` values, ignores
  non-alignment spec characters such as vertical rules, and emits `<mtable>`
  rows and cells through the existing matrix/cases row parser.
- Focused EPUB3 coverage writes `\begin{array}{rcl}x&=&1\\y&>&2\end{array}`
  style content and a fenced `array` with `|lc|r|` column syntax containing a
  fraction, vector accent, and indexed root. It asserts exact `<mtable
  columnalign="...">` serialization, confirms array environment names do not
  leak as literal MathML identifiers, and round-trips both equations as native
  math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,651 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 9,997 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,351 assertions, 0 failures with pass-count verification `pass=981 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc array constructs without claiming full TeX math
  parser parity, full LaTeX array spec parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-24 EPUB3 TeX Left Right Fence MathML Implementation Slice

- Focused PHP coverage is now 980 behavior tests and 16,336 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for explicit `\left...\right` fences
  and `\middle` separators.
- `HtmlWriter` now consumes paired `\left`/`\right` delimiters, parses the
  enclosed TeX with the existing MathML row parser, maps command delimiters such
  as `\langle` and `\rangle`, supports invisible `.` delimiters, and emits
  stretchy MathML `<mo>` fence operators around the generated row. `\middle`
  emits a stretchy separator inside the fenced row.
- Focused EPUB3 coverage writes `\left( x+1 \right)` and
  `\left\langle \frac{a}{b} \middle| \sqrt[3]{c} \right\rangle`, asserts exact
  serialization for character delimiters, command delimiters, a middle
  separator, fractions, and indexed roots, confirms fence command names do not
  leak as literal MathML identifiers, and round-trips both equations as native
  math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,636 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 9,982 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,336 assertions, 0 failures with pass-count verification `pass=980 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc explicit fence constructs without claiming full
  TeX math parser parity, full LaTeX environment parity, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation, DRM decryption, XML
  signature cryptographic validation, or broader media/layout edge-case parity
  beyond existing focused cases.

## 2026-06-24 EPUB3 TeX Cases Aligned MathML Implementation Slice

- Focused PHP coverage is now 979 behavior tests and 16,319 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded `cases` and aligned-style
  environments.
- `HtmlWriter` now treats `cases`, `dcases`, `rcases`, `aligned`, `gathered`,
  and `split` as table-like TeX math environments. One-sided fences now render
  correctly, so `cases` emits a left stretchy brace around the generated
  `<mtable>` instead of silently dropping the brace.
- Focused EPUB3 coverage writes `\begin{cases}x&x>0\\0&x=0\end{cases}` and
  `\begin{aligned}a&=b+c\\d&=\sqrt{e}\end{aligned}`, asserts exact `<mtable>`
  serialization for one-sided fenced cases and unfenced aligned rows, confirms
  environment command names do not leak as literal MathML identifiers, and
  round-trips both equations as native math nodes with `mathml` spine manifest
  properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,619 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 9,965 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,319 assertions, 0 failures with pass-count verification `pass=979 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc piecewise/aligned math constructs without
  claiming full TeX math parser parity, full LaTeX environment parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation, DRM
  decryption, XML signature cryptographic validation, or broader media/layout
  edge-case parity beyond existing focused cases.

## 2026-06-23 EPUB3 TeX Matrix MathML Implementation Slice

- Focused PHP coverage is now 978 behavior tests and 16,303 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for bounded matrix environments.
- `HtmlWriter` now recognizes `\begin{matrix}`/`\end{matrix}` style TeX
  environments, scans to the matching end environment, splits top-level rows and
  cells without splitting inside braces, brackets, or nested matrix-like
  environments, and emits MathML `<mtable>`, `<mtr>`, and `<mtd>` structures.
  Common fenced forms such as `pmatrix`, `bmatrix`, `Bmatrix`, `vmatrix`, and
  `Vmatrix` wrap the table with stretchy fence operators.
- Focused EPUB3 coverage writes `\begin{matrix}a&b\\c&d\end{matrix}` and
  `\begin{pmatrix}x_1&\frac{1}{2}\\0&\sqrt[3]{y}\end{pmatrix}`, asserts exact
  `<mtable>` serialization for bare and fenced matrices, confirms environment
  command names do not leak as literal MathML identifiers, and round-trips both
  equations as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,603 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 9,949 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,303 assertions, 0 failures with pass-count verification `pass=978 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc matrix constructs without claiming full TeX math
  parser parity, full LaTeX environment parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation, DRM decryption, XML signature
  cryptographic validation, or broader media/layout edge-case parity beyond
  existing focused cases.

## 2026-06-23 EPUB3 TeX Accent MathML Implementation Slice

- Focused PHP coverage is now 977 behavior tests and 16,287 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for common accent and underline
  commands.
- `HtmlWriter` now maps `\overline`, `\bar`, `\hat`, `\widehat`, `\tilde`,
  `\widetilde`, and `\vec` to MathML `<mover accent="true">` output, and maps
  `\underline` to `<munder accentunder="true">`. MathML row wrapping now checks
  generated fragments with a DOM parse instead of a greedy tag regex, so
  compound accent bases like `x+y` are wrapped as a single `<mrow>` child.
- Focused EPUB3 coverage writes `\overline{x+y} + \vec{v}` and
  `\underline{\frac{a}{b}} + \hat{\theta}`, asserts exact `<mover>` and
  `<munder>` serialization for compound and single bases, confirms no fallback
  literal command identifiers leak into MathML, and round-trips both equations
  as native math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,587 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 9,933 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,287 assertions, 0 failures with pass-count verification `pass=977 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for common Pandoc math accent constructs without claiming full TeX
  math parser parity, full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-23 EPUB3 TeX Indexed Root MathML Implementation Slice

- Focused PHP coverage is now 976 behavior tests and 16,271 full-suite
  assertions across the Pandoc lane. This implementation slice extends native
  PHP `epub3` writer TeX-to-MathML output for indexed roots.
- `HtmlWriter` now parses the optional bracket argument in `\sqrt[n]{...}` and
  emits MathML `<mroot>` instead of leaking `[` and `]` as ordinary operator
  nodes. Plain `\sqrt{...}` still emits `<msqrt>`, and `mroot` is now treated as
  a single row-safe MathML element when wrapping generated MathML content.
- Focused EPUB3 coverage writes `\sqrt[3]{x_2}` and
  `\sqrt[n+1]{\frac{a}{b}}`, asserts exact `<mroot>` serialization for a
  simple numeric root index and a compound symbolic root index, confirms no
  optional-bracket operator leakage, and round-trips both equations as native
  math nodes with `mathml` spine manifest properties.
- Read-back preserves inline/display math text and display attributes and avoids
  missing required manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `HtmlWriter.php` and
  `EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 5,571 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 9,917 assertions,
  0 failures; and `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  16,271 assertions, 0 failures with pass-count verification `pass=976 fail=0`.
- Dependency-closure checkpoint: this improves the EPUB3 native PHP MathML
  writer path for a common Pandoc math construct without claiming full TeX math
  parser parity, full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation, DRM decryption, XML signature cryptographic validation, or broader
  media/layout edge-case parity beyond existing focused cases.

## 2026-06-23 EPUB3 Alternate Rootfile Annotated MathML Coverage Slice

- Focused PHP coverage is now 975 behavior tests and 16,257 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for caller-supplied annotated MathML emitted through
  a generated alternate-rootfile spine.
- Existing shared `EpubWriter` MathML handling already writes explicit MathML
  payloads and generated TeX MathML in the primary package, while prior
  alternate-rootfile coverage proved generated TeX MathML and mixed required
  property inference. Focused coverage now proves explicit annotated MathML
  payloads through a generated alternate OPF package.
- Coverage forces alternate XHTML through inline and display `math` AST nodes
  carrying explicit MathML with TeX annotations, generated `mathml` manifest
  properties, display `block` attribute injection, XML-parseable generated
  XHTML, and primary/alternate OPF and XHTML isolation.
- Read-back preserves alternate `epubBodyBlocks` native math nodes, inline and
  display math text/display attributes, semantic display MathML metadata,
  readable-resource metadata, alternate body text, and avoids missing required
  manifest-property and malformed spine diagnostics.
- Verification: `php -l` passed for `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,557 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,903 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 16,257 assertions, 0 failures with
  pass-count verification `pass=975 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  annotated MathML round-trips through alternate package manifest properties,
  spine XHTML, native body math blocks, and semantic metadata without reader
  diagnostics. It does not claim a production behavior change for this
  coverage-only slice, full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation beyond the covered generated alternate annotated MathML, alternate
  raw XHTML normalization, alternate HTML5 media resources, alternate
  footnote/pagebreak semantics, alternate table XHTML, alternate raw HTML5
  XHTML, alternate semantic XHTML, alternate package viewport fallback,
  alternate NCX target sanitation, alternate Dublin Core ID attribute
  sanitation, alternate invalid rendition sanitation, alternate per-spine
  rendition refinement, alternate generated SMIL attribute, alternate manifest
  attribute, alternate collection link href/media/refines, alternate collection
  missing-member, alternate OPF NCName ID, alternate package-wide ID, alternate
  collection metadata direction, alternate package-link direction, primary
  collection metadata direction, package-link direction, OPF NCName ID,
  package-wide ID, collection missing-member, collection-link ID, nested
  collection package document reference, nested collection sidecar discovery,
  nested collection split link, nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link and media/layout edge-case parity beyond existing focused
  cases, DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Raw XHTML Normalization Coverage Slice

- Focused PHP coverage is now 974 behavior tests and 16,234 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for raw XHTML normalization emitted through a
  generated alternate-rootfile spine.
- Existing shared `EpubWriter` raw fragment handling already normalizes HTML
  entities, void tags, boolean attributes, resource URLs, script text, and
  required `scripted` manifest properties in the primary package. Focused
  coverage now proves that behavior through a generated alternate OPF package
  rather than only through the primary-package raw XHTML normalization path.
- Coverage forces alternate XHTML through `&nbsp;`, `<br>`, raw `img`, boolean
  `input` attributes, inline script escaping, alternate package manifest
  entries, payload ZIP entries, XML-parseable generated XHTML, and
  primary/alternate OPF and XHTML isolation.
- Read-back preserves alternate `epubReferencedResources`,
  `epubImageResources`, extracted resource payloads, semantic image metadata,
  semantic input boolean attributes, alternate body text, and avoids
  missing-resource and malformed spine diagnostics.
- Verification: `php -l` passed for `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,534 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,880 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 16,234 assertions, 0 failures with
  pass-count verification `pass=974 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  raw XHTML normalization round-trips through alternate manifest, ZIP payloads,
  body resource references, script-required spine properties, and spine semantic
  metadata without reader diagnostics. It does not claim a production behavior
  change for this coverage-only slice, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated alternate
  raw XHTML normalization, alternate HTML5 media resources, alternate
  footnote/pagebreak semantics, alternate table XHTML, alternate raw HTML5
  XHTML, alternate semantic XHTML, alternate package viewport fallback,
  alternate NCX target sanitation, alternate Dublin Core ID attribute
  sanitation, alternate invalid rendition sanitation, alternate per-spine
  rendition refinement, alternate generated SMIL attribute, alternate manifest
  attribute, alternate collection link href/media/refines, alternate collection
  missing-member, alternate OPF NCName ID, alternate package-wide ID, alternate
  collection metadata direction, alternate package-link direction, primary
  collection metadata direction, package-link direction, OPF NCName ID,
  package-wide ID, collection missing-member, collection-link ID, nested
  collection package document reference, nested collection sidecar discovery,
  nested collection split link, nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link and media/layout edge-case parity beyond existing focused
  cases, DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile HTML5 Media Resource Coverage Slice

- Focused PHP coverage is now 973 behavior tests and 16,214 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for HTML5 media/resource XHTML emitted through a
  generated alternate-rootfile spine.
- Existing shared `EpubWriter` media resource handling already rewrites and
  manifests `video` poster/source resources, `track` caption resources, `audio`
  sources, `picture`/`img` `srcset` resources, and `object` data resources in
  the primary package. Focused coverage now proves that behavior through a
  generated alternate OPF package rather than only through the primary-package
  media writer path.
- Coverage forces alternate XHTML through local poster images, video, captions,
  audio, responsive image `srcset`, SVG object data, package manifest entries,
  payload ZIP entries, XML-parseable generated XHTML, and primary/alternate OPF
  and XHTML isolation.
- Read-back preserves alternate `epubReferencedResources`, `epubImageResources`,
  extracted resource payloads, semantic media records for `video`, `source`,
  `track`, `audio`, and `object`, and avoids missing-resource and malformed
  spine diagnostics.
- Verification: `php -l` passed for `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,514 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,860 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 16,214 assertions, 0 failures with
  pass-count verification `pass=973 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  HTML5 media resources round-trip through alternate manifest, ZIP payloads,
  body resource references, and spine semantic metadata without reader
  diagnostics. It does not claim a production behavior change for this
  coverage-only slice, full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation beyond the covered generated alternate HTML5 media resources,
  alternate footnote/pagebreak semantics, alternate table XHTML, alternate raw
  HTML5 XHTML, alternate semantic XHTML, alternate package viewport fallback,
  alternate NCX target sanitation, alternate Dublin Core ID attribute
  sanitation, alternate invalid rendition sanitation, alternate per-spine
  rendition refinement, alternate generated SMIL attribute, alternate manifest
  attribute, alternate collection link href/media/refines, alternate collection
  missing-member, alternate OPF NCName ID, alternate package-wide ID, alternate
  collection metadata direction, alternate package-link direction, primary
  collection metadata direction, package-link direction, OPF NCName ID,
  package-wide ID, collection missing-member, collection-link ID, nested
  collection package document reference, nested collection sidecar discovery,
  nested collection split link, nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link and media/layout edge-case parity beyond existing focused
  cases, DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Footnote Pagebreak Coverage Slice

- Focused PHP coverage is now 972 behavior tests and 16,167 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for native notes and pagebreak semantics emitted
  through a generated alternate-rootfile spine.
- Existing shared `EpubWriter` note/pagebreak serialization already emits
  noteref anchors, endnote containers, footnote list items, and pagebreak spans
  with EPUB3 semantic roles in the primary package. Focused coverage now proves
  that behavior through a generated alternate OPF package rather than only
  through the primary-package semantic writer path.
- Coverage forces alternate XHTML through a native Pandoc note with formatted
  footnote content, generated `doc-noteref`/`noteref`, generated
  `doc-endnotes`/`footnotes`, generated `doc-footnote`/`footnote`, a structured
  `pagebreak` span with title metadata, XML-parseable generated XHTML, and
  primary/alternate XHTML isolation.
- Read-back preserves the alternate package `epubBodyBlocks` native note,
  formatted note body, pagebreak span AST attributes, semantic noteref,
  footnotes, footnote, and pagebreak metadata, alternate body text, and avoids
  malformed-spine-xhtml plus page-list target diagnostics.
- Verification: `php -l` passed for `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,467 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,813 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 16,167 assertions, 0 failures with
  pass-count verification `pass=972 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  footnote and pagebreak semantic output round-trips through alternate body and
  spine metadata without reader diagnostics. It does not claim a production
  behavior change for this coverage-only slice, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated alternate
  footnote/pagebreak semantics, alternate table XHTML, alternate raw HTML5
  XHTML, alternate semantic XHTML, alternate package viewport fallback,
  alternate NCX target sanitation, alternate Dublin Core ID attribute
  sanitation, alternate invalid rendition sanitation, alternate per-spine
  rendition refinement, alternate generated SMIL attribute, alternate manifest
  attribute, alternate collection link href/media/refines, alternate collection
  missing-member, alternate OPF NCName ID, alternate package-wide ID, alternate
  collection metadata direction, alternate package-link direction, primary
  collection metadata direction, package-link direction, OPF NCName ID,
  package-wide ID, collection missing-member, collection-link ID, nested
  collection package document reference, nested collection sidecar discovery,
  nested collection split link, nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link and media/layout edge-case parity beyond existing focused
  cases, DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Table Structure Coverage Slice

- Focused PHP coverage is now 971 behavior tests and 16,138 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for structured table XHTML emitted through a generated
  alternate-rootfile spine.
- Existing shared `EpubWriter` table serialization already preserves caption
  inlines, column widths, table head/body/foot sections, body header rows,
  row-header columns, spans, per-cell alignment, and HTML/data attributes.
  Focused coverage now proves that behavior through a generated alternate OPF
  package rather than only through the primary-package table writer path.
- Coverage forces alternate XHTML through a caption with inline emphasis,
  `colgroup` widths, `thead`, `tbody`, `tfoot`, `rowHeadColumns`, `headRows`,
  rowspan/colspan cells, per-cell `style` and `data-review` attributes,
  XML-parseable generated XHTML, and primary/alternate XHTML isolation.
- Read-back preserves the alternate package `epubBodyBlocks` table AST,
  table attributes, caption, widths, alignments, table sections, row and cell
  spans, cell HTML attributes, semantic table metadata, body text, and avoids
  malformed-spine-xhtml diagnostics.
- Verification: `php -l` passed for `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,438 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,784 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 16,138 assertions, 0 failures with
  pass-count verification `pass=971 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  table structure output round-trips through alternate body and spine metadata
  without reader diagnostics. It does not claim a production behavior change for
  this coverage-only slice, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation beyond the covered generated alternate table XHTML, alternate
  raw HTML5 XHTML, alternate semantic XHTML, alternate package viewport fallback,
  alternate NCX target sanitation, alternate Dublin Core ID attribute
  sanitation, alternate invalid rendition sanitation, alternate per-spine
  rendition refinement, alternate generated SMIL attribute, alternate manifest
  attribute, alternate collection link href/media/refines, alternate collection
  missing-member, alternate OPF NCName ID, alternate package-wide ID, alternate
  collection metadata direction, alternate package-link direction, primary
  collection metadata direction, package-link direction, OPF NCName ID,
  package-wide ID, collection missing-member, collection-link ID, nested
  collection package document reference, nested collection sidecar discovery,
  nested collection split link, nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link and media/layout edge-case parity beyond existing focused
  cases, DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Raw HTML5 XHTML Coverage Slice

- Focused PHP coverage is now 970 behavior tests and 16,099 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for raw HTML5 XHTML emitted through a generated
  alternate-rootfile spine.
- Existing shared `EpubWriter` raw XHTML serialization already preserves HTML5
  inline boundary tags and raw block containers while keeping generated XHTML
  XML-parseable. Focused coverage now proves that behavior through a generated
  alternate OPF package rather than only through primary-package raw HTML5 writer
  tests.
- Coverage forces alternate XHTML through raw `bdi`, `data`, `meter`, `output`,
  `details`, and `summary` elements, primary/alternate XHTML isolation, reader
  `epubBodyBlocks`, reader `semanticElements`, and WordPress HTML-block
  read-back for the alternate package.
- Read-back preserves raw inline boundary nodes, the raw `details` block,
  semantic values for `data`/`meter`/`output`, `details` open and `epub:type`
  metadata, summary text, and avoids malformed-spine-xhtml diagnostics.
- Verification: `php -l` passed for `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,399 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,745 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 16,099 assertions, 0 failures with
  pass-count verification `pass=970 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  raw HTML5 XHTML output round-trips through alternate body and spine metadata
  without reader diagnostics. It does not claim a production behavior change for
  this coverage-only slice, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation beyond the covered generated alternate raw HTML5 XHTML,
  alternate semantic XHTML, alternate package viewport fallback, alternate NCX
  target sanitation, alternate Dublin Core ID attribute sanitation, alternate
  invalid rendition sanitation, alternate per-spine rendition refinement,
  alternate generated SMIL attribute, alternate manifest attribute, alternate
  collection link href/media/refines, alternate collection missing-member,
  alternate OPF NCName ID, alternate package-wide ID, alternate collection
  metadata direction, alternate package-link direction, primary collection
  metadata direction, package-link direction, OPF NCName ID, package-wide ID,
  collection missing-member, collection-link ID, nested collection package
  document reference, nested collection sidecar discovery, nested collection
  split link, nested collection link inline-content, nested collection
  metadata-refines, nested collection-link, collection-link, package-link,
  metadata-refines, guide, NCX, manifest, package, split-spine, non-linear,
  media-overlay, and rendition-suppression paths, remaining OPF guide/NCX/link
  and media/layout edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Semantic XHTML Coverage Slice

- Focused PHP coverage is now 969 behavior tests and 16,072 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for semantic XHTML attributes emitted through a
  generated alternate-rootfile spine.
- Existing shared `EpubWriter` XHTML serialization already preserves semantic
  heading, section, link, span, figure, and image attributes, rewrites
  alternate-rootfile relative image resources, strips duplicate source aliases in
  favor of `data-source`, and produces XML-parseable spine XHTML. Focused
  coverage now proves that behavior through a generated alternate OPF package
  rather than only through primary-package semantic XHTML writer tests.
- Coverage forces alternate XHTML through `epub:type`, ARIA, `role`,
  `xml:lang`, `dir`, classes, data attributes, link rel/hreflang/type/download
  metadata, section conversion from a Pandoc div, figure caption output, image
  manifest/resource payload output, primary/alternate body isolation, and reader
  `semanticElements` read-back for the alternate spine item.
- Read-back preserves semantic heading, section, link, span, figure, and image
  records under the alternate package spine metadata, preserves the image
  payload, and avoids malformed-spine-xhtml diagnostics.
- Verification: `php -l` passed for `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,372 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,718 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 16,072 assertions, 0 failures with
  pass-count verification `pass=969 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  semantic XHTML attribute output round-trips through alternate spine metadata
  without reader diagnostics. It does not claim a production behavior change for
  this coverage-only slice, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation beyond the covered generated alternate semantic XHTML,
  alternate package viewport fallback, alternate NCX target sanitation,
  alternate Dublin Core ID attribute sanitation, alternate invalid rendition
  sanitation, alternate per-spine rendition refinement, alternate generated SMIL
  attribute, alternate manifest attribute, alternate collection link
  href/media/refines, alternate collection missing-member, alternate OPF NCName
  ID, alternate package-wide ID, alternate collection metadata direction,
  alternate package-link direction, primary collection metadata direction,
  package-link direction, OPF NCName ID, package-wide ID, collection
  missing-member, collection-link ID, nested collection package document
  reference, nested collection sidecar discovery, nested collection split link,
  nested collection link inline-content, nested collection metadata-refines,
  nested collection-link, collection-link, package-link, metadata-refines, guide,
  NCX, manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link and media/layout
  edge-case parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Package Viewport Fallback Coverage Slice

- Focused PHP coverage is now 968 behavior tests and 16,026 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for generated alternate-rootfile package-level
  `rendition:viewport` fallback into generated XHTML viewport metadata.
- Existing shared `EpubWriter` viewport handling already normalizes
  package-level rendition viewports, preserves extra viewport properties such as
  `initial-scale`, `viewport-fit`, and `interactive-widget`, and uses the package
  viewport as an XHTML viewport fallback when a spine item has no explicit
  viewport. Focused coverage now proves that behavior through a generated
  alternate OPF package rather than only through the primary package path.
- Coverage forces a primary package viewport and a distinct alternate package
  viewport through the same generated EPUB3 archive, proving that the alternate
  OPF emits the normalized package `rendition:viewport`, the generated alternate
  XHTML receives the same normalized `<meta name="viewport">` content, and the
  primary package viewport does not leak into alternate OPF/XHTML/read-back.
- Read-back preserves the alternate package `epubRenditionViewport`, the spine
  item viewport fallback, `epubViewports`, `epubViewport`, and extended viewport
  properties, while avoiding invalid-rendition-viewport and
  malformed-spine-xhtml diagnostics.
- Verification: `php -l` passed for `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,326 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,672 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 16,026 assertions, 0 failures with
  pass-count verification `pass=968 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  package viewport fallback round-trips without reader diagnostics while
  extended viewport properties survive. It does not claim a production behavior
  change for this coverage-only slice, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated alternate
  package viewport fallback, alternate NCX target sanitation, alternate Dublin
  Core ID attribute sanitation, alternate invalid rendition sanitation,
  alternate per-spine rendition refinement, alternate generated SMIL attribute,
  alternate manifest attribute, alternate collection link href/media/refines,
  alternate collection missing-member, alternate OPF NCName ID, alternate
  package-wide ID, alternate collection metadata direction, alternate
  package-link direction, primary collection metadata direction, package-link
  direction, OPF NCName ID, package-wide ID, collection missing-member,
  collection-link ID, nested collection package document reference, nested
  collection sidecar discovery, nested collection split link, nested collection
  link inline-content, nested collection metadata-refines, nested
  collection-link, collection-link, package-link, metadata-refines, guide, NCX,
  manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link and media/layout
  edge-case parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile NCX Target Sanitation Coverage Slice

- Focused PHP coverage is now 967 behavior tests and 16,012 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for generated alternate-rootfile NCX target
  sanitation when an EPUB3 package also emits an NCX compatibility sidecar.
- Existing shared `EpubWriter` NCX handling already filters explicit NCX
  navigation targets to generated linear spine resources, rejects absolute local
  paths, omits missing resources and non-linear spine targets, and preserves
  valid targetless parents by pointing them at the first surviving child.
  Focused coverage now proves that behavior through a generated EPUB3 alternate
  OPF package rather than only through OPF2/EPUB2 package modes.
- Coverage forces alternate EPUB3 NCX nav-list metadata through missing local
  targets, absolute local paths, non-linear spine targets, invalid children,
  valid TOC/page-list targets, a valid figure target, and a targetless parent
  with a valid child. The EPUB3 nav document remains on its existing behavior
  path while this slice exercises NCX-only metadata.
- Read-back preserves the alternate NCX TOC, page-list, figure nav-list,
  parent-fallback target, child target, IDs, types, values, and playOrder
  sequence, prevents alternate resources from leaking into the primary OPF, and
  avoids missing-ncx-content-src, invalid-ncx-content-src-path,
  invalid-ncx-content-src-fragment, missing-ncx-spine-target,
  ncx-target-non-linear-spine, and out-of-order-nav-toc-entry diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,312 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,658 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 16,012 assertions, 0 failures with
  pass-count verification `pass=967 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  NCX target sanitation round-trips without reader diagnostics while valid NCX
  hierarchy survives. It does not claim a production behavior change for this
  coverage-only slice, full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation beyond the covered generated alternate NCX target sanitation,
  alternate Dublin Core ID attribute sanitation, alternate invalid rendition
  sanitation, alternate per-spine rendition refinement, alternate generated SMIL
  attribute, alternate manifest attribute, alternate collection link
  href/media/refines, alternate collection missing-member, alternate OPF NCName
  ID, alternate package-wide ID, alternate collection metadata direction,
  alternate package-link direction, primary collection metadata direction,
  package-link direction, OPF NCName ID, package-wide ID, collection
  missing-member, collection-link ID, nested collection package document
  reference, nested collection sidecar discovery, nested collection split link,
  nested collection link inline-content, nested collection metadata-refines,
  nested collection-link, collection-link, package-link, metadata-refines, guide,
  manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Dublin Core ID Attribute Coverage Slice

- Focused PHP coverage is now 966 behavior tests and 15,981 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for generated alternate-rootfile Dublin Core metadata
  ID and attribute sanitation.
- Existing shared `EpubWriter` Dublin Core metadata handling already validates
  XML IDs, prevents reuse of package-wide IDs outside the unique identifier,
  suppresses duplicate metadata IDs, validates `dc:language` and `xml:lang`,
  allow-lists `dir`, and preserves valid OPF-qualified attributes. Focused
  coverage now proves that behavior through a generated alternate OPF package.
- Coverage forces alternate Dublin Core records through collisions with the
  alternate unique identifier and generated `nav` manifest ID, duplicate
  Dublin Core IDs, an invalid NCName-style ID, invalid `xml:lang`, invalid
  `dir`, and valid `opf:file-as`/`opf:role` plus valid language/direction
  attributes.
- Read-back omits invalid and colliding Dublin Core IDs, suppresses invalid
  language/direction attributes, preserves valid alternate creator and
  description metadata attributes, prevents primary Dublin Core metadata from
  leaking into the alternate OPF, and avoids invalid-metadata-id,
  duplicate-metadata-id, duplicate-package-id, invalid-metadata-xml-language,
  and invalid-metadata-dir diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,281 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,627 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,981 assertions, 0 failures with
  pass-count verification `pass=966 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  Dublin Core ID and attribute sanitation round-trips without reader diagnostics
  while valid OPF-qualified metadata survives. It does not claim a production
  behavior change for this coverage-only slice, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated alternate
  Dublin Core metadata, alternate invalid rendition sanitation, alternate
  per-spine rendition refinement, alternate generated SMIL attribute, alternate
  manifest attribute, alternate collection link href/media/refines, alternate
  collection missing-member, alternate OPF NCName ID, alternate package-wide ID,
  alternate collection metadata direction, alternate package-link direction,
  primary collection metadata direction, package-link direction, OPF NCName ID,
  package-wide ID, collection missing-member, collection-link ID, nested
  collection package document reference, nested collection sidecar discovery,
  nested collection split link, nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Invalid Rendition Sanitation Coverage Slice

- Focused PHP coverage is now 965 behavior tests and 15,938 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for generated alternate-rootfile invalid rendition
  metadata and spine alias sanitation.
- Existing shared `EpubWriter` rendition handling already allow-lists
  package-level `rendition:*` values, normalizes viewports, allow-lists
  synthesized spine rendition properties, and suppresses invalid page
  progression values; focused coverage now proves that behavior through a
  generated alternate OPF package.
- Coverage forces invalid alternate package-level `rendition:layout`,
  `rendition:orientation`, `rendition:spread`, `rendition:flow`,
  `rendition:viewport`, and `page-progression-direction` values, plus invalid
  per-spine `pageSpread`, layout, orientation, spread, flow, viewport, and
  `metadataProperties` aliases. A sibling spine item carries valid aliases to
  prove sanitation is selective rather than dropping the whole spine surface.
- Read-back omits invalid package rendition keys and invalid first-spine
  rendition keys, preserves valid second-spine itemref properties and viewport
  metadata, and avoids invalid-rendition-metadata,
  invalid-rendition-viewport, invalid-rendition-spine-property, and
  invalid-spine-page-progression-direction diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,238 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,584 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,938 assertions, 0 failures with
  pass-count verification `pass=965 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  invalid rendition aliases are omitted before read-back, while valid sibling
  rendition aliases survive. It does not claim a production behavior change for
  this coverage-only slice, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB
  graph validation beyond the covered generated alternate invalid rendition
  sanitation, alternate per-spine rendition refinement, alternate generated SMIL
  attribute, alternate manifest attribute, alternate collection link
  href/media/refines, alternate collection missing-member, alternate OPF NCName
  ID, alternate package-wide ID, alternate collection metadata direction,
  alternate package-link direction, primary collection metadata direction,
  package-link direction, OPF NCName ID, package-wide ID, collection
  missing-member, collection-link ID, nested collection package document
  reference, nested collection sidecar discovery, nested collection split link,
  nested collection link inline-content, nested collection metadata-refines,
  nested collection-link, collection-link, package-link, metadata-refines,
  guide, NCX, manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Per-Spine Rendition Refinement Coverage Slice

- Focused PHP coverage is now 964 behavior tests and 15,900 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for generated alternate-rootfile per-spine
  `rendition:*` metadata refinements.
- Existing shared `EpubWriter` spine refinement handling already rewrites stale
  source manifest IDs to the emitted itemref IDs before serializing scoped
  rendition metadata; focused coverage now proves that behavior through a
  generated alternate OPF package rather than only through the primary package.
- Coverage forces alternate split spine pages through explicit source manifest
  IDs, emitted itemref IDs, package-level scoped `rendition:layout` and
  `rendition:spread` metadata, per-spine `rendition:orientation`,
  `rendition:viewport`, `rendition:flow`, and generated itemref rendition
  properties.
- Read-back preserves alternate itemref IDs, idrefs, generated itemref
  properties, scoped rendition layout/orientation/spread/flow/viewport metadata,
  normalized viewport records, and confirms stale source IDs plus primary
  package rendition IDs do not leak into the alternate OPF refinements.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,200 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,546 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,900 assertions, 0 failures with
  pass-count verification `pass=964 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  rendition refinements no longer leave stale source spine IDs in emitted OPF
  metadata and round-trip without invalid/conflicting rendition diagnostics. It
  does not claim a production behavior change for this coverage-only slice,
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation beyond
  the covered generated alternate per-spine rendition refinement, alternate
  generated SMIL attribute, alternate manifest attribute, alternate collection
  link href/media/refines, alternate collection missing-member, alternate OPF
  NCName ID, alternate package-wide ID, alternate collection metadata direction,
  alternate package-link direction, primary collection metadata direction,
  package-link direction, OPF NCName ID, package-wide ID, collection
  missing-member, collection-link ID, nested collection package document
  reference, nested collection sidecar discovery, nested collection split link,
  nested collection link inline-content, nested collection metadata-refines,
  nested collection-link, collection-link, package-link, metadata-refines,
  guide, NCX, manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Generated SMIL Attribute Coverage Slice

- Focused PHP coverage remains 963 behavior tests and is now 15,864 full-suite
  assertions across the Pandoc lane. This coverage-only slice expands native PHP
  `epub3` writer coverage for generated alternate-rootfile media overlay SMIL
  metadata.
- Existing shared `EpubWriter` media-overlay generation already serializes SMIL
  root attributes, body attributes, sequence `epub:textref` metadata, text child
  attributes, audio child attributes, and separate sequence grouping; focused
  coverage now proves that behavior through generated alternate OPF media
  overlays rather than only through the primary package path.
- Coverage forces an alternate generated overlay through SMIL root `id`,
  `version`, class, prefix, `epub:type`, and language attributes; body `id`,
  `epub:type`, and language attributes; two explicitly grouped sequences with
  distinct `epub:textref` values; and text/audio child IDs, classes,
  `epub:type`, language, and clip timing.
- Read-back preserves the alternate overlay root/body attributes, sequence
  records, pair records, text targets, audio targets, generated SMIL resource,
  generated audio payload, and alternate spine `mediaOverlay` reference while
  continuing to reject leakage from primary-package media-overlay options.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,164 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,510 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,864 assertions, 0 failures with
  pass-count verification `pass=963 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  media overlay SMIL attributes round-trip through writer and reader metadata.
  It does not claim a production behavior change for this coverage-only slice,
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation beyond
  the covered generated alternate SMIL attribute, alternate manifest attribute,
  alternate collection link href/media/refines, alternate collection
  missing-member, alternate OPF NCName ID, alternate package-wide ID, alternate
  collection metadata direction, alternate package-link direction, primary
  collection metadata direction, package-link direction, OPF NCName ID,
  package-wide ID, collection missing-member, collection-link ID, nested
  collection package document reference, nested collection sidecar discovery,
  nested collection split link, nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Manifest Attribute Sanitation Coverage Slice

- Focused PHP coverage is now 963 behavior tests and 15,854 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for alternate-rootfile manifest `fallback`,
  `fallback-style`, and `media-overlay` sanitation.
- Existing shared `EpubWriter` manifest serialization already omits missing
  fallback targets, non-core fallback chains, fallback cycles, non-CSS
  fallback-style targets, missing media-overlay targets, and non-SMIL overlay
  targets; focused coverage now proves that behavior through a generated
  alternate OPF package.
- Coverage forces an alternate generated spine item and alternate manifest
  resources through missing fallback, missing fallback-style, missing
  media-overlay, non-core image fallback, fallback cycle, non-CSS
  fallback-style, and invalid content/target media-overlay cases.
- Read-back records only sanitized alternate spine metadata, omits invalid
  `fallback`, `fallbackStyle`, and `mediaOverlay` fields, and avoids
  missing-manifest-fallback, missing-manifest-fallback-style,
  missing-manifest-media-overlay, manifest-fallback-cycle,
  missing-manifest-fallback-core-media-type,
  invalid-manifest-fallback-style-media-type,
  invalid-media-overlay-content-media-type, and
  invalid-media-overlay-target-media-type diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,154 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,500 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,854 assertions, 0 failures with
  pass-count verification `pass=963 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  manifest attributes no longer self-diagnose invalid fallback, fallback-style,
  or media-overlay references after writer-side sanitation. It does not claim a
  production behavior change for this coverage-only slice, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation beyond the covered
  generated alternate manifest attribute, alternate collection link
  href/media/refines, alternate collection missing-member, alternate OPF NCName
  ID, alternate package-wide ID, alternate collection metadata direction,
  alternate package-link direction, primary collection metadata direction,
  package-link direction, OPF NCName ID, package-wide ID, collection
  missing-member, collection-link ID, nested collection package document
  reference, nested collection sidecar discovery, nested collection split link,
  nested collection link inline-content, nested collection metadata-refines,
  nested collection-link, collection-link, package-link, metadata-refines,
  guide, NCX, manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Collection Link Href Media Refines Coverage Slice

- Focused PHP coverage is now 962 behavior tests and 15,824 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for alternate rootfile collection metadata-link and
  direct collection-link href/media/refines sanitation.
- Existing shared `EpubWriter` package-link serialization already drops invalid
  hrefs, infers valid local media types, suppresses invalid media types, strips
  invalid record-link refines, and requires valid voicing refines; focused
  coverage now proves that behavior through generated alternate OPF collections.
- Coverage forces alternate collection metadata links through invalid media
  types, record-link refines, missing and malformed voicing refines, invalid
  href fragments, and direct collection member links through data URLs,
  absolute paths, invalid fragments, and invalid media types.
- Read-back records only sanitized alternate collection links, preserves the
  valid voicing refines target, omits invalid direct-link media types, confirms
  primary OPF output does not inherit alternate collection link IDs, and avoids
  invalid-package-link-data-url, invalid-package-link-file-url,
  invalid-package-link-href-path, invalid-package-link-href-fragment,
  invalid-package-link-media-type, missing-package-link-media-type,
  invalid-package-link-record-refines, and missing-package-link-voicing-refines
  diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,124 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,470 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,824 assertions, 0 failures with
  pass-count verification `pass=962 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  collection metadata/direct links no longer self-diagnose href/media/refines
  attributes after writer-side sanitation. It does not claim a production
  behavior change for this coverage-only slice, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated alternate
  collection link href/media/refines, alternate collection missing-member,
  alternate OPF NCName ID, alternate package-wide ID, alternate collection
  metadata direction, alternate package-link direction, primary collection
  metadata direction, package-link direction, OPF NCName ID, package-wide ID,
  collection missing-member, collection-link ID, nested collection package
  document reference, nested collection sidecar discovery, nested collection
  split link, nested collection link inline-content, nested collection
  metadata-refines, nested collection-link, collection-link, package-link,
  metadata-refines, guide, NCX, manifest, package, split-spine, non-linear,
  media-overlay, and rendition-suppression paths, remaining OPF guide/NCX/link
  edge-case parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Collection Missing-Member Coverage Slice

- Focused PHP coverage is now 961 behavior tests and 15,794 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for alternate rootfile `missing-collection-member`
  sanitation.
- Existing shared `EpubWriter` collection rendering already omits collections
  that have no surviving direct member link and no surviving nested collection;
  focused coverage now proves that behavior through a generated alternate OPF
  package.
- Coverage adds an alternate direct-member collection that survives,
  metadata-only collections that are omitted, a collection emptied by missing
  local member-link sanitation, a nested empty parent whose child is omitted,
  and a nested valid child that keeps its parent alive.
- Read-back records only the surviving alternate collections and avoids
  missing-collection-member plus missing-package-link-resource diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,094 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,440 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,794 assertions, 0 failures with
  pass-count verification `pass=961 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  collections no longer self-diagnose missing members after writer-side member
  sanitation. It does not claim a production behavior change for this
  coverage-only slice, full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation beyond the covered generated alternate collection missing-member,
  alternate OPF NCName ID, alternate package-wide ID, alternate collection
  metadata direction, alternate package-link direction, primary collection
  metadata direction, package-link direction, OPF NCName ID, package-wide ID,
  collection missing-member, collection-link ID, nested collection package
  document reference, nested collection sidecar discovery, nested collection
  split link, nested collection link inline-content, nested collection
  metadata-refines, nested collection-link, collection-link, package-link,
  metadata-refines, guide, NCX, manifest, package, split-spine, non-linear,
  media-overlay, and rendition-suppression paths, remaining OPF guide/NCX/link
  edge-case parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile OPF NCName ID Coverage Slice

- Focused PHP coverage is now 960 behavior tests and 15,771 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for alternate rootfile OPF XML NCName ID sanitation.
- Existing shared `EpubWriter::validManifestId` already accepts only XML
  NCNames via `validXmlId`; focused coverage now proves the generated alternate
  OPF path omits colon-bearing IDs before read-back.
- Coverage forces invalid NCName IDs through alternate package root IDs, spine
  IDs, resource manifest IDs, package metadata links, collection roots,
  collection metadata, collection metadata links, and direct collection member
  links.
- Read-back records anonymous surviving alternate metadata/collection entries,
  confirms primary OPF output does not inherit invalid alternate IDs, and avoids
  invalid-package-id, invalid-spine-id, invalid-manifest-id, invalid-metadata-id,
  invalid-collection-id, invalid-collection-metadata-id, and
  invalid-collection-link-id diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,071 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,417 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,771 assertions, 0 failures with
  pass-count verification `pass=960 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  OPF output no longer self-diagnoses invalid XML NCName IDs across the shared
  package ID helper's main alternate OPF callers. It does not claim a production
  behavior change for this coverage-only slice, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated alternate
  OPF NCName ID, alternate package-wide ID, alternate collection metadata
  direction, alternate package-link direction, primary collection metadata
  direction, package-link direction, OPF NCName ID, package-wide ID, collection
  missing-member, collection-link ID, nested collection package document
  reference, nested collection sidecar discovery, nested collection split link,
  nested collection link inline-content, nested collection metadata-refines,
  nested collection-link, collection-link, package-link, metadata-refines, guide,
  NCX, manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Package-Wide ID Coverage Slice

- Focused PHP coverage is now 959 behavior tests and 15,743 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for alternate rootfile package-wide duplicate ID
  sanitation.
- Existing shared `EpubWriter` package OPF serialization already reserves IDs
  across package roots, generated manifest items, package metadata properties,
  metadata links, collection roots, collection metadata, and direct collection
  member links; focused coverage now proves that behavior through a generated
  alternate OPF package.
- Coverage forces collisions across the alternate generated nav item,
  alternate Dublin Core identifier, package metadata properties, package
  metadata links, collection root IDs, collection metadata IDs, collection
  metadata-link IDs, and direct collection member-link IDs.
- Read-back records anonymous colliding alternate entries, preserves the unique
  surviving IDs, confirms primary OPF output does not inherit alternate IDs, and
  avoids duplicate-package-id, duplicate-metadata-id, duplicate-collection-id,
  duplicate-collection-metadata-id, and duplicate-collection-link-id diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,043 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,389 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,743 assertions, 0 failures with
  pass-count verification `pass=959 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  metadata and collection output no longer self-diagnoses package-wide duplicate
  IDs across package metadata properties, package metadata links, collection
  roots, collection metadata, collection metadata links, and direct collection
  links. It does not claim a production behavior change for this coverage-only
  slice, full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation
  beyond the covered generated alternate package-wide ID, alternate collection
  metadata direction, alternate package-link direction, primary collection
  metadata direction, package-link direction, OPF NCName ID, package-wide ID,
  collection missing-member, collection-link ID, nested collection package
  document reference, nested collection sidecar discovery, nested collection
  split link, nested collection link inline-content, nested collection
  metadata-refines, nested collection-link, collection-link, package-link,
  metadata-refines, guide, NCX, manifest, package, split-spine, non-linear,
  media-overlay, and rendition-suppression paths, remaining OPF guide/NCX/link
  edge-case parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Collection Metadata Direction Coverage Slice

- Focused PHP coverage remains 958 behavior tests and is now 15,710 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for alternate rootfile reader
  `invalid-collection-metadata-dir` diagnostics.
- Existing shared `EpubWriter` collection metadata serialization already emits
  `dir` attributes only for `ltr`, `rtl`, or `auto`; focused coverage now proves
  that behavior through generated alternate OPF collection metadata.
- Coverage adds invalid `dir="up"` on an alternate collection title metadata
  entry and valid `dir="rtl"` on an alternate collection subtitle metadata
  entry.
- Read-back records sanitized alternate collection metadata without invalid
  `dir` values, preserves the valid `rtl` direction, and avoids
  `invalid-collection-metadata-dir` in alternate rootfile diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,010 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,356 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,710 assertions, 0 failures with
  pass-count verification `pass=958 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  collection metadata no longer self-diagnoses direction attributes. It does not
  claim a production behavior change for this coverage-only slice, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation beyond the
  covered generated alternate collection metadata direction, alternate
  package-link direction, primary collection metadata direction, package-link
  direction, OPF NCName ID, package-wide ID, collection missing-member,
  collection-link ID, nested collection package document reference, nested
  collection sidecar discovery, nested collection split link, nested collection
  link inline-content, nested collection metadata-refines, nested
  collection-link, collection-link, package-link, metadata-refines, guide, NCX,
  manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Alternate Rootfile Package-Link Direction Coverage Slice

- Focused PHP coverage remains 958 behavior tests and is now 15,706 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for alternate rootfile reader
  `invalid-package-link-dir` diagnostics.
- Existing shared `EpubWriter` package-link serialization already emits `dir`
  attributes only for `ltr`, `rtl`, or `auto`; focused coverage now proves that
  behavior through generated alternate OPF packages.
- Coverage adds invalid `sideways` direction values to an alternate package
  metadata link, an alternate collection metadata link, and an alternate direct
  collection member link, plus valid `rtl` directions on surviving alternate
  collection metadata/member links.
- Read-back records sanitized alternate package metadata links, collection
  metadata links, and collection member links without invalid `dir` values,
  preserves valid `rtl` directions, and avoids `invalid-package-link-dir` in
  alternate rootfile diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  5,006 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,352 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,706 assertions, 0 failures with
  pass-count verification `pass=958 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 alternate-rootfile
  package metadata links, collection metadata links, and direct collection
  member links no longer self-diagnose package-link direction attributes. It
  does not claim a production behavior change for this coverage-only slice, full
  EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation beyond the covered
  generated alternate package-link direction, primary collection metadata
  direction, package-link direction, OPF NCName ID, package-wide ID, collection
  missing-member, collection-link ID, nested collection package document
  reference, nested collection sidecar discovery, nested collection split link,
  nested collection link inline-content, nested collection metadata-refines,
  nested collection-link, collection-link, package-link, metadata-refines, guide,
  NCX, manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Collection Metadata Direction Sanitation Coverage Slice

- Focused PHP coverage remains 958 behavior tests and is now 15,696 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for reader `invalid-collection-metadata-dir`
  diagnostics.
- Existing `EpubWriter` logic already emits collection metadata `dir`
  attributes only for `ltr`, `rtl`, or `auto`; focused coverage now proves that
  invalid direction values are omitted while valid `rtl` values are preserved.
- Coverage adds an invalid `dir` value on a collection title metadata entry and
  a valid `rtl` direction on a collection subtitle metadata entry, then verifies
  OPF output omits the invalid value and read-back round-trips the valid one.
- Read-back records sanitized collection metadata without invalid `dir` values
  and avoids invalid-collection-metadata-dir alongside existing
  invalid-collection-metadata-xml-language diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,996 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,342 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,696 assertions, 0 failures with
  pass-count verification `pass=958 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 collection metadata
  no longer self-diagnoses direction attributes. It does not claim a production
  behavior change for this coverage-only slice, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated collection
  metadata direction, package-link direction, OPF NCName ID, package-wide ID,
  collection missing-member, collection-link ID, nested collection package
  document reference, nested collection sidecar discovery, nested collection
  split link, nested collection link inline-content, nested collection
  metadata-refines, nested collection-link, collection-link, package-link,
  metadata-refines, guide, NCX, manifest, package, split-spine, non-linear,
  media-overlay, and rendition-suppression paths, remaining OPF guide/NCX/link
  edge-case parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Package-Link Direction Sanitation Coverage Slice

- Focused PHP coverage remains 958 behavior tests and is now 15,692 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` writer coverage for reader `invalid-package-link-dir` diagnostics.
- Existing `EpubWriter` logic already emits package link `dir` attributes only
  for `ltr`, `rtl`, or `auto`; focused coverage now proves that behavior for
  package metadata links, collection metadata links, and direct collection
  member links.
- Coverage now includes valid `rtl` round-trip cases and invalid `sideways`
  values that must be omitted before OPF output.
- Read-back records sanitized package metadata links, collection metadata links,
  and collection member links without invalid `dir` values and avoids
  invalid-package-link-dir alongside existing invalid-package-link-hreflang and
  invalid-package-link-xml-language diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,992 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,338 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,692 assertions, 0 failures with
  pass-count verification `pass=958 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 package metadata
  links, collection metadata links, and direct collection member links no longer
  self-diagnose package-link direction attributes. It does not claim a
  production behavior change for this coverage-only slice, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation beyond the covered
  generated package-link direction, OPF NCName ID, package-wide ID, collection
  missing-member, collection-link ID, nested collection package-document
  reference, nested collection sidecar discovery, nested collection split link,
  nested collection link inline-content, nested collection metadata-refines,
  nested collection-link, collection-link, package-link, metadata-refines, guide,
  NCX, manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 OPF NCName ID Sanitation Slice

- Focused PHP coverage is now 958 behavior tests and 15,680 full-suite
  assertions across the Pandoc lane. This slice closes native PHP `epub3`
  writer parity for reader invalid ID diagnostics across shared OPF ID emission.
- `EpubWriter::validManifestId` now accepts only XML NCNames via the shared
  `validXmlId` check, so package root IDs, spine IDs, manifest resource ID
  overrides, package metadata-link IDs, collection root IDs, collection metadata
  `meta`/`link` IDs, and direct collection member-link IDs with colon-bearing or
  otherwise invalid NCName syntax are omitted before OPF output while otherwise
  valid elements survive.
- Focused coverage forces invalid NCName IDs through `packageId`, `spineId`,
  `epubResourcePayloads` manifest IDs, package metadata links, collection roots,
  collection metadata, collection metadata links, and direct collection links.
- Read-back records anonymous surviving metadata/collection entries and avoids
  invalid-package-id, invalid-spine-id, invalid-manifest-id, invalid-metadata-id,
  invalid-collection-id, invalid-collection-metadata-id, and
  invalid-collection-link-id diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,980 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,326 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,680 assertions, 0 failures with
  pass-count verification `pass=958 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 OPF output no
  longer self-diagnoses invalid XML NCName IDs across the shared package ID
  helper's main OPF callers. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated OPF NCName
  ID, package-wide ID, collection missing-member, collection-link ID, nested
  collection package-document reference, nested collection sidecar discovery,
  nested collection split link, nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link edge-case parity beyond existing focused cases, DRM decryption,
  XML signature cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Package-Wide ID Sanitation Slice

- Focused PHP coverage is now 957 behavior tests and 15,654 full-suite
  assertions across the Pandoc lane. This slice closes native PHP `epub3`
  writer parity for the reader `duplicate-package-id` diagnostic across package
  metadata and collection output.
- `EpubWriter` now reserves optional package metadata-property IDs against the
  full package ID set, reserves package metadata-link IDs while rendering a
  batch, threads the package-wide ID set into collection rendering, and only
  commits collection IDs for collections that survive member sanitation.
- Collection root IDs, collection metadata `meta`/`link` IDs, and direct
  collection member-link IDs now share the same package-wide namespace, while
  otherwise valid colliding elements render anonymously.
- Focused coverage forces collisions across generated nav/book IDs, package
  metadata properties, package metadata links, collection roots, collection
  metadata, collection metadata links, and direct collection links.
- Read-back records anonymous colliding entries and avoids duplicate-package-id,
  duplicate-metadata-id, duplicate-collection-id, duplicate-collection-metadata-id,
  and duplicate-collection-link-id diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,954 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,300 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,654 assertions, 0 failures with
  pass-count verification `pass=957 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 metadata and
  collection output no longer self-diagnoses package-wide duplicate IDs across
  package metadata properties, package metadata links, collection roots,
  collection metadata, collection metadata links, and direct collection links.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation beyond the covered generated package-wide ID, collection
  missing-member, collection-link ID, nested collection package-document
  reference, nested collection sidecar discovery, nested collection split link,
  nested collection link inline-content, nested collection metadata-refines,
  nested collection-link, collection-link, package-link, metadata-refines, guide,
  NCX, manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Collection Missing-Member Sanitation Slice

- Focused PHP coverage is now 956 behavior tests and 15,624 full-suite
  assertions across the Pandoc lane. This slice closes native PHP `epub3`
  writer parity for the reader `missing-collection-member` diagnostic.
- `EpubWriter` now omits collections that have no surviving direct member link
  and no surviving nested collection after link/child sanitation, while
  preserving valid direct-member collections and valid nested-member
  collections.
- Focused coverage adds metadata-only collections, collections emptied by
  missing local member links, nested parents whose children are also omitted,
  and valid nested collection members that must survive.
- Existing collection metadata sanitation fixtures now include valid member
  links so they remain EPUB3-valid under the stricter collection member rule.
- Read-back records only the surviving collections and avoids
  missing-collection-member plus missing-package-link-resource diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,924 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,270 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,624 assertions, 0 failures with
  pass-count verification `pass=956 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 collection output
  does not self-diagnose missing collection members after member links or nested
  children are sanitized away, without breaking valid metadata-bearing
  collections that have direct or nested members. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation beyond the
  covered generated collection missing-member, collection-link ID, nested
  collection package-document reference, nested collection sidecar discovery,
  nested collection split link, nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link edge-case parity beyond existing focused cases, DRM decryption,
  XML signature cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Collection Link ID Sanitation Slice

- Focused PHP coverage is now 955 behavior tests and 15,602 full-suite
  assertions across the Pandoc lane. This slice closes native PHP `epub3`
  direct collection member link ID parity with the reader diagnostics.
- `EpubWriter` now tracks direct collection-link IDs while rendering collection
  trees, omits invalid or repeated direct collection link IDs before OPF output,
  and keeps collection metadata refines target discovery aligned with the
  surviving rendered link IDs.
- Focused coverage adds invalid and duplicate direct collection member link IDs
  plus metadata that refines the surviving valid link ID.
- Read-back records sanitized `epubCollections` with only one `series-member`
  link ID, anonymous invalid/duplicate links, and no invalid-collection-link-id,
  duplicate-collection-link-id, or collection-metadata-refines-outside
  diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,902 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,248 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,602 assertions, 0 failures with
  pass-count verification `pass=955 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 collection member
  links no longer produce invalid or duplicate collection-link ID diagnostics,
  without losing metadata refines that target a surviving valid collection link.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation beyond the covered generated collection-link ID, nested collection
  package-document reference, nested collection sidecar discovery, nested
  collection split link, nested collection link inline-content, nested collection
  metadata-refines, nested collection-link, collection-link, package-link,
  metadata-refines, guide, NCX, manifest, package, split-spine, non-linear,
  media-overlay, and rendition-suppression paths, remaining OPF guide/NCX/link
  edge-case parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Nested Collection Package-Document Reference Sanitation Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,591 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` package-link self-reference sanitation across nested collection
  metadata/member links in primary and generated alternate rootfile packages.
- Focused coverage adds nested collection metadata links and nested collection
  member links that point back into the current OPF package document, including
  generated alternate rootfile package paths.
- Existing writer code omits those nested package-document references before
  OPF output while preserving valid nested XML record links, nested XHTML member
  links, nested JSON sidecar members, top-level collection links, package
  metadata links, and alternate package isolation.
- Read-back records sanitized nested `epubCollections` without nested
  package-document link IDs and without invalid-package-link-package-document-
  reference, missing-package-link-resource, missing-package-link-fragment,
  malformed-package-link-resource, or invalid-package-link-manifest-resource
  diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,891 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,237 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,591 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 nested collection
  links no longer point back into the OPF package document where the reader
  would report invalid package-link package-document references, without
  breaking valid nested resource links or alternate-rootfile isolation. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation
  beyond the covered generated nested collection package-document reference,
  nested collection sidecar discovery, nested collection split link, nested
  collection link inline-content, nested collection metadata-refines, nested
  collection-link, collection-link, package-link, metadata-refines, guide, NCX,
  manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Nested Collection Sidecar Resource Discovery Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,583 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` package-link sidecar coverage across nested collection links in
  primary and generated alternate rootfile packages.
- Focused coverage adds nested collection JSON sidecar member links with
  distinct resource payloads.
- Existing writer code discovers nested collection sidecar resources
  recursively while preserving top-level collection/package sidecars and
  alternate package isolation.
- Generated OPF collection output includes the nested sidecar links without
  duplicating sidecars into the manifest, ZIP sidecar payloads are present, and
  read-back records `epubPackageLinkResources` with nested collection IDs plus
  extracted resource payloads.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,883 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,229 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,583 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 nested collection
  sidecar links package and read back as package-link resources with nested
  collection context, without manifest duplication or missing/invalid
  package-link diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated nested
  collection sidecar discovery, nested collection split link, nested collection
  link inline-content, nested collection metadata-refines, nested
  collection-link, collection-link, package-link, metadata-refines, guide, NCX,
  manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Nested Collection Split Link Href-Fragment Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,566 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` package-link fragment handling across nested collection
  metadata/member links in split primary packages and split generated alternate
  rootfiles.
- Focused coverage adds nested collection metadata links and nested collection
  member links that target generated split spine XHTML fragments on both first
  and second split chapters, plus missing-fragment variants that must be
  omitted.
- Existing writer code preserves valid nested split fragment links, omits
  missing nested split fragments before OPF output, rewrites read-back `href`
  values to the generated package paths, and keeps primary and alternate split
  packages isolated.
- Read-back records sanitized nested `epubCollections` links without missing
  split-fragment entries and without missing-package-link-resource,
  missing-package-link-fragment, malformed-package-link-resource, or
  invalid-package-link-manifest-resource diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,866 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,212 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,566 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 nested collection
  links can target split spine fragments and drop missing split fragments before
  the reader would diagnose them, without breaking top-level package/collection
  split link behavior or alternate-rootfile isolation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation beyond the
  covered generated nested collection split link, nested collection link
  inline-content, nested collection metadata-refines, nested collection-link,
  collection-link, package-link, metadata-refines, guide, NCX, manifest,
  package, split-spine, non-linear, media-overlay, and rendition-suppression
  paths, remaining OPF guide/NCX/link edge-case parity beyond existing focused
  cases, DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB3 Nested Collection Link Inline Content Sanitation Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,542 full-suite
  assertions across the Pandoc lane. This coverage-only slice extends native PHP
  `epub3` package-link content sanitation across nested collection
  metadata/member links in primary and generated alternate rootfile packages.
- Focused coverage adds nested collection metadata links and nested collection
  member links carrying `value`, `text`, and child `meta` payloads beside the
  existing top-level package and collection inline-content cases.
- Existing writer code emits empty OPF `link` elements for those nested links,
  preserves their `href`, `rel`, `media-type`, and `id` attributes, and omits
  inline text and child metadata before OPF output in both primary and generated
  alternate packages.
- Read-back records sanitized nested `epubCollections` links without inline
  payload text and without invalid-package-link-content diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,842 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,188 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,542 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 nested collection
  links do not serialize inline text or child metadata that the reader would
  report as invalid package-link content, without breaking valid nested link
  attributes or top-level package/collection link content sanitation. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph validation
  beyond the covered generated nested collection link inline-content, nested
  collection metadata-refines, nested collection-link, collection-link,
  package-link, metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB3 Nested Collection Metadata Refines Scope Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,524 full-suite
  assertions across the Pandoc lane. This coverage-only slice proves native PHP
  `epub3` collection metadata `refines` handling across primary and generated
  alternate rootfile packages.
- Focused coverage adds nested collections whose metadata validly refines the
  nested collection root and nested member link, beside metadata that attempts
  to refine an ancestor collection ID.
- Existing writer code preserves valid nested root/link `refines` values and
  strips ancestor `refines` before OPF output in both primary and generated
  alternate packages.
- Read-back records sanitized nested `epubCollections` metadata without
  ancestor `refines` and without invalid-collection-metadata-refines or
  collection-metadata-refines-outside diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,824 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,170 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,524 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 nested collection
  metadata no longer points `refines` attributes at ancestor collection IDs that
  the reader would report as outside the containing collection, without breaking
  valid nested collection-root or nested member-link refines. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB graph validation beyond
  the covered generated nested collection metadata-refines, nested
  collection-link, collection-link, package-link, metadata-refines, guide, NCX,
  manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Nested Collection Link Malformed XML Fragment Sanitation Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,510 full-suite
  assertions across the Pandoc lane. This coverage-only slice proves shared
  package-link malformed XML fragment hardening recurses through native PHP
  `epub3` nested collection links in primary and generated alternate rootfile
  packages.
- Focused coverage adds valid nested collection record/member links beside
  malformed XML sidecar fragment targets under top-level collections in both
  primary and generated alternate packages.
- Existing writer code omits malformed nested collection metadata/member link
  IDs before OPF output, while preserving valid nested XML record/member links,
  top-level collection links, JSON sidecar members, remote links, package
  metadata links, and alternate package isolation.
- Read-back records sanitized `epubCollections` without malformed nested
  collection link IDs and without malformed-package-link-resource,
  missing-package-link-resource, missing-package-link-fragment,
  invalid-package-link-package-document-reference, or
  invalid-package-link-manifest-resource diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,810 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,156 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,510 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 nested collection
  links no longer point at malformed XML fragment targets that the reader would
  immediately diagnose, without breaking top-level collection links, JSON
  sidecar collection members, remote links, or package metadata-link behavior.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB graph
  validation beyond the covered generated nested collection-link,
  collection-link, package-link, metadata-refines, guide, NCX, manifest,
  package, split-spine, non-linear, media-overlay, and rendition-suppression
  paths, remaining OPF guide/NCX/link edge-case parity beyond existing focused
  cases, DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB3 Collection Link Malformed XML Fragment Sanitation Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,494 full-suite
  assertions across the Pandoc lane. This slice extends shared package-link
  malformed XML fragment hardening across native PHP `epub3` collection links in
  primary and generated alternate rootfile packages.
- Focused coverage feeds malformed XML sidecar fragment targets through
  collection metadata links and direct collection member links in both primary
  and generated alternate packages.
- The shared package-link availability check omits those malformed fragment
  links before OPF output, while preserving valid XML collection records, JSON
  sidecar members, remote links, package metadata links, and alternate package
  isolation.
- Generated OPF output omits the malformed collection link IDs before
  read-back, and read-back records sanitized `epubCollections` without
  malformed-package-link-resource, missing-package-link-resource,
  missing-package-link-fragment, invalid-package-link-package-document-reference,
  or invalid-package-link-manifest-resource diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,794 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,140 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,494 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 collection links no
  longer point at malformed XML fragment targets that the reader would
  immediately diagnose, without breaking JSON sidecar collection members,
  remote links, or package metadata-link behavior. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB graph validation beyond the
  covered generated collection-link, package-link, metadata-refines, guide, NCX,
  manifest, package, split-spine, non-linear, media-overlay, and
  rendition-suppression paths, remaining OPF guide/NCX/link edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB3 Package Link Malformed XML Fragment Sanitation Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,483 full-suite
  assertions across the Pandoc lane. This slice tightens native PHP `epub3`
  package metadata-link generation across primary and generated alternate
  rootfile packages.
- `EpubWriter` now rejects package metadata links whose `href` fragment targets
  a malformed XML-like package resource, while preserving existing JSON/non-XML
  sidecar fragment links used by package-link graph scenarios and preserving
  valid XML, audio, voicing, record, and alternate-link behavior.
- Focused coverage feeds malformed XML sidecar fragment links through the
  primary package metadata-link fixture and generated alternate-rootfile
  metadata-link fixture.
- Generated OPF output omits those malformed fragment links before read-back,
  while preserving valid metadata links and existing package-link graph JSON
  fragment links.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,783 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,129 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,483 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 package metadata
  links no longer point at malformed XML fragment targets that the reader would
  immediately diagnose, without breaking JSON sidecar fragment links currently
  used by package-link graph fixtures. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation beyond the covered
  generated package-link, metadata-refines, guide, NCX, manifest, package,
  split-spine, non-linear, media-overlay, and rendition-suppression paths,
  remaining OPF guide/NCX/link edge-case parity beyond existing focused cases,
  DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB3 Metadata Refines Resource Sanitation Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,473 full-suite
  assertions across the Pandoc lane. This slice tightens native PHP `epub3`
  package metadata generation across primary and generated alternate rootfile
  packages.
- `EpubWriter` now requires package metadata property/link `refines` fragments
  that target package resources to resolve inside a well-formed XML-like
  payload, and rejects `refines` fragments into non-XML resources, while
  preserving valid package-document, generated spine XHTML, and XML sidecar
  refines.
- Focused coverage feeds valid XML sidecar and chapter/package refines, missing
  resources, missing fragments, malformed XML sidecar refines, PNG/non-XML
  fragment refines, record-link refines, and generated alternate-rootfile
  variants.
- Generated OPF output preserves valid refines, emits invalid metadata
  properties/links without `refines` attributes, packages malformed/non-XML
  sidecar resources without using them as fragment targets, keeps alternate
  sidecars out of the primary OPF, and read-back records sanitized
  `epubMetadataProperties` and `epubMetadataLinks`.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,773 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,119 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,473 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 package metadata
  refines no longer points at malformed XML or non-XML fragment targets that the
  reader would immediately diagnose. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB graph validation beyond the covered
  generated metadata-refines, guide, NCX, manifest, package, split-spine,
  non-linear, media-overlay, and rendition-suppression paths, remaining OPF
  guide/NCX/link edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB3 Guide Fragment Resource Sanitation Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,447 full-suite
  assertions across the Pandoc lane. This slice extends the shared
  guide-reference hardening to native PHP `epub3` package generation across
  primary and generated alternate rootfile packages.
- Focused coverage now feeds valid resource-backed backmatter guide references,
  duplicate same-type/same-href guide references, malformed XHTML guide targets,
  and PNG/non-XML guide targets with fragments through the EPUB3 primary
  guide-reference fixture and generated alternate-rootfile guide-reference
  fixture.
- The shared guide sanitizer preserves valid resource-backed appendix/backmatter
  references, omits duplicates, rejects malformed XML fragment targets and
  non-XML fragment targets before OPF output, and keeps remote guide references
  allowed.
- Generated EPUB3 OPF output preserves the expected guide references without
  adding malformed/non-XML fragment targets, alternate guide resources stay out
  of the primary OPF, and read-back records sanitized `epubGuideReferences`.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,747 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,093 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,447 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB3 guide output shares
  the OPF2 guide-fragment protections and does not emit local fragment
  references that the reader would immediately diagnose as malformed or
  non-resolvable. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB graph validation beyond the covered generated guide,
  NCX, manifest, package, split-spine, non-linear, media-overlay, and rendition-
  suppression paths, remaining OPF guide/NCX edge-case parity beyond existing
  focused cases, DRM decryption, XML signature cryptographic validation, or
  broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Guide Fragment Resource Sanitation OPF2 Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,431 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` OPF guide-reference generation across primary and generated alternate
  rootfile packages.
- `EpubWriter` now requires local guide-reference fragments to resolve inside a
  well-formed XML-like package resource, and rejects fragment hrefs into
  non-XML resources, while preserving fragmentless local resources and allowed
  remote guide references.
- Focused coverage feeds valid resource-backed backmatter guide references,
  duplicate guide references with the same type and href, malformed XHTML guide
  targets, and PNG/non-XML guide targets with fragments in both primary and
  generated alternate OPF2 packages.
- Generated OPF2 output preserves the valid resource-backed appendix/backmatter
  reference, omits duplicates, malformed XML targets, and non-XML fragment
  targets, preserves NCX/OPF2 output shape, and keeps alternate guide resources
  out of the primary OPF.
- Read-back records sanitized `epubGuideReferences` with
  `epubPackageVersion=2.0` and no missing-guide-reference-resource,
  missing-guide-reference-manifest-resource, missing-guide-reference-fragment,
  malformed-guide-reference-resource, invalid-guide-reference-href-path,
  invalid-guide-reference-href-fragment, invalid-guide-reference-data-url, or
  invalid-guide-reference-file-url diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,731 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,077 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,431 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves generated EPUB2 guide output no
  longer emits local fragment references that the reader would immediately
  diagnose as malformed or non-resolvable. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB2 graph validation beyond the covered
  generated guide-fragment-resource-sanitation, NCX, guide-reference,
  manifest, package, split-spine, non-linear, media-overlay, and rendition-
  suppression paths, remaining OPF 2 guide/NCX edge-case parity beyond existing
  focused cases, DRM decryption, XML signature cryptographic validation, or
  broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 NCX Non-Linear Target and Invalid-Child Fallback OPF2 Slice

- Focused PHP coverage remains 954 behavior tests and is now 15,415 full-suite
  assertions across the Pandoc lane. This slice strengthens bounded native PHP
  `epub2` NCX metadata coverage across primary and generated alternate rootfile
  packages.
- Focused writer coverage now feeds explicit page-list and nav-list targets
  that point at packaged OPF2 non-linear spine XHTML resources, plus href-less
  `pageTarget`/`navTarget` parents whose first child target is invalid and
  second child target is valid.
- The existing NCX sanitizer strips non-linear `pageTarget`/`navTarget` hrefs
  before rendering, skips the invalid child labels, lets the parent fallback
  target the first valid descendant, preserves nested valid child entries, and
  keeps stable `playOrder` values across TOC, page-list, and nav-list output.
- Generated OPF2 packages prove the non-linear resources are packaged as
  `linear="no"` itemrefs without accepting them as page/nav NCX targets, preserve
  NCX-only output with no EPUB3 nav manifest item, and keep alternate resources
  out of the primary OPF.
- Read-back records sanitized parent and child `epubPageListEntries` and
  `epubNcxNavLists`, with `epubPackageVersion=2.0`, NCX-only toc resources, and
  no missing-NCX-content-src, invalid-NCX-content-src-path,
  invalid-NCX-content-src-fragment, missing-NCX-spine-target, or
  NCX-target-non-linear-spine diagnostics.
- Verification: `php -l` passed for `EpubWriter.php` and `EpubWriterTest.php`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
  4,715 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 9,061 assertions, 0 failures; and `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 15,415 assertions, 0 failures with
  pass-count verification `pass=954 fail=0`.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages do not emit NCX page/nav targets into non-linear spine resources
  and do not let invalid child targets poison parent fallback destinations. It
  does not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition EPUB2 graph
  validation beyond the covered generated NCX-non-linear-target-and-invalid-
  child-fallback, invalid-target, play-order, rendered-depth, empty-list,
  parent-target, fallback-TOC, split-TOC/page-list, metadata, guide-reference,
  manifest, package, split-spine, non-linear, media-overlay, and rendition-
  suppression paths, remaining OPF 2 guide/NCX edge-case parity beyond existing
  focused cases, DRM decryption, XML signature cryptographic validation, or
  broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 NCX Invalid Explicit Target Sanitation OPF2 Slice

- Focused PHP coverage is now 954 behavior tests and 15,397 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` NCX metadata generation across primary and generated alternate
  rootfile packages when explicit TOC, page-list, and nav-list metadata
  contains unusable local targets.
- `EpubWriter` now carries the raw navigation `href` through normalization,
  builds an NCX target resource map from generated package chapters/resources,
  and strips invalid NCX hrefs before rendered-depth, fallback-target, ID, and
  `playOrder` emission.
- The sanitizer omits missing local resources, absolute-path local hrefs, empty
  fragments, and whitespace fragments. `pageTarget`/`navTarget` entries also
  must resolve to generated linear spine resources, while valid TOC/page/nav
  targets and href-less parent fallback behavior remain preserved.
- Focused writer coverage now writes primary and generated alternate OPF2
  packages with invalid explicit TOC, page-list, and nav-list targets beside
  one valid target in each section.
- The generated NCX files prove invalid labels plus `missing.xhtml` and
  bad-fragment targets are omitted, only the valid `navPoint`, `pageTarget`,
  and `navTarget` entries remain with `playOrder` values `1,2,3`, OPF2
  NCX-only output is preserved, and alternate resources do not leak into the
  primary OPF.
- Read-back records sanitized `epubTocEntries`, `epubPageListEntries`, and
  `epubNcxNavLists`, with `epubPackageVersion=2.0`, NCX-only toc resources,
  and no missing-NCX-content-src, invalid-NCX-content-src-path,
  invalid-NCX-content-src-fragment, or missing-NCX-spine-target diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages can accept explicit NCX navigation metadata without emitting
  malformed local targets alongside the already covered NCX play-order
  sanitation, rendered NCX depth, empty-NCX-list-ID-reservation, NCX page/nav
  parent target output, empty-metadata fallback TOCs, metadata-parent NCX TOCs,
  headingless fallback TOCs, split nested/flat generated NCX TOCs,
  split/non-split generated NCX page lists, NCX metadata, guide-reference
  sanitation, inferred manifest-property, per-spine rendition refinement,
  package-root, package-structures, fallback-style, split-spine, non-linear,
  media-overlay, and rendition-suppression paths. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB2 graph validation beyond the
  covered generated NCX-invalid-explicit-target-sanitation/
  NCX-play-order-sanitation/rendered-NCX-depth/empty-NCX-list-ID-reservation/
  NCX-page-list-nav-list-parent-target/empty-metadata-NCX-fallback-TOC/
  metadata-parent-NCX-TOC/headingless-NCX-fallback-TOC/
  split-nested-NCX-TOC/split-NCX-TOC/split-NCX-page-list/
  generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 NCX Play-Order Sanitation OPF2 Slice

- Focused PHP coverage is now 952 behavior tests and 15,337 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` NCX metadata generation across primary and generated alternate
  rootfile packages when explicit TOC, page-list, and nav-list metadata
  contains duplicate or backward `playOrder` values.
- `EpubWriter` now lets explicit `playOrder` metadata skip the sequence forward
  but never move backward or reuse a number. The shared allocator is used for
  NCX `navPoint`, `pageTarget`, and `navTarget` emission.
- Focused writer coverage now writes primary and generated alternate OPF2
  packages whose explicit NCX TOC, page-list, and nav-list entries provide
  duplicate and lower `playOrder` values.
- The generated NCX files prove those values are rewritten into the strictly
  increasing sequence `3,4,5,6,7,8`, OPF2 NCX-only output is preserved, and
  alternate resources do not leak into the primary OPF.
- Read-back records sanitized `playOrder` values across `epubTocEntries`,
  `epubPageListEntries`, and `epubNcxNavLists`, with `epubPackageVersion=2.0`,
  NCX-only toc resources, and no missing/invalid/duplicate/out-of-order NCX
  play-order or missing-NCX-spine-target diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages can accept explicit NCX play-order metadata without emitting
  duplicate or backward sequences alongside the already covered rendered NCX
  depth, empty-NCX-list-ID-reservation, NCX page/nav parent target output,
  empty-metadata fallback TOCs, metadata-parent NCX TOCs, headingless fallback
  TOCs, split nested/flat generated NCX TOCs, split/non-split generated NCX
  page lists, NCX metadata, guide-reference sanitation, inferred
  manifest-property, per-spine rendition refinement, package-root,
  package-structures, fallback-style, split-spine, non-linear, media-overlay,
  and rendition-suppression paths. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the covered generated
  NCX-play-order-sanitation/rendered-NCX-depth/empty-NCX-list-ID-reservation/
  NCX-page-list-nav-list-parent-target/empty-metadata-NCX-fallback-TOC/
  metadata-parent-NCX-TOC/headingless-NCX-fallback-TOC/
  split-nested-NCX-TOC/split-NCX-TOC/split-NCX-page-list/
  generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Rendered NCX Depth OPF2 Slice

- Focused PHP coverage is now 950 behavior tests and 15,289 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` NCX metadata accuracy across primary and generated alternate rootfile
  packages when explicit NCX page-list/nav-list metadata contains nested labels
  without usable target content.
- `EpubWriter` now computes generated `dtb:depth` from renderable NCX navigation
  trees instead of raw metadata trees, while preserving explicit
  `epubNcxDepth`/`ncxDepth` overrides.
- Focused writer coverage strengthens the primary and generated alternate empty
  NCX list ID fixtures with nested targetless page-list and nav-list metadata.
- The generated NCX files prove nested targetless labels are omitted, rendered
  `dtb:depth` remains 1 instead of being inflated by unrendered nested
  metadata, and later valid `navList` records keep unsuffixed preferred IDs.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate empty-list resources do not leak into the primary OPF.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, zero
  page-list entries, one valid NCX nav-list entry with the unsuffixed ID, and no
  package-version, empty-page-list, empty-nav-list, missing-content, or
  duplicate-ncx-id diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages can omit targetless nested NCX page/nav metadata without
  corrupting generated `dtb:depth` or later generated IDs alongside the already
  covered empty-NCX-list-ID-reservation, NCX page/nav parent target output,
  empty-metadata fallback TOCs, metadata-parent NCX TOCs, headingless fallback
  TOCs, split nested/flat generated NCX TOCs, split/non-split generated NCX
  page lists, NCX metadata, guide-reference sanitation, inferred
  manifest-property, per-spine rendition refinement, package-root,
  package-structures, fallback-style, split-spine, non-linear, media-overlay,
  and rendition-suppression paths. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the covered generated
  rendered-NCX-depth/empty-NCX-list-ID-reservation/NCX-page-list-nav-list-parent-
  target/empty-metadata-NCX-fallback-TOC/metadata-parent-NCX-TOC/
  headingless-NCX-fallback-TOC/split-nested-NCX-TOC/split-NCX-TOC/
  split-NCX-page-list/generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Empty NCX List ID Reservation OPF2 Slice

- Focused PHP coverage is now 950 behavior tests and 15,283 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` NCX navigation sanitation across primary and generated alternate
  rootfile packages when explicit `pageList`/`pageTarget` metadata and NCX
  `navList`/`navTarget` metadata contain no usable target content.
- `EpubWriter` now skips targetless `navList` sections before reserving their
  preferred NCX IDs, so omitted empty `navList` records do not force later
  valid `navList` records with the same preferred ID to gain synthetic `-2`
  suffixes.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages with targetless explicit page-list metadata, a targetless `navList`
  using a shared preferred ID, and a later valid `navList` using that same
  preferred ID.
- The generated NCX files prove empty `pageList` output is omitted, unusable
  labels are not serialized, and the later valid `navList` keeps the
  unsuffixed preferred ID.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate empty-list resources do not leak into the primary OPF.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, zero
  page-list entries, one valid NCX nav-list entry with the unsuffixed ID, and no
  package-version, empty-page-list, empty-nav-list, missing-content, or
  duplicate-ncx-id diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages can omit targetless NCX page/nav metadata without corrupting
  later generated IDs alongside the already covered NCX page/nav parent target
  output, empty-metadata fallback TOCs, metadata-parent NCX TOCs, headingless
  fallback TOCs, split nested/flat generated NCX TOCs, split/non-split generated
  NCX page lists, NCX metadata, guide-reference sanitation, inferred
  manifest-property, per-spine rendition refinement, package-root,
  package-structures, fallback-style, split-spine, non-linear, media-overlay,
  and rendition-suppression paths. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the covered generated
  empty-NCX-list-ID-reservation/NCX-page-list-nav-list-parent-target/
  empty-metadata-NCX-fallback-TOC/metadata-parent-NCX-TOC/
  headingless-NCX-fallback-TOC/split-nested-NCX-TOC/split-NCX-TOC/
  split-NCX-page-list/generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 NCX Page/Nav Parent Targets OPF2 Slice

- Focused PHP coverage is now 948 behavior tests and 15,237 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` NCX navigation sanitation across primary and generated alternate
  rootfile packages when `pageList` `pageTarget` or `navList` `navTarget`
  parent entries are labels without direct hrefs.
- `EpubWriter` now resolves href-less `pageTarget` and `navTarget` parents to
  the first descendant href, preserves nested child targets, and omits
  `pageList`/`navList` sections when rendering leaves no target content.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages with href-less page-list and nav-list parents plus level-2 child
  targets.
- The generated NCX files prove parent `pageTarget` and `navTarget` records
  point at the first child content source while preserving nested child target
  records, increasing `playOrder` values, and value/type/class metadata.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate parent-list resources do not leak into the primary OPF.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, parent
  and child page-list entries, parent and child NCX nav-list entries, and no
  package-version, empty-page-list, empty-nav-list, NCX content-src,
  fragment/path, or play-order diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages can serialize href-less NCX page/nav parents into valid target
  records alongside the already covered empty-metadata fallback TOCs,
  metadata-parent NCX TOCs, headingless fallback TOCs, split nested/flat
  generated NCX TOCs, split/non-split generated NCX page lists, NCX metadata,
  guide-reference sanitation, inferred manifest-property, per-spine rendition
  refinement, package-root, package-structures, fallback-style, split-spine,
  non-linear, media-overlay, and rendition-suppression paths. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB2 graph validation
  beyond the covered generated NCX-page-list-nav-list-parent-target/
  empty-metadata-NCX-fallback-TOC/metadata-parent-NCX-TOC/
  headingless-NCX-fallback-TOC/split-nested-NCX-TOC/split-NCX-TOC/
  split-NCX-page-list/generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Empty Metadata NCX Fallback TOC OPF2 Slice

- Focused PHP coverage is now 946 behavior tests and 15,191 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` explicit metadata TOC sanitation across primary and generated
  alternate rootfile packages when every metadata TOC entry lacks a usable
  target.
- `EpubWriter` now reuses the NCX title fallback when metadata TOC rendering
  produces no `navPoint` content after href-less target filtering.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages whose explicit metadata TOC entries have labels but no hrefs or
  descendant hrefs.
- The generated NCX files prove `navMap` output falls back to the package title
  as a single `navPoint`, targets the generated chapter XHTML file directly
  without inventing a fragment, omits the unusable metadata labels, and records
  `dtb:depth` as 1.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate fallback resources do not leak into the primary OPF.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, one
  title-derived TOC entry with navpoint ID/play-order metadata, and no
  package-version, empty-navMap, NCX content-src, or fragment/path diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages avoid empty NCX `navMap` output when explicit metadata TOCs
  contain no usable targets, alongside the already covered metadata-parent
  NCX TOCs, headingless fallback TOCs, split nested/flat generated NCX TOCs,
  split/non-split generated NCX page lists, NCX metadata, guide-reference
  sanitation, inferred manifest-property, per-spine rendition refinement,
  package-root, package-structures, fallback-style, split-spine, non-linear,
  media-overlay, and rendition-suppression paths. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition EPUB2 graph validation beyond the
  covered generated empty-metadata-NCX-fallback-TOC/metadata-parent-NCX-TOC/
  headingless-NCX-fallback-TOC/split-nested-NCX-TOC/split-NCX-TOC/
  split-NCX-page-list/generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Metadata NCX Parent TOC OPF2 Slice

- Focused PHP coverage is now 944 behavior tests and 15,149 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` explicit metadata TOC coverage across primary and generated
  alternate rootfile packages when a parent TOC entry is non-clickable.
- `EpubWriter` now emits NCX `navPoint` content for href-less metadata parents
  by targeting the first descendant href, while skipping empty-target subtrees
  so EPUB2 NCX output remains valid without adding EPUB3 `nav.xhtml` output.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages with level-1 `part` parents and level-2 child entries.
- The generated NCX files prove parent `navPoint` records target the first
  child content source while preserving nested child `navPoint` records,
  increasing `playOrder` values, and parent type/class metadata.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate metadata-parent TOC resources do not leak into the
  primary OPF.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, three TOC
  entries including the parent with its descendant href, and no
  package-version, NCX content-src, fragment/path, or play-order diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages can serialize href-less explicit metadata TOC parents into
  valid NCX alongside the already covered headingless fallback TOCs, split
  nested/flat generated NCX TOCs, split/non-split generated NCX page lists, NCX
  metadata, guide-reference sanitation, inferred manifest-property, per-spine
  rendition refinement, package-root, package-structures, fallback-style,
  split-spine, non-linear, media-overlay, and rendition-suppression paths. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB2
  graph validation beyond the covered generated metadata-parent-NCX-TOC/
  headingless-NCX-fallback-TOC/split-nested-NCX-TOC/split-NCX-TOC/
  split-NCX-page-list/generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Headingless NCX Fallback TOC OPF2 Slice

- Focused PHP coverage is now 937 behavior tests and 15,100 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` generated NCX coverage across primary and generated alternate
  rootfile packages when no heading-derived TOC entries exist.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages whose spine XHTML starts with prose rather than headings.
- The generated NCX files prove `navMap` output falls back to the package title
  as a single `navPoint`, targets the generated chapter XHTML file directly
  without inventing a fragment, and records `dtb:depth` as 1.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate fallback TOC resources do not leak into the primary OPF.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, one
  title-derived TOC entry with navpoint ID/play-order metadata, and no
  package-version, NCX content-src, or missing-content diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages generate headingless title-derived NCX fallback TOCs alongside
  the already covered split nested/flat generated NCX TOCs, split/non-split
  generated NCX page lists, NCX metadata, guide-reference sanitation, inferred
  manifest-property, per-spine rendition refinement, package-root,
  package-structures, fallback-style, split-spine, non-linear, media-overlay,
  and rendition-suppression paths. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the covered generated
  headingless-NCX-fallback-TOC/split-nested-NCX-TOC/split-NCX-TOC/
  split-NCX-page-list/generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Split Generated Nested NCX TOC OPF2 Slice

- Focused PHP coverage is now 935 behavior tests and 15,056 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` heading-derived TOC hierarchy coverage across split primary and
  generated alternate rootfile packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages with `splitLevel=1` and H1/H2/H1 headings across generated spine
  chapters.
- The generated split XHTML chapters prove the first H1 parent and nested H2
  section remain together in the first generated spine file while the second H1
  moves to the second generated spine file.
- The generated NCX files prove `navMap` output nests the level-2 `navPoint`
  under the first level-1 `navPoint`, emits increasing `playOrder` values, and
  records `dtb:depth` as 2.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate nested TOC targets do not leak into the primary OPF.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, split
  generated TOC entries with level 1/2/1 plus navpoint IDs and play-order
  metadata, and no package-version, NCX content-src, or play-order diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages derive nested split NCX TOCs from generated XHTML headings
  alongside the already covered flat split generated NCX TOCs, split/non-split
  generated NCX page lists, NCX metadata, guide-reference sanitation, inferred
  manifest-property, per-spine rendition refinement, package-root,
  package-structures, fallback-style, split-spine, non-linear, media-overlay,
  and rendition-suppression paths. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the covered generated
  split-nested-NCX-TOC/split-NCX-TOC/split-NCX-page-list/
  generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Split Generated NCX TOC OPF2 Slice

- Focused PHP coverage is now 933 behavior tests and 15,004 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` heading-derived TOC coverage across split primary and generated
  alternate rootfile packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages with `splitLevel=1` and H1 headings on separate generated spine
  chapters.
- The generated split XHTML chapters prove heading targets remain isolated to
  their generated spine files.
- The generated NCX files prove `navMap` content references point to
  `text/chapter.xhtml` and `text/chapter-2.xhtml`, or alternate
  `split-toc.xhtml` and `split-toc-2.xhtml`, as appropriate.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate split TOC targets do not leak into the primary OPF.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, split
  generated TOC text/href metadata, and no package-version or NCX content-src
  diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages derive split NCX TOCs from generated XHTML headings alongside
  the already covered split/non-split generated NCX page lists, NCX metadata,
  guide-reference sanitation, inferred manifest-property, per-spine rendition
  refinement, package-root, package-structures, fallback-style, split-spine,
  non-linear, media-overlay, and rendition-suppression paths. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB2 graph validation
  beyond the covered generated split-NCX-TOC/split-NCX-page-list/
  generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Split Generated NCX Page-List OPF2 Slice

- Focused PHP coverage is now 931 behavior tests and 14,954 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` pagebreak-derived page-list coverage across split primary and
  generated alternate rootfile packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages with `splitLevel=1` and XHTML pagebreak spans on separate generated
  spine chapters.
- The generated split XHTML chapters prove pagebreak targets remain isolated to
  their generated spine files.
- The generated NCX files prove `pageList` content references point to
  `text/chapter.xhtml` and `text/chapter-2.xhtml`, or alternate
  `split-pages.xhtml` and `split-pages-2.xhtml`, as appropriate.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate split page-list targets do not leak into the primary OPF.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, split
  generated page-list href/value metadata, and no package-version or NCX
  content-src diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages derive split NCX page lists from generated XHTML pagebreaks
  alongside the already covered non-split generated NCX page lists, NCX
  metadata, guide-reference sanitation, inferred manifest-property, per-spine
  rendition refinement, package-root, package-structures, fallback-style,
  split-spine, non-linear, media-overlay, and rendition-suppression paths. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition EPUB2
  graph validation beyond the covered generated split-NCX-page-list/
  generated-NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Generated NCX Page-List OPF2 Slice

- Focused PHP coverage is now 929 behavior tests and 14,904 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` pagebreak-derived page-list coverage across primary and generated
  alternate rootfile packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages without explicit page-list metadata but with XHTML pagebreak spans
  in the relevant chapter payloads.
- The generated XHTML proves those spans are enriched with
  `role="doc-pagebreak"` and `epub:type="pagebreak"`, and the generated NCX
  files prove `pageList` sections target the generated pagebreak fragments.
- The generated OPF2 packages prove spine `toc` attributes point at custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  generated alternate page-list targets do not leak into the primary package.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, generated
  page-list href/value metadata, and no package-version or NCX content-src
  diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages derive NCX page lists from generated XHTML pagebreaks
  alongside the already covered NCX metadata, guide-reference sanitation,
  inferred manifest-property, per-spine rendition refinement, package-root,
  package-structures, fallback-style, split-spine, non-linear, media-overlay,
  and rendition-suppression paths. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB2 graph validation beyond the covered
  generated NCX-page-list/NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 NCX Compatibility Metadata OPF2 Slice

- Focused PHP coverage is now 927 behavior tests and 14,846 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` NCX writer/read-back coverage across primary and generated alternate
  rootfile packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages with custom NCX IDs and paths, UID/depth/page-count metadata,
  document title and author language metadata, extra NCX head metadata,
  explicit TOC entries, page-list entries, and NCX `navList` sections.
- The generated OPF2 packages prove NCX resources are manifested as
  `application/x-dtbncx+xml`, spine `toc` attributes point at the custom NCX
  IDs, EPUB3 `nav.xhtml` output and nav manifest properties are omitted, and
  primary/alternate NCX resources do not leak across package boundaries.
- Read-back records `epubPackageVersion=2.0`, NCX-only toc resources, spine toc
  IDs, NCX metadata, TOC/page-list/navList entries, and no package-version or
  NCX content-src diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages round-trip richer NCX metadata alongside the already covered
  guide-reference sanitation, inferred manifest-property, per-spine rendition
  refinement, package-root, package-structures, fallback-style, split-spine,
  non-linear, media-overlay, and rendition-suppression paths. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB2 graph validation
  beyond the covered generated NCX-metadata/guide-reference/
  inferred-manifest-property/per-spine-rendition-refinement/package-root/
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, remaining OPF 2 guide/NCX edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Guide-Reference Sanitation OPF2 Slice

- Focused PHP coverage is now 925 behavior tests and 14,742 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` guide-reference writer coverage across primary and generated
  alternate rootfile packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages carrying valid local chapter-fragment guide references, valid
  remote guide references, and empty-title guide references that fall back to
  type-derived titles.
- The same coverage feeds invalid guide references with missing or invalid
  types, missing hrefs, missing resources, missing or invalid fragments,
  query-only or fragment-only hrefs, data/file URLs, absolute or
  protocol-relative paths, backslashes, and encoded dot segments.
- The generated OPF2 packages prove only the valid guide references are
  preserved, invalid guide-reference records are omitted, and generated
  alternate guide references do not leak into the primary OPF.
- Read-back records `epubPackageVersion=2.0` with only the valid guide
  references and without guide-reference or package-version diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages sanitize OPF guide references alongside the already covered
  inferred manifest-property, per-spine rendition refinement, package-root,
  package-structures, fallback-style, split-spine, non-linear, media-overlay,
  and rendition-suppression paths. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB2 graph validation beyond the covered
  generated guide-reference/inferred-manifest-property/per-spine-rendition-
  refinement/package-root/package-structures/fallback-style/split-spine/
  non-linear/media-overlay/rendition-suppression paths, OPF 2 NCX edge-case
  parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Inferred XHTML Manifest-Property Suppression OPF2 Slice

- Focused PHP coverage is now 923 behavior tests and 14,660 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` inferred manifest-property suppression across primary and generated
  alternate rootfile packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages with XHTML content that would infer EPUB3 `mathml`, `svg`,
  `scripted`, `switch`, and `remote-resources` manifest properties from
  MathML, inline SVG, scripts, `epub:switch` content, and remote image
  references.
- The generated OPF2 packages prove chapter manifest items remain plain
  `application/xhtml+xml` entries without inferred EPUB3 `properties`
  attributes while the rich XHTML chapter payloads are preserved.
- Read-back records `epubPackageVersion=2.0` with empty `manifestProperties`
  and without `missing-manifest-required-property` diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages suppress EPUB3-only inferred XHTML manifest properties
  alongside the already covered per-spine rendition refinement, package-root,
  package-structures, fallback-style, split-spine, non-linear, media-overlay,
  and rendition-suppression paths. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB2 graph validation beyond the covered
  generated inferred-manifest-property/per-spine-rendition-refinement/
  package-root/package-structures/fallback-style/split-spine/non-linear/
  media-overlay/rendition-suppression paths, OPF 2 NCX/guide edge-case parity
  beyond existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Per-Spine Rendition Refinement Suppression OPF2 Slice

- Focused PHP coverage is now 922 behavior tests and 14,627 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` spine metadata suppression across primary and generated alternate
  rootfile packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  split-spine packages carrying per-spine rendition layout, orientation, flow,
  and spread refinement metadata.
- The generated OPF2 packages prove itemref IDs and idrefs remain serialized
  while EPUB3-only `rendition:*` refinement metadata and generated rendition
  itemref `properties` attributes are omitted.
- Read-back records `epubPackageVersion=2.0` with empty itemref `properties`
  and without reconstructing `renditionLayout`, `renditionOrientation`,
  `renditionFlow`, or `renditionSpread`, and without `invalid-spine-property`
  or `conflicting-itemref-rendition` diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages suppress EPUB3-only per-spine rendition refinement metadata
  alongside the already covered package-root, package-structures,
  fallback-style, split-spine, non-linear, media-overlay, and
  rendition-suppression paths. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the covered generated
  per-spine-rendition-refinement/package-root/package-structures/
  fallback-style/split-spine/non-linear/media-overlay/rendition-suppression
  paths, OPF 2 NCX/guide edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB2 Package-Root Direction/Language Suppression OPF2 Slice

- Focused PHP coverage is now 921 behavior tests and 14,593 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` package-root suppression across primary and generated alternate
  rootfile packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages carrying `epubPackageDirection`/`epubPackageLanguage` and
  `packageDirection`/`packageLanguage` aliases.
- The generated OPF2 packages prove Dublin Core `dc:language` metadata remains
  while EPUB3-only package-root `dir` and `xml:lang` attributes are omitted.
- Read-back records `epubPackageVersion=2.0` and the Dublin Core language
  without reconstructing `epubPackageDirection` or `epubPackageLanguage`, and
  without `invalid-package-dir` or `invalid-package-language` diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages suppress EPUB3-only package-root direction/language attributes
  alongside the already covered package-structures, fallback-style,
  split-spine, non-linear, media-overlay, and rendition-suppression paths. It
  does not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition EPUB2 graph
  validation beyond the covered generated package-root/package-structures/
  fallback-style/split-spine/non-linear/media-overlay/rendition-suppression
  paths, OPF 2 NCX/guide edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB2 Package Structures Suppression OPF2 Slice

- Focused PHP coverage is now 920 behavior tests and 14,573 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` package suppression across primary and generated alternate rootfile
  packages.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages carrying `packagePrefix`, package metadata properties, package
  metadata links, bindings, and collections.
- The generated OPF2 packages prove Dublin Core package metadata remains while
  EPUB3-only `<meta property=...>` records, package `<link>` elements,
  `<bindings>`, `<collection>` elements, and package `prefix` attributes are
  omitted.
- Read-back records `epubPackageVersion=2.0` without reconstructing
  `epubMetadataProperties`, `epubMetadataLinks`, `epubBindings`, or
  `epubCollections`, and without `missing-package-link-resource`,
  `invalid-package-meta-property`, or `invalid-binding-handler` diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages suppress EPUB3-only package metadata structures alongside the
  already covered fallback-style, split-spine, non-linear, media-overlay, and
  rendition-suppression paths. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the covered generated
  package-structures/fallback-style/split-spine/non-linear/media-overlay/
  rendition-suppression paths, OPF 2 NCX/guide edge-case parity beyond existing
  focused cases, DRM decryption, XML signature cryptographic validation, or
  broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Manifest Fallback-Style Suppression OPF2 Slice

- Focused PHP coverage is now 919 behavior tests and 14,545 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` manifest attribute output across primary and generated alternate
  rootfile packages.
- `EpubWriter` now lets sanitized manifest attributes keep valid `fallback`
  targets while gating the EPUB3-only `fallback-style` attribute behind
  non-EPUB2 mode.
- Focused writer coverage now writes primary and generated alternate `epub2`
  packages with valid XHTML fallback resources plus CSS fallback-style
  resources.
- The generated OPF2 packages prove chapter manifest items preserve
  `fallback="..."` while no `fallback-style` attributes are emitted.
- Read-back records `epubPackageVersion=2.0`, preserves fallback target
  metadata, does not reconstruct `fallbackStyle` metadata, and avoids
  `missing-manifest-fallback` and `missing-manifest-fallback-style`
  diagnostics.
- Dependency-closure checkpoint: this proves primary and generated alternate
  EPUB2 packages suppress the EPUB3-only manifest fallback-style hook while
  retaining valid fallback chains. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the covered generated
  packages/fallback-style/split-spine/non-linear/media-overlay/rendition-
  suppression paths, OPF 2 NCX/guide edge-case parity beyond existing focused
  cases, DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB2 Generated Alternate-Rootfile Package Rendition/Page-Progression Suppression OPF2 Slice

- Focused PHP coverage is now 918 behavior tests and 14,523 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` package-spine suppression across generated OCF alternate rootfiles.
- Focused writer coverage now writes an `epub2` package with a generated
  alternate rootfile at `ALT/rendition.opf` carrying `packagePrefix`,
  `renditionLayout`, `renditionOrientation`, `renditionSpread`,
  `renditionFlow`, `renditionViewport`, and `pageProgressionDirection`
  metadata.
- The generated alternate OPF2 package proves output keeps a plain
  `<spine toc="toc">` while omitting `rendition:*` metadata,
  `page-progression-direction`, and EPUB3 package `prefix` attributes.
- Alternate package read-back records `epubPackageVersion=2.0` without
  reconstructing suppressed rendition/page-progression metadata and without
  `unsupported-package-version` or `conflicting-package-rendition`
  diagnostics.
- Dependency-closure checkpoint: this proves generated EPUB2 alternate
  rootfiles suppress package-level EPUB3 rendition and spine page-progression
  metadata alongside the already covered primary package, split-spine,
  non-linear, and media-overlay paths. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition EPUB2 graph validation beyond the covered
  generated packages/split-spine/non-linear/media-overlay/rendition-suppression
  paths, OPF 2 NCX/guide edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB2 Package Rendition/Page-Progression Suppression OPF2 Slice

- Focused PHP coverage is now 917 behavior tests and 14,509 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` output by removing remaining EPUB3 package-spine leakage from OPF
  2.0 packages.
- `EpubWriter` already suppressed package-level `rendition:*` metadata in
  `epub2` mode; it now also blanks `pageProgressionDirection` for OPF2 packages
  so `<spine>` does not emit the EPUB3 `page-progression-direction` attribute.
- Focused writer coverage now writes an `epub2` package carrying
  `epubRenditionLayout`, `epubRenditionOrientation`, `epubRenditionSpread`,
  `epubRenditionFlow`, `epubRenditionViewport`, and
  `epubPageProgressionDirection`.
- The generated OPF2 package proves output keeps a plain `<spine toc="toc">`
  while omitting `rendition:*` metadata, `page-progression-direction`, and EPUB3
  package `prefix` attributes.
- Read-back records `epubPackageVersion=2.0` without reconstructing suppressed
  rendition/page-progression metadata and without `unsupported-package-version`
  or `conflicting-package-rendition` diagnostics.
- Dependency-closure checkpoint: this proves primary EPUB2 packages suppress
  package-level EPUB3 rendition and spine page-progression metadata alongside
  the already covered split-spine, non-linear, and media-overlay paths. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB2 graph validation
  beyond the covered generated
  packages/split-spine/non-linear/media-overlay/rendition-suppression paths,
  OPF 2 NCX/guide edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB2 Generated Alternate-Rootfile Split Linear Spine Manifest/Itemref Suppression OPF2 Slice

- Focused PHP coverage is now 916 behavior tests and 14,495 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` split-spine property suppression across generated OCF alternate
  rootfiles.
- Focused writer coverage now writes an `epub2` package with a generated
  alternate rootfile at `ALT/split-linear.opf`, `splitLevel=1`, explicit
  alternate `spineManifestIds`, EPUB3-only `spineManifestProperties`, alternate
  `spineItemIds`, and rendition/page-spread `spineItemProperties`, while
  primary writer options carry separate IDs and properties.
- The generated alternate OPF2 package proves alternate manifest IDs and
  itemref IDs are preserved while `nav.xhtml`, primary ID leakage, EPUB3 nav
  manifest properties, EPUB3 required manifest properties, and EPUB3 rendition
  itemref properties are omitted.
- Alternate package read-back records `epubPackageVersion=2.0` with empty
  itemref `properties`, no reconstructed `pageSpread` or rendition metadata,
  and clean `unsupported-package-version`, `invalid-spine-property`, and
  `missing-manifest-required-property` diagnostics.
- Dependency-closure checkpoint: this proves generated EPUB2 alternate split
  linear spine output follows the same OPF2 property-suppression rules now
  covered for the primary split-spine, non-linear, and media-overlay paths. It
  does not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition EPUB2 graph
  validation beyond the covered generated
  packages/split-spine/non-linear/media-overlay-suppression paths, OPF 2
  NCX/guide edge-case parity beyond existing focused cases, DRM decryption,
  XML signature cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Split Linear Spine Manifest/Itemref Suppression OPF2 Slice

- Focused PHP coverage is now 915 behavior tests and 14,464 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` output coverage across split linear spine packages.
- Focused writer coverage now writes an `epub2` package with `splitLevel=1`,
  explicit `spineManifestIds`, EPUB3-only `spineManifestProperties`, explicit
  `spineItemIds`, and rendition/page-spread `spineItemProperties`.
- The generated OPF2 package proves custom manifest IDs and itemref IDs are
  preserved while `nav.xhtml`, EPUB3 nav manifest properties, EPUB3 required
  manifest properties, and EPUB3 rendition itemref properties are omitted.
- Read-back records `epubPackageVersion=2.0` with empty itemref `properties`,
  no reconstructed `pageSpread` or rendition metadata, and clean
  `unsupported-package-version`, `invalid-spine-property`, and
  `missing-manifest-required-property` diagnostics.
- Dependency-closure checkpoint: this proves split linear EPUB2 output follows
  the same OPF2 property-suppression rules already covered for non-linear and
  media-overlay paths. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB2 graph validation beyond the covered generated
  packages/split-spine/non-linear/media-overlay-suppression paths, OPF 2
  NCX/guide edge-case parity beyond existing focused cases, DRM decryption,
  XML signature cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Generated Alternate-Rootfile Media-Overlay Resource Suppression OPF2 Slice

- Focused PHP coverage is now 914 behavior tests and 14,439 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` generated media-overlay resource suppression across generated OCF
  alternate rootfiles.
- Focused writer coverage now writes an `epub2` package with a generated
  alternate rootfile at `ALT/generated-overlay.opf` carrying `mediaOverlays`
  with pairs for `ALT/text/generated-overlay.xhtml` and no explicit SMIL
  payload.
- The generated alternate package proves the writer does not synthesize
  `ALT/overlays/mo-alt-generated-epub2.smil`, does not manifest the generated
  overlay id/path, and does not emit `media-overlay` attributes or `media:*`
  overlay metadata in the OPF2 package.
- Alternate package read-back records `epubPackageVersion=2.0` without
  reconstructing `mediaOverlay` metadata and without
  `missing-media-overlay-resource` or `missing-media-overlay-duration`
  diagnostics.
- Dependency-closure checkpoint: this proves generated EPUB2 alternate
  rootfiles use the same generated-resource suppression as the primary OPF2
  package path. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB2 graph validation beyond the covered generated
  packages/non-linear/media-overlay-suppression paths, OPF 2 NCX/guide
  edge-case parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Generated Media-Overlay Resource Suppression OPF2 Slice

- Focused PHP coverage is now 913 behavior tests and 14,430 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` output for structured `mediaOverlay` entries that include overlay
  pairs but no explicit SMIL payload.
- Focused writer coverage now writes an `epub2` package carrying
  `epubMediaOverlays` with pairs for `OEBPS/text/chapter.xhtml`.
- The generated EPUB proves the writer does not synthesize
  `OEBPS/overlays/mo-generated-epub2.smil`, does not manifest the generated
  overlay id/path, and does not emit `media-overlay` attributes or `media:*`
  overlay metadata in the OPF2 package.
- Read-back records `epubPackageVersion=2.0` without reconstructing
  `mediaOverlay` metadata and without `missing-media-overlay-resource` or
  `missing-media-overlay-duration` diagnostics.
- Dependency-closure checkpoint: this proves the bounded EPUB2 writer suppresses
  generated EPUB3 media-overlay resources, not just media-overlay OPF metadata
  and attributes. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB2 graph validation beyond the covered generated
  packages/non-linear/media-overlay-suppression paths, OPF 2 NCX/guide
  edge-case parity beyond existing focused cases, DRM decryption, XML signature
  cryptographic validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Generated Alternate-Rootfile Non-Linear Spine Itemref OPF2 Slice

- Focused PHP coverage is now 912 behavior tests and 14,421 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` non-linear itemref suppression across generated OCF alternate
  rootfiles.
- Focused writer coverage now writes an `epub2` package with a generated
  alternate rootfile at `ALT/nonlinear.opf` carrying a structured
  `nonLinearSpineItems` entry for `ALT/text/appendix.xhtml` with EPUB3
  rendition/page-spread metadata.
- The generated alternate OPF proves the appendix manifest item is emitted and
  the spine uses
  `<itemref id="alt-appendix-ref" idref="appendix" linear="no"/>` without
  `rendition:page-spread`, `properties="rendition:..."`, or `properties="nav"`.
- Alternate package read-back records `epubPackageVersion=2.0`, the
  non-linear resource, the second spine itemref id/idref/`linear=false`,
  normalized empty `properties`, no reconstructed `pageSpread` or rendition
  metadata, and clean `unsupported-package-version` and
  `invalid-spine-property` diagnostics.
- Dependency-closure checkpoint: this proves generated EPUB2 alternate
  rootfiles use the same OPF2 non-linear spine itemref suppression as the
  primary package path. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition EPUB2 graph validation beyond the covered generated
  packages/non-linear/primary-and-alternate media-overlay-suppression paths,
  OPF 2 NCX/guide edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB2 Generated Alternate-Rootfile Media-Overlay Suppression OPF2 Slice

- Focused PHP coverage is now 911 behavior tests and 14,406 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` media-overlay suppression across generated OCF alternate rootfiles.
- Focused writer coverage now writes an `epub2` package with a generated
  alternate rootfile at `ALT/overlay.opf` carrying structured
  `mediaOverlays` plus an explicit `ALT/overlays/epub2.smil` payload.
- The generated alternate OPF proves the supplied SMIL resource is preserved as
  a plain `application/smil+xml` manifest item, while no `media-overlay`
  attributes, `media:duration`, or `media:playback-active-class` metadata are
  emitted.
- Alternate package read-back records `epubPackageVersion=2.0` without
  reconstructing spine or manifest `mediaOverlay` metadata and without
  `unsupported-package-version`, `missing-media-overlay-resource`, or
  `missing-media-overlay-duration` diagnostics.
- Dependency-closure checkpoint: this proves generated EPUB2 alternate
  rootfiles use the same OPF2 media-overlay suppression as the primary package
  path. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  EPUB2 graph validation beyond the covered generated
  packages/non-linear/primary-and-alternate media-overlay-suppression paths,
  OPF 2 NCX/guide edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB2 Media-Overlay Suppression OPF2 Slice

- Focused PHP coverage is now 910 behavior tests and 14,394 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` writer output by removing EPUB3 media-overlay wiring from OPF 2.0
  packages.
- `EpubWriter` now gates generated media overlay resources and manifest
  `media-overlay` attributes behind non-EPUB2 writer mode. Sanitized manifest
  attributes also skip `media-overlay` when OPF2 output is selected.
- Explicit SMIL resource payloads can still be kept as ordinary manifest
  resources in EPUB2 output; the EPUB2 writer simply avoids attaching EPUB3
  media-overlay semantics to content-document manifest items.
- Focused writer coverage now writes an `epub2` package carrying
  `epubMediaOverlays` plus an explicit `OEBPS/overlays/epub2.smil` payload.
- The generated OPF proves the supplied SMIL resource is preserved as a plain
  `application/smil+xml` manifest item, while no `media-overlay` attributes,
  `media:duration`, or `media:playback-active-class` metadata are emitted.
- Read-back records `epubPackageVersion=2.0` without reconstructing spine or
  manifest `mediaOverlay` metadata and without
  `unsupported-package-version`, `missing-media-overlay-resource`, or
  `missing-media-overlay-duration` diagnostics.
- Dependency-closure checkpoint: this proves the bounded EPUB2 writer does not
  leak EPUB3 media-overlay package semantics into OPF2 output. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition EPUB2 graph validation
  beyond the covered generated packages/non-linear/media-overlay-suppression
  paths, OPF 2 NCX/guide edge-case parity beyond existing focused cases, DRM
  decryption, XML signature cryptographic validation, or broader TeX/MathML
  parser parity.

## 2026-06-23 EPUB2 Non-Linear Spine Itemref OPF2 Slice

- Focused PHP coverage is now 909 behavior tests and 14,382 full-suite
  assertions across the Pandoc lane. This slice tightens bounded native PHP
  `epub2` writer output for non-linear spine resources.
- `EpubWriter` now suppresses EPUB3 itemref `properties` on OPF 2.0
  non-linear spine itemrefs while retaining `linear="no"` and the structured
  manifest/resource metadata needed for read-back.
- Focused writer coverage now writes an `epub2` package with
  `OEBPS/text/appendix.xhtml` as a structured non-linear resource carrying
  page-spread/rendition metadata that would produce EPUB3 itemref properties.
- The generated OPF proves the appendix manifest item is emitted and the spine
  uses `<itemref id="appendix-ref" idref="appendix" linear="no"/>` without
  `rendition:page-spread`, `properties="rendition:..."`, or `properties="nav"`.
- Read-back records `epubPackageVersion=2.0`, the non-linear resource, the
  second spine itemref id/idref/`linear=false`, normalized empty `properties`,
  no reconstructed `pageSpread` or rendition metadata, and clean
  `unsupported-package-version` and `invalid-spine-property` diagnostics.
- Dependency-closure checkpoint: this proves the bounded EPUB2 writer keeps
  OPF2 non-linear spine output valid instead of leaking EPUB3 rendition
  properties into `<itemref>`. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the covered generated
  packages/non-linear itemref path, OPF 2 NCX/guide edge-case parity beyond
  existing focused cases, DRM decryption, XML signature cryptographic
  validation, or broader TeX/MathML parser parity.

## 2026-06-23 EPUB2 Generated Alternate-Rootfile OPF2/NCX Slice

- Focused PHP coverage is now 908 behavior tests and 14,367 full-suite
  assertions across the Pandoc lane. This slice extends bounded native PHP
  `epub2` writer coverage from the primary package into generated OCF alternate
  rootfiles.
- Focused writer coverage now writes an `epub2` package with a primary
  `OEBPS/package.opf` and a generated alternate rootfile at `ALT/print.opf`.
- Both generated packages emit OPF 2.0 with NCX navigation, and neither package
  emits `nav.xhtml` payloads or EPUB3 `properties="nav"` manifest items.
- The alternate OPF preserves isolated package metadata: identifier, title,
  language, custom `ALT/print-toc.ncx` NCX path, OPF2 `meta name="cover"`
  metadata, cover guide reference, text guide reference, image manifest item,
  and readable XHTML spine body.
- `EpubReader` alternate package summaries now preserve `epubCoverItemId` from
  OPF2 `meta name="cover"` metadata, matching the primary package metadata
  surface and keeping alternate cover provenance inspectable.
- Read-back records the alternate package version, cover item id, cover image,
  guide references, readable resources, body text, and clean
  `unsupported-package-version`, `missing-package-modified`, and
  `invalid-package-opf2-meta` diagnostics.
- Dependency-closure checkpoint: this proves generated alternate rootfiles use
  the bounded EPUB2 OPF2/NCX mode instead of accidentally inheriting EPUB3
  `nav.xhtml` package behavior. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition EPUB2 graph validation beyond the generated
  alternate rootfile path, OPF 2 NCX/guide edge-case parity beyond the covered
  generated packages, DRM decryption, XML signature cryptographic validation,
  or broader TeX/MathML parser parity.

## 2026-06-23 Bounded EPUB2 Writer Output Slice

- Focused PHP coverage is now 907 behavior tests and 14,324 full-suite
  assertions across the Pandoc lane. This slice registers the upstream `epub2`
  output token as partial native PHP support.
- `PandocConverter` now passes the canonical output format into `EpubWriter`,
  and `PandocFormatRegistry` maps `epub2` to `EpubWriter`.
- `EpubWriter` now has a bounded writer-format mode: `epub3` keeps OPF 3.0
  with `nav.xhtml`, while `epub2` emits OPF 2.0, defaults to NCX navigation,
  omits the EPUB3 nav manifest item and `nav.xhtml` payload, and suppresses
  EPUB3-only package metadata/link/binding/collection blocks. The same EPUB2
  mode emits OPF2 cover metadata with `meta name="cover"` and keeps OPF guide
  references.
- Focused writer coverage now writes a generated `epub2` package with
  `OEBPS/package.opf`, `OEBPS/text/chapter.xhtml`, `OEBPS/toc.ncx`, and
  `OEBPS/images/cover.png`.
- The generated OPF proves `version="2.0"`, a `toc.ncx` NCX manifest item, and
  `<spine toc="toc">` are emitted while an OPF2 cover meta record, cover guide
  reference, text guide reference, and image manifest item are preserved.
- `nav.xhtml`, `properties="nav"`, `properties="cover-image"`,
  `dcterms:modified`, and EPUB3 package prefix attributes are absent from the
  generated EPUB2 OPF.
- Read-back through the existing EPUB reader records `epubPackageVersion=2.0`,
  `epubCoverItemId=cover`, `epubCoverImage=OEBPS/images/cover.png`, image
  resources, guide references, title, author, language, and heading body
  content, and avoids `unsupported-package-version` and
  `invalid-package-opf2-meta` diagnostics.
- Registry/converter coverage now reports `PandocConverter::canWrite('epub2')`
  as true, `phpOutputSupport()['epub2']` as partial, and unsupported upstream
  output formats at 58.
- Dependency-closure checkpoint: this closes the first direct native PHP EPUB2
  output token gap with a bounded OPF 2.0/NCX writer mode. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition EPUB2 package graph
  validation, OPF 2 NCX edge-case parity beyond the covered generated package,
  DRM decryption, XML signature cryptographic validation, or broader
  TeX/MathML parser parity.

## 2026-06-23 EPUB Primary/Alternate Split Package And Collection Link Href-Fragment Slice

- Focused PHP coverage is now 906 behavior tests and 14,285 full-suite
  assertions across the Pandoc lane. This slice extends generated EPUB3 OPF
  package-link href-fragment coverage across primary and alternate rootfiles
  with multiple generated linear spine XHTML files.
- `EpubWriter` now threads generated XML payloads into package-link href
  validation so package metadata links, collection metadata links, and direct
  collection member links that target known XML/XHTML resources only emit
  fragment hrefs when the target id exists.
- Focused writer coverage now writes a `splitLevel=1` primary EPUB3 package
  and a `splitLevel=1` alternate rootfile at `ALT/split-links.opf`.
- Primary package metadata links, collection metadata links, and direct
  collection member links preserve valid hrefs to
  `text/chapter.xhtml#split-package-link-one` and
  `text/chapter-2.xhtml#split-package-link-two`.
- Alternate package metadata links, collection metadata links, and direct
  collection member links preserve valid hrefs to
  `text/split-links.xhtml#alt-split-package-link-one` and
  `text/split-links-2.xhtml#alt-split-package-link-two`.
- Missing split-fragment hrefs are omitted from package metadata links,
  collection metadata links, and direct collection member links before OPF
  output, and do not round-trip.
- The generated alternate split link metadata does not leak into the primary
  OPF.
- Primary and alternate read-back avoid `missing-package-link-resource`,
  `missing-package-link-fragment`, `malformed-package-link-resource`, and
  `invalid-package-link-manifest-resource` diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  package links can safely target generated primary and alternate split XHTML
  fragments, and stale split-fragment links are sanitized before output.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary and
  alternate split-spine OPF package-link href validation. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML
  parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary/Alternate Split Package Metadata Resource-Refines Slice

- Focused PHP coverage is now 904 behavior tests and 14,231 full-suite
  assertions across the Pandoc lane. This slice extends generated EPUB3 OPF
  metadata resource-refines coverage across primary and alternate rootfiles
  with multiple generated linear spine XHTML files.
- Focused writer coverage now writes a `splitLevel=1` primary EPUB3 package
  and a `splitLevel=1` alternate rootfile at `ALT/split-refines.opf`.
- Primary package metadata properties preserve valid `refines` targets into
  `text/chapter.xhtml#split-refines-one` and
  `text/chapter-2.xhtml#split-refines-two`.
- Primary package metadata links preserve valid `refines` targets into the
  same split XHTML fragments while linking a generated JSON sidecar resource.
- Alternate package metadata properties preserve valid `refines` targets into
  `text/split-refines.xhtml#alt-split-refines-one` and
  `text/split-refines-2.xhtml#alt-split-refines-two`.
- Alternate package metadata links preserve valid `refines` targets into the
  same alternate split XHTML fragments while linking an alternate JSON sidecar
  resource.
- Missing split-fragment `refines` values are stripped from metadata
  properties and package links while the surrounding metadata/link entries
  remain valid and round-trip without the invalid target.
- The generated alternate split metadata resource and metadata entries do not
  leak into the primary OPF.
- Primary and alternate read-back record valid split-resource `refines` values
  while avoiding `invalid-metadata-refines`,
  `missing-metadata-refines-resource`, `missing-metadata-refines-fragment`,
  `missing-metadata-refines-target`, and
  `invalid-package-link-record-refines` diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  OPF metadata properties and package metadata links can safely refine
  generated primary and alternate split XHTML fragments, and invalid split
  fragment targets are sanitized before output.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary and
  alternate split-spine OPF metadata refines coverage. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML
  parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary/Alternate Split Auxiliary Navigation Metadata Slice

- Focused PHP coverage is now 902 behavior tests and 14,183 full-suite
  assertions across the Pandoc lane. This slice extends generated EPUB3
  auxiliary nav coverage across primary and alternate rootfiles with multiple
  generated linear spine XHTML files.
- Focused writer coverage now writes a `splitLevel=1` primary EPUB3 package
  and a `splitLevel=1` alternate rootfile at `ALT/split-auxiliary-nav.opf`.
- Generated `OEBPS/nav.xhtml` and `ALT/split-auxiliary-nav.xhtml` preserve
  auxiliary `loi` and `lot` sections with `id`, `class`, custom `h3` heading
  level, heading attributes, custom titles, and entry attributes.
- Primary auxiliary links target `text/chapter.xhtml#split-figure-one`,
  `text/chapter-2.xhtml#split-figure-two`, and
  `text/chapter-2.xhtml#split-table-two`.
- Alternate auxiliary links target
  `text/split-auxiliary.xhtml#alt-split-figure-one`,
  `text/split-auxiliary-2.xhtml#alt-split-figure-two`, and
  `text/split-auxiliary-2.xhtml#alt-split-table-two`.
- The generated alternate split auxiliary resources do not leak into the
  primary OPF.
- Primary and alternate read-back record auxiliary section counts, entry counts,
  section attributes, entry attributes, and clean navigation link diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  auxiliary primary and alternate navigation sections now survive split-spine
  output and read-back while targeting the generated split XHTML resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary and
  alternate split-spine auxiliary navigation metadata coverage. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond covered generated alternate package-link/container sidecar
  paths, DRM decryption, XML signature cryptographic validation, broader
  TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary/Alternate Split Explicit Landmark Nav Metadata Slice

- Focused PHP coverage is now 900 behavior tests and 14,148 full-suite
  assertions across the Pandoc lane. This slice extends generated EPUB3
  explicit landmark coverage across primary and alternate rootfiles with
  multiple generated linear spine XHTML files.
- Focused writer coverage now writes a `splitLevel=1` primary EPUB3 package
  and a `splitLevel=1` alternate rootfile at
  `ALT/split-explicit-landmarks.opf`.
- Generated `OEBPS/nav.xhtml` and `ALT/split-explicit-nav.xhtml` preserve
  `epub:type="landmarks guide"`, `id`, `class`, custom `h3` heading level,
  heading attributes, and custom landmark nav titles.
- Explicit primary landmark links target
  `text/chapter.xhtml#explicit-start`,
  `text/chapter-2.xhtml#explicit-bibliography`, and
  `text/chapter-2.xhtml#explicit-glossary`.
- Explicit alternate landmark links target
  `text/split-explicit.xhtml#alt-explicit-start`,
  `text/split-explicit-2.xhtml#alt-explicit-bibliography`, and
  `text/split-explicit-2.xhtml#alt-explicit-glossary`.
- The generated alternate explicit split resources do not leak into the primary
  OPF.
- Primary and alternate read-back record the landmark nav attributes, landmark
  nav titles, landmark entries, and clean navigation diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  explicit primary and alternate landmarks now survive split-spine output and
  read-back while targeting the generated split XHTML resources, independent of
  guide-derived landmark fallback.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary and
  alternate split-spine explicit landmark nav metadata coverage. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond covered generated alternate package-link/container sidecar
  paths, DRM decryption, XML signature cryptographic validation, broader
  TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Split Guide-Derived Landmark Nav Metadata Slice

- Focused PHP coverage is now 898 behavior tests and 14,107 full-suite
  assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 landmark coverage across multiple generated linear
  spine XHTML files when EPUB3 landmarks are derived from alternate OPF guide
  references and explicit alternate landmark nav attributes/title are supplied.
- Focused writer coverage now writes a `splitLevel=1` alternate rootfile at
  `ALT/split-guide-landmarks.opf` with headings split between
  `ALT/text/split-guide.xhtml` and `ALT/text/split-guide-2.xhtml`.
- Generated `ALT/split-guide-nav.xhtml` preserves
  `epub:type="landmarks guide"`, `id`, `class`, custom `h3` heading level,
  heading attributes, and the custom landmark nav title.
- Guide-derived landmark links target
  `text/split-guide.xhtml#alt-split-start`,
  `text/split-guide-2.xhtml#alt-split-bibliography`, and
  `text/split-guide-2.xhtml#alt-split-glossary`.
- Alternate OPF guide references to the nav document and a remote figures
  document remain serialized as guide metadata, but they are not promoted into
  generated EPUB3 landmark nav entries.
- The generated alternate split resources do not leak into the primary OPF.
- Alternate read-back records the landmark nav attributes, landmark nav title,
  guide references, generated landmark entries, and clean navigation
  diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate guide-derived landmarks now survive split-spine output and read-back
  with explicit landmark nav metadata while targeting the generated split XHTML
  resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  split-spine guide-derived landmark nav metadata coverage. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Guide-Derived Landmark Nav Metadata Slice

- Focused PHP coverage is now 897 behavior tests and 14,078 full-suite
  assertions across the Pandoc lane. This slice extends generated primary
  EPUB3 landmark coverage across multiple generated linear spine XHTML files
  when EPUB3 landmarks are derived from OPF guide references and explicit
  landmark nav attributes/title are supplied.
- Focused writer coverage now writes a `splitLevel=1` EPUB3 package with
  headings split between `OEBPS/text/chapter.xhtml` and
  `OEBPS/text/chapter-2.xhtml`.
- Generated `OEBPS/nav.xhtml` preserves `epub:type="landmarks guide"`, `id`,
  `class`, custom `h3` heading level, heading attributes, and the custom
  landmark nav title.
- Guide-derived landmark links target `text/chapter.xhtml#split-start`,
  `text/chapter-2.xhtml#split-bibliography`, and
  `text/chapter-2.xhtml#split-glossary`.
- OPF guide references to the nav document and a remote figures document remain
  serialized as guide metadata, but they are not promoted into generated EPUB3
  landmark nav entries.
- Read-back records the landmark nav attributes, landmark nav title, guide
  references, generated landmark entries, and clean navigation diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  primary guide-derived landmarks now survive split-spine output and read-back
  with explicit landmark nav metadata while targeting the generated split XHTML
  resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  split-spine guide-derived landmark nav metadata coverage. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Split Generated TOC Nav Metadata Slice

- Focused PHP coverage is now 896 behavior tests and 14,050 full-suite
  assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 navigation coverage across multiple generated linear
  spine XHTML files when explicit TOC nav attributes and a custom nav title are
  supplied while TOC entries are generated from split alternate heading nodes.
- Focused writer coverage now writes a `splitLevel=1` alternate rootfile at
  `ALT/split-toc-metadata.opf` with headings split between
  `ALT/text/split-toc-metadata.xhtml` and
  `ALT/text/split-toc-metadata-2.xhtml`.
- Generated `ALT/split-toc-metadata-nav.xhtml` preserves
  `epub:type="toc expanded"`, `id`, `role`, `title`, `class`, custom `h3`
  heading level, and heading attributes for the TOC nav.
- Generated nav links target
  `text/split-toc-metadata.xhtml#alt-split-toc-one` and
  `text/split-toc-metadata-2.xhtml#alt-split-toc-two`.
- Generated `ALT/split-toc-metadata.ncx` mirrors both split-spine heading
  targets.
- Alternate package read-back records the TOC nav attributes, nav title,
  generated nav.xhtml entries, NCX `navMap` entries with `playOrder`, and clean
  navigation diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  explicit alternate TOC nav metadata now survives split-spine generated TOC
  output and read-back while generated nav.xhtml and NCX entries target the
  split heading resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  split-spine generated TOC nav metadata coverage. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Generated TOC Nav Metadata Slice

- Focused PHP coverage is now 895 behavior tests and 14,031 full-suite
  assertions across the Pandoc lane. This slice extends generated primary EPUB3
  navigation coverage across multiple generated linear spine XHTML files when
  explicit TOC nav attributes and a custom nav title are supplied while TOC
  entries are generated from split heading nodes.
- Focused writer coverage now writes a `splitLevel=1` EPUB3 package with
  headings split between `OEBPS/text/chapter.xhtml` and
  `OEBPS/text/chapter-2.xhtml`.
- Generated `OEBPS/nav.xhtml` preserves `epub:type="toc expanded"`, `id`,
  `role`, `title`, `class`, custom `h3` heading level, and heading attributes
  for the TOC nav.
- Generated nav links target `text/chapter.xhtml#split-toc-one` and
  `text/chapter-2.xhtml#split-toc-two`.
- Generated `OEBPS/toc.ncx` mirrors both split-spine heading targets.
- Read-back records the TOC nav attributes, nav title, generated nav.xhtml
  entries, NCX `navMap` entries with `playOrder`, and clean navigation
  diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  explicit primary TOC nav metadata now survives split-spine generated TOC
  output and read-back while generated nav.xhtml and NCX entries target the
  split heading resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  split-spine generated TOC nav metadata coverage. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Split Generated Page-List Nav Metadata Slice

- Focused PHP coverage is now 894 behavior tests and 14,012 full-suite
  assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage across multiple generated linear
  spine XHTML files when explicit page-list nav attributes and a custom nav
  title are supplied.
- Focused writer coverage now writes a `splitLevel=1` alternate rootfile at
  `ALT/split-nav-metadata.opf` with pagebreaks split between
  `ALT/text/split-nav-metadata.xhtml` and
  `ALT/text/split-nav-metadata-2.xhtml`.
- Generated `ALT/split-nav-metadata-nav.xhtml` preserves
  `epub:type="page-list pagebreaks"`, `id`, `role`, `title`, `class`, custom
  `h3` heading level, and heading attributes for the page-list nav.
- The same generated nav links page-list entries to
  `text/split-nav-metadata.xhtml#alt-page-split-meta-1` and
  `text/split-nav-metadata-2.xhtml#alt-page-split-meta-2`.
- Generated `ALT/split-nav-metadata.ncx` uses the matching
  `Alternate Split Attribute Pages` page-list label.
- Alternate package read-back records the page-list nav attributes, nav title,
  NCX label, split-spine page-list entries, and clean navigation diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  explicit alternate page-list nav metadata now survives split-spine generated
  page-list output and read-back, not only single-spine page-list output.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  split-spine page-list nav metadata coverage. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Generated Page-List Nav Metadata Slice

- Focused PHP coverage is now 893 behavior tests and 13,993 full-suite
  assertions across the Pandoc lane. This slice extends generated primary EPUB3
  page-list coverage across multiple generated linear spine XHTML files when
  explicit page-list nav attributes and a custom nav title are supplied.
- Focused writer coverage now writes a `splitLevel=1` EPUB3 package with
  pagebreaks split between `OEBPS/text/chapter.xhtml` and
  `OEBPS/text/chapter-2.xhtml`.
- Generated `OEBPS/nav.xhtml` preserves `epub:type="page-list pagebreaks"`,
  `id`, `role`, `title`, `class`, custom `h3` heading level, and heading
  attributes for the page-list nav.
- The same generated nav links page-list entries to
  `text/chapter.xhtml#page-split-meta-1` and
  `text/chapter-2.xhtml#page-split-meta-2`.
- Generated `OEBPS/toc.ncx` uses the matching `Split Attribute Pages`
  page-list label.
- Read-back records the page-list nav attributes, nav title, NCX label,
  split-spine page-list entries, and clean navigation diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  explicit primary page-list nav metadata now survives split-spine generated
  page-list output and read-back, not only single-spine page-list output.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  split-spine page-list nav metadata coverage. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Alternate Split Raw Generated Page-List Spine-Target Slice

- Focused PHP coverage is now 892 behavior tests and 13,974 full-suite
  assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage across multiple generated linear
  spine XHTML files when pagebreaks originate in raw XHTML/HTML fragments
  rather than structured span nodes.
- Focused writer coverage now writes a `splitLevel=1` alternate rootfile at
  `ALT/split-raw-generated-pages.opf` with a raw
  `role="doc-pagebreak"` target in `ALT/text/split-raw-pages.xhtml` and a raw
  `class="pagebreak"` target normalized in
  `ALT/text/split-raw-pages-2.xhtml`.
- Generated `ALT/split-raw-generated-pages-nav.xhtml` links page-list entries
  to `text/split-raw-pages.xhtml#alt-page-raw-split-a` and
  `text/split-raw-pages-2.xhtml#alt-page-raw-split-b`.
- Generated `ALT/split-raw-generated-pages.ncx` mirrors both split-spine raw
  page targets with values `A` and `B`.
- Alternate package read-back records both generated `epubPageListEntries`
  with NCX `playOrder` values 3 and 4 after the split chapter `navMap`
  entries.
- The generated alternate package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  generated alternate-rootfile page-list extraction now follows raw XHTML/HTML
  pagebreak targets across multiple split linear spine XHTML files, not only
  structured AST pagebreak spans.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate raw
  split-spine page-list and NCX generation coverage. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Raw Generated Page-List Spine-Target Slice

- Focused PHP coverage is now 891 behavior tests and 13,950 full-suite
  assertions across the Pandoc lane. This slice extends generated primary EPUB3
  page-list coverage across multiple generated linear spine XHTML files when
  pagebreaks originate in raw XHTML/HTML fragments rather than structured span
  nodes.
- Focused writer coverage now writes a `splitLevel=1` EPUB3 package with a
  raw `role="doc-pagebreak"` target in `OEBPS/text/chapter.xhtml` and a raw
  `class="pagebreak"` target normalized in
  `OEBPS/text/chapter-2.xhtml`.
- Generated `OEBPS/nav.xhtml` links page-list entries to
  `text/chapter.xhtml#page-raw-split-a` and
  `text/chapter-2.xhtml#page-raw-split-b`.
- Generated `OEBPS/toc.ncx` mirrors both split-spine raw page targets with
  values `A` and `B`.
- Read-back records both generated `epubPageListEntries` with NCX `playOrder`
  values 3 and 4 after the split chapter `navMap` entries.
- The generated package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  generated primary page-list extraction now follows raw XHTML/HTML pagebreak
  targets across multiple split linear spine XHTML files, not only structured
  AST pagebreak spans.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary raw
  split-spine page-list and NCX generation coverage. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Split Generated Page-List Spine-Target Slice

- Focused PHP coverage is now 890 behavior tests and 13,926 full-suite
  assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage across multiple generated linear
  spine XHTML files.
- Focused writer coverage now writes a `splitLevel=1` alternate rootfile at
  `ALT/split-generated-pages.opf` with pagebreaks in both
  `ALT/text/split-pages.xhtml` and `ALT/text/split-pages-2.xhtml`.
- Generated `ALT/split-generated-pages-nav.xhtml` links page-list entries to
  `text/split-pages.xhtml#alt-page-split-20` and
  `text/split-pages-2.xhtml#alt-page-split-21`.
- Generated `ALT/split-generated-pages.ncx` mirrors both split-spine page
  targets with values `20` and `21`.
- Alternate package read-back records both generated `epubPageListEntries`
  with NCX `playOrder` values 3 and 4 after the split chapter `navMap`
  entries.
- The generated alternate package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  generated alternate-rootfile page-list extraction now follows pagebreak
  targets across multiple split linear spine XHTML files instead of only a
  single generated alternate chapter document.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  split-spine page-list and NCX generation coverage. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Generated Page-List Spine-Target Slice

- Focused PHP coverage is now 889 behavior tests and 13,902 full-suite
  assertions across the Pandoc lane. This slice extends generated primary EPUB3
  page-list coverage across multiple generated linear spine XHTML files.
- Focused writer coverage now writes a `splitLevel=1` EPUB3 package with
  pagebreaks in both `OEBPS/text/chapter.xhtml` and
  `OEBPS/text/chapter-2.xhtml`.
- Generated `OEBPS/nav.xhtml` links page-list entries to
  `text/chapter.xhtml#page-split-10` and
  `text/chapter-2.xhtml#page-split-11`.
- Generated `OEBPS/toc.ncx` mirrors both split-spine page targets with values
  `10` and `11`.
- Read-back records both generated `epubPageListEntries` with NCX `playOrder`
  values 3 and 4 after the split chapter `navMap` entries.
- The generated package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  generated primary page-list extraction now follows pagebreak targets across
  multiple split linear spine XHTML files instead of only a single generated
  chapter document.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  split-spine page-list and NCX generation coverage. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Generated Page-List Idless Pagebreak Skip Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,878
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage to semantic pagebreak markers that
  cannot be addressed.
- Focused writer coverage now emits a raw
  `<span role="doc-pagebreak" title="alt-idless"></span>` fragment without an
  id in the alternate document.
- Generated `ALT/text/generated-pages.xhtml` preserves that semantic marker in
  the alternate chapter XHTML.
- Generated `ALT/generated-pages-nav.xhtml` and `ALT/generated-pages.ncx` omit
  the idless marker because no fragment target can be formed.
- Alternate read-back page-list metadata remains limited to the eight
  addressable generated entries.
- The generated alternate package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate-rootfile semantic pagebreaks without ids may remain in XHTML, but
  generated EPUB3 nav and NCX page-lists only include addressable targets.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  page-list and NCX generation coverage for unaddressable semantic pagebreak
  markers. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Primary Generated Page-List Idless Pagebreak Skip Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,875
  full-suite assertions across the Pandoc lane. This slice extends generated
  primary EPUB3 page-list coverage to semantic pagebreak markers that cannot be
  addressed.
- Focused writer coverage now emits a raw
  `<span role="doc-pagebreak" title="idless"></span>` fragment without an id.
- Generated `OEBPS/text/chapter.xhtml` preserves that semantic marker in the
  chapter XHTML.
- Generated `OEBPS/nav.xhtml` and `OEBPS/toc.ncx` omit the idless marker
  because no fragment target can be formed.
- Read-back page-list metadata remains limited to the eight addressable
  generated entries.
- The generated package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  primary semantic pagebreaks without ids may remain in XHTML, but generated
  EPUB3 nav and NCX page-lists only include addressable targets.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  page-list and NCX generation coverage for unaddressable semantic pagebreak
  markers. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Alternate Generated Page-List Raw-Epub-Type Pagebreak Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,872
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage to raw XHTML/HTML fragments that
  declare pagebreak semantics only through `epub:type`.
- Focused writer coverage now emits a raw
  `<span id="alt-page-type-only" epub:type="pagebreak" title="8"></span>`
  fragment in the alternate document.
- Generated `ALT/text/generated-pages.xhtml` preserves that direct EPUB
  pagebreak semantic without depending on class-based writer normalization.
- Generated `ALT/generated-pages-nav.xhtml` and `ALT/generated-pages.ncx` use
  `8` as the page-list label/value for
  `text/generated-pages.xhtml#alt-page-type-only`.
- Read-back records the eighth generated alternate `epubPageListEntries` item
  with value `8` and `playOrder` 9.
- The generated alternate package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate-rootfile raw EPUB pagebreak semantics are recognized directly
  during generated page-list and NCX extraction, not only through normalized
  class or role markers.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  page-list and NCX generation coverage for raw `epub:type="pagebreak"`
  fragments. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Primary Generated Page-List Raw-Epub-Type Pagebreak Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,868
  full-suite assertions across the Pandoc lane. This slice extends generated
  primary EPUB3 page-list coverage to raw XHTML/HTML fragments that declare
  pagebreak semantics only through `epub:type`.
- Focused writer coverage now emits a raw
  `<span id="page-type-only" epub:type="pagebreak" title="8"></span>` fragment.
- Generated `OEBPS/text/chapter.xhtml` preserves that direct EPUB pagebreak
  semantic without depending on class-based writer normalization.
- Generated `OEBPS/nav.xhtml` and `OEBPS/toc.ncx` use `8` as the page-list
  label/value for `text/chapter.xhtml#page-type-only`.
- Read-back records the eighth generated `epubPageListEntries` item with value
  `8` and `playOrder` 9.
- The generated package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  primary raw EPUB pagebreak semantics are recognized directly during generated
  page-list and NCX extraction, not only through normalized class or role
  markers.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  page-list and NCX generation coverage for raw `epub:type="pagebreak"`
  fragments. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Alternate Generated Page-List Empty-Value Number Fallback Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,864
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage to pagebreak targets with no
  `title`, `aria-label`, or visible text.
- Focused writer coverage now emits a structured
  `<span id="alt-page-unlabeled" class="pagebreak"></span>` pagebreak in the
  alternate document.
- Generated `ALT/text/generated-pages.xhtml` normalizes that empty-value
  pagebreak with `role="doc-pagebreak"` and `epub:type="pagebreak"` semantics.
- Generated `ALT/generated-pages-nav.xhtml` and `ALT/generated-pages.ncx`
  assign the seventh generated page-list label/value to
  `text/generated-pages.xhtml#alt-page-unlabeled`.
- Read-back records the seventh generated alternate `epubPageListEntries` item
  with value `7` and `playOrder` 8.
- The generated alternate package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate-rootfile EPUB3 pagebreaks with no explicit label source still
  receive deterministic sequence labels in generated nav and NCX page-lists,
  then round trip through alternate package read-back metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  page-list and NCX generation coverage for the last label fallback after
  title, aria-label, and visible text extraction. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Generated Page-List Empty-Value Number Fallback Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,860
  full-suite assertions across the Pandoc lane. This slice extends generated
  primary EPUB3 page-list coverage to pagebreak targets with no `title`,
  `aria-label`, or visible text.
- Focused writer coverage now emits a structured
  `<span id="page-unlabeled" class="pagebreak"></span>` pagebreak.
- Generated `OEBPS/text/chapter.xhtml` normalizes that empty-value pagebreak
  with `role="doc-pagebreak"` and `epub:type="pagebreak"` semantics.
- Generated `OEBPS/nav.xhtml` and `OEBPS/toc.ncx` assign the seventh generated
  page-list label/value to `text/chapter.xhtml#page-unlabeled`.
- Read-back records the seventh generated `epubPageListEntries` item with value
  `7` and `playOrder` 8.
- The generated package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  when a primary EPUB3 pagebreak has no explicit label source, the generated
  nav and NCX page-list still receive deterministic sequence labels and round
  trip through page-list metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  page-list and NCX generation coverage for the last label fallback after
  title, aria-label, and visible text extraction. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Generated Page-List Raw-Div Pagebreak Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,856
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage to raw XHTML/HTML `div` fragments
  that carry pagebreak semantics before writer normalization.
- Focused writer coverage now emits a raw
  `<div id="alt-page-f" class="pagebreak" title="F"></div>` fragment in the
  alternate document.
- Generated `ALT/text/generated-pages.xhtml` normalizes that raw `div`
  pagebreak with `role="doc-pagebreak"` and `epub:type="pagebreak"` semantics.
- Generated `ALT/generated-pages-nav.xhtml` and `ALT/generated-pages.ncx` use
  `F` as the page-list label/value for
  `text/generated-pages.xhtml#alt-page-f`.
- Read-back records the sixth generated alternate `epubPageListEntries` item
  with value `F` and `playOrder` 7.
- The generated alternate package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate-rootfile raw `div` pagebreaks are normalized before generated EPUB3
  page-list and NCX extraction, then round-trip through alternate package
  read-back metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  page-list, NCX generation, and raw XHTML normalization coverage for both
  inline and block-level pagebreak carriers. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Primary Generated Page-List Raw-Div Pagebreak Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,852
  full-suite assertions across the Pandoc lane. This slice extends generated
  primary EPUB3 page-list coverage to raw XHTML/HTML `div` fragments that carry
  pagebreak semantics before writer normalization.
- Focused writer coverage now emits a raw
  `<div id="page-5" class="pagebreak" title="5"></div>` fragment.
- Generated `OEBPS/text/chapter.xhtml` normalizes that raw `div` pagebreak with
  `role="doc-pagebreak"` and `epub:type="pagebreak"` semantics.
- Generated `OEBPS/nav.xhtml` and `OEBPS/toc.ncx` use `5` as the page-list
  label/value for `text/chapter.xhtml#page-5`.
- Read-back records the sixth generated `epubPageListEntries` item with value
  `5` and `playOrder` 7.
- The generated package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  raw `div` pagebreaks are normalized before generated EPUB3 page-list and NCX
  extraction, then round-trip through read-back metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  page-list, NCX generation, and raw XHTML normalization coverage for both
  inline and block-level pagebreak carriers. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Alternate Generated Page-List Raw-Class Pagebreak Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,848
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage to raw XHTML/HTML fragments that
  carry only a `pagebreak` class before writer normalization.
- Focused writer coverage now emits a raw
  `<span id="alt-page-e" class="pagebreak" title="E"></span>` fragment in the
  alternate document.
- Generated `ALT/text/generated-pages.xhtml` normalizes that raw class-only
  pagebreak with `role="doc-pagebreak"` and `epub:type="pagebreak"` semantics.
- Generated `ALT/generated-pages-nav.xhtml` and `ALT/generated-pages.ncx` use
  `E` as the page-list label/value for
  `text/generated-pages.xhtml#alt-page-e`.
- Read-back records the fifth generated alternate `epubPageListEntries` item
  with value `E` and `playOrder` 6.
- The generated alternate package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate-rootfile raw class-only pagebreaks are normalized before generated
  EPUB3 page-list and NCX extraction, then round-trip through alternate package
  read-back metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  page-list, NCX generation, and raw XHTML normalization coverage. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond covered generated alternate package-link/container sidecar
  paths, DRM decryption, XML signature cryptographic validation, broader
  TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Generated Page-List Raw-Class Pagebreak Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,844
  full-suite assertions across the Pandoc lane. This slice extends generated
  primary EPUB3 page-list coverage to raw XHTML/HTML fragments that carry only a
  `pagebreak` class before writer normalization.
- Focused writer coverage now emits a raw
  `<span id="page-4" class="pagebreak" title="4"></span>` fragment.
- Generated `OEBPS/text/chapter.xhtml` normalizes that raw class-only pagebreak
  with `role="doc-pagebreak"` and `epub:type="pagebreak"` semantics.
- Generated `OEBPS/nav.xhtml` and `OEBPS/toc.ncx` use `4` as the page-list
  label/value for `text/chapter.xhtml#page-4`.
- Read-back records the fifth generated `epubPageListEntries` item with value
  `4` and `playOrder` 6.
- The generated package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  raw class-only pagebreaks are normalized before generated EPUB3 page-list and
  NCX extraction, then round-trip through read-back metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  page-list, NCX generation, and raw XHTML normalization coverage. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond covered generated alternate package-link/container sidecar
  paths, DRM decryption, XML signature cryptographic validation, broader
  TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Generated Page-List Raw-XHTML Pagebreak Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,840
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage to pagebreak semantics supplied by
  raw XHTML/HTML fragments, not only structured AST span nodes.
- Focused writer coverage now emits a raw
  `<span id="alt-page-d" role="doc-pagebreak">D</span>` fragment in the
  alternate document.
- Generated `ALT/text/generated-pages.xhtml` preserves that raw XHTML
  pagebreak.
- Generated `ALT/generated-pages-nav.xhtml` and `ALT/generated-pages.ncx` use
  `D` as the page-list label/value for
  `text/generated-pages.xhtml#alt-page-d`.
- Read-back records the fourth generated alternate `epubPageListEntries` item
  with value `D` and `playOrder` 5.
- The generated alternate package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate-rootfile pagebreak semantics authored in raw XHTML/HTML fragments
  can participate in generated EPUB3 page-list and NCX output, then round-trip
  through alternate package read-back metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  page-list and NCX generation coverage. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Primary Generated Page-List Raw-XHTML Pagebreak Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,836
  full-suite assertions across the Pandoc lane. This slice extends generated
  primary EPUB3 page-list coverage to pagebreak semantics supplied by raw
  XHTML/HTML fragments, not only structured AST span nodes.
- Focused writer coverage now emits a raw
  `<span id="page-3" role="doc-pagebreak">3</span>` fragment.
- Generated `OEBPS/text/chapter.xhtml` preserves that raw XHTML pagebreak.
- Generated `OEBPS/nav.xhtml` and `OEBPS/toc.ncx` use `3` as the page-list
  label/value for `text/chapter.xhtml#page-3`.
- Read-back records the fourth generated `epubPageListEntries` item with value
  `3` and `playOrder` 5.
- The generated package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  pagebreak semantics authored in raw XHTML/HTML fragments can participate in
  generated EPUB3 page-list and NCX output, then round-trip through read-back
  metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  page-list and NCX generation coverage. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Alternate Generated Page-List Text-Content Fallback Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,832
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 page-list coverage for pagebreak labels without
  `title` or `aria-label` attributes.
- Focused writer coverage now emits an alternate pagebreak span whose visible
  text is `C`.
- Generated `ALT/text/generated-pages.xhtml` preserves that text-content
  pagebreak semantics.
- Generated `ALT/generated-pages-nav.xhtml` and `ALT/generated-pages.ncx` use
  `C` as the page-list label/value for
  `text/generated-pages.xhtml#alt-page-c`.
- Read-back records the third generated alternate `epubPageListEntries` item
  with value `C` and `playOrder` 4.
- The generated alternate package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  primary and alternate pagebreak labels can both fall back to visible text when
  authoring metadata does not provide a title or accessible label.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  page-list and NCX generation coverage. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Primary Generated Page-List Text-Content Fallback Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,828
  full-suite assertions across the Pandoc lane. This slice extends generated
  primary EPUB3 page-list coverage for pagebreak labels without `title` or
  `aria-label` attributes.
- Focused writer coverage now emits a primary pagebreak span whose visible text
  is `2`.
- Generated `OEBPS/text/chapter.xhtml` preserves that text-content pagebreak
  semantics.
- Generated `OEBPS/nav.xhtml` and `OEBPS/toc.ncx` use `2` as the page-list
  label/value for `text/chapter.xhtml#page-2`.
- Read-back records the third generated `epubPageListEntries` item with value
  `2` and `playOrder` 4.
- The generated package still avoids `missing-nav-page-list-entry`,
  `nav-page-list-target-not-pagebreak`, `missing-nav-page-list-spine-target`,
  `missing-nav-link-fragment`, and `nav-page-list-value-mismatch`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  pagebreak labels can fall back to visible text when authoring metadata does
  not provide a title or accessible label.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  page-list and NCX generation coverage. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Primary Guide-Derived Landmark Specialized-Type Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,824
  full-suite assertions across the Pandoc lane. This slice extends generated
  primary EPUB3 landmark derivation to a local non-`text` OPF guide type.
- Focused writer coverage now feeds a primary `bibliography` guide reference to
  `OEBPS/text/chapter.xhtml#guide-start`.
- Generated `OEBPS/package.opf` preserves that valid local guide reference as
  OPF guide metadata.
- Generated `OEBPS/nav.xhtml` promotes the local `bibliography` reference into
  a `bibliography` landmark while still excluding cover-image, nav-document,
  remote, and non-linear guide targets from automatic landmarks.
- Read-back preserves the complete primary OPF guide metadata, reports three
  generated primary landmark entries for `bodymatter`, `bibliography`, and
  `glossary`, and still avoids `missing-nav-landmark-spine-target`,
  `nav-landmark-target-non-linear-spine`, plus the prior guide-title,
  landmark-entry, landmark-type, nav-link-resource, and nav-link-fragment
  guards.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  primary guide-derived landmarks now cover specialized local OPF guide types,
  not only `text` and repaired blank-title `glossary` references.
- Dependency-closure checkpoint: this strengthens direct EPUB3 primary
  guide-reference and navigation writer sanitation coverage. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Guide-Derived Landmark Remote-Target Sanitation Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,822
  full-suite assertions across the Pandoc lane. This slice extends generated
  primary and alternate EPUB3 landmark derivation to remote OPF guide
  references.
- Focused writer coverage now feeds a primary `loi` guide reference to
  `https://cdn.example.test/figures.xhtml` and an alternate `loi` guide
  reference to `https://cdn.example.test/alternate-figures.xhtml`.
- Generated primary and alternate OPFs preserve those valid remote guide
  references as OPF guide metadata.
- Generated `nav.xhtml` and `ALT/guide-cover-nav.xhtml` do not promote the
  remote guide targets into landmarks, while still preserving valid local
  linear content landmarks.
- Read-back preserves the complete primary and alternate OPF guide metadata
  including the remote entries, keeps generated landmark metadata limited to
  packaged linear XHTML targets, and still avoids
  `missing-nav-landmark-spine-target`, `nav-landmark-target-non-linear-spine`,
  plus the prior guide-title, landmark-entry, landmark-type, nav-link-resource,
  and nav-link-fragment guards.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  remote guide metadata can remain available for source review without creating
  external links in automatic EPUB3 landmark navigation.
- Dependency-closure checkpoint: this strengthens direct EPUB3 guide-reference
  and navigation writer sanitation coverage. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Alternate Guide-Derived Landmark Nav-Document-Target Sanitation Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,818
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile guide-derived landmark linear-target sanitation to
  alternate nav-document guide references.
- Focused writer coverage now feeds an alternate `toc` guide reference to
  `ALT/guide-cover-nav.xhtml` alongside the generated alternate cover, start,
  glossary, bibliography, and non-linear appendix guide references.
- Generated alternate output preserves the valid `toc` guide reference in
  `ALT/guide-cover.opf`.
- Generated `ALT/guide-cover-nav.xhtml` does not promote that nav-document
  target into a `toc` landmark, while still preserving the valid linear
  `Start`, blank-title `Glossary`, and fragment-target `References`
  landmarks.
- Read-back preserves the complete alternate OPF guide metadata including the
  `toc` entry, keeps alternate `epubLandmarkEntries` limited to generated
  linear content targets, and still avoids `missing-nav-landmark-spine-target`,
  `nav-landmark-target-non-linear-spine`, plus the prior guide-title,
  landmark-entry, landmark-type, nav-link-resource, and nav-link-fragment
  guards.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate guide metadata can point at the generated nav document for source
  review without causing automatic EPUB3 landmarks to link back to the nav
  document itself.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  guide-reference and navigation writer sanitation coverage. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Guide-Derived Landmark Non-Linear-Target Sanitation Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,816
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile guide-derived landmark linear-target sanitation to
  non-linear alternate spine resources.
- Focused writer coverage now feeds an alternate `backmatter` guide reference
  to `ALT/text/appendix.xhtml#appendix-start` alongside the existing generated
  alternate cover, start, glossary, and bibliography guide references.
- Generated alternate output emits `ALT/text/appendix.xhtml` as a non-linear
  spine item/resource and preserves the valid backmatter guide reference in
  `ALT/guide-cover.opf`.
- Generated `ALT/guide-cover-nav.xhtml` does not promote the non-linear
  backmatter guide reference into a landmark, while still preserving the valid
  linear `Start`, blank-title `Glossary`, and fragment-target `References`
  landmarks.
- Read-back preserves the complete alternate OPF guide metadata, records the
  alternate non-linear spine/resource metadata, and still avoids
  `nav-landmark-target-non-linear-spine` plus the prior guide-title,
  landmark-entry, landmark-type, nav-link-resource, and nav-link-fragment
  guards.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate guide metadata can remain rich for source review without causing
  automatic EPUB3 landmarks to point at non-linear reading-order resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  guide-reference and navigation writer sanitation coverage. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Guide-Derived Landmark Linear-Target Sanitation Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,810
  full-suite assertions across the Pandoc lane. This slice aligns generated
  EPUB3 landmarks with the reader's navigation target validation.
- `EpubWriter` keeps valid OPF guide references intact, but only promotes guide
  references into automatic EPUB3 landmarks when their href resolves to a
  generated linear chapter document.
- Focused writer coverage now feeds cover-image, nav-document, and non-linear
  appendix guide references alongside valid linear `Start` and blank-title
  `Glossary` references.
- Generated OPF guide output still serializes the cover, nav, non-linear
  appendix, `Start`, and `Glossary` guide references.
- Generated `nav.xhtml` landmarks now contain only the linear `bodymatter` and
  `glossary` entries, rather than emitting links to the cover image resource,
  the navigation document, or a non-linear spine resource.
- Alternate-rootfile coverage now also proves the generated cover-image guide
  reference remains out of alternate guide-derived landmarks.
- Read-back preserves the complete OPF guide metadata and avoids
  `missing-nav-landmark-spine-target` and
  `nav-landmark-target-non-linear-spine` self-diagnostics, alongside the prior
  guide-title, landmark-entry, landmark-type, nav-link-resource, and
  nav-link-fragment guards.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  guide metadata can remain rich for source review without causing automatic
  EPUB3 landmarks to point outside the generated linear reading order.
- Dependency-closure checkpoint: this strengthens direct EPUB3 guide-reference
  and navigation writer sanitation coverage. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Alternate Guide-Derived Landmark Fragment-Target Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,803
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 navigation coverage for guide-derived landmarks that
  point inside generated XHTML spine content.
- `EpubWriter` validates structured guide reference href fragments against the
  generated alternate XHTML payload before serializing OPF guide references and
  deriving landmarks.
- Focused writer coverage now adds an alternate `bibliography` guide reference
  to `ALT/text/guide-cover.xhtml#guide-cover-start`.
- Generated alternate output preserves the fragment target in
  `ALT/guide-cover.opf` guide output and emits the matching `bibliography`
  landmark link in `ALT/guide-cover-nav.xhtml`.
- Read-back preserves the fragment target in alternate `epubGuideReferences`
  and alternate `epubLandmarkEntries`.
- Generated alternate packages still avoid `missing-guide-reference-title`,
  `missing-nav-landmark-entry`, `missing-nav-landmark-link-type`,
  `missing-nav-link-resource`, and now `missing-nav-link-fragment`
  self-diagnostics while preserving the previous no-fragment `Start` and
  blank-title `Glossary` guide-derived landmarks.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  alternate guide-derived landmarks now cover generated XHTML fragment targets,
  not only package-relative content-document paths.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  guide-reference and navigation writer sanitation coverage. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Guide-Derived Landmark Blank-Title Fallback Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,800
  full-suite assertions across the Pandoc lane. This slice extends generated
  alternate-rootfile EPUB3 navigation coverage for OPF guide metadata.
- When explicit landmarks are absent in a generated alternate rootfile,
  `EpubWriter` derives landmarks from sanitized alternate guide references.
- Focused writer coverage now feeds a structured alternate `glossary` guide
  reference with a blank title into `guideReferences` for
  `ALT/guide-cover.opf`.
- Generated alternate output repairs the blank title to `Glossary` in both
  `ALT/guide-cover.opf` guide output and `ALT/guide-cover-nav.xhtml`
  landmarks output, rather than emitting a blank landmark label or dropping the
  valid local target.
- Read-back preserves the repaired `Glossary` guide record in alternate
  `epubGuideReferences` and the derived glossary landmark in alternate
  `epubLandmarkEntries`.
- Generated alternate packages still avoid `missing-guide-reference-title`,
  `missing-nav-landmark-entry`, `missing-nav-landmark-link-type`, and
  `missing-nav-link-resource` self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  guide metadata blank-title repair now covers both primary and alternate
  guide-derived EPUB3 landmarks.
- Dependency-closure checkpoint: this strengthens direct EPUB3 alternate
  guide-reference and navigation writer sanitation coverage. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Guide-Derived Landmark Blank-Title Fallback Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,796
  full-suite assertions across the Pandoc lane. This slice extends generated
  EPUB3 navigation coverage for OPF guide metadata.
- When explicit landmarks are absent, `EpubWriter` derives landmarks from
  sanitized guide references.
- Focused writer coverage now feeds a structured `glossary` guide reference
  with a blank title into primary `epubGuideReferences`.
- Generated output repairs the blank title to `Glossary` in both
  `package.opf` guide output and `nav.xhtml` landmarks output, rather than
  emitting a blank landmark label or dropping the valid local target.
- Read-back preserves the repaired `Glossary` guide record in
  `epubGuideReferences` and the derived glossary landmark in
  `epubLandmarkEntries`.
- Generated packages still avoid `missing-guide-reference-title`,
  `missing-nav-landmark-entry`, `missing-nav-landmark-link-type`,
  `missing-nav-link-resource`, and `missing-nav-link-fragment`
  self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  guide metadata blank-title repair now covers both OPF guide references and
  guide-derived EPUB3 landmarks.
- Dependency-closure checkpoint: this strengthens direct EPUB3 guide-reference
  and navigation writer sanitation coverage. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Guide-Reference Blank-Title Writer Fallback Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,794
  full-suite assertions across the Pandoc lane. This slice pins generated OPF
  guide title repair after the reader-side missing-title diagnostic.
- `EpubWriter` derives a non-empty guide reference title from the guide type
  when structured primary or alternate metadata provides a blank title.
- Focused writer coverage now feeds blank `glossary` titles to both primary
  `epubGuideReferences` and generated alternate-rootfile `guideReferences`.
- Generated primary and alternate OPFs serialize those references as
  `title="Glossary"` instead of emitting a blank title or dropping the valid
  local guide target.
- Read-back preserves the repaired `Glossary` guide references in
  `epubGuideReferences`, and the generated packages still avoid
  `missing-guide-reference-title` self-diagnostics.
- WordPress/Data Liberation workflows get a stronger generated-EPUB invariant:
  authored OPFs with blank guide titles are reported, while generated OPFs
  repair blank structured titles into valid guide metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 guide-reference
  writer sanitation coverage. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Missing Guide-Reference Title Diagnostics Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,792
  full-suite assertions across the Pandoc lane. This slice tightens OPF guide
  reference metadata validation.
- `EpubReader` now reports `missing-guide-reference-title` when a guide
  reference omits a non-empty `title`.
- The reader regression extends the guide reference target diagnostics fixture
  with a `type="text"` reference to `chapter.xhtml#start` that omits `title`.
- Read-back reports type, href, and path context for the missing-title
  diagnostic without inventing a synthetic title context, preserves the
  blank-title guide reference metadata for source review, and still imports
  readable body content.
- Existing guide diagnostics remain isolated: missing/invalid type, missing
  href, missing ZIP resource, unmanifested ZIP resource, malformed local guide
  target, missing fragment, invalid path/fragment, data/file URL, and remote
  reference behavior all stay in their existing buckets.
- Writer coverage now also asserts generated primary and alternate guide
  references do not self-diagnose `missing-guide-reference-title`.
- WordPress/Data Liberation workflows get clearer package review signals when
  OPF guide references omit their human-facing titles.
- Dependency-closure checkpoint: this strengthens direct EPUB3 guide-reference
  package validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Guide-Reference Manifest-Resource Diagnostics Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,785
  full-suite assertions across the Pandoc lane. This slice tightens OPF guide
  reference resource validation.
- `EpubReader` now reports `missing-guide-reference-manifest-resource` when a
  local guide reference `href` resolves to an archive resource that exists in
  the ZIP but is not listed in the package manifest.
- Missing ZIP entries remain in `missing-guide-reference-resource`, and remote,
  data, file, invalid-path, and invalid-fragment guide hrefs remain in their
  existing diagnostic buckets.
- The reader regression extends the guide reference target diagnostics fixture
  with `unmanifested.xhtml#start`, while keeping `broken.xhtml#start`
  manifest-listed so `malformed-guide-reference-resource` stays isolated.
- Read-back reports type, title, href, and path context for the unmanifested
  target, preserves guide reference metadata for source review, and still
  imports the readable body content.
- Writer coverage now also asserts generated primary and alternate guide
  references do not self-diagnose `missing-guide-reference-manifest-resource`.
- WordPress/Data Liberation workflows get clearer package review signals when
  OPF guide references point to local side resources that were packaged but not
  declared in the manifest.
- Dependency-closure checkpoint: this strengthens direct EPUB3 guide-reference
  package validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Invalid Guide-Reference Type Diagnostics and Writer Sanitation Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,777
  full-suite assertions across the Pandoc lane. This slice tightens OPF guide
  reference type-token validation.
- `EpubReader` now reports `invalid-guide-reference-type` when a non-empty OPF
  guide reference `type` is not a single XML name token, while preserving
  `missing-guide-reference-type` for blank types.
- `EpubWriter` now omits structured primary and alternate guide references
  whose type would self-diagnose, matching the existing sanitation for missing
  and invalid guide reference href targets.
- The reader regression extends the guide reference target diagnostics fixture
  with `type="bad type"` on a valid local chapter fragment. Read-back reports
  type, title, href, and path context for the invalid type while preserving the
  existing missing/malformed resource, missing fragment, invalid path/fragment,
  data/file URL, remote-reference, guide metadata import, and readable body
  import behavior.
- Writer coverage extends both primary and alternate guide-reference sanitation
  fixtures so invalid type entries are not serialized, not round-tripped, and
  do not produce generated `invalid-guide-reference-type` diagnostics.
- WordPress/Data Liberation workflows get clearer package review signals for
  malformed OPF guide type tokens while generated EPUBs avoid emitting guide
  references that would fail the reader's own diagnostics.
- Dependency-closure checkpoint: this strengthens direct EPUB3 guide-reference
  validation and writer self-diagnostic sanitation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Malformed Guide-Reference Resource Diagnostics Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,768
  full-suite assertions across the Pandoc lane. This slice tightens OPF guide
  reference `href` validation for existing local XML/XHTML targets.
- `EpubReader` now reports `malformed-guide-reference-resource` when a guide
  reference `href` points at an archive resource plus fragment and the
  resource exists but is not well-formed XML.
- The reader no longer silently skips fragment validation for malformed guide
  reference targets, and it does not misreport those malformed resources as
  missing fragments.
- The regression extends the guide reference target diagnostics fixture with
  `broken.xhtml#start` while preserving missing type/href/resource/fragment,
  invalid path/fragment, data/file URL, remote-reference, and readable
  guide-reference import behavior.
- Read-back reports the malformed guide-reference resource diagnostic with
  type, title, href, path, and fragment context, keeps guide reference metadata
  normalized, and still imports the readable body content.
- WordPress/Data Liberation workflows get clearer package review signals when
  an EPUB carries OPF guide references pointing into existing but malformed
  local side resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3 guide-reference
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Malformed Package-Link Resource Diagnostics Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,760
  full-suite assertions across the Pandoc lane. This slice tightens OPF package
  and collection link `href` validation for existing external XML resources.
- `EpubReader` now reports `malformed-package-link-resource` when a package
  link `href` points at an archive resource plus fragment and the resource
  exists but is not well-formed XML.
- The reader no longer silently skips fragment validation for malformed package
  link targets, and it does not misreport those malformed resources as missing
  fragments.
- The regression extends the package-link resource diagnostics fixture with a
  collection link to `metadata/broken-series.xml#series` while preserving
  existing local metadata/XML/JSON resource payload extraction, valid fragment
  checks, missing-resource, missing-fragment, invalid-path, invalid-fragment,
  data/file URL, and remote-link behavior.
- Read-back reports the malformed package-link resource diagnostic with id,
  href, path, fragment, parent, and collectionId context, keeps the malformed
  linked resource available as an extracted package-link payload, and still
  imports the readable body content.
- WordPress/Data Liberation workflows get clearer package metadata review
  signals when an EPUB carries package or collection links pointing into
  existing but malformed XML side resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package/link
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Malformed Metadata-Refines Resource Diagnostics Slice

- Focused PHP coverage remains 888 behavior tests and is now 13,744
  full-suite assertions across the Pandoc lane. This slice tightens OPF package
  metadata `refines` validation for existing external XML resources.
- `EpubReader` now reports `malformed-metadata-refines-resource` when package
  metadata `refines` points at an archive resource plus fragment and the
  resource exists but is not well-formed XML.
- The reader no longer silently skips fragment validation for malformed
  external refines targets, and it does not misreport those malformed resources
  as missing fragments.
- The regression extends the EPUB3 metadata-refines resource fixture with
  `metadata/broken.xml#record` while preserving the existing valid
  package-document, XHTML, and metadata XML fragment checks plus
  missing-resource, missing-fragment, and absolute-URL diagnostics.
- Read-back reports the malformed resource diagnostic with id, property,
  refines, path, and fragment context, keeps valid metadata refines targets
  clean, and still imports the readable body content.
- WordPress/Data Liberation workflows get clearer package metadata review
  signals when an EPUB carries review metadata pointing into an existing but
  malformed XML side resource.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package/refines
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Conflicting Itemref Rendition Metadata Diagnostics Slice

- Focused PHP coverage is now 888 behavior tests and 13,738 full-suite
  assertions across the Pandoc lane. This slice tightens OPF fixed-layout
  validation when spine itemref rendition property tokens disagree with scoped
  rendition metadata for the same spine target.
- `EpubReader` now reports `conflicting-rendition-itemref-metadata` when one
  itemref carries a single valid rendition property value and matching scoped
  rendition metadata for the itemref `id` or manifest `idref` carries one
  different valid value.
- Covered itemref-vs-metadata conflicts include `rendition:layout`,
  `rendition:orientation`, `rendition:spread`, and `rendition:flow`.
- The new regression builds an EPUB3 package with readable XHTML where the
  first itemref carries layout, orientation, spread, and flow property tokens
  that conflict with `#page1-ref`/`#page1` scoped metadata, while the second
  itemref keeps same-value layout and flow metadata.
- Read-back reports four conflict diagnostics with itemref id/idref,
  propertyGroup, itemrefProperties, itemrefValue, metadataValue,
  metadataTargets, metadataIds, and metadataRefines context, preserves raw
  itemref properties, `epubProperties`, and scoped `metadataProperties`,
  suppresses ambiguous derived `renditionLayout`, `renditionOrientation`,
  `renditionSpread`, and `renditionFlow` fields on the conflicting first spine
  item, keeps matching derived `renditionLayout` and `renditionFlow` on the
  second spine item, and still imports the readable body content.
- WordPress/Data Liberation workflows get clearer review signals for EPUB3
  fixed-layout packages that say incompatible itemref and scoped metadata
  things about one spine item without losing valid same-value metadata on
  unrelated spine items.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package/spine
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Conflicting Spine-Target Rendition Metadata Diagnostics Slice

- Focused PHP coverage is now 887 behavior tests and 13,696 full-suite
  assertions across the Pandoc lane. This slice tightens OPF per-spine
  fixed-layout validation across the two package targets that can describe one
  spine item: the itemref `id` and the manifest `idref`.
- `EpubReader` now reports `conflicting-rendition-spine-metadata` when
  rendition metadata refinements for one spine item carry multiple valid but
  mutually exclusive values across those targets.
- Covered refined properties include cross-target `rendition:layout`,
  `rendition:orientation`, and `rendition:viewport`.
- The new regression builds an EPUB3 package with readable XHTML where
  `#page1-ref` and `#page1` layout, orientation, and viewport refinements
  conflict, while `#page2-ref`/`#page2` same-value spread and viewport
  refinements plus `#page2` flow remain valid.
- Read-back reports three conflict diagnostics with itemref id/idref, property,
  values, targets, ids, and refines context, preserves raw `epubProperties` and
  merged scoped `metadataProperties`, suppresses ambiguous derived per-spine
  `renditionLayout`, `renditionOrientation`, and `viewport` fields for the
  conflicting first spine item, keeps valid scoped spread/flow/viewport metadata
  on the second spine item plus aggregate `epubViewports`, and still imports the
  readable body content.
- WordPress/Data Liberation workflows get clearer review signals for EPUB3
  fixed-layout metadata that says incompatible scoped things across the two
  package references for one spine item without losing valid scoped metadata on
  unrelated spine items.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package/spine
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Conflicting Scoped Rendition Metadata Diagnostics Slice

- Focused PHP coverage is now 886 behavior tests and 13,665 full-suite
  assertions across the Pandoc lane. This slice tightens OPF per-spine
  fixed-layout validation for ambiguous scoped rendition metadata refinements.
- `EpubReader` now reports `conflicting-rendition-metadata-refines` when valid
  local rendition metadata refinements carry multiple valid but mutually
  exclusive values for the same `refines` target and rendition property.
- Covered refined properties include itemref-targeted `rendition:layout` and
  manifest-item-targeted `rendition:orientation` and `rendition:viewport`.
- The new regression builds an EPUB3 package with readable XHTML where
  `#page1-ref` layout, `#page1` orientation, and `#page1` viewport refinements
  conflict, while `#page2` spread, flow, and viewport refinements remain valid.
- Read-back reports three conflict diagnostics with target/property/values/ids/
  refines context, preserves raw `epubProperties` and scoped
  `metadataProperties`, suppresses ambiguous derived per-spine
  `renditionLayout`, `renditionOrientation`, and `viewport` fields for the
  conflicting first spine item, keeps valid scoped spread/flow/viewport metadata
  on the second spine item plus aggregate `epubViewports`/`epubViewport`, and
  still imports the readable body content.
- WordPress/Data Liberation workflows get clearer review signals for EPUB3
  fixed-layout metadata that says incompatible scoped things about one spine
  item without losing valid scoped metadata on unrelated spine items.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package/spine
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Conflicting Package Rendition Metadata Diagnostics Slice

- Focused PHP coverage is now 885 behavior tests and 13,629 full-suite
  assertions across the Pandoc lane. This slice tightens OPF package-level
  fixed-layout validation for ambiguous global rendition metadata.
- `EpubReader` now reports `conflicting-rendition-metadata` when unrefined
  package metadata carries multiple valid but mutually exclusive values for the
  same rendition property.
- Covered properties include `rendition:layout`, `rendition:orientation`,
  `rendition:spread`, `rendition:flow`, and `rendition:viewport`.
- The new regression builds an EPUB3 package with readable XHTML and
  contradictory global rendition metadata pairs for each covered property,
  while also including a refined per-spine layout record.
- Read-back reports five conflict diagnostics with property/values/ids context,
  preserves raw `epubProperties` including refined metadata values, suppresses
  ambiguous promoted global `epubRendition*` and `epubViewport` fields, keeps
  the refined per-spine layout metadata attached to the spine item, and still
  imports the readable body content.
- WordPress/Data Liberation workflows get clearer review signals for package
  fixed-layout metadata that says incompatible global things while still
  retaining scoped spine metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package/spine
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Conflicting Rendition Spine Property Diagnostics Slice

- Focused PHP coverage is now 884 behavior tests and 13,603 full-suite
  assertions across the Pandoc lane. This slice tightens OPF spine
  fixed-layout validation for itemrefs with ambiguous rendition property
  groups.
- `EpubReader` now reports `conflicting-rendition-spine-property` when one
  spine `itemref` carries multiple valid but mutually exclusive values for the
  same rendition family.
- Covered families include `rendition:page-spread`, `rendition:layout`,
  `rendition:orientation`, `rendition:spread`, and `rendition:flow`.
- The new regression builds an EPUB3 package with two readable XHTML spine
  resources whose itemrefs combine left/right page-spread,
  reflowable/pre-paginated layout, landscape/portrait orientation, none/both
  spread, and paginated/scrolled-doc flow.
- Read-back reports five conflict diagnostics with itemref id/idref/
  propertyGroup/properties/values context, does not misclassify those tokens as
  invalid or duplicate property diagnostics, preserves raw itemref property
  tokens, suppresses ambiguous promoted rendition fields, and still imports the
  readable body content.
- WordPress/Data Liberation workflows get clearer fixed-layout review signals
  when a package says two incompatible rendition things about the same spine
  item.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package/spine
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Unreadable Spine Resource Diagnostics Slice

- Focused PHP coverage is now 883 behavior tests and 13,573 full-suite
  assertions across the Pandoc lane. This slice tightens OPF spine validation
  for content-document `itemref` targets that cannot be loaded from the EPUB
  archive.
- `EpubReader` now reports `invalid-spine-resource-href` when a spine XHTML/SVG
  content document target uses a remote, data, file, absolute-path,
  protocol-relative, backslash, encoded-dot-segment, or otherwise
  non-package-relative `href`.
- `EpubReader` now reports `missing-spine-resource` when a local spine
  content-document target is absent from the ZIP.
- The new regression builds an EPUB3 package whose spine points only at remote
  XHTML and missing local XHTML resources.
- Read-back reports the manifest-layer invalid/missing resource diagnostics
  plus `invalid-spine-resource-href` and `missing-spine-resource` with itemref
  id/idref/href/mediaType/path/reason context, preserves spine item metadata,
  imports no readable spine resources, and emits the no-readable-content
  fallback body.
- WordPress/Data Liberation workflows get clearer OPF reading-order review
  signals when the manifest has resource errors that directly explain why the
  spine produced no readable body content.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package/spine
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Blank Spine Itemref Linear Diagnostics Slice

- Focused PHP coverage is now 882 behavior tests and 13,550 full-suite
  assertions across the Pandoc lane. This slice tightens OPF spine validation.
- `EpubReader` now only counts a spine `itemref` toward
  `missing-linear-spine-itemref` after the `itemref` has a non-empty `idref`,
  so blank itemrefs no longer satisfy the package's linear reading-order
  requirement.
- The new regression builds an EPUB3 package with one blank `itemref` and one
  valid non-linear `itemref`.
- Read-back reports `missing-spine-itemref-idref` for the blank entry plus
  `missing-linear-spine-itemref` for the spine, preserves the valid non-linear
  spine metadata, keeps the non-linear XHTML out of readable body content, and
  avoids unrelated navigation cascades.
- WordPress/Data Liberation workflows get stricter OPF spine review signals
  when a package has itemref markup but no usable linear reading-order entry.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package/spine
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Malformed-Only Nav Missing-Entry Diagnostics Slice

- Focused PHP coverage is now 881 behavior tests and 13,537 full-suite
  assertions across the Pandoc lane. This slice tightens EPUB3 navigation
  document entry-presence validation.
- `EpubReader` now counts only linked anchors inside ordered-list list items as
  usable TOC, landmarks, and page-list entries for `missing-nav-toc-entry`,
  `missing-nav-landmark-entry`, and `missing-nav-page-list-entry` checks.
- Direct or stray malformed links still receive structural diagnostics and
  link-target checks, but they no longer satisfy entry presence or get imported
  as nav metadata.
- The new regression builds an EPUB3 package whose `nav.xhtml` has only
  malformed direct TOC and page-list anchors plus a stray landmarks `li`/`a`
  entry under a `section` wrapper.
- Read-back reports `invalid-nav-toc-link-parent`,
  `invalid-nav-landmark-list-item-parent`, `invalid-nav-page-list-link-parent`,
  and all three missing-entry diagnostics, avoids missing link-fragment
  cascades, imports zero TOC/landmark/page-list entries, and still imports
  readable spine content.
- WordPress/Data Liberation workflows get stricter EPUB3 nav entry-presence
  review signals without losing body import or link-target diagnostics for
  malformed links.
- Dependency-closure checkpoint: this strengthens direct EPUB3 nav
  entry-presence validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Nav Ordered-List Item Parent Diagnostics Slice

- Focused PHP coverage is now 880 behavior tests and 13,504 full-suite
  assertions across the Pandoc lane. This slice tightens EPUB3 navigation
  document validation and import for list entries that are `li` elements
  outside ordered lists.
- `EpubReader` now reports `invalid-nav-toc-list-item-parent`,
  `invalid-nav-landmark-list-item-parent`, and
  `invalid-nav-page-list-list-item-parent` with id/path/navId/type/
  parentElement/expectedParentElement/linkHref/label/entryType/value context.
- `xhtmlNavListEntries` no longer imports list item entries unless the `li` is
  directly contained by an `ol`, so stray wrapper-owned list items are review
  diagnostics instead of recovered navigation entries.
- The new regression builds an EPUB3 package whose `nav.xhtml` has stray
  toc/landmarks/page-list `li`/`a` entries under `div`/`section`/`aside`
  wrappers plus valid `ol`/`li`/`a` entries.
- Read-back reports the ordered-list parent diagnostics, avoids direct-link
  double reports plus missing entry and missing link-fragment cascades, imports
  only the valid ordered-list TOC, landmark, and page-list entries, and still
  imports readable spine content.
- WordPress/Data Liberation workflows get stricter EPUB3 nav list-shape review
  signals without losing body import or valid ordered-list navigation
  provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3 nav ordered-list
  containment validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Specialized Nav Unlinked-Label Diagnostics Slice

- Focused PHP coverage is now 879 behavior tests and 13,466 full-suite
  assertions across the Pandoc lane. This slice tightens EPUB3 landmarks and
  page-list navigation document validation for list items whose first label is
  a `span` instead of a linked anchor.
- `EpubReader` now reports visible unlinked landmarks and page-list labels as
  `missing-nav-landmark-link-href` and `missing-nav-page-list-link-href`,
  carrying nav context plus label, optional `entryType`, and page-list `value`
  context without inventing a link target.
- The new regression builds an EPUB3 package whose `nav.xhtml` has
  landmarks/page-list `li`/`span` entries plus valid `ol`/`li`/`a` entries.
- Read-back reports the unlinked label diagnostics, avoids missing
  landmark/page-list entry and missing link-fragment cascades, imports only the
  valid linked landmark and page-list entries, and still imports readable spine
  content.
- The existing structural fixture now also records the nested unlinked
  landmarks label while preserving the original missing-href anchor ordering.
- WordPress/Data Liberation workflows get stricter EPUB3 specialized nav
  label review signals without losing body import or recoverable navigation
  provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3 landmarks and
  page-list label validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Specialized Nav List-Structure Diagnostics Slice

- Focused PHP coverage is now 878 behavior tests and 13,438 full-suite
  assertions across the Pandoc lane. This slice tightens EPUB3 landmarks and
  page-list navigation document validation.
- `EpubReader` now reports landmarks and page-list nav anchors that are not
  contained in list item entries, matching the list-item based import behavior
  already enforced for TOC entries while preserving valid listed specialized
  entries.
- The new regression builds an EPUB3 package whose `nav.xhtml` has direct
  dangling landmarks/page-list anchors plus valid `ol`/`li`/`a` entries.
- Read-back reports `invalid-nav-landmark-link-parent` and
  `invalid-nav-page-list-link-parent` with id/href/path/navId/type/linkHref/
  label/value context, avoids missing landmark/page-list entry and missing
  link-fragment cascades, imports only the valid listed landmark and page-list
  entries, and still imports readable spine content.
- WordPress/Data Liberation workflows get stricter EPUB3 specialized nav
  structure review signals without losing body import or recoverable navigation
  provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3 landmarks and
  page-list list-structure validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Nav TOC List-Structure Diagnostics Slice

- Focused PHP coverage is now 877 behavior tests and 13,412 full-suite
  assertions across the Pandoc lane. This slice tightens EPUB3 navigation
  document validation.
- `EpubReader` now reports `toc` nav links that are not contained in list item
  entries, matching the list-item based TOC import behavior while preserving
  valid ordered-list entries.
- The new regression builds an EPUB3 package whose `nav.xhtml` has a dangling
  direct TOC anchor plus a valid `ol`/`li`/`a` entry.
- Read-back reports `invalid-nav-toc-link-parent` with id/href/path/navId/type/
  linkHref/label context, avoids missing toc, missing toc entry, and missing
  link-fragment cascades, imports only the valid listed TOC entry, and still
  imports readable spine content.
- WordPress/Data Liberation workflows get stricter EPUB3 nav TOC structure
  review signals without losing body import or recoverable navigation
  provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3 nav TOC
  list-structure validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Nav XHTML Namespace Diagnostics Slice

- Focused PHP coverage is now 876 behavior tests and 13,396 full-suite
  assertions across the Pandoc lane. This slice tightens EPUB3 navigation
  document validation.
- `EpubReader` now reports nav resources whose document root is not an XHTML
  `html` element, whose `html` root uses the wrong namespace, and whose `nav`
  elements use a non-XHTML namespace while preserving local-name recovery for
  readable navigation.
- The new regression builds an EPUB3 package with a manifest nav item whose
  `nav.xhtml` uses a foreign `book` root and foreign-namespace `nav` local
  names but still links to a valid spine resource.
- Read-back reports `invalid-nav-document-root` and
  `invalid-nav-element-namespace` with id/href/path/element/namespace/expected
  context, avoids missing toc, missing toc entry, and missing link-resource
  cascades, preserves the recovered TOC entry, and still imports readable
  spine content.
- WordPress/Data Liberation workflows get stricter EPUB3 nav document
  namespace review signals without losing body import or recoverable
  navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3 nav document
  root and namespace validation. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graph validation beyond covered generated
  alternate package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB NCX Navigation Child Order Diagnostics Slice

- Focused PHP coverage is now 875 behavior tests and 13,375 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  content-model validation for EPUB3 packages.
- `EpubReader` now reports out-of-order direct children in NCX `navMap`,
  `pageList`, `navList`, `navPoint`, `pageTarget`, and `navTarget` elements,
  and reports duplicate direct `content` children on NCX navigation entries
  while preserving recoverable metadata and navigation.
- The new regression builds an EPUB3 package with reordered but otherwise
  allowed NCX children plus duplicate `content` children in TOC, page-list,
  and nav-list contexts.
- Read-back reports `invalid-ncx-navigation-child-order` and
  `duplicate-ncx-content` with path/element/childElement/position/id/type/text/
  contentSrc/previous context, avoids unexpected-child, missing-label,
  empty-label, missing-content, and spine-target cascades, preserves the valid
  EPUB3 nav TOC plus recovered NCX uid/title/TOC/page-list/nav-list entries,
  and still imports readable spine content.
- WordPress/Data Liberation workflows get stricter NCX navigation
  content-model review signals without losing body import or recoverable
  navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX navigation
  child-order and content cardinality validation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered generated alternate package-link/container sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML parser
  parity, or full EPUB2 output parity.

## 2026-06-23 EPUB NCX Unexpected Child Diagnostics Slice

- Focused PHP coverage is now 874 behavior tests and 13,330 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  content-model validation for EPUB3 packages.
- `EpubReader` now reports unexpected correctly namespaced NCX child elements
  under `ncx`, `head`, `navMap`, `navPoint`, `pageList`, `pageTarget`,
  `navList`, and `navTarget` while preserving recoverable metadata and
  navigation.
- The new regression builds an EPUB3 package with unexpected NCX children in
  root, metadata, TOC, page-list, and nav-list contexts.
- Read-back reports `invalid-ncx-child-element` with path/element/childElement/
  id/type/text context, avoids uid/label/content/spine cascades, preserves the
  valid EPUB3 nav TOC plus recovered NCX uid/title/TOC/page-list/nav-list
  entries, and still imports readable spine content.
- WordPress/Data Liberation workflows get stricter NCX content-model review
  signals without losing body import or recoverable navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX child
  content-model validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB NCX ID Diagnostics Slice

- Focused PHP coverage is now 873 behavior tests and 13,297 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility ID
  validation for EPUB3 packages.
- `EpubReader` now reports malformed NCX `id` attributes and duplicate valid
  NCX `id` attributes across correctly namespaced NCX documents while
  preserving recoverable metadata and navigation.
- The new regression builds an EPUB3 package whose NCX `docAuthor`, `navPoint`,
  and `navList` carry malformed IDs, and whose `pageTarget` duplicates a valid
  `navPoint` ID.
- Read-back reports `invalid-ncx-id` and `duplicate-ncx-id` with path/element/
  parent/id/type/text and previous element/type/text context, avoids
  uid/title/label/content/spine cascades, preserves the valid EPUB3 nav TOC
  plus recovered NCX uid/title/author/TOC/page-list/nav-list entries, and
  still imports readable spine content.
- WordPress/Data Liberation workflows get stricter NCX ID review signals
  without losing body import or recoverable navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX XML ID
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB NCX Root Attribute Diagnostics Slice

- Focused PHP coverage is now 872 behavior tests and 13,261 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  root attribute validation for EPUB3 packages.
- `EpubReader` now reports missing NCX root `version`, invalid NCX root
  `version`, and invalid `xml:lang`/`lang` values on correctly namespaced NCX
  roots while preserving recoverable NCX metadata and navigation.
- The new regression builds an EPUB3 package with two NCX resources: one
  missing `version="2005-1"` and carrying an invalid root `xml:lang` value,
  and one declaring legacy `version="2004-2"` plus an invalid root `lang`
  value.
- Read-back reports `missing-ncx-version`, `invalid-ncx-version`, and
  `invalid-ncx-root-language` with path/element/version/expectedVersion/
  attribute/language context, avoids uid/title/label/content/spine cascades,
  preserves the valid EPUB3 nav TOC plus recovered NCX uid/title/navigation
  entries from both NCX resources, and still imports readable spine content.
- Valid NCX reader fixtures now declare `version="2005-1"`, matching the
  writer output and keeping expected-valid fixtures out of the new diagnostic
  path.
- WordPress/Data Liberation workflows get stricter NCX root attribute review
  signals without losing body import or recoverable navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX root
  version and root language validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or
  full EPUB2 output parity.

## 2026-06-23 EPUB NCX Label Language Diagnostics Slice

- Focused PHP coverage is now 871 behavior tests and 13,231 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  language validation for EPUB3 packages.
- `EpubReader` now reports invalid `xml:lang`/`lang` values on NCX document
  labels, navigation entry labels, and `pageList`/`navList` container labels
  while preserving recovered labels and local targets.
- The new regression builds an EPUB3 package with invalid language tags on
  `docTitle`, `docAuthor` text, `navPoint` `navLabel`, `pageTarget` label
  text, `navTarget` `navLabel`, `pageList` container `navLabel`, and
  `navList` container label text.
- Read-back reports `invalid-ncx-doc-title-language`,
  `invalid-ncx-doc-author-language`, `invalid-ncx-nav-label-language`, and
  `invalid-ncx-container-nav-label-language` with path/element/id/navType/type/
  text/childElement/attribute/language context, avoids missing-label,
  empty-label, missing-content, and spine-target cascades, preserves the valid
  EPUB3 nav TOC plus recovered NCX uid/title/author/page-list/nav-list text
  and targets, and omits invalid language tags from normalized NCX metadata.
- WordPress/Data Liberation workflows get stricter NCX label language review
  signals without losing body import or recoverable navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX label
  language validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB NCX Document Metadata Namespace Diagnostics Slice

- Focused PHP coverage is now 870 behavior tests and 13,181 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  metadata/container namespace validation for EPUB3 packages.
- `EpubReader` now reports wrong-namespace NCX `head` `meta`, `docTitle`
  text, `docAuthor` text, and `pageList`/`navList` container label children
  while preserving local-name recovery for import provenance.
- The new regression builds an EPUB3 package with a valid NCX root and
  correctly namespaced root containers whose metadata and container labels use
  DAISY NCX local names from a foreign namespace.
- Read-back reports `invalid-ncx-head-meta-namespace`,
  `invalid-ncx-doc-title-text-namespace`,
  `invalid-ncx-doc-author-text-namespace`,
  `invalid-ncx-container-nav-label-namespace`, and
  `invalid-ncx-container-nav-label-text-namespace` with path/element/id/type/
  text/name/content/childElement/namespace context, avoids parent namespace,
  missing uid, empty title, missing/empty label, missing content, and
  spine-target cascades, preserves the valid EPUB3 nav TOC plus recovered NCX
  uid/title/author/page-list/nav-list metadata, and still imports readable
  spine content.
- WordPress/Data Liberation workflows get stricter NCX document metadata
  namespace review signals without losing body import or recoverable
  navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX metadata
  and container label namespace validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption, XML
  signature cryptographic validation, broader TeX/MathML parser parity, or
  full EPUB2 output parity.

## 2026-06-23 EPUB NCX Navigation Child Namespace Diagnostics Slice

- Focused PHP coverage is now 869 behavior tests and 13,136 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  namespace validation for EPUB3 packages.
- `EpubReader` now reports wrong-namespace `navLabel`, label `text`, and
  `content` children under correctly namespaced NCX navigation entries while
  preserving local-name recovery for import provenance.
- The new regression builds an EPUB3 package with valid NCX root/navigation
  parents and three malformed children: a wrong-namespace `navLabel` on a
  `navPoint`, a wrong-namespace `text` child inside a `pageTarget` `navLabel`,
  and a wrong-namespace `content` child inside a `navTarget`.
- Read-back reports `invalid-ncx-nav-label-namespace`,
  `invalid-ncx-nav-label-text-namespace`, and
  `invalid-ncx-content-namespace` with path/navType/id/text/childElement/
  namespace context, avoids parent namespace, missing-label, empty-label,
  missing-content, and spine-target cascades, preserves the valid EPUB3 nav
  TOC plus recovered NCX TOC/page-list/nav-list entries, and still imports
  readable spine content.
- WordPress/Data Liberation workflows get stricter NCX navigation child
  namespace review signals without losing body import or recoverable
  navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX navigation
  child namespace validation. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graph validation beyond covered generated
  alternate package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB NCX Namespace Lookalike Diagnostics Slice

- Focused PHP coverage is now 868 behavior tests and 13,104 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  namespace validation for EPUB3 packages.
- `EpubReader` now reports NCX root elements, direct root child containers,
  and navigation elements that use NCX local names from the wrong XML
  namespace while still preserving local-name recovery for import provenance.
- The new regression builds an EPUB3 package with a valid EPUB3 `nav.xhtml`
  TOC, a valid NCX root containing wrong-namespace `head`/`pageList`/`navPoint`
  lookalikes, and a second NCX resource whose root local name is `ncx` but
  whose root namespace is wrong while its children use the DAISY NCX namespace.
- Read-back reports `invalid-ncx-root-namespace`,
  `invalid-ncx-child-namespace`, and `invalid-ncx-navigation-namespace` with
  path/element/namespace/expectedNamespace and available id/text context,
  avoids uid/label/content/spine cascades, preserves imported NCX
  uid/title/page-list metadata from recovered local-name content, preserves
  the valid EPUB3 nav TOC plus recovered NCX navigation entries, and still
  imports readable spine content.
- WordPress/Data Liberation workflows get stricter NCX namespace review
  signals without losing body import or recoverable navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX namespace
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB NCX Root Child Order Diagnostics Slice

- Focused PHP coverage is now 867 behavior tests and 13,070 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  document validation for EPUB3 packages.
- `EpubReader` now reports duplicate NCX `head`, `docTitle`, `navMap`, and
  `pageList` singleton containers, and reports direct NCX root children that
  violate the expected `head`, `docTitle`, `docAuthor`, `navMap`, `pageList`,
  `navList` content-model order.
- The new regression builds an EPUB3 package with valid readable spine
  content, a valid EPUB3 `nav.xhtml` TOC, a valid first NCX
  `head`/`navMap`/`pageList`, late `docTitle`/`docAuthor` children, and
  duplicate `head`/`docTitle`/`navMap`/`pageList` containers.
- Read-back reports `duplicate-ncx-head`, `duplicate-ncx-doc-title`,
  `duplicate-ncx-nav-map`, `duplicate-ncx-page-list`, and
  `invalid-ncx-child-order` with path/element/position/previous-position
  context, avoids missing/empty container, playOrder, and spine-target
  cascades, preserves imported NCX uid/title/author/page-list metadata,
  preserves both the EPUB3 nav TOC and first valid NCX navMap entry, and still
  imports readable spine content.
- WordPress/Data Liberation workflows get stricter NCX root content-model
  review signals without losing body import or valid navigation provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX
  compatibility document validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption,
  XML signature cryptographic validation, broader TeX/MathML parser parity, or
  full EPUB2 output parity.

## 2026-06-23 EPUB NCX Empty Navigation Container Diagnostics Slice

- Focused PHP coverage is now 866 behavior tests and 13,026 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  navigation validation for EPUB3 packages.
- `EpubReader` now reports empty NCX `navMap`, `pageList`, and `navList`
  containers when those elements are present without their required direct
  navigation entry children.
- The new regression builds an EPUB3 package with a valid EPUB3 `nav.xhtml`
  TOC, an empty NCX `navMap`, an empty labeled `pageList`, and an empty
  labeled `navList` carrying id/class metadata.
- Read-back reports `empty-ncx-nav-map`, `empty-ncx-page-list`, and
  `empty-ncx-nav-list` with path/element and available id/type/text context,
  avoids missing `navMap` and entry-level label/content/spine cascades,
  preserves the valid EPUB3 nav TOC entry, preserves labeled empty nav-list
  metadata, does not invent NCX page-list entries, and still imports readable
  spine content.
- WordPress/Data Liberation workflows get stricter NCX navigation-container
  review signals without losing body import or inventing missing navigation
  entries.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX
  compatibility navigation validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption,
  XML signature cryptographic validation, broader TeX/MathML parser parity, or
  full EPUB2 output parity.

## 2026-06-23 EPUB NCX Head Numeric Metadata Diagnostics Slice

- Focused PHP coverage is now 865 behavior tests and 13,001 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  metadata validation for EPUB3 packages.
- `EpubReader` now reports invalid `dtb:depth`, `dtb:totalPageCount`, and
  `dtb:maxPageNumber` content values when those NCX head records are present,
  instead of silently dropping them from normalized metadata.
- The new regression builds an EPUB3 package with valid NCX uid/docTitle/navMap
  data and invalid numeric head metadata values: `dtb:depth="0"`,
  `dtb:totalPageCount="-1"`, and `dtb:maxPageNumber="many"`.
- Read-back reports `invalid-ncx-depth`, `invalid-ncx-total-page-count`, and
  `invalid-ncx-max-page-number` with path/name/value context, avoids
  uid/label/spine cascades for otherwise valid NCX data, preserves raw head
  records, does not synthesize normalized invalid numeric metadata, and still
  imports readable spine content.
- WordPress/Data Liberation workflows get stricter NCX head metadata review
  signals without losing raw provenance or body import.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX
  compatibility metadata validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption,
  XML signature cryptographic validation, broader TeX/MathML parser parity, or
  full EPUB2 output parity.

## 2026-06-23 EPUB NCX Document-Structure Diagnostics Slice

- Focused PHP coverage is now 864 behavior tests and 12,979 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  navigation validation for EPUB3 packages.
- `EpubReader` now reports NCX documents with the wrong root element, missing
  `head`, missing `dtb:uid` metadata, missing `docTitle`, empty `docTitle`
  text, and missing `navMap` before entry-level NCX diagnostics run.
- The new regression builds an EPUB3 package with a valid EPUB3 nav document,
  a structurally incomplete NCX resource, and a second NCX resource with a
  non-`ncx` root.
- Read-back reports `missing-ncx-uid`, `empty-ncx-doc-title`,
  `missing-ncx-nav-map`, and `invalid-ncx-root` with path/root context, avoids
  entry-level label/content/spine cascades, preserves the valid EPUB3 nav TOC
  entry, does not synthesize missing NCX uid/docTitle metadata, and still
  imports readable spine content.
- WordPress/Data Liberation workflows get stricter NCX document-level review
  signals without losing body import or inventing NCX metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX
  compatibility navigation validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption,
  XML signature cryptographic validation, broader TeX/MathML parser parity, or
  full EPUB2 output parity.

## 2026-06-23 EPUB NCX Navigation Label Diagnostics Slice

- Focused PHP coverage is now 863 behavior tests and 12,957 full-suite
  assertions across the Pandoc lane. This slice tightens NCX compatibility
  navigation validation for EPUB3 packages.
- `EpubReader` now reports NCX navigation elements that are missing a direct
  `navLabel` child and `navLabel` elements that do not contain usable text.
- The new regression builds an EPUB3 package with a valid NCX `navPoint`, a
  `navPoint` missing `navLabel`, a typed `pageTarget` with empty label text,
  and a `navTarget` missing `navLabel`.
- Read-back reports `missing-ncx-nav-label` and `empty-ncx-nav-label` with
  toc/page-list/nav-list context, avoids content-src and spine-target
  cascades for valid local targets, does not import unlabeled NCX navigation
  entries into TOC/page-list/nav-list metadata, and still imports readable
  spine content.
- WordPress/Data Liberation workflows get stricter NCX navigation review
  signals without rewriting or inventing navigation labels during import.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX
  compatibility navigation validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption,
  XML signature cryptographic validation, broader TeX/MathML parser parity, or
  full EPUB2 output parity.

## 2026-06-23 EPUB NCX PageTarget Value Diagnostics Slice

- Focused PHP coverage is now 862 behavior tests and 12,936 full-suite
  assertions across the Pandoc lane. This slice tightens NCX page-list
  validation for EPUB3 packages that still include NCX compatibility
  navigation.
- `EpubReader` now reports missing or blank NCX `pageTarget value`
  attributes while preserving imported NCX page-list entries exactly as
  authored.
- The new regression builds an EPUB3 package with a valid typed page target
  carrying `value="1"` and a typed page target missing `value`.
- Read-back reports `missing-ncx-page-target-value` with page-list
  context/type/text, avoids missing-type and spine-target cascades for valid
  local content targets, preserves the authored value on the valid entry, does
  not synthesize a value for the invalid entry, and still imports readable
  spine content.
- WordPress/Data Liberation workflows get stricter NCX page-list diagnostics
  without rewriting or inventing pageTarget value data during import.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX
  compatibility navigation validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption,
  XML signature cryptographic validation, broader TeX/MathML parser parity, or
  full EPUB2 output parity.

## 2026-06-23 EPUB NCX PageTarget Type Diagnostics Slice

- Focused PHP coverage is now 861 behavior tests and 12,922 full-suite
  assertions across the Pandoc lane. This slice tightens NCX page-list
  validation for EPUB3 packages that still include NCX compatibility
  navigation.
- `EpubReader` now reports missing NCX `pageTarget type` attributes and
  invalid values outside `front`, `normal`, or `special`.
- NCX navigation diagnostic context now reads `type` from `pageTarget`
  elements, while retaining `class`-based type context for `navPoint` and
  `navTarget` elements.
- Existing NCX path/order fixtures now declare valid `type="normal"` where
  their target is not pageTarget type validation.
- The new regression covers a valid `front` target, a missing type, and an
  invalid `appendix` type. Read-back preserves authored page-list entries,
  reports targeted `missing-ncx-page-target-type` and
  `invalid-ncx-page-target-type` diagnostics, avoids spine-target cascades for
  valid local content targets, and still imports readable spine content.
- WordPress/Data Liberation workflows get stricter NCX page-list diagnostics
  without rewriting or inventing pageTarget type data during import.
- Dependency-closure checkpoint: this strengthens direct EPUB3/NCX
  compatibility navigation validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond covered
  generated alternate package-link/container sidecar paths, DRM decryption,
  XML signature cryptographic validation, broader TeX/MathML parser parity, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Unique-Identifier Scope Validation Slice

- Focused PHP coverage is now 860 behavior tests and 12,903 full-suite
  assertions across the Pandoc lane. This slice tightens EPUB package
  identity validation for collection-heavy packages.
- `EpubReader` now requires the package `unique-identifier` attribute to
  resolve to a direct package metadata `dc:identifier`, rather than accepting
  any same-id descendant in the primary package tree.
- The new regression builds an EPUB3 package whose package metadata contains
  a valid `dc:identifier`, while `unique-identifier` points at a
  collection-scoped `dc:identifier`.
- Read-back now reports `invalid-unique-identifier-target` with a
  `not-package-metadata` reason, preserves the expected collection
  content-model diagnostic, avoids misreporting the id as missing, and still
  imports readable spine content.
- WordPress/Data Liberation workflows get stricter EPUB3 package identity
  diagnostics without losing content extraction when incoming EPUBs contain
  scoped collection identifiers or auxiliary metadata.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package
  metadata validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Generated NCX Page-List Writer Slice

- Focused PHP coverage is now 859 behavior tests and 12,886 full-suite
  assertions across the Pandoc lane. This slice mirrors generated pagebreak
  navigation into optional NCX compatibility output.
- `EpubWriter` now feeds generated pagebreak-derived page-list entries into
  NCX `pageList`/`pageTarget` output when explicit `epubPageListEntries`
  metadata is absent.
- NCX output maps generated EPUB nav `pagebreak` entries to NCX
  `type="normal"` page targets while retaining page labels and target
  fragments.
- `EpubReader` now de-duplicates merged XHTML nav and NCX page-list entries
  by page target identity, preserving XHTML `pagebreak` semantics and NCX
  `playOrder` metadata.
- Focused writer/read-back coverage builds primary and generated alternate
  EPUB3 packages with pagebreak spans, generated `nav.xhtml` page-list
  sections, generated NCX `pageList` output, and merged read-back page-list
  entries.
- Read-back preserves `epubPageListEntries` with NCX `playOrder` without
  duplicate page-list entries, missing page-list, non-pagebreak target,
  spine-target, fragment, or value-mismatch diagnostics.
- WordPress/Data Liberation workflows can now emit source pagebreak spans once
  and have both EPUB3 nav and optional NCX page navigation generated from the
  same spine content.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated NCX page-list synthesis and reader
  XHTML/NCX merge behavior. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered generated alternate
  package-link/container sidecar paths, DRM decryption, XML signature
  cryptographic validation, broader TeX/MathML parser parity, or full EPUB2
  output parity.

## 2026-06-23 EPUB Generated Page-List Writer Slice

- Focused PHP coverage is now 859 behavior tests and 12,867 full-suite
  assertions across the Pandoc lane. This slice bridges generated spine
  pagebreak semantics into generated EPUB3 navigation documents.
- `EpubWriter` now derives page-list entries from pagebreak markers in
  generated spine XHTML when explicit `epubPageListEntries` metadata is
  absent, while keeping explicit page-list metadata authoritative.
- Derived labels come from `title`, `aria-label`, or visible pagebreak text,
  with page-order fallback for unlabeled markers; markers without `id` are
  skipped because they cannot be targeted from nav links.
- Focused writer/read-back coverage builds a primary EPUB3 package and a
  generated alternate rootfile whose bodies contain pagebreak spans but no
  page-list metadata.
- Generated primary and alternate nav documents include page-list links to
  real pagebreak fragments in generated spine XHTML.
- Read-back preserves `epubPageListEntries` without missing page-list,
  non-pagebreak target, spine-target, fragment, or value-mismatch diagnostics.
- WordPress/Data Liberation workflows can now emit EPUB3 page navigation from
  source pagebreak spans without duplicating the page-list metadata by hand.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated nav page-list synthesis. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond covered generated alternate package-link/container sidecar
  paths, DRM decryption, XML signature cryptographic validation, broader
  TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Guide-Derived Nav Landmark Writer Slice

- Focused PHP coverage is now 857 behavior tests and 12,840 full-suite
  assertions across the Pandoc lane. This slice bridges OPF guide metadata
  into generated EPUB3 navigation documents.
- `EpubWriter` now derives nav landmarks from sanitized guide references when
  explicit landmark metadata is absent, maps guide `text` to landmark
  `bodymatter`, preserves cover/toc guide types, and keeps explicit
  `epubLandmarkEntries` authoritative.
- Landmark target validation uses the packaged resource set plus generated
  chapter/nav paths, so guide-derived landmarks retain the same local
  resource and fragment checks as OPF guide output.
- Focused writer/read-back coverage builds a primary EPUB3 package whose only
  navigation semantics come from `epubGuideReferences`, plus a generated
  alternate rootfile with structured guide and cover metadata.
- Generated primary and alternate nav documents include cover/bodymatter/toc
  landmarks while the OPF guide references remain intact.
- Read-back preserves `epubLandmarkEntries`, `epubGuideReferences`, and cover
  payload extraction without missing landmark link/type/resource/fragment
  diagnostics.
- WordPress/Data Liberation workflows can now author EPUB3 exports from
  legacy guide metadata and still expose semantic landmarks to modern nav
  consumers.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated nav landmark synthesis. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond covered generated alternate package-link/container sidecar
  paths, DRM decryption, XML signature cryptographic validation, broader
  TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Generated Raw XHTML Required Property Inference Slice

- Focused PHP coverage is now 856 behavior tests and 12,819 full-suite
  assertions across the Pandoc lane. This slice aligns generated spine XHTML
  manifest inference with standalone XHTML resource payload scans.
- `EpubWriter` now treats raw generated forms and `javascript:` URL hooks as
  `scripted`, and uses the same remote-reference scanner for generated raw
  HTML, including `action`/`formaction`, inline CSS `url()`, remote link/base/
  SVG href paths, and base-relative remote references.
- Focused writer/read-back coverage builds a primary EPUB3 package and
  generated `ALT/raw-required.opf` alternate rootfile with raw XHTML forms,
  JavaScript links, and inline remote CSS without explicit
  `spineManifestProperties`.
- Generated primary and alternate OPFs emit `properties="scripted
  remote-resources"` on their chapter items and preserve the raw XHTML payloads.
- Read-back preserves primary and alternate `manifestProperties` and reports
  clean required-property diagnostics for both packages.
- WordPress/Data Liberation workflows can now author generated primary and
  alternate EPUB3 renditions with interactive raw XHTML body content without
  manual manifest property overrides.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated raw XHTML manifest property inference.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond covered generated alternate package-link/container
  sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Mixed XHTML Required Property Inference Slice

- Focused PHP coverage is now 855 behavior tests and 12,807 full-suite
  assertions across the Pandoc lane. This slice closes a manifest-property gap
  for generated alternate rootfile spine XHTML.
- `EpubWriter` now detects EPUB `switch` markup in generated spine ASTs and
  emits the `switch` manifest property alongside existing `mathml`, `svg`,
  `scripted`, and `remote-resources` inference, matching standalone XHTML
  resource payload behavior.
- Focused writer/read-back coverage builds `ALT/mixed-required.opf` with
  generated alternate XHTML containing TeX-to-MathML output, inline SVG,
  script content, `epub:switch` markup, and a remote image without explicit
  `spineManifestProperties`.
- Generated OPF output emits `properties="mathml svg scripted switch
  remote-resources"` on the alternate chapter item, keeps those properties out
  of the primary OPF, and preserves the XHTML feature content.
- Read-back preserves alternate package title, readable resource, inferred
  `manifestProperties`, body text, and clean required-property diagnostics.
- WordPress/Data Liberation workflows can now author generated alternate EPUB3
  renditions with mixed rich XHTML feature content without needing manual
  manifest property overrides.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated alternate XHTML manifest property
  inference. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond covered generated alternate package-link/
  container sidecar paths, DRM decryption, XML signature cryptographic
  validation, broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Generated Alternate Package-Link Graph Sidecar Isolation Slice

- Focused PHP coverage is now 854 behavior tests and 12,788 full-suite
  assertions across the Pandoc lane. This slice closes a multi-rendition OPF
  link graph gap for generated alternate rootfiles.
- Focused writer/read-back coverage builds two generated alternate rootfiles,
  `ALT/package-links.opf` and `MOBI/package-links.opf`, each with independent
  package-level metadata links, collection metadata links, direct collection
  sidecar links, voicing audio sidecars, mixed `metadataLinks`/
  `epubMetadataLinks` and `resources`/`epubResources` aliases, and readable
  XHTML.
- Generated OPFs render only their own link graph, keep JSON/audio package-link
  sidecars out of manifests and out of the primary OPF, package payloads under
  the correct alternate directories, and avoid cross-package leakage between
  print and mobile renditions.
- Read-back preserves alternate package titles, metadata links, collection link
  IDs, package-link resource media types, payload extraction, readable body
  text, and clean package-link diagnostics.
- WordPress/Data Liberation workflows can now author normal primary EPUB3
  exports with multiple generated alternate renditions that each retain
  package-scoped OPF metadata/collection sidecar provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated alternate package-link graph sidecars.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond covered generated alternate package-link/container
  sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Split Fixed Layout Alternate Media Overlay Alias Slice

- Focused PHP coverage is now 853 behavior tests and 12,708 full-suite
  assertions across the Pandoc lane. This slice closes a media/layout
  interaction gap in generated alternate rootfiles.
- Focused writer/read-back coverage builds `ALT/fixed-overlay.opf` as a split
  fixed-layout alternate rendition with page-progression metadata,
  package-level rendition metadata, per-itemref rendition viewport refinements,
  two generated XHTML spine pages, two generated SMIL media overlays, default
  SMIL sequence `epub:textref` values, and packaged audio payloads.
- Generated OPF output keeps `media-overlay` manifest attributes aligned to
  `chapter-1` and `chapter-2`, preserves fixed-layout itemref properties and
  viewport refinements, packages both SMIL and audio resources, and keeps
  alternate overlay resources out of the primary OPF.
- Read-back preserves package rendition metadata, spine itemref `mediaOverlay`
  IDs, page spreads, orientations, per-page viewports, overlay summaries,
  audio/text targets, payload extraction, and alternate body text without
  missing overlay resource, text, or audio diagnostics.
- WordPress/Data Liberation workflows can now author normal generated
  alternate fixed-layout EPUB3 renditions with concise per-page media overlay
  metadata while retaining package-level provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for alternate fixed-layout/media-overlay
  interactions. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered rootfile/container
  link sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Multiple Generated Alternate Rootfile Graph Slice

- Focused PHP coverage is now 852 behavior tests and 12,651 full-suite
  assertions across the Pandoc lane. This slice closes a deeper
  multi-rendition package graph coverage gap.
- Focused writer/read-back coverage builds a primary EPUB3 package with two
  generated alternate rootfiles, `ALT/print.opf` and `MOBI/mobile.opf`, each
  with independent package metadata, navigation document, readable spine
  XHTML, and CSS resource payloads.
- The generated OCF container also carries two JSON sidecars under
  `META-INF/metadata/`, with container links that refine the matching
  `print-rendition` and `mobile-rendition` rootfile IDs.
- Generated `META-INF/container.xml` preserves both alternate rootfiles and
  both sidecar links, generated OPFs reference only their own package
  resources, and ZIP payloads land in the correct package directories.
  Read-back preserves container rootfile/link metadata, sidecar payload bytes,
  alternate readable resources, alternate body text, and resource payload
  isolation without missing container link, rootfile, or manifest diagnostics.
- WordPress/Data Liberation workflows can now author normal primary EPUB3
  packages with multiple generated alternate renditions and package-scoped
  resource provenance in one export.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for multi-rendition generated rootfile graphs. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond covered rootfile/container link sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML
  parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Alternate Direct Resource Alias Isolation Slice

- Focused PHP coverage is now 851 behavior tests and 12,606 full-suite
  assertions across the Pandoc lane. This slice closes a multi-rendition
  direct resource boundary gap.
- `EpubWriter` alternate-rootfile resource collection now accepts
  `resources` and `epubResources` direct string maps alongside structured
  `resourcePayloads`, while primary direct resources remain scoped to the
  primary package and scrubbed from generated alternate-rootfile base
  metadata.
- Focused writer/read-back coverage builds a primary EPUB3 package with direct
  CSS/image metadata resources and a generated `ALT/resources-alias.opf`
  alternate package with its own direct CSS/image aliases plus XML
  `resourcePayloads`.
- Generated OPFs reference only their own manifest resources, ZIP payloads
  land at their package paths, and generated alternate navigation targets the
  alternate spine. Read-back extraction keeps primary payloads out of
  `epubAlternateRootfilePackages` and alternate payloads out of the primary
  payload map without missing manifest diagnostics.
- WordPress/Data Liberation workflows can now author normal primary and
  alternate EPUB3 packages with direct resource alias maps while retaining
  package-level resource provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for primary and generated alternate resource
  boundary handling. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered rootfile/container
  link sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Spine Media Overlay Alias Slice

- Focused PHP coverage is now 850 behavior tests and 12,578 full-suite
  assertions across the Pandoc lane. This slice closes generated primary
  split-spine media overlay authoring coverage.
- `EpubWriter` now accepts direct primary metadata `resources`/`epubResources`
  payload maps, scrubs those aliases from alternate-rootfile primary-body base
  metadata, and supplies a default SMIL sequence `epub:textref` from
  `contentPath`/`contentHref` when callers omit one.
- Focused writer/read-back coverage builds a primary EPUB3 package with
  `splitLevel=1`, package-level media duration/narrator/active-class metadata,
  compact `mediaOverlays` entries for `chapter.xhtml` and `chapter-2.xhtml`,
  generated SMIL overlays, and direct audio resources.
- Generated `OEBPS/package.opf` emits per-page `media-overlay` links, refined
  `media:duration`/`media:playback-active-class` metadata, generated SMIL
  resources, and packaged audio items. Read-back preserves media overlay
  summaries, spine `mediaOverlay` IDs, overlay resource payloads, package media
  metadata, and primary body content without missing media overlay, sequence
  textref, duration, text, or audio diagnostics.
- WordPress/Data Liberation workflows can now author normal primary multi-page
  EPUB3 exports with concise narrated-page media overlay arrays while retaining
  reader provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for primary package split-spine media overlay
  authoring. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered rootfile/container
  link sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Spine Manifest Attribute Alias Slice

- Focused PHP coverage is now 849 behavior tests and 12,522 full-suite
  assertions across the Pandoc lane. This slice closes generated primary
  split-spine manifest fallback and fallback-style compact alias coverage.
- Focused writer/read-back coverage builds a primary EPUB3 package with
  `splitLevel=1`, compact `spineManifestIds`, `spineManifestProperties`, and
  `spineManifestAttributes` arrays, plus packaged fallback XHTML and CSS
  payloads.
- Generated `OEBPS/package.opf` emits separate manifest properties and
  `fallback`/`fallback-style` attributes for `chapter.xhtml` and
  `chapter-2.xhtml`, packages the referenced fallback resources, and keeps
  `nav.xhtml` targets aligned with the split pages.
- Read-back preserves `manifestProperties`, fallback IDs, fallback paths/media
  types, `fallbackStyle`, and primary body content through the reconstructed
  document AST without missing or invalid fallback diagnostics.
- WordPress/Data Liberation workflows can now author normal primary multi-page
  EPUB3 exports with concise per-spine manifest fallback arrays while
  retaining reader provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for primary package split-spine manifest attribute
  authoring. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered rootfile/container
  link sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Spine Language Direction Viewport Alias Slice

- Focused PHP coverage is now 848 behavior tests and 12,490 full-suite
  assertions across the Pandoc lane. This slice closes generated primary
  split-spine XHTML localization and viewport compact alias coverage.
- Focused writer/read-back coverage builds a primary EPUB3 package with
  `splitLevel=1` and compact `spineLanguages`, `spineDirections`, and
  `viewports` arrays. Generated `chapter.xhtml` and `chapter-2.xhtml` emit
  independent `lang`/`xml:lang`/`dir` root attributes and viewport meta tags.
- Generated navigation targets the split pages, and the pages do not leak each
  other's language or viewport metadata. Read-back preserves per-spine
  language, direction, viewport records, aggregated `epubViewports`, and
  primary body content through the reconstructed document AST without
  malformed XHTML diagnostics.
- WordPress/Data Liberation workflows can now author normal primary multi-page
  EPUB3 exports with concise per-spine localization and viewport arrays while
  retaining reader provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for primary package split-spine XHTML localization
  and viewport metadata. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered rootfile/container
  link sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Spine Manifest Itemref Alias Slice

- Focused PHP coverage is now 847 behavior tests and 12,467 full-suite
  assertions across the Pandoc lane. This slice closes generated primary
  split-spine manifest ID, itemref ID, and itemref property alias coverage.
- Focused writer/read-back coverage builds a primary EPUB3 package with
  `splitLevel=1` and compact `spineManifestIds`, `spineItemIds`, and
  `spineItemProperties` arrays. The generated OPF emits custom primary XHTML
  manifest item IDs, custom itemref IDs, and page-spread/layout/flow itemref
  properties for `chapter.xhtml` and `chapter-2.xhtml`.
- Generated navigation targets the custom primary split pages, and read-back
  preserves idrefs, itemref IDs, href/path provenance, property tokens,
  derived rendition metadata, and primary body content through the
  reconstructed document AST without missing or duplicate spine diagnostics.
- WordPress/Data Liberation workflows can now author normal primary multi-page
  EPUB3 exports with concise spine manifest/itemref arrays while retaining
  per-page reader provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for primary package split-spine manifest and itemref
  authoring. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered rootfile/container
  link sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Primary Split Spine XHTML Metadata Alias Slice

- Focused PHP coverage is now 846 behavior tests and 12,434 full-suite
  assertions across the Pandoc lane. This slice closes generated primary
  split-spine XHTML head/root/body compact alias coverage.
- Focused writer/read-back coverage builds a primary EPUB3 package with
  `splitLevel=1` and compact `spineRootAttributes`, `spineBodyAttributes`,
  `spineHeadTitles`, `spineHeadMetas`, `spineHeadBases`, `spineHeadLinks`,
  `spineHeadStyles`, and `spineHeadScripts` arrays using direct and nested
  alias shapes.
- Generated `OEBPS/package.opf` marks both XHTML spine pages with
  `scripted remote-resources`; generated `chapter.xhtml` and
  `chapter-2.xhtml` emit independent title/root/body attributes plus head
  meta/base/link/style/script metadata, duplicate viewport head metadata is
  filtered, and `nav.xhtml` targets the split pages.
- Read-back preserves `headTitle`, `rootAttributes`, `bodyAttributes`,
  `headMetas`, `headBases`, `links`, `headStyles`, `headScripts`, and primary
  body content through the reconstructed document AST.
- WordPress/Data Liberation workflows can now author normal primary EPUB3
  multi-page exports with concise XHTML metadata arrays while retaining
  per-page reader provenance.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for primary package split-spine XHTML metadata. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond covered rootfile/container link sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML
  parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate-Rootfile Split Spine XHTML Metadata Alias Slice

- Focused PHP coverage is now 845 behavior tests and 12,380 full-suite
  assertions across the Pandoc lane. This slice closes generated alternate
  split-spine XHTML head/root/body compact alias arrays.
- `EpubWriter` now reads per-chapter compact arrays from document metadata and
  writer options for `spineRootAttributes`, `spineBodyAttributes`,
  `spineHeadTitles`, `spineHeadMetas`, `spineHeadBases`, `spineHeadLinks`,
  `spineHeadStyles`, and `spineHeadScripts`. Alternate-rootfile metadata maps
  and scrubs those aliases so primary package values do not bleed into
  generated alternate renditions.
- Focused writer/read-back coverage builds a structured
  `ALT/xhtml-aliases.opf` alternate rootfile with two split XHTML pages using
  direct and nested alias shapes. The generated pages emit independent
  title/root/body attributes plus head meta/base/link/style/script metadata,
  and duplicate viewport head metadata is filtered.
- The regression supplies conflicting primary aliases and proves they stay out
  of the alternate files. Read-back preserves `headTitle`, `rootAttributes`,
  `bodyAttributes`, `headMetas`, `headBases`, `links`, `headStyles`,
  `headScripts`, and alternate body text.
- WordPress/Data Liberation workflows can now author generated multi-page
  alternate EPUB3 renditions with concise XHTML metadata arrays while keeping
  primary package XHTML metadata isolated.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated alternate-rootfile XHTML metadata. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond covered rootfile/container link sidecar paths, DRM
  decryption, XML signature cryptographic validation, broader TeX/MathML
  parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate-Rootfile Split Spine Manifest Itemref Alias Slice

- Focused PHP coverage is now 844 behavior tests and 12,326 full-suite
  assertions across the Pandoc lane. This slice closes generated alternate
  split-spine manifest ID, itemref ID, and itemref property alias arrays.
- Focused writer/read-back coverage builds a structured `ALT/itemrefs.opf`
  alternate rootfile with two split XHTML pages and compact
  `spineManifestIds`, `spineItemIds`, and `spineItemProperties` arrays. The
  generated OPF emits the requested XHTML manifest item IDs, itemref IDs, and
  page-spread/layout/flow itemref properties.
- The regression supplies conflicting primary writer arrays and proves those
  primary manifest IDs, itemref IDs, and page-spread properties stay out of the
  alternate OPF. Generated navigation targets the custom alternate manifest
  pages, and read-back preserves idrefs, itemref IDs, href/path provenance,
  property tokens, derived rendition metadata, and alternate body text.
- WordPress/Data Liberation workflows can now author generated multi-page
  alternate renditions with concise spine manifest/itemref arrays while keeping
  primary package spine metadata isolated.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated alternate-rootfile OPF manifest and
  spine itemref authoring. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered rootfile/container
  link sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate-Rootfile Split Spine Direction Language Viewport Alias Slice

- Focused PHP coverage is now 843 behavior tests and 12,297 full-suite
  assertions across the Pandoc lane. This slice closes generated alternate
  split-spine language, direction, and viewport alias handling.
- `EpubWriter` now reads per-chapter metadata arrays from document metadata
  under alternate-rootfile metadata precedence, not only from writer options or
  `epubSpineItemRefs`. Generated alternate rootfile entries can also map
  unprefixed `viewport` and `viewports` aliases into EPUB viewport metadata.
- Focused writer/read-back coverage builds a structured `ALT/aliases.opf`
  alternate rootfile with two split XHTML pages and unprefixed
  `spineLanguages`, `spineDirections`, and `viewports` arrays. The generated
  pages emit independent `lang`/`xml:lang`/`dir` root attributes and viewport
  meta tags.
- The regression also supplies conflicting primary writer options and proves
  those primary language/direction/viewport values do not leak into alternate
  XHTML. Read-back preserves per-spine language, direction, viewport records,
  aggregated `epubViewports`, and alternate body text.
- WordPress/Data Liberation workflows can now author generated fixed-layout or
  localized multi-page alternate renditions with compact per-spine metadata
  arrays instead of expanding every page into `spineItemRefs`.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated alternate-rootfile XHTML layout and
  localization metadata. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered rootfile/container
  link sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Generated Alternate-Rootfile Container Graph Sidecar Slice

- Focused PHP coverage is now 842 behavior tests and 12,279 full-suite
  assertions across the Pandoc lane. This slice closes the generated
  alternate-rootfile half of the EPUB3 OCF container graph sidecar edge.
- Focused writer/read-back coverage builds a primary `OEBPS/package.opf`, a
  structured generated `ALT/generated-print.opf` alternate rootfile with
  `id="generated-print-rendition"`, a
  `META-INF/metadata/generated-renditions.json` sidecar payload, and a
  container `<link rel="record alternate"
  refines="#generated-print-rendition">` entry.
- Generated output keeps the generated alternate OPF in OCF rootfiles, keeps
  the JSON sidecar under `META-INF`, and does not write either
  `ALT/generated-print.opf` or `metadata/generated-renditions.json` into the
  primary OPF manifest.
- Read-back preserves the alternate rootfile ID/properties, container link
  href/rel/media type/refines/properties, OCF sidecar payload bytes, alternate
  package title, readable resource, and body text without
  `missing-container-link-resource` or `missing-container-rootfile-resource`
  self-diagnostics.
- WordPress/Data Liberation workflows can now generate multi-rendition EPUB3
  containers with rendition-level sidecar metadata without first hand-authoring
  raw alternate OPF payloads.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back multi-rendition container graph parity for generated
  alternate rootfiles. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond covered rootfile/container
  link sidecar paths, DRM decryption, XML signature cryptographic validation,
  broader TeX/MathML parser parity, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate-Rootfile Generated TeX MathML Writer Slice

- Focused PHP coverage is now 841 behavior tests and 12,252 full-suite
  assertions across the Pandoc lane. This slice closes the alternate-rendition
  propagation gap for generated XHTML/EPUB MathML output.
- Focused coverage writes a primary package with no math plus a generated
  alternate `ALT/math.opf` rootfile whose alternate document contains inline
  `x^2` and display `\frac{\alpha + 1}{\sqrt{x_2}}` TeX math nodes with no
  explicit MathML payload and no supplied `spineManifestProperties`.
- Generated output infers `properties="mathml"` on the alternate XHTML spine
  item, keeps that alternate math property out of the primary OPF, and emits
  MathML with `application/x-tex` annotations in `ALT/text/math.xhtml` instead
  of legacy span-only math output.
- Read-back preserves the alternate package title, readable resource, and
  `epubSpineItemRefs[0].manifestProperties = ["mathml"]`.
- WordPress/Data Liberation workflows can now rely on generated multi-rendition
  EPUB3 exports carrying semantic math in alternate packages, not just the
  primary spine.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back parity for generated alternate-rootfile MathML propagation.
  It does not claim broader texmath parser parity, full MathML parser parity,
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond covered rootfile/container-link paths, DRM decryption, XML signature
  cryptographic validation, or full EPUB2 output parity.

## 2026-06-23 EPUB Generated TeX MathML Writer Slice

- Focused PHP coverage is now 840 behavior tests and 12,237 full-suite
  assertions across the Pandoc lane. This slice closes generated XHTML/EPUB
  MathML output for math nodes that carry TeX source but no explicit MathML
  payload.
- `HtmlWriter` with `writerHTMLMathMethod=mathml` now emits a bounded native
  MathML subset for identifiers, numbers, operators, scripts, grouped rows,
  `\frac`, `\sqrt`, `\text`, common Greek commands, and common operator
  commands. It preserves the original source in an `application/x-tex`
  annotation.
- `EpubWriter` now marks generated XHTML manifest items with
  `properties="mathml"` for ordinary TeX math nodes rendered through that
  path, not only for explicit MathML payloads.
- Focused coverage verifies Markdown HTML writer MathML output from TeX
  source, generated EPUB XHTML MathML output, rejection of the prior
  span-only math output in EPUB XHTML, and read-back of inline/display math
  text plus manifest properties.
- WordPress/Data Liberation workflows can now export EPUB3 math as semantic
  MathML for the covered TeX subset while keeping the source TeX annotation
  available for review or downstream regeneration.
- Dependency-closure checkpoint: this strengthens direct EPUB3 writer and
  shared HTML writer math output parity for a bounded native TeX subset. It
  does not claim full texmath parser parity, full MathML parser parity, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond
  covered rootfile/container-link paths, DRM decryption, XML signature
  cryptographic validation, or full EPUB2 output parity.

## 2026-06-23 EPUB Multi-Rendition Container Graph Link Sidecar Slice

- Focused PHP coverage is now 838 behavior tests and 12,223 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3 OCF
  graph edge where a container rootfile payload and a container metadata-link
  sidecar describe the same alternate rendition.
- Focused writer/read-back coverage builds a primary `OEBPS/package.opf`, an
  alternate `ALT/print.opf` rootfile with `id="print-rendition"`, a
  `META-INF/metadata/renditions.json` sidecar payload, and a container
  `<link rel="record alternate" refines="#print-rendition">` entry.
- Generated output keeps the alternate OPF in OCF rootfiles, keeps the JSON
  sidecar under `META-INF`, and does not write either `ALT/print.opf` or
  `metadata/renditions.json` into the primary OPF manifest.
- Read-back preserves the alternate rootfile ID/properties, container link
  href/rel/media type/refines/properties, OCF sidecar payload bytes, and
  alternate package summary without `missing-container-link-resource` or
  `missing-container-rootfile-resource` self-diagnostics.
- WordPress/Data Liberation workflows can now model rendition-level sidecar
  metadata in generated EPUB3 containers without turning alternate OPFs or OCF
  sidecars into primary package resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back multi-rendition container graph parity for the
  rootfile/container-link sidecar edge. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the
  current rootfile/container-link sidecar edge, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output parity.

## 2026-06-23 EPUB Alternate-Rootfile Dublin Core Content-Model Writer Sanitation Slice

- Focused PHP coverage is now 837 behavior tests and 12,202 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 OPF
  Dublin Core content-model writer sanitation coverage for alternate rootfile
  packages.
- `EpubReader` now promotes the full extracted Dublin Core summary keys for
  alternate packages, matching the metadata keys already extracted from OPF:
  contributor, subject, type, format, source, relation, and coverage now join
  identifier, title, language, author, description, publisher, date, and
  rights in alternate package summaries.
- Focused writer/read-back coverage builds `ALT/content-model.opf` with empty
  Dublin Core values, invalid `dc:language`, unsupported Dublin Core element
  names, duplicate explicit dates, and empty package `meta` values.
- Generated alternate OPF preserves valid identifier, fallback title/language,
  first date, and contributor metadata while omitting invalid content. Read-back
  avoids `empty-package-metadata-value`,
  `invalid-package-metadata-dublin-core-element`,
  `invalid-package-metadata-child-element`, `multiple-metadata-date`, and
  `invalid-metadata-language`.
- WordPress/Data Liberation workflows can now rely on generated alternate
  EPUB3 packages not to round-trip source metadata noise into OPF metadata
  content-model self-diagnostics, while also seeing the alternate package's
  extracted contributor and other Dublin Core summary fields.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF Dublin Core content-model parity for alternate
  rootfiles. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond the current container-rootfile path, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Dublin Core Content-Model Writer Sanitation Slice

- Focused PHP coverage is now 836 behavior tests and 12,167 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 OPF
  Dublin Core content-model writer sanitation coverage for primary packages.
- `EpubWriter` already filters malformed Dublin Core and empty package meta
  inputs; focused coverage now locks those filters against the reader's
  package metadata content-model diagnostics.
- Focused writer coverage feeds `epubDublinCoreMetadata` and
  `epubMetadataProperties` with empty title/language/creator values, invalid
  `dc:language`, unsupported Dublin Core element names, duplicate explicit
  dates, and empty package `meta` values.
- Generated `OEBPS/package.opf` preserves the valid identifier, fallback
  title/language, first date, and contributor while omitting invalid content.
  Read-back avoids `empty-package-metadata-value`,
  `invalid-package-metadata-dublin-core-element`,
  `invalid-package-metadata-child-element`, `multiple-metadata-date`, and
  `invalid-metadata-language`.
- WordPress/Data Liberation workflows can now rely on generated primary EPUB3
  packages not to round-trip source metadata noise into OPF metadata
  content-model self-diagnostics.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF Dublin Core content-model parity for primary packages.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond the current container-rootfile path, DRM decryption,
  XML signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Alternate-Rootfile Guide-Reference Writer Target Sanitation Slice

- Focused PHP coverage is now 835 behavior tests and 12,134 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 OPF
  guide-reference target sanitation coverage for alternate rootfile packages.
- The existing shared `EpubWriter` `packageOpf` guide-reference sanitation
  path is now covered for generated multi-rendition packages with alternate
  package directories, alternate chapter payloads, and alternate read-back
  metadata.
- Focused writer coverage builds `ALT/guide-sanitized.opf` with a valid local
  alternate chapter anchor and a valid remote guide reference. Generated
  alternate OPF omits missing type/href records, missing local resources,
  missing XML fragments, query-only and fragment-only hrefs, data/file URLs,
  absolute paths, protocol-relative URLs, backslash paths, encoded dot-segment
  paths, empty fragments, and whitespace fragments.
- Read-back confirms the alternate package summary recovers only the valid
  guide references and does not self-diagnose missing/invalid guide-reference
  target shapes.
- WordPress/Data Liberation workflows can now rely on the same guide-reference
  target sanitation behavior for generated alternate rootfiles as for primary
  EPUB3 packages.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF guide-reference target parity for alternate rootfiles.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond the current container-rootfile path, DRM decryption,
  XML signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Guide-Reference Writer Target Sanitation Slice

- Focused PHP coverage is now 834 behavior tests and 12,104 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 OPF
  guide-reference target sanitation coverage for primary packages.
- `EpubWriter` now validates guide-reference hrefs against the same URL,
  path, fragment, package-resource, and XML-payload target rules used by the
  EPUB reader diagnostics before serializing OPF guide entries.
- Focused writer coverage keeps a valid local chapter anchor and a valid
  remote guide reference, while omitting missing type/href records, missing
  local resources, missing XML fragments, query-only and fragment-only hrefs,
  data/file URLs, absolute paths, protocol-relative URLs, backslash paths,
  encoded dot-segment paths, empty fragments, and whitespace fragments.
- Read-back confirms generated packages recover only the valid guide
  references and do not self-diagnose missing/invalid guide-reference target
  shapes.
- WordPress/Data Liberation workflows can now export primary EPUB3 packages
  without stale imported guide metadata pointing at missing resources or
  invalid anchors in generated OPF guide entries.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF guide-reference target parity for primary packages. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond the current container-rootfile path, DRM decryption,
  XML signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Alternate-Rootfile Package Root/Spine ID Writer Sanitation Slice

- Focused PHP coverage is now 833 behavior tests and 12,075 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3
  multi-rendition OPF package-wide ID sanitation coverage for alternate
  rootfile packages.
- The shared `EpubWriter` `packageOpf` path now has focused alternate-rootfile
  coverage for package root and spine IDs that collide with generated or
  required package IDs.
- Focused writer coverage builds `ALT/sanitized-ids.opf` with `packageId=nav`,
  `packageUniqueIdentifierId=alt-book-id`, and `spineId=alt-book-id`.
  Generated alternate OPF preserves the real navigation manifest item with
  `id="nav"` and the package identifier with `id="alt-book-id"` while omitting
  duplicate package-root and spine IDs.
- Read-back confirms the alternate package summary omits `epubPackageId` and
  `epubSpineId` for those colliding values and does not self-diagnose
  `duplicate-package-id`.
- WordPress/Data Liberation workflows can now rely on the same package-wide
  ID sanitation behavior for generated alternate rootfiles as for the primary
  OPF package.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back multi-rendition OPF package-wide ID parity. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond the current container-rootfile path, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Package Root/Spine ID Writer Sanitation Slice

- Focused PHP coverage is now 832 behavior tests and 12,064 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 OPF
  package-wide ID sanitation for package root and spine IDs that collide with
  required/generated package IDs.
- `EpubWriter` now reserves generated manifest IDs, generated itemref IDs,
  and the package unique identifier ID before emitting package root `id` and
  spine `id` attributes. Colliding `packageId`/`spineId` values are omitted
  instead of producing `duplicate-package-id` read-back diagnostics.
- Focused writer coverage sets `packageId=nav` and `spineId=book-id`.
  Generated `OEBPS/package.opf` preserves the real navigation manifest item
  with `id="nav"` and the package identifier with `id="book-id"` while
  omitting duplicate package-root and spine IDs.
- Read-back confirms generated packages omit `epubPackageId` and
  `epubSpineId` for those colliding values and do not self-diagnose
  `duplicate-package-id`.
- WordPress/Data Liberation workflows can now export EPUB3 packages from
  package-level metadata without imported root/spine IDs colliding with the
  writer-generated OPF manifest item IDs or the required package identifier
  anchor.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF package-wide ID parity. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Dublin Core Reserved Package-ID Writer Sanitation Slice

- Focused PHP coverage remains 831 behavior tests and is now 12,053
  full-suite assertions across the Pandoc lane. This slice closes generated
  EPUB3 OPF metadata identity sanitation for explicit Dublin Core records that
  try to reuse generated package-wide IDs.
- `EpubWriter` now passes generated manifest IDs into explicit
  `epubDublinCoreMetadata` normalization. Imported Dublin Core records that
  try to use reserved IDs such as the generated `nav` manifest item keep their
  metadata value but lose the colliding ID before OPF serialization.
- Focused writer coverage adds a subject record with `id="nav"`. Generated
  `OEBPS/package.opf` preserves `<dc:subject>Conflicting Nav Subject</dc:subject>`,
  preserves the manifest item with `id="nav"`, and omits
  `<dc:subject id="nav">`.
- Read-back confirms generated packages do not self-diagnose
  `duplicate-package-id` for that collision shape, while the earlier
  unique-identifier collision guard still avoids `duplicate-metadata-id`.
- WordPress/Data Liberation workflows can now export EPUB3 packages from
  metadata-rich sources without imported Dublin Core record IDs colliding with
  writer-generated manifest item IDs.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF package-wide ID parity. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Dublin Core Unique-Identifier ID Writer Sanitation Slice

- Focused PHP coverage remains 831 behavior tests and is now 12,050
  full-suite assertions across the Pandoc lane. This slice closes generated
  EPUB3 OPF writer sanitation for Dublin Core metadata records that try to
  reuse the package unique identifier ID.
- `EpubWriter` now reserves the package unique identifier ID, such as
  `book-id`, for Dublin Core identifier records when normalizing explicit
  `epubDublinCoreMetadata` entries. Earlier creator/contributor/etc. records
  that try to use the same ID keep their metadata value but lose the colliding
  ID before OPF serialization.
- Focused writer coverage adds a creator record with `id="book-id"` before
  the explicit identifier record. Generated `OEBPS/package.opf` preserves
  `<dc:creator>Conflicting Creator</dc:creator>`, preserves the single
  package identifier with `id="book-id"`, and omits
  `<dc:creator id="book-id">`.
- Read-back confirms generated packages do not self-diagnose
  `duplicate-metadata-id` or `duplicate-package-id` for that collision shape.
- WordPress/Data Liberation workflows can now export EPUB3 packages from
  metadata-rich sources without letting an imported non-identifier Dublin Core
  record steal the package identifier anchor required by OPF metadata
  refinement.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF Dublin Core metadata identity parity. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond the current container-rootfile path, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output parity.

## 2026-06-23 EPUB Direct Collection-Link Standalone Sidecar Writer Sanitation Slice

- Focused PHP coverage remains 831 behavior tests and is now 12,047
  full-suite assertions across the Pandoc lane. This slice closes generated
  EPUB3 OPF writer sanitation for direct collection links to local
  non-content sidecar resources.
- `EpubWriter` now treats direct `collection-link` records to local
  non-content resources as standalone package-link sidecars, matching the
  existing package metadata and collection metadata sidecar handling.
  Manifest-backed EPUB content documents still remain in the OPF manifest.
- Focused writer coverage adds a direct collection link to
  `metadata/member.json`. Generated `OEBPS/package.opf` keeps the collection
  `<link href="metadata/member.json" rel="contents"
  media-type="application/json"/>` while omitting the corresponding manifest
  `<item>`.
- Read-back confirms generated packages do not self-diagnose
  `invalid-package-link-manifest-resource`, and that
  `epubPackageLinkResources` and `epubResourcePayloads` recover the sidecar
  path, relation, collection parentage, collection ID, and JSON payload.
- WordPress/Data Liberation workflows can now export EPUB3 collection records
  with JSON/XML/audio/etc. sidecar payloads without duplicating those payloads
  into the manifest as invalid package-link resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF collection link sidecar parity. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Package-Link Package-Document Reference Writer Sanitation Slice

- Focused PHP coverage remains 831 behavior tests and is now 12,037
  full-suite assertions across the Pandoc lane. This slice closes generated
  EPUB3 OPF package-link provenance for metadata and collection links that
  point back into the OPF package document.
- `EpubWriter` now rejects fragment-only metadata-link hrefs such as
  `#book-id` and package-document fragment hrefs such as `package.opf#book-id`
  or `./package.opf#series` when serializing package metadata links,
  collection metadata links, and direct collection links.
- Focused writer coverage extends the package/collection local-link
  sanitation case with primary metadata, collection metadata, and collection
  link package-document references. Generated `OEBPS/package.opf` omits those
  invalid links while preserving valid local sidecar records, remote records,
  and collection members.
- Read-back confirms generated packages do not self-diagnose
  `invalid-package-link-package-document-reference`,
  `missing-package-link-resource`, or `missing-package-link-fragment`.
- WordPress/Data Liberation workflows can now export metadata-rich EPUB3
  packages without creating package links that refer to OPF package elements as
  if they were standalone metadata resources.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF package-link provenance parity. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Package Modified-Metadata Writer Sanitation Slice

- Focused PHP coverage remains 831 behavior tests and is now 12,028
  full-suite assertions across the Pandoc lane. This slice closes generated
  EPUB3 OPF writer sanitation for unrefined `dcterms:modified` package
  metadata duplicates.
- `EpubWriter` now drops unrefined `dcterms:modified` entries supplied through
  `epubMetadataProperties`/`metadataProperties`, even when those records carry
  valid IDs or other attributes. Refined `dcterms:modified` records are still
  preserved because the reader intentionally ignores refined modified metadata
  for the package-level required timestamp cardinality check.
- Focused writer coverage extends the package metadata properties case with an
  unrefined duplicate modified timestamp and a refined modified timestamp.
  Generated `OEBPS/package.opf` omits the unrefined duplicate and preserves the
  refined record.
- Read-back confirms generated packages do not self-diagnose
  `multiple-package-modified` or `invalid-package-modified`, and that the
  refined modified metadata still round-trips.
- WordPress/Data Liberation workflows can now export EPUB3 packages from
  metadata-rich sources without accidentally adding a second package-level
  `dcterms:modified` record.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF package metadata content-model parity. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond the current container-rootfile path, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Dublin Core Date-Cardinality Writer Sanitation Slice

- Focused PHP coverage is now 831 behavior tests and 12,019 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 OPF
  writer sanitation for `dc:date` cardinality.
- `EpubWriter` now emits at most one Dublin Core `date` element when building
  package metadata. List-style document metadata such as `date:
  [2026-06-21, 2026-06-22]` and explicit `epubDublinCoreMetadata` records with
  multiple `date` entries preserve the first valid value and omit later dates.
- Focused writer coverage proves `OEBPS/package.opf` keeps the first generated
  `dc:date`, omits the second list-style date, keeps the first explicit
  `dc:date id="issued-a"`, omits `issued-b`, and preserves other valid
  Dublin Core records such as `dc:creator`.
- Read-back confirms generated packages do not self-diagnose
  `multiple-metadata-date` while preserving the selected package date and
  metadata author.
- WordPress/Data Liberation workflows can now export EPUB3 packages from noisy
  source metadata without generating OPF packages that immediately fail the
  reader's Dublin Core date-cardinality check.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF package metadata content-model parity. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond the current container-rootfile path, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Manifest Required Resource-Property Writer Inference Slice

- Focused PHP coverage remains 830 behavior tests and is now 12,010
  full-suite assertions across the Pandoc lane. This slice closes generated
  EPUB3 OPF manifest property inference for arbitrary resource payloads whose
  bytes require EPUB3 `mathml`, `svg`, `scripted`, `switch`, or
  `remote-resources` declarations.
- `EpubWriter` now passes resource bytes into manifest property inference for
  packaged XHTML, SVG, CSS, and SMIL resources. Byte-level scans mirror the
  reader's required-property checks for MathML, inline SVG, script/form or
  JavaScript URL content, EPUB switch markup, remote attributes, `srcset`,
  style/CSS `url()` and `@import`, SVG `href`/`xlink:href`, link hrefs, and
  remote base URLs that make package-relative references remote.
- Focused writer coverage extends the optional resource payload package case
  with remote CSS, remote SMIL, scripted/remote SVG, and auxiliary XHTML
  carrying MathML, inline SVG, form content, `epub:switch`, and remote media.
  Generated `OEBPS/package.opf` declares the required resource properties
  before read-back.
- Read-back confirms the generated package does not self-diagnose
  `missing-manifest-required-property` while preserving the existing cover,
  CSS, JSON, XML, referenced-resource, image-resource, and guide metadata.
- WordPress/Data Liberation workflows can now export generated EPUB3 packages
  with structured sidecar resources that do not immediately round-trip as
  missing required manifest property errors.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF manifest property parity for generated resource
  payloads. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond the current container-rootfile path, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Manifest Fallback Core Media Writer Sanitation Slice

- Focused PHP coverage remains 830 behavior tests and is now 11,997
  full-suite assertions across the Pandoc lane. This slice closes generated
  EPUB3 OPF writer sanitation for foreign-resource `fallback` chains that do
  not reach an EPUB core media type.
- `EpubWriter` now uses the planned OPF manifest item map to evaluate the
  complete manifest fallback chain before serializing `fallback` attributes.
  Fallbacks from foreign resources are preserved only when the chain reaches a
  valid EPUB core media type. Chains such as HEIC -> AVIF are stripped because
  they would otherwise round-trip as
  `missing-manifest-fallback-core-media-type`.
- Focused writer coverage proves generated `OEBPS/package.opf` preserves a
  valid `application/x-demo-widget` -> XHTML fallback chain while stripping a
  non-core-only `image/heic` -> `image/avif` fallback chain. Existing missing
  target, bad `fallback-style`, bad `media-overlay`, and fallback-cycle
  sanitation coverage remains green.
- Read-back confirms the generated package does not self-diagnose
  `missing-manifest-fallback-core-media-type` alongside the existing manifest
  fallback, fallback-style, media-overlay, and fallback-cycle diagnostics.
- WordPress/Data Liberation workflows can now export generated EPUB3 packages
  without keeping invalid foreign-resource fallback chains that point only to
  other non-core media types.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF manifest fallback sanitation parity. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond the current container-rootfile path, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output parity.

## 2026-06-23 EPUB Manifest Reference Writer Sanitation Parity Slice

- Focused PHP coverage is now 830 behavior tests with 11,991 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 OPF
  manifest reference writer sanitation for `fallback`, `fallback-style`, and
  `media-overlay` attributes in primary generated packages.
- `EpubWriter` now plans OPF manifest items before serialization so reference
  attributes can be validated against the full emitted manifest graph instead
  of only local ID syntax. Valid forward references are preserved, while
  missing fallback targets, missing media-overlay targets, non-CSS
  `fallback-style` targets, media-overlay links to non-SMIL resources, and
  manifest fallback cycles are stripped before writing `package.opf`.
- Focused writer coverage proves generated `OEBPS/package.opf` omits invalid
  chapter and resource manifest attributes, including missing fallback
  targets, wrong fallback-style targets, missing/invalid overlay targets, and
  cyclic fallback chains, without breaking existing valid forward fallback and
  media-overlay cases.
- Read-back confirms the sanitized package does not self-diagnose
  `missing-manifest-fallback`, `missing-manifest-fallback-style`,
  `missing-manifest-media-overlay`, `manifest-fallback-cycle`,
  `invalid-manifest-fallback-style-media-type`,
  `invalid-media-overlay-content-media-type`, or
  `invalid-media-overlay-target-media-type`.
- WordPress/Data Liberation workflows can now export generated EPUB3 packages
  with sanitized manifest reference attributes that do not immediately
  round-trip as invalid OPF packages.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF manifest reference sanitation parity. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond the current container-rootfile path, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output parity.

## 2026-06-23 EPUB OPF Binding Writer Sanitation Parity Slice

- Focused PHP coverage is now 829 behavior tests with 11,967 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 OPF
  binding writer sanitation parity for primary and alternate-rootfile
  packages.
- `EpubWriter` now builds a structured emitted-manifest item map while
  authoring OPF package documents and passes it into binding serialization.
  Generated `<bindings>` entries are preserved only when they use a valid
  non-core custom media type and a handler that is an emitted manifest item,
  is XHTML, and declares the `scripted` property.
- Focused writer fixtures prove primary `OEBPS/package.opf` and alternate
  `ALT/bindings.opf` preserve valid custom binding records while stripping
  duplicate binding media types, EPUB core media types, malformed media types,
  invalid handler IDREFs, missing handler targets, non-XHTML handlers,
  unscripted handlers, missing media types, missing handlers, and primary
  binding leakage into alternate packages.
- Read-back confirms the sanitized primary and alternate packages do not
  self-diagnose `missing-binding-handler-target`,
  `missing-binding-media-type`, `missing-binding-handler`,
  `duplicate-binding-media-type`, `binding-core-media-type`,
  `invalid-binding-media-type`, `invalid-binding-handler-idref`,
  `invalid-binding-handler-media-type`, or
  `missing-binding-handler-scripted-property`.
- WordPress/Data Liberation workflows can now export primary and alternate
  EPUB3 renditions with binding records that do not immediately round-trip as
  invalid OPF packages.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back OPF binding sanitation parity. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Rootfile Package Metadata Link Sanitation Parity Slice

- Focused PHP coverage is now 827 behavior tests with 11,914 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3
  alternate-rootfile OPF package metadata link sanitation parity.
- The focused writer fixture emits `ALT/metadata-links.opf` from structured
  alternate-rootfile metadata carrying valid and noisy OPF metadata link
  records plus alternate-scoped XML and audio sidecar payloads.
- Generated alternate OPF preserves valid record and voicing links with
  inferred media types, declared custom property tokens, `refines`,
  `hreflang`, `xml:lang`, and `dir` attributes while stripping unsafe data
  URLs, invalid fragments, invalid or undeclared relation/property tokens,
  invalid media types, invalid language values, invalid refines values,
  voicing links without valid refines targets, and primary metadata-link
  leakage.
- Read-back under `epubAlternateRootfilePackages` confirms the sanitized
  alternate package does not self-diagnose `missing-package-link-rel`,
  `duplicate-package-link-rel`, `invalid-package-link-rel`,
  `undeclared-package-link-rel-prefix`, `duplicate-package-link-property`,
  `invalid-package-link-property`, `undeclared-package-link-property-prefix`,
  `invalid-package-link-data-url`, `invalid-package-link-file-url`,
  `invalid-package-link-href-path`, `invalid-package-link-href-fragment`,
  `invalid-package-link-media-type`, `missing-package-link-media-type`,
  `invalid-package-link-hreflang`, `invalid-package-link-xml-language`,
  `invalid-package-link-record-refines`, or
  `missing-package-link-voicing-refines`.
- WordPress/Data Liberation workflows can now export alternate EPUB3
  renditions with sanitized package metadata links and recover valid record
  and voicing links for block-oriented processing.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back alternate-rootfile package metadata link sanitation parity.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond the current container-rootfile path, DRM decryption,
  XML signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Rootfile Package Metadata Property Sanitation Parity Slice

- Focused PHP coverage is now 826 behavior tests with 11,865 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3
  alternate-rootfile OPF package metadata property sanitation parity.
- The focused writer fixture emits `ALT/metadata-properties.opf` from
  structured alternate-rootfile metadata carrying valid and noisy OPF metadata
  property records.
- Generated alternate OPF preserves valid `custom:ranking` metadata with
  `id`, `refines`, `scheme`, `xml:lang`, and `dir` attributes while stripping
  invalid property names, undeclared property prefixes, duplicate metadata
  IDs, invalid metadata IDs, bad `refines` values, invalid schemes, undeclared
  scheme prefixes, invalid language/direction attributes, and primary metadata
  leakage.
- Read-back under `epubAlternateRootfilePackages` confirms the sanitized
  alternate package does not self-diagnose `invalid-package-meta-property`,
  `undeclared-package-meta-property-prefix`, `invalid-package-meta-scheme`,
  `undeclared-package-meta-scheme-prefix`, `invalid-metadata-id`,
  `duplicate-metadata-id`, `invalid-metadata-refines`,
  `invalid-metadata-xml-language`, or `invalid-metadata-dir`.
- WordPress/Data Liberation workflows can now export alternate EPUB3
  renditions with sanitized package metadata properties and recover valid
  custom metadata for block-oriented processing.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back alternate-rootfile package metadata property sanitation
  parity. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond the current container-rootfile path, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Rootfile Nav Document Metadata And Specialized Lists Parity Slice

- Focused PHP coverage is now 825 behavior tests with 11,819 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3
  alternate-rootfile nav document metadata parity for alternate TOC,
  landmarks, page-list, and auxiliary navigation sections.
- The focused writer fixture emits `ALT/nav-meta.xhtml` from structured
  alternate-rootfile metadata carrying alternate-scoped nav root attributes,
  nav body attributes, TOC nav attributes/title, landmark nav attributes/title,
  page-list nav attributes/title, TOC entries, landmark entries, page-list
  entries, and an auxiliary `loi` navigation section.
- Generated alternate nav XHTML preserves alternate-relative links, alternate
  nav section headings and attributes, a pagebreak-backed page-list target in
  `ALT/text/nav.xhtml`, and keeps primary navigation metadata out of the
  alternate nav document while keeping alternate navigation metadata out of the
  primary nav document.
- Read-back under `epubAlternateRootfilePackages` confirms the alternate
  package keeps its TOC, landmark, page-list, auxiliary nav, nav root/body
  attributes, nav section attributes/titles, page-list value, and pagebreak
  target without page-list target diagnostics or primary navigation metadata
  leakage.
- WordPress/Data Liberation workflows can now export alternate EPUB3
  renditions with scoped navigation metadata and recover those navigation
  summaries on import for block-oriented processing.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back alternate-rootfile nav document metadata parity. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond the current container-rootfile path, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Split Alternate Rootfile XHTML Per-Spine Metadata Parity Slice

- Focused PHP coverage is now 824 behavior tests with 11,776 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 split
  alternate-rootfile XHTML metadata parity for per-spine head, root, and body
  metadata on split alternate spine documents.
- The focused writer fixture emits `ALT/text/split.xhtml` and
  `ALT/text/split-2.xhtml` from structured alternate-rootfile metadata carrying
  two distinct `spineItemRefs` entries with separate head titles, viewport
  metadata, head metas, head links, XHTML root attributes, and XHTML body
  attributes.
- Generated split alternate XHTML preserves page-specific head/root/body
  metadata on each generated spine document, marks the alternate OPF manifest
  items with `remote-resources` when head links require it, and keeps metadata
  from the first generated page out of the second page and vice versa.
- Read-back under `epubAlternateRootfilePackages` confirms the split alternate
  spine keeps distinct per-item head titles, viewports, head metas, head links,
  root attributes, and body attributes without `malformed-spine-xhtml`
  diagnostics or primary XHTML metadata leakage.
- WordPress/Data Liberation workflows can now export split alternate EPUB3
  renditions with page-specific generated XHTML metadata and recover those
  per-spine distinctions on import for block-oriented processing.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back split alternate-rootfile XHTML per-spine metadata parity.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graph validation beyond the current container-rootfile path, DRM decryption,
  XML signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Rootfile XHTML Head Root Body Metadata Parity Slice

- Focused PHP coverage is now 823 behavior tests with 11,732 full-suite
  assertions across the Pandoc lane. This slice closes generated alternate
  EPUB3 rootfile XHTML metadata parity for head, root, and body metadata on
  generated alternate spine documents.
- The focused writer fixture emits `ALT/text/xhtml.xhtml` from structured
  alternate-rootfile metadata carrying `spineItemRefs` head title, viewport,
  head metas, head base, head links, inline head styles, external and inline
  head scripts, XHTML root attributes, XHTML body attributes, and sidecar
  resource payloads.
- Generated alternate XHTML preserves the expected head/root/body attributes,
  rewrites linked CSS/script/image paths relative to the alternate chapter,
  emits `scripted` manifest properties for alternate spine XHTML, packages
  alternate sidecar resources, and ignores primary spine XHTML metadata that
  should not leak into the alternate rendition.
- Read-back under `epubAlternateRootfilePackages` confirms the alternate spine
  keeps its head title, viewport, head metas, bases, links, styles, scripts,
  root attributes, body attributes, and resource payloads without
  `malformed-spine-xhtml` diagnostics or primary XHTML metadata leakage.
- WordPress/Data Liberation workflows can now export alternate EPUB3
  renditions with rich generated XHTML head/root/body metadata and recover the
  same metadata on import for block-oriented processing.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back generated alternate-rootfile XHTML metadata parity. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond the current container-rootfile path, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Alternate Rootfile Collection Sanitation Parity Slice

- Focused PHP coverage is now 822 behavior tests with 11,688 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3
  alternate-rootfile OPF collection sanitation parity for collection roots,
  nested collections, collection metadata, and collection links.
- The focused writer fixture emits `ALT/collections.opf` from structured
  alternate-rootfile metadata carrying duplicate and malformed collection
  roles, reserved `idpf.org` role IRIs, duplicate collection IDs, invalid root
  language/direction attributes, invalid metadata IDs, duplicate metadata IDs,
  undeclared metadata property/scheme prefixes, invalid metadata refines, bad
  collection link relation/property tokens, and invalid link language tags.
- Generated alternate OPF preserves valid collection roles, nested collection
  language, first valid collection and metadata IDs, valid local metadata and
  link refines, declared-prefix metadata, valid direct collection member links,
  and sanitized link token/language attributes. It omits invalid-only
  collections, duplicate IDs, invalid attributes, undeclared-prefix metadata
  and link tokens, bad refines, and malformed link language values.
- Read-back under `epubAlternateRootfilePackages` confirms the sanitized
  collections round-trip without invalid role/language/direction/id,
  duplicate-id, undeclared-prefix, bad-refines, missing-rel, or invalid
  package-link token/language diagnostics.
- WordPress/Data Liberation workflows can now export alternate EPUB3 rendition
  collection graphs from noisy structured metadata without causing alternate
  OPF collection self-diagnostics or leaking alternate collection IDs into the
  primary OPF.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back alternate-rootfile OPF collection sanitation parity. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond the current container-rootfile path, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output parity.

## 2026-06-23 EPUB Alternate Rootfile Package Root And Dublin Core Sanitation Parity Slice

- Focused PHP coverage is now 821 behavior tests with 11,596 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3
  alternate-rootfile package-root and Dublin Core metadata sanitation parity.
- The focused writer fixture emits `ALT/package-root.opf` from structured
  alternate-rootfile metadata carrying mixed valid and invalid package prefix
  declarations, an invalid package `xml:lang`, an invalid `dc:language`, and a
  duplicate Dublin Core metadata `id`.
- Generated alternate OPF preserves the valid package id, unique identifier,
  package prefix declarations, fallback `dc:language`, and first Dublin Core
  creator id while omitting invalid prefix declarations, invalid language
  values, duplicate DC ids, and primary-package scalar leakage.
- Read-back under `epubAlternateRootfilePackages` confirms the alternate
  package keeps the sanitized prefix and language metadata, does not round-trip
  the invalid `en_US` Dublin Core language payload, and does not
  self-diagnose package-prefix, package-language, metadata-language, or
  duplicate-id errors.
- WordPress/Data Liberation workflows can now export alternate EPUB3 rendition
  package-root and Dublin Core metadata from noisy structured metadata without
  creating invalid alternate OPF root attributes, invalid DC language records,
  or duplicate metadata ids.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back alternate-rootfile package-root and Dublin Core metadata
  sanitation parity. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond the current container-rootfile
  path, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Rootfile Package And Collection Link Sanitation Parity Slice

- Focused PHP coverage is now 820 behavior tests with 11,563 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3
  alternate-rootfile coverage for package metadata links, collection metadata
  links, and direct collection member links carrying valid local hrefs, valid
  remote hrefs, missing local hrefs, and stray inline content in structured
  metadata.
- The existing shared `EpubWriter::packageOpf()` path already carries primary
  package/collection link sanitation into generated alternate rootfiles.
  Focused coverage now proves that `ALT/links.opf` preserves valid alternate
  sidecar links, generated alternate XHTML collection member links, and remote
  links while omitting missing local package metadata, collection metadata, and
  direct collection member hrefs.
- The same alternate-rootfile fixture feeds `value`, `text`, and child-like
  arrays on package links, collection metadata links, and direct collection
  member links. Generated OPF serializes the surviving links as empty
  `<link/>` elements and does not leak forbidden inline text or child payloads.
- Read-back under `epubAlternateRootfilePackages` confirms valid links remain
  in package and collection summaries, missing local IDs do not round-trip,
  sidecar payloads are packaged, and the alternate package does not
  self-diagnose `missing-package-link-resource`,
  `missing-package-link-fragment`, or `invalid-package-link-content`.
- WordPress/Data Liberation workflows can now export alternate EPUB3 rendition
  link graphs from noisy structured metadata without producing stale local
  references or package-link content-model violations in alternate OPFs.
- Dependency-closure checkpoint: this strengthens direct EPUB3
  writer/read-back alternate-rootfile package and collection link sanitation
  parity. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond the current container-rootfile path, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Package And Collection Link Inline Content Writer Sanitation Slice

- Focused PHP coverage is now 819 behavior tests with 11,518 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic coverage for package metadata links, collection metadata
  links, and direct collection member links carrying stray inline content in
  structured metadata.
- `EpubWriter` already serializes OPF package-link entries as empty
  `<link/>` elements. Focused coverage now proves that structured metadata
  fields such as `value`, `text`, and child-like arrays on package links are
  ignored during OPF serialization instead of becoming forbidden link text or
  child elements.
- The test feeds package metadata links, collection metadata links, and direct
  collection member links with inline text/value fields plus child-like
  metadata arrays. Generated `OEBPS/package.opf` preserves the intended link
  attributes while omitting all inline text and child payloads.
- Read-back confirms generated metadata and collection summaries keep the
  package/collection links, do not round-trip the stray inline text, and do
  not self-diagnose `invalid-package-link-content`.
- WordPress/Data Liberation workflows can now pass through noisy structured
  link metadata without producing OPF package links that violate the EPUB3
  package-link content model.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package and
  collection link content-model sanitation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Package And Collection Link Missing Resource Sanitation Slice

- Focused PHP coverage is now 818 behavior tests with 11,498 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps for package metadata links, collection metadata links,
  and direct collection member links that point at missing local ZIP resources.
- `EpubWriter` now threads the emitted package resource map into package-link
  serialization. Local package-relative `href` values must resolve to an
  emitted chapter, nav, NCX, resource payload, or the package document itself
  before they are written as OPF package links. Remote links remain allowed
  when their other package-link attributes are valid.
- Focused writer coverage feeds valid local sidecar links, valid direct
  collection links to generated XHTML, valid remote package/collection links,
  and missing local package metadata, collection metadata, and direct
  collection member links. Generated `OEBPS/package.opf` preserves the valid
  local/remote links and omits the missing local links.
- Read-back confirms generated metadata and collection summaries do not
  round-trip the missing local links and do not self-diagnose
  `missing-package-link-resource` or stale missing-fragment package-link
  diagnostics.
- A primary-body alternate-rootfile fixture now packages the primary
  `metadata/primary.xml` sidecar it was already linking, keeping that scenario
  focused on preventing primary metadata inheritance into alternate OPFs rather
  than depending on an unbacked local metadata link.
- WordPress/Data Liberation workflows can now export EPUB3 package/collection
  link graphs from user-supplied metadata without creating OPF links to local
  resources that the generated ZIP does not contain, while still preserving
  legitimate remote metadata/member links.
- Dependency-closure checkpoint: this strengthens direct EPUB3 package and
  collection link resource-provenance sanitation. It does not claim package
  link inline-content diagnostics, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond the current container-rootfile
  path, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Alternate Rootfile Metadata Resource Refines Parity Slice

- Focused PHP coverage is now 817 behavior tests with 11,475 full-suite
  assertions across the Pandoc lane. This slice closes the alternate-rootfile
  coverage gap for generated EPUB3 OPF package metadata resource-target
  `refines` serialization.
- The existing shared `EpubWriter::packageOpf()` path already carries the
  primary package metadata resource-refines sanitation into generated alternate
  rootfiles. Focused coverage now proves that alternate OPFs preserve valid
  metadata `refines` targets to alternate sidecar XML resources, generated
  alternate XHTML fragments, and the alternate package document itself.
- The same coverage feeds missing alternate resources, missing sidecar XML
  fragments, missing alternate XHTML fragments, and metadata record links with
  disallowed `refines`. Generated `ALT/refines.opf` omits those invalid or
  unprovable targets while preserving valid metadata properties and links.
- Read-back checks the alternate package summary under
  `epubAlternateRootfilePackages` and confirms it does not self-diagnose
  `invalid-metadata-refines`, `missing-metadata-refines-resource`,
  `missing-metadata-refines-fragment`, `missing-metadata-refines-target`, or
  `invalid-package-link-record-refines`.
- WordPress/Data Liberation workflows can now rely on generated alternate EPUB3
  renditions to preserve package metadata refinements to their own sidecar
  resources and XHTML content without leaking alternate sidecars into the
  primary OPF or producing alternate-package diagnostics for stale refines.
- Dependency-closure checkpoint: this strengthens direct EPUB3 writer/read-back
  alternate-rootfile package metadata resource-refines parity. It does not
  claim full collection/link content/resource-provenance sanitation, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Package-Wide Collection ID Writer Sanitation Slice

- Focused PHP coverage is now 816 behavior tests with 11,444 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in package-wide OPF collection `id` serialization.
- `EpubWriter` now tracks emitted collection IDs across recursive collection
  rendering. The first valid collection ID is preserved, later duplicate IDs
  are omitted, and the same emitted-ID state is shared by nested collection
  rendering so duplicates are sanitized across the whole package rather than
  only within a single collection branch.
- Collection metadata `refines` target calculation now mirrors the collection
  IDs that will actually be emitted. Metadata inside duplicate-ID collections
  no longer writes `refines` values pointing at omitted duplicate root IDs, and
  parent collection metadata no longer writes `refines` values pointing at
  omitted duplicate nested collection IDs.
- Focused writer coverage feeds a top-level duplicate collection ID, a later
  nested duplicate collection ID, valid metadata refines to the first emitted
  collection ID, and invalid metadata refines to omitted duplicate IDs.
  Generated `OEBPS/package.opf` contains each collection ID only once,
  preserves valid refines to emitted IDs, omits refines to suppressed IDs, and
  read-back does not self-diagnose `duplicate-collection-id`,
  `invalid-collection-metadata-refines`, or
  `collection-metadata-refines-outside`.
- WordPress/Data Liberation workflows can now export EPUB3 collection graphs
  from user-supplied metadata without repeated collection identifiers causing
  generated OPF to fail the same reader diagnostics or leaving stale collection
  metadata refinements behind.
- Dependency-closure checkpoint: this strengthens direct EPUB3 writer/read-back
  OPF collection ID sanitation. It does not claim alternate-rootfile-specific
  coverage for this behavior, full collection/link content/resource-provenance
  sanitation, full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond the current container-rootfile path, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output parity.

## 2026-06-23 EPUB Collection Root Role/Language Writer Sanitation Slice

- Focused PHP coverage is now 815 behavior tests with 11,422 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in OPF collection root `role` and `xml:lang`
  serialization.
- `EpubWriter` now normalizes collection role token lists before writing OPF
  collection elements. Duplicate valid role tokens are de-duplicated,
  malformed role tokens such as `bad/role` are omitted, reserved `idpf.org`
  custom role IRIs are omitted, valid absolute custom role IRIs are preserved,
  and collections with no valid role token are skipped instead of producing a
  missing-role diagnostic.
- Collection root `xml:lang` values now use the same language-tag validation
  path as collection metadata and links. Invalid root language tags are omitted
  while valid nested collection languages still round-trip.
- Focused writer coverage feeds duplicate schema roles, malformed role tokens,
  reserved `https://idpf.org/custom` roles, valid custom absolute role IRIs,
  invalid root language tags, valid nested root language tags, and an
  invalid-only collection role. Generated `OEBPS/package.opf` preserves only
  valid role and language attributes, and read-back does not self-diagnose
  `invalid-collection-role`, `duplicate-collection-role`,
  `invalid-collection-role-idpf-host`, `invalid-collection-xml-language`, or
  `missing-collection-role`.
- WordPress/Data Liberation workflows can now export EPUB3 collection graphs
  from user-supplied metadata without raw collection role strings or root
  languages causing the generated OPF package to fail the same reader's
  collection diagnostics.
- Dependency-closure checkpoint: this strengthens direct EPUB3 writer/read-back
  OPF collection root sanitation. It does not claim duplicate collection-id
  writer sanitation, alternate-rootfile-specific coverage for this behavior,
  full collection/link content/resource-provenance sanitation, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the current
  container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output parity.

## 2026-06-23 EPUB Top-Level Metadata Resource Refines Writer Sanitation Slice

- Focused PHP coverage is now 814 behavior tests with 11,404 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in top-level OPF metadata resource-target `refines`
  serialization.
- `EpubWriter` now validates top-level package metadata property/link
  `refines` values against emitted package IDs, generated spine/nav resources,
  and XML-like resource payloads before writing them. Valid resource targets
  such as metadata sidecars and generated XHTML fragments are preserved, while
  missing resources, missing XML/XHTML fragments, malformed fragments,
  absolute URLs, and invalid record-link refines are omitted.
- Focused writer coverage feeds metadata properties and links with valid
  sidecar XML, generated chapter XHTML, and `package.opf#id` refines targets
  plus missing resources, missing fragments, absolute URLs, and record-link
  refines. Generated `OEBPS/package.opf` preserves only the valid resource
  refines attributes and read-back does not self-diagnose
  `invalid-metadata-refines`, `missing-metadata-refines-resource`,
  `missing-metadata-refines-fragment`, `missing-metadata-refines-target`, or
  `invalid-package-link-record-refines`.
- WordPress/Data Liberation workflows can now export top-level EPUB3 package
  metadata refinements that point at packaged content or metadata resources
  without producing OPF that the same reader immediately flags as invalid when
  user-supplied refines values are malformed or point at missing targets.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF top-level metadata resource-refines path. It does not
  claim alternate-rootfile resource-target metadata refines behavior, full
  collection/link content/resource-provenance sanitation, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,606 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 5,054 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  11,404 assertions, 0 failures. Full pass-count verification reported
  `pass=814 fail=0`.

## 2026-06-23 EPUB Collection Metadata Refines Writer Sanitation Slice

- Focused PHP coverage is now 813 behavior tests with 11,373 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in OPF collection metadata `refines` serialization.
- `EpubWriter` now computes the ID set for the emitted containing collection
  tree before writing collection metadata. Valid `#id` refines values are kept
  when they point to collection-local metadata or link elements, while
  malformed, missing, or package-level outside targets are omitted.
- Focused writer coverage feeds collection metadata with valid local metadata
  and collection-link refines values, a package-level outside target, a bare
  non-fragment target, a malformed fragment, and a missing collection-local
  target. Generated `OEBPS/package.opf` preserves only the valid refines
  attributes and read-back does not self-diagnose
  `invalid-collection-metadata-refines` or
  `collection-metadata-refines-outside`.
- WordPress/Data Liberation workflows can now export EPUB3 collection metadata
  refinements from user-supplied collection structures without producing OPF
  that the same reader immediately flags as invalid when refines values are
  malformed or point outside the collection.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF collection metadata refines path. It does not claim
  top-level resource-target metadata refines writer behavior, full
  collection/link content/resource-provenance sanitation, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,575 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 5,023 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  11,373 assertions, 0 failures. Full pass-count verification reported
  `pass=813 fail=0`.

## 2026-06-23 EPUB Top-Level Metadata Property Writer Sanitation Slice

- Focused PHP coverage is now 812 behavior tests with 11,353 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in top-level OPF `meta property` serialization.
- `EpubWriter` now sanitizes `epubMetadataProperties`: malformed property
  values are skipped, undeclared property prefixes are skipped, duplicate
  metadata IDs are suppressed, invalid XML IDs are omitted, invalid `refines`
  values are suppressed, invalid or undeclared scheme attributes are omitted,
  invalid `xml:lang` and `dir` attributes are omitted, and valid
  custom-prefixed package metadata properties are preserved.
- Focused writer coverage feeds top-level package metadata properties with
  malformed and undeclared property values, duplicate and invalid IDs, invalid
  refines values, malformed and undeclared schemes, invalid language and
  direction attributes, and a valid declared custom-prefixed property.
  Generated `OEBPS/package.opf` emits only sanitized metadata records and
  read-back does not self-diagnose package metadata property, scheme, ID,
  refines, language, or direction errors.
- WordPress/Data Liberation workflows can now export top-level EPUB3 package
  metadata properties with malformed user-supplied metadata without producing
  OPF that the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF top-level package metadata property path. It does not
  claim full resource-target metadata refines behavior, full collection/link
  content/resource-provenance sanitation, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond the current
  container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,555 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 5,003 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  11,353 assertions, 0 failures. Full pass-count verification reported
  `pass=812 fail=0`.

## 2026-06-23 EPUB Top-Level Metadata-Link Writer Sanitation Slice

- Focused PHP coverage is now 811 behavior tests with 11,312 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in top-level OPF metadata-link href, relation/property
  token, language, media-type, and refines serialization.
- `EpubWriter` now applies package-link sanitation to `epubMetadataLinks`:
  unsafe hrefs are skipped, relation/property token lists are filtered against
  OPF property-value syntax and declared package prefixes, duplicate valid
  tokens are de-duplicated, invalid language tags are omitted, invalid explicit
  media types are dropped while local resource media types are inferred,
  invalid `refines` values are suppressed, and metadata `voicing` relations
  without a valid `#id` refines target are skipped.
- Focused writer coverage feeds top-level metadata links with data URLs,
  invalid fragments, malformed and undeclared relation/property tokens,
  duplicate tokens, invalid language tags, invalid explicit media types,
  record links with invalid refines, voicing links with missing or invalid
  refines values, and a valid voicing link. Generated `OEBPS/package.opf`
  emits only sanitized links, infers `application/xml` and `audio/mpeg` where
  appropriate, preserves declared custom property tokens, and read-back does
  not self-diagnose package-link href, rel/property, language, media-type,
  record-refines, or voicing-refines errors.
- WordPress/Data Liberation workflows can now export top-level EPUB3 metadata
  links with malformed user-supplied metadata without producing OPF that the
  same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF top-level metadata-link path. It does not claim full
  package metadata property sanitation, full collection/link content/resource
  provenance sanitation, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graph validation beyond the current container-rootfile path, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,514 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,962 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  11,312 assertions, 0 failures. Full pass-count verification reported
  `pass=811 fail=0`.

## 2026-06-23 EPUB Collection Link Href/Media/Refines Writer Sanitation Slice

- Focused PHP coverage is now 810 behavior tests with 11,268 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in OPF collection link href, media-type, and
  metadata-link refines serialization.
- `EpubWriter` now rejects unsafe package-link hrefs before writing collection
  link elements, omits invalid explicit media types while still inferring
  valid local resource media types, suppresses invalid metadata-link
  `refines` values, and drops metadata `voicing` relations that do not have a
  valid `#id` refines target.
- Focused writer coverage feeds collection links with data URLs, absolute
  paths, invalid fragments, invalid explicit media types, metadata record links
  with invalid media/refines values, metadata voicing links with missing or
  invalid refines values, and a valid voicing link. Generated
  `OEBPS/package.opf` skips unsafe links, infers `application/xml` and
  `audio/mpeg` where appropriate, preserves the valid voicing link, and
  read-back does not self-diagnose package-link href, media-type, record
  refines, or voicing-refines errors.
- WordPress/Data Liberation workflows can now export EPUB3 collection links
  with malformed href/media/refines metadata without producing OPF that the
  same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF collection link href/media/refines path. It does not
  claim full collection/link content/resource-provenance sanitation, top-level
  package metadata-link parity, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond the current container-rootfile
  path, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,470 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,918 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  11,268 assertions, 0 failures. Full pass-count verification reported
  `pass=810 fail=0`.

## 2026-06-23 EPUB Collection Link Relation/Property Writer Sanitation Slice

- Focused PHP coverage is now 809 behavior tests with 11,239 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in OPF collection link `rel` and `properties` token
  serialization.
- `EpubWriter` now filters collection link relation/property token lists
  against OPF property-value syntax and declared package prefixes before
  writing OPF link elements. Duplicate valid tokens are de-duplicated,
  declared custom-prefixed tokens are preserved, and links with no valid
  relation are skipped.
- Focused writer coverage feeds direct collection links with malformed tokens,
  duplicate tokens, and undeclared-prefixed tokens. Generated
  `OEBPS/package.opf` emits only the valid `rel="contents custom:part"` and
  `properties="series custom:featured"` tokens, omits invalid link rows, and
  read-back does not self-diagnose `duplicate-package-link-rel`,
  `invalid-package-link-rel`, `undeclared-package-link-rel-prefix`,
  `duplicate-package-link-property`, `invalid-package-link-property`,
  `undeclared-package-link-property-prefix`, or `missing-package-link-rel`.
- WordPress/Data Liberation workflows can now export EPUB3 collection links
  with malformed, duplicated, or undeclared user-supplied relation/property
  tokens without producing OPF that the same reader immediately flags as
  invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF collection link relation/property token path. It does
  not claim full collection/link media-type/path/refines/content sanitation,
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond the current container-rootfile path, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,441 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,889 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  11,239 assertions, 0 failures. Full pass-count verification reported
  `pass=809 fail=0`.

## 2026-06-23 EPUB Collection Link Language Writer Sanitation Slice

- Focused PHP coverage is now 808 behavior tests with 11,222 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in OPF collection link `hreflang` and `xml:lang`
  serialization.
- `EpubWriter` now validates collection link `hreflang` and `xml:lang`
  attributes before writing OPF link elements. Invalid values are omitted while
  valid language tags are preserved.
- Focused writer coverage feeds direct collection links with invalid
  `hreflang="bad lang"` and `xml:lang="en_US"` alongside valid `hreflang="pl"`
  and `xml:lang="fr"` values. Generated `OEBPS/package.opf` omits the invalid
  language attributes, preserves valid language tags, and read-back does not
  self-diagnose `invalid-package-link-hreflang` or
  `invalid-package-link-xml-language`.
- WordPress/Data Liberation workflows can now export EPUB3 collection links
  with malformed user-supplied language metadata without producing OPF that the
  same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF collection link language path. It does not claim full
  collection/link rel/property/media-type/path sanitation, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,424 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,872 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  11,222 assertions, 0 failures. Full pass-count verification reported
  `pass=808 fail=0`.

## 2026-06-23 EPUB Collection Metadata Prefix Writer Sanitation Slice

- Focused PHP coverage is now 807 behavior tests with 11,210 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in OPF collection metadata property/scheme prefix
  declarations.
- `EpubWriter` now builds the collection metadata declared-prefix set from
  reserved EPUB package prefixes plus sanitized package `prefix` declarations.
  Records with undeclared property prefixes are skipped, scheme attributes with
  undeclared prefixes are omitted, and valid custom-prefixed properties/schemes
  still round-trip.
- Focused writer coverage feeds collection metadata with undeclared
  `missing:position` property and `missingScheme:codelist` scheme values plus
  a valid declared `custom:ranking` / `custom:codelist` pair. Generated
  `OEBPS/package.opf` omits the undeclared values while preserving the valid
  custom metadata, and read-back does not self-diagnose
  `undeclared-collection-metadata-property-prefix` or
  `undeclared-collection-metadata-scheme-prefix`.
- WordPress/Data Liberation workflows can now export EPUB3 collection metadata
  with undeclared user-supplied property/scheme prefixes without producing OPF
  that the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF collection metadata prefix path. It does not claim full
  collection/link attribute sanitation, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond the current container-rootfile
  path, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,412 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,860 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  11,210 assertions, 0 failures. Full pass-count verification reported
  `pass=807 fail=0`.

## 2026-06-23 EPUB Collection Metadata Property/Scheme Writer Sanitation Slice

- Focused PHP coverage is now 806 behavior tests with 11,196 full-suite
  assertions across the Pandoc lane. This slice closes generated EPUB3 writer
  self-diagnostic gaps in OPF collection metadata `property` and `scheme`
  serialization.
- `EpubWriter` now validates collection metadata `property` values before
  writing collection `<meta>` elements. Records with invalid properties are
  skipped rather than exported as malformed OPF. Invalid `scheme` attributes
  are omitted while valid property/scheme values are preserved.
- Focused writer coverage feeds collection metadata with invalid
  `bad property`, invalid `bad scheme`, and valid prefixed values such as
  `schema:position` and `marc:relators`; it proves generated
  `OEBPS/package.opf` omits the malformed record and bad scheme while
  preserving valid metadata, and confirms read-back does not self-diagnose
  `invalid-collection-metadata-property` or
  `invalid-collection-metadata-scheme`.
- WordPress/Data Liberation workflows can now export EPUB3 collection metadata
  with malformed user-supplied property/scheme values without producing OPF
  that the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF collection metadata property/scheme path. It does not
  claim undeclared-prefix sanitation for collection metadata, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,398 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,846 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  11,196 assertions, 0 failures. Full pass-count verification reported
  `pass=806 fail=0`.

## 2026-06-23 EPUB Collection Metadata ID Writer Sanitation Slice

- Focused PHP coverage is now 805 behavior tests with 11,183 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in OPF collection metadata `id` serialization.
- `EpubWriter` now validates collection metadata meta IDs and tracks IDs per
  collection metadata block. Invalid IDs are omitted, while later metadata
  entries with duplicate valid IDs keep their text but lose the repeated `id`
  attribute.
- Focused writer coverage feeds collection metadata with an invalid
  `bad collection id` value and duplicate `series-subtitle` IDs, proves
  generated `OEBPS/package.opf` omits the invalid and repeated IDs while
  preserving metadata text, and confirms read-back does not self-diagnose
  `invalid-collection-metadata-id` or `duplicate-collection-metadata-id`.
- WordPress/Data Liberation workflows can now round-trip EPUB3 packages with
  imported or user-supplied collection metadata ID collisions without
  exporting OPF that the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF collection metadata identity path. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond the current container-rootfile path, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,385 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,833 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,183 assertions, 0 failures. Full
  pass-count verification reported `pass=805 fail=0`.

## 2026-06-23 EPUB Collection Metadata Language Writer Sanitation Slice

- Focused PHP coverage is now 804 behavior tests with 11,172 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in OPF collection metadata `xml:lang`
  serialization.
- `EpubWriter` now validates collection metadata language fields before writing
  collection `<meta>` elements. Invalid language tags are omitted, while valid
  values such as `pl` are preserved.
- Focused writer coverage feeds collection metadata with an invalid
  `xml:lang` value such as `bad lang`, proves generated `OEBPS/package.opf`
  omits that invalid attribute while preserving valid collection metadata
  language, and confirms read-back does not self-diagnose
  `invalid-collection-metadata-xml-language`.
- WordPress/Data Liberation workflows can now round-trip EPUB3 packages with
  imported or user-supplied collection metadata language issues without
  exporting OPF that the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF collection metadata language path. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond the current container-rootfile path, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,374 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,822 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,172 assertions, 0 failures. Full
  pass-count verification reported `pass=804 fail=0`.

## 2026-06-23 EPUB Dublin Core Metadata ID Writer Sanitation Slice

- Focused PHP coverage is now 803 behavior tests with 11,165 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in OPF Dublin Core metadata `id` serialization.
- `EpubWriter` now tracks IDs while building custom `epubDublinCoreMetadata`
  and `dublinCoreMetadata` records. Later records with duplicate valid IDs keep
  their metadata text, but their repeated `id` attribute is suppressed.
- Focused writer coverage feeds a duplicate `creator-primary` ID across
  custom `dc:creator` and `dc:contributor` records, proves generated
  `OEBPS/package.opf` emits the ID only once while preserving the contributor
  text, and confirms read-back does not self-diagnose `duplicate-metadata-id`
  or package-wide `duplicate-package-id`.
- WordPress/Data Liberation workflows can now round-trip EPUB3 packages with
  imported or user-supplied Dublin Core metadata ID collisions without
  exporting OPF that the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF Dublin Core metadata identity path. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond the current container-rootfile path, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,367 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,815 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,165 assertions, 0 failures. Full
  pass-count verification reported `pass=803 fail=0`.

## 2026-06-23 EPUB Dublin Core Metadata Language Writer Sanitation Slice

- Focused PHP coverage is now 802 behavior tests with 11,160 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in OPF Dublin Core metadata language
  serialization.
- `EpubWriter` now validates custom `epubDublinCoreMetadata` and
  `dublinCoreMetadata` language fields before writing Dublin Core metadata.
  Invalid `dc:language` values are dropped so the normal valid package
  language fallback can satisfy required metadata, and invalid Dublin Core
  `xml:lang` attributes are omitted.
- Focused writer coverage feeds invalid Dublin Core language metadata such as
  `en_US`, proves generated `OEBPS/package.opf` omits both bad
  `<dc:language>` values and bad Dublin Core `xml:lang` attributes, and
  confirms read-back does not self-diagnose `invalid-metadata-language` or
  `invalid-metadata-xml-language`.
- WordPress/Data Liberation workflows can now round-trip EPUB3 packages with
  imported or user-supplied Dublin Core language metadata without exporting
  OPF that the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF Dublin Core metadata language path. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graph validation
  beyond the current container-rootfile path, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,362 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,810 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,160 assertions, 0 failures. Full
  pass-count verification reported `pass=802 fail=0`.

## 2026-06-23 EPUB Package Language Writer Sanitation Slice

- Focused PHP coverage is now 801 behavior tests with 11,154 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in OPF package `xml:lang` serialization.
- `EpubWriter` now validates incoming `packageLanguage` and
  `epubPackageLanguage` metadata before writing the OPF package root
  `xml:lang` attribute, using the same language-tag rule the reader applies.
- Focused writer coverage feeds invalid package language metadata such as
  `en_US`, proves generated `OEBPS/package.opf` omits the bad `xml:lang`, and
  confirms read-back does not self-diagnose `invalid-package-language`.
- WordPress/Data Liberation workflows can now round-trip EPUB3 packages with
  imported or user-supplied package language metadata without exporting OPF
  that the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF package-language metadata path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,356 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,804 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,154 assertions, 0 failures. Full
  pass-count verification reported `pass=801 fail=0`.

## 2026-06-23 EPUB Package Prefix Writer Sanitation Slice

- Focused PHP coverage is now 800 behavior tests with 11,151 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in OPF package `prefix` serialization.
- `EpubWriter` now sanitizes incoming `packagePrefix` and `epubPackagePrefix`
  metadata before writing generated OPF packages. Malformed prefix tokens,
  invalid XML prefix names, relative IRIs, reserved-prefix overrides, and
  duplicate prefix declarations are skipped.
- Valid package prefix declarations are preserved in OPF order after malformed
  pairs are dropped. Focused writer coverage proves valid `schema`, `custom`,
  and `rendition` declarations survive while invalid `bad*`, relative IRI,
  `_`, reserved `dcterms`, and duplicate `custom` rows are omitted.
- Read-back confirms generated packages do not self-diagnose
  `invalid-package-prefix`, `invalid-package-prefix-name`,
  `invalid-package-prefix-iri`, `reserved-package-prefix`,
  `duplicate-package-prefix`, or `overridden-package-prefix`.
- WordPress/Data Liberation workflows can now round-trip EPUB3 packages with
  imported or user-supplied package-prefix metadata without exporting OPF that
  the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OPF package-prefix metadata path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,353 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,801 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,151 assertions, 0 failures. Full
  pass-count verification reported `pass=800 fail=0`.

## 2026-06-23 EPUB OCF Container Version Normalization Slice

- Focused PHP coverage remains 799 behavior tests with 11,138 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in OCF `META-INF/container.xml` version
  serialization.
- `EpubWriter` now always emits the OCF-required container `version="1.0"`
  instead of preserving incoming `containerVersion` or `epubContainerVersion`
  metadata such as `2.0`.
- Focused writer coverage now feeds an invalid requested container version,
  proves generated `container.xml` normalizes it to `1.0`, and confirms
  read-back does not self-diagnose `invalid-container-version`.
- WordPress/Data Liberation workflows can now round-trip EPUB3 packages with
  stale or foreign container-version metadata without exporting a container
  document that the same reader immediately flags as invalid.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OCF container metadata path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,340 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,788 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,138 assertions, 0 failures. Full
  pass-count verification reported `pass=799 fail=0`.

## 2026-06-23 EPUB OCF Rootfile Writer Sanitation Slice

- Focused PHP coverage is now 799 behavior tests with 11,136 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in OCF `META-INF/container.xml` rootfile
  serialization and ZIP payload collection.
- `EpubWriter` now validates source paths for preserved and generated
  alternate rootfile payloads before normalizing them for ZIP output. Unsafe
  `data:`/`file:`/remote paths, absolute paths, backslashes, URL suffixes,
  traversal, encoded dot segments, and empty-normalized paths are skipped
  instead of being packaged or listed as rootfiles.
- Generated OCF rootfile rows now preserve valid OPF package payloads while
  suppressing invalid optional metadata: bad or duplicate rootfile IDs are
  omitted, duplicate/invalid rootfile property tokens are filtered, and invalid
  or non-OPF media types are defaulted to `application/oebps-package+xml`.
- Focused writer coverage now feeds a valid primary package, valid alternate
  OPF packages with duplicate IDs and bad optional metadata, and malformed
  payload keys. It proves only valid rootfile rows and ZIP entries survive,
  alternate OPFs stay outside the primary OPF manifest, and re-ingest does not
  self-diagnose invalid/duplicate rootfile IDs, invalid/duplicate properties,
  malformed rootfile full paths, invalid media types, or missing rootfile
  resources.
- WordPress/Data Liberation workflows can now export EPUB3 multi-rendition
  rootfile payloads without immediately flagging their own generated OCF
  rootfile metadata as invalid during re-ingest.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OCF rootfile path. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the current
  container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,338 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,786 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,136 assertions, 0 failures. Full
  pass-count verification reported `pass=799 fail=0`.

## 2026-06-23 EPUB OCF Container Link Writer Sanitation Slice

- Focused PHP coverage is now 798 behavior tests with 11,103 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in OCF `META-INF/container.xml` link
  serialization.
- `EpubWriter` now filters structured `epubContainerLinks` before writing
  container XML: unsafe `data:`/`file:` links, protocol-relative or absolute
  local paths, backslashes, dot segments, encoded dot segments, query-only
  paths, and local links to unpackaged sidecar payloads are skipped.
- Generated OCF links now normalize valid sidecar hrefs relative to
  `META-INF/`, de-duplicate and validate `rel` and `properties` token lists,
  require valid XML IDs for `id` and fragment IDs for `refines`, and suppress
  invalid optional `hreflang`, `xml:lang`, `dir`, and media-type attributes.
- Focused writer coverage now feeds one valid sidecar link alongside malformed
  rows and proves only the valid sanitized link is emitted, the sidecar payload
  remains outside the OPF manifest, and re-ingest does not self-diagnose
  invalid container-link IDs, refines values, relation/property tokens, href
  paths, or missing sidecar resources.
- WordPress/Data Liberation workflows can now export EPUB3 container-level
  linked metadata sidecars without immediately flagging their own generated
  OCF container link metadata as invalid during re-ingest.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back OCF container-link path. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the current
  container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,305 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,753 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,103 assertions, 0 failures. Full
  pass-count verification reported `pass=798 fail=0`.

## 2026-06-23 EPUB Writer Link Refines Sanitation Slice

- Focused PHP coverage remains 797 behavior tests with 11,077 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap in package-link and collection-link serialization.
- `EpubWriter` now suppresses `refines` on OPF metadata links whose `rel`
  token list includes `record`, while preserving valid metadata-link
  refinements such as `alternate` and `voicing`.
- Direct collection member links generated from `epubCollections[].links` no
  longer emit `refines`, matching the reader diagnostic rule that direct OPF
  collection links must not include that attribute. Collection metadata links
  generated from `epubCollections[].metadata` still use link refinements where
  the relation allows them.
- Focused writer coverage now proves package metadata `record` links,
  collection metadata `record alternate` links, and direct collection member
  links do not round-trip invalid `refines` attributes and do not self-diagnose
  `invalid-package-link-record-refines` or `invalid-collection-link-refines`.
- WordPress/Data Liberation workflows can now export EPUB3 linked metadata and
  collection member links without immediately flagging generated OPF as invalid
  during re-ingest.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back package-link and collection-link serialization path. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graph
  validation beyond the current container-rootfile path, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,279 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,448 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,727 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,077 assertions, 0 failures.

## 2026-06-23 EPUB Collection OPF2-Style Meta Name Validation Slice

- Focused PHP coverage remains 797 behavior tests with 11,069 full-suite
  assertions across the Pandoc lane. This slice closes a collection metadata
  content-model validation gap parallel to package-level OPF2 meta validation.
- `EpubReader` now reports `invalid-collection-opf2-meta` when EPUB3 OPF
  collection metadata `meta` elements carry an obsolete OPF2 `name` attribute
  even if they also have a valid EPUB3 `property` attribute.
- The malformed collection diagnostics fixture now covers both name-only
  collection metadata such as `meta name="cover"` and mixed
  `meta property="title-type" name="legacy-title-type"` records. The mixed
  diagnostic includes the collection id, property, legacy name, refines target,
  and value so downstream review can route the bad metadata precisely.
- WordPress/Data Liberation workflows can now surface mixed OPF2/EPUB3
  collection metadata during EPUB intake instead of accepting obsolete
  collection-local `name` attributes as valid EPUB3 metadata.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  collection metadata content-model validation path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,448 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,271 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,719 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,069 assertions, 0 failures.

## 2026-06-23 EPUB Writer Cover Metadata OPF3 Cleanup Slice

- Focused PHP coverage remains 797 behavior tests with 11,062 full-suite
  assertions across the Pandoc lane. This slice closes a generated EPUB3
  writer self-diagnostic gap introduced by stricter OPF2 meta validation.
- `EpubWriter` no longer emits the legacy OPF2 `<meta name="cover">` shim in
  generated OPF 3.0 package metadata. Generated primary and alternate rootfile
  packages now rely on the EPUB3 manifest `cover-image` property plus OPF guide
  references for cover intent.
- `EpubWriterTest` now proves generated primary cover resources and structured
  alternate-rootfile cover resources do not contain `<meta name="cover">`, do
  retain `properties="cover-image"` on the intended manifest item, and do not
  self-diagnose `invalid-package-opf2-meta` after read-back.
- WordPress/Data Liberation workflows can now export EPUB3 cover images without
  immediately re-importing their own generated package as mixed OPF2/EPUB3
  metadata, while reader compatibility still preserves legacy cover fallback
  metadata from existing EPUBs.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  writer/read-back cover metadata path. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the current
  container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/src/PandocFormatRegistry.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,271 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
  passed 1 file, 3,441 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,712 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,062 assertions, 0 failures.

## 2026-06-23 EPUB Package OPF2-Style Meta Name Validation Slice

- Focused PHP coverage remains 797 behavior tests with 11,059 full-suite
  assertions across the Pandoc lane. This slice closes an EPUB3 package
  metadata content-model validation gap.
- `EpubReader` now reports `invalid-package-opf2-meta` when an EPUB3 package
  `meta` element carries an obsolete OPF2 `name` attribute even if it also has
  a valid EPUB3 `property` attribute.
- The OPF2 meta diagnostics fixture now covers both legacy
  `meta name="cover"` records and mixed `meta property="title-type"
  name="legacy-title-type"` records. The legacy cover fallback metadata remains
  preserved for import compatibility.
- WordPress/Data Liberation workflows can now surface mixed OPF2/EPUB3 package
  metadata during EPUB intake instead of accepting the obsolete `name`
  attribute as valid EPUB3 metadata.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  package metadata content-model validation path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,441 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,268 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,709 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,059 assertions, 0 failures.

## 2026-06-23 EPUB Nested Collection Package-Link Resource Provenance Round-Trip Slice

- Focused PHP coverage remains 797 behavior tests with 11,053 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB
  writer collection/package metadata case.
- `EpubWriterTest` now proves generated nested collection metadata sidecars
  round-trip through `EpubReader` as enriched `epubPackageLinkResources` with
  both child and parent collection identity.
- The generated `schema:Collection` fixture now contains a child
  `schema:Periodical` collection with its own JSON metadata link and payload.
  The OPF output, structured collection metadata readback, extracted payload,
  and package-link resource record are all asserted.
- The re-read nested sidecar keeps `rel`, `refines`, `hreflang`, language,
  direction, properties, inferred media type, `parent`, `collectionId`, and
  `parentCollectionId`, while the linked JSON payload extraction remains
  unchanged.
- WordPress/Data Liberation workflows can now rely on generated EPUB nested
  collection sidecars retaining enough provenance to distinguish series-level
  package metadata from child issue/periodical metadata on re-ingest.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  write/read nested collection package-link path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,435 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,268 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,703 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,053 assertions, 0 failures.

## 2026-06-23 EPUB Writer Package-Link Resource Provenance Round-Trip Slice

- Focused PHP coverage remains 797 behavior tests with 11,039 full-suite
  assertions across the Pandoc lane. This slice strengthens existing EPUB
  writer package metadata and collection sidecar cases.
- `EpubWriterTest` now proves generated package metadata links and collection
  metadata/voicing links round-trip through `EpubReader` as enriched
  `epubPackageLinkResources`, not just packaged payloads.
- Generated top-level OPF metadata links retain `rel`, `refines`, `hreflang`,
  language, direction, properties, inferred media type, and parent metadata
  context after write/read.
- Generated collection metadata links retain package-link resource provenance,
  including source `rel`, `refines`, language/direction metadata, properties,
  `parent`, and `collectionId`, while linked XML/JSON/audio payload extraction
  remains unchanged.
- WordPress/Data Liberation workflows can now rely on exported EPUB sidecars
  carrying enough re-read provenance to route package metadata, collection
  metadata records, and voicing sidecars without reparsing the generated OPF.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  write/read package-link path. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graph validation beyond the current
  container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,435 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,254 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,689 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,039 assertions, 0 failures.

## 2026-06-23 EPUB Package-Link Resource Provenance Slice

- Focused PHP coverage remains 797 behavior tests with 11,030 full-suite
  assertions across the Pandoc lane. This slice strengthens existing EPUB
  linked metadata extraction cases.
- `EpubReader` now preserves source OPF link provenance on
  `epubPackageLinkResources`, including `rel`, `refines`, `hreflang`,
  language, direction, properties, parent element, `collectionId`, and
  `parentCollectionId` where present.
- Extracted local XML/JSON metadata resources no longer collapse to only
  id/href/path/media type; downstream consumers can tell whether each payload
  came from top-level package metadata or a collection link.
- `EpubReaderTest` strengthens the primary linked-resource diagnostics fixture
  and the alternate-rootfile linked metadata fixture, proving top-level
  metadata links and collection links retain source provenance while resource
  payload extraction remains unchanged.
- WordPress/Data Liberation workflows can now route linked records as package
  metadata or collection metadata without reparsing the OPF solely to recover
  link semantics.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  package metadata link extraction path. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graph validation beyond the current
  container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,435 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,245 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,680 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,030 assertions, 0 failures.

## 2026-06-23 EPUB Package Metadata Direction Diagnostics Slice

- Focused PHP coverage remains 797 behavior tests with 11,011 full-suite
  assertions across the Pandoc lane. This slice strengthens existing EPUB
  package metadata validation and preservation cases.
- `EpubReader` now reports `invalid-metadata-dir` for bad `dir` attributes on
  package metadata records, while continuing to leave OPF `link` direction
  checks to package-link diagnostics.
- Valid Dublin Core metadata direction and language attributes are explicitly
  preserved: the package metadata refinements fixture now proves a creator
  `xml:lang` and `dir` round-trip into `epubDublinCoreMetadata`.
- `EpubReaderTest` strengthens the required metadata namespace/language
  diagnostics fixture with invalid direction values on `dc:title` and
  unrefined `dcterms:modified` `meta` records.
- WordPress/Data Liberation workflows can now surface malformed text-direction
  metadata during EPUB package review instead of silently dropping bad `dir`
  values, while keeping valid creator language/direction metadata available for
  downstream mapping.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  package validation path. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond the current container-rootfile
  path, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,416 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,245 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,661 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,011 assertions, 0 failures.

## 2026-06-23 EPUB Package Metadata Empty Required-Copy Diagnostics Slice

- Focused PHP coverage remains 797 behavior tests with 11,000 full-suite
  assertions across the Pandoc lane. This slice strengthens existing EPUB
  package metadata validation cases.
- `EpubReader` now reports `empty-package-metadata-value` for blank duplicate
  `dc:identifier`, `dc:title`, and `dc:language` records when another non-empty
  sibling satisfies the required package metadata.
- The validator still avoids redundant missing-required diagnostics when no
  valid sibling exists, so empty required-name metadata produces the most useful
  package diagnostics for import review.
- `EpubReaderTest` strengthens the package metadata content-model fixture and
  the empty selected unique-identifier fixture, proving empty duplicate
  required-name records are reported while valid fallback identifiers remain
  usable.
- WordPress/Data Liberation workflows can now surface malformed blank duplicate
  package title, identifier, and language records instead of silently accepting
  them during EPUB intake.
- Dependency-closure checkpoint: this strengthens the current direct EPUB3
  package validation path. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graph validation beyond the current container-rootfile
  path, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,405 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,245 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,650 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 11,000 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Split-Spine Imported Block Provenance Slice

- Focused PHP coverage remains 797 behavior tests with 10,987 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing
  alternate-rootfile multi-spine behavior case.
- `EpubReaderTest` now proves imported alternate-rootfile body blocks keep
  page-level source provenance when an alternate OPF has multiple linear XHTML
  spine resources.
- The `ALT/viewports.opf` fixture imports `ALT/text/page-one.xhtml` and
  `ALT/text/page-two.xhtml` as a single native alternate-rootfile document div.
- Assertions prove page-one and page-two heading/paragraph blocks carry
  distinct `data-epub-rootfile`, `data-epub-spine-path`,
  `data-epub-spine-idref`, `data-epub-spine-index`, and
  `data-epub-body-block-index` values.
- WordPress block output now has explicit coverage for distinct paragraph
  provenance for both alternate XHTML pages.
- Dependency-closure checkpoint: this strengthens the current direct
  multi-rendition container-rootfile import path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,392 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,245 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,637 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 10,987 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Fallback Block Source Provenance Slice

- Focused PHP coverage remains 797 behavior tests with 10,964 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing
  alternate-rootfile fallback behavior case.
- `EpubReaderTest` now proves imported alternate-rootfile body blocks keep
  provenance when a linear opaque spine item falls through a manifest fallback
  chain to readable XHTML.
- The `ALT/fallback.opf` fixture uses an `application/x-demo-widget` spine item
  that falls back through SVG to `ALT/text/fallback.xhtml`.
- Assertions prove recovered alternate heading/paragraph blocks carry
  `data-epub-rootfile`, readable fallback spine path/idref/index, body block
  index, and original widget source idref/path.
- WordPress block output now has explicit coverage for those `data-epub-*`
  attributes on the recovered alternate fallback paragraph.
- Dependency-closure checkpoint: this strengthens the current direct
  multi-rendition container-rootfile import path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,369 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,245 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,614 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 10,964 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Imported Block Source Provenance Slice

- Focused PHP coverage remains 797 behavior tests with 10,948 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing
  alternate-rootfile behavior case.
- `EpubReader` now annotates each reconstructed alternate-rootfile body block
  with `data-epub-rootfile`, `data-epub-spine-path`,
  `data-epub-spine-idref`, `data-epub-spine-index`, and
  `data-epub-body-block-index`.
- If a readable XHTML body arrives through a manifest fallback chain, the
  imported block can also retain source fallback idref/path provenance without
  overwriting existing block attributes.
- Focused coverage extends the rootfile matrix fixture for `ALT/package.opf`
  and `MOBILE/package.opf`, proving imported child heading/paragraph nodes
  carry per-spine provenance.
- WordPress block output now renders the per-block `data-epub-*` attributes on
  alternate body paragraphs, so downstream block workflows can trace imported
  content back to the exact alternate OPF rootfile and spine resource.
- Dependency-closure checkpoint: this strengthens the current direct
  multi-rendition container-rootfile import path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,353 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,245 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,598 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 10,948 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Multi-Rendition Body Provenance Slice

- Focused PHP coverage remains 797 behavior tests with 10,933 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing
  alternate-rootfile behavior case.
- `EpubReader` now carries container rootfile `id` and raw `full-path`
  provenance onto imported alternate-rootfile native body divs as
  `data-epub-rootfile-id` and `data-epub-rootfile-full-path`.
- Imported alternate bodies already carried path, media type, title,
  identifier, language, and rootfile properties; this slice makes the native
  body block provenance complete enough to distinguish multiple secondary
  renditions such as print and mobile.
- Focused coverage expands the container rootfile matrix fixture from one
  alternate OPF to two alternate OPF rootfiles, `ALT/package.opf` and
  `MOBILE/package.opf`.
- Assertions prove `epubAlternateRootfilePackages` keeps both package
  summaries, both readable bodies import into the document as ordered
  `epub-alternate-rootfile` div children after the primary spine body, and
  WordPress block output retains per-rootfile id/full-path/property
  attributes.
- Dependency-closure checkpoint: this strengthens the current direct
  multi-rendition container-rootfile import path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current container-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,338 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,245 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,583 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 10,933 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Native Body Import Slice

- Focused PHP coverage remains 797 behavior tests with 10,908 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing
  alternate-rootfile behavior case.
- `EpubReader` now reconstructs alternate rootfile `epubBodyBlocks` into
  native document `div` blocks after the primary spine body instead of leaving
  alternate readable bodies only in metadata summaries.
- Each imported alternate body is wrapped in an `epub-alternate-rootfile` div
  with `data-epub-rootfile`, media type, title, identifier, language, and
  rootfile-property provenance attributes when those values are available.
- Existing `epubAlternateRootfilePackages` summaries are preserved, including
  `epubBodyAst`, `epubBodyBlocks`, `epubBodyBlockSources`, `epubBodyText`, and
  `epubWordPressBlocks`.
- Focused coverage strengthens the container rootfile matrix fixture: the
  source `ALT/package.opf` summary still exposes its alternate heading and
  paragraph metadata, while the imported document children now contain a
  provenance-wrapped alternate rootfile div whose WordPress block output keeps
  the same source attributes.
- Dependency-closure checkpoint: this closes the current direct alternate-body
  import gap for the structured alternate-rootfile path. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graph validation beyond the
  current alternate-rootfile path, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,313 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,245 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,558 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 10,908 assertions, 0 failures.

## 2026-06-23 EPUB3 Primary Package Scalar Metadata Alias Slice

- Focused PHP coverage is now 797 behavior tests with 10,896 full-suite
  assertions across the Pandoc lane. This slice adds one EPUB3 generated
  primary package writer behavior case.
- `EpubWriter` now treats unprefixed document metadata aliases as primary
  package metadata fallbacks for package root scalars, unique identifier IDs,
  Dublin Core records, modified timestamps, page progression direction, spine
  IDs, NCX inclusion/path/ID, NCX UID/depth/page counts, and NCX
  title/page-list labels.
- Primary package output still keeps option-first precedence, and generated
  alternate rootfiles still keep metadata-first scoped precedence.
- Focused coverage starts from `packageId`, `packageUniqueIdentifierId`,
  `packagePrefix`, `packageDirection`, `packageLanguage`, `modified`,
  `pageProgressionDirection`, `spineId`, `spineTocId`, `includeNcx`,
  `ncxPath`, `ncxUid`, `ncxDepth`, `ncxTotalPageCount`,
  `ncxMaxPageNumber`, `ncxDocTitle`, `ncxDocTitleLang`, `ncxDocAuthors`,
  `ncxPageListLabel`, `ncxPageListLabelLang`, `dublinCoreMetadata`, and
  `pageListEntries`.
- Assertions prove generated OPF and NCX output serialize those aliases and
  reader round-trip exposes canonical `epubPackage*`, `epubProperties`,
  `epubSpine*`, `epubTocResources`, and `epubNcx*` metadata.
- Dependency-closure checkpoint: this closes a bounded primary package
  scalar metadata authoring alias branch for EPUB3 package output. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, arbitrary multi-rendition package graphs beyond
  the current alternate-rootfile path, direct merge of all alternate bodies
  into the primary document children, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php` and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,245 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,546 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,896 assertions, 0 failures.

## 2026-06-23 EPUB3 Primary Nav Metadata Alias Slice

- Focused PHP coverage is now 796 behavior tests with 10,862 full-suite
  assertions across the Pandoc lane. This slice adds one EPUB3 generated
  primary navigation writer behavior case.
- `EpubWriter` now accepts primary-document unprefixed navigation metadata
  aliases for nav root/body attributes, TOC/landmark/page-list section
  attributes and titles, and TOC/landmark/page-list entries.
- This matches the structured alternate-rootfile metadata surface, where the
  same unprefixed aliases were already remapped before alternate OPF/nav
  generation.
- Focused coverage starts from `navRootAttributes`, `navBodyAttributes`,
  `tocNavAttributes`, `tocNavTitle`, `landmarkNavAttributes`,
  `landmarkNavTitle`, `pageListNavAttributes`, `pageListNavTitle`,
  `tocEntries`, `landmarkEntries`, `pageListEntries`, and
  `auxiliaryNavSections`.
- Assertions prove generated `nav.xhtml` serializes the alias data and reader
  round-trip exposes canonical `epubNav*`, `epubToc*`, `epubLandmark*`,
  `epubPageList*`, and `epubAuxiliaryNavSections` metadata.
- Dependency-closure checkpoint: this closes a bounded primary navigation
  authoring alias branch for EPUB3 package output. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graphs beyond the current
  alternate-rootfile path, direct merge of all alternate bodies into the
  primary document children, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php` and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,211 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,512 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,862 assertions, 0 failures.

## 2026-06-23 EPUB3 Writer OPF Package Version Normalization Slice

- Focused PHP coverage remains 795 behavior tests with 10,834 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  generated package writer round-trip behavior case.
- `EpubWriter` now normalizes EPUB 3 minor `packageVersion` metadata to OPF
  `version="3.0"` for generated primary and alternate rootfile packages.
- Focused coverage strengthens the structured `ALT/print.opf`
  alternate-rootfile generation case with `packageVersion` 3.3 on both the
  primary package options and alternate package metadata.
- Assertions prove the primary OPF serializes `version="3.0"`, primary reader
  metadata reports `epubPackageVersion` 3.0, alternate package metadata reports
  `epubPackageVersion` 3.0, and neither generated package emits
  `unsupported-package-version` diagnostics.
- Dependency-closure checkpoint: this closes a bounded writer-side OPF package
  version branch for EPUB3 minor metadata inputs. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graphs beyond the current
  alternate-rootfile path, direct merge of all alternate bodies into the
  primary document children, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php` and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,183 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,484 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,834 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Non-Linear Fallback Spine Resource Slice

- Focused PHP coverage remains 795 behavior tests with 10,830 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  generated alternate-rootfile writer round-trip behavior case.
- `EpubWriterTest` now proves generated alternate rootfiles keep explicit
  non-linear itemrefs for non-XHTML resources when their manifest fallback
  chain resolves to readable XHTML.
- The generated `ALT/nonlinear.opf` preserves the opaque widget itemref,
  manifest fallback and fallback-style attributes, packaged fallback
  SVG/XHTML/style resources, and non-linear itemref properties.
- Reader round-trip assertions prove `epubFallbackSpineResources` is exposed
  under `epubAlternateRootfilePackages` with the source widget path, terminal
  XHTML fallback idref/path, and media types while excluding non-linear
  fallback body text from the alternate readable body.
- Dependency-closure checkpoint: this closes a bounded generated
  alternate-rootfile branch for non-linear fallback spine resources. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition package graphs
  beyond the current alternate-rootfile path, direct merge of all alternate
  bodies into the primary document children, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/tests/EpubWriterTest.php` and `php -l
  lanes/pandoc/src/EpubWriter.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,179 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,480 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,830 assertions, 0 failures.

## 2026-06-23 EPUB Writer Non-Linear Fallback Spine Resource Slice

- Focused PHP coverage remains 795 behavior tests with 10,809 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  package/spine writer round-trip behavior case.
- `EpubWriter` now keeps explicit non-linear spine itemrefs for non-XHTML
  resources when their manifest fallback chain resolves to readable XHTML.
- The generated OPF preserves the opaque resource itemref, manifest fallback
  and fallback-style attributes, fallback-chain resources, and non-linear
  itemref properties.
- Reader round-trip assertions prove `epubFallbackSpineResources` records the
  source widget path, terminal XHTML fallback idref/path, and media types while
  keeping both non-linear resource content and fallback body text out of the
  imported linear body.
- Dependency-closure checkpoint: this closes a bounded writer-side round-trip
  branch for non-linear fallback spine resources. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graphs beyond the current
  alternate-rootfile path, direct merge of all alternate bodies into the
  primary document children, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php` and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,158 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,459 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,809 assertions, 0 failures.

## 2026-06-23 EPUB Non-Linear Fallback Spine Resource Provenance Slice

- Focused PHP coverage remains 795 behavior tests with 10,788 full-suite
  assertions across the Pandoc lane. This slice strengthens existing EPUB3
  package/spine metadata behavior cases.
- `EpubReader` now records `epubFallbackSpineResources` for non-linear
  foreign-resource itemrefs whose fallback chain resolves to readable XHTML.
- The provenance includes the source opaque spine item path, terminal fallback
  idref/path, and media types while still keeping non-linear fallback body
  content out of the imported document body.
- Alternate-rootfile summaries now merge non-linear fallback provenance with
  any linear readable-body fallback provenance instead of one source masking
  the other.
- Focused coverage strengthens the primary non-linear navigation fixture and
  the alternate rootfile resource summary fixture; assertions prove selected
  packages and alternate packages expose non-linear fallback provenance and
  still exclude auxiliary fallback text from body summaries.
- Dependency-closure checkpoint: this closes a bounded non-linear fallback
  spine provenance branch. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the current alternate-rootfile path,
  direct merge of all alternate bodies into the primary document children, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,301 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,438 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,788 assertions, 0 failures.

## 2026-06-23 EPUB Readable Fallback Non-Linear Navigation Target Slice

- Focused PHP coverage remains 795 behavior tests with 10,783 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  package/spine navigation diagnostics behavior case.
- `EpubReader` navigation reading-order maps now classify the readable XHTML
  terminal of a non-linear foreign-resource fallback chain as a non-linear
  spine target.
- This prevents landmark and page-list links to that fallback XHTML from being
  misclassified as links to a loose manifest resource outside the spine.
- Focused coverage strengthens the existing landmark/page-list fixture with a
  non-linear widget itemref, `fallback-appendix` XHTML, and nav links to the
  fallback document and its pagebreak fragment.
- Assertions prove `nav-landmark-target-non-linear-spine` and
  `nav-page-list-target-non-linear-spine` report `appendix-widget`, while the
  unrelated manifest-only loose page remains `missing-nav-page-list-spine-target`.
- Dependency-closure checkpoint: this closes a bounded readable-fallback
  non-linear navigation branch. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graphs beyond the current
  alternate-rootfile path, direct merge of all alternate bodies into the
  primary document children, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,296 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,433 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,783 assertions, 0 failures.

## 2026-06-23 EPUB Readable Fallback Encrypted Linear Spine Slice

- Focused PHP coverage remains 795 behavior tests with 10,770 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  OCF/package validation behavior case.
- `EpubReader` encrypted-resource diagnostics now expand linear spine fallback
  chains the same way body import and hyperlink membership do.
- This means an OPF spine itemref that points at an opaque foreign resource but
  falls back to readable XHTML is still treated as linear spine content when
  `META-INF/encryption.xml` references the fallback XHTML path.
- Focused coverage strengthens the OCF sidecar diagnostics fixture with a
  foreign widget itemref, `fallback-doc` XHTML, and a CipherReference for
  `OPS/fallback.xhtml`.
- Assertions prove `epubEncryptedResources` includes the fallback XHTML path,
  `encrypted-linear-spine-resource` reports `fallback-widget` with
  `application/xhtml+xml`, and the fallback body is still imported.
- Dependency-closure checkpoint: this closes a bounded readable-fallback
  encrypted linear spine branch. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graphs beyond the current
  alternate-rootfile path, direct merge of all alternate bodies into the
  primary document children, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,283 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,420 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,770 assertions, 0 failures.

## 2026-06-23 EPUB Readable Fallback Spine Hyperlink Target Slice

- Focused PHP coverage remains 795 behavior tests with 10,764 full-suite
  assertions across the Pandoc lane. This slice strengthens existing EPUB3
  package/spine diagnostics behavior cases.
- `EpubReader` recursive spine hyperlink diagnostics now treat the readable
  XHTML terminal of a foreign-resource fallback chain as represented by the
  OPF spine itemref.
- This aligns hyperlink membership checks with the actual body import path and
  with navigation reading-order handling: a spine itemref that points at an
  opaque widget but falls back to XHTML should not make links to that fallback
  XHTML look like links to an unspined content document.
- Focused coverage strengthens the primary spine hyperlink diagnostics fixture
  with a foreign widget itemref and `fallback-doc` XHTML, proving
  `fallback-doc` is not reported as `missing-spine-hyperlink-target` while the
  fallback body is imported.
- Alternate-rootfile fallback coverage now also proves a self-link inside
  `ALT/text/fallback.xhtml` remains a referenced resource without producing a
  fallback XHTML spine-membership diagnostic.
- Dependency-closure checkpoint: this closes a bounded readable-fallback
  hyperlink membership branch. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graphs beyond the current
  alternate-rootfile path, direct merge of all alternate bodies into the
  primary document children, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,277 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,414 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,764 assertions, 0 failures.

## 2026-06-23 EPUB Malformed Spine XHTML Diagnostics Slice

- Focused PHP coverage is now 795 behavior tests with 10,761 full-suite
  assertions across the Pandoc lane. This slice adds a reader-side EPUB3
  package/spine diagnostics behavior case.
- `EpubReader` now records `malformed-spine-xhtml` diagnostics for readable
  XHTML spine resources before best-effort XHTML switch normalization and body
  conversion.
- Primary package diagnostics include the malformed spine resource `idref` and
  package path, while alternate rootfile package diagnostics remain scoped
  under `epubAlternateRootfilePackages` with their own `idref` and path.
- Focused coverage builds a primary `OPS/package.opf` and alternate
  `ALT/malformed.opf` package with valid nav documents and malformed readable
  XHTML resources, proving both packages report the malformed resource while
  still exposing recoverable body text for import.
- Dependency-closure checkpoint: this closes a bounded malformed readable spine
  branch. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, arbitrary multi-rendition
  package graphs beyond the current alternate-rootfile path, direct merge of
  all alternate bodies into the primary document children, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,274 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,411 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,761 assertions, 0 failures.

## 2026-06-23 EPUB Fixed-Layout Page-Spread Center Slice

- Focused PHP coverage is now 794 behavior tests with 10,746 full-suite
  assertions across the Pandoc lane. This slice adds a generated EPUB3 writer
  behavior case and strengthens source/alternate fixed-layout coverage.
- `EpubReader` source EPUB diagnostics now prove `rendition:page-spread-center`
  is a valid spine itemref property, promotes `pageSpread` as `center`, and
  does not report the property as invalid.
- `EpubWriter` generated primary EPUB3 output now has focused coverage for
  metadata-provided `pageSpread: center`, emitting
  `rendition:page-spread-center` on the OPF itemref while preserving per-spine
  rendition layout refinement metadata.
- Generated alternate-rootfile fixed-layout output now also round-trips a
  centered alternate page spread through `ALT/fixed.opf` and
  `epubAlternateRootfilePackages`, so multi-rendition fixed-layout exports do
  not only cover left/right page placement.
- Dependency-closure checkpoint: this closes a bounded fixed-layout page
  placement branch. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the current alternate-rootfile path,
  direct merge of all alternate bodies into the primary document children, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/tests/EpubReaderTest.php` and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,259 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 1,137 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 4,396 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 10,746 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Body Block Source Provenance Slice

- Focused PHP coverage remains 793 behavior tests with 10,738 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  alternate-rootfile reader behavior case.
- `EpubReader` alternate package body summaries now preserve
  `epubBodyBlockSources` alongside `epubBodyAst` and `epubBodyBlocks`,
  recording each emitted block index, readable spine path, idref, and spine
  index. Fallback-readable spines also retain their original spine idref and
  path in the per-block source metadata.
- Focused coverage extends the `ALT/viewports.opf` aggregated spine viewport
  case, proving the page-one and page-two heading/paragraph blocks keep
  ordered provenance for `ALT/text/page-one.xhtml` and
  `ALT/text/page-two.xhtml` while the existing viewport, text, AST, and
  WordPress block projections remain available.
- Dependency-closure checkpoint: this makes alternate rendition bodies
  traceable back to their source spine resources for downstream import,
  reconciliation, and diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graphs beyond the current
  alternate-rootfile path, direct merge of all alternate bodies into the
  primary document children, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,256 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,388 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,738 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Structured Body AST Slice

- Focused PHP coverage remains 793 behavior tests with 10,733 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  alternate-rootfile reader behavior case.
- `EpubReader` alternate package body summaries now preserve structured
  AST-shaped records under `epubBodyAst` and `epubBodyBlocks`, in addition to
  the existing body block count, flattened body text, and WordPress block
  projection.
- Focused coverage extends the alternate container rootfile matrix test for
  `ALT/package.opf`, proving the readable alternate spine exposes a document
  record whose children match heading and paragraph block records, including
  block types, attributes, and child text nodes.
- Dependency-closure checkpoint: this makes alternate rendition bodies
  machine-readable from metadata without requiring downstream tools to reparse
  flattened body text or rendered WordPress markup. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graphs, direct merge of all
  alternate bodies into the primary document children, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,251 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,383 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,733 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Primary-Body Metadata Isolation Slice

- Focused PHP coverage is now 793 behavior tests with 10,722 full-suite
  assertions across the Pandoc lane. This slice adds a generated EPUB3
  alternate-rootfile behavior case.
- `EpubWriter` now scrubs primary package-scoped EPUB metadata before
  generating an alternate rootfile that explicitly reuses the primary document
  body via `usePrimaryDocument`.
- Focused coverage writes `ALT/reused.opf` from the primary AST body while the
  source document carries primary package id, Dublin Core records, metadata
  properties, metadata links, bindings, collections, guide references,
  rendition/media metadata, spine id, NCX metadata, and nav root attributes.
- Assertions prove the primary OPF keeps those primary package structures while
  the alternate OPF keeps its own package id, unique identifier, prefix,
  language/direction, `dc:identifier`, title, language, manifest, spine, and
  shared body content without inheriting the primary package metadata. Reader
  round-trip metadata preserves the alternate package identity and body summary
  under `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens structured multi-rendition OPF
  package isolation when alternate renditions reuse primary body content. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, arbitrary multi-rendition package
  graphs beyond the structured alternate-rootfile path, full alternate body
  import into the primary AST, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,132 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,372 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,722 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Identifier Option Precedence Slice

- Focused PHP coverage remains 792 behavior tests with 10,674 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  writer behavior case rather than adding a new named test.
- `EpubWriter` package identifier selection now respects scoped
  metadata-over-options mode: generated alternate rootfiles use alternate
  metadata for `dc:identifier`, rather than falling back to primary writer
  `identifier` options.
- Focused coverage extends structured `ALT/print.opf` output with a
  conflicting primary `identifier` option.
- Assertions prove the primary OPF still uses primary identifier metadata while
  the alternate OPF keeps `urn:uuid:structured-print-rendition` as its
  `book-id` `dc:identifier`, and reader round-trip metadata preserves that
  alternate identifier under `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens generated multi-rendition OPF
  package identity independence. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graphs beyond the structured
  alternate-rootfile path, full alternate body import into the primary AST,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,084 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,324 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,674 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Modified Timestamp Option Precedence Slice

- Focused PHP coverage remains 792 behavior tests with 10,673 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  writer behavior case rather than adding a new named test.
- `EpubWriter` package modified timestamp selection now respects scoped
  metadata-over-options mode: generated alternate rootfiles use explicit
  alternate `epubModified` metadata or an independent generated timestamp,
  rather than falling back to primary writer `modified` options.
- Focused coverage extends structured `ALT/resources.opf` output with a fixed
  primary `modified` option and no alternate modified metadata.
- Assertions prove the primary OPF keeps `2001-02-03T04:05:06Z` while the
  alternate OPF emits a different valid EPUB timestamp, and reader round-trip
  metadata preserves that independent alternate `dcterms:modified` value under
  `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens generated multi-rendition OPF
  package metadata independence. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graphs beyond the structured
  alternate-rootfile path, full alternate body import into the primary AST,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,083 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,323 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,673 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Resource Media-Type Option Precedence Slice

- Focused PHP coverage remains 792 behavior tests with 10,667 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  writer behavior case rather than adding a new named test.
- `EpubWriter` resource media-type selection now respects scoped
  metadata-over-options mode: generated alternate rootfiles start from
  alternate metadata only, so primary writer `resourceMediaTypes` options no
  longer type alternate package resources.
- Focused coverage extends structured `ALT/resources.opf` output with an
  untyped no-extension alternate resource and a conflicting primary
  `resourceMediaTypes` option for that same alternate path.
- Assertions prove the alternate OPF keeps `primary-option-css` as
  `application/octet-stream`, does not treat it as `text/css`, leaves its
  payload unrewritten, preserves explicitly typed alternate CSS/image
  resources and CSS URL rewriting, and round-trips alternate manifest media
  types under `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens generated multi-rendition OPF
  resource typing independence. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graphs beyond the structured
  alternate-rootfile path, full alternate body import into the primary AST,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,077 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,317 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,667 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Cover Option Precedence Slice

- Focused PHP coverage remains 792 behavior tests with 10,663 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  writer behavior case rather than adding a new named test.
- `EpubWriter` cover image selection now respects scoped metadata-over-options
  mode: generated alternate rootfiles choose `epubCoverImage` / `coverImage`
  metadata before any primary writer `coverImage` option, while primary OPF
  output keeps option-first behavior.
- Focused coverage extends structured `ALT/guide-cover.opf` output with a
  conflicting primary `coverImage` option pointing at another image inside the
  alternate package.
- Assertions prove the alternate OPF keeps `alt-cover` as the package cover
  and the only `cover-image` item, packages `primary-option-cover` as a normal
  manifest image, preserves guide references, and round-trips both resource
  payloads and manifest properties under `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens generated multi-rendition OPF
  cover independence. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the structured alternate-rootfile
  path, full alternate body import into the primary AST, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,073 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,313 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,663 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Scalar Option Precedence Slice

- Focused PHP coverage remains 792 behavior tests with 10,657 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  writer behavior case rather than adding a new named test.
- `EpubWriter` now uses metadata-only fallback while scoped alternate-rootfile
  metadata precedence is active for package/root scalar and generated XHTML
  sources.
- The scoped source selection covers package version, package id, unique
  identifier id, Dublin Core option records, package prefix, package
  direction/language, page progression direction, spine id, package
  rendition/media metadata, language fallback, chapter indexed strings,
  chapter direction, and generated XHTML viewport sources.
- Focused coverage extends structured `ALT/print.opf` output with conflicting
  primary `packageVersion`, `packageId`, `packageUniqueIdentifierId`,
  `packagePrefix`, `packageDirection`, `packageLanguage`,
  `pageProgressionDirection`, `spineId`, Dublin Core, rendition, media,
  viewport, and spine direction options.
- Assertions prove the primary OPF still honors those primary options while
  the generated alternate OPF keeps `version="3.0"`, `book-id`, its own
  identifier/title/language, no primary package scalars, no primary
  rendition/media package metadata, and no primary viewport or direction in
  alternate XHTML.
- Dependency-closure checkpoint: this tightens generated multi-rendition OPF
  and XHTML independence. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the structured alternate-rootfile
  path, full alternate body import into the primary AST, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,067 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,307 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,657 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile NCX Option Precedence Slice

- Focused PHP coverage remains 792 behavior tests with 10,635 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  writer behavior case rather than adding a new named test.
- `EpubWriter` NCX generation now uses metadata-only fallback while scoped
  alternate-rootfile metadata precedence is active.
- The scoped NCX source selection covers `includeNcx`, `ncxPath`,
  `spineTocId`, and `ncxId`, so primary writer options can still affect the
  primary OPF without leaking into generated alternate packages.
- Focused coverage extends structured `ALT/ncx.opf` output with conflicting
  primary `includeNcx`, `ncxPath`, and `ncxId` options. Assertions prove the
  primary OPF emits `primary-toc` at `OEBPS/primary-toc.ncx`, while the
  alternate OPF keeps `alt-toc` and `ALT/toc.ncx`.
- The same coverage verifies the alternate NCX keeps its own `navMap`,
  `pageTarget`, and `navList` metadata without `primary-toc` leakage.
- Dependency-closure checkpoint: this tightens generated multi-rendition NCX
  independence. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the structured alternate-rootfile
  path, full alternate body import into the primary AST, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,045 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,285 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,635 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Spine Manifest/ID Option Precedence Slice

- Focused PHP coverage remains 792 behavior tests with 10,631 full-suite
  assertions across the Pandoc lane. This slice strengthens existing EPUB3
  writer behavior cases rather than adding a new named test.
- `EpubWriter` `optionOrMetaValue()` now supports metadata-only fallback while
  scoped metadata precedence is active.
- Generated alternate rootfiles use that mode for list-structured package and
  spine sources, so absent alternate spine metadata falls back to generated
  alternate defaults instead of primary writer options.
- Focused coverage extends structured `ALT/split.opf` output with conflicting
  primary `spineManifestIds`, `spineManifestProperties`, and
  `spineManifestAttributes`. Assertions prove the primary OPF uses
  `primary-split-page` while the alternate OPF still generates
  `chapter-1` / `chapter-2` with its own `svg` / `scripted remote-resources`
  properties and `fallback` / `fallback-style` attributes.
- Focused coverage also extends structured `ALT/fixed.opf` output with
  conflicting primary `spineManifestIds`, `spineItemIds`, and
  `spineItemProperties`. Assertions prove the primary OPF uses
  `primary-fixed-page` / `primary-fixed-ref` while the alternate OPF keeps
  `chapter-1` / `chapter-2`, `alt-page-one-ref` / `alt-page-two-ref`, and its
  fixed-layout rendition itemref properties.
- Dependency-closure checkpoint: this tightens generated multi-rendition OPF
  independence. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the structured alternate-rootfile
  path, full alternate body import into the primary AST, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,041 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,281 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,631 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Package Structure Option Precedence Slice

- Focused PHP coverage remains 792 behavior tests with 10,625 full-suite
  assertions across the Pandoc lane. This slice strengthens existing EPUB3
  writer behavior cases rather than adding a new named test.
- `EpubWriter` now routes metadata properties, metadata links, package-link
  sidecar discovery, bindings, collections, and media overlays through scoped
  `optionOrMetaValue()` source selection.
- Generated alternate rootfile payloads therefore prefer alternate metadata
  over primary writer options for richer OPF package structures, while primary
  OPF output still honors primary options.
- Focused coverage extends structured `ALT/aliases.opf` output with
  deliberately conflicting primary `metadataProperties`, `metadataLinks`,
  `bindings`, and `collections`. Assertions prove the primary OPF emits those
  primary structures while the alternate OPF keeps its own `title-type` and
  schema accessibility metadata, ONIX metadata link, binding handler,
  collection links/sidecars, and linked payload extraction without primary
  metadata-link, property, binding, or collection leakage.
- Focused coverage also extends generated `ALT/overlay-generated.opf` output
  with a conflicting primary `mediaOverlays` option. Assertions prove the
  alternate OPF still emits `mo-alt-generated` and rejects
  `primary-option-overlay` leakage.
- Dependency-closure checkpoint: this tightens generated multi-rendition OPF
  independence. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the structured alternate-rootfile
  path, full alternate body import into the primary AST, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,035 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,275 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,625 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Modified Timestamp Slice

- Focused PHP coverage remains 792 behavior tests with 10,615 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  writer behavior case rather than adding a new named test.
- `EpubWriter` package OPF generation now computes `dcterms:modified` from
  scoped package metadata.
- Generated alternate rootfiles now map and preserve `modified`,
  `packageModified`, and `epubModified` metadata aliases, so an alternate OPF
  can carry its own required package-modified timestamp instead of inheriting
  the primary writer timestamp.
- Focused coverage extends structured `ALT/aliases.opf` output with a primary
  `epubModified` value of `2026-06-22T00:00:00Z` and an alternate `modified`
  value of `2026-06-23T01:02:03Z`. Assertions prove the primary OPF keeps the
  primary timestamp, the alternate OPF emits the alternate
  `dcterms:modified`, and reader round-trip metadata preserves the alternate
  timestamp under `epubProperties`.
- Dependency-closure checkpoint: this tightens generated multi-rendition OPF
  independence. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the structured alternate-rootfile
  path, full alternate body import into the primary AST, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,025 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,265 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,615 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Metadata Option Precedence Slice

- Focused PHP coverage remains 792 behavior tests with 10,612 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  writer behavior case rather than adding a new named test.
- `EpubWriter` now generates alternate rootfile payloads under a scoped
  metadata-over-options precedence mode.
- This prevents primary/global writer options from overwriting explicit
  alternate package metadata and resource media types while leaving primary
  package option behavior intact.
- The scoped precedence covers package-root string options, package unique
  identifiers, Dublin Core metadata records, NCX inclusion, spine rendition
  metadata, explicit non-linear spine resources, and resource media types.
- Focused coverage extends structured `ALT/aliases.opf` output with
  deliberately conflicting primary writer options for package id, unique
  identifier, package prefix, direction, language, page progression, Dublin
  Core records, and resource media types. Assertions prove the primary OPF
  uses the primary options while the alternate OPF keeps `alt-pub-id`,
  `alt-package`, schema prefix metadata, `rtl` / `pl` package metadata,
  structured Dublin Core records, package-link sidecars, and
  `application/xhtml+xml` handler media type without `primary-option` leakage.
- Dependency-closure checkpoint: this tightens generated multi-rendition OPF
  independence. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the structured alternate-rootfile
  path, full alternate body import into the primary AST, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,022 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,262 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,612 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Structured Metadata Identity Slice

- Focused PHP coverage remains 792 behavior tests with 10,604 full-suite
  assertions across the Pandoc lane. This slice strengthens an existing EPUB3
  writer behavior case rather than adding a new named test.
- `EpubWriter` generated alternate rootfiles now map
  `packageUniqueIdentifierId` to `epubPackageUniqueIdentifierId` and
  `dublinCoreMetadata` to `epubDublinCoreMetadata`.
- Direct `epubPackageUniqueIdentifierId` and `epubDublinCoreMetadata` entries
  are also preserved inside generated alternate-rootfile metadata.
- Focused coverage extends structured `ALT/aliases.opf` output with
  `unique-identifier="alt-pub-id"`, matching `dc:identifier`, `dc:title`,
  `dc:creator`, and `dc:language` records, OPF `file-as` / `role` attributes,
  `title-type` refinements, package-link sidecars, and reader round-trip
  assertions that the alternate package selects its own identifier and
  preserves structured Dublin Core metadata under
  `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens structured alternate OPF
  identity and metadata authoring. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graphs beyond the structured
  alternate-rootfile path, full alternate body import into the primary AST,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 1,014 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,254 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,604 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Explicit Non-Linear Spine Resource Slice

- Focused PHP coverage is now 792 behavior tests with 10,588 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` now accepts explicit non-linear spine resources from document
  metadata through `nonLinearSpineItems` and `epubNonLinearSpineItemRefs`, not
  only through global writer options or read-back `epubSpineItemRefs`.
- Generated alternate rootfiles now promote `nonLinearSpineItems` into
  alternate package metadata, so structured multi-rendition output can package
  extra XHTML resources as `linear="no"` itemrefs without forcing them into the
  linear reading body.
- Explicit non-linear entries now derive rendition spine properties such as
  `rendition:page-spread-left` from aliases like `pageSpread`, even when the
  entry does not carry a prebuilt `properties` list.
- Focused coverage writes `ALT/nonlinear.opf` from structured metadata with a
  generated linear chapter, a packaged `ALT/text/appendix.xhtml` resource, a
  non-linear itemref id, derived `rendition:page-spread-left` metadata, an EPUB
  landmark link, and reader round-trip assertions that the appendix is reported
  under `epubNonLinearResources` while its body text is excluded from the
  readable alternate body.
- Dependency-closure checkpoint: this tightens structured alternate-rendition
  spine authoring. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs beyond the structured alternate-rootfile path,
  full alternate body import into the primary AST, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 998 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,238 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,588 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile CSS Media-Type Rewrite Slice

- Focused PHP coverage remains 791 behavior tests with 10,572 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` generated alternate rootfile resource rewriting now receives
  alternate document metadata and uses `resourceMediaTypes()` when deciding
  whether resource payloads should be treated as CSS.
- This lets resources declared as `text/css` through `manifestResources` or
  `resourcePayloads` get CSS URL rewriting even when the ZIP path has no
  `.css` extension.
- Focused coverage extends generated `ALT/resources.opf` output with a
  no-extension `ALT/styles/theme` resource declared as `text/css`, a referenced
  `ALT/images/bg.png` image, OPF manifest media-type assertions, ZIP payload
  assertion that the package-path URL rewrites to `../images/bg.png`, and
  reader round-trip assertions that the alternate manifest media types survive
  under `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens structured alternate-rendition
  resource packaging and URL rewriting. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graphs beyond the structured
  alternate-rootfile path, full alternate body import into the primary AST, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 982 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,222 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,572 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Generated Media Overlay Slice

- Focused PHP coverage remains 791 behavior tests with 10,566 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` generated alternate rootfiles now accept `mediaDuration`,
  `mediaNarrator`, `mediaActiveClass`, `mediaPlaybackActiveClass`,
  `mediaOverlays`, and their `epub*` metadata forms.
- Generated alternate packages now synthesize SMIL overlay resources from
  overlay pairs inside the alternate package resource map instead of requiring
  a prebuilt alternate OPF/SMIL payload.
- Focused coverage writes `ALT/overlay-generated.opf` from structured
  metadata with alternate media package metadata, refined overlay
  duration/playback metadata, generated `ALT/overlays/mo-alt-generated.smil`,
  packaged audio, chapter `media-overlay` manifest linkage, and reader
  round-trip summaries under `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens structured multi-rendition
  media-overlay authoring for generated alternate rootfiles. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graphs beyond the
  structured alternate-rootfile path, full alternate body import into the
  primary AST, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 976 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,216 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,566 assertions, 0 failures.

## 2026-06-23 EPUB Alternate Rootfile Spine Manifest Attribute Alias Slice

- Focused PHP coverage remains 791 behavior tests with 10,533 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` generated alternate rootfiles now accept unprefixed
  `spineManifestAttributes` and map them to `epubSpineManifestAttributes`
  before building alternate OPF package item entries.
- Focused coverage extends the split `ALT/split.opf` alternate-rootfile
  generation case with fallback XHTML and fallback CSS resources, verifies
  `fallback` and `fallback-style` attributes are emitted on generated
  alternate chapter manifest items, verifies fallback resource items are
  packaged, and verifies reader round-trip `epubSpineItemRefs` preserve
  fallback metadata per alternate spine item.
- Dependency-closure checkpoint: this tightens structured multi-rendition
  authoring aliases for generated alternate rootfiles. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graphs, full alternate body
  import into the primary AST, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 943 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,183 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,533 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Spine Manifest Property Alias Slice

- Focused PHP coverage remains 791 behavior tests with 10,528 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` generated alternate rootfiles now accept unprefixed
  `spineManifestProperties` and map them to `epubSpineManifestProperties`
  before building alternate OPF package item entries.
- Focused coverage extends the split `ALT/split.opf` alternate-rootfile
  generation case with two generated spine chapters, verifies `chapter-1` and
  `chapter-2` OPF manifest properties are emitted from the unprefixed alias,
  and verifies reader round-trip `epubSpineItemRefs` preserve the explicit
  `manifestProperties` per alternate spine item.
- Dependency-closure checkpoint: this tightens structured multi-rendition
  authoring aliases for generated alternate rootfiles. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graphs, full alternate body
  import into the primary AST, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 938 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,178 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,528 assertions, 0 failures.

## 2026-06-22 EPUB Generated Remote Head Resource Property Slice

- Focused PHP coverage now reaches 791 behavior tests with 10,526 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` now infers the EPUB3 OPF `remote-resources` manifest property
  from generated XHTML head metadata, not only body HTML and head script
  `src` attributes.
- Focused coverage writes a primary EPUB whose only remote resources appear in
  a generated head `<base>`, head `<link href>`, `imagesrcset`, and inline
  CSS `@import`/`url(...)` references. The generated OPF marks the spine item
  with `remote-resources`, and read-back does not report missing
  remote-resource diagnostics.
- Structured alternate-rootfile output now also proves the same inference for
  `ALT/print.opf` when alternate head CSS references a remote asset; read-back
  preserves `remote-resources` on the alternate spine item metadata.
- Dependency-closure checkpoint: this tightens generated EPUB3 package
  validity for head-only remote resources across primary and alternate
  renditions. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs, full alternate body import into the primary
  AST, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 936 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,176 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,526 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Metadata Link Payload Slice

- Focused PHP coverage now reaches 790 behavior tests with 10,516 full-suite
  assertions across the Pandoc lane.
- `EpubReader` alternate package summaries now prove OPF metadata properties,
  Dublin Core creator refinements, metadata link records, standalone
  package-link resources, linked XML/JSON payload extraction, readable
  resources, and body text under `epubAlternateRootfilePackages`.
- Focused coverage builds `ALT/metadata.opf` with `dcterms:modified`
  metadata, `display-seq` refinements, schema accessibility metadata,
  `dc:creator` file-as/role attributes, OPF metadata link records,
  non-manifested linked XML and JSON metadata resources, and extracted payload
  assertions.
- Dependency-closure checkpoint: this tightens alternate-rendition OPF linked
  metadata intake and reviewer sidecar handoff. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graphs, full alternate body
  import into the primary AST, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,240 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,166 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,516 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Table/Style Conversion Slice

- Focused PHP coverage now reaches 789 behavior tests with 10,500 full-suite
  assertions across the Pandoc lane.
- `EpubReader` alternate package body summaries now prove table structure,
  table semantic metadata, linked head styles, inline CSS resource rewriting,
  package-scoped style/image resource references, extracted payloads, and
  generated WordPress table markup under `epubAlternateRootfilePackages`.
- Focused coverage builds `ALT/tables.opf` with a linear XHTML spine page
  containing a styled table, colgroup/col metadata, row/column header
  attributes, rowspan, cell background resources, a linked stylesheet, an
  inline style with `@import` and `url(...)` references, and packaged CSS/image
  payloads.
- Dependency-closure checkpoint: this tightens alternate-rendition table/style
  body conversion and reviewer resource handoff. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, arbitrary multi-rendition package graphs, full alternate body
  import into the primary AST, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,224 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,150 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,500 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Footnote Conversion Slice

- Focused PHP coverage now reaches 788 behavior tests with 10,469 full-suite
  assertions across the Pandoc lane.
- `EpubReader` alternate package body summaries now prove EPUB aside
  noteref/footnote conversion through the same native note path as primary
  EPUB bodies.
- Focused coverage builds `ALT/notes.opf` with a linear XHTML spine page
  containing an `epub:type="noteref"` link and an aside
  `epub:type="footnote"`, then verifies alternate readable resources, body
  text, generated WordPress footnotes, removal of duplicate aside wrappers and
  original backlinks from body markup, and semantic footnote metadata under
  `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens alternate-rendition XHTML body
  conversion and WordPress footnote handoff. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graphs, full alternate body import
  into the primary AST, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,193 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,119 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,469 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Fixed-Layout Spine Alias Slice

- Focused PHP coverage now reaches 787 behavior tests with 10,459 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` generated alternate rootfiles now accept unprefixed
  `spineItemRefs` for structured alternate renditions, promote unprefixed
  package-level rendition aliases, and use spine entry viewport metadata as an
  OPF `rendition:viewport` refinement when no explicit `renditionViewport` is
  supplied.
- Focused coverage writes `ALT/fixed.opf` from structured metadata with
  `pageProgressionDirection`, package-level fixed-layout rendition metadata,
  two split XHTML spine pages, itemref ids, page-spread/layout/orientation/
  spread/flow itemref properties, OPF viewport refinements, XHTML viewport
  metas, and reader round-trip summaries under
  `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens structured alternate-rendition
  fixed-layout authoring and readback parity. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graphs, full alternate body import
  into the primary AST, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 926 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,109 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,459 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Viewport Summary Slice

- Focused PHP coverage now reaches 786 behavior tests with 10,427 full-suite
  assertions across the Pandoc lane.
- `EpubReader` alternate package summaries now aggregate per-spine viewport
  metadata from `epubSpineItemRefs` into `epubViewports` and choose the first
  viewport as `epubViewport`, matching primary EPUB package summary behavior.
- Focused coverage builds `ALT/viewports.opf` with two linear XHTML spine
  pages and verifies the per-spine viewports, aggregate viewports, first
  viewport, readable resources, and recovered body text under
  `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens alternate-rendition viewport
  intake metadata. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs, full alternate body import into the primary
  AST, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,183 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,077 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,427 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Non-Linear And Image Resource Summary Slice

- Focused PHP coverage now reaches 785 behavior tests with 10,420 full-suite
  assertions across the Pandoc lane.
- `EpubReader` alternate package summaries now expose
  `epubNonLinearResources` for non-linear alternate spine items and
  `epubImageResources` for alternate OPF image manifest resources.
- The alternate summary keeps non-linear bodies out of readable body text while
  preserving readable resources, referenced resources, cover image, body text,
  and generated WordPress block markup under `epubAlternateRootfilePackages`.
- Focused coverage builds `ALT/resources.opf` with a linear chapter,
  non-linear appendix, figure image, and cover image, then verifies the
  non-linear body is not merged into readable body text while resources remain
  inspectable.
- Dependency-closure checkpoint: this tightens alternate-rendition spine and
  asset intake metadata. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs, full alternate body import into the primary
  AST, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,176 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,070 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,420 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Media Overlay Summary Slice

- Focused PHP coverage now reaches 784 behavior tests with 10,410 full-suite
  assertions across the Pandoc lane.
- `EpubReader` alternate package summaries now promote package-level media
  metadata and expose `epubMediaOverlayCount`, `epubMediaOverlays`, and
  `epubMediaOverlayResources` for alternate OPFs, matching the primary package
  summary shape.
- Alternate media overlay entries preserve content/overlay ids and paths,
  duration/narrator/active-class metadata, root/body SMIL attributes,
  sequences, pairs, text targets, audio targets, and spine/manifest
  `media-overlay` links under `epubAlternateRootfilePackages`.
- Focused coverage builds `ALT/overlay.opf` with
  `ALT/overlays/chapter.smil` and verifies media overlay metadata plus
  recovered alternate body text.
- Dependency-closure checkpoint: this tightens alternate-rendition media
  overlay intake and reviewer metadata. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graphs, full alternate body import
  into the primary AST, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,166 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,060 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,410 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Fallback Spine Resource Slice

- Focused PHP coverage now reaches 783 behavior tests with 10,388 full-suite
  assertions across the Pandoc lane.
- `EpubReader` alternate package body summaries now record
  `epubFallbackSpineResources` and `epubFallbackSpineResourceCount` when an
  alternate spine item falls through an unsupported manifest resource to a
  readable XHTML fallback.
- The alternate summary preserves the original spine `idref`, source path,
  source media type, resolved fallback idref/path/media type, recovered
  readable resources, referenced resources, body text, and generated WordPress
  block markup under `epubAlternateRootfilePackages`.
- Focused coverage builds `ALT/fallback.opf` with an
  `application/x-demo-widget` spine item that falls through SVG to XHTML, then
  verifies fallback metadata and recovered body output.
- Dependency-closure checkpoint: this tightens alternate-rendition fallback
  spine intake and reviewer metadata. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graphs, full alternate body import
  into the primary AST, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,144 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,038 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,388 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Rich XHTML Spine Metadata Slice

- Focused PHP coverage now reaches 782 behavior tests with 10,374 full-suite
  assertions across the Pandoc lane.
- `EpubReader` alternate package summaries now preserve rich XHTML spine
  metadata through `epubSpineItemRefs`, including viewport,
  language/direction, root/body attributes, head title/meta/link/style/script
  records, semantic element records, and local iframe resources.
- Alternate readable body conversion now uses local iframe resources for
  embedded spine content and merges XHTML image/hyperlink diagnostics into the
  alternate package diagnostics.
- Focused coverage builds `ALT/semantic.opf` and verifies rich XHTML metadata,
  local iframe body handoff, body text, and generated WordPress block markup
  under `epubAlternateRootfilePackages`.
- Dependency-closure checkpoint: this tightens alternate-rendition XHTML
  intake and reviewer metadata. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  arbitrary multi-rendition package graphs, full alternate body import into the
  primary AST, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,130 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,024 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,374 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Scoped Diagnostics Slice

- Focused PHP coverage now reaches 781 behavior tests with 10,350 full-suite
  assertions across the Pandoc lane.
- `EpubReader` alternate package summaries now run OPF package diagnostics,
  navigation TOC reading-order diagnostics, landmark/page-list target
  diagnostics, page-list ordering diagnostics, and NCX navigation diagnostics
  against each alternate rootfile package.
- Alternate OPF diagnostics stay scoped to the matching
  `epubAlternateRootfilePackages[...]` entry and expose `epubDiagnostics`,
  `epubDiagnosticCount`, `epubDiagnosticErrorCount`, and
  `epubDiagnosticWarningCount` without leaking alternate OPF or NCX paths into
  primary package diagnostics.
- Focused coverage builds a valid primary package plus a deliberately broken
  `ALT/broken.opf`, then proves `nav-toc-target-non-linear-spine` and
  `invalid-ncx-play-order` diagnostics are reported under the alternate
  package summary while primary diagnostics do not mention `ALT/broken.opf` or
  `ALT/toc.ncx`.
- Dependency-closure checkpoint: this tightens multi-rendition intake and
  scoped alternate-package validation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, arbitrary multi-rendition package graphs, full alternate body import
  into the primary AST, richer XHTML spine conversion, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,106 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 4,000 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,350 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rendition NCX Compatibility Slice

- Focused PHP coverage now reaches 780 behavior tests with 10,337 full-suite
  assertions across the Pandoc lane.
- Structured `epubAlternateRootfiles` / `alternateRootfiles` entries now accept
  unprefixed NCX/navigation aliases for generated alternate OPF packages,
  including `includeNcx`, `ncxPath`, `ncxId`, `ncxUid`, `ncxDepth`,
  `ncxTotalPageCount`, `ncxMaxPageNumber`, `ncxHeadMetadata`,
  `ncxDocTitle`, `ncxDocAuthorRecords`, `ncxPageListLabel`, `tocEntries`,
  `pageListEntries`, and `ncxNavLists`.
- Generated alternate OPFs now emit a scoped NCX resource, the OPF
  `application/x-dtbncx+xml` manifest item, and the alternate spine `toc`
  pointer while keeping the alternate NCX file out of the primary OPF
  manifest.
- `EpubReader` alternate package summaries now expose navigation resources and
  NCX metadata under `epubAlternateRootfilePackages`, including
  `epubTocResources`, promoted `epubNcx*` metadata, `epubTocEntries`,
  `epubPageListEntries`, and `epubNcxNavLists`.
- Focused coverage writes `ALT/ncx.opf` with `ALT/toc.ncx`, verifies OPF
  manifest/spine NCX wiring, NCX head/doc title/doc author/navMap/pageList/navList
  output, primary OPF isolation, and reader round-trip alternate-package NCX
  summaries.
- Dependency-closure checkpoint: this tightens generated alternate-rendition
  NCX compatibility and alternate package intake summaries. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graphs, full
  alternate body import into the primary AST, richer XHTML spine conversion,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 894 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,987 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,337 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rendition Guide Cover Metadata Slice

- Focused PHP coverage now reaches 779 behavior tests with 10,304 full-suite
  assertions across the Pandoc lane.
- Structured `epubAlternateRootfiles` / `alternateRootfiles` entries now accept
  unprefixed `coverImage` and `guideReferences` aliases for generated alternate
  OPF packages.
- Generated alternate OPFs now use those aliases to emit a `cover-image`
  manifest property, legacy `<guide><reference/>` entries, and OPF cover
  metadata while keeping alternate cover resources scoped to the alternate
  package instead of leaking into the primary OPF manifest.
- `EpubReader` alternate package summaries now expose alternate-package
  `epubCoverImage` and `epubGuideReferences`, and the extraction-enabled
  summary includes the alternate cover payload under `epubResourcePayloads`.
- Focused coverage writes `ALT/guide-cover.opf` with an alternate cover image
  and guide references, verifies OPF manifest/guide/cover metadata and primary
  OPF isolation, then reads the EPUB back to prove
  `epubAlternateRootfilePackages` reports the cover path, guide references,
  cover-image manifest property, and extracted cover bytes.
- Dependency-closure checkpoint: this tightens generated alternate-rendition
  guide/cover package metadata and alternate package intake summaries. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition package graphs,
  full alternate body import into the primary AST, richer XHTML spine
  conversion, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 861 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,954 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,304 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rendition Package Metadata Alias Slice

- Focused PHP coverage now reaches 778 behavior tests with 10,294 full-suite
  assertions across the Pandoc lane.
- Structured `epubAlternateRootfiles` / `alternateRootfiles` entries now accept
  the same unprefixed package metadata authoring aliases used by the primary
  writer path for generated alternate OPF packages.
- Alternate rootfile entries now normalize `metadataProperties`,
  `metadataLinks`, `metaProperties`, `metaLinks`, `bindings`, `collections`,
  `packageId`, `packagePrefix`, `packageVersion`, `packageDirection`,
  `packageLanguage`, `pageProgressionDirection`, `spineId`, `spineTocId`, and
  `ncxId` into the corresponding EPUB metadata keys before generating the
  alternate package.
- `EpubReader` alternate package summaries now expose bindings, collections,
  package-link resource metadata, asset resource paths, and extracted
  package-link sidecar payloads when resource extraction is enabled.
- Focused coverage writes `ALT/aliases.opf` with package attributes, metadata
  properties, OPF package metadata links, bindings, collections, collection
  metadata links, and XML/JSON/MP3 sidecars. It verifies the alternate OPF
  output, verifies sidecars are packaged without leaking into the primary OPF
  manifest, then reads the EPUB back to prove `epubAlternateRootfilePackages`
  reports the package attributes, metadata links, bindings, collections,
  package-link resources, and extracted sidecar bytes.
- Dependency-closure checkpoint: this tightens generated alternate-rendition
  package metadata and alternate package intake summaries. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graphs, full
  alternate body import into the primary AST, richer XHTML spine conversion,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 851 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,944 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,294 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rendition Resource Manifest Metadata Slice

- Focused PHP coverage now reaches 777 behavior tests with 10,251 full-suite
  assertions across the Pandoc lane.
- Structured `epubAlternateRootfiles` / `alternateRootfiles` entries can now
  carry entry-level resource manifest metadata for generated alternate OPF
  packages instead of relying only on global writer resource options.
- Alternate rootfile entries now normalize `manifestResources` to
  `epubManifestResources` and `resourcePayloads` to `epubResourcePayloads`,
  and can pass `resourceMediaTypes` / `epubResourceMediaTypes` into the
  generated alternate package.
- The writer now derives resource media types from `epubManifestResources` as
  well as payload metadata, so generated alternate OPFs preserve explicit
  manifest item ids, media types, properties, fallbacks, and resource bytes.
- Focused coverage writes `ALT/resources.opf` with SVG and XHTML fallback
  resources, verifies the alternate OPF manifest emits `image/svg+xml`,
  `application/xhtml+xml`, `properties="svg scripted"`, and
  `fallback="fallback-doc"`, then reads the EPUB back to prove
  `epubAlternateRootfilePackages` reports the manifest resources with their
  media type, properties, and fallback metadata.
- Dependency-closure checkpoint: this tightens generated alternate-rendition
  resource manifests. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, arbitrary
  multi-rendition package graphs, full alternate body import into the primary
  AST, richer XHTML spine conversion, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 808 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,901 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,251 assertions, 0 failures.

## 2026-06-22 EPUB Split Alternate Rendition Generation Slice

- Focused PHP coverage now reaches 776 behavior tests with 10,239 full-suite
  assertions across the Pandoc lane.
- Structured `epubAlternateRootfiles` / `alternateRootfiles` entries can now
  provide their own `splitLevel`, letting generated alternate renditions split
  their body into multiple linear XHTML spine chapters independently of the
  primary rendition.
- The writer now reuses a parameterized chapter-splitting helper for alternate
  packages, then emits the alternate OPF manifest, ordered spine itemrefs, nav
  links, and chapter XHTML payloads for each split chapter while preserving the
  primary OPF manifest isolation.
- Focused coverage writes `ALT/split.opf`, `ALT/split-nav.xhtml`,
  `ALT/text/part.xhtml`, and `ALT/text/part-2.xhtml`, verifies OPF manifest and
  spine order, nav fragment targets, and both XHTML chapter payloads, then
  reads the EPUB back to prove `epubAlternateRootfilePackages` reports two
  readable resources, four body blocks, normalized combined body text, and
  generated WordPress block markup from the split alternate spine.
- Dependency-closure checkpoint: this broadens generated multi-rendition output
  from one alternate body to split alternate spine chapters. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, arbitrary multi-rendition package graphs, full
  alternate body import into the primary AST, richer XHTML spine conversion,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 796 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,889 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,239 assertions, 0 failures.

## 2026-06-22 EPUB Structured Alternate Rendition Generation Slice

- Focused PHP coverage now reaches 775 behavior tests with 10,221 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` now accepts structured `epubAlternateRootfiles` /
  `alternateRootfiles` entries and can generate an alternate EPUB rootfile
  package instead of only preserving or authoring raw OPF payload bytes.
- Generated alternate renditions include their own OPF package document, nav
  XHTML, linear spine XHTML body, and local resource payloads. The primary OPF
  manifest stays clean while `META-INF/container.xml` advertises the alternate
  rootfile with id/media-type/properties metadata.
- Focused coverage writes `ALT/print.opf`, `ALT/nav.xhtml`,
  `ALT/text/print.xhtml`, and `ALT/styles/print.css` from structured metadata,
  verifies OPF manifest/spine/nav/chapter/resource bytes, and reads the EPUB
  back to prove `epubAlternateRootfilePackages` reports the generated title,
  language, manifest count, spine count, readable resource path, body text,
  WordPress block markup, and referenced stylesheet.
- Dependency-closure checkpoint: this moves multi-rendition output beyond raw
  OPF payload preservation, but remains a bounded generated-rendition path. It
  does not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, arbitrary multi-rendition package graphs,
  full alternate body import into the primary AST, richer XHTML spine
  conversion, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 778 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,871 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,221 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rendition Body Summary Slice

- Focused PHP coverage remains at 774 behavior tests and now reaches 10,197
  full-suite assertions across the Pandoc lane.
- `EpubReader` now parses readable linear spine content from alternate OPF
  rootfiles referenced by `META-INF/container.xml` and records that content
  inside each `epubAlternateRootfilePackages` entry instead of only exposing
  package-level metadata.
- Alternate packages can now report `epubReadableResources`,
  `epubBodyBlockCount`, `epubBodyText`, `epubWordPressBlocks`, and referenced
  local resources while keeping the selected OPF as the only source for the
  primary document body.
- Focused coverage extends the container-rootfile matrix fixture with an
  alternate `ALT/package.opf` and `ALT/print.xhtml`, then verifies the readable
  resource path, two imported body blocks, normalized body text, and generated
  WordPress paragraph markup under the alternate package summary.
- Dependency-closure checkpoint: this is a review/intake summary for alternate
  rendition bodies. It does not claim full alternate rendition body import into
  the main AST, full structured multi-rendition OPF generation from ASTs, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, richer XHTML spine conversion, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 3,847 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 10,197 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Package Summary Slice

- Focused PHP coverage now reaches 774 behavior tests with 10,188 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now parses existing alternate OPF rootfiles referenced by
  `META-INF/container.xml` into `epubAlternateRootfilePackages`, exposing
  rootfile identity, package attributes, Dublin Core metadata, metadata
  properties/links, manifest resources, and spine item summaries without
  importing alternate renditions into the primary document body.
- This makes multi-rendition intake inspectable beyond raw payload preservation:
  import/export callers can see alternate rendition title/language metadata,
  manifest resource counts, spine counts, and resource paths while keeping the
  selected OPF as the source for the main AST.
- Focused coverage reads both a preserved imported alternate OPF and an
  authored `ALT/print.opf` alternate OPF back from generated EPUB3 output, then
  verifies `epubAlternateRootfilePackageCount`, title/language metadata,
  manifest counts, spine counts, and preserved raw payload bytes.
- Dependency-closure checkpoint: this is limited to metadata summaries for
  alternate OPF rootfiles. It does not claim alternate rendition body import,
  full structured multi-rendition OPF generation from ASTs, full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, richer XHTML spine conversion, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 754 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,838 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,188 assertions, 0 failures.

## 2026-06-22 EPUB Alternate Rootfile Authoring Slice

- Focused PHP coverage now reaches 773 behavior tests with 10,179 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` can now author alternate EPUB container rootfiles from
  writer-provided `containerRootfilePayloads` entries, not only preserve
  alternate OPF payloads previously extracted from an imported EPUB.
- Alternate rootfile payloads accept preserved internal bytes, authored XML or
  content strings, and the existing base64 extraction shape. Generated
  `META-INF/container.xml` advertises the alternate OPF rootfile with id,
  media type, and properties while the primary OPF manifest stays clean.
- Focused coverage writes a generated EPUB3 package with an authored
  `ALT/print.opf` payload, verifies both rootfiles in `container.xml`, verifies
  the alternate OPF bytes in the ZIP, verifies no bogus primary-manifest entry,
  and reads the generated package back with extraction enabled.
- Dependency-closure checkpoint: this is limited to native PHP alternate OPF
  rootfile payload authoring. It does not claim full multi-rendition OPF
  generation from structured ASTs, full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, richer XHTML spine
  conversion, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 745 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,829 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,179 assertions, 0 failures.

## 2026-06-22 EPUB OCF Container-Link Payload Preservation Slice

- Focused PHP coverage now reaches 772 behavior tests with 10,171 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now extracts valid local OCF container-link targets under
  `META-INF/` into `epubOcfSidecarPayloads` when resource extraction is
  enabled, preserving sidecar bytes for catalog/ONIX-style records referenced
  by `META-INF/container.xml`.
- Extraction remains bounded and conservative: remote URLs, unsafe local href
  shapes, `META-INF/container.xml`, known OCF sidecar duplicates, selected and
  alternate rootfiles, missing entries, and entries over the existing resource
  byte cap are not imported as container-link sidecar payloads.
- `EpubWriter` now writes those arbitrary `META-INF` OCF sidecar payloads back
  through the sidecar channel and skips duplicate writes from resource/rootfile
  payload paths, so the regenerated OPF manifest does not grow bogus sidecar
  entries.
- Focused coverage imports a `container.xml` link to
  `metadata/catalog.json`, verifies the extracted JSON sidecar payload metadata,
  writes EPUB3, verifies regenerated `container.xml` and
  `META-INF/metadata/catalog.json`, and reads the generated EPUB back with
  extraction enabled.
- Dependency-closure checkpoint: this is limited to native PHP OCF
  container-link payload preservation during EPUB read/write. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, richer XHTML spine conversion, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 737 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,821 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,171 assertions, 0 failures.

## 2026-06-22 EPUB OCF Container-Link Output Media-Type Inference Slice

- Focused PHP coverage remains 771 behavior tests with 10,157 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` now derives `media-type` attributes for regenerated
  `META-INF/container.xml` `<link/>` entries when caller/import metadata omits
  the field and the local href has a recognized extension.
- Explicit container-link media types still win, remote/fragment-only hrefs
  remain unmodified, and unknown local extensions are not forced to
  `application/octet-stream` in the optional OCF link attribute.
- Focused coverage removes `media-type` from the imported OCF metadata sidecar
  link in the sidecar preservation round-trip, verifies regenerated
  `container.xml` emits `media-type="application/xml"`, verifies read-back
  metadata carries `application/xml`, and preserves OCF sidecar payload bytes.
- Dependency-closure checkpoint: this is limited to native PHP OCF
  container-link media-type inference during EPUB output. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, richer XHTML spine conversion, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 723 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,807 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,157 assertions, 0 failures.

## 2026-06-22 EPUB Package-Link Output Media-Type Inference Slice

- Focused PHP coverage remains 771 behavior tests with 10,156 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` now derives OPF `<link media-type="..."/>` values for local
  package metadata links and collection metadata record/voicing links when
  caller metadata omits `mediaType` / `media-type`.
- The writer keeps explicit media types authoritative, infers from the local
  href path after stripping query/fragment suffixes, and avoids adding
  unnecessary media types to ordinary direct collection member links whose
  relation does not require one.
- Focused coverage now removes explicit media types from a top-level JSON
  metadata link, a collection XML record link, and a collection MP3 voicing
  link, then verifies generated OPF media-type attributes, round-trip metadata
  values, preserved sidecar payload bytes, and absence of generated
  `missing-package-link-media-type` diagnostics.
- Dependency-closure checkpoint: this is limited to native PHP package-link
  media-type inference during EPUB output. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, richer XHTML spine conversion, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 722 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,806 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,156 assertions, 0 failures.

## 2026-06-22 EPUB Collection Package-Link Missing-Media Extraction Fallback Slice

- Focused PHP coverage remains 771 behavior tests with 10,154 full-suite
  assertions across the Pandoc lane.
- `EpubReader` collection package-link extraction now has explicit regression
  coverage for local linked metadata records whose OPF `<link/>` omits
  `media-type`.
- The focused regression extends `records missing epub3 package link resources
  without flagging remote links` with a collection-level JSON record link
  whose `media-type` is intentionally omitted, verifies the
  `missing-package-link-media-type` diagnostic with collection context,
  verifies `epubPackageLinkResources` infers `application/json`, and verifies
  the extracted sidecar bytes are preserved in `epubResourcePayloads`.
- Dependency-closure checkpoint: this is limited to native PHP package-link
  extraction coverage for collection metadata resources. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, richer XHTML spine conversion, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/tests/EpubReaderTest.php`; `php
  tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php` passed 1 file,
  3,084 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 3,804 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 10,154 assertions, 0 failures.

## 2026-06-22 EPUB Spine/Fallback Missing-Media Metadata Fallback Slice

- Focused PHP coverage remains 771 behavior tests with 10,146 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now applies the shared extension fallback when exposing
  `epubSpineItemRefs` media types and manifest-fallback metadata, while
  keeping the source OPF strict enough to report
  `missing-manifest-media-type` diagnostics.
- The focused regression omits `media-type` from an SVG manifest fallback
  item, verifies the expected missing-media diagnostic for `fallback-svg`,
  verifies the spine item fallback metadata still exposes `image/svg+xml`,
  and verifies the terminal XHTML fallback-spine resource remains
  `application/xhtml+xml`.
- Dependency-closure checkpoint: this is limited to native PHP spine and
  manifest-fallback media-type metadata during EPUB intake. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, richer XHTML spine conversion, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,076 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,796 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,146 assertions, 0 failures.

## 2026-06-22 EPUB Manifest Missing-Media Extraction Fallback Slice

- Focused PHP coverage remains 771 behavior tests with 10,144 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now applies the shared extension fallback when exposing
  manifest resources through `epubManifestResources` and opt-in extracted
  `epubResourcePayloads`, while leaving the original OPF declaration strict
  enough to keep `missing-manifest-media-type` diagnostics.
- The focused regression adds manifest resources for a JSON catalog sidecar and
  a generic XML/ONIX sidecar whose `<item/>` declarations intentionally omit
  `media-type`, then verifies both missing-media diagnostics, inferred
  manifest metadata, extracted payload media types, ZIP payload bytes, and
  regenerated writer manifest entries.
- Dependency-closure checkpoint: this is limited to native PHP manifest
  resource media-type fallback during EPUB intake/extraction and regeneration
  metadata handoff. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, richer XHTML spine
  conversion, generated multi-rendition authoring beyond preserved OPF
  payloads, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,074 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,794 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,144 assertions, 0 failures.

## 2026-06-22 EPUB Manifest Resource Media-Type Fallback Slice

- Focused PHP coverage remains 771 behavior tests with 10,131 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` now derives `application/json` for `.json` resources and
  `application/xml` for generic `.xml` resources when generated OPF manifest
  entries have no explicit `resourceMediaTypes` or payload `mediaType`
  override.
- This keeps ordinary package sidecars such as catalog JSON and ONIX/XML
  records from degrading to `application/octet-stream` during EPUB
  regeneration.
- Focused coverage extends `writes optional epub3 resource payloads into the
  package manifest` with JSON and XML resources supplied only through writer
  `resources`, verifies the payload bytes in the ZIP, verifies the generated
  OPF `<item/>` media types, and round-trips the inferred media types through
  `epubManifestResources`.
- Dependency-closure checkpoint: this is limited to native PHP manifest
  resource media-type fallback during EPUB output. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, richer XHTML spine conversion, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 720 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,781 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,131 assertions, 0 failures.

## 2026-06-22 EPUB Package-Link Extracted Media-Type Fallback Slice

- Focused PHP coverage remains 771 behavior tests with 10,125 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now derives a useful media type for existing unmanifested OPF
  package-link resources when the source `<link/>` omits `media-type`, instead
  of always reporting extracted payload metadata as `application/octet-stream`.
- Missing OPF package-link `media-type` diagnostics remain intact; the fallback
  affects only the `epubPackageLinkResources` / opt-in `epubResourcePayloads`
  metadata for preserved archive bytes.
- The fallback covers common EPUB package sidecar/resource extensions including
  XML, JSON, XHTML, CSS, JavaScript, image, audio, video, SMIL, NCX, VTT, and
  font payloads, while unknown extensions still use
  `application/octet-stream`.
- Focused coverage extends `records missing epub3 package link resources
  without flagging remote links` with an unmanifested JSON metadata sidecar
  whose OPF link intentionally omits `media-type`, asserts the expected
  missing-media-type diagnostic, preserves the payload bytes, and records
  `application/json` on the extracted package-link resource.
- Dependency-closure checkpoint: this is limited to native PHP package-link
  payload media-type fallback during extraction. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, richer XHTML spine conversion, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,061 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,775 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,125 assertions, 0 failures.

## 2026-06-22 EPUB Metadata-Link Standalone Payload Slice

- Focused PHP coverage remains 771 behavior tests with 10,116 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` now treats top-level package metadata links and collection
  metadata links as standalone package-link payload candidates for local
  non-content resources beyond only `record` and `voicing` relations.
- Direct collection member links keep their narrower standalone behavior, and
  EPUB content document media types remain manifestable so readable XHTML/SVG
  content is not removed from generated `<manifest>` output.
- The writer still writes standalone metadata-link payload bytes into the ZIP
  while omitting invalid duplicate manifest entries; read-back with resource
  extraction preserves those bytes through `epubResourcePayloads`.
- Focused coverage extends `writes epub3 metadata properties refinements and
  links` with an `alternate` `application/json` package metadata link, asserts
  the JSON payload is packaged but not manifested, round-trips the OPF link
  metadata, extracts the payload bytes, and avoids self-generated
  `invalid-package-link-manifest-resource` diagnostics.
- Dependency-closure checkpoint: this is limited to native PHP metadata-link
  standalone payload preservation and manifest omission. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, richer XHTML spine conversion, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 714 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 3,766 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  10,116 assertions, 0 failures.

## 2026-06-22 EPUB Package Link Payload Preservation Slice

- Focused PHP coverage remains 771 behavior tests with 10,108 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records existing unmanifested OPF package-link resources
  as `epubPackageLinkResources` and includes them in opt-in
  `epubResourcePayloads` extraction, so valid linked metadata records can be
  regenerated without requiring invalid manifest entries.
- Package-link resource extraction deduplicates paths, ignores package
  document self-references, skips already-manifested resources, and only keeps
  resources that actually exist in the EPUB archive.
- `EpubWriter` now omits standalone local `record` and `voicing` package-link
  payloads from generated `<manifest>` output while still writing the payload
  bytes into the ZIP and preserving OPF `<link/>` metadata.
- The writer rule applies to top-level package metadata links, direct
  collection links, and collection metadata links recursively; EPUB content
  document media types remain manifestable.
- Focused coverage verifies unmanifested package-link XML payload extraction,
  top-level metadata-link payload preservation, collection metadata `record`
  and `voicing` payload preservation, and absence of self-generated
  `invalid-package-link-manifest-resource` diagnostics.
- Dependency-closure checkpoint: this is limited to native PHP package-link
  payload preservation and manifest omission for standalone linked resources.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, richer XHTML spine conversion,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,052 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 1 file, 706 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,921
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,108 assertions, 0 failures.

## 2026-06-22 EPUB Collection Metadata Link Import/Export Preservation Slice

- Focused PHP coverage remains 771 behavior tests with 10,093 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now imports OPF collection metadata links from
  `<collection><metadata><link .../></metadata></collection>` as structured
  collection metadata records instead of dropping empty link elements.
- Imported collection metadata links preserve package-relative `href` values
  including fragments, `rel`, `hreflang`, `media-type`, `refines`, `id`,
  `properties`, `xml:lang`, and `dir`.
- `EpubWriter` now emits metadata records with `href` or `name: link` back as
  OPF `<link/>` elements inside collection metadata, reusing the package
  relative href and attribute handling used by direct collection links.
- Focused coverage extends the EPUB writer package attributes/bindings/
  collections fixture with collection metadata `record` and `voicing` links,
  linked payload resources, generated OPF assertions, and read-back assertions.
- Dependency-closure checkpoint: this is limited to native PHP collection
  metadata link import/export preservation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, richer XHTML spine conversion, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 697 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,906
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,093 assertions, 0 failures.

## 2026-06-22 EPUB Collection Metadata Link Relation/Refines Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,080 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now applies the OPF metadata link `record` / `voicing`
  relation/refines contract to links inside collection metadata as well as
  top-level package metadata.
- Collection metadata links with `rel="record"` now report
  `invalid-package-link-record-refines` when they carry a `refines` attribute,
  with collection-aware diagnostic context.
- Collection metadata links with `rel="voicing"` now report
  `missing-package-link-voicing-refines` when they omit `refines`, again
  preserving the collection id in the diagnostic context.
- Direct collection member links are still governed by the existing
  collection-link rules; this slice only changes OPF `link` elements whose
  parent is an OPF `metadata` element.
- Focused coverage extends the package-link attribute fixture with
  collection-metadata `record` and `voicing` cases while keeping readable
  spine import intact.
- Dependency-closure checkpoint: this is limited to native PHP OPF metadata
  link relation/refines diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, richer XHTML spine conversion, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,046 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,893
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,080 assertions, 0 failures.

## 2026-06-22 EPUB SVG Href Resource-Element Remote Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,069 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now scans a broader set of SVG href-bearing resource elements
  during required manifest property checks: `altGlyph`, `clipPath`,
  `color-profile`, `cursor`, `feImage`, `filter`, `font-face-uri`,
  `glyphRef`, `image`, `linearGradient`, `marker`, `mask`, `mpath`,
  `pattern`, `radialGradient`, `script`, `textPath`, `tref`, and `use`.
- `image/svg+xml` manifest items now report
  `missing-manifest-required-property` for `remote-resources` when SVG cursor
  or paint/template definitions point at remote resources through `href` or
  `xlink:href`.
- The scanner remains scoped away from hyperlink-only SVG `<a href>` elements
  so ordinary external links do not become publication-resource diagnostics.
- Package-relative SVG href detection uses the same element coverage, keeping
  remote-base/resource-reference checks aligned for these SVG resource sources.
- Focused coverage extends the required manifest property fixture with remote
  SVG cursor and `linearGradient` resources and keeps readable spine import
  intact.
- Dependency-closure checkpoint: this is limited to native PHP SVG href
  extraction for required EPUB manifest properties. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser SVG rendering behavior, live remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,035 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,882
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,069 assertions, 0 failures.

## 2026-06-22 EPUB SVG font-face-uri Remote-Resource Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,061 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now scans SVG `font-face-uri` elements with `href` /
  `xlink:href` alongside SVG `image`, `use`, `feImage`, and `script`
  href-bearing resource elements during required manifest property checks.
- `image/svg+xml` manifest items now report
  `missing-manifest-required-property` for `remote-resources` when an SVG font
  face declaration points at a remote font resource.
- Package-relative SVG href detection uses the same element coverage, keeping
  remote-base/resource-reference checks aligned for SVG font face sources.
- Focused coverage extends the required manifest property fixture with an SVG
  font resource using remote `font-face-uri` content and keeps readable spine
  import intact.
- Dependency-closure checkpoint: this is limited to native PHP SVG
  `font-face-uri` href extraction for required EPUB manifest properties. It
  does not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, browser SVG font loading behavior, live
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,027 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,874
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,061 assertions, 0 failures.

## 2026-06-22 EPUB SVG Script Remote-Resource Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,057 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now scans SVG `script` elements with `href` / `xlink:href`
  alongside SVG `image`, `use`, and `feImage` href-bearing resource elements
  during required manifest property checks.
- `image/svg+xml` manifest items now report
  `missing-manifest-required-property` for `remote-resources` when an SVG
  script element loads a remote script resource, even when the manifest already
  declares the separate `scripted` property.
- Package-relative SVG href detection uses the same element coverage, keeping
  remote-base/resource-reference checks aligned for SVG external script
  sources.
- Focused coverage extends the required manifest property fixture with an SVG
  script resource using a remote script href and an explicit `scripted`
  declaration, so the new diagnostic proves the missing `remote-resources`
  property specifically.
- Dependency-closure checkpoint: this is limited to native PHP SVG script href
  extraction for required EPUB manifest properties. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser SVG/script execution behavior, live remote-resource
  fetching, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,023 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,870
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,057 assertions, 0 failures.

## 2026-06-22 EPUB SVG feImage Remote-Resource Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,053 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now scans SVG filter `feImage` elements alongside `image` and
  `use` elements during required manifest property checks.
- `image/svg+xml` manifest items now report
  `missing-manifest-required-property` for `remote-resources` when an SVG
  filter image points at a remote resource through `href` or `xlink:href`.
- Package-relative SVG href detection uses the same element coverage, keeping
  remote-base/resource-reference checks aligned for SVG filter image sources.
- Focused coverage extends the required manifest property fixture with an SVG
  filter resource using remote `feImage` content and keeps readable spine
  import intact.
- Dependency-closure checkpoint: this is limited to native PHP SVG href
  extraction for required EPUB manifest properties. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser SVG/filter rendering behavior, live remote-resource
  fetching, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,019 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,866
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,053 assertions, 0 failures.

## 2026-06-22 EPUB CSS Unquoted URL Remote-Resource Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,049 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now extracts unquoted CSS `url(...)` references correctly during
  required manifest property scans instead of reading the empty quoted capture
  branch from the CSS URL regex.
- `text/css` manifest items now report `missing-manifest-required-property`
  for `remote-resources` when a stylesheet contains unquoted remote references
  such as `url(https://cdn.example.test/image.png)`.
- Package-relative CSS URL detection uses the same corrected extraction path,
  keeping remote-base/resource-reference checks aligned with quoted URL
  behavior.
- Focused coverage extends the required manifest property fixture with an
  unquoted remote CSS URL resource and keeps readable spine import intact.
- Dependency-closure checkpoint: this is limited to native PHP CSS URL
  extraction for required EPUB manifest properties. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser CSS loading behavior, live remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,015 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,862
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,049 assertions, 0 failures.

## 2026-06-22 EPUB SMIL Textref Remote-Resource Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,045 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now treats SMIL `textref` / `epub:textref` attributes as EPUB
  resource references during required manifest property scans.
- `application/smil+xml` manifest items now report
  `missing-manifest-required-property` for `remote-resources` when sequence
  textrefs point at remote text documents, even if the overlay does not also
  contain a remote audio `src`.
- Existing media overlay path, fragment, resource, and ordering diagnostics
  remain separate: remote textrefs are still not reported as missing ZIP
  entries, while invalid local textref paths/fragments continue to produce
  their existing diagnostics.
- Focused coverage extends the required manifest property fixture with a
  textref-only remote SMIL resource and keeps readable spine import intact.
- Dependency-closure checkpoint: this is limited to native PHP remote-resource
  property detection for SMIL textrefs. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, live remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,011 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,858
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,045 assertions, 0 failures.

## 2026-06-22 EPUB Collection Metadata Link Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,041 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now includes OPF collection metadata `link` elements in the
  package-link diagnostics pass instead of validating only direct package
  metadata links and direct collection member links.
- Collection metadata links now receive the same native PHP diagnostics for
  missing `rel`, missing required `media-type`, invalid content, URL/resource
  issues, relation tokens, language/direction, and duplicate property tokens.
- Diagnostic context now preserves `collectionId` and `parentCollectionId`
  where applicable, while keeping `parent=metadata` for nested collection
  metadata links.
- Focused coverage adds nested collection metadata links for missing `rel`,
  missing `media-type`, and non-empty text content, while preserving readable
  spine import.
- Dependency-closure checkpoint: this is limited to routing OPF collection
  metadata links through existing package-link diagnostics. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, browser rendering behavior, live remote-resource
  fetching, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 3,007 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,854
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,041 assertions, 0 failures.

## 2026-06-22 EPUB Collection Metadata Value Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,026 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `empty-collection-metadata-value` when OPF
  collection-local `meta` records with a `property` attribute are empty after
  whitespace stripping.
- Missing collection metadata `property` diagnostics and OPF2-style
  `name`/`content` collection metadata diagnostics remain separate, so this
  value check does not double-count legacy or untyped collection records.
- The focused coverage extends the malformed collection fixture with an empty
  `property="title"` record and preserves collection id, metadata id, and
  property context for WordPress/Data Liberation review.
- Dependency-closure checkpoint: this is limited to OPF collection metadata
  value emptiness diagnostics. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, live remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,992 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,839
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,026 assertions, 0 failures.

## 2026-06-22 EPUB Package Link Content Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,021 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-package-link-content` when an OPF package
  metadata `link` contains non-whitespace text content.
- `EpubReader` also reports `invalid-package-link-content` when a package
  collection `link` contains nested child elements.
- The diagnostic preserves link id, parent context, href, normalized text, or
  the offending child element name so WordPress/Data Liberation review can
  distinguish direct metadata links from collection records.
- Existing href, media-type, rel/property, package-document reference,
  manifest-resource, missing-resource, relation media-type, and
  relation/refines diagnostics remain separate.
- Dependency-closure checkpoint: this is limited to EPUB3 OPF package link
  empty-content diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, live remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,987 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,834
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,021 assertions, 0 failures.

## 2026-06-22 EPUB Package Metadata Value Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,012 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `empty-package-metadata-value` when optional Dublin
  Core package metadata values are empty after whitespace stripping.
- `EpubReader` also reports `empty-package-metadata-value` when ordinary OPF
  `meta` elements with a `property` attribute have empty text values.
- Required Dublin Core elements remain handled by the existing required-field
  diagnostics, and specialized `dcterms:modified` and `media:duration` metadata
  continue to use their existing missing/invalid diagnostics so this generic
  value check does not double-count those cases.
- The focused coverage exercises an empty optional `dc:creator` and an empty
  `meta property="display-seq"` refinement, preserving context for the
  element, id, property/refines, or Dublin Core name.
- Dependency-closure checkpoint: this is limited to EPUB3 package metadata
  value emptiness diagnostics. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, live remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,978 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,825
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,012 assertions, 0 failures.

## 2026-06-22 EPUB Package Metadata Link Relation/Refines Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 10,004 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-package-link-record-refines` when an OPF
  package metadata link uses `rel=record` with a `refines` attribute.
- `EpubReader` now reports `missing-package-link-voicing-refines` when an OPF
  package metadata link uses `rel=voicing` without a `refines` attribute.
- The focused coverage includes invalid `record`+`refines`, missing
  `voicing` `refines`, and a valid `voicing` refinement control so the
  diagnostic does not over-report valid linked-audio metadata.
- Generic metadata `link` target-resolution fixtures now use a neutral
  relation value for refined links, keeping malformed/missing `refines`
  diagnostics isolated from the relation vocabulary contract checks.
- Existing invalid media type, href path/fragment, rel/property token,
  language, duplicate-token, package-document reference, manifest-resource,
  missing-resource, and relation media-type diagnostics remain separate.
- Dependency-closure checkpoint: this is limited to EPUB3 package metadata
  link relation/refines vocabulary diagnostics for `record` and `voicing`. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, browser rendering behavior, live
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,970 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,817
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 10,004 assertions, 0 failures.

## 2026-06-22 EPUB Package Link Relation Media-Type Diagnostics Slice

- Focused PHP coverage remains 771 behavior tests with 9,992 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `missing-package-link-media-type` when an OPF package
  link uses a relation value whose EPUB metadata link vocabulary entry requires
  a concrete media type.
- The focused coverage exercises `record` links for local package metadata,
  remote linked metadata, and collection records, plus `voicing` links that
  refine package metadata.
- Generic external links without `record` or `voicing` remain allowed without a
  `media-type`, while local package metadata links still require it under the
  general in-container linked-resource rule.
- Existing invalid media type, href path/fragment, rel/property token,
  language, duplicate-token, package-document reference, manifest-resource, and
  missing-resource diagnostics remain separate.
- Dependency-closure checkpoint: this is limited to EPUB3 package link
  relation-specific `media-type` requirements for `record` and `voicing`. It
  does not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, browser rendering behavior, live
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,958 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,805
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,992 assertions, 0 failures.

## 2026-06-22 EPUB Package Link Manifest-Resource Diagnostics Slice

- Focused PHP coverage is now 771 behavior tests with 9,980 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-package-link-manifest-resource` when a
  standalone OPF package link resource is also listed as a package manifest
  publication resource.
- The diagnostic covers direct package metadata links and package collection
  links, preserving parent context so WordPress/Data Liberation review can
  distinguish publication-level records from collection records.
- Remote linked metadata records, unmanifested in-container linked records,
  spine/content document links, and malformed href cases already handled by
  existing package-link diagnostics remain unreported by this new check.
- Diagnostic context includes the link id/rel, parent, source href, resolved
  archive path, manifest id, manifest href, and manifest media type.
- Import remains diagnostic-only: readable spine content, title metadata, and
  spine item refs are preserved so WordPress/Data Liberation review can show
  the EPUB while surfacing the package-model issue.
- Dependency-closure checkpoint: this is limited to EPUB3 package link
  standalone-record versus manifest publication-resource diagnostics. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, browser rendering behavior, live remote-resource
  fetching, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,946 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,793
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,980 assertions, 0 failures.

## 2026-06-22 EPUB Package Metadata Link Media-Type Diagnostics Slice

- Focused PHP coverage is now 770 behavior tests with 9,954 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `missing-package-link-media-type` when an EPUB3 OPF
  package metadata `<link>` points to a package-local in-container resource
  without a `media-type` attribute.
- Remote metadata links remain allowed without `media-type`, matching the EPUB3
  distinction between linked records in the container and external linked
  records.
- Existing `invalid-package-link-media-type`, href path/fragment, `rel`,
  `property`, language, and duplicate-token diagnostics remain separate, so the
  new required-attribute check does not mask more specific malformed-link
  failures.
- Import remains diagnostic-only: readable spine content, title metadata, and
  spine item refs are preserved so WordPress/Data Liberation review can show
  the EPUB while surfacing the malformed metadata link.
- Dependency-closure checkpoint: this is limited to EPUB3 OPF package metadata
  local-link `media-type` diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, live remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,920 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,767
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,954 assertions, 0 failures.

## 2026-06-22 EPUB Package Metadata Content-Model Diagnostics Slice

- Focused PHP coverage is now 770 behavior tests with 9,949 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-package-metadata-dublin-core-element` when
  an EPUB3 OPF `metadata` section contains unsupported `dc:*` children outside
  the package metadata Dublin Core vocabulary.
- The reader now reports `invalid-package-metadata-child-element` for OPF or
  foreign child elements outside the EPUB3 metadata content model, while
  keeping existing required-metadata namespace diagnostics separate.
- The metadata diagnostics pass now reports `multiple-metadata-date` when more
  than one `dc:date` appears in the package metadata section.
- Import remains diagnostic-only: readable spine content, title metadata, and
  spine item refs are preserved so WordPress/Data Liberation review can show
  the EPUB while surfacing the malformed metadata.
- Dependency-closure checkpoint: this is limited to EPUB3 OPF package metadata
  child vocabulary/cardinality diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, live remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,915 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,762
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,949 assertions, 0 failures.

## 2026-06-22 EPUB Cover-Image Property Diagnostics Slice

- Focused PHP coverage is now 769 behavior tests with 9,922 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-cover-image-property-media-type` when an
  OPF manifest item declares the reserved `cover-image` property on a resource
  whose media type is not an image type.
- The manifest diagnostics pass now reports `multiple-cover-image-manifest-items`
  when more than one `item` declares `cover-image`, preserving the declared ids
  for WordPress/Data Liberation review.
- Cover metadata selection now ignores invalid non-image `cover-image`
  declarations and selects the first valid image manifest item before falling
  back to legacy OPF2 cover metadata or guide references.
- Dependency-closure checkpoint: this is limited to EPUB3 OPF manifest
  `cover-image` property applicability/cardinality diagnostics and cover
  metadata selection. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, live remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,888 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,735
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,922 assertions, 0 failures.

## 2026-06-22 EPUB Page-List Value Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,907 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `nav-page-list-value-mismatch` warnings when an
  EPUB page-list navigation link value or visible label disagrees with the
  referenced target pagebreak value.
- Target pagebreak value extraction prefers `title`, then `aria-label`, then
  element text, so pagebreaks such as `<span epub:type="pagebreak" title="II">`
  surface the end-user page number that editorial review needs.
- Existing page-list target-not-pagebreak errors, duplicate-target checks,
  reading-order checks, missing-label/missing-href checks, nav link
  path/fragment diagnostics, malformed token diagnostics, type prefix
  diagnostics, and readable spine import remain separate.
- Dependency-closure checkpoint: this is limited to EPUB page-list/pagebreak
  value consistency diagnostics and does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, live remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,873 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,720
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,907 assertions, 0 failures.

## 2026-06-22 EPUB Nav Type Prefix Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,899 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates prefix declarations for EPUB navigation document
  `epub:type` property tokens on nav elements, landmarks links, and page-list
  links.
- Tokens such as `missing:landmarks`, `missing:bodymatter`, and
  `missing:pagebreak` now surface `undeclared-nav-type-prefix`,
  `undeclared-nav-landmark-link-type-prefix`, and
  `undeclared-nav-page-list-link-type-prefix` diagnostics with nav id, link
  href/label where applicable, offending value/type, and prefix context.
- Reserved EPUB property prefixes and prefixes declared on the navigation
  document root `prefix` attribute remain accepted; malformed token diagnostics
  such as `invalid-nav-type`, `invalid-nav-landmark-link-type`, and
  `invalid-nav-page-list-link-type` remain separate.
- Existing duplicate landmark target checks, page-list duplicate-target,
  missing-label, missing-href, reading-order checks, pagebreak target checks,
  nav link path/fragment checks, and readable spine import remain separate.
- Dependency-closure checkpoint: this is limited to EPUB navigation type prefix
  validation and does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, browser rendering behavior,
  live remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,865 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,712
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,899 assertions, 0 failures.

## 2026-06-22 EPUB Nav Element Type Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,886 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-nav-type` when EPUB navigation document
  `nav` elements declare malformed `epub:type` property tokens.
- Nav elements such as `epub:type="landmarks bad:"` now surface nav id, full
  type value, and offending token context while still participating in
  specialized landmarks/page-list/toc handling when they also contain a valid
  type token.
- Existing landmarks and page-list link type validation, duplicate landmark
  target checks, page-list duplicate-target/missing-label/missing-href checks,
  nav link path and fragment checks, page-list reading order checks, pagebreak
  target checks, and readable spine import remain separate.
- Dependency-closure checkpoint: this is limited to EPUB navigation element
  type token validation and does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, live remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,852 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,699
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,886 assertions, 0 failures.

## 2026-06-22 EPUB Page-List Link Type Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,883 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-nav-page-list-link-type` when specialized
  page-list navigation links declare malformed `epub:type` property tokens.
- Page-list links such as `epub:type="bad:"` now surface nav id, link href,
  accessible label, and offending type token context while preserving the
  page-list entry for WordPress review.
- Existing landmarks link type validation, duplicate landmark target checks,
  page-list duplicate-target/missing-label/missing-href checks, nav link path
  and fragment checks, page-list reading order checks, pagebreak target checks,
  and readable spine import remain separate.
- Dependency-closure checkpoint: this is limited to specialized page-list
  navigation link type token validation and does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, live remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,849 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,696
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,883 assertions, 0 failures.

## 2026-06-22 EPUB Landmark Link Type Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,879 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-nav-landmark-link-type` when specialized
  landmarks navigation links declare malformed `epub:type` property tokens.
- Landmark links such as `epub:type="bad:"` now surface nav id, link href,
  accessible label, and offending type token context while preserving the raw
  nav entry for WordPress review.
- Existing valid landmark type handling, duplicate landmark target checks,
  page-list duplicate-target/missing-label/missing-href checks, nav link path
  and fragment checks, pagebreak target checks, and readable spine import
  remain separate.
- Dependency-closure checkpoint: this is limited to specialized landmarks
  navigation link type token validation and does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, live remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,845 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,692
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,879 assertions, 0 failures.

## 2026-06-22 EPUB XHTML Remote Base Required Property Slice

- Focused PHP coverage remains 768 behavior tests with 9,875 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now treats a remote XHTML `<base href="https://...">` plus
  package-relative resource references as requiring the `remote-resources`
  manifest property.
- XHTML resources such as `remote-base.xhtml` with a remote base and relative
  stylesheet/image resources now report `missing-manifest-required-property`
  for `remote-resources` with manifest id, href, package path, media type, and
  property context.
- The detector is intentionally scoped to resource-carrying attributes,
  `srcset`/`imagesrcset`, CSS references, head-link resource `href`, and SVG
  embedding hrefs after a remote base. Ordinary XHTML `<a href>` hyperlinks do
  not become false remote-resource package errors. Existing `background`,
  `imagesrcset`, head-link `href`, SVG href remote-resource, XHTML/SVG MathML,
  inline SVG, scripted, switch, CSS, SMIL, `src`, `srcset`, `poster`, `data`,
  `action`, and `formaction` checks remain separate.
- Dependency-closure checkpoint: this is still limited to remote XHTML base
  interaction with required manifest property validation and does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, browser rendering behavior, live remote-resource
  fetching, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,841 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,688
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,875 assertions, 0 failures.

## 2026-06-22 EPUB XHTML Background Remote Resource Required Property Slice

- Focused PHP coverage remains 768 behavior tests with 9,871 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now treats remote XHTML `background` attribute values as
  requiring the `remote-resources` manifest property, matching the existing
  package-relative rewrite support for that resource attribute.
- XHTML resources such as `background-remote.xhtml` with
  `background="https://..."` now report `missing-manifest-required-property`
  for `remote-resources` with manifest id, href, package path, media type, and
  property context.
- The detector is intentionally scoped to resource-carrying `background`
  attributes so ordinary XHTML `<a href>` hyperlinks do not become false
  remote-resource package errors. Existing `imagesrcset`, head-link `href`,
  SVG href remote-resource, XHTML/SVG MathML, inline SVG, scripted, switch,
  CSS, SMIL, `src`, `srcset`, `poster`, `data`, `action`, and `formaction`
  checks remain separate.
- Dependency-closure checkpoint: this is still limited to XHTML `background`
  remote resource detection in required manifest property validation and does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, browser rendering behavior, live
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,837 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,684
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,871 assertions, 0 failures.

## 2026-06-22 EPUB XHTML Imagesrcset Remote Resource Required Property Slice

- Focused PHP coverage remains 768 behavior tests with 9,867 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now treats remote candidates from XHTML `imagesrcset`
  attributes as requiring the `remote-resources` manifest property, matching
  the existing `srcset` candidate scanner.
- XHTML resources such as `linked-imagesrcset.xhtml` that use a head
  `<link rel="preload" as="image" imagesrcset="https://...">` candidate now
  report `missing-manifest-required-property` for `remote-resources` with
  manifest id, href, package path, media type, and property context.
- The detector is intentionally scoped to resource-carrying `imagesrcset`
  candidates so ordinary XHTML `<a href>` hyperlinks do not become false
  remote-resource package errors. Existing head-link `href`, SVG href
  remote-resource, XHTML/SVG MathML, inline SVG, scripted, switch, CSS, SMIL,
  `src`, `srcset`, `poster`, `data`, `action`, and `formaction` checks remain
  separate.
- Dependency-closure checkpoint: this is still limited to XHTML `imagesrcset`
  remote resource detection in required manifest property validation and does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, browser rendering behavior, live
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,833 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,680
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,867 assertions, 0 failures.

## 2026-06-22 EPUB XHTML Head Link Remote Resource Required Property Slice

- Focused PHP coverage remains 768 behavior tests with 9,863 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now treats remote resource references from XHTML head
  `<link href="https://...">` elements as requiring the `remote-resources`
  manifest property.
- XHTML resources such as `linked-head.xhtml` that reference a remote
  stylesheet now report `missing-manifest-required-property` for
  `remote-resources` with manifest id, href, package path, media type, and
  property context.
- The detector is intentionally scoped to head-style link resources so
  ordinary XHTML `<a href>` hyperlinks do not become false remote-resource
  package errors. Existing SVG href remote-resource, XHTML/SVG MathML, inline
  SVG, scripted, switch, CSS, SMIL, `src`, `srcset`, `poster`, `data`,
  `action`, and `formaction` checks remain separate.
- Dependency-closure checkpoint: this is still limited to XHTML head-link
  remote resource detection in required manifest property validation and does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, browser rendering behavior, live
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,829 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,676
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,863 assertions, 0 failures.

## 2026-06-22 EPUB SVG Remote Href Required Property Slice

- Focused PHP coverage remains 768 behavior tests with 9,859 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now treats remote resource references embedded from SVG
  `image`/`use` `href` or `xlink:href` attributes as requiring the
  `remote-resources` manifest property.
- SVG resources such as `images/remote.svg` that reference `https://...`
  image content now report `missing-manifest-required-property` for
  `remote-resources` with manifest id, href, package path, media type, and
  property context.
- The detector is intentionally scoped to SVG embedding elements so ordinary
  XHTML hyperlinks do not become false remote-resource package errors. The
  existing XHTML/SVG MathML, inline SVG, scripted, switch, CSS, SMIL, `src`,
  `srcset`, `poster`, `data`, `action`, and `formaction` checks remain
  separate.
- Dependency-closure checkpoint: this is still limited to SVG href-based
  remote resource detection in required manifest property validation and does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, browser rendering behavior, live
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,825 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,672
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,859 assertions, 0 failures.

## 2026-06-22 EPUB NCX Content Src Fragment Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,855 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now diagnoses malformed local NCX navigation `content@src`
  fragments before spine-target fallthrough.
- Empty fragments such as `one.xhtml#` and decoded-whitespace fragments such
  as `one.xhtml#bad%20target` report
  `invalid-ncx-content-src-fragment`.
- Diagnostics keep NCX path, element/nav type, id/text, original
  `contentSrc`, rewritten href, resolved target path, reason, and decoded
  fragment when present. Invalid NCX paths, remote targets, fragment-only
  targets, missing spine targets, non-linear spine targets, and play-order
  diagnostics remain separate behavior.
- Dependency-closure checkpoint: this is still limited to NCX
  `content@src` fragment syntax and does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,821 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,668
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,855 assertions, 0 failures.

## 2026-06-22 EPUB Spine Hyperlink Href Fragment Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,837 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now diagnoses malformed local spine XHTML hyperlink fragments
  before manifest spine-target fallthrough.
- Empty fragments such as `appendix.xhtml#` and decoded-whitespace fragments
  such as `appendix.xhtml#bad%20target` report
  `invalid-spine-hyperlink-href-fragment` for `a@href` and `area@href`
  values.
- Diagnostics keep source XHTML path, element/attribute, original href,
  resolved target path, reason, decoded fragment when present, and link text
  or area alt context. Missing unspined hyperlink targets, recursive scans
  from linked content documents, non-linear spine targets, remote links, and
  fragment-only self links remain separate behavior.
- Dependency-closure checkpoint: this is still limited to spine XHTML
  hyperlink href fragment syntax and does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,803 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,650
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,837 assertions, 0 failures.

## 2026-06-22 EPUB SMIL Media Overlay Text Fragment Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,824 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now diagnoses malformed local SMIL media overlay text target
  fragments before ZIP resource checks and target-fragment fallthrough.
- Empty fragments such as `../text/chapter.xhtml#` and decoded-whitespace
  fragments such as `../text/chapter.xhtml#bad%20target` report
  `invalid-media-overlay-text-src-fragment` for `text@src` and
  `invalid-media-overlay-seq-textref-fragment` for `seq@epub:textref`.
- Diagnostics keep overlay/content context, SMIL element/id, original
  reference, resolved path, reason, and decoded fragment when present. Missing
  media overlay resources and missing target fragments remain independent
  diagnostics. Remote references, malformed path checks, audio clip timing,
  and target-order checks stay with their existing branches.
- Dependency-closure checkpoint: this is still limited to SMIL media overlay
  text target fragment syntax and does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,790 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,637
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,824 assertions, 0 failures.

## 2026-06-22 EPUB Navigation Link Href Fragment Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,804 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now diagnoses malformed local EPUB navigation document
  `a@href` fragments before ZIP resource checks and target-fragment
  fallthrough.
- Empty fragments such as `chapter.xhtml#` and decoded-whitespace fragments
  such as `chapter.xhtml#bad%20target` report
  `invalid-nav-link-href-fragment` with nav context, link label/value,
  original href, resolved target path, reason, and decoded fragment when
  present.
- Missing nav link resources and missing nav link target fragments remain
  independent diagnostics. Remote nav links, malformed path checks, and
  page-list pagebreak target checks stay with their existing branches.
- Dependency-closure checkpoint: this is still limited to navigation document
  link href fragment syntax and does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,770 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,617
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,804 assertions, 0 failures.

## 2026-06-22 EPUB Guide Reference Href Fragment Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,794 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now diagnoses malformed local OPF `guide/reference@href`
  fragments before ZIP resource checks and target-fragment fallthrough.
- Empty fragments such as `chapter.xhtml#` and decoded-whitespace fragments
  such as `chapter.xhtml#bad%20target` report
  `invalid-guide-reference-href-fragment` with guide type/title, href,
  resolved path, reason, and decoded fragment when present.
- Missing guide resources and missing guide target fragments remain
  independent diagnostics, so malformed fragment syntax no longer collapses
  into a weaker missing-fragment report. Remote guide references, data/file
  URL checks, and malformed path checks stay with their existing branches.
- Dependency-closure checkpoint: this is still limited to OPF guide reference
  href fragment syntax and does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,760 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,607
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,794 assertions, 0 failures.

## 2026-06-22 EPUB Package Link Href Fragment Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,783 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now diagnoses malformed local OPF package metadata and
  collection `link@href` fragments before resource-resolution fallthrough.
- Empty fragments such as `metadata/onix.xml#` and decoded-whitespace
  fragments such as `metadata/onix.xml#bad%20record` report
  `invalid-package-link-href-fragment` with link id, parent context, href,
  reason, and decoded fragment when present.
- Missing linked resources and missing linked fragments remain independent
  diagnostics, so malformed fragment syntax no longer cascades into misleading
  linked-metadata resource checks. Remote links and fragment-only package
  document references stay with their existing branches.
- Dependency-closure checkpoint: this is still limited to OPF
  package/collection link href fragment syntax and does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,749 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,596
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,783 assertions, 0 failures.

## 2026-06-22 EPUB Recursive Spine Hyperlink Target Diagnostics Slice

- Focused PHP coverage remains 768 behavior tests with 9,764 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now follows local XHTML/SVG EPUB content-document hyperlinks
  reached from readable spine resources and recursively scans those linked
  resources for additional `a`/`area` targets.
- The reader reports `missing-spine-hyperlink-target` when any scanned local
  OPF manifest resource target is not represented by an OPF spine `itemref`,
  including second-hop XHTML targets. The diagnostic keeps source XHTML path,
  element/attribute, original href, resolved target path, manifest item
  id/href/media type, and link text or area alt text where present.
- The fixture now covers a chapter link to an unspined appendix plus an
  appendix link to a second unspined XHTML document. Non-linear spine targets,
  duplicate links, fragment-only links, external URLs, embedded `img src`
  resources, and external links inside recursively scanned resources remain
  quiet.
- Dependency-closure checkpoint: this is still limited to local XHTML/SVG
  `a`/`area` hyperlink spine-membership diagnostics. It does not claim
  scripted-link discovery, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, browser rendering behavior,
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l lanes/pandoc/src/EpubWriter.php`,
  `php -l lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,730 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,577
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,764 assertions, 0 failures.

## 2026-06-22 EPUB Spine Hyperlink Target Diagnostics Slice

- Focused PHP coverage is now 768 behavior tests with 9,756 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now inspects readable XHTML spine `a` and `area` hyperlinks and
  reports `missing-spine-hyperlink-target` when a local OPF manifest resource
  target is not represented by any OPF spine `itemref`.
- The diagnostic records source XHTML path, element/attribute, original href,
  resolved target path, manifest item id/href/media type, and link text or area
  alt text where present.
- Non-linear spine targets satisfy the rule. Duplicate links, fragment-only
  self links, external URLs, and embedded `img src` references stay quiet, so
  asset embedding remains separate from top-level navigation diagnostics.
- Dependency-closure checkpoint: this is limited to readable XHTML `a`/`area`
  hyperlink spine-membership diagnostics. It does not claim scripted-link
  discovery, recursive traversal into newly diagnosed unspined resources, full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l lanes/pandoc/src/EpubWriter.php`,
  `php -l lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,722 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,569
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,756 assertions, 0 failures.

## 2026-06-22 EPUB XHTML Picture Source Fallback Diagnostics Slice

- Focused PHP coverage remains 767 behavior tests with 9,733 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now evaluates readable XHTML `picture` intrinsic image fallback
  behavior. Foreign `source srcset` resources without OPF manifest fallback
  stay unflagged when the same `picture` has an `img` fallback that resolves
  to an EPUB Core Media Type resource.
- When a `picture` lacks a Core Media Type `img` fallback, foreign
  `source srcset` resources without OPF fallback now report
  `missing-manifest-fallback-core-media-type` with `element=source`,
  `attribute=srcset`, descriptor/type context, the source XHTML path, and the
  resolved target path. Foreign `img` fallback resources without OPF fallback
  continue to report independently.
- Dependency-closure checkpoint: this is limited to readable XHTML
  `picture`/`source` image fallback diagnostics. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l lanes/pandoc/src/EpubWriter.php`,
  `php -l lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,699 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,546
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,733 assertions, 0 failures.

## 2026-06-22 EPUB XHTML Image Fallback Diagnostics Slice

- Focused PHP coverage remains 767 behavior tests with 9,711 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now inspects readable XHTML spine `img` references and reports
  `missing-manifest-fallback-core-media-type` with `missing-fallback` context
  when a directly embedded foreign image resource is declared in the OPF
  manifest without a fallback chain.
- The diagnostic records the manifest item id/href/path/media-type, source
  XHTML path, element/attribute, original source href, resolved target path,
  fallback chain, and `missing-fallback` reason. Existing manifest fallback
  chains that terminate without reaching a Core Media Type resource still
  report, while chains that resolve to a Core Media Type resource remain
  unflagged.
- Dependency-closure checkpoint: this is limited to readable XHTML `img`
  foreign-resource fallback diagnostics. It does not yet claim full
  `picture`/`source` media fallback parity, full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l lanes/pandoc/src/EpubWriter.php`,
  `php -l lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,677 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,524
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,711 assertions, 0 failures.

## 2026-06-22 EPUB Manifest Fallback Core Media Diagnostics Slice

- Focused PHP coverage is now 767 behavior tests with 9,700 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `missing-manifest-fallback-core-media-type` when a
  foreign-resource OPF manifest fallback chain terminates without reaching an
  EPUB Core Media Type resource.
- The diagnostic records the source manifest item id/href/path/media-type, the
  fallback chain, terminal fallback item id/href/path/media-type, and the
  `chain-ended` reason. Foreign-resource fallback chains that reach a Core
  Media Type resource, such as `audio/mpeg`, remain unflagged.
- Dependency-closure checkpoint: this is limited to OPF manifest fallback
  core-media chain validation. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l lanes/pandoc/src/EpubWriter.php`,
  `php -l lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,666 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,513
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,700 assertions, 0 failures, with 767 counted `PASS` lines.

## 2026-06-22 EPUB Spine Root ID Diagnostics Slice

- Focused PHP coverage remains 766 behavior tests with 9,684 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-spine-id` when OPF `spine@id` is present
  but is not an XML NCName.
- The diagnostic preserves permissive import behavior: the raw spine id remains
  available as `epubSpineId` for intake review, while existing `toc`,
  page-progression-direction, itemref linear, and non-linear resource
  diagnostics continue independently.
- Dependency-closure checkpoint: this is limited to OPF spine root ID
  validation. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, remote-resource fetching, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l lanes/pandoc/src/EpubWriter.php`,
  `php -l lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,650 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,497
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,684 assertions, 0 failures, with 766 counted `PASS` lines.

## 2026-06-22 EPUB Linear Spine Itemref Diagnostics Slice

- Focused PHP coverage is now 766 behavior tests with 9,680 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `missing-linear-spine-itemref` when an OPF `spine`
  has `itemref` children but none whose `linear` attribute is omitted or set
  to `yes`.
- Invalid itemref `linear` values remain reported as
  `invalid-spine-itemref-linear` and do not satisfy the EPUB3 required linear
  itemref rule.
- The diagnostic preserves permissive import behavior: non-linear spine
  resources remain recorded in `epubSpineItemRefs` and
  `epubNonLinearResources`, and non-linear XHTML still stays out of readable
  body blocks.
- Dependency-closure checkpoint: this is limited to OPF spine linear itemref
  conformance diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l lanes/pandoc/src/EpubWriter.php`,
  `php -l lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,646 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,493
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,680 assertions, 0 failures, with 766 counted `PASS` lines.

## 2026-06-22 EPUB Collection Content-Model Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,666 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `duplicate-collection-metadata` when an OPF
  `collection` repeats direct `metadata` children.
- `EpubReader` now reports `invalid-collection-child-order` when collection
  `metadata` appears after members, or when nested `collection` members appear
  after direct `link` members.
- `EpubWriter` now emits collection children as metadata, nested collections,
  then links so generated EPUB3 package output follows the EPUB3 collection
  content model.
- Dependency-closure checkpoint: this is limited to OPF collection
  content-model order/cardinality validation and matching writer output order.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, browser rendering behavior,
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, `php -l lanes/pandoc/src/EpubWriter.php`,
  `php -l lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 3,316 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,479
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,666 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB Collection Child Element Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,649 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-collection-child-element` when an OPF
  `collection` contains unexpected direct child elements other than
  `metadata`, `link`, or nested `collection`.
- `EpubReader` now reports `invalid-collection-metadata-child-element` when
  collection `metadata` contains unexpected children other than OPF `meta` or
  `link` elements.
- Invalid collection metadata children are no longer imported as valid
  collection metadata records, while valid meta/link metadata, collection
  links, nested collections, existing namespace diagnostics, and readable
  spine import continue.
- Dependency-closure checkpoint: this is limited to OPF collection child
  element validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,616 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,462
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,649 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB Spine TOC Media-Type Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,638 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-spine-toc-media-type` when OPF
  `spine@toc` references an existing manifest item whose media type is not
  `application/x-dtbncx+xml`.
- The diagnostic preserves the `toc` idref plus target href and media type,
  while `missing-spine-toc` and `invalid-spine-toc-idref` remain responsible
  for absent or malformed references.
- Valid NCX `spine@toc` references continue to import NCX metadata and
  navigation.
- Dependency-closure checkpoint: this is limited to OPF spine `toc`
  media-type validation. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,605 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,451
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,638 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB Manifest Fallback-Style Media-Type Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,634 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-manifest-fallback-style-media-type` when
  an OPF manifest `fallback-style` attribute targets a manifest item that is
  not a CSS resource.
- The diagnostic preserves source item id/href/media type plus target
  id/href/media type, while existing missing/invalid IDREF diagnostics and
  valid CSS fallback-style targets stay unchanged.
- Dependency-closure checkpoint: this is limited to OPF manifest
  `fallback-style` target media-type validation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,601 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,447
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,634 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB Collection Language/Direction Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,626 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-collection-dir` and
  `invalid-collection-xml-language` when OPF collection elements carry
  malformed direction or XML language attributes.
- `EpubReader` now reports `invalid-collection-metadata-dir` and
  `invalid-collection-metadata-xml-language` when collection metadata children
  carry malformed direction or XML language attributes.
- Diagnostics keep collection id, metadata id/property/value, and bad
  dir/lang context separate from existing role, member, and refines checks
  while readable spine import and valid collection metadata recovery continue.
- Dependency-closure checkpoint: this is limited to OPF collection and
  collection metadata language/direction diagnostics. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,593 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,439
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,626 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB Package Link XML Language Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,608 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-package-link-xml-language` when OPF
  package metadata links or collection links carry malformed `xml:lang`
  language tags.
- Link diagnostics keep `id`, `href`, `rel`, parent, and bad language context
  separate from existing `hreflang` and `dir` checks.
- Package metadata links now use the link-specific diagnostic path instead of
  double-reporting through the generic package metadata `xml:lang` validator.
- Dependency-closure checkpoint: this is limited to OPF package and collection
  link `xml:lang` diagnostics. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,575 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,421
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,608 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB Package Metadata XML Language Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,599 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-metadata-xml-language` when OPF package
  metadata child elements, including Dublin Core elements and OPF `meta`
  entries, carry malformed `xml:lang` language tags.
- The diagnostic preserves the element/id/property/value context plus the bad
  language tag, while metadata extraction, modified timestamp handling, and
  readable spine import continue.
- Existing `dc:language` text diagnostics and package root `xml:lang`
  diagnostics remain separate, so malformed metadata-local language tags do not
  mask required metadata recovery.
- Dependency-closure checkpoint: this is limited to OPF package metadata
  `xml:lang` diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,566 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,412
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,599 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB OCF Duplicate Container Token Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,590 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `duplicate-container-rootfile-property` when OCF
  `rootfile@properties` token lists repeat a token.
- `EpubReader` now reports `duplicate-container-link-rel` when OCF
  `link@rel` token lists repeat a token.
- `EpubReader` now reports `duplicate-container-link-property` when OCF
  `link@properties` token lists repeat a token.
- Duplicate otherwise-valid OCF rootfile/link token values stay separate from
  invalid property/rel syntax diagnostics, and raw duplicate token lists remain
  visible in `epubContainerRootfiles` and `epubContainerLinks` metadata while
  readable OPF import continues.
- Dependency-closure checkpoint: this is limited to OCF container token-list
  duplicate diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,557 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,403
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,590 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB OPF Duplicate Collection Role Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,577 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `duplicate-collection-role` when an OPF collection
  `role` token list repeats a role token.
- Duplicate otherwise-valid collection roles stay separate from
  `invalid-collection-role` and `invalid-collection-role-idpf-host`, so syntax
  errors, reserved-host role IRIs, and duplicate valid tokens remain distinct
  review diagnostics.
- Readable import and valid collection metadata/link recovery continue.
- Dependency-closure checkpoint: this is limited to OPF collection role
  token-list duplicate diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,544 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,390
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,577 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB OPF Duplicate Link Token Diagnostics Slice

- Focused PHP coverage remains 765 behavior tests with 9,573 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `duplicate-package-link-rel` when OPF package
  metadata or collection `link@rel` token lists repeat a token.
- `EpubReader` now reports `duplicate-package-link-property` when OPF package
  metadata or collection `link@properties` token lists repeat a token.
- Duplicate link property tokens remain preserved in imported
  `epubMetadataLinks` and `epubCollections` metadata, while `rel` remains
  visible as the raw source attribute string for review.
- Existing link syntax, prefix, media-type, hreflang, direction, unsafe URL,
  missing-resource, and package-document reference diagnostics remain separate:
  otherwise-valid duplicates do not trigger invalid-token diagnostics.
- Dependency-closure checkpoint: this is limited to OPF package metadata and
  collection link token-list duplicate diagnostics. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,540 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,386
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,573 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB OPF Duplicate Property Token Diagnostics Slice

- Focused PHP coverage is now 765 behavior tests with 9,553 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `duplicate-manifest-property` when OPF manifest
  `item@properties` token lists repeat a property token.
- `EpubReader` now reports `duplicate-spine-itemref-property` when OPF spine
  `itemref@properties` token lists repeat a property token.
- Duplicate tokens remain preserved in imported manifest and spine metadata so
  review surfaces can show the raw OPF state, while readable spine import and
  valid rendition metadata recovery continue.
- Existing property syntax and prefix diagnostics remain separate:
  otherwise-valid duplicates do not trigger `invalid-manifest-property`,
  `undeclared-manifest-property-prefix`, `invalid-spine-itemref-property`, or
  `undeclared-spine-itemref-property-prefix`.
- Dependency-closure checkpoint: this is limited to OPF manifest and spine
  property token-list duplicate diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,520 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,366
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,553 assertions, 0 failures, with 765 counted `PASS` lines.

## 2026-06-22 EPUB OPF Empty Unique Identifier Diagnostics Slice

- Focused PHP coverage remains 764 behavior tests with 9,528 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `empty-unique-identifier` when the OPF package
  `unique-identifier` attribute resolves to a `dc:identifier` element whose
  text content is empty after trimming.
- The diagnostic records the selected identifier ID and target element while
  preserving readable import when another non-empty `dc:identifier` satisfies
  the required metadata check.
- The existing missing and invalid unique-identifier diagnostics remain
  separate: absent selected IDs still report `missing-unique-identifier`,
  malformed IDREF values still report `invalid-unique-identifier`, and
  selected IDs that resolve to non-identifier elements still report
  `invalid-unique-identifier-target`.
- Dependency-closure checkpoint: this is limited to OPF empty selected
  unique-identifier diagnostics. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,495 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,341
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,528 assertions, 0 failures, with 764 counted `PASS` lines.

## 2026-06-22 EPUB OPF Package-Wide Duplicate ID Diagnostics Slice

- Focused PHP coverage is now 764 behavior tests with 9,516 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `duplicate-package-id` when valid XML `id`
  attributes collide across OPF package scopes, such as a metadata element ID
  colliding with a manifest item ID.
- Existing narrower duplicate diagnostics remain authoritative for local
  scopes that already had coverage: metadata children, manifest items, spine
  itemrefs, collection IDs, collection links, and collection metadata entries.
- The diagnostic records the repeated element, repeated ID, useful link or
  metadata context, and previous element/value context while preserving
  readable import.
- Dependency-closure checkpoint: this is limited to OPF package-wide duplicate
  XML ID diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,483 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,329
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,516 assertions, 0 failures, with 764 counted `PASS` lines.

## 2026-06-22 EPUB OPF Package Root Element Diagnostics Slice

- Focused PHP coverage is now 763 behavior tests with 9,504 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-package-root-element` when an OPF package
  document uses a root local name other than `package`.
- The previous fatal root-name rejection is now a recoverable diagnostic for
  package documents whose OPF child branches, manifest, spine, and resources
  are still usable.
- The diagnostic records the qualified root element name, observed local name,
  and expected root element while preserving readable WordPress import.
- Dependency-closure checkpoint: this is limited to OPF package root element
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, remote-resource fetching, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,471 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,317
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,504 assertions, 0 failures, with 763 counted `PASS` lines.

## 2026-06-22 EPUB OPF Package Root Namespace Diagnostics Slice

- Focused PHP coverage is now 762 behavior tests with 9,494 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-package-root-namespace` when an OPF
  package document uses the wrong namespace on its root element.
- The diagnostic records the qualified root element name, observed namespace,
  and expected OPF namespace while preserving readable import when the package
  children, manifest, spine, and resources remain usable.
- This closes one more package-level validation branch without changing the
  existing package metadata, manifest, spine, or XHTML conversion paths.
- Dependency-closure checkpoint: this is limited to OPF package root namespace
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, remote-resource fetching, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,461 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,307
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,494 assertions, 0 failures, with 762 counted `PASS` lines.

## 2026-06-22 EPUB OCF Invalid Rootfile Selection Diagnostics Slice

- Focused PHP coverage is now 761 behavior tests with 9,484 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reuses a single container rootfile diagnostic helper for
  successful package metadata diagnostics and early `EpubContainerException`
  diagnostics.
- Valid OCF `rootfiles` branches containing only invalid child elements or
  namespace lookalikes now report `invalid-container-rootfiles` plus the
  underlying rootfile namespace and rootfiles-branch child diagnostics on the
  exception.
- Valid OCF `rootfiles` branches whose direct `rootfile` entries have no
  usable package path now report `missing-container-opf-rootfile` plus the
  rootfile full-path diagnostics that explain why no OPF package could be
  selected.
- Existing successful EPUB import diagnostics are preserved through the shared
  helper path, including rootfile id/property/media-type/full-path/resource
  diagnostics.
- Dependency-closure checkpoint: this is limited to invalid rootfile selection
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, remote-resource fetching, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,451 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,297
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,484 assertions, 0 failures, with 761 counted `PASS` lines.

## 2026-06-22 EPUB OCF Required Rootfiles Diagnostics Slice

- Focused PHP coverage is now 759 behavior tests with 9,462 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now throws `EpubContainerException`, an
  `InvalidArgumentException` subclass, when `META-INF/container.xml` cannot
  identify an OPF package document.
- Valid OCF containers without a direct `rootfiles` branch now carry a
  structured `missing-container-rootfiles` diagnostic on the exception.
- Valid OCF containers with an empty primary `rootfiles` branch now carry a
  structured `empty-container-rootfiles` diagnostic on the exception.
- Existing rejection compatibility is preserved for callers that only catch
  `InvalidArgumentException`; no readable import path is claimed when there is
  no package document to load.
- Dependency-closure checkpoint: this is limited to required OCF rootfiles
  rejection diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubContainerException.php`, `php -l
  lanes/pandoc/src/EpubReader.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,429 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,275
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,462 assertions, 0 failures, with 759 counted `PASS` lines.

## 2026-06-22 EPUB OCF Branch Child Diagnostics Slice

- Focused PHP coverage remains 757 behavior tests with 9,448 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-container-rootfiles-child-namespace` and
  `invalid-container-rootfiles-child-element` for unexpected direct children
  inside the primary OCF `rootfiles` branch.
- `EpubReader` now reports `invalid-container-links-child-namespace` and
  `invalid-container-links-child-element` for unexpected direct children inside
  the primary OCF `links` branch.
- Rootfile/link namespace lookalikes are deliberately left to the existing
  `invalid-container-rootfile-namespace` and `invalid-container-link-namespace`
  diagnostics, so branch-child validation does not double-count those entries.
- Dependency-closure checkpoint: this is limited to OCF branch-child
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, remote-resource fetching, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,415 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,261
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,448 assertions, 0 failures, with 757 counted `PASS` lines.

## 2026-06-22 EPUB OCF Container Child Diagnostics Slice

- Focused PHP coverage remains 757 behavior tests with 9,434 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-container-child-namespace` for direct
  `META-INF/container.xml` children outside the OCF container namespace and
  `invalid-container-child-element` for same-namespace direct children whose
  local names are not `rootfiles` or `links`.
- The diagnostics are scoped to valid OCF container roots. Malformed-root
  fallback behavior is unchanged, so WordPress intake can still recover
  readable content when a non-OCF root has local-name rootfile records.
- Dependency-closure checkpoint: this is limited to OCF container direct-child
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, remote-resource fetching, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,401 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,247
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,434 assertions, 0 failures, with 757 counted `PASS` lines.

## 2026-06-22 EPUB OCF Duplicate Container Branch Diagnostics Slice

- Focused PHP coverage is now 757 behavior tests with 9,424 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `duplicate-container-rootfiles` and
  `duplicate-container-links` when valid OCF `META-INF/container.xml` files
  declare repeated top-level `rootfiles` or `links` branches.
- Duplicate-branch `rootfile` and `link` records are excluded from normalized
  `epubContainerRootfiles` and `epubContainerLinks` metadata, so they cannot
  become selected OPF packages or trigger duplicate missing-resource checks.
- Duplicate branch tracking uses DOM node paths rather than PHP wrapper object
  identity, because DOM may expose the same XML node through different wrapper
  instances across XPath and child traversal.
- Dependency-closure checkpoint: this is limited to duplicate OCF container
  branch diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,391 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,237
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,424 assertions, 0 failures, with 757 counted `PASS` lines.

## 2026-06-22 EPUB OCF Container Branch Scope Diagnostics Slice

- Focused PHP coverage remains 756 behavior tests with 9,404 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now requires valid OCF `rootfile` records to be direct children
  of the top-level `container/rootfiles` branch and valid OCF container `link`
  records to be direct children of the top-level `container/links` branch.
- Valid-namespace entries in nested `rootfiles` or `links` branches now report
  the existing `invalid-container-rootfile-parent` or
  `invalid-container-link-parent` diagnostics with parent and grandparent
  context, and they stay out of normalized `epubContainerRootfiles` and
  `epubContainerLinks` metadata.
- The compatibility fallback for malformed container roots remains intact:
  when the container root itself is already invalid, the reader can still use
  local-name rootfile/link records to recover readable content or review
  metadata while reporting the root-level container diagnostic.
- Dependency-closure checkpoint: this is limited to OCF container branch
  scoping. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, browser rendering behavior,
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,371 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,217
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,404 assertions, 0 failures, with 756 counted `PASS` lines.

## 2026-06-22 EPUB OCF Container Link ID Diagnostics Slice

- Focused PHP coverage is now 756 behavior tests with 9,394 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-container-link-id` when
  `META-INF/container.xml` link `id` attributes are not XML NCNames and
  `duplicate-container-link-id` when valid container link IDs repeat within the
  valid OCF container link list.
- Malformed and duplicate-ID container link records remain preserved in
  `epubContainerLinks` for review; the diagnostics add explicit provenance
  without dropping the link metadata.
- Dependency-closure checkpoint: this is limited to OCF container link ID
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, remote-resource fetching, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,361 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,207
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,394 assertions, 0 failures, with 756 counted `PASS` lines.

## 2026-06-22 EPUB OCF Container Link Namespace Diagnostics Slice

- Focused PHP coverage is now 755 behavior tests with 9,380 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-container-link-namespace` for
  foreign-namespace `META-INF/container.xml` link lookalikes and
  `invalid-container-link-parent` for link elements outside the OCF `links`
  branch.
- Valid OCF container links remain preserved in `epubContainerLinks`, while
  foreign or wrong-parent lookalikes are excluded from normalized container
  link metadata and cannot trigger missing-resource checks.
- The compatibility fallback for malformed container roots remains intact:
  when the container root itself is already invalid, the reader can still use
  local-name link records to preserve review metadata while reporting the
  root-level container diagnostic.
- Dependency-closure checkpoint: this is limited to OCF container link
  namespace/parent diagnostics and container link metadata hardening. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, browser rendering behavior, remote-resource
  fetching, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,347 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,193
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,380 assertions, 0 failures, with 755 counted `PASS` lines.

## 2026-06-22 EPUB OCF Rootfile Namespace Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,361 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-container-rootfile-namespace` for
  foreign-namespace `META-INF/container.xml` rootfile lookalikes and
  `invalid-container-rootfile-parent` for rootfile elements outside the
  `rootfiles` branch.
- Valid OCF rootfiles remain selectable, while foreign or wrong-parent
  lookalikes are excluded from `epubContainerRootfiles` and cannot become the
  selected OPF package.
- The compatibility fallback for malformed container roots remains intact:
  when the container root itself is already invalid, the reader can still use
  local-name rootfile records to recover readable content while reporting the
  root-level container diagnostic.
- Dependency-closure checkpoint: this is limited to OCF rootfile
  namespace/parent diagnostics and rootfile selection hardening. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, browser rendering behavior, remote-resource
  fetching, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,328 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,174
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,361 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB OCF Rootfile Backslash Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,342 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-container-rootfile-full-path` with reason
  `backslash` for `META-INF/container.xml` rootfile `full-path` values that
  contain literal backslashes.
- Malformed Windows-style rootfile aliases stay visible in
  `epubContainerRootfiles` for review, but they are excluded from OPF rootfile
  selection and do not cascade into false `missing-container-rootfile-resource`
  diagnostics.
- Dependency-closure checkpoint: this is limited to OCF rootfile backslash path
  diagnostics and rootfile selection hardening. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,309 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,155
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,342 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB OCF Rootfile Encoded Dot-Segment Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,339 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-container-rootfile-full-path` with reason
  `encoded-dot-segment` for `META-INF/container.xml` rootfile `full-path`
  segments that percent-decode to `.` or `..`.
- Malformed encoded traversal rootfile entries stay visible in
  `epubContainerRootfiles` for review, but they are excluded from OPF rootfile
  selection and do not cascade into false `missing-container-rootfile-resource`
  diagnostics.
- Dependency-closure checkpoint: this is limited to OCF rootfile
  encoded-dot-segment path diagnostics and rootfile selection hardening. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, browser rendering behavior,
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,306 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,152
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,339 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB Metadata Refines Encoded Dot-Segment Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,336 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-metadata-refines` with reason
  `encoded-dot-segment` for OPF metadata `refines` path segments that
  percent-decode to `.` or `..`, including both `meta` and `link` refinement
  elements.
- Encoded traversal aliases no longer cascade into false
  `missing-metadata-refines-resource`,
  `missing-metadata-refines-fragment`, or package-document target diagnostics.
- Dependency-closure checkpoint: this is limited to OPF metadata-refines
  encoded dot-segment path diagnostics and cascade prevention. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, browser rendering behavior, remote-resource
  fetching, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,303 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,149
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,336 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB Metadata Refines Fragment Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,323 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `missing-metadata-refines-fragment` for OPF
  metadata `refines` values that target an existing local XHTML/XML package
  resource but an unresolved fragment ID, such as
  `chapter.xhtml#missing-section` or `metadata/onix.xml#missing-record`.
- Valid resource fragments remain unflagged, missing local files stay in the
  existing `missing-metadata-refines-resource` bucket, and package-document ID
  checks remain in `missing-metadata-refines-target`.
- Dependency-closure checkpoint: this is limited to OPF metadata-refines
  resource fragment diagnostics and cascade prevention. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,290 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,136
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,323 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB Package Link Fragment Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,310 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `missing-package-link-fragment` for OPF package
  metadata and collection `link@href` values that target an existing local XML
  resource but an unresolved fragment ID, such as
  `metadata/onix.xml#missing-record`.
- Valid local linked-record fragments remain unflagged, remote package links
  remain unflagged, missing local files stay in the existing
  `missing-package-link-resource` bucket, and package-document references stay
  in `invalid-package-link-package-document-reference`.
- Dependency-closure checkpoint: this is limited to OPF package-link fragment
  diagnostics and cascade prevention. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,277 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,123
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,310 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB NCX Content Src Path Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,297 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-ncx-content-src-path` for NCX
  `navPoint`, `pageTarget`, and `navTarget` `content src` values that use
  query-only or fragment-only local paths, leading-slash absolute paths,
  protocol-relative URLs, backslash paths, or encoded dot segments.
- NCX spine-target checks now skip those malformed source path shapes, so
  invalid NCX references do not cascade into false `missing-ncx-spine-target`
  or `ncx-target-non-linear-spine` diagnostics.
- NCX TOC, page-list, and nav-list metadata now preserves raw invalid `src`
  values instead of normalizing them to misleading package paths, while remote
  absolute targets remain unflagged.
- Dependency-closure checkpoint: this is limited to NCX content source path
  diagnostics and metadata scoping. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,264 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,110
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,297 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 Nav Link Href Path Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,262 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-nav-link-href-path` for `nav.xhtml`
  anchor `href` values that use query-only or fragment-only local paths,
  leading-slash absolute paths, protocol-relative URLs, backslash paths, or
  encoded dot segments.
- Nav resource, page-list pagebreak, landmark/page-list duplicate-target, and
  reading-order/linear-spine checks now skip those malformed path shapes, so
  invalid navigation links do not cascade into false missing-resource,
  missing-fragment, pagebreak, duplicate-target, or spine diagnostics.
- Remote absolute navigation links remain unflagged, preserving the existing
  distinction between external references and malformed local archive paths.
- Dependency-closure checkpoint: this is limited to EPUB3 `nav.xhtml` link
  href path diagnostics and cascade prevention. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,229 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,075
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,262 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 SMIL Media-Overlay Reference Path Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,245 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports
  `invalid-media-overlay-seq-textref-path`,
  `invalid-media-overlay-text-src-path`, and
  `invalid-media-overlay-audio-src-path` for SMIL media-overlay
  `seq epub:textref` and `text`/`audio` `src` values that use query-only local
  paths, leading-slash absolute paths, protocol-relative URLs, backslash paths,
  or encoded dot segments.
- Media-overlay resource, fragment, and target-order checks now skip those
  malformed path shapes, so invalid overlay references do not cascade into
  false missing-resource, missing-fragment, or ordering diagnostics.
- SMIL overlay metadata now preserves raw invalid references instead of
  normalizing them into misleading archive paths, while valid query-bearing
  local targets and remote targets remain unflagged.
- Dependency-closure checkpoint: this is limited to SMIL media-overlay
  reference path diagnostics and metadata scoping. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,212 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,058
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,245 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 OCF Sidecar Reference URI Path Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,212 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports
  `invalid-ocf-encryption-cipher-reference-uri-path` and
  `invalid-ocf-signature-reference-uri-path` for OCF encryption
  `CipherReference` and XML Signature `Reference` URI values that use
  query-only local paths, leading-slash absolute paths, protocol-relative
  URLs, backslash paths, or encoded dot segments.
- Encryption and signature resource lookups now skip those malformed path
  shapes, so invalid sidecar URI values do not cascade into false
  `missing-ocf-encrypted-resource` or
  `missing-ocf-signature-reference-resource` diagnostics.
- OCF encrypted-resource metadata now avoids recording normalized archive
  paths for invalid sidecar URI aliases while preserving valid encrypted
  resource paths and algorithms.
- Dependency-closure checkpoint: this is limited to OCF sidecar reference URI
  path diagnostics and metadata scoping. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,179 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 3,025
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,212 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 OCF Container Link Href Path Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,178 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-container-link-href-path` for
  `META-INF/container.xml` `link@href` values that use query-only local paths,
  leading-slash absolute paths, protocol-relative URLs, backslash paths, or
  encoded dot segments.
- Container-link import now preserves the raw `href` internally for
  diagnostics while exposing the existing normalized `epubContainerLinks`
  metadata to readers and round-trip output.
- Missing local container-link resource diagnostics now skip hrefs already
  classified as malformed path shapes, so invalid container link URL/path
  values do not cascade into false `missing-container-link-resource` ZIP
  diagnostics.
- Dependency-closure checkpoint: this is limited to OCF container-link href
  path diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,145 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,991
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,178 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 Encoded Dot-Segment Href Path Diagnostics Slice

- Focused PHP coverage remains 752 behavior tests with 9,160 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `encoded-dot-segment` through
  `invalid-manifest-href-path`, `invalid-package-link-href-path`, and
  `invalid-guide-reference-href-path` when local OPF manifest, package link,
  or guide reference `href` path segments percent-decode exactly to `.` or
  `..`.
- Resource lookup, restricted-resource checks, and package-document-reference
  checks now skip those malformed encoded path shapes, so encoded traversal
  aliases do not cascade into false `missing-*`, reserved-resource, or
  package-document-reference diagnostics.
- Ordinary percent-encoded filenames continue to resolve through the package
  URL path resolver, preserving existing behavior for aliases such as
  `images/%63over.svg`.
- Dependency-closure checkpoint: this is limited to OPF manifest/package
  resource and guide/package link encoded dot-segment path diagnostics. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, browser rendering behavior,
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output
  behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,127 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,973
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,160 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 Package Link URL/Path Diagnostics Slice

- Focused PHP coverage is now 752 behavior tests with 9,142 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now expands `invalid-package-link-href-path` beyond query-only
  local paths to report absolute paths, protocol-relative URLs, and backslash
  paths in OPF package metadata and collection `link@href` values.
- Package link resource diagnostics now skip hrefs already classified as
  malformed path shapes, so invalid URL/path values no longer cascade into
  false `missing-package-link-resource` ZIP diagnostics.
- Existing `invalid-package-link-data-url`,
  `invalid-package-link-file-url`, package-document reference, and remote-link
  skip behavior remain separate.
- Dependency-closure checkpoint: this is limited to OPF package metadata and
  collection link URL/path diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,109 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,955
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,142 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 Guide Reference URL/Path Diagnostics Slice

- Focused PHP coverage is now 752 behavior tests with 9,118 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-guide-reference-data-url` and
  `invalid-guide-reference-file-url` when OPF guide `reference@href` values use
  unsafe `data:` or `file:` schemes.
- Guide `reference@href` path validation now also reports
  `invalid-guide-reference-href-path` reasons for absolute paths,
  protocol-relative URLs, backslash paths, and empty local paths such as
  query-only or fragment-only references.
- Malformed guide references remain inspectable in `epubGuideReferences`, and
  invalid URL/path values no longer cascade into false
  `missing-guide-reference-resource` ZIP diagnostics.
- Dependency-closure checkpoint: this is limited to OPF guide reference
  URL/path diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, remote-resource fetching, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,085 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,931
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,118 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 Guide Reference Href Empty-Path Diagnostics Slice

- Focused PHP coverage is now 752 behavior tests with 9,100 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-guide-reference-href-path` with reason
  `empty-path` when OPF guide `reference@href` values contain only a query or
  fragment, such as `?print=1` or `#cover`.
- Remote guide references remain exempt from local ZIP resource diagnostics,
  while query-only and fragment-only local guide hrefs no longer disappear
  from path validation.
- Dependency-closure checkpoint: this is limited to OPF guide reference
  empty-path href diagnostics. It does not claim full EPUBCheck validation,
  full upstream Haskell runner parity, full EPUB fixture corpus parity,
  browser rendering behavior, remote-resource fetching, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,067 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,913
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,100 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 Package Link Href Empty-Path Diagnostics Slice

- Focused PHP coverage is now 753 behavior tests with 9,090 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-package-link-href-path` with reason
  `empty-path` when OPF package metadata or collection `link@href` values
  contain only a query, such as `?rev=1` or `?edition=2`.
- Fragment-only package links remain covered by
  `invalid-package-link-package-document-reference`, while query-only package
  links no longer disappear from diagnostics or become missing ZIP resource
  checks.
- Dependency-closure checkpoint: this is limited to OPF package metadata and
  collection link empty-path href diagnostics. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,057 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,903
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,090 assertions, 0 failures, with 753 counted `PASS` lines.

## 2026-06-22 EPUB3 Manifest Href Empty-Path Diagnostics Slice

- Focused PHP coverage is now 752 behavior tests with 9,081 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-manifest-href-path` with reason
  `empty-path` when OPF manifest item `href` values contain only a query or
  fragment, such as `?rev=1` or `#package-fragment`.
- Fragment-only href values continue to report
  `invalid-manifest-href-fragment`, while empty-path values no longer collapse
  into duplicate resource aliases or missing ZIP resource checks.
- Dependency-closure checkpoint: this is limited to OPF manifest empty-path
  href diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, remote-resource fetching, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,048 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,894
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,081 assertions, 0 failures, with 752 counted `PASS` lines.

## 2026-06-22 EPUB3 Cover/Image Manifest URL Normalization Slice

- Focused PHP coverage is now 751 behavior tests with 9,064 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now resolves OPF manifest cover-image and image resource
  `href` values through the package URL path resolver before storing
  `epubCoverImage` and `epubImageResources`.
- Percent-encoded local manifest paths such as `images/%63over.svg` and
  `images/th%75mb.PNG` now surface as decoded ZIP paths such as
  `EPUB/images/cover.svg` and `EPUB/images/thumb.PNG`, matching spine/resource
  path handling and keeping guide metadata aligned.
- Dependency-closure checkpoint: this is limited to cover/image manifest URL
  path normalization for local package resources. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,031 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,877
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,064 assertions, 0 failures, with 751 counted `PASS` lines.

## 2026-06-22 EPUB3 Metadata Refines Resource Target Diagnostics Slice

- Focused PHP coverage is now 750 behavior tests with 9,061 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now accepts package metadata `refines` values that are
  path-relative resource URLs with optional fragments, including content
  document targets, linked metadata records, and `package.opf#id` references
  back to package elements.
- Missing path-relative resource targets now emit
  `missing-metadata-refines-resource` with resolved ZIP path and fragment
  context, while fragment-only missing package element ids remain
  `missing-metadata-refines-target` and malformed or absolute values remain
  `invalid-metadata-refines` with reason context.
- Dependency-closure checkpoint: this is limited to package metadata
  `refines` URL/resource diagnostics. Collection-local metadata refines
  retain their existing containing-collection scoping, and this does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, browser rendering behavior, remote-resource fetching,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 2,028 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,874
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,061 assertions, 0 failures, with 750 counted `PASS` lines.

## 2026-06-22 EPUB3 Package Link Package-Document Reference Diagnostics Slice

- Focused PHP coverage is now 749 behavior tests with 9,029 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports
  `invalid-package-link-package-document-reference` when OPF package metadata
  or collection `link@href` values resolve back to the selected package
  document with a fragment, including fragment-only `#id` links and
  package-relative `package.opf#id` references.
- Valid fragments into linked metadata resources such as
  `metadata/onix.xml#record` remain accepted, package link metadata remains
  inspectable with normalized resource hrefs, and package-document element
  references no longer collapse into generic `missing-package-link-resource`
  diagnostics.
- Dependency-closure checkpoint: this is limited to package metadata and
  collection `link@href` references back into the selected package document.
  It does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, browser rendering behavior,
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,996 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,842
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,029 assertions, 0 failures, with 749 counted `PASS` lines.

## 2026-06-22 EPUB3 Manifest Href Fragment Diagnostics Slice

- Focused PHP coverage is now 748 behavior tests with 9,000 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports `invalid-manifest-href-fragment` when an OPF
  manifest item `href` contains a fragment identifier.
- Fragment-bearing manifest entries still resolve the resource path before
  the `#` for ZIP lookup, readable spine import, manifest/spine metadata, and
  TOC metadata, so WordPress intake can surface the invalid package while
  preserving recoverable content.
- The diagnostic records the raw `href`, resolved ZIP `path`, media type, id,
  and decoded fragment when available, and it does not also emit false
  `missing-manifest-resource` diagnostics when the archive resource exists.
- Dependency-closure checkpoint: this is limited to manifest `item@href`
  fragment diagnostics and readable recovery for existing local resources. It
  does not claim full EPUBCheck validation, full upstream Haskell runner
  parity, full EPUB fixture corpus parity, browser rendering behavior,
  remote-resource fetching, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,967 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,813
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 9,000 assertions, 0 failures, with 748 counted `PASS` lines.

## 2026-06-22 EPUB3 Parsed Manifest URL Diagnostics Slice

- Focused PHP coverage is now 747 behavior tests with 8,986 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now resolves package-relative manifest, spine, navigation, and
  content-resource URLs through shared URL path parsing before ZIP lookup.
- Percent-encoded local aliases such as `text/%63hapter.xhtml` now resolve to
  the same archive resource as `text/chapter.xhtml`, so the reader reports
  `duplicate-manifest-href` after parsed URL normalization instead of treating
  the alias as a missing ZIP entry.
- Readable spine import, TOC metadata, navigation target diagnostics, and
  rewritten content links now use the decoded ZIP path while preserving
  query/fragment suffixes.
- Dependency-closure checkpoint: this is limited to package-relative URL path
  parsing for local EPUB resources and parsed manifest URL duplicate
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, remote-resource fetching, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,953 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 5 files, 2,799
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,986 assertions, 0 failures, with 747 counted `PASS` lines.

## 2026-06-22 EPUB3 Restricted Manifest Resource Diagnostics Slice

- Focused PHP coverage is now 746 behavior tests with 8,974 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports manifest entries that reference the selected OPF
  package document or reserved OCF files.
- The reader emits `manifest-package-document-resource` when an item `href`
  resolves to the selected package document, and
  `manifest-reserved-ocf-resource` when an item resolves to `mimetype` or a
  `META-INF/` container resource such as `META-INF/container.xml`.
- Restricted manifest entries remain inspectable in `epubManifestResources`,
  and readable spine import still proceeds for otherwise valid content.
- Dependency-closure checkpoint: this is limited to restricted manifest
  package/OCF resource diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,941 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php lanes/pandoc/tests/DocxReaderTest.php`
  passed 4 files, 2,700 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 8,974 assertions, 0 failures, with 746
  counted `PASS` lines.

## 2026-06-22 EPUB3 Package Child Order Diagnostics Slice

- Focused PHP coverage is now 745 behavior tests with 8,957 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports first valid OPF package children that violate the
  EPUB package content-model order: `metadata`, `manifest`, `spine`, `guide`,
  `bindings`, then `collection`.
- The reader emits `invalid-package-child-order` diagnostics with the child
  position, expected order, and previous later-ranked sibling context.
- Readable spine import still proceeds, and valid guide references, bindings,
  and collection metadata remain available for WordPress intake review when
  the package is otherwise readable.
- Dependency-closure checkpoint: this is limited to OPF package child ordering
  diagnostics for first valid package branches. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,924 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php lanes/pandoc/tests/DocxReaderTest.php`
  passed 4 files, 2,683 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 8,957 assertions, 0 failures, with 745
  counted `PASS` lines.

## 2026-06-22 EPUB3 Duplicate Optional Package Child Diagnostics Slice

- Focused PHP coverage is now 744 behavior tests with 8,937 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports duplicate top-level OPF `guide` and `bindings`
  elements instead of merging alternate optional package branches.
- The reader emits `duplicate-package-guide` and
  `duplicate-package-bindings` diagnostics when more than one valid OPF
  guide/bindings child appears.
- Guide and bindings import now follows first-valid-branch precedence:
  duplicate guide references are not imported, duplicate bindings are not
  imported, and duplicate optional branches do not leak missing guide
  resource/type diagnostics, binding media-type/handler diagnostics, or extra
  deprecated-bindings warnings.
- Dependency-closure checkpoint: this is limited to duplicate OPF
  guide/bindings diagnostics and first-branch import/diagnostic precedence for
  those optional package children. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,904 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php lanes/pandoc/tests/DocxReaderTest.php`
  passed 4 files, 2,663 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 8,937 assertions, 0 failures, with 744
  counted `PASS` lines.

## 2026-06-22 EPUB3 Duplicate Package Branch Scoping Slice

- Focused PHP coverage is now 743 behavior tests with 8,914 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now scopes package-level diagnostics and ID lookups to the
  primary OPF `metadata`, `manifest`, and `spine` branches, matching the
  import precedence already used for duplicate package children.
- Duplicate metadata/spine branches no longer satisfy package
  `unique-identifier` lookup or metadata `refines` target lookup, and no
  longer leak package-link, missing-resource, rendition metadata, or spine
  rendition-property diagnostics.
- The focused regression keeps valid primary metadata, links, manifest, spine,
  and readable content import intact while reporting the duplicate package
  branches and the now-missing primary-branch `unique-identifier`/`refines`
  targets.
- Dependency-closure checkpoint: this is limited to duplicate package branch
  scoping for package diagnostics and ID lookup. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,881 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubPackageMetadataReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php lanes/pandoc/tests/DocxReaderTest.php`
  passed 4 files, 2,640 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 8,914 assertions, 0 failures, with 743
  counted `PASS` lines.

## 2026-06-22 EPUB3 Unexpected Package Child Diagnostics Slice

- Focused PHP coverage is now 742 behavior tests with 8,888 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports unexpected direct OPF/no-namespace package root
  children outside the package content model instead of silently ignoring them.
- The reader now emits `invalid-package-child-element` diagnostics when a
  direct package child is not one of `metadata`, `manifest`, `spine`, `guide`,
  `bindings`, or `collection`.
- Valid readable spine import still proceeds for packages that include this
  diagnostic, so WordPress intake can surface the package-structure issue
  without dropping valid content.
- Dependency-closure checkpoint: this is limited to unexpected direct package
  child diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,855 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,682
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,888 assertions, 0 failures, with 742 counted `PASS` lines.

## 2026-06-22 EPUB3 Duplicate Package Required-Child Diagnostics Slice

- Focused PHP coverage is now 741 behavior tests with 8,880 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports duplicate top-level OPF `metadata`, `manifest`, and
  `spine` elements instead of silently merging or ignoring alternate package
  branches.
- The reader now emits `duplicate-package-metadata`,
  `duplicate-package-manifest`, and `duplicate-package-spine` diagnostics when
  more than one OPF metadata/manifest/spine child appears.
- Duplicate metadata is no longer merged into imported package metadata; the
  reader keeps import precedence on the first valid OPF metadata, manifest,
  and spine elements so alternate duplicate branches do not leak into title,
  language, manifest resources, or readable spine content.
- Dependency-closure checkpoint: this is limited to duplicate OPF
  metadata/manifest/spine diagnostics and direct import precedence for those
  package children. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,847 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,674
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,880 assertions, 0 failures, with 741 counted `PASS` lines.

## 2026-06-22 EPUB3 Package Manifest/Spine Namespace Diagnostics Slice

- Focused PHP coverage is now 740 behavior tests with 8,858 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports foreign-namespace top-level OPF manifest/spine
  lookalikes instead of only relying on missing-manifest or missing-spine
  fallout.
- The reader now emits `invalid-package-manifest-namespace` and
  `invalid-package-spine-namespace` diagnostics when `manifest` or `spine`
  lookalikes appear outside the OPF namespace.
- Foreign-namespace package manifest/spine lookalikes remain excluded from
  manifest resource import and readable spine content import; valid OPF
  manifest and spine elements still import normally.
- Dependency-closure checkpoint: this is limited to OPF package manifest/spine
  namespace diagnostics and direct manifest/spine import exclusion. It does
  not claim full EPUBCheck validation, full upstream Haskell runner parity,
  full EPUB fixture corpus parity, browser rendering behavior, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,825 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,652
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,858 assertions, 0 failures, with 740 counted `PASS` lines.

## 2026-06-22 EPUB3 Guide/Bindings Namespace And Content-Model Diagnostics Slice

- Focused PHP coverage is now 739 behavior tests with 8,844 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now makes OPF guide/reference and bindings/mediaType handling
  namespace-sensitive while retaining namespace-less legacy recovery.
- The reader now emits `invalid-guide-namespace`,
  `invalid-guide-reference-namespace`, `invalid-guide-child-element`,
  `invalid-bindings-namespace`, `invalid-binding-media-type-namespace`, and
  `invalid-bindings-child-element` diagnostics when foreign-namespace
  guide/reference/bindings/mediaType lookalikes or unexpected guide/bindings
  children appear.
- Foreign-namespace guide and bindings lookalikes are no longer imported into
  `epubGuideReferences` or `epubBindings`; valid OPF guide references and
  bindings still import normally.
- Dependency-closure checkpoint: this is limited to OPF guide/bindings
  namespace and direct child content-model diagnostics plus direct guide and
  bindings import. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,811 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,638
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,844 assertions, 0 failures, with 739 counted `PASS` lines.

## 2026-06-22 EPUB3 Collection Metadata Child Namespace Diagnostics Slice

- Focused PHP coverage is now 738 behavior tests with 8,814 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now makes OPF collection metadata/link namespace handling
  symmetric with package metadata, including collection metadata ID checks and
  `epubCollections` metadata/link import.
- The reader now emits `invalid-collection-metadata-namespace`,
  `invalid-collection-meta-namespace`, and `invalid-collection-link-namespace`
  diagnostics when foreign-namespace `metadata`/`meta`/`link` lookalikes
  appear under OPF collections.
- Foreign-namespace collection metadata child lookalikes are no longer
  imported into `epubCollections` metadata or link records; valid OPF
  collection metadata and links still import normally.
- Dependency-closure checkpoint: this is limited to OPF collection-local
  metadata/link namespace diagnostics and direct collection metadata/link
  import. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, browser rendering behavior,
  generated multi-rendition authoring beyond preserved OPF payloads, DRM
  decryption, XML signature cryptographic validation, generic TeX-to-MathML
  conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,781 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,608
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,814 assertions, 0 failures, with 738 counted `PASS` lines.

## 2026-06-22 EPUB3 Package Metadata Child Namespace Diagnostics Slice

- Focused PHP coverage is now 737 behavior tests with 8,788 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now makes OPF package metadata `meta`/`link` import, package
  metadata element validation, metadata refinement detection, modified
  metadata checks, and media-overlay metadata checks namespace-sensitive while
  retaining namespace-less legacy recovery.
- The reader now emits `invalid-package-meta-namespace` and
  `invalid-package-link-namespace` diagnostics when foreign-namespace
  `meta`/`link` lookalikes appear under OPF metadata.
- Foreign-namespace metadata child lookalikes are no longer imported into
  `epubMetadataProperties` or `epubMetadataLinks`; valid OPF metadata records
  still import normally.
- Dependency-closure checkpoint: this is limited to OPF package metadata child
  namespace diagnostics and direct package metadata child import. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, browser rendering behavior, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,755 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,582
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,788 assertions, 0 failures, with 737 counted `PASS` lines.

## 2026-06-22 EPUB3 Package Collection Namespace Diagnostics Slice

- Focused PHP coverage is now 736 behavior tests with 8,766 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now makes OPF package collection import, package-link
  detection, collection child traversal, and collection membership checks
  namespace-sensitive while retaining namespace-less legacy recovery.
- The reader now emits `invalid-package-collection-namespace` diagnostics when
  foreign-namespace `collection` lookalikes appear under the OPF package.
- Foreign-namespace collection lookalikes are no longer imported into
  `epubCollections`; valid OPF collection metadata and link records still
  import normally.
- Dependency-closure checkpoint: this is limited to OPF package collection
  namespace diagnostics and direct collection import. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,733 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,560
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,766 assertions, 0 failures, with 736 counted `PASS` lines.

## 2026-06-22 EPUB3 Package Metadata Namespace Diagnostics Slice

- Focused PHP coverage is now 735 behavior tests with 8,752 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now selects direct OPF package `metadata` records with
  namespace awareness while retaining namespace-less legacy recovery.
- The reader now emits `invalid-package-metadata-namespace` diagnostics when
  foreign-namespace `metadata` lookalikes appear under the OPF package.
- Foreign-namespace metadata lookalikes are no longer imported as package
  metadata; valid OPF metadata still imports into title, identifier, language,
  and package metadata records normally.
- Dependency-closure checkpoint: this is limited to OPF package metadata
  namespace diagnostics and direct package metadata extraction. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, browser rendering behavior, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,719 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,546
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,752 assertions, 0 failures, with 735 counted `PASS` lines.

## 2026-06-22 EPUB3 Package Child Namespace Diagnostics Slice

- Focused PHP coverage is now 734 behavior tests with 8,741 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now selects direct OPF `manifest`, `spine`, `item`, and
  `itemref` package records with namespace awareness while retaining
  namespace-less legacy recovery.
- The reader now emits `invalid-manifest-item-namespace` and
  `invalid-spine-itemref-namespace` diagnostics when foreign-namespace
  `item`/`itemref` lookalikes appear under OPF manifest or spine.
- Foreign-namespace lookalikes are no longer imported as manifest resources or
  spine entries; valid OPF records still import normally.
- Dependency-closure checkpoint: this is limited to OPF package child
  namespace diagnostics and direct manifest/spine extraction. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, browser rendering behavior, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,708 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,535
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,741 assertions, 0 failures, with 734 counted `PASS` lines.

## 2026-06-22 EPUB3 Unexpected Manifest Child Element Diagnostics Slice

- Focused PHP coverage is now 733 behavior tests with 8,727 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates the OPF manifest content model enough to report
  direct non-`item` element children.
- The reader now emits `invalid-manifest-child-element` diagnostics with the
  offending child element name when a malformed manifest includes direct
  elements other than `item`.
- The diagnostic preserves readable spine import, existing manifest item
  diagnostics, and valid manifest resource metadata.
- Dependency-closure checkpoint: this is limited to unexpected direct manifest
  child element diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, generated multi-rendition authoring beyond preserved OPF
  payloads, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,694 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,521
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,727 assertions, 0 failures, with 733 counted `PASS` lines.

## 2026-06-22 EPUB3 Unexpected Spine Child Element Diagnostics Slice

- Focused PHP coverage is now 732 behavior tests with 8,720 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates the OPF spine content model enough to report
  direct non-`itemref` element children.
- The reader now emits `invalid-spine-child-element` diagnostics with the
  offending child element name when a malformed spine includes direct elements
  other than `itemref`.
- The diagnostic preserves readable spine import, existing itemref diagnostics,
  and metadata for valid spine entries.
- Dependency-closure checkpoint: this is limited to unexpected direct spine
  child element diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, generated multi-rendition authoring beyond preserved OPF
  payloads, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,687 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,514
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,720 assertions, 0 failures, with 732 counted `PASS` lines.

## 2026-06-22 EPUB3 Duplicate Spine Itemref IDREF Diagnostics Slice

- Focused PHP coverage is now 731 behavior tests with 8,713 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates OPF spine itemrefs so the same manifest item id is
  not referenced by more than one spine `itemref`.
- The reader now emits `duplicate-spine-itemref-idref` diagnostics with the
  target `idref`, repeated itemref id, first itemref id, manifest `href`, and
  media type context when a duplicate spine reference is encountered.
- The diagnostic preserves readable spine import, non-linear itemref metadata,
  first/repeated itemref context, and existing missing/invalid idref diagnostic
  behavior.
- Dependency-closure checkpoint: this is limited to duplicate spine itemref
  IDREF diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,680 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,507
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,713 assertions, 0 failures, with 731 counted `PASS` lines.

## 2026-06-22 EPUB3 Spine Foreign-Content Fallback Diagnostics Slice

- Focused PHP coverage is now 730 behavior tests with 8,701 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates OPF spine itemrefs that reference foreign content
  document manifest items. Foreign spine items must provide a manifest fallback
  chain that reaches an EPUB content document.
- The reader now emits `missing-spine-fallback-content-document` diagnostics
  with `missing-fallback`, `chain-ended`, `missing-target`, or `cycle` reasons
  when a foreign spine item's fallback chain does not reach XHTML or SVG EPUB
  content.
- The diagnostic preserves valid XHTML/SVG spine items, valid foreign spine
  fallback chains, existing readable XHTML fallback import, raw manifest
  metadata, and readable spine import from other items.
- Dependency-closure checkpoint: this is limited to spine foreign-content
  fallback-chain diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, generated multi-rendition authoring beyond preserved OPF
  payloads, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,668 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,495
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,701 assertions, 0 failures, with 730 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Property-Prefix Diagnostics Slice

- Focused PHP coverage is now 729 behavior tests with 8,679 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates remaining OPF property data type prefix uses
  against the union of reserved EPUB package prefixes and valid declarations in
  the package `prefix` attribute.
- The reader now emits `undeclared-package-link-rel-prefix`,
  `undeclared-package-link-property-prefix`,
  `undeclared-manifest-property-prefix`,
  `undeclared-spine-itemref-property-prefix`,
  `undeclared-collection-metadata-property-prefix`, and
  `undeclared-collection-metadata-scheme-prefix` diagnostics.
- The new diagnostics cover package metadata and collection `link@rel` /
  `link@properties`, manifest `item@properties`, spine
  `itemref@properties`, and collection metadata `property` / `scheme`
  values while preserving unprefixed default-vocabulary values, reserved
  prefixes, declared custom prefixes, raw metadata import, and readable spine
  import.
- Dependency-closure checkpoint: this is limited to OPF property-prefix
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,646 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,473
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,679 assertions, 0 failures, with 729 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Package Metadata Undeclared-Prefix Diagnostics Slice

- Focused PHP coverage is now 728 behavior tests with 8,643 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates package metadata property data type prefixes
  against the union of reserved EPUB package prefixes and valid declarations in
  the package `prefix` attribute.
- The reader now emits `undeclared-package-meta-property-prefix` diagnostics
  for EPUB3 package `meta@property` values whose prefixes are neither reserved
  nor declared.
- The reader now emits `undeclared-package-meta-scheme-prefix` diagnostics for
  EPUB3 package `meta@scheme` values whose prefixes are neither reserved nor
  declared.
- The new diagnostics preserve unprefixed default-vocabulary values, reserved
  prefixes such as `dcterms` and `onix`, declared custom prefixes, raw metadata
  import, and readable spine import.
- Dependency-closure checkpoint: this is limited to OPF package metadata
  property/scheme prefix-use diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,610 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,437
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,643 assertions, 0 failures, with 728 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Package Reserved-Prefix Override Diagnostics Slice

- Focused PHP coverage is now 727 behavior tests with 8,626 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now recognizes reserved EPUB package-document prefixes and
  reports declarations that remap those reserved names to unrelated IRIs.
- The reader now emits `overridden-package-prefix` warnings for reserved
  package prefix overrides while preserving accepted reserved mappings,
  including the existing `schema: https://schema.org/` fixture convention.
- The new warning bucket preserves raw `epubPackagePrefix` metadata and
  readable spine import, so WordPress EPUB intake can surface suspicious
  vocabulary remapping without dropping recoverable package content.
- Dependency-closure checkpoint: this is limited to OPF package reserved-prefix
  override diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, generated multi-rendition authoring beyond preserved OPF
  payloads, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,593 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,420
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,626 assertions, 0 failures, with 727 counted `PASS` lines.

## 2026-06-22 EPUB OPF Package Version Diagnostics Slice

- Focused PHP coverage is now 726 behavior tests with 8,613 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now distinguishes numeric but unsupported OPF package version
  values from malformed non-numeric package versions.
- The reader now emits `unsupported-package-version` diagnostics when an OPF
  package declares a version outside the supported `2.0` and `3.0` values.
- The new diagnostic preserves the raw `epubPackageVersion` metadata and
  readable spine import, so WordPress EPUB intake can surface unsupported
  package-version evidence without dropping recoverable content.
- Dependency-closure checkpoint: this is limited to OPF package version
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, generated multi-rendition authoring beyond preserved OPF
  payloads, DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,580 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,407
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,613 assertions, 0 failures, with 726 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Package Prefix Diagnostics Slice

- Focused PHP coverage is now 725 behavior tests with 8,604 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates additional OPF package `prefix` declaration
  constraints after the existing pair/name/absolute-IRI checks.
- The reader now emits `duplicate-package-prefix` diagnostics when the same
  package prefix name is declared more than once, retaining the first IRI in
  diagnostic context.
- The reader now emits `reserved-package-prefix` diagnostics when the package
  declares the EPUB-reserved `_` prefix.
- The new diagnostic buckets preserve the raw `epubPackagePrefix` metadata
  and readable spine import, so WordPress EPUB intake can surface malformed
  prefix declarations without dropping recoverable package content.
- Dependency-closure checkpoint: this is limited to OPF package prefix
  duplicate/reserved-name diagnostics. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output
  behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,571 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,398
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,604 assertions, 0 failures, with 725 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Manifest Href Path Diagnostics Slice

- Focused PHP coverage remains 724 behavior tests with 8,592 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates OPF manifest `item@href` values against the
  EPUB 3.3 requirement that manifest publication-resource IRIs be absolute
  URLs or path-relative scheme-less URLs.
- The reader now emits `invalid-manifest-href-path` diagnostics for local
  manifest href shapes that the previous ZIP-path normalizer could hide:
  leading-slash absolute paths, protocol-relative URLs, and backslash paths.
- The new diagnostic bucket is deliberately separate from existing
  `invalid-manifest-data-url`, `invalid-manifest-file-url`,
  remote-resource diagnostics, duplicate-href diagnostics, and
  `missing-manifest-resource`, so invalid local URL shapes do not also
  produce misleading missing ZIP-entry errors.
- Dependency-closure checkpoint: this is limited to manifest href path shape
  diagnostics. It does not claim full EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, browser rendering
  behavior, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature cryptographic validation, generic
  TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: W3C EPUB 3.3 manifest examples and requirements document
  manifest `item@href` as absolute URLs or path-relative scheme-less URLs;
  `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,559 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,386
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,592 assertions, 0 failures, with 724 counted `PASS` lines.

## 2026-06-22 EPUB3 OCF Rootfile Resource Diagnostics Slice

- Focused PHP coverage is now 724 behavior tests with 8,584 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates that OPF-typed EPUB3 OCF
  `META-INF/container.xml` rootfile `full-path` values resolve to entries in
  the EPUB ZIP archive.
- The reader now emits `missing-container-rootfile-resource` diagnostics for
  OPF-typed rootfiles whose normalized package-relative path is absent from
  the archive.
- The resource check is deliberately separate from malformed full-path
  diagnostics and invalid or non-OPF media-type diagnostics, so remote/unsafe
  rootfile paths and non-OPF fallbacks do not also produce misleading missing
  ZIP-entry errors.
- Dependency-closure checkpoint: this is limited to OCF rootfile resource
  existence diagnostics. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, browser
  rendering behavior, generated multi-rendition authoring beyond preserved
  OPF payloads, DRM decryption, XML signature cryptographic validation,
  generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,551 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,378
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,584 assertions, 0 failures, with 724 counted `PASS` lines.

## 2026-06-22 EPUB3 OCF Rootfile Full-Path Diagnostics Slice

- Focused PHP coverage is now 723 behavior tests with 8,571 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OCF `META-INF/container.xml` rootfile
  `full-path` values before they participate in OPF rootfile selection.
- The reader now emits `invalid-container-rootfile-full-path` diagnostics for
  unsafe or non-archive rootfile paths, including `data:`, `file:`, remote
  absolute URLs, leading-slash absolute paths, query/fragment suffixes,
  parent-directory traversal, and paths that normalize to an empty archive
  entry.
- OPF rootfile selection now ignores those invalid full-path values instead
  of letting `normalizeZipPath()` silently fold them into plausible archive
  paths; valid primary OPF import and malformed rootfile metadata preservation
  remain intact.
- Dependency-closure checkpoint: this is limited to OCF rootfile full-path
  diagnostics and rootfile selection hardening. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,538 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,365
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,571 assertions, 0 failures, with 723 counted `PASS` lines.

## 2026-06-22 EPUB3 OCF Rootfile ID and Property Diagnostics Slice

- Focused PHP coverage is now 722 behavior tests with 8,554 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OCF `META-INF/container.xml` rootfile
  `id` and `properties` attributes in the container diagnostics pass.
- The reader now emits `invalid-container-rootfile-id`,
  `duplicate-container-rootfile-id`, and
  `invalid-container-rootfile-property` diagnostics while continuing to
  preserve malformed rootfile metadata for review.
- OPF rootfile selection remains based on the first usable OPF media type,
  and malformed non-selected rootfile metadata no longer disappears just
  because the selected OPF package imports successfully.
- Dependency-closure checkpoint: this is limited to OCF rootfile ID/property
  diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,521 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,348
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,554 assertions, 0 failures, with 722 counted `PASS` lines.

## 2026-06-22 EPUB3 OCF Container Link Attribute Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,537 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OCF `META-INF/container.xml` link
  relation, language, direction, refines, and properties attributes in the
  container diagnostics pass.
- The reader now emits `invalid-container-link-rel`,
  `invalid-container-link-hreflang`, `invalid-container-link-dir`,
  `invalid-container-link-refines`, and `invalid-container-link-property`
  diagnostics while continuing to preserve the malformed link metadata for
  review.
- Missing required container-link fields, unsafe `data:`/`file:` URL checks,
  invalid media-type checks, missing local-resource diagnostics, and remote
  link ZIP-resource skips remain separate diagnostic buckets.
- Dependency-closure checkpoint: this is limited to OCF container-link
  attribute diagnostics and diagnostic context. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,504 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,331
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,537 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OCF Container Link Unsafe URL Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,516 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OCF `META-INF/container.xml` `link@href`
  values for unsafe `data:` and `file:` URL schemes in the container
  diagnostics pass.
- The reader now emits `invalid-container-link-data-url` and
  `invalid-container-link-file-url` diagnostics with container link id, href,
  rel, and media-type context when OCF container links use those schemes.
- Missing local container-link resource diagnostics remain separate, remote
  container links continue to be skipped by ZIP-resource checks, and unsafe
  URLs no longer disappear simply because they are non-package-relative.
- Dependency-closure checkpoint: this is limited to OCF container-link unsafe
  URL diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,483 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,310
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,516 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Package Link Unsafe URL Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,508 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OPF package and collection `link@href`
  values for unsafe `data:` and `file:` URL schemes in the shared package-link
  attribute diagnostics pass.
- The reader now emits `invalid-package-link-data-url` and
  `invalid-package-link-file-url` diagnostics for package metadata links and
  collection links that use those schemes.
- Missing local package-link resource diagnostics remain separate, remote
  package links continue to be skipped by ZIP-resource checks, and unsafe URLs
  no longer disappear simply because they are non-package-relative.
- Dependency-closure checkpoint: this is limited to package-link unsafe URL
  diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,475 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,302
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,508 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Package Link Relation Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,494 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OPF package and collection `link@rel`
  token values in the shared package-link attribute diagnostics pass.
- The reader now emits `invalid-package-link-rel` diagnostics when a package
  metadata or collection link relation token is not a valid property data type
  value, for example an empty prefixed reference such as `bad:`.
- Missing relation diagnostics remain separate as `missing-package-link-rel`,
  and malformed `link@properties` tokens remain separate as
  `invalid-package-link-property`.
- Dependency-closure checkpoint: this is limited to package-link relation
  diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,461 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,288
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,494 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Spine Itemref Properties Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,485 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OPF spine `itemref@properties` token
  values in the spine diagnostics pass.
- The reader now emits `invalid-spine-itemref-property` diagnostics when a
  spine itemref property token is not a valid property data type value, for
  example an empty prefixed reference such as `bad:`.
- Existing rendition-specific spine property diagnostics remain separate, so
  unsupported `rendition:*` values still use `invalid-rendition-spine-property`
  while malformed generic property tokens use the new itemref syntax bucket.
- Dependency-closure checkpoint: this is limited to spine itemref properties
  diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,452 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,279
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,485 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Manifest Item Properties Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,481 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OPF manifest `item@properties` token
  values before the existing required-property scans use those declarations.
- The reader now emits `invalid-manifest-property` diagnostics when a manifest
  item property token is not a valid property data type value, for example an
  empty prefixed reference such as `bad:`.
- Manifest media type, fallback/fallback-style/media-overlay IDREF,
  required-property, remote-resource, and local resource diagnostics remain
  separate, while valid manifest property tokens continue to drive existing
  nav/mathml/scripted/svg/switch/remote-resources checks.
- Dependency-closure checkpoint: this is limited to manifest item properties
  diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,448 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,275
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,481 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Package Link Properties Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,476 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OPF `link@properties` token values for
  direct package metadata links and collection-local links.
- The reader now emits `invalid-package-link-property` diagnostics when a
  package or collection link property token is not a valid property data type
  value, for example an empty prefixed reference such as `bad:`.
- Required `href`/`rel`, optional `media-type`/`hreflang`/`dir`, and missing
  local resource diagnostics remain separate, while valid link property tokens
  remain preserved with the link metadata records.
- Dependency-closure checkpoint: this is limited to package-link properties
  diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,443 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,270
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,476 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Metadata Scheme Value Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,469 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OPF metadata `meta@scheme` values for
  direct package metadata and collection-local metadata.
- The reader now emits `invalid-package-meta-scheme` and
  `invalid-collection-metadata-scheme` diagnostics when a metadata scheme
  value is not a valid property data type value, for example when it contains
  whitespace.
- Existing metadata `property` diagnostics remain separate from malformed
  `scheme` diagnostics, and metadata records are still preserved for review.
- Dependency-closure checkpoint: this is limited to metadata scheme-value
  diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,436 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,263
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,469 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Metadata Property Value Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,457 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates non-empty EPUB3 OPF metadata `meta@property` values
  for direct package metadata and collection-local metadata.
- The reader now emits `invalid-package-meta-property` and
  `invalid-collection-metadata-property` diagnostics when a metadata property
  value is not a valid property data type value, for example when it contains
  whitespace or has an empty prefixed reference.
- Missing property diagnostics remain separate from malformed non-empty
  property diagnostics, and malformed metadata is still preserved for review
  instead of being dropped.
- Dependency-closure checkpoint: this is limited to metadata property-value
  diagnostics and diagnostic context. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,424 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,251
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,457 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Collection Link ID Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,447 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates direct EPUB3 OPF collection member `<link>` `id`
  attributes as XML NCNames and tracks duplicate collection link IDs.
- The reader now emits `invalid-collection-link-id` and
  `duplicate-collection-link-id` diagnostics with the offending link element,
  link ID, containing collection ID, and previous element context.
- Existing `invalid-collection-link-refines` diagnostics now use the same
  link-specific context, so review packets identify `element=link` and the
  containing collection separately instead of collapsing the element onto the
  collection node.
- Dependency-closure checkpoint: this is limited to collection member link
  identity diagnostics and diagnostic context. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,414 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,241
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,447 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Collection ID Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,436 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates EPUB3 OPF `collection` `id` attributes as XML
  NCNames before continuing through role, member, metadata, and link checks.
- The reader now emits `invalid-collection-id` for malformed collection IDs
  and `duplicate-collection-id` when a valid collection ID appears more than
  once in the package.
- The bounded malformed collection fixture keeps valid links on the new
  malformed-ID and duplicate-ID collections, so the diagnostics isolate
  collection identity failures without adding missing-member noise.
- Dependency-closure checkpoint: this is limited to collection identity
  diagnostics and preservation of readable spine import. It does not claim
  full EPUBCheck validation, full upstream Haskell runner parity, full EPUB
  fixture corpus parity, browser rendering behavior, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or
  full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,403 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,230
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,436 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Collection Metadata Property Diagnostics Slice

- Focused PHP coverage remains 721 behavior tests with 8,429 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports collection-local EPUB3 OPF metadata `<meta>`
  elements that omit both the required `property` attribute and the legacy
  OPF2 `name` attribute.
- The reader now emits `missing-collection-metadata-property` diagnostics with
  the meta element, ID, collection ID, and text value context for review
  routing.
- OPF2-style collection metadata stays a separate
  `invalid-collection-opf2-meta` diagnostic, so untyped EPUB3 collection
  metadata and legacy named collection metadata remain distinct review buckets.
- Dependency-closure checkpoint: this is limited to collection-local metadata
  property diagnostics and metadata preservation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,396 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,223
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,429 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Package Meta Property Diagnostics Slice

- Focused PHP coverage now includes 721 behavior tests with 8,425 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now reports EPUB3 package metadata `<meta>` elements that omit
  both the required EPUB3 `property` attribute and legacy OPF2 `name`
  attribute.
- The reader now emits `missing-package-meta-property` diagnostics with the
  meta element, ID, and text value context for review routing.
- OPF2-style package metadata stays a separate
  `invalid-package-opf2-meta` diagnostic, so untyped EPUB3 metadata and legacy
  named metadata are no longer collapsed into the same review bucket.
- Dependency-closure checkpoint: this is limited to direct package metadata
  `meta` property diagnostics and metadata preservation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,392 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,219
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,425 assertions, 0 failures, with 721 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Package OPF2 Meta Diagnostics Slice

- Focused PHP coverage now includes 720 behavior tests with 8,415 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates direct package metadata `<meta>` elements in
  EPUB3 packages and reports OPF2-style `<meta name="...">` records that lack
  EPUB3 `property` attributes.
- The reader now emits `invalid-package-opf2-meta` diagnostics with the meta
  element, ID, name, and content context for review routing.
- Legacy package metadata is still preserved where useful: the
  `meta name="cover"` fallback continues to populate the legacy cover item ID
  and cover image path when the referenced manifest item exists.
- Collection-local OPF2-style metadata remains covered separately by
  `invalid-collection-opf2-meta`.
- Dependency-closure checkpoint: this is limited to direct package metadata
  OPF2-style meta diagnostics and metadata preservation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,382 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,209
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,415 assertions, 0 failures, with 720 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Collection Metadata ID Diagnostics Slice

- Focused PHP coverage remains 719 behavior tests with 8,403 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates `id` attributes on direct children of
  collection-local OPF `<metadata>` blocks, not only direct package metadata
  children.
- The reader now emits `invalid-collection-metadata-id` for collection
  metadata child IDs that are not XML NCNames and
  `duplicate-collection-metadata-id` when a valid collection metadata ID is
  reused within the same collection metadata element.
- Diagnostic context includes the offending metadata child, ID, useful
  metadata fields such as `property`, collection element, and collection ID,
  so WordPress import review can separate collection-local metadata identity
  problems from package-level metadata ID problems.
- Existing collection diagnostics continue to report OPF2-style metadata,
  malformed collection metadata `refines`, outside-collection references, and
  invalid collection link refinements without losing readable spine import.
- Dependency-closure checkpoint: this is limited to collection-local OPF
  metadata ID diagnostics and metadata preservation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,370 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,197
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,403 assertions, 0 failures, with 719 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Package Link Attribute Diagnostics Slice

- Focused PHP coverage now includes 719 behavior tests with 8,391 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates optional OPF package-link attributes on both
  direct package metadata links and collection links after the existing
  required `href`/`rel` checks.
- The reader now emits `invalid-package-link-media-type`,
  `invalid-package-link-hreflang`, and `invalid-package-link-dir` diagnostics
  when package or collection `<link>` elements carry malformed `media-type`,
  `hreflang`, or `dir` values.
- Diagnostic context includes the link element, `id`, `rel`, parent element,
  `href` when present, and the offending attribute value, so WordPress import
  review can route malformed external-record attributes separately from missing
  resource and missing required-attribute diagnostics.
- Valid local package/collection links remain unreported, existing local ZIP
  resource checks still catch absent local link targets, and remote metadata
  links remain skipped by missing-resource checks.
- Dependency-closure checkpoint: this is limited to OPF package/collection
  link attribute diagnostics and metadata preservation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,358 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,185
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,391 assertions, 0 failures, with 719 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Collection Metadata Refines Diagnostics Slice

- Focused PHP coverage remains 718 behavior tests with 8,362 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates collection-local OPF metadata `refines`
  attributes separately from direct package metadata refinements, so collection
  metadata errors are no longer double-reported by the package metadata scanner.
- The reader now emits `invalid-collection-metadata-refines` diagnostics for
  collection metadata with empty `refines` attributes, non-fragment values,
  empty fragments, and fragments whose target token is not an XML ID/NCName.
- Well-formed collection metadata `#id` refinements still use
  `collection-metadata-refines-outside` when the target is outside the
  containing collection.
- Dependency-closure checkpoint: this is limited to collection-local OPF
  metadata `refines` syntax diagnostics and metadata preservation. It does not
  claim full EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, browser rendering behavior, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature cryptographic validation, generic TeX-to-MathML conversion, or full
  EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,329 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,156
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,362 assertions, 0 failures, with 718 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Metadata Refines Diagnostics Slice

- Focused PHP coverage now includes 718 behavior tests with 8,354 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates OPF package metadata `refines` attributes before
  target lookup, so malformed refinement IRIs are no longer silently skipped.
- The reader now emits `invalid-metadata-refines` diagnostics for empty
  `refines` attributes, non-fragment values, empty fragments, and fragments
  whose target token is not an XML ID/NCName.
- Valid `#id` refinements continue through the existing package element target
  lookup, so `missing-metadata-refines-target` remains reserved for well-formed
  `refines` values whose target ID is absent.
- Dependency-closure checkpoint: this is limited to OPF metadata `refines`
  syntax diagnostics and metadata preservation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,321 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,148
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,354 assertions, 0 failures, with 718 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Metadata ID Diagnostics Slice

- Focused PHP coverage now includes 717 behavior tests with 8,333 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now validates direct OPF package metadata child `id`
  attributes while still preserving metadata records and readable spine import.
- The reader now emits `invalid-metadata-id` diagnostics for metadata child
  IDs that are not XML NCNames and `duplicate-metadata-id` diagnostics when
  metadata child IDs repeat within the same metadata element.
- Diagnostic context includes the metadata child element, offending `id`, prior
  element for duplicates, and useful metadata fields such as `property`,
  `name`, `rel`, `href`, or Dublin Core text values where available.
- Dependency-closure checkpoint: this is limited to OPF metadata ID
  diagnostics and metadata preservation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,300 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,127
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,333 assertions, 0 failures, with 717 counted `PASS` lines.

## 2026-06-22 EPUB3 OPF Guide Reference Diagnostics Slice

- Focused PHP coverage includes 716 behavior tests with 8,314 full-suite
  assertions across the Pandoc lane.
- `EpubReader` validates legacy OPF `<guide><reference>` entries while still
  preserving them in `epubGuideReferences`.
- The reader emits `missing-guide-reference-type`,
  `missing-guide-reference-href`, `missing-guide-reference-resource`, and
  `missing-guide-reference-fragment` diagnostics with guide reference type,
  title, href, normalized path, and fragment context where available.
- Missing-resource checks are limited to package-relative guide hrefs. Remote
  guide references, such as externally hosted cover images, remain external
  metadata instead of being reported as absent ZIP entries.
- Dependency-closure checkpoint: this is limited to OPF guide-reference
  diagnostics and metadata preservation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
  Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,281 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,108
  assertions, 0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed
  11 files, 8,314 assertions, 0 failures, with 716 counted `PASS` lines.

## 2026-06-22 EPUB3 OCF Container Link Diagnostics Slice

- Focused PHP coverage now includes 715 behavior tests with 8,295 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now preserves malformed OCF `container.xml` link records in
  `epubContainerLinks`, including records with missing `href` or missing `rel`,
  so external-record metadata remains inspectable during WordPress import
  review.
- The reader now emits `missing-container-link-href`,
  `missing-container-link-rel`, `invalid-container-link-media-type`, and
  `missing-container-link-resource` diagnostics with container link index and
  attribute context.
- Missing-resource checks are limited to package-relative container links.
  Remote container metadata links are preserved as external references instead
  of being reported as absent ZIP entries.
- Dependency-closure checkpoint: this is limited to OCF container link
  diagnostics and metadata preservation. It does not claim full EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, browser rendering behavior, generated multi-rendition authoring
  beyond preserved OPF payloads, DRM decryption, XML signature cryptographic
  validation, generic TeX-to-MathML conversion, or full EPUB2 output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,262 assertions, 0
  failures, with 64 counted `PASS` lines in
  `/tmp/pandoc-epub-reader-container-link-diagnostics-20260622T101117Z.log`;
  `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,089
  assertions, 0 failures, with 117 counted `PASS` lines in
  `/tmp/pandoc-epub-container-link-diagnostics-adjacent-20260622T101118Z.log`;
  `php tools/run-tests.php lanes/pandoc/tests` passed 11 files, 8,295
  assertions, 0 failures, with 715 counted `PASS` lines in
  `/tmp/pandoc-full-epub-container-link-diagnostics-20260622T101117Z.log`.

## 2026-06-22 EPUB3 OCF Container Metadata Diagnostics Slice

- Focused PHP coverage now includes 714 behavior tests with 8,270 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now preserves OCF `container.xml` rootfile entries even when
  `full-path` normalizes empty, so malformed rootfile metadata remains
  inspectable in `epubContainerRootfiles`.
- The reader now emits `invalid-container-root`, `invalid-container-version`,
  `missing-container-rootfile-full-path`,
  `missing-container-rootfile-media-type`, and
  `invalid-container-rootfile-media-type` diagnostics with rootfile index and
  attribute context.
- Fallback rootfile selection still imports a readable OPF package when no
  OPF-typed rootfile exists but the container has a usable path, matching the
  existing recovery behavior while surfacing the conformance errors.
- Dependency-closure checkpoint: this is limited to OCF container metadata
  diagnostics and fallback import preservation. It does not claim full
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, browser rendering behavior, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
  output behavior.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php` and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,237 assertions, 0
  failures, with 63 counted `PASS` lines in
  `/tmp/pandoc-epub-reader-container-diagnostics-20260622T100528Z.log`; `php
  tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
  lanes/pandoc/tests/EpubWriterTest.php
  lanes/pandoc/tests/PandocConverterTest.php
  lanes/pandoc/tests/PandocFormatRegistryTest.php` passed 4 files, 2,064
  assertions, 0 failures, with 116 counted `PASS` lines in
  `/tmp/pandoc-epub-container-diagnostics-adjacent-20260622T100528Z.log`;
  `php tools/run-tests.php lanes/pandoc/tests` passed 11 files, 8,270
  assertions, 0 failures, with 714 counted `PASS` lines in
  `/tmp/pandoc-full-epub-container-diagnostics-20260622T100528Z.log`.

## 2026-06-22 EPUB3 Semantic HtmlAttributes Slice

- Focused PHP coverage remains 674 behavior tests with 7,352 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records bounded source XHTML `style` and `data-*`
  attributes on semantic spine descendants as
  `epubSpineItemRefs[*].semanticElements[*].htmlAttributes`.
- CSS resource references inside captured style metadata are normalized to
  package-relative paths, matching the existing resource URL normalization used
  for body XHTML and head style metadata.
- Source body descendants that are otherwise not EPUB/ARIA-bearing are now
  retained in semantic metadata when `style` or `data-*` attributes carry
  layout or migration provenance.
- Generated EPUB read-back now exposes writer-emitted `data-source` attributes
  in semantic metadata for figures, sections, and links.
- Dependency-closure checkpoint: this is limited to bounded source style/data
  metadata fidelity. It does not claim browser CSS/layout rendering parity,
  arbitrary DOM preservation, full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  validation, or generic TeX-to-MathML conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 1,023 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 7,352 assertions, 0 failures, with 674
  counted `PASS` lines in
  `/tmp/pandoc-full-epub-semantic-htmlattrs-20260622T031401Z.log`.

## 2026-06-22 EPUB3 XHTML Head Title Slice

- Focused PHP coverage remains 674 behavior tests with 7,351 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records direct XHTML spine `<head><title>` text as
  `epubSpineItemRefs[*].headTitle`, separate from package-level document
  metadata titles.
- `EpubWriter` now prefers metadata-provided per-spine `headTitle` values for
  generated chapter `<title>` elements and keeps the document metadata title as
  the fallback.
- The focused regressions cover source EPUB3 package intake, generated EPUB3
  package output, and native PHP read-back preserving the per-spine head title.
- Dependency-closure checkpoint: this is limited to XHTML head title metadata
  fidelity. It does not claim full EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, browser title/navigation
  rendering behavior, generated multi-rendition authoring beyond preserved OPF
  payloads, DRM decryption, XML signature validation, or generic
  TeX-to-MathML conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 1,022 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 7,351 assertions, 0 failures, with 674
  counted `PASS` lines in
  `/tmp/pandoc-full-epub-head-title-20260622T030528Z.log`.

## 2026-06-21 EPUB3 Nav Document Root/Body Attribute Slice

- Focused PHP coverage remains 660 behavior tests with 7,103 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records root `<html>` and `<body>` attributes from EPUB3
  `nav.xhtml` resources as `epubNavRootAttributes` and
  `epubNavBodyAttributes`, matching the bounded spine XHTML attribute surface:
  ids, roles, titles, ARIA labels, classes, language/direction, hidden state,
  root `prefix`, and body `epub:type`.
- `EpubWriter` now consumes the same metadata and emits those attributes on
  generated `nav.xhtml`, while keeping required XHTML and EPUB namespaces and
  default language attributes intact.
- The focused regressions cover direct nav.xhtml intake, generated nav.xhtml
  output, and native PHP read-back preserving both root and body attribute
  maps.
- Dependency-closure checkpoint: this is limited to nav document root/body
  attribute fidelity. It does not claim full EPUBCheck validation, full
  upstream Haskell runner parity, full EPUB fixture corpus parity, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature validation, or generic TeX-to-MathML conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 791 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 7,103 assertions, 0 failures, with 660
  counted `PASS` lines in
  `/tmp/pandoc-full-epub-nav-root-body-20260621T225450Z.log`.

## 2026-06-21 EPUB3 NCX Metadata Preservation Slice

- Focused PHP coverage remains 647 behavior tests with 6,880 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records NCX head metadata and document records from
  `toc.ncx`, including `dtb:uid`, `dtb:depth`, `dtb:totalPageCount`,
  `dtb:maxPageNumber`, custom head `<meta>` records, `docTitle`, and
  `docAuthor` values. The metadata is exposed as `epubNcxMetadata` plus
  shorthand `epubNcx*` fields.
- `EpubWriter` now consumes explicit NCX metadata when generating optional
  `toc.ncx`, preserving uid/depth/page-count values, custom non-core head
  records, document title, document authors, and the existing explicit
  play-order metadata.
- The focused regressions now cover NCX source metadata intake, generated NCX
  metadata output, custom head records, multiple authors, and native PHP
  read-back preserving those records.
- Dependency-closure checkpoint: this is limited to NCX head/doc metadata and
  play-order preservation. It does not claim full NCX vocabulary preservation,
  EPUBCheck validation, full upstream Haskell runner parity, full EPUB fixture
  corpus parity, generated multi-rendition authoring beyond preserved OPF
  payloads, DRM decryption, XML signature validation, or generic
  TeX-to-MathML conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 617 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,880 assertions, 0 failures, with 647
  counted `PASS` lines in
  `/tmp/pandoc-epub3-ncx-metadata-full-20260621.log`.

## 2026-06-21 EPUB3 NCX PlayOrder Preservation Slice

- Focused PHP coverage remains 647 behavior tests with 6,860 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records NCX `playOrder` from `navPoint` and `pageTarget`
  elements on TOC and page-list metadata. When both `nav.xhtml` and `toc.ncx`
  describe the same entry, deduplication now merges the NCX-only `playOrder`
  into the existing entry instead of dropping it.
- `EpubWriter` now accepts explicit `playOrder` on metadata-provided TOC and
  page-list entries and reuses it when generating optional `toc.ncx` navMap
  and pageList output, while continuing monotonic generated ordering for
  entries without source order metadata.
- The focused regressions now cover source NCX intake with nonsequential
  `playOrder` values for nav points and page targets, generated optional NCX
  output with explicit values, and native PHP read-back preserving those
  values.
- Dependency-closure checkpoint: this is limited to NCX `playOrder`
  preservation. It does not claim full NCX vocabulary preservation, EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, generated multi-rendition authoring beyond preserved OPF payloads,
  DRM decryption, XML signature validation, or generic TeX-to-MathML
  conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 597 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,860 assertions, 0 failures, with 647
  counted `PASS` lines in
  `/tmp/pandoc-epub3-ncx-playorder-full-20260621.log`.

## 2026-06-21 EPUB3 Auxiliary Navigation Output Slice

- Focused PHP coverage is now 647 behavior tests with 6,858 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` now consumes `epubAuxiliaryNavSections`/`auxiliaryNavSections`
  metadata and emits EPUB3 auxiliary `nav.xhtml` sections for lists of
  illustrations, tables, audio, and video. `loi`, `lot`, `loa`, and `lov`
  sections preserve headings, nav-root attributes, linked labels, span-only
  labels, entry EPUB types, label titles, classes, and read back through
  `EpubReader` as auxiliary section metadata.
- The focused regression writes an EPUB3 package with TOC plus generated `loi`,
  `lot`, and `lov` navigation sections, inspects the generated `nav.xhtml`,
  then reads the package back and verifies section/entry counts and structured
  metadata.
- Dependency-closure checkpoint: this is limited to metadata-driven EPUB3
  auxiliary navigation output and read-back. It does not claim full nav
  document vocabulary validation, EPUBCheck validation, full upstream Haskell
  runner parity, full EPUB fixture corpus parity, generated multi-rendition
  authoring beyond preserved OPF payloads, DRM decryption, XML signature
  validation, or generic TeX-to-MathML conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 595 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,858 assertions, 0 failures, with 647
  counted `PASS` lines in
  `/tmp/pandoc-epub3-aux-nav-output-full-20260621.log`.

## 2026-06-21 EPUB3 Auxiliary Navigation Slice

- Focused PHP coverage is now 646 behavior tests with 6,848 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records EPUB3 auxiliary `nav.xhtml` sections for lists of
  illustrations, tables, audio, and video. `loi`, `lot`, `loa`, and `lov`
  sections are preserved as `epubAuxiliaryNavSections`, with section type,
  heading, nav-root attributes, normalized entries, plus aggregate section and
  entry counts.
- The focused regression builds an EPUB3 package with TOC plus `loi`, `lot`,
  `loa`, and `lov` navigation sections and verifies the reader keeps section
  ids/classes/ARIA/hidden state, headings, linked entries, span-only entries,
  and entry label attributes.
- Dependency-closure checkpoint: this is limited to native PHP EPUB3 auxiliary
  navigation intake metadata. It does not claim generated auxiliary nav output,
  full nav document vocabulary validation, EPUBCheck validation, full upstream
  Haskell runner parity, full EPUB fixture corpus parity, generated
  multi-rendition authoring beyond preserved OPF payloads, DRM decryption, XML
  signature validation, or generic TeX-to-MathML conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 585 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,848 assertions, 0 failures, with 646
  counted `PASS` lines in `/tmp/pandoc-epub3-aux-nav-full-20260621.log`.

## 2026-06-21 EPUB3 Nav Section Attributes Slice

- Focused PHP coverage is now 644 behavior tests with 6,842 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records EPUB3 nav-section root attributes for TOC,
  landmarks, and page-list sections as `epubTocNavAttributes`,
  `epubLandmarkNavAttributes`, and `epubPageListNavAttributes`, including
  extra `epub:type` tokens, custom ids, roles, titles, ARIA labels, classes,
  language/direction, and hidden state.
- `EpubReader` also records custom nav section headings as
  `epubTocNavTitle`, `epubLandmarkNavTitle`, and `epubPageListNavTitle` when
  they differ from the writer defaults.
- `EpubWriter` now consumes those metadata records and emits them on
  generated `nav.xhtml` TOC, landmarks, and page-list `<nav>` roots while
  preserving the required EPUB nav type tokens. Read-back verifies nav root
  attributes and headings survive through native PHP EPUB output and input.
- Dependency-closure checkpoint: this is limited to EPUB3 nav-section
  root/heading fidelity. It does not claim full nav document vocabulary
  validation, EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, generated multi-rendition authoring beyond
  preserved OPF payloads, DRM decryption, XML signature validation, or generic
  TeX-to-MathML conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 579 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,842 assertions, 0 failures, with 644
  counted `PASS` lines in
  `/tmp/pandoc-epub3-nav-section-attrs-full-20260621.log`.

## 2026-06-21 EPUB3 Spine Id Slice

- Focused PHP coverage is now 642 behavior tests with 6,821 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records the OPF spine root `id` attribute as
  `epubSpineId` alongside the existing spine `toc` and
  `page-progression-direction` metadata.
- `EpubWriter` now emits a validated `spineId`/`epubSpineId` value as
  `<spine id="...">` in generated OPF packages and verifies it survives
  native PHP EPUB read-back.
- Dependency-closure checkpoint: this is limited to the OPF spine root `id`
  attribute. It does not claim full OPF vocabulary validation, generated
  multi-rendition authoring beyond preserved OPF payloads, EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, DRM decryption, XML signature validation, or generic TeX-to-MathML
  conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 558 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,821 assertions, 0 failures, with 642
  counted `PASS` lines in `/tmp/pandoc-epub3-spine-id-full-20260621.log`.

## 2026-06-21 EPUB3 Package Root Id Slice

- Focused PHP coverage is now 642 behavior tests with 6,818 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now records the OPF package root `id` attribute as
  `epubPackageId` alongside the existing package version, unique-identifier,
  prefix, direction, and language metadata.
- `EpubWriter` now emits a validated `packageId`/`epubPackageId` value as
  `<package id="...">` in generated OPF packages and verifies it survives
  native PHP EPUB read-back.
- Dependency-closure checkpoint: this is limited to the package root `id`
  attribute. It does not claim full package vocabulary validation, generated
  multi-rendition authoring beyond preserved OPF payloads, EPUBCheck
  validation, full upstream Haskell runner parity, full EPUB fixture corpus
  parity, DRM decryption, XML signature validation, or generic TeX-to-MathML
  conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 555 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,818 assertions, 0 failures, with 642
  counted `PASS` lines in `/tmp/pandoc-epub3-package-id-full-20260621.log`.

## 2026-06-21 EPUB3 Package Link Language Slice

- Focused PHP coverage is now 642 behavior tests with 6,815 full-suite
  assertions across the Pandoc lane.
- `EpubReader` now preserves `hreflang` on OPF package metadata `<link>`
  records and collection `<link>` records, and records `dir` plus
  `xml:lang`/`lang` on collection-local `<metadata><meta>` records.
- `EpubWriter` emits the same metadata back into generated OPF packages:
  package metadata links and collection links keep `hreflang`, while
  collection metadata records keep text direction and language qualifiers.
- The bounded tests extend the existing package metadata and collection
  fixtures rather than adding a synthetic standalone case. They verify raw OPF
  output and read-back metadata for package links, collection links, and
  collection-local metadata records.
- Dependency-closure checkpoint: this is limited to EPUB package link-language
  and collection metadata text qualifiers. It does not claim full metadata
  vocabulary validation, generated multi-rendition authoring beyond preserved
  OPF payloads, EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, DRM decryption, XML signature validation, or
  generic TeX-to-MathML conversion.
- Evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 552 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,815 assertions, 0 failures, with 642
  counted `PASS` lines in
  `/tmp/pandoc-epub3-link-language-full-20260621.log`.

## 2026-06-21 EPUB3 Direct Rendition Flow Refinement Slice

- Focused PHP coverage is now 642 behavior tests with 6,811 full-suite
  assertions across the Pandoc lane.
- `EpubWriter` now treats direct per-spine `renditionFlow`,
  `epubRenditionFlow`, or `flow` values as OPF metadata refinements as well as
  itemref flow properties. Generated packages can therefore emit both
  `properties="rendition:flow-..."` on the `<itemref>` and a targeted
  `<meta property="rendition:flow" refines="#itemref-id">...</meta>` record
  when the source spine metadata carries direct flow intent.
- The focused regression removes the prebuilt `metadataProperties` flow record
  from the writer fixture and verifies direct `renditionFlow` alone produces
  the rewritten itemref-targeted OPF refinement, survives read-back as
  `epubSpineItemRefs[*].renditionFlow`, and keeps stale source refines targets
  out of the generated OPF.
- Dependency-closure checkpoint: this is limited to direct per-spine
  `rendition:flow` refinement output. It does not claim full rendition/layout
  validation, generated multi-rendition authoring beyond preserved OPF
  payloads, EPUBCheck validation, full upstream Haskell runner parity, full
  EPUB fixture corpus parity, DRM decryption, XML signature validation, or
  generic TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubWriterTest.php`, and `php -l
  lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 548 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,811 assertions, 0 failures, with 642
  counted `PASS` lines in `/tmp/pandoc-epub3-flow-full-20260621.log`.

## 2026-06-21 EPUB3 XHTML Root Attribute Slice

- Focused PHP coverage is now 640 behavior tests with 6,793 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 XHTML root-attribute round-trip coverage for spine
  documents. `EpubReader` carries `<html>` `id`, class tokens, `role`,
  `title`, `aria-label`, `prefix`, `xml:lang`/`lang`, `dir`, and `hidden`
  state on spine item metadata when semantic root attributes beyond plain
  language/direction are present.
- `EpubWriter` emits metadata-provided root attributes onto generated spine
  `<html>` elements, while continuing to generate namespace, language, and
  direction attributes through the validated EPUB writer path.
- Dependency-closure checkpoint: this remains XHTML root-attribute metadata
  preservation only. It does not claim arbitrary XHTML root DOM preservation,
  generated multi-rendition authoring, EPUBCheck validation, full upstream
  Haskell runner parity, full fixture corpus parity, DRM decryption, XML
  signature validation, or generic TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 530 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,793 assertions, 0 failures.

## 2026-06-21 EPUB3 XHTML Body Attribute Slice

- Focused PHP coverage is now 639 behavior tests with 6,789 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 XHTML body-attribute round-trip coverage for spine
  documents. `EpubReader` carries `<body>` `id`, class tokens, `epub:type`,
  `role`, `title`, `aria-label`, `xml:lang`/`lang`, `dir`, and `hidden` state
  on spine item metadata where present.
- `EpubWriter` emits metadata-provided body attributes onto generated spine
  `<body>` elements, preserving semantic source-document hints without
  widening the claim to arbitrary body DOM preservation.
- Dependency-closure checkpoint: this remains XHTML body-attribute metadata
  preservation only. It does not claim arbitrary XHTML body DOM preservation,
  generated multi-rendition authoring, EPUBCheck validation, full upstream
  Haskell runner parity, full fixture corpus parity, DRM decryption, XML
  signature validation, or generic TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 526 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,789 assertions, 0 failures.

## 2026-06-21 EPUB3 XHTML Head Style Slice

- Focused PHP coverage is now 638 behavior tests with 6,787 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 XHTML head-style round-trip coverage for spine
  documents. `EpubReader` carries `<head><style>` records on spine item
  metadata with CSS text plus `id`, `type`, `media`, `title`, `xml:lang`, and
  `dir` where present.
- `EpubWriter` emits metadata-provided head styles into generated spine XHTML
  after linked stylesheets, escaping CSS text so operators such as `>` and `&`
  remain XML-valid while read-back recovers the original CSS.
- Dependency-closure checkpoint: this remains XHTML head-style metadata
  preservation only. It does not claim arbitrary XHTML head DOM preservation,
  CSS cascade validation, browser rendering, generated multi-rendition
  authoring, EPUBCheck validation, full upstream Haskell runner parity, full
  fixture corpus parity, DRM decryption, XML signature validation, or generic
  TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 524 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,787 assertions, 0 failures.

## 2026-06-21 EPUB3 XHTML Head Meta Slice

- Focused PHP coverage is now 637 behavior tests with 6,781 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 XHTML head-meta round-trip coverage for spine documents.
  `EpubReader` carries semantic `<head><meta>` records on spine item metadata
  for `name`, `property`, and `http-equiv` records, including `id`, `content`,
  `refines`, `scheme`, `xml:lang`, and `dir` where present.
- `EpubWriter` emits metadata-provided head metas into generated spine XHTML
  while keeping generated charset and viewport metadata special-cased, so
  round trips do not accumulate duplicate serialization or layout meta tags.
- Dependency-closure checkpoint: this remains XHTML head-meta metadata
  preservation only. It does not claim arbitrary XHTML head DOM preservation,
  generated multi-rendition authoring, EPUBCheck validation, full upstream
  Haskell runner parity, full fixture corpus parity, DRM decryption, XML
  signature validation, or generic TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 518 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,781 assertions, 0 failures.

## 2026-06-21 EPUB3 XHTML Head Link Slice

- Focused PHP coverage is now 636 behavior tests with 6,773 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 XHTML head-link round-trip coverage for spine documents.
  `EpubReader` carries per-spine `<head><link>` metadata for package-relative
  and remote hrefs, including `id`, `rel`, `type`, `media`, `title`, and
  `properties`.
- `EpubWriter` emits metadata-provided head links into generated spine XHTML
  and skips automatic duplicate CSS links when the same packaged stylesheet is
  already represented by the metadata link.
- Dependency-closure checkpoint: this remains XHTML head-link metadata
  preservation only. It does not claim arbitrary XHTML head DOM preservation,
  generated multi-rendition authoring, EPUBCheck validation, full upstream
  Haskell runner parity, full fixture corpus parity, DRM decryption, XML
  signature validation, or generic TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 510 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,773 assertions, 0 failures.

## 2026-06-21 EPUB3 Rich Nav Label Attribute Slice

- Focused PHP coverage is now 635 behavior tests with 6,766 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 navigation label attribute preservation for TOC,
  landmark, and page-list entries. `EpubReader` now carries nav label `id`,
  `title`, `role`, `aria-label`, class tokens, and `hidden` state from
  `<a>`/`<span>` labels into entry metadata.
- `EpubWriter` now emits those same bounded fields back onto generated
  `nav.xhtml` labels for metadata-provided entries, while retaining existing
  href rewriting and non-clickable parent span behavior.
- Dependency-closure checkpoint: this remains nav label metadata preservation
  only. It does not claim arbitrary nav DOM preservation, generated
  multi-rendition authoring, EPUBCheck validation, full upstream Haskell
  runner parity, full fixture corpus parity, DRM decryption, XML signature
  validation, or generic TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 503 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,766 assertions, 0 failures.

## 2026-06-21 EPUB3 Alternate Rootfile Payload Slice

- Focused PHP coverage is now 634 behavior tests with 6,762 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 multi-rootfile preservation for real alternate OPF
  payloads declared in `META-INF/container.xml`. When resource extraction is
  enabled, `EpubReader` now captures non-selected OPF rootfiles that exist in
  the ZIP as base64 metadata while preserving rootfile `id`, `media-type`, and
  `properties`.
- `EpubWriter` now writes those preserved alternate OPF payloads back into the
  archive and re-emits matching `<rootfile>` entries. It still refuses
  missing/non-OPF rootfile entries, so metadata-only rootfiles are not promoted
  into invalid multi-rendition packages.
- Dependency-closure checkpoint: this is payload preservation only. It does
  not claim generated multi-rendition authoring, EPUBCheck validation, full
  upstream Haskell runner parity, full fixture corpus parity, DRM decryption,
  XML signature validation, or generic TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 499 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,762 assertions, 0 failures.

## 2026-06-21 EPUB3 Container Metadata Slice

- Focused PHP coverage is now 633 behavior tests with 6,748 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 `META-INF/container.xml` metadata preservation for the
  container version, complete rootfile matrix, selected OPF rootfile marker,
  rootfile `id`, media type, and property tokens, including OPF media types
  with parameters such as `application/oebps-package+xml;profile=epub3`.
- `EpubReader` now records container `<links>` as normalized metadata relative
  to `META-INF`, so records such as `metadata.xml` survive as
  `META-INF/metadata.xml` with `rel`, `media-type`, `id`, `refines`, and
  `properties` fields.
- `EpubWriter` now emits metadata-provided container links back into
  `META-INF/container.xml`. It deliberately keeps generated container rootfiles
  limited to the packaged OPF until alternate rootfile payloads are preserved,
  avoiding invalid multi-rendition packages.
- Dependency-closure checkpoint: this remains container metadata/link
  preservation only. It does not claim multi-rendition writer payloads,
  EPUBCheck validation, full upstream Haskell runner parity, full fixture
  corpus parity, DRM decryption, XML signature validation, or generic
  TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, `php -l
  lanes/pandoc/tests/EpubReaderTest.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 485 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests` passed 11 files, 6,748 assertions, 0 failures.

## 2026-06-21 EPUB3 OCF Sidecar Slice

- Focused PHP coverage is now 632 behavior tests with 6,734 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 OCF sidecar preservation for
  `META-INF/encryption.xml`, `META-INF/metadata.xml`,
  `META-INF/rights.xml`, and `META-INF/signatures.xml`.
- `EpubReader` now records fixed OCF sidecars as metadata with ZIP size
  provenance, XML readability/well-formedness, root name/namespace checks,
  and encryption resource references plus encryption method algorithms from
  XML Encryption `EncryptedData`/`CipherReference` records.
- When resource extraction is enabled, OCF sidecar payloads are carried under
  `epubOcfSidecarPayloads`. `EpubWriter` writes those payloads back to
  `META-INF` without adding the sidecars to the OPF manifest, preserving
  encrypted-resource package state while keeping sidecars out of normal asset
  catalogs.
- Dependency-closure checkpoint: this remains metadata/payload preservation
  only. It does not validate DRM rights, decrypt resources, validate XML
  signatures, run EPUBCheck, execute the upstream Haskell test runner, or claim
  generic TeX-to-MathML conversion.
- Focused evidence: `php -l lanes/pandoc/src/EpubReader.php`, `php -l
  lanes/pandoc/src/EpubWriter.php`, and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 346 assertions, 0
  failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  6,734 assertions, 0 failures.

## 2026-06-21 EPUB3 Table Fidelity Slice

- Focused PHP coverage is now 628 behavior tests with 6,646 full-suite
  assertions across the Pandoc lane.
- Added bounded EPUB3 table fidelity coverage for package XHTML generated by
  `EpubWriter` and read back by `EpubReader`: caption inlines, colgroup
  widths, `thead`/`tbody`/`tfoot` ids, row-head columns, body header rows,
  colspan, nested paragraph/strong cell content, data attributes, retained
  non-alignment style such as `background-color`, and left/center/right
  alignment metadata.
- `MarkdownReader` now derives table-level alignments from HTML/XHTML
  `<col>`/cell `align` and `text-align` style evidence, preferring explicit
  column and single-column cell evidence before using colspan fallback. This
  closes the EPUB read-back gap where generated spine XHTML preserved
  alignment in cell styles but the reconstructed table reported all
  alignments as `default`.
- Dependency-closure checkpoint: this remains native PHP and lane-local on the
  existing package/DOM helpers. It does not claim full upstream Haskell runner
  parity, EPUBCheck validation, generic TeX-to-MathML conversion, arbitrary
  HTML5 tree construction, full EPUB fixture corpus parity, or EPUB2-specific
  output behavior.
- Focused evidence: `php -l lanes/pandoc/src/MarkdownReader.php` and `php -l
  lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
  lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
  passed 2 files, 383 assertions, 0 failures; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,481 assertions,
  0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
  6,646 assertions, 0 failures with 628 PASS lines recorded in
  `/tmp/pandoc-full-epub-table-fidelity-20260621.log`.

## 2026-05-24 HTML Reader Standalone Button Slice

- Superseded by the standalone SVG slice below. Focused PHP coverage was
  419 behavior tests with 4,470 assertions.

## 2026-05-25 HTML Reader Standalone SVG Slice

- Focused PHP coverage is now 420 behavior tests with 4,477 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  inline behavior, `pSvg`/raw-inline SVG handling, and `TagCategories`
  `eitherBlockOrInline` classification for standalone `<svg>` fragments.
- `MarkdownReader` now admits top-level `<svg>...</svg>` fragments into the
  existing HTML inline parser. With raw HTML enabled, WordPress output keeps
  the raw SVG source markup visible for source-review handoff instead of
  treating the packet as an unmapped block. Existing in-paragraph SVG behavior
  and disabled-raw SVG data-image fallback remain unchanged.
- Added `fixtures/upstream-html-standalone-svg-inline.html` and
  `examples/wordpress-native-html-standalone-svg-handoff.php` for a WordPress
  Data Liberation handoff where imported source icons remain auditable as raw
  SVG in a paragraph.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, SVG sanitization policy, image extraction,
  and arbitrary browser DOM behavior remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-svg-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-svg-handoff.php`
  emitted WordPress paragraph raw SVG markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,477 assertions,
  0 failures. No no-argument root harness was assigned or started.

- Focused PHP coverage is now 419 behavior tests with 4,470 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  inline behavior, `pRawHtmlInline` fallback, and `TagCategories`
  `eitherBlockOrInline` classification for standalone `<button>` fragments.
- `MarkdownReader` now admits top-level `<button>...</button>` fragments into
  the existing HTML inline parser, preserving raw HTML button boundaries while
  parsing nested inline content such as `<strong>`. The older indented
  list-item button handling remains raw-block compatible for the mapped
  upstream list-item raw HTML tests.
- Added `fixtures/upstream-html-standalone-button-inline.html` and
  `examples/wordpress-native-html-standalone-button-handoff.php` for a
  WordPress Data Liberation handoff where imported classic-editor review
  controls remain active and auditable.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, form submission semantics, and browser
  interaction behavior remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-button-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-button-handoff.php`
  emitted WordPress paragraph button markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,470 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Ins Slice

- Superseded by the standalone button slice above. Focused PHP coverage was
  418 behavior tests with 4,460 assertions.

- Focused PHP coverage is now 418 behavior tests with 4,460 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  inline behavior and `TagCategories` `eitherBlockOrInline` classification
  for standalone `<ins>` fragments.
- `MarkdownReader` already routes top-level `<ins>...</ins>` fragments
  through the HTML inline parser; this slice records the native semantics:
  inserted editorial text lowers to the existing underline AST and WordPress
  `<u>` paragraph handoff.
- Added `fixtures/upstream-html-standalone-ins-inline.html` and
  `examples/wordpress-native-html-standalone-ins-handoff.php` for a WordPress
  Data Liberation review packet where imported insertion text remains visible
  for editorial audit.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, change-tracking metadata preservation for
  arbitrary HTML, and DOCX/OpenXML review metadata parsing remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-ins-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-ins-handoff.php`
  emitted WordPress paragraph underline markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,460 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Noscript Slice

- Focused PHP coverage is now 417 behavior tests with 4,455 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  plus inline raw-HTML fallback behavior and `TagCategories`
  `eitherBlockOrInline` classification for standalone `<noscript>` fragments.
- `MarkdownReader` now admits top-level `<noscript>...</noscript>` fragments
  into the existing HTML inline parser and preserves raw HTML noscript
  boundaries when raw HTML is enabled. WordPress output keeps script-disabled
  fallback links reviewable instead of escaping them as literal text.
- Added `fixtures/upstream-html-standalone-noscript-inline.html` and
  `examples/wordpress-native-html-standalone-noscript-handoff.php` for a
  WordPress Data Liberation handoff where fallback import links stay visible as
  active paragraph HTML.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, script execution policy, and arbitrary
  noscript browser rendering semantics remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-noscript-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-noscript-handoff.php`
  emitted WordPress paragraph noscript markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,455 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Applet Slice

- Focused PHP coverage is now 416 behavior tests with 4,448 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  plus inline raw-HTML fallback behavior and `TagCategories`
  `eitherBlockOrInline` classification for standalone `<applet>` fragments.
- `MarkdownReader` now admits top-level `<applet>...</applet>` fragments into
  the existing HTML inline parser and preserves raw HTML applet boundaries
  when raw HTML is enabled. WordPress output keeps legacy Java applet handoff
  markup reviewable instead of escaping it as literal text.
- Added `fixtures/upstream-html-standalone-applet-inline.html` and
  `examples/wordpress-native-html-standalone-applet-handoff.php` for a
  WordPress Data Liberation handoff where legacy applet fallback text and
  source boundaries stay visible as active paragraph HTML.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, arbitrary applet parameter handling, and
  browser/plugin execution semantics remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-applet-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-applet-handoff.php`
  emitted WordPress paragraph applet markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,448 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Object/Embed Slice

- Superseded by the standalone applet slice above. Focused PHP coverage was
  415 behavior tests with 4,442 assertions for this slice.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  plus inline raw-HTML fallback behavior and `TagCategories`
  `eitherBlockOrInline` classification for standalone `<object>` fragments
  with `<embed>` fallback children.
- `MarkdownReader` now admits top-level `<object>...</object>` fragments into
  the existing HTML inline parser and preserves raw HTML object/embed
  boundaries when raw HTML is enabled. WordPress output keeps legacy
  interactive embed markup reviewable instead of escaping it as literal text.
- Added `fixtures/upstream-html-standalone-object-embed-inline.html` and
  `examples/wordpress-native-html-standalone-object-handoff.php` for a
  WordPress Data Liberation handoff where classic-editor object/embed packets
  remain visible as active paragraph HTML.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, arbitrary media/object fallback semantics,
  and full plugin/embed DOM policy remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-object-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-object-handoff.php`
  emitted WordPress paragraph object/embed markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,442 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Video/Source/Track Slice

- Superseded by the standalone object/embed slice above. Focused PHP coverage
  was 414 behavior tests with 4,435 assertions for this slice.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  plus inline raw-HTML fallback behavior and `TagCategories`
  `eitherBlockOrInline` classification for standalone `<video>` fragments
  with `<source>` and `<track>` children.
- `MarkdownReader` now admits top-level `<video>...</video>` fragments into
  the existing HTML inline parser and preserves raw HTML video/source/track
  boundaries when raw HTML is enabled. WordPress output keeps active imported
  video markup and caption-track metadata instead of escaping it as literal
  text.
- Added `fixtures/upstream-html-standalone-video-inline.html` and
  `examples/wordpress-native-html-standalone-video-handoff.php` for a
  WordPress Data Liberation handoff where classic-editor video embeds stay
  playable during review.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, arbitrary media/object fallback semantics,
  and full media DOM policy remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-video-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-video-handoff.php`
  emitted WordPress paragraph video markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,435 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Audio/Source/Track Slice

- Superseded by the standalone video/source/track slice above. Focused PHP
  coverage was 413 behavior tests with 4,427 assertions for this slice.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  plus inline raw-HTML fallback behavior and `TagCategories`
  `eitherBlockOrInline` classification for standalone `<audio>` fragments
  with `<source>` and `<track>` children.
- `MarkdownReader` now admits top-level `<audio>...</audio>` fragments into
  the existing HTML inline parser and preserves raw HTML audio/source/track
  boundaries when raw HTML is enabled. WordPress output keeps active imported
  audio markup and caption-track metadata instead of escaping it as literal
  text.
- Added `fixtures/upstream-html-standalone-audio-inline.html` and
  `examples/wordpress-native-html-standalone-audio-handoff.php` for a
  WordPress Data Liberation handoff where classic-editor audio embeds stay
  playable during review.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, arbitrary media/object fallback semantics,
  and full media DOM policy remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-audio-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-audio-handoff.php`
  emitted WordPress paragraph audio markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,427 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Map/Area Slice

- Superseded by the standalone audio/source/track slice above. Focused PHP
  coverage was 412 behavior tests with 4,419 assertions for this slice.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  plus inline raw-HTML fallback behavior and `TagCategories`
  `eitherBlockOrInline` classification for standalone `<map>` fragments with
  `<area>` children. Targeted source inspection covered `pPlain`, inline
  fallback through `pRawHtmlInline`, `isBlockTag`, `isInlineTag`, and the
  `eitherBlockOrInline` row that includes `map` and `area`; targeted
  inspection counted 6 focused source hits.
- `MarkdownReader` now admits top-level `<map>...</map>` fragments into the
  existing HTML inline parser and preserves raw HTML map/area boundaries when
  raw HTML is enabled. WordPress output keeps active image-map hotspot markup
  instead of escaping it as literal text.
- Added `fixtures/upstream-html-standalone-map-inline.html` and
  `examples/wordpress-native-html-standalone-map-handoff.php` for a WordPress
  Data Liberation handoff where classic-editor image-map hotspots stay visible
  as active paragraph HTML.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, arbitrary either-block-or-inline
  classification, standalone anchor reconciliation, and full image-map DOM
  semantics remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-map-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-map-handoff.php`
  emitted WordPress paragraph image-map markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,419 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Del Slice

- Superseded by the standalone progress and map/area slices below. Focused PHP coverage was
  410 behavior tests with 4,407 assertions for this slice.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  plus inline dispatch behavior and `TagCategories` `eitherBlockOrInline`
  classification for standalone `<del>` fragments. Targeted source inspection
  covered `pPlain`, inline `s`/`strike`/`del` strikeout handling, and the
  `eitherBlockOrInline` row that includes `del`; targeted inspection counted 5
  focused source hits.
- `MarkdownReader` now admits top-level `<del>...</del>` fragments into the
  existing HTML inline parser. The parsed AST keeps native `strikeout` inlines
  and WordPress output emits paragraph `<del>` markup instead of escaped tags
  or raw block boundaries.
- Added `fixtures/upstream-html-standalone-del-inline.html` and
  `examples/wordpress-native-html-standalone-del-handoff.php` for a WordPress
  Data Liberation handoff where classic-editor deletion markup remains active
  reviewer HTML alongside inserted replacement copy.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, standalone anchor reconciliation, arbitrary
  inline raw HTML flow, and full del/ins block-container ambiguity remain
  unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-del-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-del-handoff.php`
  emitted WordPress paragraph deletion/insert markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,407 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Progress Slice

- Superseded by the standalone map/area slice above.
- Focused PHP coverage is now 411 behavior tests with 4,413 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  plus inline raw-HTML fallback behavior and `TagCategories`
  `eitherBlockOrInline` classification for standalone `<progress>` fragments.
  Targeted source inspection covered `pPlain`, inline fallback through
  `pRawHtmlInline`, `isBlockTag`, and the `eitherBlockOrInline` row that
  includes `progress`; targeted inspection counted 6 focused source hits.
- `MarkdownReader` now admits top-level `<progress>...</progress>` fragments
  into the existing HTML inline parser and preserves raw HTML open/close
  boundaries around text content when raw HTML is enabled. WordPress output
  keeps active progress markup instead of dropping to text-only paragraph
  output.
- Added `fixtures/upstream-html-standalone-progress-inline.html` and
  `examples/wordpress-native-html-standalone-progress-handoff.php` for a
  WordPress Data Liberation handoff where import/review progress indicators
  stay visible as active paragraph HTML.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, arbitrary either-block-or-inline
  classification, standalone anchor reconciliation, and full form-control DOM
  semantics remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` and
  `src/Text/Pandoc/Readers/HTML/TagCategories.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-progress-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-progress-handoff.php`
  emitted WordPress paragraph progress markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,413 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Linebreak Slice

- Focused PHP coverage is now 409 behavior tests with 4,405 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain`
  plus `pLineBreak` behavior when a source packet starts with a standalone
  HTML `<br>` fragment. Targeted source/fixture inspection covered `pPlain`,
  the `inline` dispatch branch for `"br" -> pLineBreak`, the `pLineBreak`
  definition, and command fixtures `test/command/2874.md` and
  `test/command/3619.md`; targeted grep counted 7 focused source/fixture
  hits.
- `MarkdownReader` now routes top-level `<br/>`, `<br>text`, and attributed
  `<br>` fragments through the existing HTML inline parser instead of treating
  them as literal Markdown text. The parsed AST keeps native `linebreak`
  inlines, and WordPress output emits paragraph `<br/>` markup.
- Added `fixtures/upstream-html-standalone-linebreak.html` and
  `examples/wordpress-native-html-standalone-linebreak-handoff.php` for a
  WordPress Data Liberation handoff where classic-editor line-break
  placeholders stay active reviewer HTML instead of escaped tags.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader standalone anchor/html-flow reconciliation and arbitrary HTML5
  tree-construction parity remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` around `pPlain`, `inline`, and
  `pLineBreak`; targeted `git -C .upstream-cache/pandoc grep` inspected
  `test/command/2874.md` and `test/command/3619.md`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-linebreak-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-linebreak-handoff.php`
  emitted WordPress paragraph line breaks; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,405 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Standalone Inline Flow Slice

- Focused PHP coverage is now 408 behavior tests with 4,390 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pPlain` plus
  `inline` dispatch behavior when a source packet starts with balanced inline
  HTML rather than a block tag. Targeted source inspection covered `pPlain`,
  the `inline` case dispatch, `pSmall`, `pSuperscript`, `pSubscript`,
  `pSpanLike`, `pQ`, `pBdo`, `pRawHtmlInline`, and `pInlinesInTags`; targeted
  `git show | rg` counted 37 focused source hits.
- `MarkdownReader` now routes bounded standalone inline fragments through the
  existing HTML inline parser instead of treating them as literal Markdown text.
  The accepted slice covers `<small>`, `<sup>`, `span.smallcaps`, `<time>`,
  `<q cite>`, and `<cite>`. Standalone `<a>` is deliberately excluded because
  a prior Markdown raw-HTML anchor boundary test expects that top-level form to
  remain raw HTML unless a future slice reconciles both behaviors explicitly.
- Added `fixtures/upstream-html-standalone-inline-flow.html` and
  `examples/wordpress-native-html-standalone-inline-flow-handoff.php` for a
  WordPress Data Liberation handoff where classic-editor inline fragments become
  WordPress paragraphs with fine print, small-caps terms, time metadata, quoted
  sources, and cite markup preserved.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Broader HTML5 tree-construction, standalone anchor reconciliation, and
  arbitrary inline raw HTML flow remain unclaimed.
- Focused evidence: `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-native-html-standalone-inline-flow-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-standalone-inline-flow-handoff.php`
  emitted WordPress paragraph HTML for the fixture; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,390 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Cite/Wbr Raw Inline Slice

- Focused PHP coverage is now 407 behavior tests with 4,358 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML`
  `pRawHtmlInline` fallback behavior for inline tags that are not handled by a
  richer semantic branch. Targeted source inspection covered the default
  `inline` fall-through to `pRawHtmlInline`, the `Ext_raw_html` raw-inline
  branch, the disabled-extension `ignore raw` branch, and `isInlineTag` /
  `isBlockTag` classification; targeted grep counted 12 focused source hits.
- `MarkdownReader` now preserves `<cite>` open/close boundaries around parsed
  child inlines and preserves `<wbr>` as a raw inline boundary while keeping
  the text that libxml attaches under the void-like element. With raw HTML
  disabled, the same bounded slice drops the raw boundaries but keeps parsed
  child text/emphasis.
- Added `fixtures/upstream-html-cite-wbr-raw-inline.html` and
  `examples/wordpress-native-html-cite-wbr-handoff.php` for a WordPress Data
  Liberation handoff where imported source-title markup and long-slug word
  break hints remain visible in reviewer HTML.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  Full HTML5 raw inline fallback parity beyond the bounded generic tags remains
  unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` and
  `git -C .upstream-cache/pandoc grep` inspected
  `src/Text/Pandoc/Readers/HTML.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-cite-wbr-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-cite-wbr-handoff.php` emitted
  WordPress paragraph HTML with preserved `<cite>` and `<wbr>` boundaries;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 4,358 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 HTML Reader Pre/Code Break Slice

- Focused PHP coverage is now 406 behavior tests with 4,336 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pCodeBlock`
  and `tagToText` behavior: `TagText` contributes literal text, `TagOpen "br"`
  contributes a newline, and `<pre>` content imports as a native `CodeBlock`.
  Targeted source inspection covered `src/Text/Pandoc/Readers/HTML.hs` lines
  around `pCodeBlock`, `matchTagOpen "pre"`, `matchTagOpen "code"`,
  `tagToText`, `TagText`, `TagOpen "br"`, and `codeBlockWith`, counting 9
  focused source branch lines.
- Strengthened the static upstream inventory with a local native smoke pass:
  `NativeReader` parsed 252/252 upstream `.native` fixtures under `test/`, and
  all 252 rendered through `WordPressBlockWriter`, `HtmlWriter`, and
  `MarkdownWriter`. This is inventory/smoke evidence, not full Haskell runner
  parity.
- `MarkdownReader` now preserves `<br>` as newlines inside HTML pre/code source
  snippets, imports bare `<pre>` exports as code blocks, and keeps the existing
  upstream-like attribute precedence where `<pre>` attributes win over nested
  `<code>` attributes.
- Added `fixtures/upstream-html-pre-code-br.html` and
  `examples/wordpress-native-html-pre-code-breaks-handoff.php` for a WordPress
  Data Liberation handoff where classic-editor code exports keep reviewer line
  breaks and bare preformatted exports become WordPress code blocks.
- Dependency-closure checkpoint: this slice remains lane-local on existing
  `DOMDocument` parsing. It does not activate `xml-html5-dom-core`,
  `shared-zip-package-core`, OpenXML/OpenDocument package parsing, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion, TeX
  reference conversion, Unicode/charset, or syntax-highlighting support rows.
  General HTML5 tree-construction and broader malformed-HTML parser parity
  remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` and
  `git -C .upstream-cache/pandoc grep` inspected
  `src/Text/Pandoc/Readers/HTML.hs`; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-pre-code-breaks-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-pre-code-breaks-handoff.php`
  emitted WordPress code blocks with preserved line breaks; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  4,336 assertions, 0 failures. No no-argument root harness was assigned or
  started.

## 2026-05-24 WordPress Writer DOCX Nested Link Label Slice

- Focused PHP coverage is now 405 behavior tests with 4,316 assertions.
- Added bounded coverage for upstream `test/docx/nested_anchors_in_header.native`
  and Pandoc writer nested-link handling. The upstream DOCX fixture has 15
  lines and 10 `Link` constructors, including outer TOC/cross-reference links
  whose labels contain inner page-number links. Targeted source inspection
  covered `Text.Pandoc.Writers.Shared.removeLinks` and the `Writer.HTML`
  `Link` call sites that use `removeLinks` before rendering anchor labels.
- `WordPressBlockWriter` now applies the same no-nested-anchor rule to link
  labels: nested `link` inline nodes are rendered as `span` nodes with link
  target/title fields removed, while the outer link remains active. This keeps
  DOCX-generated TOC and cross-reference labels valid for WordPress HTML.
- Added `fixtures/upstream-native-docx-nested-anchors-header.native` and
  `examples/wordpress-native-docx-nested-links-handoff.php` for a WordPress
  Data Liberation handoff where page-number labels are spans inside the outer
  source links instead of nested anchors.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `shared-zip-package-core`, `xml-html5-dom-core`, OpenXML package
  parsing, PDF handoff/text extraction, citation/CSL, PlainMath/MathML
  conversion, TeX reference conversion, Unicode/charset, or syntax-highlighting
  support rows. Direct DOCX package ingestion remains behind the shared
  ZIP/OpenXML gate; this slice maps a copied upstream Native fixture only.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` and
  `git -C .upstream-cache/pandoc grep` inspected
  `test/docx/nested_anchors_in_header.native`,
  `src/Text/Pandoc/Writers/Shared.hs`,
  `src/Text/Pandoc/Writers/HTML.hs`,
  `test/Tests/Readers/Docx.hs`, and `test/Tests/Writers/Docx.hs`. `php -l`
  passed for `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-docx-nested-links-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-nested-links-handoff.php` emitted
  outer WordPress links with inner page labels as spans; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  4,316 assertions, 0 failures. No no-argument root harness was assigned or
  started.

## 2026-05-24 Markdown Writer GFM Details List Slice

- Focused PHP coverage is now 404 behavior tests with 4,302 assertions.
- Added bounded coverage for upstream command fixture `test/command/9792.md`,
  where Pandoc `-t gfm` inserts blank lines around a nested list enclosed by
  raw `<details>` boundaries inside a list item. The static upstream checkpoint
  inspected `Text.Pandoc.Writers.Markdown` `fixBlocks`/`RawBlock`/list
  branches and counted 21 lines in `test/command/9792.md` plus 31 focused
  source/fixture hits for `fixBlocks`, `RawBlock`, list blocks, and
  `details`.
- `MarkdownWriter` now keeps the default Markdown writer behavior unchanged
  while adding the GFM-specific blank line before a nested list after a raw
  `<details>` opening block, and before a raw `</details>` closing block after
  that nested list.
- Added `fixtures/upstream-command-gfm-details-list.md` and
  `examples/wordpress-markdown-gfm-details-list-handoff.php` for a WordPress
  reviewer handoff where the same AST can produce GFM-safe disclosure markup
  and active WordPress list/raw-HTML blocks.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion,
  TeX reference conversion, Unicode/charset, or syntax-highlighting support
  rows. General HTML5 tree-construction, arbitrary raw block container parsing,
  and broader CommonMark/GFM raw HTML container rules remain unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` and
  `git -C .upstream-cache/pandoc grep` inspected `test/command/9792.md`,
  `src/Text/Pandoc/Writers/Markdown.hs`, and
  `src/Text/Pandoc/Writers/CommonMark.hs`; `test/command/9792.md` has 21
  lines and the focused grep counted 31 source/fixture hits. `php -l` passed
  for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-markdown-gfm-details-list-handoff.php`; `php
  lanes/pandoc/examples/wordpress-markdown-gfm-details-list-handoff.php`
  emitted the expected GFM reviewer handoff and WordPress list/raw-HTML blocks;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
  1 file, 4,302 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 Markdown Details/Summary Raw HTML Slice

- Focused PHP coverage is now 403 behavior tests with 4,297 assertions.
- Added bounded coverage for upstream command fixture `test/command/6385.md`,
  where Pandoc preserves `<details>`/`<summary>` raw HTML boundaries while the
  details body remains Markdown-parsed content. The static upstream checkpoint
  also inspected `HTML/TagCategories.hs`, where `details` and `summary` are
  block HTML tags, plus `test/command/9792.md` for the related list-contained
  details fixture.
- `MarkdownReader` now recognizes a balanced top-level `<details>` block,
  emits raw HTML boundaries for the `details` and `summary` tags when raw HTML
  is enabled, and recursively parses the remaining body as Markdown paragraphs
  and inline markup. With raw HTML disabled, the slice drops the raw boundaries
  and keeps the summary text/body content for review.
- Added `fixtures/upstream-command-details-summary.md` and
  `examples/wordpress-markdown-details-summary-handoff.php` for a WordPress
  Data Liberation handoff where imported disclosure widgets are no longer
  escaped as paragraph text, while their body copy remains editable paragraph
  content.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion,
  TeX reference conversion, Unicode/charset, or syntax-highlighting support
  rows. General HTML5 tree-construction and arbitrary raw block container
  parsing remain unclaimed; the related list-nested GFM writer formatting is
  covered by the later `test/command/9792.md` slice above.
- Focused evidence: targeted `git -C .upstream-cache/pandoc grep` counted 17
  `details`/`summary` hits across `test/command/6385.md`,
  `test/command/9792.md`, `src/Text/Pandoc/Readers/HTML/TagCategories.hs`,
  and `src/Text/Pandoc/Readers/HTML.hs`; `test/command/6385.md` has 18 lines.
  `php -l` passed for `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-markdown-details-summary-handoff.php`; `php
  lanes/pandoc/examples/wordpress-markdown-details-summary-handoff.php` emitted
  active WordPress raw HTML blocks for `<details>`/`<summary>` plus editable
  paragraph body blocks; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,297 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Orphan List Block Slice

- Focused PHP coverage is now 402 behavior tests with 4,276 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML`
  `pBulletList`, `pListItem`, and `pOrderedList` orphan handling around
  `#9187`. Upstream treats direct block children under `ul`/`ol` outside
  `<li>` scope as list content: leading orphans become the first list item,
  while orphans after a list item are concatenated into that item.
- `MarkdownReader` now imports direct orphan paragraphs, nested lists, and
  block continuations in source order. This keeps malformed source-export list
  content attached to native `list_item` nodes instead of dropping it.
- Added `fixtures/upstream-html-orphan-list-blocks.html` and
  `examples/wordpress-native-html-orphan-list-blocks-handoff.php` for a
  WordPress Data Liberation handoff where malformed source lists preserve a
  leading review paragraph, nested orphan list, and ordered-list continuation
  block.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion,
  TeX reference conversion, Unicode/charset, or syntax-highlighting support
  rows. Broader malformed-HTML parser parity beyond this source-order orphan
  list branch remains explicitly unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` lines around `pBulletList`, `pListItem`,
  `pOrderedList`, `orphans`, `mconcat xs`, and the `#9187` comments. Targeted
  `git grep` counted 8 source branch lines and `rg -c` counted 21 source
  pattern hits. No dedicated command fixture for `#9187` was found in the
  static inventory, so the mapped native fixture records the source branch
  directly. `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-native-html-orphan-list-blocks-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-orphan-list-blocks-handoff.php`
  emitted the malformed-list WordPress handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,276 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader List Item ID Slice

- Focused PHP coverage is now 401 behavior tests with 4,248 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pListItem`
  `addId` behavior from `test/command/3596.md`. Upstream wraps a tight
  list item's first `Plain` inline run in `Span (id, [], [])` and wraps
  block/loose item content in `Div (id, [], [])`.
- `MarkdownReader` now imports tight `<li id>` content as a native `span`
  anchor around the leading inline run, leaves nested child lists outside that
  span, and imports loose/block `<li id>` content as a native `div` anchor.
- Added `fixtures/upstream-html-list-item-id.html` and
  `examples/wordpress-native-html-list-item-id-handoff.php` for a WordPress
  Data Liberation handoff where source list anchors survive inside review list
  markup without broadening into a full HTML5 DOM support port.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion,
  TeX reference conversion, Unicode/charset, or syntax-highlighting support
  rows. Orphan list-block handling from upstream #9187 remains explicitly
  unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` lines around `pListItem`, `addId`,
  `Span`, and `B.divWith`; targeted `rg -c` counted 6 source branch lines and
  6 `test/command/3596.md` fixture hits around tight, nested, and loose
  `<li id>` behavior. `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-native-html-list-item-id-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-list-item-id-handoff.php`
  emitted tight span anchors and loose div anchors inside WordPress lists;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
  1 file, 4,248 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Reader Generic Raw Inline Fallback Slice

- Focused PHP coverage is now 400 behavior tests with 4,227 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML`
  `pRawHtmlInline` fallback behavior. Upstream emits `RawInline (Format
  "html")` for raw inline tags/comments when raw HTML is enabled and ignores
  those raw boundaries when raw HTML is disabled.
- `MarkdownReader` now imports bounded generic inline tags (`button`, `time`,
  and `blink`) as raw HTML opening/closing boundary nodes with parsed children
  between them. HTML comments become `raw_html_inline` nodes when raw HTML is
  enabled. The disabled `htmlRawHtml`/`rawHtml` path drops those raw
  boundaries/comments while keeping child text and inline structure.
- Added `fixtures/upstream-html-generic-raw-inline.html` and
  `examples/wordpress-native-html-generic-raw-inline-handoff.php` for a
  WordPress Data Liberation handoff where source action markup, time metadata,
  and migration comments remain reviewable without broadening into a full
  HTML5 DOM support port.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion,
  TeX reference conversion, Unicode/charset, or syntax-highlighting support
  rows. Raw inline fallback beyond the bounded tags/comments remains
  explicitly unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show` inspected
  `src/Text/Pandoc/Readers/HTML.hs` lines around `pRawHtmlInline`; targeted
  `sed`/`rg -c` counted 7 source branch lines for `pRawHtmlInline`,
  `tagComment`, `isInlineTag`, `Ext_raw_html`, `B.rawInline "html"`, and
  `ignore raw`; targeted `git grep` counted 4 `test/command/parse-raw.md`
  fixture hits around `<blink>` raw inline behavior. `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-generic-raw-inline-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-generic-raw-inline-handoff.php`
  emitted raw button/time/comment WordPress markup; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,227 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Span SmallCaps Class Slice

- Focused PHP coverage is now 399 behavior tests with 4,199 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pSpan`
  logic where a `span` whose class tokens include `smallcaps` is lowered to
  native `SmallCaps`, alongside the already mapped `font-variant:
  small-caps` style path.
- `MarkdownReader` now imports `<span class="smallcaps">` as a `small_caps`
  AST node even when neighboring source classes are present, drops the source
  span id/classes/data attributes like Pandoc's `SmallCaps` constructor, and
  preserves nested inline content such as links.
- Added `fixtures/upstream-html-smallcaps-class.html` and
  `examples/wordpress-native-html-smallcaps-class-handoff.php` for a
  WordPress Data Liberation handoff where source glossary text keeps native
  small-caps semantics without leaking legacy span metadata.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion,
  TeX reference conversion, Unicode/charset, or syntax-highlighting support
  rows. Broader malformed-HTML parser parity remains explicitly unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc grep` inspected
  `fontVariant == "small-caps"`, `"smallcaps" \`elem\` classes`, and
  `span class="smallcaps"` output in `src/Text/Pandoc/Readers/HTML.hs`,
  `test/command/1592.md`, `test/command/4528.md`, and
  `test/command/nested-spanlike.md`, counting 2 source branch lines and 3
  command fixture files. `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-native-html-smallcaps-class-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-smallcaps-class-handoff.php`
  emitted WordPress small-caps spans with a nested glossary link; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 4,199 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Reader Checkbox List Slice

- Focused PHP coverage is now 398 behavior tests with 4,182 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML`
  `pCheckbox` and the inline `input type="checkbox"` branch guarded by
  `inListItem`. Upstream emits checked/unchecked ballot markers only for
  checkbox inputs inside list items; non-checkbox controls and outside-list
  checkboxes do not become task-list content in the mapped default path.
- `MarkdownReader` now imports checked and unchecked HTML checkbox inputs
  inside `<li>` as task-list item metadata, strips the generated source
  control marker from reviewer text, keeps literal ballot glyphs as text,
  keeps mixed lists from claiming every item is a task, and drops non-checkbox
  or outside-list controls.
- Added `fixtures/upstream-html-checkbox-list.html` and
  `examples/wordpress-native-html-checkbox-list-handoff.php` for a WordPress
  Data Liberation handoff where HTML-export reviewer task lists become native
  WordPress checkbox labels without leaking source form controls.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML conversion,
  TeX reference conversion, Unicode/charset, or syntax-highlighting support
  rows. Full form-control DOM semantics and raw checkbox passthrough outside
  list items remain explicitly unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc grep` counted 7
  source hits for `pCheckbox`, `inListItem`, `type="checkbox"`, and ballot
  marker handling in `src/Text/Pandoc/Readers/HTML.hs`, plus 26 checkbox
  command-fixture hits across `test/command/9047.md`,
  `test/command/tasklist.md`, and `test/command/gfm.md`. `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-checkbox-list-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-checkbox-list-handoff.php`
  emitted WordPress checkbox labels plus a plain non-task item and outside-list
  paragraph; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,182 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Reader MathML Annotation Slice

- Focused PHP coverage is now 397 behavior tests with 4,152 assertions.
- Added bounded coverage for upstream `Text.Pandoc.Readers.HTML` `pMath`,
  `extractTeXAnnotation`, and the `pSpan` `MJX_Assistive_MathML` branch.
  Upstream unwraps assistive MathML spans, uses embedded
  `annotation encoding="application/x-tex"` before attempting full MathML
  conversion, and treats `display="block"` as display math.
- `MarkdownReader` now imports embedded TeX annotations as native `math` nodes,
  unwraps `MJX_Assistive_MathML` spans so they do not leak as generic spans,
  and leaves MathML without embedded TeX as a reviewable `span` with class
  `math` instead of claiming full MathML-to-TeX parity.
- Added `fixtures/upstream-html-mathml-annotation.html` and
  `examples/wordpress-native-html-mathml-annotation-handoff.php` for a
  WordPress Data Liberation handoff where source MathJax/KaTeX assistive
  MathML keeps the authored TeX once, while opaque MathML remains visible for
  review.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML full conversion,
  TeX reference conversion, Unicode/charset, or syntax-highlighting support
  rows. General MathML conversion beyond embedded TeX annotations remains
  explicitly unclaimed.
- Focused evidence: targeted `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '880,965p'` inspected
  `MJX_Assistive_MathML`, `pMath`, `mathMLToTeXMath`, and
  `extractTeXAnnotation`; targeted `rg -c` counted 13 source hits around
  those branches; targeted `git grep` found the exact source branches plus
  three MathML-related command fixtures under `test/command`. `php -l` passed
  for `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-mathml-annotation-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-mathml-annotation-handoff.php`
  emitted inline and display WordPress math spans plus a reviewable fallback
  math span; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,152 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Reader Doc-Noteref Table Placement Slice

- Focused PHP coverage is now 396 behavior tests with 4,129 assertions.
- Added bounded coverage for upstream command fixtures
  `test/command/8770-document.md`, `test/command/8770-block.md`, and
  `test/command/8770-section.md`, which exercise Pandoc footnote placement
  around `role="doc-noteref"` anchors in a document paragraph, table caption,
  table header cell, table body cell, and following paragraph.
- The upstream source checkpoint maps `Text.Pandoc.Readers.HTML` `replaceNotes`
  and `eNoteref` plus `Text.Pandoc.Readers.HTML.Table` caption parsing through
  `pInTags "caption" block`.
- `MarkdownReader` already imports those anchors as native `note` nodes inside
  table caption/header/body locations. `WordPressBlockWriter` now renders table
  caption inlines before row content for numbering purposes, then emits the
  saved caption HTML after the table to keep WordPress table markup stable.
  This preserves Pandoc's logical caption-before-cell footnote numbering while
  keeping WordPress `figcaption` placement.
- Added `fixtures/upstream-html-doc-noteref-table-placement.html` and
  `examples/wordpress-native-html-doc-noteref-table-handoff.php` for a
  WordPress Data Liberation handoff where source table notes stay ordered as
  paragraph, caption, header cell, body cell, and following paragraph.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows. Block/section reference-location import
  shape from 8770 remains mapped as upstream inventory, not claimed as a
  full WordPress placement policy beyond the document fixture.
- Focused evidence: targeted `git -C .upstream-cache/pandoc grep -n` over the
  three 8770 command fixtures counted paragraph, caption, header-cell,
  body-cell, and following-paragraph doc-noteref anchors plus document/block/
  section footnote containers; targeted `git grep` over
  `src/Text/Pandoc/Readers/HTML.hs` and `src/Text/Pandoc/Readers/HTML/Table.hs`
  confirmed `replaceNotes`, `RawInline (Format "noteref")`, doc-noteref
  matching, `pTable block`, and `pInTags "caption" block`. `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-doc-noteref-table-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-doc-noteref-table-handoff.php`
  emitted ordered WordPress footnotes 1-5; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,129 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Math Renderer Span Skip Slice

- Focused PHP coverage is now 395 behavior tests with 4,111 assertions.
- Added bounded `MarkdownReader` coverage for upstream
  `Text.Pandoc.Readers.HTML` `pSpan` guards that discard visual MathJax and
  KaTeX renderer output. Upstream skips spans carrying `mjx-chtml`,
  `MathJax_CHTML`, or `MathJax_Preview`, and skips exact
  `class="katex-html"` spans.
- `MarkdownReader` now applies those skip rules before generic span lowering,
  so HTML imports that include both `script type="math/tex"` source and
  generated renderer spans produce one native math node and no duplicated
  visual text in Native or WordPress output.
- Added `fixtures/upstream-html-math-renderer-spans.html` and
  `examples/wordpress-native-html-math-renderer-handoff.php` for a WordPress
  Data Liberation handoff where source equations stay reviewable while
  renderer-only MathJax/KaTeX spans are omitted.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, PlainMath/MathML conversion, TeX math/ref
  conversion, citation/CSL, shared ZIP/OpenXML/OpenDocument, PDF handoff/text
  extraction, Unicode/charset, or syntax-highlighting support rows.
  `MJX_Assistive_MathML` MathML-to-TeX parity remains explicitly unclaimed.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '872,900p'` inspected the
  `pSpan` renderer guards; `git -C .upstream-cache/pandoc grep -n
  'mjx-chtml\|MathJax_CHTML\|MathJax_Preview\|MJX_Assistive_MathML\|katex-html'
  HEAD -- src/Text/Pandoc/Readers/HTML.hs test/command
  test/Tests/Readers/HTML.hs` counted 5 upstream source branches and no
  checked-in command fixtures for these class names. `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-math-renderer-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-math-renderer-handoff.php`
  emitted single MathJax/KaTeX WordPress math spans without duplicate renderer
  text; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 4,111 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Reader Span Strikeout Slice

- Focused PHP coverage is now 394 behavior tests with 4,089 assertions.
- Added bounded `MarkdownReader` coverage for upstream
  `Text.Pandoc.Readers.HTML` `pStrikeout` handling. Upstream routes `<s>`,
  `<strike>`, `<del>`, and exact `<span class="strikeout">` to native
  `Strikeout`, and routes `<u>`/`<ins>` to native `Underline`.
- `MarkdownReader` now handles the exact span-class branch in the DOM-backed
  HTML path before the generic span fallback, so legacy source strikeout spans
  become native `strikeout` nodes and WordPress gets the existing `<del>`
  review handoff. The slice also confirms adjacent `del`/`ins` edit markup.
- Added `fixtures/upstream-html-span-strikeout.html` and
  `examples/wordpress-native-html-span-strikeout-handoff.php` for a WordPress
  Data Liberation handoff where legacy strikeout spans and explicit edit marks
  remain reviewable as native WordPress inline markup.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows. Broader malformed HTML/class-token
  normalization beyond upstream exact `class="strikeout"` matching is not
  claimed by this slice.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '760,825p'` inspected
  `pStrikeout` and `pUnderline`; targeted `rg -c` counted 10 source hits around
  the `s`/`strike`/`del`/`u`/`ins` and `span class="strikeout"` branches; `git
  -C .upstream-cache/pandoc grep -l` counted 7 related command/reader fixture
  files. `php -l` passed for `MarkdownReader.php`, `MarkdownReaderTest.php`,
  and `wordpress-native-html-span-strikeout-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-span-strikeout-handoff.php`
  emitted the WordPress deletion/review handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,089 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Line-Block Slice

- Focused mapped count is now 2,276 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage for upstream
  `Text.Pandoc.Readers.HTML` `pLineBlock` handling. Upstream dispatches an
  exact `<div class="line-block">` to `pLineBlock`, trims inline content,
  filters `SoftBreak`, and splits on `LineBreak` to produce a native
  `LineBlock`.
- `MarkdownReader` now handles this behavior for both fragment-level imports
  and DOM-backed full HTML documents: exact line-block divs become
  `line_block` nodes, hard `<br>` elements split lines, double breaks preserve
  empty lines, and NBSP indentation remains in the Native/WordPress handoff.
- Added `fixtures/upstream-html-line-block.html` and
  `examples/wordpress-native-html-line-block-handoff.php` for a WordPress Data
  Liberation handoff where reviewer stanzas and source edit links stay in a
  paragraph-shaped handoff instead of a generic div.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows. Broader malformed HTML/class-token parity
  is not claimed by this slice.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '228,242p'` inspected the
  `div` dispatch to `pLineBlock`; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '478,490p'` inspected
  `pLineBlock`, `splitWhen (== LineBreak)`, and `filter (/= SoftBreak)`;
  targeted `git grep` counted 6 line-block source hits and 2 command fixture
  files (`test/command/10825.md`, `test/command/4162.md`). `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-line-block-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-line-block-handoff.php` emitted
  the WordPress stanza handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,073 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Raw-HTML Disabled Skip Slice

- Focused mapped count is now 2,268 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage for upstream
  `Text.Pandoc.Readers.HTML` raw HTML extension guards. Upstream
  `pRawHtmlBlock` and `pRawHtmlInline` return raw HTML only when
  `Ext_raw_html` is enabled; otherwise they call `ignore` and skip the raw
  payload.
- `MarkdownReader` now handles this behavior in the DOM-backed full HTML
  document path: with `htmlRawHtml`/`rawHtml` disabled, inline and block
  `<style>`, generic `<script>`, and `<textarea>` payloads are skipped rather
  than emitted as raw nodes. Body-level `script type="math/tex..."` remains a
  native `math` node inside a `plain` block.
- Added `fixtures/upstream-html-raw-disabled-skip.html` and
  `examples/wordpress-native-html-raw-disabled-handoff.php` for a WordPress
  Data Liberation handoff where raw migration payloads are suppressed while
  TeX source math remains reviewable.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows. SkippedContent warning-message parity is
  not claimed by this slice.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '536,565p'` inspected
  `pRawHtmlBlock`, `pHtmlBlock`, and `ignore`; `git -C .upstream-cache/pandoc
  show HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '900,930p'` inspected
  `pRawHtmlInline`; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '620,730p'` inspected
  `pPlain` and script math dispatch. Targeted `rg -c` counted 21 source hits
  around `Ext_raw_html`, raw block/inline, `ignore`, `SkippedContent`, and
  script/style/textarea boundaries; `git -C .upstream-cache/pandoc grep -l`
  counted 45 raw_html/style/script/textarea fixture files under
  `test/command` and `test/Tests/Readers/HTML.hs`. `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-raw-disabled-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-raw-disabled-handoff.php`
  emitted the sanitized WordPress handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,055 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Script Raw-Block Slice

- Focused mapped count is now 2,251 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage for upstream
  `Text.Pandoc.Readers.HTML` generic script raw-block handling. Upstream
  `pRawHtmlBlock` tries `pHtmlBlock "script"` before style and textarea, while
  the inline parser still reserves `script type="math/tex..."` for
  `pScriptMath`.
- `MarkdownReader` now handles this behavior in the DOM-backed full HTML
  document path: body-level generic `<script>...</script>` becomes a
  `raw_html` block, `NativeWriter` emits `RawBlock (Format "html")`, and
  `WordPressBlockWriter` emits a core HTML block instead of wrapping the
  script in a paragraph as raw inline HTML. Body-level math/tex scripts remain
  native `math` nodes inside a `plain` block.
- Added `fixtures/upstream-html-script-raw-block.html` and
  `examples/wordpress-native-html-script-block-handoff.php` for a WordPress
  Data Liberation handoff where migration script payloads remain reviewable as
  source HTML blocks.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows. Raw-HTML disabled warning/skip parity is
  not claimed by this slice.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '536,565p'` inspected
  `pRawHtmlBlock` and `pHtmlBlock`; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '620,730p'` inspected `pPlain`
  and inline script math dispatch; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML/TagCategories.hs | sed -n '1,100p'`
  inspected the `script` block/either-block-or-inline category; targeted
  `rg -c` counted 25 static source hits around script raw-block and script-math
  boundaries; `git -C .upstream-cache/pandoc grep -l '<script' HEAD --
  test/command test/Tests/Readers/HTML.hs` counted 3 static command fixtures.
  `php -l` passed for `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-script-block-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-script-block-handoff.php` emitted
  the WordPress HTML-block handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,038 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Style Raw-Block Slice

- Focused mapped count is now 2,233 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage for upstream
  `Text.Pandoc.Readers.HTML` block-level style handling. The upstream block
  dispatcher routes `"style" -> pRawHtmlBlock`, and `pRawHtmlBlock` uses
  `pHtmlBlock "style"` to preserve the complete element as `RawBlock
  (Format "html")`.
- `MarkdownReader` now handles this behavior in the DOM-backed full HTML
  document path: body-level `<style>...</style>` becomes a `raw_html` block,
  `NativeWriter` emits `RawBlock (Format "html")`, and `WordPressBlockWriter`
  emits a core HTML block instead of wrapping the stylesheet in a paragraph as
  raw inline HTML.
- Added `fixtures/upstream-html-style-raw-block.html` and
  `examples/wordpress-native-html-style-block-handoff.php` for a WordPress
  Data Liberation handoff where migration stylesheet payloads remain reviewable
  as source HTML blocks.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows. Raw-HTML disabled warning/skip parity and
  generic script block parity are not claimed by this slice.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '152,285p'` inspected block
  dispatch; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '536,566p'` inspected
  `pRawHtmlBlock` and `pHtmlBlock`; targeted `rg -c` counted 11 source hits
  around style raw-block and inline-style boundaries; `git -C
  .upstream-cache/pandoc grep -l '<style' HEAD -- test/command
  test/Tests/Readers/HTML.hs` counted 29 static files with style-shaped
  fixtures. `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and `wordpress-native-html-style-block-handoff.php`;
  `php lanes/pandoc/examples/wordpress-native-html-style-block-handoff.php`
  emitted the WordPress HTML-block handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,016 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Doc-Noteref Footnote Slice

- Focused mapped count is now 2,216 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage for upstream
  `Text.Pandoc.Readers.HTML` note replacement semantics: `role="doc-endnotes"`
  containers populate the note table, `role="doc-noteref"` anchors become
  native `Note` nodes, and `role="doc-backlink"` links are stripped from the
  note body.
- `MarkdownReader` now handles this behavior for full HTML imports parsed via
  the DOM path. Fragment-level forward replacement and table/caption note
  placement remain future slices; this slice does not claim EPUB extension
  parity beyond recognizing the same bounded `epub:type` names.
- Added `fixtures/upstream-html-doc-noteref-footnotes.html` and
  `examples/wordpress-native-html-doc-noteref-handoff.php` for a WordPress
  Data Liberation handoff where imported source notes become native WordPress
  endnotes instead of duplicating the original HTML endnotes section.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '120,140p'` inspected
  `replaceNotes`; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '286,337p'` inspected
  `eFootnote`, `eFootnotes`, and `eNoteref`; `git -C .upstream-cache/pandoc
  show HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '684,690p'` inspected the
  inline dispatch to `eNoteref`; targeted `rg -c` counted 30 static source
  hits around note-table, doc-endnotes, doc-noteref, and backlink handling; `git
  -C .upstream-cache/pandoc grep -l` counted 6 command fixtures mentioning the
  same doc-noteref/doc-endnotes/epub-noteref surface. `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-doc-noteref-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-doc-noteref-handoff.php` emitted
  the WordPress footnote handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,999 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Textarea Raw-Block Slice

- Focused mapped count is now 2,199 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage for the upstream
  `Text.Pandoc.Readers.HTML` `pRawHtmlBlock` branch where block-level
  `<textarea>` is preserved as `RawBlock (Format "html")`.
- `MarkdownReader` now keeps top-level and body-level
  `<textarea>...</textarea>` blocks as `raw_html` nodes for `NativeWriter`, `HtmlWriter`, and
  `WordPressBlockWriter` handoff. Inline textarea parsing, form-control
  semantics, browser DOM behavior, and disabled raw-HTML warning behavior are
  not claimed by this slice.
- Added `fixtures/upstream-html-textarea-raw-block.html` and
  `examples/wordpress-native-html-textarea-handoff.php` for a WordPress source
  review packet where a legacy textarea payload stays literal inside a core
  HTML block.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '215,255p'` inspected the
  block dispatch where `"textarea" -> pRawHtmlBlock`; `git -C
  .upstream-cache/pandoc show HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n
  '542,566p'` inspected `pRawHtmlBlock` and `pHtmlBlock "textarea"`; targeted
  `rg -c` over the same branch counted 6 static source hits; `php -l` passed
  for `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-textarea-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-textarea-handoff.php` emitted
  the WordPress textarea review block; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,983 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Style/Script Inline Slice

- Focused mapped count is now 2,182 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage for the upstream
  `Text.Pandoc.Readers.HTML` inline `style` and `script type="math/tex..."`
  branches. Upstream preserves `<style>` through `B.rawInline "html"` and
  converts `math/tex` script payloads through `pScriptMath` into inline or
  display math.
- `MarkdownReader` now keeps inline `<style>...</style>` elements as
  `raw_html_inline` nodes and converts `<script type="math/tex">` plus
  display-suffixed `math/tex` script payloads to native `math` nodes. Generic
  TeX parsing, PlainMath, MathML conversion, and TeX reference/environment
  conversion are not claimed by this slice.
- Added `fixtures/upstream-html-style-script-inline.html` and
  `examples/wordpress-native-html-style-script-handoff.php` for a WordPress
  source review packet where source CSS remains visible and TeX math stays
  native in the WordPress handoff.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '640,865p'` inspected inline
  dispatch to `style`, `script`, and `pRawHtmlInline`; `git -C
  .upstream-cache/pandoc show HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n
  '865,960p'` inspected `pRawHtmlInline` and `pScriptMath`; `git -C
  .upstream-cache/pandoc show HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n
  '1070,1090p'` inspected `isInlineTag` style/script boundaries; a targeted
  `rg -c` over `style`, `script`, `math/tex`, `pScriptMath`,
  `pRawHtmlInline`, `pHtmlBlock "style"`, and inline tag cases counted 24
  static source hits around the branch; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-style-script-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-style-script-handoff.php`
  emitted the WordPress style/script review block; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,964 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader SVG Raw-HTML-Enabled Slice

- Focused mapped count is now 2,162 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage for the upstream
  `Text.Pandoc.Readers.HTML` SVG branch split: `pSvg` handles the
  disabled-raw-HTML image fallback, while the raw-HTML-enabled path falls back
  through `pRawHtmlInline` and `B.rawInline "html"` instead of rewriting the
  SVG to a data image.
- `MarkdownReader` now preserves inline `<svg>...</svg>` elements as
  `raw_html_inline` nodes when `htmlRawHtml`/`rawHtml` is enabled. The existing
  disabled-raw path remains unchanged and still maps SVG to a
  `data:image/svg+xml;base64,...` image fallback with Font Awesome width hints.
- Added `fixtures/upstream-html-svg-raw-html.html` and
  `examples/wordpress-native-html-svg-raw-handoff.php` for a WordPress source
  review packet where source SVG markup stays visible as raw inline HTML.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '680,780p'` inspected inline
  dispatch to `pSvg` and `pRawHtmlInline`; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '800,865p'` inspected `pSvg`;
  a targeted `rg -c` over `pSvg`, `guardDisabled Ext_raw_html`,
  `pRawHtmlInline`, `B.rawInline "html"`, and `renderTags` counted 20 static
  source hits around the branch; `php -l`
  passed for `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-svg-raw-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-svg-raw-handoff.php` emitted the
  raw SVG WordPress review block; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,944 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Span-Like Inline Slice

- Focused mapped count is now 2,148 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage from upstream
  `Text.Pandoc.Readers.HTML` `pSpanLike` behavior and
  `Text.Pandoc.Shared` `htmlSpanLikeElements` without invoking upstream
  Pandoc, live fetching, a browser, converter shell-outs, PDF processing,
  ZIP/package parsing, citation/CSL engines, or broader XML/HTML parser
  expansion.
- Targeted upstream inspection covered the inline dispatch fallback for
  `htmlSpanLikeElements`, the `pSpanLike` parser, the
  `Set.fromList ["kbd", "mark", "dfn", "abbr"]` element set, and the
  `tagName : cs` class-prepending behavior. The PHP slice maps `<kbd>`,
  `<mark>`, `<dfn>`, and `<abbr>` to Pandoc `span` nodes with the tag-name
  class prepended, preserves source id/classes/data/title metadata, preserves
  nested inline content, and keeps `<kbd>` distinct from the existing
  `code`/`tt`/`samp`/`var` code path.
- Added `fixtures/upstream-html-spanlike-inline.html` and
  `examples/wordpress-native-html-spanlike-handoff.php` for a WordPress
  source review packet where keyboard shortcuts, publish highlights, and
  source terms remain reviewable as semantic inline HTML.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '650,910p'` inspected the
  upstream inline dispatch and `pSpanLike` branch; `git -C
  .upstream-cache/pandoc show HEAD:src/Text/Pandoc/Shared.hs | sed -n
  '704,714p'` inspected `htmlSpanLikeElements`; `git -C
  .upstream-cache/pandoc show HEAD:src/Text/Pandoc/Readers/HTML.hs | rg -c
  'htmlSpanLikeElements|pSpanLike|tagName : cs|Set\.fromList \["kbd",
  "mark", "dfn", "abbr"\]|"kbd"|"mark"|"dfn"|"abbr"'` counted 6 static source
  hits around the branch; `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and `wordpress-native-html-spanlike-handoff.php`;
  `php lanes/pandoc/examples/wordpress-native-html-spanlike-handoff.php`
  emitted the WordPress span-like review block; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,930 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader BDO Direction Slice

- Focused mapped count is now 2,132 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage from upstream
  `Text.Pandoc.Readers.HTML` `pBdo` behavior without invoking upstream
  Pandoc, live fetching, a browser, converter shell-outs, PDF processing,
  ZIP/package parsing, citation/CSL engines, or broader XML/HTML parser
  expansion.
- Targeted upstream inspection covered the inline dispatch branch for
  `"bdo" -> pBdo`, the `pBdo` definition, its `dir` lookup, the
  `B.spanWith ("",[],[("dir",T.toLower dir)])` lowering, and the no-dir
  branch that returns plain contents. The PHP slice maps `<bdo dir>` to a
  Pandoc `span` node with lowercased `dir` metadata, preserves nested inline
  content, and returns plain inline contents when `dir` is absent.
- Added `fixtures/upstream-html-bdo-direction.html` and
  `examples/wordpress-native-html-bdo-handoff.php` for a WordPress source
  review packet where directional source title text remains reviewable as a
  span with `dir` metadata.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '677,875p'` inspected the
  upstream inline dispatch and `pBdo` branch; `git -C .upstream-cache/pandoc
  show HEAD:src/Text/Pandoc/Readers/HTML.hs | rg -c
  '"bdo" -> pBdo|pBdo|tagOpen \(=="bdo"\)|lookup "dir"|B\.spanWith
  \("", \[], \[\("dir",T\.toLower dir\)\]\)|Nothing  -> contents'` counted 7
  static source hits around the branch; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-bdo-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-bdo-handoff.php` emitted the
  WordPress bdo direction review block; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,909 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Small Inline Slice

- Focused mapped count is now 2,121 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage from upstream
  `Text.Pandoc.Readers.HTML` `pSmall` behavior without invoking upstream
  Pandoc, live fetching, a browser, converter shell-outs, PDF processing,
  ZIP/package parsing, citation/CSL engines, or broader XML/HTML parser
  expansion.
- Targeted upstream inspection covered the inline dispatch branch for
  `"small" -> pSmall`, the `pSmall` definition, and its
  `B.spanWith ("",["small"],[])` lowering. The PHP slice maps `<small>` to a
  Pandoc `span` node with class `small`, preserves nested inline content, and
  intentionally drops source id/class attributes to match upstream.
- Added `fixtures/upstream-html-small-inline.html` and
  `examples/wordpress-native-html-small-inline-handoff.php` for a WordPress
  source review packet where imported source fine print remains reviewable as
  a classed inline span.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '692,786p'` inspected the
  upstream inline dispatch and `pSmall` branch; `git -C
  .upstream-cache/pandoc show HEAD:src/Text/Pandoc/Readers/HTML.hs | rg -c
  '"small" -> pSmall|pSmall|pInlinesInTags "small"|B\.spanWith
  \("",\["small"\],\[\]\)'` counted 3 static source hits around the branch;
  `php -l` passed for `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-small-inline-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-small-inline-handoff.php`
  emitted the WordPress small-inline review block; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,894 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader SVG Disabled Raw-HTML Slice

- Focused mapped count is now 2,106 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage from upstream
  `Text.Pandoc.Readers.HTML` `pSvg` behavior without invoking upstream
  Pandoc, live fetching, a browser, converter shell-outs, PDF processing,
  ZIP/package parsing, citation/CSL engines, or broader XML/HTML DOM support.
- Targeted upstream inspection covered the `pSvg` branch in
  `src/Text/Pandoc/Readers/HTML.hs`, including `guardDisabled Ext_raw_html`,
  base64 `data:image/svg+xml` image fallback, id/class preservation, and
  Font Awesome `fa-w-14`/`fa-w-16`/`fa-fw` width hints. The PHP slice maps the
  disabled-raw-HTML branch through `htmlRawHtml=false` or `rawHtml=false`; it
  does not claim exact raw-HTML-enabled token passthrough.
- `MarkdownReader` now maps inline `<svg>` nodes to Pandoc `image` nodes with
  data-image URLs, empty alt/title labels, preserved id/classes, and
  `width=1em` for the Font Awesome width classes. This keeps local source SVG
  icons inspectable without running external renderers.
- Added `fixtures/upstream-html-svg-disabled-raw-html.html` and
  `examples/wordpress-native-html-svg-disabled-raw-handoff.php` for a
  WordPress source review packet where an imported SVG icon remains reviewable
  as an inline image.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '830,845p'` inspected the
  upstream branch; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | rg -c
  'pSvg|guardDisabled Ext_raw_html|data:image/svg|fa-w-14|fa-w-16|fa-fw|B.imageWith'`
  counted 11 static source hits around the branch; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-svg-disabled-raw-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-svg-disabled-raw-handoff.php`
  emitted the WordPress SVG review block; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,879 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Iframe Local-Resource Slice

- Focused mapped count is now 2,080 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage from upstream
  `Text.Pandoc.Readers.HTML` `pIframe` behavior without invoking upstream
  Pandoc, live fetching, a browser, converter shell-outs, PDF processing,
  ZIP/package parsing, citation/CSL engines, or broader XML/HTML parser
  expansion.
- Targeted upstream inspection covered the `pIframe` branch in
  `src/Text/Pandoc/Readers/HTML.hs`, including the disabled-raw-HTML guard,
  required `src`, text/html resource parsing, image resource wrapping, generic
  MIME fallback with `src`, and fetch-failure ignore path. The PHP slice maps
  the resource-result branches through explicit local resource injection only;
  it does not claim live `openURL` parity.
- `MarkdownReader` now accepts `htmlIframeResources` as a local resource map.
  Text/html resources are parsed through the native HTML reader into a
  `Div .iframe`; `image/*` resources become a plain image inside `Div
  .iframe`; other MIME values become an empty `Div .iframe` with the resolved
  `src` stored in the native AST.
- Added `fixtures/upstream-html-iframe-local-resource.html` and
  `examples/wordpress-native-html-iframe-handoff.php` for a WordPress source
  review packet where embedded HTML and image frames remain reviewable without
  fetching remote URLs.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate `xml-html5-dom-core`, shared ZIP/OpenXML/OpenDocument, PDF
  handoff/text extraction, citation/CSL, PlainMath/MathML, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '516,540p'` inspected the
  upstream branch; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | rg -n
  'pIframe|iframe|openURL|CouldNotFetchResource'` verified the targeted
  source hits; `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-native-html-iframe-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-iframe-handoff.php` emitted the
  WordPress iframe review blocks; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,853 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Writer Link removeLinks Slice

- Focused mapped count is now 2,072 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `Link` label handling and
  `Text.Pandoc.Writers.Shared` `removeLinks` behavior without invoking
  upstream Pandoc, converter shell-outs, PDF processing, ZIP/package parsing,
  XML/HTML parser expansion, citation/CSL engines, TeX math/ref conversion, or
  broader syntax-highlighting support.
- Targeted upstream inspection covered the two HTML writer `Link` branches
  that call `removeLinks`, the shared `removeLinks` helper that rewrites
  nested `Link` nodes to `Span` nodes, and the 29 `Link` constructors counted
  in `test/testsuite.native`.
- `HtmlWriter` now renders link labels through a recursive links-as-spans pass
  before emitting the outer anchor. Nested link target URL/title metadata is
  dropped, while label ids, classes, and source data attributes are preserved
  on the resulting span. This maps Pandoc's invalid-nested-anchor guard for
  native AST packets and hand-built writer inputs.
- Added `examples/wordpress-html-writer-remove-links-handoff.php` for a
  WordPress source-review packet where a nested source-note link becomes a
  span inside the outer review link, keeping source metadata visible in a
  WordPress HTML block without emitting nested anchors.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate shared ZIP/OpenXML/OpenDocument, PDF handoff/text extraction,
  citation/CSL, PlainMath/MathML, XML/HTML DOM, Unicode/charset, or
  syntax-highlighting support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '1580,1602p'` inspected the
  upstream `Link` branches; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/Shared.hs | sed -n '813,823p'` inspected
  `removeLinks`; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | rg -n 'removeLinks|\(Link'` counted
  two HTML writer link branches using the helper; `git -C
  .upstream-cache/pandoc show HEAD:test/testsuite.native | rg -c '\bLink\b'`
  counted 29 `Link` constructors; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-html-writer-remove-links-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-remove-links-handoff.php`
  emitted the HTML nested-link-label preview and WordPress review block; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,832 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 HTML Writer RawInline Slice

- Focused mapped count is now 2,065 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `RawInline` behavior without invoking upstream
  Pandoc, converter shell-outs, PDF processing, ZIP/package parsing, external
  renderers, citation/CSL engines, or broader syntax-highlighting support.
- Targeted upstream inspection covered the `RawInline` branch in
  `src/Text/Pandoc/Writers/HTML.hs`, the single `RawInline` constructor
  occurrence in `test/testsuite.native`, and `test/command/parse-raw.md` raw
  attribute examples. This slice maps HTML-family raw inline pass-through and
  non-HTML raw inline omission. TeX math-environment/ref conversion from raw
  TeX inlines is explicitly not claimed and remains behind a future bounded
  math support gate if required.
- `HtmlWriter` now renders `raw_inline` nodes whose format is `html`,
  `html4`, `html5`, EPUB, or supported HTML slide formats as pre-escaped HTML,
  and renders other raw inline formats as empty output on the HTML writer path.
- Added `examples/wordpress-html-writer-raw-inline-handoff.php` for a
  WordPress source-review packet that preserves trusted inline HTML badges and
  markups inside a reviewable block-editor HTML section while omitting a TeX
  citation payload from the HTML preview.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate shared ZIP/OpenXML/OpenDocument, PDF handoff/text extraction,
  citation/CSL, PlainMath/MathML, Unicode/charset, or syntax-highlighting
  support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '1515,1595p'` inspected the
  upstream `RawInline` branch; `git -C .upstream-cache/pandoc show
  HEAD:test/testsuite.native | rg -c '\bRawInline\b'` counted 1 constructor
  occurrence; `git -C .upstream-cache/pandoc show
  HEAD:test/command/parse-raw.md` inspected raw-attribute examples; `php -l`
  passed for `HtmlWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-html-writer-raw-inline-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-raw-inline-handoff.php` emitted
  the HTML raw-inline preview and WordPress review block; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,825 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 HTML Writer SoftBreak/LineBreak Slice

- Focused mapped count is now 2,057 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `inlineToHtml` break handling without invoking
  upstream Pandoc, converter shell-outs, PDF processing, ZIP/package parsing,
  external renderers, citation/CSL engines, or broader syntax-highlighting
  support.
- Targeted upstream inspection covered `SoftBreak` output under `WrapAuto`,
  `WrapNone`, and `WrapPreserve`, the hard `LineBreak` branch that emits
  `<br />` plus a following newline, `Text.Pandoc.Options.WrapOption`
  spellings/defaults, and the 30 `SoftBreak`/`LineBreak` occurrences in
  `test/testsuite.native`.
- `HtmlWriter` now defaults softbreaks to a space, preserves source line folds
  only when `writerWrapText`/`wrap` is `preserve` or `wrap-preserve`, and
  keeps hard line breaks as semantic HTML breaks followed by a newline.
- Added `examples/wordpress-html-writer-softbreak-handoff.php` for a
  WordPress source-review packet that compares compact HTML output with
  source-line-preserving output inside a reviewable block-editor HTML section.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate shared ZIP/OpenXML/OpenDocument, PDF handoff/text extraction,
  citation/CSL, PlainMath/MathML, Unicode/charset, or syntax-highlighting
  support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '1400,1425p'` inspected the
  upstream break branches; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Options.hs | rg -n
  'WrapNone|WrapAuto|WrapPreserve|writerWrapText' -C 2` inspected option
  spellings/defaults; `git -C .upstream-cache/pandoc show
  HEAD:test/testsuite.native | rg -c '\bSoftBreak\b|\bLineBreak\b'` counted
  30 constructor occurrences; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-html-writer-softbreak-handoff.php`;
  `php lanes/pandoc/examples/wordpress-html-writer-softbreak-handoff.php`
  emitted compact, source-line-preserving, and WordPress review block output;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
  1 file, 3,817 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Writer Span-Like Class Lowering Slice

- Focused mapped count is now 2,049 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `Span` class lowering without invoking upstream
  Pandoc, converter shell-outs, PDF processing, ZIP/package parsing, external
  renderers, citation/CSL engines, or broader syntax-highlighting support.
- Targeted upstream inspection covered the `Span` branch in
  `src/Text/Pandoc/Writers/HTML.hs`, `htmlSpanLikeElements` in
  `src/Text/Pandoc/Shared.hs`, and `test/command/nested-spanlike.md`. The
  slice maps `kbd`, `mark`, `dfn`, and `abbr` span-like elements,
  `smallcaps` and `underline` composition, class dropping before the first
  span-like marker, later non-special class retention on the outer lowered
  tag, id/data/title attribute carrying, and `csl-no-emph`,
  `csl-no-strong`, and `csl-no-smallcaps` style reset markers.
- Added `examples/wordpress-html-writer-spanlike-handoff.php` for a WordPress
  source-review packet that preserves keyboard shortcuts, highlighted publish
  preview text, and abbr/dfn source notes inside a reviewable block-editor
  HTML section.
- Dependency-closure checkpoint: this slice remains lane-local. It does not
  activate shared ZIP/OpenXML/OpenDocument, PDF handoff/text extraction,
  citation/CSL, PlainMath/MathML, Unicode/charset, or syntax-highlighting
  support rows.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '1408,1452p'` inspected the
  upstream `Span` branch; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Shared.hs | rg -n 'htmlSpanLikeElements' -C 3`
  inspected the bounded element set; `git -C .upstream-cache/pandoc show
  HEAD:test/command/nested-spanlike.md` inspected the command example; `php -l`
  passed for `HtmlWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-html-writer-spanlike-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-spanlike-handoff.php` emitted the
  HTML span-like preview and WordPress review block; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,807 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Writer Media Category Slice

- Focused mapped count is now 2,027 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` image media-category output and
  `Text.Pandoc.MIME.mediaCategory` behavior without invoking upstream Pandoc,
  fetching media, converter shell-outs, PDF processing, ZIP/package parsing,
  external renderers, or broader rich document-format support.
- Targeted upstream inspection covered `test/command/video-audio.md` with five
  command-output cases, `src/Text/Pandoc/Writers/HTML.hs` `Image` branch cases
  for image/video/audio/embed output, and `src/Text/Pandoc/MIME.hs`
  `mediaCategory`. The slice maps mp4/webm video output with fallback links
  and `controls`, mp3 audio output, PDF generic embed output, ordinary image
  output, image text fallback labels, and width attributes on media nodes.
- Added `examples/wordpress-html-writer-media-handoff.php` for a WordPress
  source-review packet that preserves the HTML media preview inside a
  reviewable block-editor HTML section.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/command/video-audio.md` inspected the five media command examples;
  `git -C .upstream-cache/pandoc show HEAD:src/Text/Pandoc/Writers/HTML.hs |
  sed -n '1600,1640p'` inspected the upstream image/media branch; `git -C
  .upstream-cache/pandoc show HEAD:src/Text/Pandoc/MIME.hs | sed -n '1,70p'`
  inspected `mediaCategory`; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-html-writer-media-handoff.php`;
  `php lanes/pandoc/examples/wordpress-html-writer-media-handoff.php` emitted
  the HTML media preview and WordPress review block; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,789 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Figure/Figcaption Slice

- Focused mapped count is now 2,017 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage from upstream
  `Text.Pandoc.Readers.HTML` `pFigure` and `pImage` behavior without invoking
  upstream Pandoc, a browser, converter shell-outs, ZIP/package parsing,
  external renderers, or broader XML/HTML support-library expansion.
- Targeted upstream inspection covered `src/Text/Pandoc/Readers/HTML.hs`
  `pFigure`/`pImage` branches, `test/command/html-read-figure.md` with five
  reader command examples, and `test/command/figures-html.md` with two
  reader figure examples. The slice maps caption-before and caption-after
  ordering, missing captions, p-wrapped image bodies, rich caption inlines,
  preserved figure classes/data attributes, bare image `Plain` body blocks,
  and list body content inside figures.
- Added `fixtures/upstream-html-figure-caption.html` and
  `examples/wordpress-native-html-figure-handoff.php` for a WordPress
  source-media review packet that keeps source media id/classes, image
  alt/title metadata, and rich figcaption links/emphasis reviewable as a
  core image block.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '632,652p'` inspected
  `pFigure`; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '812,850p'` inspected
  `pImage`; `git -C .upstream-cache/pandoc show
  HEAD:test/command/html-read-figure.md` inspected five figure reader
  examples; `git -C .upstream-cache/pandoc show HEAD:test/command/figures-html.md`
  inspected two reader examples; `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and `wordpress-native-html-figure-handoff.php`;
  `php lanes/pandoc/examples/wordpress-native-html-figure-handoff.php`
  emitted the WordPress image-block preview; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,783 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Reader Native Div-Like Section/Aside Slice

- Focused mapped count is now 2,005 Markdown/HTML/Native/WordPress checks.
- Added bounded `MarkdownReader` coverage from upstream
  `Text.Pandoc.Readers.HTML` native div-like parsing for `section` and
  `aside` under `Ext_native_divs`, extending the existing `main` and `header`
  coverage without invoking upstream Pandoc, a browser, converter shell-outs,
  ZIP/package parsing, external renderers, or broader XML/HTML support-library
  expansion.
- Targeted upstream inspection covered `test/Tests/Readers/HTML.hs` native
  div tests for `main`/`header` and `src/Text/Pandoc/Readers/HTML.hs`
  `isDivLike`/`pDiv` branches for `div`, `section`, `header`, `main`, and
  `aside`. The slice maps section/aside wrapper classes, preserved
  id/class/data attributes, nested block contents, and the upstream rule that
  clears a first heading id when it is the same as the native div wrapper id.
- Added `fixtures/upstream-html-section-aside-native-divs.html` and
  `examples/wordpress-native-html-section-aside-handoff.php` for a WordPress
  source-review packet that keeps source section content and migration-note
  aside content reviewable as block-editor HTML with stable wrapper metadata.
- Additional static smoke evidence: a local PHP sweep over upstream Native
  expectation files under `test/odt/native`, `test/docx`, `test/epub`, and
  `test/html-reader.native` parsed 145 files, rendered them through
  `NativeWriter`, and rendered them through `WordPressBlockWriter` with 0
  exceptions. This is not full upstream runner parity and does not claim all
  fixture semantics, but it strengthens the cloned static denominator evidence.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Readers/HTML.hs | sed -n '80,135p'` inspected native-div
  tests; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Readers/HTML.hs | sed -n '480,520p'` and `sed -n
  '190,246p'` inspected `isDivLike`/`pDiv` branches; `php -l` passed for
  `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `wordpress-native-html-section-aside-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-section-aside-handoff.php`
  emitted the main/section/aside WordPress preview; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,754 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Writer Math Method Slice

- Focused mapped count is now 1,995 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` math output branches for MathJax, KaTeX,
  WebTeX, and GladTeX methods without invoking upstream Pandoc, TeXMath
  conversion, MathML conversion, image fetching, browser tooling, converter
  shell-outs, ZIP/package parsing, external renderers, or a broader math
  support library.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. Targeted subinventory for this slice
  inspected the upstream `Math` branch in
  `src/Text/Pandoc/Writers/HTML.hs`, the default writer option in
  `src/Text/Pandoc/Options.hs`, counted 7 `Math` constructors in
  `test/testsuite.native` including 6 `InlineMath` and 1 `DisplayMath`, and
  counted 2 math-targeted Markdown command fixtures by `git ls-tree` path
  inspection.
- `HtmlWriter` now renders bounded `math` AST nodes as
  `span.math.inline`/`span.math.display` output, preserving MathJax delimiters
  for `htmlMathMethod=mathjax` and raw TeX payloads for
  `writerHTMLMathMethod=katex`. It also renders WebTeX as `img` tags with
  vertical-align style, `math inline`/`math display` classes, stripped TeX in
  `alt`/`title`, and URL-encoded `\textstyle`/`\displaystyle` TeX payloads,
  plus GladTeX `eq` tags with `env="math"` or `env="displaymath"`.
  Unsupported PlainMath/MathML conversion stays an escaped TeX fallback rather
  than claiming TeX parser/MathML parity.
- Added `examples/wordpress-html-writer-math-handoff.php` for a WordPress
  source-review packet that emits MathJax, KaTeX, WebTeX, and GladTeX HTML
  previews while the matching `WordPressBlockWriter` output keeps equation
  source reviewable as block editor HTML.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '1500,1575p'` inspected the
  WebTeX/GladTeX/MathJax/KaTeX/PlainMath/MathML branches; `git -C
  .upstream-cache/pandoc show HEAD:src/Text/Pandoc/Options.hs | rg -n
  'HTMLMathMethod|MathJax|KaTeX|WebTeX|GladTeX|PlainMath|MathML|writerHTMLMathMethod|defaultWebTeXURL'`
  inspected defaults and method names; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/URI.hs | sed -n '1,80p'` inspected the `urlEncode`
  wiring; `git -C .upstream-cache/pandoc show
  HEAD:test/testsuite.native | rg -c '\bMath\b'` counted 7 Math constructors;
  `rg -c 'InlineMath'` counted 6 and `rg -c 'DisplayMath'` counted 1; `git -C
  .upstream-cache/pandoc ls-tree -r --name-only HEAD test/command | rg -c
  '(math|mathml|katex|mathjax|latex-math|tex-group).*\.md$'` counted 2 command
  fixtures; `php -l` passed for `HtmlWriter.php`, `MarkdownReaderTest.php`,
  and `wordpress-html-writer-math-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-math-handoff.php` emitted
  MathJax, KaTeX, WebTeX, GladTeX, and WordPress previews; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,732 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Writer Citation Role Slice

- Focused mapped count is now 1,979 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` citation and footnote role branches without
  invoking upstream Pandoc, citeproc/CSL processing, browser tooling,
  converter shell-outs, ZIP/package parsing, external renderers, or rich
  document-format support.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. Targeted subinventory for this slice
  inspected the upstream `Note`, `Cite`, `addBibliorefRole`, `data-cites`,
  `doc-noteref`, and `doc-backlink` branches in
  `src/Text/Pandoc/Writers/HTML.hs`, counted 5 `Note` constructors in
  `test/testsuite.native`, and counted 72 footnote/citation Markdown command
  fixtures by `git ls-tree` path inspection.
- `HtmlWriter` now renders bounded `citation` AST nodes as
  `<span class="citation" data-cites="...">`, adds `role="doc-biblioref"` to
  citation display links targeting `#ref-*`, preserves ordinary citation
  display links as ordinary links, and emits `role="doc-noteref"` and
  `role="doc-backlink"` on HTML footnote reference/backlink anchors.
- Added `examples/wordpress-html-writer-citation-role-handoff.php` for a
  WordPress source-review packet that emits an accessible HTML citation and
  footnote preview while the matching `WordPressBlockWriter` output keeps the
  citation payload and bibliography source reviewable as WordPress blocks.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | rg -n
  '\(Note contents\)|\(Cite cits il\)|addBibliorefRole|doc-noteref|doc-backlink|footnote-back|data-cites'`
  inspected the branch targets; `git -C .upstream-cache/pandoc show
  HEAD:test/testsuite.native | rg -c '\bNote\b'` counted 5 Note constructors;
  `git -C .upstream-cache/pandoc ls-tree -r --name-only HEAD test/command |
  rg -c '(footnote|note|citeproc|citation|cite-in-inline-note|locators).*\.md$'`
  counted 72 command fixtures; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-html-writer-citation-role-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-citation-role-handoff.php`
  emitted the HTML preview plus WordPress source-block handoff; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,716 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Writer Wrapper/CSL Div Slice

- Focused mapped count is now 1,971 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `Div` wrapper and CSL bibliography branches
  without invoking upstream Pandoc, a CSL processor, browser tooling,
  converter shell-outs, ZIP/package parsing, external renderers, or rich
  document-format support.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. Targeted subinventory for this slice counted
  26 upstream `test/command/*.csl` files and 70 citeproc/citation Markdown
  command fixtures by `git ls-tree` path inspection, kept the existing 5
  `Div` constructors in `test/testsuite.native`, and inspected the upstream
  wrapper transfer, `refs`/`csl-bib-body`, `csl-entry`, role, entry-spacing,
  and `paraToPlain` branches in `src/Text/Pandoc/Writers/HTML.hs`.
- `HtmlWriter` now unwraps `Div` nodes with `wrapper=1` onto the single child
  root element, removing the wrapper marker while preserving id/classes and
  data attributes. CSL bibliography bodies emit `role="list"`, CSL entries
  emit `role="listitem"`, and paragraphs inside `csl-entry` nodes render as
  plain inline content instead of nested paragraphs.
- Added `examples/wordpress-html-writer-csl-wrapper-handoff.php` for a
  WordPress bibliography review packet that emits an accessible HTML preview
  and the matching WordPress HTML-block source handoff.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | rg -n
  'wrapper|csl-bib-body|csl-entry|entry-spacing|role"|paraToPlain'` inspected
  the branch targets; `git -C .upstream-cache/pandoc ls-tree -r --name-only
  HEAD test/command | rg -c '\.csl$'` counted 26 CSL files; `git -C
  .upstream-cache/pandoc ls-tree -r --name-only HEAD test/command | rg -c
  '(citeproc|cite-in-inline-note|locators).*\.md$'` counted 70 citation
  command fixtures; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-html-writer-csl-wrapper-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-csl-wrapper-handoff.php` emitted
  the wrapper paragraph preview plus CSL role/list preview and WordPress
  blocks; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 3,712 assertions, 0 failures. No no-argument root harness
  was assigned or started.

## 2026-05-24 HTML Writer RawBlock/Div Slice

- Focused mapped count is now 1,961 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `Div` and `RawBlock` rendering without invoking
  upstream Pandoc, browser tooling, converter shell-outs, ZIP/package parsing,
  external renderers, or rich document-format support.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. Targeted subinventory for this slice counted
  23 `RawBlock` constructors and 5 `Div` constructors in
  `test/testsuite.native`, and inspected the upstream `Div`, generic div,
  section div, `RawBlock`, `isRawHtml`, EPUB raw-format, and slide raw-format
  branches.
- `HtmlWriter` now renders bounded `div` AST nodes as `div` wrappers,
  section-class divs as `section` elements, nested block contents inside those
  wrappers, and column `width` keyvals as `style` width declarations. It
  passes through HTML-format raw blocks (`html`, `html4`, `html5`, EPUB, and
  HTML slide variants) and omits non-HTML raw blocks in HTML output.
- Added `examples/wordpress-html-writer-raw-div-handoff.php` for a WordPress
  source-review packet that emits an HTML section preview with raw HTML and
  nested div structure while the matching `WordPressBlockWriter` output keeps
  raw source packets reviewable inside the div handoff.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '700,860p'`, `sed -n
  '860,960p'`, `sed -n '1788,1812p'`, and `sed -n '1812,1832p'` were
  inspected for div/raw-block writer semantics; `git -C .upstream-cache/pandoc
  show HEAD:test/testsuite.native | rg -c "RawBlock"` counted 23 constructors
  and `rg -c "Div \\("` counted 5 constructors; `php -l` passed for
  `HtmlWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-html-writer-raw-div-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-raw-div-handoff.php` emitted an
  HTML section preview plus WordPress raw-source review handoff; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,706 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 HTML Writer Table Slice

- Focused mapped count is now 1,953 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `Table` rendering without invoking upstream
  Pandoc, browser tooling, converter shell-outs, ZIP/package parsing, external
  renderers, or rich document-format support.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. Targeted subinventory for this slice counted
  1 `Table` constructor in `test/testsuite.native` and inspected the upstream
  `tableToHtml`, `tableBodyToHtml`, `tableHeadToHtml`, `tableFootToHtml`,
  `tablePartToHtml`, `colSpecListToHtml`, `tableRowToHtml`, and
  `tableCellToHtml` branches for caption, colgroup, section, row, cell,
  colspan/rowspan, row-head, and alignment behavior.
- `HtmlWriter` now renders bounded `table` AST nodes as HTML tables with
  table id/classes/key/value attributes, caption inlines, colgroups from
  explicit column widths, table-level width style when explicit widths sum
  below 100%, `thead`/`tbody`/`tfoot` sections, intermediate body head rows,
  row-head columns, cell spans, escaped block cell content, and column/cell
  alignment styles.
- Added `examples/wordpress-html-writer-table-handoff.php` for a WordPress
  source-review packet that emits an HTML table preview with caption,
  colgroup, section metadata, row-head cells, spans, and review text while the
  matching `WordPressBlockWriter` output emits a core table handoff.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '1140,1188p'`,
  `sed -n '1180,1375p'`, and `sed -n '1375,1495p'` were inspected for table
  writer semantics; `git -C .upstream-cache/pandoc show
  HEAD:test/testsuite.native | rg -n "Table|TableHead|TableBody|TableFoot|Caption|ColSpec"`
  found the table fixture area; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-html-writer-table-handoff.php`;
  `php lanes/pandoc/examples/wordpress-html-writer-table-handoff.php` emitted
  an HTML table preview plus WordPress table handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,698 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Writer Structural Block Slice

- Focused mapped count is now 1,940 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `HorizontalRule`, `LineBlock`, and `Figure`
  branches without invoking upstream Pandoc, browser tooling, converter
  shell-outs, ZIP/package parsing, external renderers, or rich document-format
  support.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. Targeted subinventory for this slice counted
  13 `HorizontalRule` occurrences and 1 `Figure` occurrence in
  `test/testsuite.native`, and inspected the upstream HTML writer branches for
  `<hr />`, `div.line-block` with `LineBreak` separators, HTML5 `figure`
  output, `figcaption`, and `aria-hidden` captions when the caption text
  matches the image alt text.
- `HtmlWriter` now renders bounded `horizontal_rule`, `line_block`, and
  `figure` AST nodes as native HTML output. Figure output preserves
  id/classes/key/value attributes on `<figure>`, renders the bounded body
  blocks, emits captions, and marks alt-equivalent captions with
  `aria-hidden="true"`.
- Added `examples/wordpress-html-writer-figure-line-handoff.php` for a
  WordPress source-review packet that emits an HTML figure/line-block/section
  break preview while the matching `WordPressBlockWriter` output emits image,
  paragraph, and separator block handoff markup.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '760,780p'`, `sed -n
  '936,966p'`, and `sed -n '1080,1116p'` were inspected for the `LineBlock`,
  `HorizontalRule`, and `Figure` branches; `git -C .upstream-cache/pandoc show
  HEAD:test/testsuite.native | rg -c 'HorizontalRule'` counted 13
  occurrences; `git -C .upstream-cache/pandoc show HEAD:test/testsuite.native
  | rg -c '^  , Figure|^  \\[ Figure|Figure'` counted 1 occurrence; `php -l`
  passed for `HtmlWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-html-writer-figure-line-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-figure-line-handoff.php` emitted
  an HTML preview plus WordPress image/paragraph/separator handoff; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,694 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Writer CodeBlock Fallback Slice

- Focused mapped count is now 1,928 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `CodeBlock` fallback behavior without invoking
  upstream Pandoc, browser tooling, converter shell-outs, ZIP/package parsing,
  external renderers, or a syntax-highlighting engine.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. Targeted subinventory for this slice counted
  11 `CodeBlock` constructors in `test/testsuite.native` and inspected the
  upstream HTML writer `CodeBlock` branch where non-highlighted fallback output
  renders `pre > code` and keeps the original identifier, classes, and
  key/value attributes on `pre`.
- `HtmlWriter` now renders bounded `code_block` AST nodes as HTML
  `<pre><code>...</code></pre>` output, escapes code text, preserves multiline
  code, and renders fallback id/class/data attributes on `pre`.
- Added `examples/wordpress-html-writer-code-block-handoff.php` for a
  WordPress source-review packet that emits an HTML code-block preview with
  source attributes while the matching `WordPressBlockWriter` output emits a
  block-editor code handoff.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '949,1000p'` was inspected
  for `CodeBlock` fallback semantics; `git -C .upstream-cache/pandoc show
  HEAD:test/testsuite.native | rg -c 'CodeBlock'` counted 11 constructors;
  `php -l` passed for `HtmlWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-html-writer-code-block-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-code-block-handoff.php` emitted
  an HTML code-block preview plus WordPress code-block handoff; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,684 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Writer List Slice

- Focused mapped count is now 1,922 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream
  `Text.Pandoc.Writers.HTML` `BulletList` and `OrderedList` branches plus
  `test/testsuite.native` list constructor shapes without invoking upstream
  Pandoc, browser tooling, converter shell-outs, ZIP/package parsing, or rich
  document-format support.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. Targeted subinventory for this slice counted
  17 `BulletList` and 19 `OrderedList` constructors in `test/testsuite.native`
  and inspected the upstream HTML writer list branches for list item wrapping,
  `ul`/`ol` emission, task-list class handling, ordered `start` attributes,
  and HTML5 ordered-list `type` attributes for decimal/roman/alpha styles.
- `HtmlWriter` now renders bounded `bullet_list` and `ordered_list` AST nodes
  as HTML lists. Tight Plain-shaped items render as bare `li` content, loose
  Para-shaped items keep paragraph wrappers, nested lists remain inside the
  parent `li`, explicit ordered-list styles emit `type`, non-1 starts emit
  `start`, and task-list items render checkbox labels under `ul.task-list`.
- Added `examples/wordpress-html-writer-list-handoff.php` for a WordPress
  source-review packet that emits an HTML import preview with ordered
  start/type metadata, nested list evidence, and task checkbox labels while the
  matching `WordPressBlockWriter` output emits block-list handoff markup.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/HTML.hs | sed -n '1048,1075p'` was inspected
  for `BulletList`/`OrderedList` branches; `sed -n '480,530p'` and `sed -n
  '1370,1382p'` were inspected for list item wrapping; `git -C
  .upstream-cache/pandoc show HEAD:test/testsuite.native | sed -n '340,450p'`
  was inspected for tight/loose/nested list fixture shapes; `rg -c` over that
  targeted fixture counted 17 `BulletList` and 19 `OrderedList` constructors;
  `php -l` passed for `HtmlWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-html-writer-list-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-list-handoff.php` emitted HTML
  list preview plus WordPress block-list handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,679 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 LaTeX Writer Ordered-List Slice

- Focused mapped count is now 1,908 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream
  `Text.Pandoc.Writers.LaTeX` `OrderedList` and `Text.Pandoc.Shared`
  `isTightList` behavior without invoking upstream Pandoc, a TeX/PDF engine,
  templates, converter shell-outs, or rich package/document-format support.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps upstream ordered-list label
  and counter semantics: explicit lower-roman, decimal, and upper-alpha styles
  emit LaTeX `\def\label...` commands; non-1 starts emit `\setcounter`; nested
  ordered lists use `enumii`; tight lists emit `\tightlist`; and paragraph
  list items remain loose without `\tightlist`.
- `LatexWriter` now tracks nested ordered-list depth, renders first-four-level
  enumerate counters (`enumi` through `enumiv`), applies Pandoc-style label
  commands for one-paren/two-parens/period delimiters, preserves start offsets,
  and indents nested list output inside list items. The existing blockquote and
  inline-code list expectations were updated to include Pandoc's tight-list
  line for tight list shapes.
- Added `examples/wordpress-latex-ordered-list-handoff.php` for a WordPress
  source review packet that emits LaTeX ordered-list labels/counters while the
  matching WordPress block handoff keeps `start` and `type` metadata on the
  ordered lists.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/LaTeX.hs | sed -n '615,650p'` was inspected for
  `OrderedList` label/counter/tight-list behavior; `git -C
  .upstream-cache/pandoc show HEAD:src/Text/Pandoc/Shared.hs | sed -n
  '648,660p'` was inspected for `isTightList`; `php -l` passed for
  `LatexWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-latex-ordered-list-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-ordered-list-handoff.php` emitted
  LaTeX ordered-list counter output plus WordPress ordered-list handoff; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,670 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 LaTeX Writer Quote/Horizontal-Rule Slice

- Focused mapped count is now 1,898 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream
  `Text.Pandoc.Writers.LaTeX` `BlockQuote` and `HorizontalRule` branches
  without invoking upstream Pandoc, a TeX/PDF engine, templates, or converter
  shell-outs.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the direct upstream writer
  semantics where block quotes render as `quote` environments and horizontal
  rules render as Pandoc's centered `\rule{0.5\linewidth}{0.5pt}` block. It
  also maps the `test/testsuite.native` Block Quotes and horizontal-rule
  fixture shapes, including quote children with paragraphs, code blocks,
  ordered lists, nested quotes, and section separators.
- `LatexWriter` now renders bounded `blockquote` AST nodes as nested LaTeX
  quote environments and renders `horizontal_rule` nodes as the centered rule
  output used by upstream Pandoc. This is a writer branch only and does not
  activate TeX/PDF execution, doctemplates, syntax highlighting, ZIP/package,
  OpenXML/OpenDocument, or rich document-format support gates.
- Added `examples/wordpress-latex-quote-hr-handoff.php` for a WordPress source
  review packet that emits a LaTeX quote plus centered section break while the
  matching WordPress block handoff keeps a core quote block, separator block,
  and following publish checklist paragraph.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/LaTeX.hs | sed -n '470,510p'` and `sed -n
  '660,672p'` were inspected for `BlockQuote` and `HorizontalRule` semantics;
  `git -C .upstream-cache/pandoc show HEAD:test/testsuite.native | sed -n
  '230,296p'` was inspected for the Block Quotes fixture shape; `php -l`
  passed for `LatexWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-latex-quote-hr-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-quote-hr-handoff.php` emitted LaTeX
  quote/rule output plus WordPress quote/separator handoff; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,663 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 LaTeX Writer Raw TeX Passthrough Slice

- Focused mapped count is now 1,891 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream
  `Text.Pandoc.Writers.LaTeX` `RawInline` and `RawBlock` branches without
  invoking upstream Pandoc, a TeX/PDF engine, templates, or a converter
  shell-out.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the direct upstream writer
  semantics where `Format "tex"` and `Format "latex"` raw inlines/blocks pass
  through literally, while non-LaTeX raw formats are omitted from LaTeX output.
  It also maps the `test/testsuite.native` LaTeX section fixtures for
  `RawInline (Format "tex") "\\cite[22-23]{smith.1899}"` and the tabular
  `RawBlock (Format "tex")` packet.
- `LatexWriter` now renders bounded raw TeX/LaTeX inline and block AST nodes
  literally and keeps non-LaTeX raw nodes out of LaTeX output. This is a writer
  passthrough branch only and does not activate TeX/PDF execution,
  doctemplates, syntax highlighting, ZIP/package, OpenXML/OpenDocument, or rich
  document-format support gates.
- Added `examples/wordpress-latex-raw-tex-handoff.php` for a WordPress source
  review packet that emits raw citation/table TeX in the LaTeX reviewer export
  while the matching WordPress block handoff preserves raw TeX as review-safe
  inline/code markup.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/LaTeX.hs | sed -n '570,592p'` and `sed -n
  '1070,1112p'` were inspected for the `RawBlock`/`RawInline` passthrough
  branches; `git -C .upstream-cache/pandoc show HEAD:test/testsuite.native |
  sed -n '1338,1425p'` and `sed -n '1488,1514p'` were inspected for the raw
  TeX citation and tabular fixtures; `php -l` passed for `LatexWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-latex-raw-tex-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-raw-tex-handoff.php` emitted raw TeX
  LaTeX output plus WordPress block handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,657 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 LaTeX Writer Highlighted Strikeout Inline-Code Slice

- Focused mapped count is now 1,883 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  `inline code` behavior for the `struck out and highlighted` case without
  invoking upstream Pandoc, a TeX/PDF engine, templates, or a syntax
  highlighting engine.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the direct upstream case
  where `strikeout (codeWith ("",["haskell"],[]) "foo" <> space <> str
  "bar")` renders as `\st{\mbox{\VERB|\NormalTok{foo}|} bar}`.
- `LatexWriter` now renders bounded Haskell inline code spans as
  `\VERB|\NormalTok{...}|` under default inline highlighting and keeps
  Pandoc's `\mbox{...}` protection inside strikeout. The disabled-highlight
  guard still falls back to `\texttt{...}`. This is a single inline-code
  token boundary and does not activate the broader syntax-highlighting,
  TeX/PDF, document-template, ZIP/package, or rich document-format support
  gates.
- Added `examples/wordpress-latex-highlighted-strikeout-code-handoff.php` for
  a WordPress source-review packet that emits highlighted strikeout LaTeX for
  a Haskell import helper while the matching WordPress block handoff preserves
  `<del>`, inline `code` class, and `data-source` metadata.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '55,95p'` was inspected for the
  bounded inline-code group; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Writers/LaTeX.hs | sed -n '1000,1125p'` was inspected
  for the `Code`/`VERB`/soul-command protection boundary; `php -l` passed for
  `LatexWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-latex-highlighted-strikeout-code-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-highlighted-strikeout-code-handoff.php`
  emitted highlighted strikeout LaTeX plus WordPress block handoff; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,652 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 LaTeX Writer Listing Code-Block Slice

- Focused mapped count is now 1,878 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  `code blocks` behavior for `writerHighlightMethod = IdiomaticHighlighting`
  without invoking upstream Pandoc, a TeX/PDF engine, templates, or a syntax
  highlighting engine.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps two direct upstream cases:
  `codeBlockWith ("id",[],[]) "hi"` renders as `lstlisting` with
  `[label=id]`, and a plain `codeBlock "hi"` renders as `lstlisting` without
  an option bracket. Default `Verbatim` output remains covered by the
  footnote-code-block slice.
- `LatexWriter` now accepts `writerHighlightMethod`/`highlightMethod` values
  for IdiomaticHighlighting/listing output and renders bounded `code_block`
  AST nodes as `\begin{lstlisting}` / `\end{lstlisting}`, preserving node ids
  as listing labels. This is a writer option branch only; it does not activate
  broader TeX/PDF, document-template, syntax-highlighting, ZIP/package, or rich
  document-format support-library gates.
- Added `examples/wordpress-latex-listing-code-handoff.php` for a WordPress
  source-snippet review packet that emits LaTeX listing output with a stable
  snippet label while the WordPress block handoff preserves the code-block id,
  language class, and source metadata.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '35,75p'` was inspected for the
  bounded code-block listing group; `php -l` passed for `LatexWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-latex-listing-code-handoff.php`;
  `php lanes/pandoc/examples/wordpress-latex-listing-code-handoff.php` emitted
  `lstlisting` LaTeX output plus WordPress code-block handoff; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,648 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 LaTeX Writer Underline/Strikeout Inline-Note Slice

- Focused mapped count is now 1,872 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  `inline note` behavior for `underline` and `strikeout`, plus the adjacent
  strikeout code-span mbox shape, without invoking upstream Pandoc, a TeX/PDF
  engine, templates, or syntax-highlighting tooling.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps three direct upstream cases:
  multi-paragraph notes split outside `\ul{}`, multi-paragraph notes split
  outside `\st{}`, and strikeout code spans render as
  `\st{\mbox{\texttt{foo}} bar}`. The prior emph/strong inline-note cases
  remain covered.
- `LatexWriter` now renders bounded underline and strikeout inline AST nodes as
  LaTeX style commands, treats multi-block notes inside those style scopes as
  style boundaries with indented continuation footnote blocks, and wraps code
  spans inside strikeout with `\mbox{}`. This does not activate broader TeX/PDF,
  document-template, syntax-highlighting, ZIP/package, or rich
  document-format support-library gates.
- Added `examples/wordpress-latex-underline-strikeout-note-handoff.php` for a
  WordPress review packet that emits split LaTeX insertion/deletion mark output
  while preserving WordPress `<u>`/`<del>` inline markup plus endnotes.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '80,170p'` was inspected for the
  bounded inline-note and strikeout-code group; `php -l` passed for
  `LatexWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-latex-underline-strikeout-note-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-underline-strikeout-note-handoff.php`
  emitted split LaTeX underline/strikeout note output plus WordPress block
  handoff; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 3,642 assertions, 0 failures. No no-argument root harness
  was assigned or started.

## 2026-05-24 LaTeX Writer Styled Inline-Note Slice

- Focused mapped count is now 1,866 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  `inline note` behavior for `emph` and `strong` without invoking upstream
  Pandoc, a TeX/PDF engine, templates, or syntax-highlighting tooling.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps five direct upstream cases
  from the inline-note group: multi-paragraph notes split outside `\emph{}`,
  multi-paragraph notes split outside `\textbf{}`, single-paragraph notes stay
  inside `\emph{}`, nested `emph`/`strong` note splits close and reopen both
  wrappers, and two multi-paragraph notes in one emphasized run split twice.
  Underline and strikeout note branches remain unmapped.
- `LatexWriter` now renders bounded emphasized and strong inline AST nodes as
  LaTeX style commands and treats multi-block notes inside those style scopes
  as style boundaries with indented continuation footnote blocks. This does not
  activate broader TeX/PDF, document-template, syntax-highlighting, ZIP/package,
  or rich document-format support-library gates.
- Added `examples/wordpress-latex-inline-note-style-handoff.php` for a
  WordPress review packet that emits the split LaTeX reviewer output while
  preserving emphasized/strong WordPress inline markup plus endnotes.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '80,160p'` was inspected for the
  bounded inline-note group; `php -l` passed for `LatexWriter.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-latex-inline-note-style-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-inline-note-style-handoff.php` emitted
  split LaTeX emphasized/strong note output plus WordPress block handoff; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,634 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 LaTeX Writer Top-Level Division Slice

- Focused mapped count is now 1,856 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  `writer options` / `top-level division` behavior without invoking upstream
  Pandoc, a TeX/PDF engine, or syntax-highlighting tooling.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps four direct normal-LaTeX
  writer option cases from the upstream group: `TopLevelChapter` maps level
  1/2/3 headings to `\chapter`, `\section`, and `\subsection`;
  `TopLevelPart` maps them to `\part`, `\chapter`, and `\section`;
  `TopLevelDefault` stays on section-style defaults; and an unnumbered
  part-level heading emits `\part*` plus `\addcontentsline{toc}{part}{...}`.
  The remaining beamer-specific branches stay unmapped.
- `LatexWriter` now accepts `writerTopLevelDivision`/`topLevelDivision`
  options and uses the selected command family for regular and unnumbered
  heading output. This does not activate broader TeX/PDF, document-template,
  ZIP/package, syntax-highlighting, or document-format support-library gates.
- Added `examples/wordpress-latex-top-level-division-handoff.php` for a
  WordPress review packet that emits LaTeX chapter/section reviewer output
  while preserving ordinary WordPress heading blocks for source book hierarchy.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '120,230p'` was inspected for the
  bounded writer-options group; `php -l` passed for `LatexWriter.php`,
  `MarkdownReaderTest.php`, and
  `wordpress-latex-top-level-division-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-top-level-division-handoff.php`
  emitted LaTeX chapter/section output plus WordPress heading output; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,624 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 LaTeX Writer Unnumbered-Heading Note Slice

- Focused mapped count is now 1,848 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  `headers` / `unnumbered header` behavior without invoking upstream Pandoc, a
  TeX/PDF engine, or syntax-highlighting tooling.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the direct upstream case
  where `headerWith ("foo",["unnumbered"],[]) 1 (text "Header 1" <> note
  (plain $ text "note"))` renders as a starred section with
  `\texorpdfstring{Header 1\footnote{note}}{Header 1}`, `\label{foo}`, and
  `\addcontentsline{toc}{section}{Header 1}`.
- `LatexWriter` now renders bounded unnumbered heading AST nodes with starred
  section commands, PDF-string fallbacks for note-bearing headings, labels, and
  table-of-contents lines. Note text is omitted from the PDF-string fallback.
  This does not activate broader TeX/PDF, document-template, ZIP/package,
  syntax-highlighting, or document-format support-library gates.
- Added `examples/wordpress-latex-unnumbered-heading-note-handoff.php` for a
  WordPress review packet that emits both LaTeX reviewer heading/footnote
  output and WordPress heading/endnote handoff markup for source audit context.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs` was inspected for the bounded headers
  group; `php -l` passed for `LatexWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-latex-unnumbered-heading-note-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-unnumbered-heading-note-handoff.php`
  emitted LaTeX starred heading/footnote output plus WordPress heading/endnote
  output; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 3,616 assertions, 0 failures. No no-argument root harness
  was assigned or started.

## 2026-05-24 LaTeX Writer Footnote-Code-Block Slice

- Focused mapped count is now 1,842 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  `code blocks` / `in footnotes` behavior without invoking upstream Pandoc, a
  TeX/PDF engine, or syntax-highlighting tooling.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the direct upstream case
  where a Note containing `Para "hi"` and `CodeBlock "hi"` renders as
  `\footnote{hi` plus a blank line and a `Verbatim` code block inside the
  footnote.
- `LatexWriter` now renders bounded `code_block` AST nodes as
  `\begin{Verbatim}` / `\end{Verbatim}` blocks and inline `note` nodes as
  `\footnote{...}`. This does not activate broader TeX/PDF, syntax
  highlighting, listings, document-template, ZIP/package, or document-format
  support-library gates.
- Added `examples/wordpress-latex-footnote-code-handoff.php` for a WordPress
  review packet that emits both LaTeX reviewer footnote/code output and
  WordPress endnote/code-block handoff markup for the same source audit note.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs` was inspected for the bounded code blocks
  and inline note groups; `php -l` passed for `LatexWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-latex-footnote-code-handoff.php`;
  `php lanes/pandoc/examples/wordpress-latex-footnote-code-handoff.php`
  emitted LaTeX footnote/Verbatim output plus WordPress endnote/code-block
  output; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 3,610 assertions, 0 failures. No no-argument root harness
  was assigned or started.

## 2026-05-24 LaTeX Writer Heading-Image Slice

- Focused mapped count is now 1,836 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  `headers` / `containing image` behavior without invoking upstream Pandoc, a
  TeX/PDF engine, or image-processing tooling.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the direct upstream case
  where `header 1 (image "imgs/foo.jpg" "" (text "Alt text"))` renders as
  `\section{\texorpdfstring{\protect\pandocbounded{\includegraphics[keepaspectratio,alt={Alt text}]{imgs/foo.jpg}}}{Alt text}}`.
- `LatexWriter` now detects image-bearing headings, protects nested
  `\pandocbounded{\includegraphics[...]}` output for LaTeX moving arguments,
  and supplies the heading's plain alt text as the PDF-string fallback. This
  does not activate broader TeX/PDF, image-processing, syntax-highlighting,
  ZIP/package, or document-template support-library gates.
- Added `examples/wordpress-latex-heading-image-handoff.php` for a WordPress
  review packet that emits both LaTeX reviewer heading output and WordPress
  heading-block handoff markup for the same source hero image.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '1,340p'` was inspected for the
  bounded headers group; `php -l` passed for `LatexWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-latex-heading-image-handoff.php`;
  `php lanes/pandoc/examples/wordpress-latex-heading-image-handoff.php`
  emitted LaTeX heading output plus WordPress heading block output; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,604 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 LaTeX Writer Figure-Placement Slice

- Focused mapped count is now 1,832 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  figure placement behavior without invoking upstream Pandoc, a TeX/PDF
  engine, or image-processing tooling.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the direct upstream
  `figures`/`placement` case: a figure with `latex-placement="htbp"` renders
  as a LaTeX `figure` environment, emits `\centering`, renders the first
  nested image as
  `\pandocbounded{\includegraphics[keepaspectratio,alt={alt text}]{img.jpg}}`,
  and preserves the caption as `\caption{caption}`.
- `LatexWriter` now renders bounded `figure` AST nodes, placement options,
  nested image alt text, and captions. This does not activate broader
  TeX/PDF, image-processing, syntax-highlighting, ZIP/package, or document
  template support-library gates.
- Added `examples/wordpress-latex-figure-handoff.php` for a WordPress media
  review packet that emits both LaTeX reviewer figure output and WordPress
  image block handoff markup for the same imported frame.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '1,340p'` was inspected for the
  bounded figures group; `php -l` passed for `LatexWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-latex-figure-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-figure-handoff.php` emitted LaTeX
  figure output plus WordPress image block output; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,600 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 LaTeX Writer Definition-List Slice

- Focused mapped count is now 1,827 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  definition-list and heading-in-definition-list cases without invoking
  upstream Pandoc or any TeX/PDF engine.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps two direct upstream cases:
  a definition-list term that is an internal link renders inside a LaTeX
  `description` item with `\tightlist` and `\hyperref`, and a definition body
  beginning with a level-2 header renders after the `\item[foo] ~ ` marker
  shape.
- `LatexWriter` now renders bounded `definition_list` AST nodes as
  `description` environments, emits `\tightlist` for plain-only definitions,
  and renders internal `link` nodes as LaTeX `\hyperref` targets. This does
  not activate a broader TeX/PDF, template, syntax-highlighting, or document
  class support-library gate.
- Added `examples/wordpress-latex-definition-list-handoff.php` for a
  WordPress review-definition packet that emits both LaTeX description output
  and WordPress definition-list HTML for linked source-review terms.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '1,260p'` was inspected for the
  bounded definition-list and header groups; `php -l` passed for
  `LatexWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-latex-definition-list-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-definition-list-handoff.php` emitted
  LaTeX reviewer description output plus WordPress definition-list blocks;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,595 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 LaTeX Writer Heading Slice

- Focused mapped count is now 1,819 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  heading groups without invoking upstream Pandoc or any TeX/PDF engine.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps two direct upstream cases:
  the writer-options default top-level output where level 1/2/3 headings
  render as `\section`, `\subsection`, and `\subsubsection`, and the
  `headers` group list-item case where a level-2 heading renders after
  `\item ~`.
- `LatexWriter` now renders bounded heading AST nodes and preserves the
  special list-item heading marker shape used by upstream Pandoc. This does
  not activate a broader TeX/PDF, template, syntax-highlighting, or document
  class support-library gate.
- Added `examples/wordpress-latex-heading-handoff.php` for a WordPress review
  outline packet that emits both LaTeX section commands and WordPress heading
  blocks for the same migration-review outline.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '55,160p'` was inspected for the
  bounded header and writer-options groups; `php -l` passed for
  `LatexWriter.php`, `MarkdownReaderTest.php`, and
  `wordpress-latex-heading-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-heading-handoff.php` emitted LaTeX
  reviewer section commands plus WordPress heading blocks; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,587 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 LaTeX Writer Inline Code Escape Slice

- Focused mapped count is now 1,813 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  inline code groups without invoking upstream Pandoc or any TeX/PDF engine.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps two direct upstream cases:
  code text `dog's` renders as `\texttt{dog\textquotesingle{}s}`, and code
  text `` `nu?` `` renders as
  `\texttt{\textasciigrave{}nu?\textasciigrave{}}`.
- `LatexWriter` now renders bounded `code` AST nodes as `\texttt{...}` and
  escapes apostrophes/backticks inside that code literal surface. This is a
  narrow writer slice and does not activate a broader TeX, PDF, syntax
  highlighting, or listings support-library gate.
- Added `examples/wordpress-latex-code-handoff.php` for a WordPress review
  packet that emits both LaTeX reviewer export text and WordPress inline code
  spans for source command/code handoff.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '70,105p'` was inspected for the
  bounded inline code group; `php -l` passed for `LatexWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-latex-code-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-code-handoff.php` emitted escaped
  LaTeX reviewer code plus WordPress inline code spans; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,581 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 LaTeX Writer Math Pipe Slice

- Focused mapped count is now 1,807 Markdown/HTML/Native/WordPress checks.
- Added bounded `LatexWriter` coverage from upstream `Tests.Writers.LaTeX`
  math group without invoking upstream Pandoc or any TeX/PDF engine.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the direct upstream
  `math`/`escape |` case: an inline math node containing
  `\sigma|_{\{x\}}` renders as `\(\sigma|_{\{x\}}\)`, preserving the
  pipe operator inside math rather than treating it as escapable prose.
- `LatexWriter` now renders inline and display `math` AST nodes as
  `\(...\)` and `\[...\]` in paragraph/list contexts. This is a bounded
  writer surface and does not activate a broader TeX, PDF, or math-rendering
  support-library gate.
- Added `examples/wordpress-latex-math-handoff.php` for a WordPress review
  packet that emits both LaTeX reviewer export text and WordPress block math
  handoff markup for inline and display equations.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/LaTeX.hs | sed -n '1,120p'` was inspected for the
  bounded math group; `php -l` passed for `LatexWriter.php`,
  `MarkdownReaderTest.php`, and `wordpress-latex-math-handoff.php`; `php
  lanes/pandoc/examples/wordpress-latex-math-handoff.php` emitted inline and
  display LaTeX math plus WordPress block math spans; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,575 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Writer Section-Div Footnotes Slice

- Focused mapped count is now 1,803 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream `Tests.Writers.HTML`
  footnote groups for the remaining `EndOfSection` plus `writerSectionDivs`
  case.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the fourth direct upstream
  writer footnote case without invoking upstream Pandoc: `Page title` is
  wrapped in a level1 section div, `First section` and `Second section` are
  wrapped in level2 section divs, and the first section's footnotes are emitted
  before the note-bearing level2 section closes.
- `HtmlWriter` now accepts `writerSectionDivs`/`sectionDivs` for a bounded
  heading-derived section tree, preserves the previously mapped
  `EndOfDocument`, `EndOfBlock`, and non-section `EndOfSection` footnote
  placements, and keeps end-of-section note blocks inside their section.
- Added `examples/wordpress-html-writer-section-div-footnotes-handoff.php` for
  a WordPress review HTML packet where source-note footnotes stay attached to
  the source-notes section before the publish checklist section starts.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/HTML.hs | sed -n '1,260p'` was inspected for the
  bounded footnote group; `git -C .upstream-cache/pandoc show
  HEAD:src/Text/Pandoc/Shared.hs | sed -n '520,610p'` was inspected for
  `makeSections`; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-html-writer-section-div-footnotes-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-section-div-footnotes-handoff.php`
  emitted section-wrapped reviewer footnotes with backlinks; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,570 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 HTML Writer Footnote-Placement Slice

- Focused mapped count is now 1,798 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream `Tests.Writers.HTML`
  footnote groups.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps three of the four direct
  upstream writer footnote cases without invoking upstream Pandoc:
  `EndOfDocument`, `EndOfBlock`, and `EndOfSection`. The upstream section-divs
  case was left for a separate slice and is now mapped in the section above.
- `HtmlWriter` now renders inline note references as Pandoc-style
  `footnote-ref` anchors, gathers footnote bodies, renders backlinks, preserves
  note-body links, and emits blockquotes around note-bearing quoted sections.
- Added `examples/wordpress-html-writer-footnote-placement-handoff.php` for a
  WordPress review HTML packet where paragraph-scoped and quote-scoped reviewer
  notes are emitted at the end of each block with source edit links preserved.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/HTML.hs | sed -n '1,260p'` was inspected for the
  bounded footnote group; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-html-writer-footnote-placement-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-footnote-placement-handoff.php`
  emitted end-of-block reviewer footnotes with backlinks; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,565 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Writer Highlighted-Code Slice

- Focused mapped count is now 1,786 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream `Tests.Writers.HTML`
  inline-code style groups.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps four direct upstream writer
  cases without invoking upstream Pandoc: `haskell` code renders as
  `sourceCode haskell` with the `>>=` operator inside `span.op`,
  `nolanguage` code remains an unhighlighted classed `code`, sample+haskell
  code wraps the highlighted code in `samp`, and haskell+variable code wraps
  the highlighted code in `var`.
- `HtmlWriter` now implements a deliberately bounded native Haskell
  operator-token highlighting branch for the accepted upstream examples. It
  does not activate the broader Skylighting support-library gate.
- Added `examples/wordpress-html-writer-highlighted-code-handoff.php` for a
  WordPress review HTML packet where source transform diagnostics retain
  highlighted Haskell-style operator markup while preserving semantic
  `sample`/`variable` roles.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/HTML.hs | sed -n '1,140p'` was inspected for the
  bounded inline-code and style-code groups; `php -l` passed for
  `HtmlWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-html-writer-highlighted-code-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-highlighted-code-handoff.php`
  emitted reviewer `sourceCode haskell` HTML with `samp`/`var` wrappers; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,557 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Writer Definition-List Slice

- Focused mapped count is now 1,778 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream `Tests.Writers.HTML`
  block groups.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the direct upstream writer
  case for a definition list with an empty `<dt>`: the blank term is preserved
  as `<dt></dt>`, and the paragraph definition remains inside `<dd>`.
- `HtmlWriter` now renders `definition_list` AST nodes as Pandoc-style
  multiline `<dl>`, `<dt>`, and `<dd>` HTML without inventing placeholder text
  for blank source terms.
- Added `examples/wordpress-html-writer-definition-list-handoff.php` for a
  WordPress review HTML packet where a source glossary/status list preserves a
  blank term for editorial audit without shelling out to Pandoc.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/HTML.hs | sed -n '50,75p'` was inspected for the
  bounded blocks group; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-html-writer-definition-list-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-definition-list-handoff.php`
  emitted reviewer `<dl>` HTML with an empty `<dt>`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,550 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Writer Quote/Q-Tags Slice

- Focused mapped count is now 1,774 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream `Tests.Writers.HTML`
  quote groups.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps two direct upstream writer
  cases without invoking upstream Pandoc: quote-with-cite default output
  renders smart quote punctuation around a cite-bearing `span`, while
  `htmlQTags` output lifts `cite` onto a `q` element.
- `HtmlWriter` now accepts an `htmlQTags` option for this accepted branch.
  Default output keeps the inner span, and `htmlQTags` output emits
  `<q cite="...">...</q>` when the quote contains only a cite-bearing span.
- Added `examples/wordpress-html-writer-quote-cite-handoff.php` for a
  WordPress review HTML packet where a source reviewer quote keeps its cite
  metadata on `q` without shelling out to Pandoc.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/HTML.hs | sed -n '75,90p'` was inspected for the
  bounded quote group; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-html-writer-quote-cite-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-quote-cite-handoff.php` emitted
  a `q cite` reviewer HTML fragment; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,545 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Writer Image/Heading Attribute Slice

- Focused mapped count is now 1,768 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream `Tests.Writers.HTML`
  image and block attribute groups.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps two direct upstream writer
  cases without invoking upstream Pandoc: image alt text stringification for
  formatted inline labels (`my ` plus emphasized `image`) and heading
  attribute filtering where `invalid="1"` is dropped while `lang="en"` is
  preserved.
- `HtmlWriter` now renders accepted image inlines with escaped `src`, `title`,
  plain-stringified `alt`, and safe data/aria handoff attributes. Heading
  output filters unsupported attributes for this HTML writer branch while
  keeping allowed document-language metadata.
- Added `examples/wordpress-html-writer-image-attrs-handoff.php` for a
  WordPress media-review packet where a source image title/data marker and
  formatted alt label survive, while noisy legacy heading metadata is not
  emitted.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/HTML.hs | sed -n '55,78p'` was inspected for the
  bounded image and heading-disallowed-attributes groups; `php -l` passed for
  `HtmlWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-html-writer-image-attrs-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-image-attrs-handoff.php` emitted
  a lang-preserving heading plus media-review image with title/alt/data-source
  attributes; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 3,541 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 HTML Writer Code-Role Slice

- Focused mapped count is now 1,762 Markdown/HTML/Native/WordPress checks.
- Added bounded `HtmlWriter` coverage from upstream `Tests.Writers.HTML`
  simple code-role groups.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps four direct upstream writer
  cases without invoking upstream Pandoc: escaped ordinary Code `@&`, ordinary
  Code text `Answer is 42`, sample-class Code rendering as `samp`, and
  variable-class Code rendering as `var`.
- `HtmlWriter` is intentionally narrow for this slice: it supports small HTML
  fragments for plain/paragraph/heading blocks and core inline nodes, while
  the accepted upstream behavior is limited to the code/sample/variable role
  boundary. Syntax highlighting and the broader HTML writer suite remain
  future slices.
- Added `examples/wordpress-html-writer-code-roles-handoff.php` for a
  WordPress reviewer-diagnostics packet where a block name remains `code`, a
  sample diagnostic renders as `samp`, and a post-field variable renders as
  `var`.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Writers/HTML.hs | sed -n '50,95p'` was inspected for the
  bounded code/sample/variable groups; `php -l` passed for `HtmlWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-html-writer-code-roles-handoff.php`; `php
  lanes/pandoc/examples/wordpress-html-writer-code-roles-handoff.php` emitted
  reviewer diagnostics with `code`, `samp`, and `var`; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,535 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 HTML Pre/Code Attribute Reader Slice

- Focused mapped count is now 1,754 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/WordPress coverage from upstream
  `Tests.Readers.HTML` `code block` group.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps two direct upstream HTML
  reader cases without invoking upstream Pandoc:
  `<pre><code id="a" class="python">\nprint('hi')\n</code></pre>` and
  `<pre id="c"><code id="d">print('hi mom!')\n</code></pre>`.
- MarkdownReader now preserves nested `code` id/classes/data attributes on
  `code_block` nodes, strips `language-` prefixes for code-block language
  classes, and applies Pandoc's `pre`-attribute precedence when the wrapper
  has its own attr tuple.
- Added `fixtures/upstream-html-pre-code-attributes.html` and
  `examples/wordpress-native-html-codeblock-attrs-handoff.php` for a WordPress
  import fixture whose source snippet id, language, and safe reviewer metadata
  survive on the WordPress code block, while a `pre` wrapper id beats a nested
  `code` id when upstream precedence applies.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Readers/HTML.hs | sed -n '124,150p'` was inspected for the
  bounded `code block` group; `php -l` passed for `MarkdownReader.php`,
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-html-codeblock-attrs-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-codeblock-attrs-handoff.php`
  emitted WordPress code blocks with `legacy-snippet`/`data-source` metadata
  and `pre-wrapper-wins` precedence metadata; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,528 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Lang Metadata Reader Slice

- Focused mapped count is now 1,748 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/WordPress coverage from upstream
  `Tests.Readers.HTML` `lang` group.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps two direct upstream HTML
  reader cases without invoking upstream Pandoc: `<html lang="es">hola` and
  `<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="es"><head></head><body>hola</body></html>`.
- MarkdownReader now recognizes EOF-terminated `<html>` documents for this
  upstream shape, carries root `lang` or `xml:lang` into document metadata,
  and keeps the body text as parsed paragraph content instead of literal HTML
  source text.
- Added `fixtures/upstream-html-lang-metadata.html` and
  `examples/wordpress-native-html-lang-metadata-handoff.php` for a WordPress
  import fixture whose source language is visible in an opt-in metadata review
  block alongside the parsed body copy.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Readers/HTML.hs | sed -n '1,220p'` was inspected for the
  bounded `lang` group; `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-html-lang-metadata-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-lang-metadata-handoff.php` emitted
  a metadata review block with `lang=es`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,506 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Inline Code Alias Reader Slice

- Focused mapped count is now 1,742 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/WordPress coverage from upstream
  `Tests.Readers.HTML` `code`, `tt`, `samp`, and `var` groups.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps four direct upstream HTML
  reader cases without invoking upstream Pandoc: `<code>Answer is 42</code>`,
  `<tt>Answer is 42</tt>`, `<samp>Answer is 42</samp>`, and
  `<var>result</var>`.
- MarkdownReader now recognizes top-level inline-code HTML fragments and
  emits paragraph `code` inlines rather than literal raw tag text. `tt`
  follows ordinary code semantics, while `samp` carries Pandoc's `sample`
  class and `var` carries Pandoc's `variable` class.
- Added `fixtures/upstream-html-inline-code-aliases.html` and
  `examples/wordpress-native-html-inline-code-handoff.php` for a WordPress
  import fixture whose inline block name, shortcode-like legacy `tt` text,
  sample diagnostic, and variable name remain visible as inline code.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Readers/HTML.hs | sed -n '112,128p'` was inspected for the
  bounded code/tt/samp/var groups; `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-html-inline-code-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-inline-code-handoff.php` emitted
  inline code with `class="sample"` and `class="variable"` handoff markers;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,495 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 HTML Native-Div Header Reader Slice

- Focused mapped count is now 1,734 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/WordPress coverage from upstream
  `Tests.Readers.HTML` native-div `header` group.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps the upstream
  `<header id="title">Title</header>` case without invoking upstream Pandoc,
  plus WordPress handoff coverage for article headers nested inside extracted
  `<main>` content.
- MarkdownReader now has an opt-in `htmlNativeDivs` path that converts
  top-level `<header>` fragments and recursive HTML block headers into native
  `div` nodes with the Pandoc `header` class. Existing id/class/data metadata
  is preserved, and nested headers inside plain `<main>` content remain in the
  extracted document body.
- Added `fixtures/upstream-html-header-native-divs.html` and
  `examples/wordpress-native-html-header-handoff.php` for a WordPress import
  fixture whose main body keeps an attributed article header div while still
  using the previous native-div main extraction path.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Readers/HTML.hs | sed -n '82,128p'` was inspected for the
  bounded main/header group; `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-html-header-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-header-handoff.php` emitted the
  expected WordPress handoff with nested `class="header"` article metadata;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,472 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 HTML Native-Div Main Reader Slice

- Focused mapped count is now 1,729 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/WordPress coverage from upstream
  `Tests.Readers.HTML` native-div `main` group.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps five upstream HTML reader
  cases without invoking upstream Pandoc: `<main>` content extraction,
  role-preserving main-to-div conversion, attributed main-to-div conversion
  with generated `role=main`, implicit `<p>` close before `<main>`, and
  trailing non-main text exclusion.
- MarkdownReader now has an opt-in `htmlNativeDivs` path that extracts the
  first `<main>` element from full HTML documents or HTML fragments. Plain
  `<main>` content is unwrapped into document blocks, while attributed main
  elements become native `div` nodes with preserved id/classes/data
  attributes. Surrounding header/nav/footer/text boilerplate is ignored for
  this focused upstream branch.
- Added `fixtures/upstream-html-main-native-divs.html` and
  `examples/wordpress-native-html-main-handoff.php` for a WordPress import
  fixture whose attributed main body survives with `role="main"` while export
  boilerplate is dropped.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Readers/HTML.hs | sed -n '82,112p'` was inspected for the
  bounded main group; `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-html-main-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-main-handoff.php` emitted only
  the attributed main wrapper and body content; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,451 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 HTML Anchor/Image Attribute Reader Slice

- Focused mapped count is now 1,720 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/WordPress coverage from upstream
  `Tests.Readers.HTML` anchors and img groups.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps three upstream HTML reader
  cases without invoking upstream Pandoc: `<a name="anchor"/>` without href,
  `<img data-external="1" ...>`, and `<img title="...">`.
- MarkdownReader now converts href-less HTML anchors with `name` or `id`
  attributes into Pandoc-style `span` anchors instead of empty links. Explicit
  `<a href="">` placeholders remain links, preserving existing empty-link
  import behavior.
- MarkdownReader now preserves safe image attributes such as `data-external`
  on HTML image nodes while keeping `src`, `alt`, and `title` in their
  dedicated image fields. WordPress output carries `data-external` through to
  the rendered `<img>` tag for reviewer handoff.
- Added `fixtures/upstream-html-anchor-image-attrs.html` and
  `examples/wordpress-native-html-anchor-image-attrs-handoff.php` for a
  WordPress import fixture with legacy section anchors, reviewer jump targets,
  and external image metadata.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Readers/HTML.hs | sed -n '40,82p'` was inspected for the
  bounded anchor/img groups; `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-html-anchor-image-attrs-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-anchor-image-attrs-handoff.php`
  emitted span anchors and an image retaining `data-external`; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,427 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 HTML Base Href Reader Slice

- Focused mapped count is now 1,714 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/WordPress coverage from upstream
  `Tests.Readers.HTML` base-tag cases.
- The cloned static denominator remains 2,276 inspected upstream
  test/data/benchmark artifacts. This slice maps four upstream base URL
  shapes without invoking upstream Pandoc: file-like base paths, directory
  base paths, root-relative image paths, and already-absolute image URLs.
- MarkdownReader now records the first `<base href>` while parsing a full HTML
  document and resolves HTML `<img src>` plus `<a href>` values against it.
  Existing no-base HTML reader imports keep their original source URLs.
- Added `fixtures/upstream-html-base-media.html` as an upstream-shaped
  WordPress import fixture. It proves relative exported media, relative audit
  links, and root-relative media become absolute WordPress URLs before
  WordPressBlockWriter output.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/Tests/Readers/HTML.hs` was inspected for the bounded base-tag
  group; `php -l` passed for `MarkdownReader.php`, `MarkdownReaderTest.php`,
  and `examples/wordpress-native-html-base-media-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-base-media-handoff.php` emitted
  absolute image/link URLs resolved from the HTML base; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,411 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 Markdown Abbreviation Reader Slice

- Focused mapped count is now 1,707 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/NativeWriter/WordPress coverage from upstream
  command fixture `test/command/md-abbrevs.md` plus the upstream
  `data/abbreviations` list.
- The command subinventory remains 1,155 artifacts under `test/command`, and
  the data subinventory remains 247 artifacts under `data/`. This slice maps
  the md-abbrevs command output without invoking upstream Pandoc.
- The copied fixture records Pandoc's native output for `Mr. Bob` as
  `Str "Mr.\160Bob"` and the escaped-period case `Hi Mr\. Bob` as ordinary
  `Space` constructors.
- MarkdownReader now recognizes known abbreviations from upstream
  `data/abbreviations` when the period is unescaped and followed by a letter,
  binding the following space as `U+00A0`. Escaped periods remain ordinary text
  plus `Space`, and digit-following initials such as `M.A. 2007` stay ordinary
  spacing to preserve existing upstream reader fixtures.
- WordPress output preserves the nonbreaking abbreviation groups in paragraph
  text so migration review packets do not split titles such as `Mr. Bob`,
  `Dr. Rivera`, or `e.g. examples` during import review.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/command/md-abbrevs.md)
  lanes/pandoc/fixtures/upstream-command-md-abbrevs.md` produced no output;
  `git -C .upstream-cache/pandoc show HEAD:data/abbreviations` was inspected
  for the bounded abbreviation list; `php -l` passed for `MarkdownReader.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-markdown-abbrev-handoff.php`; `php
  lanes/pandoc/examples/wordpress-markdown-abbrev-handoff.php` emitted the
  expected WordPress abbreviation handoff; the first focused test run exposed
  an over-broad digit-following `M.A. 2007` conversion, which was corrected;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` then
  passed 1 file, 3,401 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 Markdown Raw Attribute Reader Slice

- Focused mapped count is now 1,698 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/MarkdownWriter/NativeWriter/WordPress coverage
  from upstream command fixture `test/command/parse-raw.md`.
- The command subinventory remains 1,155 artifacts under `test/command`, and
  this slice maps the raw-enabled Markdown output examples without invoking
  upstream Pandoc.
- The copied fixture records Pandoc's raw TeX/HTML command outputs:
  ``*Hi `\foo{there}`{=latex}*`` for `latex+raw_tex`, and
  ``*Hi `<blink>`{=html}there`</blink>`{=html}*`` for `html+raw_html`.
- MarkdownReader now consumes code-span raw attributes before ordinary code
  attributes, preserving `{=latex}` as `RawInline (Format "latex")` and
  `{=html}` as raw HTML inline nodes instead of leaving `{=format}` as literal
  paragraph text. It also parses fenced raw-attribute blocks into raw block
  nodes for WordPress handoff tests.
- WordPress output preserves raw HTML literally, while latex/opml-style raw
  payloads remain visible as `data-pandoc-raw-format` reviewer spans or code
  blocks. Ordinary code attributes such as `{.javascript}` remain code nodes.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/command/parse-raw.md)
  lanes/pandoc/fixtures/upstream-command-parse-raw.md` produced no output;
  `php -l` passed for `MarkdownReader.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-markdown-raw-attribute-handoff.php`; `php
  lanes/pandoc/examples/wordpress-markdown-raw-attribute-handoff.php` emitted
  the expected WordPress raw-attribute reviewer handoff; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,390 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Table GridBefore Slice

- Focused mapped count is now 1,676 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from a compact
  upstream-derived slice of `test/docx/table_gridbefore.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This maps the Native table packet without activating
  DOCX ZIP/OpenXML package parsing.
- The copied slice preserves the DOCX gridBefore/gridAfter shape as explicit
  empty cells, with eleven scientific `ColWidth` values, a header cell spanning
  eight bit columns, spacer rows, a body cell with `ColSpan 2`, and a reserved
  text cell with `ColSpan 10`.
- NativeReader preserves the scientific widths, empty cells, and colspans;
  NativeWriter read-back keeps `ColWidth` and `ColSpan` markers; default
  WordPressBlockWriter preserves blank table cells without extra attributes,
  while opt-in `markEmptyTableCells` emits `data-pandoc-empty-cell="true"` on
  nineteen blank source cells for reviewer handoffs.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/docx/table_gridbefore.native` was inspected for the bounded
  fixture slice; `php -l` passed for `WordPressBlockWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-table-gridbefore-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-table-gridbefore-handoff.php`
  emitted the expected WordPress table with empty-cell reviewer markers; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,360 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 Empty Paragraph Command Slice

- Focused mapped count is now 1,651 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  command fixture `test/command/empty_paragraphs.md`.
- The command subinventory remains 1,155 artifacts under `test/command`, and
  this slice maps the native-to-HTML empty-paragraph policy without invoking
  upstream Pandoc.
- The copied command fixture records the default `html5` behavior dropping two
  empty `Para []` blocks and the `html5+empty_paragraphs` behavior preserving
  them as two empty `<p></p>` elements.
- NativeReader preserves the two empty paragraph packets; NativeWriter
  read-back keeps `Para [  ]`; WordPressBlockWriter keeps the existing default
  drop behavior and adds opt-in `preserveEmptyParagraphs` output for import
  review packets that need source blank paragraph evidence.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/command/empty_paragraphs.md)
  lanes/pandoc/fixtures/upstream-command-empty-paragraphs.md` produced no
  output; `php -l` passed for `WordPressBlockWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-empty-paragraphs-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-empty-paragraphs-handoff.php` emitted
  four WordPress paragraph blocks including two empty `<p></p>` blocks; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,335 assertions, 0 failures. The focused file now contains 330
  behavior tests. `jq empty` passed for the lane manifest and lane status, and
  `git diff --check -- lanes/pandoc` passed. No no-argument root harness was
  assigned or started.

## 2026-05-24 Native ODT Multi-Header Table Slice

- Focused mapped count is now 1,643 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream ODT
  Native fixture `test/odt/native/simpleTableWithMultipleHeaderRows.native`.
- The targeted ODT subinventory remains 99 artifacts under `test/odt`,
  including 29 `.native` expectations, 18 Markdown fixtures, and 52 `.odt`
  inputs. This maps another ODT Native table shape without activating
  OpenDocument ZIP/XML package parsing.
- The copied fixture contributes one table plus a trailing empty `Para []`.
  The table has three `ColWidthDefault` columns, two header rows (`A/B/C` and
  `I/II/II`), three body rows, and six empty body cells.
- NativeReader preserves the two `TableHead` rows and default-width cells;
  NativeWriter read-back keeps `ColWidthDefault`; WordPressBlockWriter emits a
  two-row `<thead>`, a three-row `<tbody>`, no invented `<colgroup>`, and no
  empty paragraph block for the trailing `Para []`.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/odt/native/simpleTableWithMultipleHeaderRows.native)
  lanes/pandoc/fixtures/upstream-native-odt-simple-table-multiple-header-rows.native`
  produced no output; `php -l` passed for `MarkdownReaderTest.php` and
  `examples/wordpress-native-odt-multi-header-table-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-odt-multi-header-table-handoff.php`
  emitted the expected two-header-row WordPress table; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,321 assertions,
  0 failures. The focused file now contains 329 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Insertion/Deletion Decision Slice

- Focused mapped count is now 1,628 Markdown/HTML/Native/WordPress checks.
- Normalized `benchmarkDenominator.total` to numeric `2276` while retaining
  the prose cloned-inventory summary in `totalDescription`.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX Native fixtures `test/docx/track_changes_insertion_accept.native`,
  `test/docx/track_changes_insertion_reject.native`,
  `test/docx/track_changes_deletion_accept.native`, and
  `test/docx/track_changes_deletion_reject.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This maps applied accept/reject decisions without
  activating DOCX ZIP/OpenXML package parsing.
- The four copied fixtures contribute one plain paragraph each: accepted
  insertion keeps `two exciting`, rejected insertion omits those words,
  accepted deletion omits `an excessively modified`, and rejected deletion
  retains that deleted text.
- NativeReader preserves the four applied paragraph texts; NativeWriter
  read-back emits plain `Para` packets with no residual `Span` constructors;
  WordPressBlockWriter emits plain paragraph blocks with no residual
  `<ins>`/`<del>` review markup.
- Focused evidence: `php -l` passed for `MarkdownReaderTest.php` and
  `examples/wordpress-native-docx-track-changes-decision-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-track-changes-decision-handoff.php`
  emitted the four expected plain WordPress paragraphs; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,306 assertions, 0 failures. The focused file now contains 328
  behavior tests. No no-argument root harness was assigned or started.

## 2026-05-24 Markdown Nested Spanlike Slice

- Focused mapped count is now 1,604 Markdown/HTML/Native/WordPress checks.
- Added bounded MarkdownReader/WordPress coverage from upstream command
  fixture `test/command/nested-spanlike.md`.
- The command subinventory remains 1,155 artifacts under `test/command`, and
  this slice maps the single nested-spanlike command block without invoking
  upstream Pandoc.
- The upstream expected HTML shape is
  `<kbd id="bar"><u><span class="smallcaps">test</span></u></kbd>` for source
  Markdown `[test]{.foo .underline #bar .smallcaps .kbd}`.
- MarkdownReader preserves the source bracketed span id/classes; WordPress
  output consumes the spanlike marker classes (`kbd`, `underline`, and
  `smallcaps`), attaches the id to the outer wrapper, and does not leak the
  pre-spanlike `.foo` class into the rendered wrapper.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/command/nested-spanlike.md` matched the expected HTML wrapper;
  `php -l` passed for `WordPressBlockWriter.php`, `MarkdownReaderTest.php`,
  and `examples/wordpress-markdown-spanlike-handoff.php`; `php
  lanes/pandoc/examples/wordpress-markdown-spanlike-handoff.php` emitted the
  expected WordPress paragraph wrapper; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,274 assertions,
  0 failures. The focused file now contains 327 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Paragraph Split Decision Slice

- Focused mapped count is now 1,598 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX Native fixtures `test/docx/paragraph_insertion_deletion_accept.native`
  and `test/docx/paragraph_insertion_deletion_reject.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This maps accepted/rejected paragraph split review
  decisions without activating DOCX ZIP/OpenXML package parsing.
- The copied accepted fixture contributes two plain paragraphs in accepted
  split order: `This is a` then `split Paragraph.`. The copied rejected fixture
  contributes two plain paragraphs in rejected order: `This is a split` then
  `Paragraph.`.
- NativeReader preserves both paragraph orders; NativeWriter read-back emits
  plain `Para` packets with no residual paragraph-change `Span` nodes;
  WordPressBlockWriter emits plain paragraph blocks with no residual
  `data-pandoc-paragraph-change` metadata.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/docx/paragraph_insertion_deletion_accept.native)
  lanes/pandoc/fixtures/upstream-native-docx-paragraph-insertion-deletion-accept.native`
  and the equivalent reject-fixture diff both produced no output; `php -l`
  passed for `MarkdownReaderTest.php` and
  `examples/wordpress-native-docx-paragraph-change-decision-handoff.php`;
  `php lanes/pandoc/examples/wordpress-native-docx-paragraph-change-decision-handoff.php`
  emitted accepted and rejected WordPress sections in the expected paragraph
  order; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 3,268 assertions, 0 failures. The focused file now contains
  326 behavior tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Moved-Text Decision Slice

- Focused mapped count is now 1,584 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX Native fixtures `test/docx/track_changes_move_accept.native` and
  `test/docx/track_changes_move_reject.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This maps accepted/rejected review decisions without
  activating DOCX ZIP/OpenXML package parsing.
- The copied accepted fixture contributes three plain paragraphs in context,
  moved-text, later-context order. The copied rejected fixture contributes the
  same three plain paragraphs in context, later-context, moved-text order.
- NativeReader preserves both paragraph orders; NativeWriter read-back emits
  plain `Para` packets with no residual review `Span` nodes; WordPressBlockWriter
  emits plain paragraph blocks with no residual `<ins>` or `<del>` review markup.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/docx/track_changes_move_accept.native)
  lanes/pandoc/fixtures/upstream-native-docx-track-changes-move-accept.native`
  and the equivalent reject-fixture diff both produced no output; `php -l`
  passed for `MarkdownReaderTest.php` and
  `examples/wordpress-native-docx-track-changes-move-decision-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-track-changes-move-decision-handoff.php`
  emitted accepted and rejected WordPress sections in the expected paragraph
  order; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 3,254 assertions, 0 failures. The focused file now contains
  325 behavior tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Overlapping Targets Slice

- Focused mapped count is now 1,570 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX Native fixture `test/docx/overlapping_targets.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This maps a cross-reference anchor packet without
  activating DOCX ZIP/OpenXML package parsing.
- The copied fixture contributes three paragraphs: two links targeting `#Fizz`
  and one empty `Span ("Fizz",["anchor"],[]) []` target before the linked
  paragraph text.
- NativeReader preserves the same-fragment links and empty anchor span;
  NativeWriter round-trips the `Span` anchor packet; WordPressBlockWriter now
  marks empty anchor spans with `data-pandoc-anchor="empty-target"` so DOCX
  cross-reference targets remain visible to migration reviewers. The same
  marker also applies to existing upstream DOCX table-caption anchors.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/docx/overlapping_targets.native)
  lanes/pandoc/fixtures/upstream-native-docx-overlapping-targets.native`
  confirmed the copied fixture matches upstream; `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-overlapping-targets-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-overlapping-targets-handoff.php`
  emitted two `#Fizz` links and an empty anchor target with
  `data-pandoc-anchor="empty-target"`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,240 assertions,
  0 failures. The focused file now contains 324 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Scrubbed Metadata Slice

- Focused mapped count is now 1,555 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX Native fixture `test/docx/track_changes_scrubbed_metadata.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This maps scrubbed review metadata without
  activating DOCX ZIP/OpenXML package parsing.
- The copied fixture contributes one paragraph with four review spans:
  deletion, insertion, comment-start, and comment-end. The deletion,
  insertion, and comment-start spans preserve author metadata while omitting
  date metadata.
- WordPressBlockWriter now marks author-only review metadata as missing-date
  handoff data (`data-pandoc-change-date-status="missing"` or
  `data-pandoc-comment-date-status="missing"`) instead of inventing a
  `datetime` value or leaking raw upstream `author`/`date` attributes.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/docx/track_changes_scrubbed_metadata.native)
  lanes/pandoc/fixtures/upstream-native-docx-track-changes-scrubbed-metadata.native`
  confirmed the copied fixture matches upstream; `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-scrubbed-metadata-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-scrubbed-metadata-handoff.php`
  emitted WordPress review spans with explicit missing-date status; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 3,225 assertions, 0 failures. The focused file now contains 323
  behavior tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Move-Tracking Slice

- Focused mapped count is now 1,541 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX Native fixture `test/docx/track_changes_move_all.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This maps the moved-text all-changes review shape
  without activating DOCX ZIP/OpenXML package parsing.
- The copied fixture contributes four paragraph packets: normal context before
  and after the moved text, plus paired `Span` nodes carrying `insertion` and
  `deletion` classes with the same `author` and `date` metadata.
- NativeReader preserves the paired moved-text spans; NativeWriter round-trips
  the shared metadata; WordPressBlockWriter renders the handoff as `<ins>` and
  `<del>` review spans with `data-pandoc-change-*` metadata while avoiding raw
  upstream `author` or `date` attributes.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/docx/track_changes_move_all.native)
  lanes/pandoc/fixtures/upstream-native-docx-track-changes-move-all.native`
  confirmed the copied fixture matches upstream; `php -l` passed for
  `MarkdownReaderTest.php` and
  `examples/wordpress-native-docx-track-changes-move-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-track-changes-move-handoff.php`
  emitted WordPress paragraphs with `<ins>`/`<del>` review spans; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  3,207 assertions, 0 failures. The focused file now contains 322 behavior
  tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Image Textbox Caption Slice

- Focused mapped count is now 1,525 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX Native fixture `test/docx/image_with_textbox_caption.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This strengthens DOCX Native fixture mapping without
  activating DOCX ZIP/OpenXML package parsing.
- The copied fixture contributes a `Figure` whose caption comes from a DOCX
  image textbox, plus an empty-alt EMF `Image` with source width/height
  attributes and target `media/image1.emf`.
- WordPressBlockWriter now uses a Figure caption as a reviewer-safe image alt
  fallback only when the source Image has empty alt text and no non-image
  figure-body alt fallback. The rendered image is marked with
  `data-pandoc-alt-source="figure-caption"` while keeping the figcaption,
  source dimensions, `data-pandoc-source-format="emf"`, and NativeWriter
  read-back shape intact.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/docx/image_with_textbox_caption.native)
  lanes/pandoc/fixtures/upstream-native-docx-image-textbox-caption.native`
  confirmed the copied fixture matches upstream; `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-image-textbox-caption-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-image-textbox-caption-handoff.php`
  emitted a WordPress image block with `data-pandoc-alt-source="figure-caption"`
  and `data-pandoc-source-format="emf"`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,191 assertions,
  0 failures. The focused file now contains 321 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Diagram Placeholder Slice

- Focused mapped count is now 1,512 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX Native fixture `test/docx/diagram.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This strengthens DOCX Native fixture mapping without
  activating DOCX ZIP/OpenXML package parsing.
- The copied fixture contributes a level-1 heading with id `diagram-after` and
  a paragraph containing `Span ("",["diagram"],[]) [Str "[DIAGRAM]"]`, matching
  Pandoc's unsupported DOCX diagram placeholder handoff.
- WordPressBlockWriter now renders upstream `diagram` spans as explicit review
  placeholders with `data-pandoc-diagram="unsupported-docx-diagram"` while
  preserving the visible `[DIAGRAM]` marker and NativeWriter round-trip shape.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/docx/diagram.native)
  lanes/pandoc/fixtures/upstream-native-docx-diagram.native` confirmed the
  copied fixture matches upstream; `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-diagram-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-diagram-handoff.php` emitted a
  WordPress paragraph containing
  `data-pandoc-diagram="unsupported-docx-diagram"`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,178 assertions,
  0 failures. The focused file now contains 320 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native JATS Figure Alt-Text Slice

- Focused mapped count is now 1,503 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  Native fixture region `test/jats-reader.native` lines 853-858.
- The targeted static denominator remains 2,276 inspected upstream
  test/data/benchmark files and artifacts. This slice reuses the existing
  cloned static inventory and does not activate a JATS/XML parser dependency.
- The copied fixture contributes a `Figure` with id `fig-1`, caption `bar`, a
  source body `Plain [ Str "alternative-decription" ]` packet, and an Image
  nested under a paragraph with target `foo.png` and empty alt text.
- WordPressBlockWriter now finds Image nodes nested inside Figure child blocks
  and uses non-image figure body text as an alt fallback when the Image packet
  itself has no alt text. This prevents reviewer handoffs from emitting an
  empty placeholder `src=""` image while keeping the source caption visible as
  a figcaption.
- Focused evidence: `git -C .upstream-cache/pandoc show
  HEAD:test/jats-reader.native | sed -n '853,858p'` matched the copied fixture
  region; `php -l` passed for `WordPressBlockWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-jats-figure-alt-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-jats-figure-alt-handoff.php` emitted a
  WordPress image block with `src="foo.png"` and
  `alt="alternative-decription"`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,169 assertions,
  0 failures. The focused file now contains 319 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX VML/Object Image Slice

- Focused mapped count is now 1,492 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream DOCX
  native fixture `test/docx/image_vml_as_object.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This strengthens DOCX Native fixture mapping without
  activating DOCX ZIP/OpenXML package parsing.
- The copied fixture contributes a normal paragraph followed by a paragraph
  Image node whose target is `media/image1.emf`, matching Pandoc's VML object
  image handoff.
- NativeReader preserves the EMF Image packet; NativeWriter round-trips the
  `Image ... ( "media/image1.emf" , "" )` shape; WordPress output keeps the
  existing image-block handoff but adds `data-pandoc-source-format="emf"` so
  migration reviewers can identify source media that WordPress/browser paths
  may need conversion. Browser-native image targets are not flagged.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/docx/image_vml_as_object.native)
  lanes/pandoc/fixtures/upstream-native-docx-vml-object-image.native`
  confirmed the copied fixture matches upstream; `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-vml-object-image-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-vml-object-image-handoff.php`
  emitted a WordPress image block with `data-pandoc-source-format="emf"`; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  3,154 assertions, 0 failures. The focused file now contains 318 behavior
  tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native HTML Row Header Table Slice

- Focused mapped count is now 1,449 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  `test/html-reader.native` for the HTML reader row-header table region.
- The upstream static denominator remains 2,276 inspected upstream
  test/data/benchmark files and artifacts. This slice reuses the existing
  `test/html-reader.native` inventory rather than activating a new dependency.
- The copied lane fixture contributes the upstream-shaped `Row headers`
  paragraph plus a three-column table whose `TableBody` carries
  `RowHeadColumns 1` without per-cell `header` attributes.
- NativeReader preserves the `RowHeadColumns` metadata; NativeWriter
  round-trips the `TableBody ... (RowHeadColumns 1)` packet; WordPress output
  now renders first-column body cells as `<th>` while leaving regular data
  cells as `<td>`. The renderer only upgrades cells fully contained in the
  row-head column range, so existing spanning summary cells remain data cells.
- Focused evidence: targeted upstream-cache reads inspected the row-header
  table region of `test/html-reader.native`; `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-html-row-header-table-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-html-row-header-table-handoff.php`
  emitted a WordPress table with first-column body `<th>` cells; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  3,121 assertions, 0 failures. The focused file now contains 314 behavior
  tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native ODT Nested Continued List Slice

- Focused mapped count is now 1,442 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream ODT
  native fixture `test/odt/native/listContinueNumbering2.native`.
- The targeted ODT subinventory remains 99 artifacts under `test/odt`,
  including 29 `.native` expectations, 18 Markdown source fixtures, and 52
  `.odt` inputs. This extends ODT Native fixture mapping without activating
  OpenDocument ZIP/XML package parsing.
- The copied fixture contributes three split top-level ordered lists with
  start values `1`, `2`, and `3`, three nested `LowerAlpha`/`Period` sublists,
  two interleaved text paragraphs, and four empty `Para []` separators.
- NativeReader preserves the continued-list starts, nested list markers, text
  paragraphs, and empty separators for Native read-back; NativeWriter
  round-trips the `LowerAlpha`/`Period` markers; WordPress output omits empty
  paragraph artifacts while preserving top-level `start` attributes and, in
  opt-in reviewer mode, `data-pandoc-list-style` plus
  `data-pandoc-list-delimiter="period"` for the nested alpha lists.
- Focused evidence: `diff -u <(git -C .upstream-cache/pandoc show
  HEAD:test/odt/native/listContinueNumbering2.native)
  lanes/pandoc/fixtures/upstream-native-odt-list-continuation-nested.native`
  confirmed the copied fixture matches upstream; `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-odt-nested-list-continuation-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-odt-nested-list-continuation-handoff.php`
  emitted continued ordered-list blocks with nested lower-alpha source
  metadata; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
  passed 1 file, 3,109 assertions, 0 failures; `git diff --check --
  lanes/pandoc` passed. The focused file now contains 313 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native ODT Table Span Slice

- Focused mapped count is now 1,422 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream ODT
  native fixture `test/odt/native/tableWithSpans.native`.
- The targeted ODT subinventory remains 99 artifacts under `test/odt`,
  including 29 `.native` expectations, 18 Markdown source fixtures, and 52
  `.odt` inputs. This strengthens ODT Native fixture mapping without activating
  OpenDocument ZIP/XML package parsing.
- The copied fixture contributes a two-row table head with a row-spanning
  header and a column-spanning second header row, a body row with a 3-row span,
  a body row with a 2-column span, a body cell with both a 2-row span and a
  2-column span, and a trailing empty `Para []` packet.
- NativeReader preserves those row/column span counts and the trailing empty
  paragraph for Native read-back; NativeWriter round-trips `RowSpan` and
  `ColSpan` markers; WordPress output emits `rowspan`/`colspan` table cells,
  omits the trailing empty paragraph, and does not invent a `colgroup` for
  default-width ODT columns.
- Targeted upstream-cache smoke evidence parsed all 144 selected DOCX, ODT,
  and EPUB `.native` expectations through NativeReader, NativeWriter, and
  WordPressBlockWriter. Focused PHP evidence: `php -l` passed for
  `MarkdownReaderTest.php` and
  `examples/wordpress-native-odt-table-spans-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-odt-table-spans-handoff.php` emitted a
  WordPress table with header/body rowspans and colspans; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  3,089 assertions, 0 failures. The focused file now contains 312 behavior
  tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Task List Glyph Slice

- Focused mapped count is now 1,402 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream DOCX
  native fixture `test/docx/task_list.native`.
- The targeted DOCX subinventory remains 233 artifacts under `test/docx`,
  including 112 `.native` expectations, 121 `.docx` inputs/goldens, and 38
  golden `.docx` outputs. This strengthens DOCX Native fixture mapping without
  activating DOCX ZIP/OpenXML package parsing.
- The copied fixture contributes top-level unchecked/checked ballot glyph list
  items, a checked item with a continuation paragraph, a nested checked
  sublist, a nested unchecked sub-sublist, and an ordered child list.
- NativeReader preserves the upstream ballot glyphs as Native text nodes and
  NativeWriter round-trips them as numeric string escapes. WordPress output
  remains faithful by default; reviewer handoffs can opt into
  `taskGlyphsAsCheckboxes` to convert those source glyphs into checkbox labels
  without leaving glyphs in the visible labels.
- Targeted upstream-cache smoke evidence parsed all 112 DOCX `.native`
  expectations and all 29 ODT `.native` expectations with NativeReader.
  Focused PHP evidence: `php -l` passed for `WordPressBlockWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-task-list-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-task-list-handoff.php` emitted
  top-level, continuation-paragraph, and nested checkbox handoff markup; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  3,069 assertions, 0 failures. The focused file now contains 311 behavior
  tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native ODT Mixed List Slice

- Focused mapped count is now 1,392 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream ODT
  native fixture `test/odt/native/orderedListMixed.native`.
- The targeted ODT subinventory remains 99 artifacts under `test/odt`,
  including 29 `.native` expectations, 18 Markdown source fixtures, and 52
  `.odt` inputs. This continues to avoid activating the OpenDocument ZIP/XML
  package dependency gate.
- The copied fixture contributes one top-level Decimal/Period ordered list, a
  nested Decimal/Period ordered list, a nested LowerAlpha/OneParen ordered
  list, an empty paragraph separator, and a start=4 continuation list.
- NativeReader already preserved the list marker tuple; NativeWriter now has
  focused round-trip coverage for the ODT `OneParen` marker, and
  WordPressBlockWriter has an opt-in `preserveListAttributes` handoff mode that
  emits reviewer-facing `data-pandoc-list-style` and
  `data-pandoc-list-delimiter` attributes without changing default WordPress
  list output.
- Targeted upstream-cache smoke evidence parsed the full
  `test/odt/native/orderedListMixed.native` fixture as 3 top-level blocks, 4
  ordered lists, 1 one-paren list, and 1 `OneParen` NativeWriter round-trip
  marker. Focused PHP evidence: `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-odt-mixed-list-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-odt-mixed-list-handoff.php` emitted a
  nested WordPress ordered list with opt-in source list marker metadata; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  3,054 assertions, 0 failures. The focused file now contains 310 behavior
  tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native EPUB Default List Style Slice

- Focused mapped count is now 1,383 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream EPUB
  native fixture `test/epub/formatting.native`.
- The targeted EPUB subinventory remains 11 artifacts under `test/epub`,
  including 3 `.native` expectations and 8 `.epub` inputs. This strengthens
  Native read-back for EPUB fixture semantics without activating ZIP/package
  parsing.
- The copied slice contributes EPUB styling title/identifier metadata, one
  source XHTML `Span` marker, one section `Div`, and one `OrderedList` using
  Pandoc Native `( 1 , DefaultStyle , DefaultDelim )`.
- NativeReader now distinguishes `DefaultStyle`/`DefaultDelim` from concrete
  `Decimal`/`Period`; NativeWriter preserves the default markers during
  read-back while keeping hand-authored AST lists with no style/delimiter on
  the existing Decimal/Period output path. WordPress output emits the default
  list as a plain `<ol>` without a misleading concrete `type` attribute.
- Targeted upstream-cache smoke evidence parsed the full
  `test/epub/formatting.native` fixture as 14 top-level blocks with 21 ordered
  lists; all 21 use `DefaultStyle`/`DefaultDelim`, and NativeWriter round-trip
  output preserved 21 default marker pairs. Focused PHP evidence: `php -l`
  passed for `NativeReader.php`, `NativeWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-epub-default-list-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-epub-default-list-handoff.php`
  emitted a metadata review block, source XHTML marker, and a section wrapper
  containing `<ol><li>.</li></ol>`; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,034 assertions,
  0 failures. The focused file now contains 309 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native EPUB Math Slice

- Focused mapped count is now 1,375 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream EPUB
  native fixture `test/epub/features.native`.
- The targeted EPUB subinventory remains 11 artifacts under `test/epub`,
  including 3 `.native` expectations and 8 `.epub` inputs. This strengthens
  the Native fixture mapping without activating EPUB ZIP/package parsing.
- The copied fixture contributes the EPUB testsuite title/identifier metadata,
  one source XHTML `Span` marker, one MathML section `Div`, one required test
  `Div`, one optional test `Div`, three `Math DisplayMath` packets, and one
  `Math InlineMath` packet.
- NativeWriter now preserves boolean display math parsed by NativeReader
  instead of rewriting `DisplayMath` as `InlineMath` during read-back.
  WordPress output renders the source math as display or inline math spans
  while keeping the source EPUB section ids/classes in reviewer HTML blocks.
- Targeted upstream-cache smoke evidence parsed the full
  `test/epub/features.native` fixture as 6 top-level blocks, 11 math nodes,
  and 6 display math nodes, then round-tripped 6 `DisplayMath` and 5
  `InlineMath` markers through NativeWriter. Focused PHP evidence: `php -l`
  passed for `NativeWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-epub-math-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-epub-math-handoff.php` emitted a
  metadata review block, source XHTML section ids/classes, three display math
  spans, and one inline math span; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 3,018 assertions,
  0 failures. The focused file now contains 308 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native EPUB Section Slice

- Focused mapped count is now 1,363 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream EPUB
  native fixture `test/epub/wasteland.native`.
- The targeted EPUB subinventory is now explicitly counted: 11 artifacts under
  `test/epub`, including 3 `.native` expectations and 8 `.epub` inputs. This
  strengthens the static inventory without activating EPUB ZIP/package parsing.
- The copied fixture contributes `The Waste Land` title/date/author metadata,
  a cover `Image` packet, one source XHTML `Span` marker, and three section
  `Div` packets carrying source XHTML ids plus `section`/`frontmatter`/
  `bodymatter` classes.
- WordPress output now preserves safe Native `Div` ids/classes/data/aria-like
  attributes in reviewer HTML blocks while continuing to map source
  `custom-style` to `data-pandoc-custom-style`. NativeWriter also preserves a
  parsed single-author `MetaInlines` value instead of coercing it through a
  list-string path.
- Targeted upstream cache reads inspected `test/epub/wasteland.native`; `git
  ls-tree` counted `test/epub` artifacts. Focused PHP evidence: `php -l`
  passed for `NativeWriter.php`, `WordPressBlockWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-epub-section-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-epub-section-handoff.php` emitted a
  metadata review block, cover image block, source XHTML marker, and nested
  section divs with source ids/classes; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 2,991 assertions,
  0 failures. The focused file now contains 307 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native ODT Image Caption Slice

- Focused mapped count is now 1,344 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream ODT
  native fixture `test/odt/native/imageWithCaption.native`.
- The targeted ODT subinventory remains 99 artifacts under `test/odt`,
  including 29 `.native` expectations, 18 Markdown source fixtures, and 52
  `.odt` inputs. This continues to avoid activating the OpenDocument ZIP/XML
  package dependency gate.
- The copied fixture contributes one `Figure` packet, one `Caption` inline
  list, one `Image` node, the `Pictures/...jpg` source target, ODT-derived
  `width="5.292cm"` and `height="5.292cm"` attributes, and an image alt label
  that includes the source figure number text.
- WordPress output now renders Native figure captions from parsed caption
  inline nodes before falling back to plain caption strings. That keeps the
  visible WordPress figcaption as `Image caption` while preserving the source
  image alt label `Abbildung 1: Image caption` and source dimensions as
  reviewer-safe image metadata.
- Targeted upstream cache reads inspected
  `test/odt/native/imageWithCaption.native`; `git ls-tree` counted `test/odt`
  artifacts. Focused PHP evidence: `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-odt-image-caption-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-odt-image-caption-handoff.php` emitted
  a WordPress image block with source dimensions and figcaption; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  2,972 assertions, 0 failures. The focused file now contains 306 behavior
  tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native ODT Reference Anchor Slice

- Focused mapped count is now 1,332 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream ODT
  native fixtures `test/odt/native/referenceToText.native` and
  `test/odt/native/referenceToListItem.native`.
- The targeted ODT subinventory remains 99 artifacts under `test/odt`,
  including 29 `.native` expectations, 18 Markdown source fixtures, and 52
  `.odt` inputs. This extends the Native fixture mapping without activating the
  OpenDocument ZIP/XML package dependency gate.
- The copied fixtures contribute three anchor `Span` packets, two
  same-document `Link` targets, one explicit `LineBreak`, and two empty
  `Para []` separators. One upstream anchor id contains a space (`an anchor`),
  which is preserved in NativeReader/NativeWriter round-trip output.
- WordPress output now normalizes whitespace-containing same-document
  fragments to valid HTML ids/hrefs, while preserving source values as
  `data-pandoc-source-id` and `data-pandoc-source-href` metadata. Plain
  `#anchor` list-item references remain unchanged, and empty separators are
  still omitted from block output.
- Targeted upstream cache reads inspected
  `test/odt/native/referenceToText.native` and
  `test/odt/native/referenceToListItem.native`; `git ls-tree` counted
  `test/odt` artifacts. Focused PHP evidence: `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-odt-reference-anchor-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-odt-reference-anchor-handoff.php`
  emitted normalized same-document fragments with source metadata; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  2,957 assertions, 0 failures. The focused file now contains 305 behavior
  tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native ODT Continued List Slice

- Focused mapped count is now 1,316 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream ODT
  native fixture `test/odt/native/listContinueNumbering.native`.
- The targeted ODT subinventory is now explicitly counted: 99 artifacts under
  `test/odt`, including 29 `.native` expectations, 18 Markdown source
  fixtures, and 52 `.odt` inputs. This strengthens the static inventory
  without activating the OpenDocument ZIP/XML package dependency gate.
- The copied fixture contributes five continued ordered-list start values
  (`1`, `2`, `4`, `1`, `2`) separated by empty `Para []` nodes that Pandoc
  preserves in Native output.
- NativeReader preserves the continued-list starts and empty paragraph
  separators, NativeWriter round-trips those empty `Para` packets, and
  WordPress output omits the empty paragraph separators while preserving
  `<ol start="...">` attributes for continued lists. This keeps ODT import
  review blocks from accumulating blank paragraph artifacts.
- Targeted upstream cache reads inspected
  `test/odt/native/listContinueNumbering.native` and `git ls-tree` counted
  `test/odt` artifacts. Focused PHP evidence: `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-odt-list-continuation-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-odt-list-continuation-handoff.php`
  emitted WordPress ordered-list blocks with preserved starts and no empty
  `<p></p>` blocks; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 2,936 assertions,
  0 failures. The focused file now contains 304 behavior tests. No
  no-argument root harness was assigned or started.

## 2026-05-24 Native Citation Metadata Slice

- Focused mapped count is now 1,290 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  fixture `test/markdown-citations.native`.
- The copied slice contributes normal, author-in-text, suppress-author,
  grouped, non-ASCII, and note-contained `Cite`/`Citation` records with ids,
  modes, note numbers, prefixes, suffixes, and rendered citation display text.
- WordPress output now preserves visible citation text while wrapping it in a
  `pandoc-citation` span with `data-pandoc-citation-*` metadata. That gives
  import/review tooling a citation handoff without invoking upstream Pandoc or
  citeproc.
- Targeted upstream cache reads inspected `test/markdown-citations.native`.
  A focused probe parsed all 112 `test/docx/*.native` fixtures through
  `NativeReader` without failures; this is parser smoke evidence, not an
  upstream Haskell runner. Focused PHP evidence: `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-citation-metadata-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-citation-metadata-handoff.php`
  emitted WordPress citation metadata spans; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 2,910 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Table Caption Anchor Slice

- Focused mapped count is now 1,304 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX native fixture `test/docx/table_captions_with_field.native`.
- The copied fixture contributes two table cross-reference links to `#_Ref...`
  targets and two `Caption` packets whose long captions begin with empty
  `Span` nodes carrying the matching `_Ref...` ids and `anchor` classes.
- NativeReader now preserves single-block caption inline nodes in
  `captionInlines` instead of flattening captions to plain text only. That
  keeps hidden caption anchors available for NativeWriter round-trip output and
  WordPress figcaption rendering.
- WordPress output now keeps the source table anchors as
  `<span id="_Ref..." class="anchor"></span>` inside figcaptions while
  preserving the surrounding "See Table" links. This supports DOCX imports
  where Word table fields/cross-references need stable in-page targets after a
  block conversion.
- Targeted upstream cache reads inspected
  `test/docx/table_captions_with_field.native`. Focused PHP evidence:
  `php -l` passed for `NativeReader.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-table-caption-anchor-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-table-caption-anchor-handoff.php`
  emitted WordPress table captions with preserved `_Ref` anchors; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file,
  2,924 assertions, 0 failures. The focused file now contains 303 behavior
  tests. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Image Dimension Slice

- Focused mapped count is now 1,262 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX native fixture `test/docx/image_no_embed.native`.
- The copied fixture contributes an `Image` inline with a `media/image1.jpg`
  target, title text, alt inlines, and DOCX-derived `width="6.5in"` plus
  `height="5.508333333333334in"` Pandoc attributes.
- WordPress output now preserves these source dimensions as
  `data-pandoc-width`/`data-pandoc-height` plus sanitized CSS dimensions on
  the rendered image. It intentionally does not emit raw HTML `width`/`height`
  attributes with CSS units.
- Targeted upstream cache reads inspected `test/docx/image_no_embed.native`.
  Focused PHP evidence: `php -l` passed for `WordPressBlockWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-image-dimensions-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-image-dimensions-handoff.php`
  emitted a WordPress image block with source image dimensions preserved as
  reviewer-safe metadata; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 2,882 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Table Header Rowspan Slice

- Focused mapped count is now 1,251 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream
  DOCX native fixture `test/docx/table_header_rowspan.native`.
- The copied slice contributes scientific-notation `ColWidth` literals such as
  `9.707602339181289e-2`, a two-row `TableHead`, row-spanned header cells, a
  grouped `ColSpan 3` header, and an eight-cell body row.
- NativeReader now accepts exponent notation in Native numeric tokens, which
  is required for upstream DOCX table width packets. NativeWriter renders the
  parsed values back as deterministic decimal `ColWidth` values.
- WordPress output renders the upstream-shaped table as a core table with
  `colgroup` widths, `rowspan`, `colspan`, and inherited first-column
  alignment for the second header row.
- Targeted upstream cache reads inspected `test/docx/table_header_rowspan.native`
  for the source expectation. Focused PHP evidence: `php -l` passed for
  `NativeReader.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-table-header-rowspan-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-table-header-rowspan-handoff.php`
  emitted a WordPress table with scientific width-derived colgroup output plus
  spanned header cells; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 2,871 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Empty Field / Index Reference Slice

- Focused mapped count is now 1,235 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream DOCX
  native fixture `test/docx/empty_field.native`.
- The copied fixture contributes an empty `Span` with class `indexref` and an
  `entry` attribute, two imported external `Link` nodes, and Haskell numeric
  string escapes with `\&` separators in surrounding Unicode text.
- WordPress output now maps `indexref` spans to
  `data-pandoc-index-entry`, so migration reviewers can preserve source index
  terms without emitting raw upstream `entry` attributes.
- Targeted upstream cache reads inspected `test/docx/empty_field.native` for
  the source expectation. Focused PHP evidence: `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-index-field-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-index-field-handoff.php` emitted
  WordPress paragraphs with a `data-pandoc-index-entry` span, decoded text, and
  escaped link URLs; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 2,855 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 Native DOCX Document Properties Slice

- Focused mapped count is now 1,222 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream DOCX
  native fixture `test/docx/document-properties.native`.
- The copied fixture contributes a full `Pandoc (Meta ...)` packet with title,
  author, custom properties, `MetaBlocks` descriptions, keyword `MetaList`
  values, raw HTML metadata in `RawInline (Format "html")` nodes, and nested
  `MetaMap` custom values.
- WordPress output now has an opt-in `includeMetadata` mode that emits a
  reviewer-safe metadata block for document properties while escaping raw HTML
  metadata instead of executing it.
- Targeted upstream cache reads inspected `test/docx/document-properties.native`
  plus adjacent `document-properties-short-desc.native`,
  `metadata.native`, and `metadata_after_normal.native` expectations for
  semantic context. Focused PHP evidence: `php -l` passed for
  `WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-document-properties-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-document-properties-handoff.php`
  emitted a WordPress metadata review block plus the body paragraph; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
  file, 2,841 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 Native DOCX Custom Style Slice

- Focused mapped count is now 1,203 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/NativeWriter/WordPress coverage from upstream DOCX
  native fixture `test/docx/custom_style.native`.
- The copied fixture contributes three `Span` packets and one `Div` packet
  carrying DOCX `custom-style` attributes for inline and block Word styles.
- WordPress output now maps those attributes to
  `data-pandoc-custom-style`, so migration reviewers can see source Word style
  boundaries without emitting raw upstream `custom-style` attributes.
- Targeted upstream cache reads inspected `test/docx/custom_style.native` plus
  adjacent preserve/roundtrip custom-style expectations for semantic context.
  Focused PHP evidence: `php -l` passed for `WordPressBlockWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-custom-style-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-custom-style-handoff.php`
  emitted WordPress paragraphs and a reviewer HTML block with
  `data-pandoc-custom-style` attributes; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 2,822
  assertions, 0 failures. No no-argument root harness was assigned or
  started.

## 2026-05-24 Native DOCX Paragraph Change Slice

- Focused mapped count is now 1,191 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/WordPress coverage from upstream DOCX native
  fixture `test/docx/paragraph_insertion_deletion_all.native`.
- The copied fixture contributes one `Span` with class `paragraph-insertion`
  and one `Span` with class `paragraph-deletion`, both carrying DOCX author and
  date metadata on empty paragraph-boundary markers.
- WordPress output now maps those paragraph boundary spans to zero-width
  review markers with `data-pandoc-paragraph-change`, `data-pandoc-change-*`,
  and `datetime` metadata, so paragraph split/merge review state remains
  inspectable without leaking raw upstream `author` or `date` attributes.
- Targeted upstream cache reads inspected
  `test/docx/paragraph_insertion_deletion_all.native` plus the adjacent
  accept/reject expectations for semantic context. Focused PHP evidence:
  `php -l` passed for `WordPressBlockWriter.php`, `MarkdownReaderTest.php`,
  and `examples/wordpress-native-docx-paragraph-change-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-paragraph-change-handoff.php`
  emitted WordPress paragraphs with paragraph insertion/deletion metadata;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
  1 file, 2,810 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 Native DOCX Raw OpenXML Slice

- Focused mapped count is now 1,177 Markdown/HTML/Native/WordPress checks.
- Added bounded NativeReader/WordPress coverage from upstream DOCX native
  fixtures: `test/docx/raw-bookmarks.native` and
  `test/docx/raw-blocks.native`.
- The copied raw-bookmarks fixture contributes two `RawInline (Format
  "openxml")` bookmark boundary nodes. WordPress output now maps
  `w:bookmarkStart` and `w:bookmarkEnd` to zero-width spans carrying
  `data-pandoc-bookmark-id` and, for starts, `data-pandoc-bookmark-name`.
- The copied raw-blocks fixture contributes three `RawBlock (Format
  "openxml")` table-fragment packets. WordPress output now renders generic raw
  blocks as escaped review code blocks with `data-pandoc-raw-format`, so DOCX
  OpenXML remains visible to migration reviewers without becoming active HTML.
- Focused PHP evidence: `php -l` passed for `WordPressBlockWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-docx-raw-openxml-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-docx-raw-openxml-handoff.php` emitted
  WordPress paragraphs with bookmark metadata plus escaped OpenXML code blocks;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
  1 file, 2,796 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 NativeWriter Table Slice

- Focused mapped count is now 1,114 Markdown/HTML/Native/WordPress checks.
- Added bounded Native writer coverage from upstream native table output:
  `test/pipe-tables.native`, `test/tables.native`,
  `test/markdown-reader-more.native`, and the `Tests.Writers.Native`
  block-list round-trip boundary.
- NativeWriter now emits Pandoc-style `Table` blocks with table Attr tuples,
  `Caption` output, `ColSpec` alignment/width fields, `TableHead`,
  `TableBody` with `RowHeadColumns` and body-local head rows, `TableFoot`,
  and `Cell` Attr/Align/RowSpan/ColSpan fields. Inline cells are represented
  as `Plain` blocks, and block cells preserve nested block lists.
- Focused PHP evidence: `php -l` passed for `NativeWriter.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-table-handoff.php`;
  `php lanes/pandoc/examples/wordpress-native-table-handoff.php` emitted a
  standalone Pandoc Native reviewer packet with table metadata, caption,
  colspecs, row-head columns, spanned cells, and nested block-cell content;
  `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
  1 file, 2,674 assertions, 0 failures. No no-argument root harness was
  assigned or started.

## 2026-05-24 Native String Numeric Escape Slice

- Focused mapped count is now 1,130 Markdown/HTML/Native/WordPress checks.
- Added targeted Native string coverage from upstream `test/writer.native`,
  which contains the Haskell string escape shape `Str "M.A.\160\&2007"`,
  and from the `Tests.Writers.Native` read/show boundary.
- NativeReader now treats Haskell `\&` as an empty separator instead of a
  literal ampersand, so decimal character escapes immediately followed by
  digits decode correctly. NativeWriter now inserts `\&` after decimal escapes
  when the next character is a digit, preventing ambiguous output such as
  `\16042`.
- WordPress handoff now includes
  `examples/wordpress-native-string-escape-handoff.php`, which reads a Native
  packet containing nonbreaking-space source IDs and emits WordPress paragraph
  blocks without invoking upstream Pandoc.
- Focused PHP evidence: `php -l` passed for `NativeReader.php`,
  `NativeWriter.php`, `MarkdownReaderTest.php`, and
  `examples/wordpress-native-string-escape-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-string-escape-handoff.php` emitted
  WordPress paragraphs with nonbreaking-space source IDs; `php
  tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
  file, 2,711 assertions, 0 failures. No no-argument root harness was assigned
  or started.

## 2026-05-24 NativeReader Read-Back Slice

- Focused mapped count is now 1,126 Markdown/HTML/Native/WordPress checks.
- Added bounded Native reader coverage for the upstream
  `Tests.Writers.Native` property boundary: `p_write_rt` reads full Pandoc
  Native output back to an AST, and `p_write_blocks_rt` reads blocks-only
  Native output back to a block list. The PHP slice does this for the lane's
  bounded common AST rather than the full upstream arbitrary AST.
- NativeReader now parses the Native syntax emitted by NativeWriter for
  `Pandoc` documents, `Meta { unMeta = fromList ... }`, blocks-only lists,
  Attr tuples, common block and inline constructors, Figures, Cites, Tables,
  captions, citation records, row/column spans, table head/body/foot sections,
  and Haskell-style string escapes.
- WordPress handoff now includes
  `examples/wordpress-native-reader-handoff.php`, which writes a Native packet,
  reads it back through NativeReader, and emits WordPress heading,
  paragraph/link, and table blocks.
- Focused PHP evidence: `php -l` passed for `NativeReader.php`,
  `MarkdownReaderTest.php`, and
  `examples/wordpress-native-reader-handoff.php`; `php
  lanes/pandoc/examples/wordpress-native-reader-handoff.php` emitted WordPress
  block markup from a NativeReader-parsed packet; `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 2,704 assertions,
  0 failures. No no-argument root harness was assigned or started.

## 2026-05-24 NativeWriter Figure/Cite Slice

- Focused mapped count is now 1,105 Markdown/HTML/Native/WordPress checks.
- Added bounded Native writer coverage from upstream native fixture output:
  `test/command/html-read-figure.md`, `test/command/cite-in-inline-note.md`,
  `test/markdown-citations.native`, and the Images section of
  `test/testsuite.native`.
- NativeWriter now emits Pandoc-style `Figure` blocks with Attr tuples,
  `Caption Nothing` or `Caption (Just [...])`, long caption blocks, and
  Plain-wrapped image bodies. The slice intentionally covers the existing
  lane AST's figure/image boundary, not arbitrary nested figures.
- NativeWriter now emits `Cite` inline nodes with `Citation` records,
  prefix/suffix inline lists, `NormalCitation`, `AuthorInText`, and
  `SuppressAuthor` modes, plus note-number/hash fields. Citeproc processing
  and bibliography rendering remain outside this native writer slice.
- Focused PHP evidence: `php tools/run-tests.php
  lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 2,666
  assertions, 0 failures. No no-argument root harness was assigned or started.

## Counted Static Denominator

- `test/` files/artifacts: 1,974
- `test/Tests/**/*.hs` Haskell test modules: 62
- Reader test modules: 34
- Writer test modules: 22
- Shared/support test modules: 6
- `test/command` artifacts: 1,155
- `test/command/*.md` command fixture files: 1,064
- `.native` expected artifacts under `test/`: 252
- `test/testsuite.txt` top-level Markdown sections: 14
- `test/testsuite.native` rendered native AST lines: 2,238
- `test/testsuite.native` `BlockQuote` nodes in the full rendered suite: 7
- `test/testsuite.native` `CodeBlock` nodes in the full rendered suite: 11
- `test/testsuite.native` `BulletList`/`OrderedList` nodes in the full rendered
  suite: 36
- `test/testsuite.txt` Lists section slice inspected in this run: 163 Markdown
  lines from `# Lists` through the start of `# Definition Lists`
- `test/testsuite.txt` Definition Lists section slice inspected in this run: 93
  Markdown lines through the start of `# HTML Blocks`
- `test/testsuite.native` Definition Lists rendered native AST slice inspected
  in this run: 163 lines
- `test/testsuite.txt` HTML Blocks section slice inspected in this run: 102
  Markdown lines through the start of `# Inline Markup`
- `test/testsuite.native` HTML Blocks rendered native AST slice inspected in
  this run: 161 lines, including 22 `RawBlock` markers, 9 `Div` markers, and 4
  `CodeBlock` markers
- `test/testsuite.txt` Inline Markup section slice inspected in this run: 30
  Markdown lines through the start of `# Smart quotes, ellipses, dashes`
- `test/testsuite.native` Inline Markup rendered native AST slice inspected in
  this run: 168 lines, including 9 `Emph` markers, 6 `Strong` markers,
  1 `Strikeout` marker, 3 `Superscript` markers, and 3 `Subscript` markers
- `test/testsuite.txt` Smart quotes, ellipses, dashes section slice inspected
  in this run: 21 Markdown lines through the start of `# LaTeX`
- `test/testsuite.native` Smart quotes, ellipses, dashes rendered native AST
  slice inspected in this run: 154 lines, including 14 `Quoted` markers plus
  smart apostrophe, em-dash, en-dash, and ellipsis code points in `Str` nodes
- `test/testsuite.txt` LaTeX section slice inspected in this run: 30 Markdown
  lines through the start of `# Special Characters`
- `test/testsuite.native` LaTeX rendered native AST slice inspected in this
  run: 152 lines, including 6 `InlineMath` markers, 1 `DisplayMath` marker,
  1 TeX `RawInline`, and 1 TeX `RawBlock`
- `test/testsuite.txt` Special Characters section slice inspected in this run:
  54 Markdown lines through the start of `# Links`, including five Unicode
  bullet-list item lines, one HTML entity line, sixteen punctuation
  backslash-escape lines, and a dashed horizontal rule
- `test/testsuite.native` Special Characters rendered native AST slice
  inspected in this run: 86 lines, including one `BulletList`, 45 `Str`
  markers, 22 `Para` markers, and one `HorizontalRule`
- `test/testsuite.txt` Links section slice inspected in this run: 86 Markdown
  lines through the start of `# Images`, covering explicit links, reference
  links, ampersand URL/text cases, URI/email autolinks, and no-autolink code
  contexts
- `test/testsuite.native` Links rendered native AST slice inspected in this
  run: 290 lines, including 25 `Link` nodes plus the code-block and code-span
  cases where autolinks must not fire
- `test/testsuite.txt` Images section slice inspected in this run: 12 Markdown
  lines through the start of `# Footnotes`, covering a standalone collapsed
  reference image with title metadata and an inline image inside a paragraph
- `test/testsuite.native` Images rendered native AST slice inspected in this
  run: 48 lines, including 2 `Image` nodes and 1 `Figure` node
- `test/testsuite.txt` Footnotes section slice inspected in this run: 28
  Markdown lines through end of file, covering reference notes, inline notes,
  quote-contained notes, list-contained notes, multi-block definitions,
  whitespace-separated termination, and an invalid spaced footnote label
- `test/testsuite.native` Footnotes rendered native AST slice inspected in this
  run: 305 lines, including 4 `Note` nodes
- `test/markdown-reader-more.txt` inspected in this run: 366 Markdown lines in
  Pandoc's additional Markdown reader fixture.
- `test/markdown-reader-more.native` inspected in this run: 1,715 rendered
  native AST lines.
- `test/markdown-reader-more.txt` title-block slice inspected in this run:
  six leading metadata lines covering a multiline `%` title, author lines split
  by both line boundaries and semicolons, and a blank separator before the first
  body heading.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 1-27 show `MetaInlines` title content
  with a `SoftBreak`, a four-entry `MetaList` author field, no date metadata,
  and `# Additional markdown reader tests` as the first body `Header`.
- `test/markdown-reader-more.txt` blank-reference and URL-space slice inspected
  in this run: 44 Markdown lines covering two reference definitions whose
  targets/titles live on following lines, four inline link destinations with
  spaces or multiline spaces, and three reference link destinations with spaces
  plus one parenthesized title.
- `test/markdown-reader-more.native` corresponding rendered AST slice inspected
  in this run: 100 lines showing two split reference-definition links and
  seven space-containing link destinations normalized with `%20`.
- `test/markdown-reader-more.txt` implicit-header-reference slice inspected in
  this run: upstream lines 169-189 cover shortcut, collapsed, and
  case-insensitive implicit references, an explicit reference definition that
  overrides an implicit heading reference, and explicit heading attributes.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 329-450 show Pandoc's duplicate
  generated heading id behavior (`my-header-1` after an earlier
  `my-header`), `#my-header` links for the implicit forms, `/foo` for the
  explicit override, and `#foobar` plus class/key metadata for the attributed
  heading.
- `test/markdown-reader-more.txt` backslash-newline and code-span slice
  inspected in this run: upstream lines 101-117 cover an explicit trailing
  backslash hard break, a code span ending in a literal backslash, a multiline
  code span, a longer backtick-delimited code span containing four literal
  backticks, and a blank-line-terminated unterminated code span.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 235-249 show `LineBreak`, three
  `Code` nodes with normalized code text, and two literal paragraph strings for
  the blank-line-terminated unterminated code span.
- `test/markdown-reader-more.txt` multilingual URL and numbered-example slice
  inspected in this run: upstream lines 119-135 cover one Unicode URI
  autolink, one inline link whose destination and title include Unicode source
  text, one Unicode e-mail autolink, two initial numbered examples, two inline
  references to example labels, and a later labeled example.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 249-295 show three `Link` nodes, an
  `OrderedList (1, Example, TwoParens)` with two items, inline paragraph text
  where `(@foo)` and `(@bar)` have become `(2)` and `(3)`, and a later
  `OrderedList (3, Example, TwoParens)` with one item.
- `test/markdown-reader-more.txt` case-insensitive reference, curly quote, and
  consecutive-list slice inspected in this run: upstream lines 142-167 cover
  three shortcut reference links whose definitions differ by case, two
  paragraphs containing already-curly Unicode quote marks, and three adjacent
  list families where the final one-space-indented `a.`/`b.` list remains a
  separate top-level lower-alpha list.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 301-328 show three resolved `Link`
  nodes, two literal `Str` nodes containing U+201C/U+201D and U+2018/U+2019,
  followed by separate `BulletList`, decimal `OrderedList`, and lower-alpha
  `OrderedList` blocks.
- `test/markdown-reader-more.txt` line-block slice inspected in this run:
  upstream lines 191-201 cover one pipe-prefixed line block with four
  indentation levels, one empty line entry, and two continuation-line cases
  where indented non-pipe lines fold into the previous line.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 451-516 show one `LineBlock` with
  seven line entries, including nonbreaking-space indentation counts of 4, 8,
  12, and 2 before the visible text.
- `test/markdown-reader-more.txt` indented-code-at-beginning-of-list slice
  inspected in this run: upstream lines 85-99 cover one bullet item whose first
  child is a code block, two nested ordered-list items whose first children are
  code blocks, one nested bullet item whose first child is a code block, and
  one four-space guard item that stays ordinary prose.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 207-234 show `CodeBlock` nodes for the
  five-space marker-padding items, an `OrderedList (1, Decimal, Period)` whose
  second item is numbered `12345678`, and a nested `BulletList` where
  `-    no code` remains `Plain [ Str "no" , Space , Str "code" ]`.
- `test/markdown-reader-more.txt` raw TeX environment and macro slices
  inspected in this run: upstream lines 20-37 cover Raw ConTeXt and Raw LaTeX
  environments, and lines 136-140 cover a `\newcommand` macro followed by math
  using the macro.
- `test/markdown-reader-more.native` corresponding rendered AST slices
  inspected in this run: upstream lines 61-94 show one `\placeformula
  \startformula` `RawBlock`, one paragraph ending with a `\stopformula`
  `RawInline`, one nested `\start[a2]`/`\stop[a2]` `RawBlock`, and one nested
  LaTeX `center`/`tikzpicture` `RawBlock`; upstream lines 296-300 show a
  `\newcommand{\tuple}[1]{\langle #1 \rangle}` `RawBlock` and later math
  expanded to `\langle x,y \rangle`.
- `test/markdown-reader-more.txt` `$ in math` slice inspected in this run:
  upstream lines 67-75 cover escaped dollar signs inside inline math, dollars
  inside a TeX `\text{...}` braced argument, and the `$PATH 90 $PATH`
  non-math guard.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 173-192 show two `Math InlineMath`
  nodes, including `x = \text{the $n$th root of $y$}`, followed by one
  ordinary paragraph containing literal `$PATH 90 $PATH` text.
- `test/markdown-reader-more.txt` horizontal-rule/raw-HTML/commented-list slice
  inspected in this run: upstream lines 55-83 cover two trailing-space
  horizontal-rule forms, one empty raw HTML anchor immediately before a level-3
  heading, and a commented-out list marker shape between two ordinary list
  items.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 137-206 show two `HorizontalRule`
  nodes, a paragraph with separate raw HTML open/close inline nodes for
  `<a></a>`, a `Header 3` with identifier `my-header`, and one `BulletList`
  whose commented marker lines remain attached to list item text.
- `test/markdown-reader-more.txt` rectangular grid-table slice inspected in
  this run: 74 mapped Markdown lines from the Grid Tables section cover the
  simple headed table, headless table, aligned headed table, aligned headless
  table, trailing-space table, East Asian width table, zero-width German and
  Persian text cases, and empty cells.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: 642 mapped native AST lines show Pandoc `Table`
  nodes with `ColWidth` values derived from grid widths divided by 72, default
  and right/left/center alignments, `TableHead []` for headless cases,
  `SoftBreak` entries inside multiline scalar cells, Unicode text cells, and
  empty `Cell ... []` bodies.
- `test/markdown-reader-more.txt` grid-table multiple-block cell case
  inspected in this run: upstream lines 252-261 cover a rectangular grid table
  whose cells contain Markdown headings, paragraph-separated text caused by an
  empty interior cell line, bullet-list items, and scalar multiline text.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 987-1087 show a headless `Table` whose
  first body row contains `Header` plus `Para` block children in each cell, and
  whose second body row contains two `Para` blocks, one `BulletList`, and one
  scalar `Plain` cell with `SoftBreak` entries.
- `test/markdown-reader-more.txt` remaining grid-table span cases inspected in
  this run: upstream lines 290-313 cover a row/column-span table plus a complex
  multi-row header table.
- `test/markdown-reader-more.native` corresponding rendered AST slice
  inspected in this run: upstream lines 1260-1492 show a header `Cell` with
  `ColSpan 2`, a body `Cell` with `RowSpan 3`, and a complex two-row
  `TableHead` whose `Location` header has `RowSpan 2` and whose temperature
  header has `ColSpan 3`.
- `test/markdown-reader-more.txt` post-grid reference-link edge slice inspected
  in this run: upstream lines 337-358 cover a backslash-containing link label,
  an unresolved reference-looking fallback pair, a shortcut reference followed
  by a citation marker, and an empty reference definition before ordinary
  paragraph text.
- `test/markdown-reader-more.native` corresponding rendered AST slice inspected
  in this run: upstream lines 1549-1649 show a `Link` whose label contains
  `Str "*"` plus `RawInline "\\a"`, bracketed fallback text retaining emphasized
  contents, a `Link` immediately followed by a `Cite`, and an empty-destination
  `Link` after the intervening `bar` paragraph.
- `test/markdown-reader-more.txt` wrapping/bracketed-span tail slice inspected
  in this run: upstream lines 360-366 cover one long bullet item ending in
  `2015.` and one bracketed span with `.class`, `#id`, and `key=val`
  attributes.
- `test/markdown-reader-more.native` corresponding rendered AST slice inspected
  in this run: upstream lines 1650-1715 show the heading id
  `wrapping-shouldnt-introduce-new-list-items`, one tight `BulletList` item
  whose `2015.` suffix remains plain text, and one `Span` containing nested
  `Emph` plus a `Link`.
- `test/pipe-tables.txt` pipe-table fixture inspected in this run: 82 Markdown
  lines covering 11 upstream pipe tables, including captioned, uncaptioned,
  headerless, side-less, one-column, no-body, relative-width, and tricky
  escaped-pipe/code-span cell cases
- `test/pipe-tables.native` pipe-table rendered native AST inspected in this
  run: 927 lines, including 11 `Table` nodes, 88 `Cell` nodes, two headerless
  `TableHead []` shapes, one `Code` node containing a literal pipe, and three
  relative-width `ColWidth` entries
- `test/tables.markdown` simple/multiline table fixture inspected in this run:
  76 Markdown lines covering seven gridless tables; all seven simple and
  multiline gridless table cases are now mapped
- `test/tables.native` rendered native AST inspected in this run: 964 lines,
  including seven `Table` nodes and two headerless `TableHead []` shapes
- `test/command/short-caption.md` command fixture inspected in this run: one
  LaTeX reader example whose native output is a `Table` with
  `Caption (Just [Str "short", Space, Str "caption"]) [Plain [...]]`, two
  left-aligned columns, no table head, and one body row.
- `test/command/table-with-cell-align.md` command fixture inspected in this
  run: 105 lines covering a DocBook `informaltable` reader example whose
  native output keeps per-cell `AlignCenter`, `AlignLeft`, `AlignRight`, and
  `AlignDefault` cell alignment while leaving table-level column alignments
  default.
- `test/command/table-with-column-span.md` command fixture inspected in this
  run: 385 lines covering a DocBook `informaltable` reader example with 16
  `colspec` entries, `ColWidth 6.25e-2`, strong emphasis inside spanned cells,
  and `namest`/`nameend` entries that become `ColSpan 8` cells.
- `test/command/rst-writer-gridtable-if-rowspans.md` command fixture inspected
  in this run: 246 lines covering Pandoc native table input rendered to RST
  grid tables. The native AST includes `RowSpan 2` cells in body, head, and
  foot sections; the bounded PHP slice maps those row-span and section shapes
  through DocBook `morerows`, `thead`, `tbody`, and `tfoot` input and
  WordPress `rowspan`/`tfoot` output.
- `test/command/nested-table-to-asciidoc-6942.md` command fixture inspected in
  this run: 82 lines covering HTML input rendered to AsciiDoc, including a
  two-level nested table that Pandoc renders as a nested table and a separate
  three-level case where the AsciiDoc writer warns because that target format
  only supports two table levels. The bounded PHP slice maps both the
  two-level nested-table AST shape and the full HTML document third-level case
  at the WordPress boundary; WordPress output preserves the third nested table
  rather than applying the AsciiDoc-specific downgrade.
- `test/command/tasklist.md` command fixture inspected in this run: 104 lines
  covering Pandoc's HTML writer output for simple task lists, nested task
  lists, mixed task/plain bullet lists, ordered task items, loose task items,
  plus separate LaTeX and Markdown round-trip examples. The bounded PHP slice
  maps the three HTML task-list examples plus the LaTeX and Markdown
  writer-specific examples.
- `src/Text/Pandoc/Writers/Markdown.hs` and `src/Text/Pandoc/Shared.hs`
  ordered-list writer path inspected in this run: Pandoc enables fancy list
  enumerators for Markdown, calls `orderedListMarkers`, preserves
  Decimal/DefaultStyle, upper/lower alpha, upper/lower roman, Period,
  OneParen, and TwoParens attributes, and pads markers shorter than three
  characters before hanging item content at the default tab stop.
- `test/html-reader.html` table section inspected in this run: 366 HTML lines
  from the upstream HTML reader fixture covering table head/body/foot sections,
  omitted section tags, row headers, colspan, rowspan, two tables with multiple
  `<tbody>` sections, plain tables without headers, and empty tables. This run
  used it as bounded reader context without claiming full HTML reader parity.
- `test/html-reader.native` table section inspected in this run: 1,393 native
  AST lines covering 18 `Table` nodes from the upstream HTML reader fixture.
  The inspected HTML slice contains 19 `<table` starts, 47 `<th` cells, 10
  `<thead` starts, 17 `<tbody` starts, 5 `<tfoot` starts, 20 native
  `TableBody` nodes, two native tables with multiple `TableBody` sections, one
  `Cell ... [ Para [ Str "2" ] ]` paragraph-bearing table cell in the second
  multiple-body case, one native `RowHeadColumns 1` body shape, and four native
  `TableBody` nodes with body-local head rows before ordinary body rows. The
  bounded PHP mapping now includes the two native colspan/rowspan table shapes,
  the attribute-carrying table shape, the two multiple-body table shapes, the
  paragraph-bearing cell from the second multiple-body case, the four
  body-local `TableBody` head-row cases, the four plain `Tables without
  Headers` body-only/body-omitted/empty-head/body-plus-foot shapes, plus the two
  empty table inputs omitted from the upstream native output.
- `test/html-reader.html` upstream Attributes table inspected in this run:
  lines 766-786 include table id metadata, `thead` class metadata, a head-row
  class, `tbody` class plus `data-part`, body-row `data-part`, practical cell
  attrs, `tfoot` class metadata, and a foot-row `bgcolor` marker.
- `test/html-reader.native` upstream Attributes table rendered AST inspected in
  this run: lines 3202-3272 show those fields as Pandoc native attributes on
  `Table`, `TableHead`, `Row`, `TableBody`, `TableFoot`, and `Cell` nodes.
- `test/html-reader.html` full-document head, intro, Headers, and Paragraphs
  slice inspected in this run: upstream lines 1-35 cover title/generator
  metadata, the title heading class, intro paragraph, early horizontal rules,
  generated heading identifiers, inline links/emphasis in headings, paragraphs
  immediately following headings with no blank line, a hard-wrapped paragraph
  whose middle sentence looks list-like, a literal bullet-looking paragraph,
  and a hard line break.
- `test/html-reader.native` full-document head, Headers, and Paragraphs rendered
  AST slice inspected in this run: upstream lines 1-230 show two `Meta` fields,
  ten early `Header` nodes including the title heading with class `title`, six
  early `Para` nodes, two `HorizontalRule` nodes before the hard-line-break
  case, one heading `Link`, two heading `Emph` shapes, and the `LineBreak`
  node in the final paragraph.
- `test/html-reader.html` paragraph and inline-quote slice inspected in this
  run: upstream lines 33-86 cover a paragraph hard line break and two `<q>`
  examples, one with a `cite` attribute and one without.
- `test/html-reader.native` paragraph and inline-quote rendered native AST
  slice inspected in this run: upstream lines 213-228 show `LineBreak` between
  text nodes, and lines 360-405 show two `Quoted DoubleQuote` nodes, including
  one citation-bearing `Span` child with the source URL preserved in native
  key-value attributes.
- `test/html-reader.html` inline style slice inspected in this run: upstream
  lines 323-325 cover one `font-variant: small-caps` span, `<u>` and `<ins>`
  underline inputs, and `<s>`, `<strike>`, and `<del>` strikeout inputs.
- `test/html-reader.native` inline style rendered native AST slice inspected in
  this run: upstream lines 922-958 show one `SmallCaps`, two `Underline`, and
  three `Strikeout` nodes for those HTML inputs.
- `test/html-reader.html` Code Blocks slice inspected in this run: upstream
  lines 88-102 cover two standalone `<pre><code>` blocks, one with blank lines
  and one with literal backslash escapes.
- `test/html-reader.native` Code Blocks rendered native AST slice inspected in
  this run: upstream lines 408-420 show those two inputs as `CodeBlock`
  nodes whose text removes the final closing-tag newline while preserving
  internal blank lines, four-space indentation, and literal `\$`, `\\`, `\>`,
  `\[`, and `\{` escapes.
- `test/html-reader.html` Block Quotes slice inspected in this run: upstream
  lines 36-83 cover eight `<blockquote>` containers, including a simple
  paragraph quote, a quote with `<pre><code>` and an ordered list, two nested
  sibling quotes, a box-style code quote, a list-only quote, and a nested quote
  inside another quote.
- `test/html-reader.native` Block Quotes rendered native AST slice inspected in
  this run: upstream lines 231-355 show eight `BlockQuote` nodes, two
  `CodeBlock` nodes inside quotes, and two `OrderedList` nodes inside quotes.
  The bounded PHP mapping now preserves the same quote/container shape and
  keeps HTML text-node apostrophes as straight HTML-reader text rather than
  applying Markdown smart punctuation inside imported HTML.
- `test/html-reader.html` top-level Lists slice inspected in this run:
  upstream lines 104-198 cover the `Lists` heading through the `List styles`
  cases, including six unordered tight/loose list examples, five ordered
  tight/loose/multiple-paragraph examples, and six empty ordered-list style
  metadata examples.
- `test/html-reader.native` top-level Lists rendered native AST slice
  inspected in this run: upstream lines 421-541 show six top-level
  `BulletList` nodes and 11 top-level `OrderedList` nodes. The six style cases
  map to DefaultStyle, LowerRoman via `type="i"`, LowerRoman via class,
  DefaultStyle for bare `style="lower-roman"`, and LowerRoman via
  `list-style` and `list-style-type` declarations.
- `test/html-reader.html` Nested/Tabs/Fancy list slice inspected in this run:
  upstream lines 199-302 cover the `Nested`, `Tabs and spaces`, and
  `Fancy list markers` sections immediately after the top-level list-style
  examples. The slice includes three HTML headings, nested `ul` levels, ordered
  lists with nested unordered children, paragraph-bearing list items, nested
  decimal/lower-roman/upper-alpha/upper-roman/lower-alpha ordered-list styles,
  and a nested default-style autonumbering shape.
- `test/html-reader.native` Nested/Tabs/Fancy list rendered native AST slice
  inspected in this run: upstream lines 542-764 show three `Header` nodes,
  seven `BulletList` nodes, and 11 `OrderedList` nodes. The bounded PHP mapping
  now preserves Pandoc's tight `Plain` shape for list items whose only block
  child is a nested list, keeps paragraph-bearing HTML list items loose, and
  preserves start/style metadata through the nested ordered-list chain.
- `test/html-reader.html` Definition slice inspected in this run: upstream
  lines 303-311 cover one `<dl>` with two term groups, including consecutive
  `<dt>` aliases (`Cello` and `Violoncello`) before a shared definition body.
- `test/html-reader.native` Definition rendered native AST slice inspected in
  this run: upstream lines 765-790 show one `DefinitionList`, two term groups,
  three definition bodies, and a `LineBreak` between the consecutive
  `Cello`/`Violoncello` terms. The bounded PHP mapping now preserves that term
  grouping and emits WordPress-safe `<dl>` output.
- `test/html-reader.html` initial Inline Markup slice inspected in this run:
  upstream lines 313-317 cover the `Inline Markup` heading, two emphasis nodes,
  two strong nodes, an implicitly closed paragraph with empty `<strong>` and
  `<em>` nodes, and an emphasized link paragraph immediately after it.
- `test/html-reader.native` initial Inline Markup rendered native AST slice
  inspected in this run: upstream lines 792-846 show the `inline-markup`
  header, two `Emph` nodes, two non-empty `Strong` nodes, empty `Strong []` and
  `Emph []` nodes, and an `Emph [ Link ... ]` shape. The bounded PHP mapping
  now preserves those nodes and handles the implicit paragraph close without
  swallowing the following emphasized-link paragraph.
- `test/html-reader.html` remaining Inline Markup nested/code slice inspected
  in this run: upstream lines 318-322 cover four nested
  `<strong><em>...</em></strong>` paragraphs plus one paragraph with five
  `<code>` spans containing `>`, `$`, `\`, `\$`, and `<html>`.
- `test/html-reader.native` remaining Inline Markup rendered native AST slice
  inspected in this run: upstream lines 847-921 show four nested
  `Strong [ Emph ... ]` paragraph shapes and five `Code` inline nodes. The
  bounded PHP mapping now preserves the nested strong/emphasis shape and code
  span literal text through WordPress output.
- `test/html-reader.html` Smart quotes, ellipses, dashes slice inspected in
  this run: upstream lines 326-336 cover two bare self-closing `<hr />`
  separators, the section heading, four straight quote/apostrophe paragraphs,
  one quoted HTML `<code>`/`<a>` paragraph, two dash/hyphen paragraphs, and one
  spaced ellipsis paragraph.
- `test/html-reader.native` Smart quotes, ellipses, dashes rendered native AST
  slice inspected in this run: upstream lines 961-1118 show two
  `HorizontalRule` nodes, one `Header`, and eight `Para` nodes. Unlike
  Pandoc's Markdown reader smart-punctuation section, the HTML reader keeps
  straight quotes, apostrophes, dash strings, numeric hyphen ranges, and
  ellipsis dots as literal `Str` text while preserving the quoted code/link
  span boundaries.
- `test/html-reader.html` LaTeX slice inspected in this run: upstream lines
  337-357 cover the `LaTeX` heading, nine TeX/math-looking list items, a
  "These shouldn't be math" paragraph, three not-math list items with
  `<code>` and `<em>` children, a LaTeX table-introduction paragraph, a
  one-line `\begin{tabular}` paragraph, and a self-closing `<hr />` separator.
- `test/html-reader.native` LaTeX rendered native AST slice inspected in this
  run: upstream lines 1119-1297 show the section as literal `Str` text, `Code`
  for the explicit HTML code spans, `Emph` for the explicit HTML emphasis
  spans, and a final `HorizontalRule`. Unlike Pandoc's Markdown reader LaTeX
  section, the HTML reader does not produce `Math` or TeX `RawInline` nodes for
  dollar-delimited or backslash-command source text.
- `test/html-reader.html` Special Characters slice inspected in this run:
  upstream lines 358-388 cover the `Special Characters` heading, one intro
  paragraph, five Unicode list items, five entity/comparison paragraphs,
  sixteen punctuation-token paragraphs, and a self-closing `<hr />` separator.
- `test/html-reader.native` Special Characters rendered native AST slice
  inspected in this run: upstream lines 1298-1385 show one `Header`, one
  `BulletList` with five `Plain` list items, 22 `Para` nodes, and one
  `HorizontalRule`. Unlike the Markdown-reader Special Characters section, the
  HTML reader gets already-decoded text from the HTML parser and does not treat
  `*`, `_`, `[`, `]`, `#`, or other punctuation tokens as Markdown syntax.
- `test/html-reader.html` Links slice inspected in this run: upstream lines
  389-430 cover the `Links` heading, explicit link paragraphs with href/title
  metadata, an empty href, reference-shaped link text that is already HTML,
  ampersand-bearing href/title/text cases, explicit autolink-looking anchors,
  link-looking code spans and code blocks, and mixed HTML flow lines where bare
  text is immediately followed by `<p>` or `<blockquote>`.
- `test/html-reader.native` Links rendered native AST slice inspected in this
  run: upstream lines 1386-1687 show four headers, 24 `Link` nodes, two
  link-free e-mail-text paragraphs, two code contexts where
  `<http://example.com/>` stays literal, one `BlockQuote`, one `BulletList`,
  and the closing `HorizontalRule`. The bounded PHP mapping now keeps the same
  HTML-reader path without invoking Markdown reference or autolink parsing.
- `test/html-reader.html` Images slice inspected in this run: upstream lines
  431-435 cover the `Images` heading, a source-credit paragraph, one
  standalone `<img>` paragraph with `src`, `title`, and `alt`, one inline
  `<img>` paragraph with `src` and `alt`, and a self-closing `<hr />`
  separator.
- `test/html-reader.native` Images rendered native AST slice inspected in this
  run: upstream lines 1688-1728 show one `Header`, two `Para` nodes with
  ordinary text, two `Image` nodes, and one closing `HorizontalRule`. The
  bounded PHP mapping keeps HTML `<img>` nodes on the HTML-reader path as
  image inline AST nodes instead of re-parsing them through Markdown image
  syntax.
- `test/tables/nordics.html5` fixture inspected in this run: 59 HTML lines
  from the upstream table writer artifacts, including caption inline emphasis,
  four `colgroup` widths, a `thead`, one `tbody`, one `tfoot`, row-header
  cells, `<br>` line breaks, and a superscript unit. The bounded PHP slice maps
  this structured HTML table shape into the native table AST and WordPress
  table output.
- `Tests.Readers.Markdown` definition-list cases: 8, all of which are now
  mapped by focused PHP tests
- `Tests.Readers.Markdown` smart apostrophe-after-math regression: 1 focused
  case, now mapped by a PHP test
- `Tests.Readers.Markdown` smart unclosed double-quote regression: 1 focused
  case, now mapped by a PHP test. `**this should "be bold**` stays a `Strong`
  node while the unmatched opening quote becomes Pandoc's left double quote.
- `Tests.Readers.Markdown` footnote edge cases: 3 focused cases, now mapped by
  PHP tests for whitespace-only indented separator termination, indented
  continuation after a blank line, and recursive references left literal inside
  note bodies
- `Tests.Readers.Markdown` MultiMarkdown sub- and superscripts group: 14
  focused cases, now mapped by PHP tests for regular delimited sub/superscripts,
  short digit scripts terminated by spaces, newlines, EOF, punctuation, and
  emphasis, plus the two no-nesting guards
- `Tests.Readers.Markdown` citation and citation-following-boundary cases: 8
  focused cases, now mapped by PHP tests for simple bare citation ids,
  digit-leading ids, citation followed by a footnote, inline link, reference
  link, shortcut reference link, implicit header link, and regular citation
  suffix text
- `Tests.Readers.Markdown` entities group: 3 focused cases, now mapped by PHP
  tests for named character references, decimal and hexadecimal numeric
  references, and entity decoding inside link titles
- `Tests.Readers.Markdown` inline-code attribute cases: 2 focused cases, now
  mapped by PHP tests for immediate attribute attachment and spaced
  attribute-looking text remaining literal
- `Tests.Readers.Markdown` autolink attribute cases: 2 focused cases, now
  mapped by PHP tests for immediate link attribute attachment and spaced
  attribute-looking text remaining literal
- `Tests.Readers.Markdown` bare URI autolink extension cases: all 41 upstream
  `bareLinkTests` cases now mapped by PHP tests, including raw HTML anchor
  pass-through, Greek and long encoded URLs, port/tilde/%20 variants, at-sign
  paths, DOI/Git/file/mailto schemes, and punctuation boundaries
- `Tests.Readers.Markdown` no-links-inside-link-label cases: 3 focused cases,
  now mapped by PHP tests for autolinks, inline links, and bare URI-looking
  text staying literal inside ordinary link labels
- `Tests.Readers.Markdown` raw HTML regression cases: 4 focused cases, now
  mapped by PHP tests for block-start `<del>test</del>` becoming raw-open,
  plain-content, raw-close blocks, invalid tags remaining literal paragraph
  text, technically invalid comments staying raw HTML, and the
  GitHub-flavored split `<`/`a>` case remaining two paragraphs
- `Tests.Readers.Markdown` raw email address cases: 1 focused GitHub-flavored
  Markdown case, now mapped by a PHP test that keeps `**@user**` as strong text
  rather than treating `@user` as link syntax
- `Tests.Readers.Markdown` emoji extension cases: 1 focused GitHub-flavored
  Markdown case, now mapped by a PHP test that converts `:smile:` and `:+1:`
  into emoji `Span` nodes with `class="emoji"` and `data-emoji` metadata
- Focused `# Lists` fancy-marker mappings from `test/testsuite.txt`: 4 local
  checks covering parenthesized decimal starts, lower/upper roman numerals,
  upper/lower alphabetic markers, and Pandoc autonumbering
- Markdown fixture files under `test/`: 1,096
- Office/archive fixtures (`docx`, `odt`, `epub`, `pptx`, `xlsx`, `rtf`): 309
- HTML/XML/JATS fixtures: 29
- `pandoc-lua-engine/test/**/*.hs` modules: 5
- `pandoc-lua-engine/test/` artifacts: 54
- `benchmark/` files: 1
- `data/` files: 247

The lane denominator is now 2,276 inspected upstream test/data/benchmark
files/artifacts: 1,974 under `test/`, all 54 tracked artifacts under
`pandoc-lua-engine/test/`, 247 files under `data/`, and one benchmark file
under `benchmark/`. This replaces the earlier 2,028 count that only included
the main test tree plus Lua-engine test artifacts.

## Runner Blocker

The full upstream suite was not executed in this run. Pandoc's `test-pandoc` and
`test-pandoc-lua-engine` suites must be built as Haskell Tasty executables from
a full checkout before they can run command, golden, HUnit, QuickCheck, and Lua
tests. `ghc` 9.10.3 and `cabal` 3.12.1.0 are now on PATH, while `stack` is not.
The current upstream cache is blob-filtered/no-checkout with mass working-tree
deletions, and a Cabal run would require hydrating the broad checkout plus
downloading and building Pandoc's dependency graph. The defensible denominator
used for this lane is therefore the cloned static `git ls-tree` inventory plus
targeted `git show` reads from the upstream object database, not upstream
runner parity.

## Native PHP Mapping Added

The current PHP slice maps a narrow part of `Tests.Readers.Markdown` semantics:

- ATX and setext headings, including all eight adjacent
  `Tests.Readers.Markdown` Header and Implicit header references cases:
  blank-leading ATX headings, bracketed heading text, closing ATX `#`
  normalization, setext headings, and implicit header references whose labels
  trim surrounding spaces. The existing `test/markdown-reader-more.txt`
  implicit header reference slice remains covered too: generated identifiers,
  duplicate generated-id suffixes, shortcut/collapsed/case-insensitive
  implicit links, explicit heading attributes, and explicit reference
  definitions overriding implicit heading targets.
- Paragraph joining, including the `test/markdown-reader-more.txt`
  backslash-newline slice where an unescaped trailing backslash before a line
  boundary becomes a `LineBreak` node instead of a soft line wrap.
- Pandoc title-block metadata from the start of
  `test/markdown-reader-more.txt`, cross-checked against
  `test/markdown-reader-more.native`: the leading `%` block is consumed before
  body parsing, a multiline title keeps a `SoftBreak` in metadata inlines,
  semicolon- and line-separated authors become four author entries, the empty
  date field stays absent, and the first body heading remains the first block.
- Bullet and ordered list blocks
- Inline emphasis with `*text*`
- Inline strong with `**text**`
- Inline underscore emphasis/strong from the `# Inline Markup` section of
  `test/testsuite.txt`, cross-checked against `test/testsuite.native`: `_is
  this_` maps to `Emph`, `__is this__` maps to `Strong`, and triple `***`/`___`
  delimiters map to Pandoc's `Strong [Emph [...]]` shape.
- Inline strikeout, superscript, and subscript from the same `# Inline Markup`
  slice: `~~This is *strikeout*.~~` maps to a `Strikeout` node containing
  nested emphasis, `a^bc^d`/`a^*hello*^`/`a^hello\ there^` map to
  superscripts with escaped spaces normalized to non-breaking spaces,
  `H~2~O`/`H~23~O`/`H~many\ of\ them~O` map to subscripts, and the upstream
  unescaped-space examples remain plain text rather than script spans.
- MultiMarkdown short script delimiters from `Tests.Readers.Markdown`: `O~2`
  and `x^2` forms become subscript/superscript nodes when followed by a space,
  newline, EOF, punctuation, or emphasis, while `y~*2*` and `y^*2*` keep the
  marker literal and parse only the following emphasis.
- Citation cases from `Tests.Readers.Markdown`: bare `@item1` and
  `@1657:huyghens` become author-in-text citation nodes, `@cita[^note]` leaves
  the following note reference attached, citation plus inline/reference/
  shortcut/implicit-header links keeps the real link separate, and
  `@cita [foo]` becomes one citation node with suffix text when `[foo]` is not
  otherwise a link.
- Inline code spans, including the `test/markdown-reader-more.txt` cases where
  a trailing backslash is literal inside code, embedded newlines normalize to
  spaces, longer backtick delimiters permit literal backticks, and a blank line
  terminates an otherwise unterminated code span into ordinary paragraphs.
- Inline code attributes from `Tests.Readers.Markdown`: immediate
  `{.javascript}` after a closing backtick run attaches class metadata to the
  Code node, while a space before `{.haskell .special x="7"}` keeps that
  attribute-looking text literal.
- Autolink attributes from `Tests.Readers.Markdown`: immediate
  `{#i .j .z k=v}` after `<http://foo.bar>` attaches id/class/key metadata to
  the Link node and replaces the default `uri` class, while a space before the
  attribute spec keeps it as literal text after the autolink.
- Bare URI autolinks from `Tests.Readers.Markdown` with the
  `Ext_autolink_bare_uris` extension: leading http(s) source URLs become
  `uri` links, trailing sentence punctuation stays outside the link, balanced
  parentheses remain part of the destination, uppercase schemes are accepted,
  and square brackets in bare paths are percent-encoded for the link
  destination while remaining visible in the label text.
- Link-label recursion boundaries from `Tests.Readers.Markdown`: autolinks,
  nested inline links, and bare URI-looking text inside an ordinary link label
  remain literal label text, while non-link inline markup such as emphasis still
  parses inside the label.
- Raw HTML regression boundaries from `Tests.Readers.Markdown`: a single-line
  `<del>test</del>` at block start becomes a raw HTML opening block, a plain
  content block, and a raw HTML closing block; malformed tags such as
  `</ div></.div>` stay paragraph text; invalid comments such as
  `<!-- pandoc --help -->` stay raw comment blocks; and GitHub-flavored split
  angle-bracket input remains separate literal paragraphs.
- Multilingual URI/e-mail links from `test/markdown-reader-more.txt`: Unicode
  URI autolinks keep the URL as both text and destination, inline links keep
  Unicode destination text plus title metadata, and Unicode e-mail autolinks
  become `mailto:` links.
- Numbered examples from `test/markdown-reader-more.txt`: `(@)` and
  `(@label)` markers become Pandoc Example-style ordered lists with
  two-parentheses delimiters, and inline `(@label)` references render as the
  visible example numbers.
- Indented code at the beginning of list items from
  `test/markdown-reader-more.txt`: list marker padding of five spaces starts a
  `code_block` child for bullet, decimal ordered, long-decimal ordered, and
  nested bullet items, while the four-space `-    no code` guard stays ordinary
  list-item prose. This matches Pandoc's native shape for the bounded fixture
  without changing ordinary list continuation text.
- Inline links with `[label](url)`
- Indented fenced code blocks from `test/command/indented-fences.md`, including
  Pandoc's opening-fence indentation stripping and both bare language and
  `{.class}` info strings
- Indented code blocks from the `# Code Blocks` section of `test/testsuite.txt`,
  cross-checked against `test/testsuite.native`: blank lines stay inside the
  block, a one-tab indent starts a block with no remaining indent, two leading
  tabs leave one expanded four-space indent in the code text, and literal
  backslashes are preserved rather than unescaped.
- Block quote cases from the `# Block Quotes` section of `test/testsuite.txt`,
  cross-checked against `test/testsuite.native`: simple quoted paragraphs,
  quote-contained indented code, ordered lists, nested block quotes, and the
  lazy-continuation case where `> 1.` stays inside a paragraph instead of
  starting a quote.
- Horizontal rules from the `# Code Blocks` and `# Lists` sections of
  `test/testsuite.txt`, cross-checked against `test/testsuite.native`: the
  underscore divider before the Lists section and the indented spaced-asterisk
  divider after `B. Williams` both become `HorizontalRule` nodes instead of
  paragraphs or bullet lists.
- Tight/loose list item shape and continuation paragraphs from the
  `# Lists` section of `test/testsuite.txt`, cross-checked against
  `test/testsuite.native`: blank lines between list items mark the list loose
  and turn item text into paragraph blocks, tab/space-indented continuation
  lines remain inside the current list item, multi-paragraph ordered items keep
  both paragraphs under the same item, and loose nested lists keep the parent
  item paragraph before the nested `BulletList`.
- Fancy ordered-list markers from the same `# Lists` section: `(2)` and `(3)`
  produce a decimal `OrderedList` starting at 2 with a two-parentheses
  delimiter, `iv.`/`v.` produce a lower-roman nested list starting at 4,
  `(A)`/`(B)` produce an upper-alpha nested list, `A.`/`I.`/`(6)`/`c)` produce
  the nested upper-alpha, upper-roman, decimal, and lower-alpha shape shown in
  `test/testsuite.native`, and `#.` produces Pandoc-style autonumbered lists.
- HTML task-list examples from `test/command/tasklist.md`: leading `[ ]` and
  `[x]` markers at the start of list items are stripped from the first
  paragraph and stored as `taskChecked` metadata, bullet lists whose items are
  all tasks receive `taskList` metadata, mixed task/plain lists intentionally do
  not receive the task-list class, ordered task items still render checkbox
  labels, and loose task items wrap only the first paragraph in the checkbox
  label while preserving later paragraphs as ordinary list content.
- Markdown and LaTeX writer examples from the same `test/command/tasklist.md`
  fixture are now mapped too: native Markdown output round-trips unchecked and
  checked task markers as `- [ ]` and `- [x]`, while native LaTeX output uses
  Pandoc's task labels `\item[$\square$]` and `\item[$\boxtimes$]` for loose
  task-list items.
- Markdown writer fancy ordered-list marker generation is now mapped from
  `Text.Pandoc.Writers.Markdown` and `Text.Pandoc.Shared`: native Markdown
  output emits `(2)`/`(3)`, `iv.`/`v.`, `A.`/`I.`, `(6)`, `c)`, default
  autonumbered decimal markers, and Pandoc-style short-marker padding for
  reviewer handoff lists.
- `test/Tests/Writers/Markdown.hs` inspected for its bounded note/reference
  location group: 20 HUnit cases are present in the module, including four
  note/reference-location cases. The PHP slice maps those four cases for
  `EndOfDocument`, `EndOfBlock`, `EndOfBlock` plus shortcut reference links,
  and `EndOfSection`, including setext headings, block quote prefixing,
  footnote definition placement, and the indented shortcut reference definition
  shape used by Pandoc's Markdown writer.
- Definition-list cases from `Tests.Readers.Markdown`: no blank space,
  blank space before the first definition, lazy continuation lines, indented
  continuation paragraphs, blank space before the second definition, first-line
  marker at column zero, a list inside a definition, and the definition list
  nested inside an HTML div.
- Definition-list cases from the `# Definition Lists` section of
  `test/testsuite.txt`, cross-checked against `test/testsuite.native`: terms can
  contain emphasis, indented continuation blocks can add additional paragraphs,
  eight-space-indented continuation lines become code blocks, continuation
  block quotes remain block quotes inside the definition body, alternate `~`
  markers are accepted after a blank term line, and an indented ordered list
  stays nested inside the `orange` definition body.
- HTML-block cases from the `# HTML Blocks` section of `test/testsuite.txt`,
  cross-checked against `test/testsuite.native`: one-line and nested `<div>`
  containers become `div` AST nodes, raw `<table>` blocks preserve their HTML
  boundary while interpreting Markdown in cell text, `<script>` bodies are kept
  raw without interpreting Markdown, HTML comments become raw HTML blocks with
  tabs expanded as Pandoc does, trailing spaces are trimmed from raw comments and
  `<hr>` tags, and tab-indented HTML remains an indented code block.
- The two-level nested HTML table case from
  `test/command/nested-table-to-asciidoc-6942.md` is now represented for the
  WordPress table boundary: balanced nested `<table>` blocks are parsed into
  native table AST nodes inside `table_cell` children, while simple non-nested
  raw HTML tables continue to use the existing raw HTML block path.
- The same upstream command fixture's full HTML document with a third-level
  nested table is now represented too. Pandoc's AsciiDoc writer warns and
  flattens the third level because that target format only supports two table
  levels; the PHP WordPress writer records a separate target policy and
  preserves the third-level nested table HTML for reviewer inspection.
- Structured HTML table imports from `test/tables/nordics.html5` are now
  represented for the WordPress table boundary: tables with explicit
  `caption`, `colgroup`, `thead`, or `tfoot` parse into table AST nodes,
  caption inline emphasis is preserved, col widths become `ColWidth`-style
  fractions, table head/body/foot sections remain distinct, row-header cells
  stay marked as headers in the AST, `<br>` becomes a hard `linebreak`, and
  `<sup>`/`<sub>` inline content maps to script nodes. Simple non-structured
  raw HTML tables still use the raw HTML path so legacy import-review markup
  is not over-normalized.
- Bounded non-table HTML-reader paragraph cases from `test/html-reader.html`
  and `test/html-reader.native` are now represented: standalone HTML
  paragraphs parse through the native inline path, `<br />` becomes a
  `linebreak` node matching Pandoc's `LineBreak`, `<q>` becomes a double
  `quoted` node, and q `cite` metadata is preserved as a Pandoc-style `span`
  child. The WordPress writer emits `<br/>` for the hard break and preserves
  citation metadata on the rendered inline span.
- Bounded HTML-reader table cases from `test/html-reader.html` and
  `test/html-reader.native` are now represented: a first all-`th` row without
  explicit `<thead>`/`<tbody>` tags is inferred as `table_head`, bodies whose
  rows begin with `<th>` cells record `rowHeadColumns=1`, omitted `</thead>`,
  `</tbody>`, and `</tfoot>` end tags are normalized into distinct
  head/body/foot AST sections, no-header HTML tables with only `colspan`
  metadata parse through the native table AST, headed tables preserve
  `colspan`/`rowspan`, Pandoc-style table, section, row, and cell attributes
  are captured with `data-*` keys normalized to native-style key-value
  attributes, the two upstream multiple-`tbody` tables stay as distinct
  `table_body` AST nodes instead of being flattened, and the second
  multiple-body table's direct `<p>` cell becomes a paragraph block child
  rather than inline text. Four body-local `TableBody` head-row cases now keep
  leading all-`th` rows in `headRows` metadata before ordinary body rows,
  covering explicit tbody plus foot/details, omitted tbody after a promoted
  top-level header, explicit tbody-only body heads, and empty-thead body heads.
  The plain `Tables without Headers` body-only, tbody-omitted, empty-head, and
  explicit body-plus-foot shapes now parse as native table AST nodes too when
  the cells are plain scalar text. The WordPress writer now emits body
  row-header cells as `<th>` instead of flattening them to `<td>`, renders
  body-local head rows inside `<tbody>` before ordinary body rows, preserves
  table identity attributes, emits section and row attrs on `<thead>`, `<tbody>`,
  `<tfoot>`, and `<tr>`, carries practical cell attributes such as `abbr`,
  `valign`, `data-*`, and non-alignment `style` values, emits one `<tbody>` per
  `table_body` node, preserves paragraph cells as `<td><p>...</p></td>`, and
  emits headerless plain import grids as core table blocks. The upstream
  empty-table section is
  mapped too: the empty `<tbody>` table and the fully empty `<table></table>`
  input are consumed and omitted, matching `test/html-reader.native`.
- Smart-punctuation cases from the `# Smart quotes, ellipses, dashes` section
  of `test/testsuite.txt`, cross-checked against `test/testsuite.native`: nested
  single and double quotes become `quoted` AST nodes, apostrophes inside words
  normalize to Pandoc's right single quotation mark, quoted code spans remain
  code, quoted one-line reference links resolve through collected definitions,
  `---` becomes an em dash, numeric `--` ranges become en dashes, and `...`
  becomes an ellipsis while preserving a fourth trailing dot.
- LaTeX cases from the `# LaTeX` section of `test/testsuite.txt`,
  cross-checked against `test/testsuite.native`: raw TeX citation commands
  become `raw_tex` inline nodes, `$...$` spans become inline `math` nodes,
  `$$...$$` spans become display `math` nodes, `$p$-Tree` keeps the trailing
  word text outside math, currency-like dollar examples and escaped dollars stay
  non-math text, and `\begin{tabular}` through the matching `\end{tabular}`
  becomes a raw TeX block.
- The `Tests.Readers.Markdown` apostrophe-after-math regression is mapped:
  `$x$'s` parses as inline math followed by a right apostrophe text node, and
  the trailing possessive apostrophe in `systems' condition` normalizes to
  Pandoc's right single quotation mark.
- The `Tests.Readers.Markdown` unclosed double-quote smart-punctuation
  regression is mapped too: `**this should "be bold**` remains a `Strong` node
  and the unmatched opening quote is normalized to a left double quote instead
  of staying straight source text.
- Special Characters cases from `test/testsuite.txt`, cross-checked against
  `test/testsuite.native`: Unicode list item text stays literal, `AT&amp;T`
  decodes to `AT&T` in the inline text node, literal `&`, `<`, and `>` examples
  stay text rather than HTML, Pandoc's punctuation backslash escapes collapse to
  their literal characters, and the dashed divider remains a `HorizontalRule`.
- Links cases from `test/testsuite.txt`, cross-checked against
  `test/testsuite.native`: explicit links support empty destinations,
  double/single-quoted titles, quote-containing titles, backslash escapes in
  link text, mailto URLs, and pointy-brace destinations; reference links support
  full, collapsed, and shortcut forms, nested brackets in link text, and
  definitions indented by up to three spaces while a four-space definition
  remains an indented code block; ampersands remain intact in URLs, link text,
  and titles; URI and email autolinks work in paragraphs, lists, and block
  quotes; autolinks do not fire inside code spans or indented code blocks.
- Images cases from `test/testsuite.txt`, cross-checked against
  `test/testsuite.native`: `![lalune][]` resolves through an up-to-three-space
  indented reference definition into a standalone `figure` block with image alt,
  source, title, and caption metadata, while `![movie](movie.jpg)` remains an
  inline `image` node inside its paragraph.
- Footnotes cases from `test/testsuite.txt`, cross-checked against
  `test/testsuite.native`: `[^1]` and `[^longnote]` resolve through collected
  footnote definitions into inline `note` nodes, invalid labels containing
  spaces remain literal text, inline notes parse nested emphasis, links, code
  spans containing `]`, and bracketed plain text, notes inside block quotes and
  list items stay attached to their containing block, and multi-block
  definitions preserve paragraphs plus indented code. The three
  `Tests.Readers.Markdown` footnote edge cases are also mapped: whitespace-only
  indented separators terminate a note before flush-left text, indented text
  after a separator remains in the note, and recursive `[^1]` references inside
  their own note body remain literal text.
- All 11 pipe-table cases from `test/pipe-tables.txt`, cross-checked against
  `test/pipe-tables.native`: default and aligned caption/no-caption tables keep
  caption text plus right/left/default/center alignment metadata, headerless
  tables omit the table head, the one-dash `|:-:|` header-less one-column form
  parses as centered, side-less rows split correctly without leading or
  trailing pipes, indented left-column values trim to their intended cells,
  one-column and no-body tables preserve their header/body shape, long
  delimiter rows produce relative column-width metadata, and tricky cells keep
  escaped `\|` pipes as text while `foo` plus a code span containing `bar|baz`
  remains one cell.
- All seven gridless table cases from `test/tables.markdown`, cross-checked
  against `test/tables.native`: captioned and uncaptioned simple tables infer
  right/left/center/default alignment from header spacing, the
  two-space-indented simple-table fixture still parses as a table rather than an
  indented code block, no-column-header simple tables use opening and closing
  delimiter rows with alignment inferred from the first body row, multiline
  header/body rows merge wrapped physical lines into `softbreak`-bearing cell
  content, multiline tables preserve Pandoc's 80-column `ColWidth` fractions,
  multiline captions can span continuation lines, and the no-header multiline
  table preserves the upstream final-column `AlignDefault` distinction while
  the headed multiline examples infer `AlignLeft`.
- Parsed table caption inline content is now mapped for pipe and simple tables:
  the AST keeps the legacy plain caption string but also stores parsed caption
  inline nodes, matching Pandoc's native `Caption ... [Plain [...]]` block
  shape observed in `test/tables.native`, `test/pipe-tables.native`, and the
  short-caption command fixture. WordPress figcaptions now render emphasis,
  links with titles, code spans, and smart punctuation instead of escaping
  Markdown markup as literal caption text.
- The optional short-caption shape from `test/command/short-caption.md` is now
  mapped for a narrow LaTeX table environment slice: `\caption[short
  caption]{long caption}` keeps the long caption as visible table caption
  content and stores the short caption separately on the table AST. The
  WordPress writer preserves that short label as `data-pandoc-short-caption`
  on the table figure while rendering the long caption in the figcaption.
- The DocBook structural-cell shapes from `test/command/table-with-cell-align.md`
  and `test/command/table-with-column-span.md` are now mapped for a narrow
  `informaltable` slice: the native PHP reader uses DOM parsing for bounded
  DocBook table fragments, keeps `colspec` widths, per-cell alignment,
  `namest`/`nameend` column spans, and strong emphasis inside cells. The
  WordPress writer emits core table HTML with escaped `style` and `colspan`
  attributes, so structural cells survive import without shelling out to Pandoc.
- The row-span table-section shape from
  `test/command/rst-writer-gridtable-if-rowspans.md` is now mapped through the
  same bounded table AST: DocBook `morerows="1"` becomes `rowspan=2`,
  `thead`/`tbody`/`tfoot` become `table_head`/`table_body`/`table_foot`, and
  the WordPress writer emits `<thead>`, `<tbody>`, `<tfoot>`, and `rowspan`
  attributes without shelling out to Pandoc.
- The inline style shape from `test/html-reader.html` is now mapped for a
  narrow HTML reader slice: `font-variant: small-caps` spans become
  `small_caps` AST nodes, `<u>` and `<ins>` become `underline`, and `<s>`,
  `<strike>`, and `<del>` become `strikeout`. The WordPress writer renders
  those as safe inline small-caps, underline, and deletion markup without
  invoking Pandoc.
- The standalone pre/code shape from the `test/html-reader.html` Code Blocks
  section is now mapped for a narrow HTML reader slice: `<pre><code>` becomes a
  native `code_block`, internal blank lines and indentation are preserved, the
  closing-tag newline is stripped like Pandoc's native output, and literal
  backslash escapes stay literal instead of being treated as Markdown escapes.
  WordPress output renders the node as a core code block and normalizes
  `language-*` classes for imported legacy snippets.
- The blockquote container shape from the `test/html-reader.html` Block Quotes
  section is now mapped for a narrow HTML reader slice: balanced
  `<blockquote>` blocks become native `blockquote` nodes, nested quotes stay
  nested, paragraph/code/list children stay as block children, ordered lists
  inside quotes stay on the native list path, and HTML text nodes are not
  passed through Markdown smart punctuation.
- The top-level list shape from the `test/html-reader.html` Lists section is
  now mapped for a narrow HTML reader slice: balanced `<ul>` and `<ol>` blocks
  become native list nodes, tight `<li>text</li>` items stay inline/plain-like,
  `<li><p>text</p></li>` items stay paragraph-like, multiple paragraphs remain
  attached to one ordered item, and `type`, class, `list-style`, and
  `list-style-type` metadata preserve ordered-list styles while the upstream
  bare `style="lower-roman"` case remains default. The WordPress writer emits
  safe `type` attributes for roman/alpha ordered lists.
- The next HTML-reader list shape from `test/html-reader.html` is now mapped
  for the `Nested`, `Tabs and spaces`, and `Fancy list markers` sections:
  top-level HTML headings become native heading nodes with generated or
  preserved Pandoc-style identifiers, nested-list-only items remain tight
  `Plain`-shaped list items, paragraph-wrapped items remain paragraph-shaped,
  and decimal/lower-roman/upper-alpha/upper-roman/lower-alpha ordered-list
  styles and starts survive through nested list chains. The WordPress writer
  emits heading anchors and nested ordered-list `start`/`type` attributes
  without invoking Pandoc.
- The HTML-reader definition-list shape from `test/html-reader.html` is now
  mapped for the `Definition` section: balanced `<dl>` blocks become native
  `definition_list` nodes, consecutive `<dt>` terms are joined with a
  Pandoc-style `linebreak`, multiple `<dd>` bodies stay attached to the same
  term, and the WordPress writer emits glossary/FAQ `<dl>` markup without
  invoking Pandoc.
- The HTML-reader Smart quotes, ellipses, dashes shape from
  `test/html-reader.html` is now mapped for a narrow HTML reader slice: bare
  self-closing `<hr />` separators become `horizontal_rule` nodes, the section
  heading gets the Pandoc-style identifier, straight source quotes and
  apostrophes remain literal text, quoted HTML code/link boundaries stay
  semantic, and dash/ellipsis strings are not converted through Markdown smart
  punctuation.
- The HTML-reader LaTeX shape from `test/html-reader.html` is now mapped for a
  narrow HTML reader slice: TeX commands, dollar-delimited math-looking text,
  and `\begin{tabular}` source in HTML text nodes stay literal text, while only
  explicit HTML inline tags such as `<code>` and `<em>` become semantic inline
  nodes. WordPress output preserves the source text without creating math spans
  or raw-TeX spans on the HTML-reader path.
- The HTML-reader Special Characters shape from `test/html-reader.html` is now
  mapped for a narrow HTML reader slice: Unicode list text survives unchanged,
  HTML entities decode once, comparison characters stay ordinary text,
  Markdown-sensitive punctuation tokens remain literal, and the final
  self-closing `<hr />` remains a `horizontal_rule` node.
- The HTML-reader Links shape from `test/html-reader.html` is now mapped for a
  narrow HTML reader slice: explicit anchors preserve href/title metadata,
  empty hrefs stay empty, reference-looking text stays literal, code contexts
  do not autolink, and mixed bare-text-plus-block-tag flow is split before
  block parsing so it stays on the native HTML-reader path.
- The HTML-reader Images shape from `test/html-reader.html` is now mapped for a
  narrow HTML reader slice: `<img>` becomes an `image` inline node with
  `src`/`title`/`alt` metadata, standalone image-only paragraphs retain
  Pandoc's paragraph-image AST shape, and inline image paragraphs keep the
  image between ordinary text nodes.
- The HTML-reader Footnotes shape from `test/html-reader.html` is now mapped
  for a narrow HTML reader slice: the 12-line upstream section and 249-line
  native AST slice preserve four footnote-style anchors as ordinary `Link`
  nodes, keep the invalid space-containing footnote marker as literal text,
  leave footnote-body paragraphs and the pre/code continuation as normal
  paragraph/code blocks, and move leading/trailing whitespace around emphasis
  wrappers outside the emphasis node like Pandoc's native output.
- The early full-document HTML-reader shape from `test/html-reader.html` is now
  mapped for a narrow HTML reader slice: complete `<html>` documents preserve
  title and generator metadata on the document node, body blocks are parsed
  through the native HTML-reader path, source heading classes survive on
  generated heading ids, heading links/emphasis remain inline nodes, and
  HTML-reader paragraphs keep list-marker-looking text literal instead of being
  re-parsed as Markdown lists.
- The post-grid Markdown reader shape from `test/markdown-reader-more.txt` is
  now mapped for a narrow link slice: upstream lines 315-335 and native AST
  lines 1493-1548 cover four entity-decoded link/title cases plus three
  parenthesized URL cases. Inline/reference link destinations decode
  `&uuml;`, titles decode `&ouml;`, URI/e-mail autolinks decode both href and
  visible label text, balanced parentheses stay inside inline URLs, escaped
  closing parentheses survive, and nested parenthesized reference destinations
  remain intact.
- The next post-grid Markdown reader shape from `test/markdown-reader-more.txt`
  is now mapped for a narrow reference-link edge slice: upstream lines 337-358
  and native AST lines 1549-1649 cover backslash/TeX content in link labels,
  unresolved reference-link fallback text, a shortcut reference followed by a
  citation marker, and empty reference definitions. The native PHP reader keeps
  escaped label punctuation and a bare `\a` TeX command inside the link label,
  falls back to bracketed emphasized text when the reference is undefined,
  preserves the `[@mapreduce]` marker as a citation inline adjacent to the
  resolved `Google` link, and leaves the paragraph after `[foo2]:` intact
  before emitting the later empty-destination shortcut link.
- The final `test/markdown-reader-more.txt` tail slice is now mapped too:
  upstream lines 360-366 and native AST lines 1650-1715 cover the wrapping
  regression and bracketed-span extension. The native PHP reader now generates
  Pandoc's apostrophe-free heading id for `shouldn't`, keeps the long bullet
  item as one tight list item instead of treating `2015.` as an ordered marker,
  and builds a `span` AST node preserving id, class, and key/value attributes
  around parsed emphasis and link children. The WordPress writer emits safe
  span id/class/data/title attrs for the fixture's migration-review marker.
- The mid-fixture `test/markdown-reader-more.txt` reference/quote/list slice is
  now mapped too: reference labels normalize case for shortcut lookup, curly
  quote code points stay literal text rather than becoming Markdown smart quote
  nodes, and a one-space-indented lower-alpha list after a decimal list is kept
  as a sibling list. The nested-list guard still keeps column-zero initials such
  as `B. Williams` as paragraphs and preserves existing two-column nested list
  behavior from the indented-code-at-beginning-of-list slice.
- The `Tests.Readers.Markdown` inline-code attribute slice is now mapped too:
  immediate inline code attributes become AST id/class/key-value metadata, and
  the spaced attribute-looking form stays literal text instead of being parsed
  or smart-quoted. The WordPress writer emits safe inline `<code>` attributes
  for reviewer/source tokens.
- The `Tests.Readers.Markdown` autolink attribute slice is now mapped too:
  immediate autolink attributes become AST id/class/key-value metadata on the
  Link node, and the spaced attribute-looking form stays literal text. The
  WordPress writer emits safe link id/class/data/title attrs for reviewer
  source links while keeping ordinary URI/e-mail autolinks visually unchanged.
- The `Tests.Readers.Markdown` bare URI autolink extension slice is now mapped
  against all 41 upstream `bareLinkTests` cases: leading http(s) URLs, raw HTML
  anchor pass-through without nested autolinking, query URLs followed by
  sentence punctuation, parenthesized URLs, uppercase schemes, Greek URLs,
  balanced parenthesized paths, bracketed and braced destinations with safe
  percent-encoding, `doi:`, `git://`, `file://`, and `mailto:` source URIs, the
  `Use http:` non-link guard, long encoded HTTP URLs, port/tilde/%20 variants,
  at-sign archive paths, semicolon/query/fragment/plus URL shapes, repeated
  plain HTTP inputs, and both trailing-hyphen forms.
- The `Tests.Readers.Markdown` no-links-inside-link-label slice is now mapped
  too: `[<https://example.org>](url)`, `[[a](url2)](url)`, and
  `[https://example.org(](url)` each produce one outer Link whose label content
  stays literal text. The helper used for link and image labels keeps recursive
  link parsing disabled while preserving non-link inline markup such as
  emphasis.
- The adjacent `Tests.Readers.Markdown` raw email, emoji, and GitHub wiki-link
  extension slice is now mapped too: `**@user**` stays a `Strong` node with
  literal `@user` text, GitHub-flavored `:smile: and :+1:` becomes two emoji
  `Span` nodes with `class="emoji"`, `data-emoji` metadata, and the expected
  glyph text, and the six `Github wiki links` cases become classed Link nodes.
  The mapped wiki cases cover bare URL links, title-before-pipe links,
  non-URL page targets, page names with spaces, page names containing a literal
  `]`, and labels containing backticks/asterisks that stay literal text.
  Unknown emoji aliases remain literal text.
- The adjacent `Tests.Readers.Markdown` MultiMarkdown short sub/superscript
  slice is now mapped too: the 14-case group covers the regular delimited
  `H~2~` and `x^3^` cases, short digit scripts before whitespace/newline/EOF,
  punctuation, and emphasis, and the no-nesting guards where `y~*2*` and
  `y^*2*` remain literal marker text followed by emphasis.
- The adjacent `Tests.Readers.Markdown` citation and
  citation-following-boundary slice is now mapped too: the 8-case group covers
  simple author-in-text ids, digit-leading ids, footnote/link boundaries after
  `@cita`, reference and shortcut reference disambiguation, implicit header
  links, and the regular citation suffix case. Bare citation parsing is
  deliberately kept out of nested emphasis so GitHub-flavored `**@user**`
  remains strong literal text in the earlier raw-email slice.
- The adjacent `Tests.Readers.Markdown` figures slice is now mapped for the
  `latex placement` case: `![caption](img.jpg){latex-placement="htbp"
  alt="alt text"}` becomes a standalone Figure with `latex-placement` metadata,
  while the image's alt text is overridden to `alt text` and the visible caption
  remains `caption`.
- The adjacent `Tests.Readers.Markdown` emph/strong delimiter slice is now
  mapped for two upstream cases from the `emph and strong` group:
  `*x **xx** x*` and `***a**b **c**d*`. The native PHP reader keeps the outer
  emphasis open across inner strong delimiter runs, yielding `Emph` nodes with
  nested `Strong` children instead of prematurely closing at the first `**`
  run.
- The same upstream `emph and strong` group is now mapped for the alternating
  softbreak case too: `*xxx* ***xxx*** xxx` followed by another physical
  paragraph line keeps the newline as a `SoftBreak` inline node between the two
  emphasized runs. Paragraph `text` attributes remain space-normalized for
  existing callers, while the AST and WordPress output preserve the line
  boundary. The full upstream group has four cases; three are now explicitly
  mapped in this focused slice.
- The adjacent `Tests.Readers.Markdown` smart-punctuation unclosed double quote
  case is now mapped too: the native PHP reader keeps
  `**this should "be bold**` as strong content and converts the unmatched
  opening quote to a left double quote, matching Pandoc's smart reader.
- The same upstream `Tests.Readers.Markdown` smart-punctuation group is now
  fully mapped for its seven named cases across upstream lines 362-383. The
  latest slice covers quote-before-ellipsis (`'...hi'`), apostrophe before
  emphasis (`D'oh! A l'*aide*!`), and the French guillemet case
  (`l'«impossibilité...`). Smart apostrophe boundaries now use Unicode
  letter/number checks, while issue #11613 inline-note quote delimiters inside
  `^[...]` notes still stay inside the note instead of closing the surrounding
  single or double quoted span.
- The adjacent `Tests.Readers.Markdown` list issue #1154 case is now mapped:
  a list item beginning with `<div>` keeps the following div, single-line
  `<button>...</button>` raw HTML container, and second div as block children
  of the same list item. This prevents the native PHP reader from splitting a
  migration review list into a stray paragraph plus top-level HTML blocks.
- The adjacent `Tests.Readers.Markdown` `lhs` extension case is now mapped for
  the bounded bird-track shape: when `MarkdownReader` is constructed with
  `literateHaskell => true`, `> ` lines become Haskell literate code blocks and
  `< ` inverse-bird lines become Haskell code blocks, while the default reader
  still treats `> ` as Markdown block quotes.
- The upstream `test/lhs-test.markdown+lhs` fixture boundary is now mapped too:
  column-zero bird-track lines become `["haskell","literate"]` code blocks,
  the indented ordinary code block remains unclassed code, and the fixture's
  one-space-indented ` > foo bar` line remains a block quote when
  `literateHaskell => true`.
- The adjacent `Tests.Readers.Markdown` unbalanced-bracket and backslash-escape
  cases are now explicitly mapped: a long unmatched bracket run remains literal
  paragraph text, inline-link `\)` becomes a literal `)` inside the URL,
  escaped quotes inside inline titles survive, and escaped punctuation in
  reference-link URLs/titles is unescaped through the same native path. The
  reader now narrows Markdown backslash escapes to Pandoc/CommonMark-style
  ASCII punctuation instead of treating any non-alphanumeric byte as escapable.
- The adjacent `Tests.Readers.Markdown` intraword underscore and raw-LaTeX URL
  guard cases are now explicitly mapped from upstream lines 228-233:
  `_foot_ball_` becomes a single `Emph` node whose text is `foot_ball`, while a
  bare `\begin` line stays paragraph text instead of becoming a raw TeX inline.
  The native reader now names both guard paths directly: intraword underscores
  cannot close or open a delimiter run, and bare LaTeX environment commands
  require an argument before they are treated as raw TeX.
- The adjacent `Tests.Readers.Markdown` entities group is now explicitly mapped
  from upstream lines 515-523: `&lang; &ouml;` decodes to text, decimal and
  lowercase/uppercase hexadecimal numeric references decode to `,DD`, and
  entity references inside link titles are decoded before WordPress escaping.

The WordPress writer emits block comments and escaped HTML for the same AST
without calling the upstream `pandoc` binary.

Focused local verification on 2026-05-23: the pandoc-local test file passed
with 181 behavior tests, 2,043 assertions, and 0 failures after this slice.
Root verification for this batch was started after the required duplicate-root
gate returned clear. `php tools/run-tests.php` exited 1 with 192 test files,
20,864 assertions, and 1 failure in `lanes/quadrable/tests/QuadbStoreTest.php`
(`native quadb store imports and merges proof-backed heads across reopen`;
expected `RuntimeException` was not thrown). Pandoc tests passed inside that
root run.

Focused local verification on 2026-05-23 after the raw-HTML-in-list slice:
`php -l` passed for `MarkdownReader.php`, `WordPressBlockWriter.php`, and
`MarkdownReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,056
assertions, and 0 failures. The first required root verification gate found
active root PID `1534970` owned by `claude` (`php tools/run-tests.php`), so no
duplicate root run was started then. A later exact gate was clear, and
`php tools/run-tests.php` passed 196 test files, 21,368 assertions, and 0
failures.

Focused local verification on 2026-05-23 after the literate-Haskell slice:
`php -l` passed for `MarkdownReader.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-literate-haskell.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,073
assertions, and 0 failures.

Required root verification gate on 2026-05-23 found an active exact root
harness, so this lane did not start a duplicate run: PID `1766434`, owner
`claude`, PPID `1604183`, elapsed `00:41`, command `php tools/run-tests.php`.
Root result remains pending for the supervisor/integrator.

Focused local verification on 2026-05-23 after the lhs-test boundary slice:
`php -l` passed for `MarkdownReader.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-literate-haskell.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,083
assertions, and 0 failures.

Required root verification on 2026-05-23: the duplicate-root gate returned
clear, so `php tools/run-tests.php` was run once and passed 196 test files,
21,585 assertions, and 0 failures.

Focused local verification on 2026-05-23 after the unbalanced-bracket and
backslash-escape slice: `php -l` passed for `MarkdownReader.php` and
`MarkdownReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,097
assertions, and 0 failures.

Required root verification on 2026-05-23 after the same slice: the duplicate
root gate returned clear, so `php tools/run-tests.php` was run once and passed
198 test files, 21,767 assertions, and 0 failures.

Focused local verification on 2026-05-23 after the intraword-underscore and
raw-LaTeX URL guard slice: `php -l` passed for `MarkdownReader.php` and
`MarkdownReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,105
assertions, and 0 failures.

Required root verification on 2026-05-23 after the same slice was left pending:
the duplicate-root gate returned active root harness PID `2089975` owned by
`claude` (`php tools/run-tests.php`, parent `2009714`, elapsed `00:10`, state
`Rs`), so this lane did not start a duplicate root run.

Focused local verification on 2026-05-23 after the entity-reference slice:
`php -l` passed for `MarkdownReader.php` and `MarkdownReaderTest.php`;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
1 test file, 2,114 assertions, and 0 failures.

Required root verification on 2026-05-23 after the entity-reference slice:
the duplicate-root gate returned clear, so `php tools/run-tests.php` was run
once. It exited 1 with 198 test files, 21,846 assertions, and 45 failures.
Pandoc tests passed inside that root run; the failures were outside this lane,
concentrated in `lanes/lightningcss/tests/TransitionPrefixerTest.php` because
`PortLibs\LightningCSS\TransitionPrefixer::rewriteDisplayFlexPrefixEntries()`
is missing, plus one `lanes/syncthing/tests/FileInfoScannerTest.php` scanner
checkpoint condition failure.

Focused local verification on 2026-05-23 after the task-list slice: `php -l`
passed for `MarkdownReader.php`, `WordPressBlockWriter.php`, and
`MarkdownReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,139
assertions, and 0 failures.

Required root verification on 2026-05-23 after the task-list slice was left
pending: the duplicate-root gate returned active root harness PID `2399793`
owned by `claude` (`php tools/run-tests.php`, parent `2264530`, elapsed
`00:19`, state `Rs`), so this lane did not start a duplicate root run.

Focused local verification on 2026-05-23 after the title-block metadata slice:
`php -l` passed for `MarkdownReader.php` and `MarkdownReaderTest.php`;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
1 test file, 2,151 assertions, and 0 failures. The focused file now contains
191 behavior tests.

Required root verification on 2026-05-23 after the title-block metadata slice
was left pending: the duplicate-root gate returned active root harness PID
`2479573` owned by `claude` (`php tools/run-tests.php`, parent `2479572`,
elapsed `00:14`, state `R`), so this lane did not start a duplicate root run.

Focused local verification on 2026-05-23 after the nested-dollar inline math
slice: `php -l` passed for `MarkdownReader.php` and `MarkdownReaderTest.php`;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
1 test file, 2,176 assertions, and 0 failures. The focused file now contains
194 behavior tests.

Required root verification on 2026-05-23 after the nested-dollar inline math
slice was left pending: the duplicate-root gate returned active root harness
PID `2613382` owned by `claude` (`php tools/run-tests.php`, parent `2613380`,
elapsed `00:19`, state `R`), so this lane did not start a duplicate root run.

Focused local verification on 2026-05-23 after the raw-HTML-before-header and
commented-list slice: `php -l` passed for `MarkdownReader.php` and
`MarkdownReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,199
assertions, and 0 failures. The focused file now contains 196 behavior tests.

Required root verification on 2026-05-23 after the raw-HTML-before-header and
commented-list slice: the duplicate-root gate returned clear, so
`php tools/run-tests.php` was run once. It exited 1 with 202 test files,
23,114 assertions, and 2 failures. Pandoc tests passed inside that root run;
the visible failure was outside this lane in
`lanes/readability/tests/ArticleExtractorTest.php`, where the
`firefox-nightly-blog` byline fixture expected `Mike Conley` and got `NULL`.

Focused local verification on 2026-05-23 after the task-list writer slice:
`php -l` passed for `MarkdownReader.php`, `WordPressBlockWriter.php`,
`MarkdownWriter.php`, `LatexWriter.php`, and `MarkdownReaderTest.php`;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed
1 test file, 2,234 assertions, and 0 failures. The focused file now contains
198 behavior tests.

Required root verification on 2026-05-23 after the task-list writer slice: the
duplicate-root gate returned clear before the final root run, and
`php tools/run-tests.php` passed 204 test files, 23,553 assertions, and
0 failures.

Focused local verification on 2026-05-23 after the Markdown writer
note/reference-location slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted the
expected setext-heading handoff Markdown with block-local footnotes and
shortcut reference definitions; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,242
assertions, and 0 failures. The focused file now contains 200 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer
note/reference-location slice eventually ran after earlier active-root lock
races and passed: `php tools/run-tests.php` reported 209 test files, 24,067
assertions, and 0 failures.

`test/Tests/Writers/Markdown.hs` was inspected again for the bounded
`shortcutLinkRefsTests` group. The PHP Markdown writer now maps all 12 cases:
shortcutable simple links, adjacent links, space-plus-link boundaries,
repeated labels with numbered references, bracket-following text escaping,
raw markdown inline boundaries with and without a leading space, and citation
boundaries with and without a leading space. Consecutive reference definitions
are emitted as adjacent definition lines to match Pandoc's `refsToMarkdown`
shape instead of becoming separate paragraphs.

Focused local verification on 2026-05-23 after the Markdown writer shortcut
reference-link boundary slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted a
handoff packet with duplicate adjacent source links, numbered reference labels,
escaped bracketed reviewer text, and citation-adjacent references; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,254 assertions, and 0 failures. The focused file now contains 201
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer shortcut
reference-link boundary slice was left pending: the duplicate-root gate
returned active exact-root PID `2994382` owned by `claude` (`php
tools/run-tests.php`, parent `2994380`, elapsed `00:07`, state `R`), so this
lane did not start a duplicate root run.

`test/Tests/Writers/Markdown.hs` was inspected again for its three top-level
tests. The PHP Markdown writer now maps all three: an ordered list with a
second paragraph followed by an indented code block emits Pandoc's raw HTML
`<!-- -->` separator before the code block, tight nested bullet lists remain
compact (`- foo` followed by an indented `- bar` without a blank loose-list
gap), and delimiter-adjacent whitespace is moved outside nested strong/emphasis
markers for the upstream `#10696` case.

Focused local verification on 2026-05-23 after the Markdown writer top-level
slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted the
expected reviewer handoff packet; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,257
assertions, and 0 failures. The focused file now contains 202 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer top-level
slice was left pending: the duplicate-root gate returned active exact-root PID
`3087737` owned by `claude` (`php tools/run-tests.php`, parent `3087673`,
elapsed `00:18`, state `R`), so this lane did not start a duplicate root run.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected for the bounded
`escapeText` and `getReference` paths. The PHP Markdown writer now maps 21
focused checks from that source boundary: ATX-looking leading `#` text,
smart dash and ellipsis escapes, fenced-div colon-run escapes, image and
strikeout delimiter guards, intraword underscore passthrough, Markdown
formatting/math/table punctuation escapes, angle bracket escapes under
Pandoc's all-symbols-escapable extension, character-reference ampersand
escaping, raw-TeX backslash escaping, generated numeric labels for
bracket-containing reference labels, same-target reference definition reuse,
and numbered disambiguation for duplicate human labels.

This also normalizes the `Tests.Writers.Markdown` leaf-test inventory count:
the upstream module has 19 behavior tests, not 20 (three top-level cases,
four note/reference-location cases, and 12 shortcut-reference cases).

Focused local verification on 2026-05-23 after the Markdown writer inline
escaping/reference-definition slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted the
reviewer handoff packet with block-local notes plus literal audit tokens
escaped for Pandoc-compatible Markdown; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,258
assertions, and 0 failures. The focused file now contains 203 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer inline
escaping/reference-definition slice first found active exact-root PID `3110747`
owned by `claude` (`php tools/run-tests.php`, parent `3096285`, elapsed
`00:18`, state `Rs`), so this lane did not start a duplicate root run at that
point. A later duplicate-root gate was clear, so this lane ran
`php tools/run-tests.php` once. It exited red with 214 test files, 24,638
assertions, and 1 failure. Pandoc tests passed inside the root run, but the
retained tool-output chunks did not include the failing `FAIL ...` line, so the
failing non-pandoc test name is not known from this lane run. A post-run gate
found active exact-root PID `3168962` owned by `claude` (`php
tools/run-tests.php`, parent `3093040`, elapsed `00:13`, state `Rs`), so no
second root run was started. A final duplicate-root sample still found active
exact-root PID `3174787` owned by `claude` (`php tools/run-tests.php`, parent
`3105286`, elapsed `00:27`, state `Rs`). After the exact-root gate cleared
again, a final filtered root capture ran `php tools/run-tests.php` and passed
214 test files, 24,677 assertions, and 0 failures.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Link` emission branch. The PHP Markdown writer now maps eight focused
checks from that source boundary: URI autolinks render as `<url>` when the
label matches the target, `mailto:` targets render as `<address>` without the
scheme, autolinks bypass reference-link mode, inline links preserve quoted
titles, inline links append id/class/key-value attributes with Pandoc's
`attrsToMarkdown` shape, reference definitions append link attributes, targets
that differ only by attributes get distinct reference labels, and repeated
attributed targets reuse the same reference definition.

Focused local verification on 2026-05-23 after the Markdown writer
URI/e-mail autolink and link-attribute slice: `php -l` passed for
`MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-markdown-review-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted angle
bracket URI/e-mail autolinks plus an attributed reviewer packet reference
definition; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,260 assertions, and 0 failures. The focused file now
contains 205 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer URI/e-mail
autolink and link-attribute slice: the duplicate-root gate returned clear, so
`php tools/run-tests.php` was run once. It passed 216 test files, 24,927
assertions, and 0 failures.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Image` emission branch, and `src/Text/Pandoc/Writers/Markdown.hs` was
checked for the single-image `Figure` implicit-figure path. The PHP Markdown
writer now maps five focused checks from that boundary: a testsuite-style
single-image figure writes `![lalune](lalune.jpg "Voyage dans la Lune")`, an
inline movie image writes inside paragraph text, an image whose alt text equals
its URI target writes an empty label to avoid `!<uri>` autolink output, image
titles/id/classes/key-value attrs serialize with Pandoc's Markdown attribute
shape, and distinct stored alt text is preserved as an `alt="..."` image
attribute when the visible caption differs.

Focused local verification on 2026-05-23 after the Markdown writer image
emission slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted a
reference-style reviewer image definition carrying id/class/alt/data-source
metadata; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,262 assertions, and 0 failures. The focused file now
contains 207 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer image
emission slice: the duplicate-root gate returned clear, so
`php tools/run-tests.php` was run once. It passed 223 test files, 25,545
assertions, and 0 failures.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Code` and `Span` emission branches. `src/Text/Pandoc/Writers/Markdown.hs`,
`test/Tests/Writers/Markdown.hs`, `test/testsuite.txt`,
`test/testsuite.native`, `test/markdown-reader-more.txt`, and
`test/markdown-reader-more.native` were also checked as the targeted static
inventory for this slice, for seven upstream paths total. The PHP Markdown
writer now maps five focused checks from that boundary: code attrs serialize
with Pandoc's `attrsToMarkdown` shape, code text containing backticks is
wrapped in a longer backtick marker with interior spaces, attributed spans
write `[content]{#id .class key="value"}`, emoji spans with `data-emoji` write
`:alias:`, and literal text ending in `!` before a following link/span is
escaped so writer output does not accidentally become image syntax.

Focused local verification on 2026-05-23 after the Markdown writer Code/Span
emission slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted a
bracketed reviewer span carrying id/class/data/title metadata, an attributed
inline code token, and an emoji alias; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,264
assertions, and 0 failures. The focused file now contains 209 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer Code/Span
emission slice: the duplicate-root gate returned no active exact root harness
process, so `php tools/run-tests.php` was run once. Pandoc tests passed inside
the root run, but the root harness exited red with 224 test files, 25,731
assertions, and 1 failure outside this lane:
`lanes/libsqlite/tests/SQLiteHeaderTest.php` test `plans wordpress replacement
by merging a non-root composite index parent under a multi-child root` expected
IDs `[1, 2, 3, 4, 6, 7]` and got `[1, 2, 3, 4, 5, 6, 7]`.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded Strikeout, Superscript, Subscript, Math, and RawInline branches. The PHP
Markdown writer now maps eight focused checks from that source boundary:
strikeout delimiter output, superscript delimiter output, subscript delimiter
output, script-space escaping, inline TeX math dollar output, display TeX math
dollar output, the upstream math-followed-by-digit `<!-- -->` guard from
`inlineListToMarkdown`, raw TeX raw-attribute output, and raw-attribute fallback
syntax for unsupported raw inline formats.

Focused local verification on 2026-05-23 after the Markdown writer
strike/script/math/raw inline slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted
subscript, superscript, strikeout, inline math with a digit guard, raw TeX
raw-attribute output, and raw-attribute fallback output; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,265
assertions, and 0 failures. The focused file now contains 210 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer
strike/script/math/raw inline slice: the duplicate-root gate returned no active
exact root harness process, so `php tools/run-tests.php` was run once. The root
harness passed 224 test files, 25,874 assertions, and 0 failures.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Quoted`, `Underline`, and `SmallCaps` writer branches. The PHP
Markdown writer now maps five focused checks from that source boundary: smart
single-quote delimiter output, smart double-quote delimiter output with nested
link content, bracketed underline span output, nested emphasis inside an
underline span, and small-caps output through Pandoc's `.smallcaps` span class.

Focused local verification on 2026-05-23 after the Markdown writer
quoted/underline/small-caps slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted
`"source excerpt"`, `[manual underlines]{.underline}`, and
`[source glossary]{.smallcaps}` in the native reviewer handoff packet; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,266 assertions, and 0 failures. The focused file now contains 211
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer
quoted/underline/small-caps slice: the duplicate-root gate returned no active
exact root harness process, so `php tools/run-tests.php` was run once. The root
harness passed 226 test files, 26,113 assertions, and 0 failures.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Cite` writer branch. The PHP Markdown writer now maps eight focused
checks from that source boundary: author-in-text citations with suffix/rest
brackets, bracketed normal citation lists, suppress-author `-@` citations,
prefix/suffix inline rendering with Pandoc's punctuation spacing rule,
semicolon-separated multi-citation joins, reader-compatible string suffixes,
digit-leading cite keys, and braced invalid cite keys.

Focused local verification on 2026-05-23 after the Markdown writer citation
rendering slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted
`@migration-audit [p. 12; see @source-log ch. 4]` and
`[-@{legacy key}, appendix]`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,268
assertions, and 0 failures. The focused file now contains 212 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer citation
rendering slice: the duplicate-root gate returned no active exact root harness
process, so `php tools/run-tests.php` was run once. The root harness passed
226 test files, 26,288 assertions, and 0 failures.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Span ("",["mark"],[])` branch and the matching inline escaping branch
for literal `==` text. The PHP Markdown writer now maps three focused checks
from that source boundary: exact `.mark` spans emit Pandoc-style
`==...==` highlight syntax, literal `==` source text is escaped so reviewer
copy does not become accidental highlight markup, and `.mark` spans with an
id or other attributes remain on the normal bracketed-span attribute path.

Focused local verification on 2026-05-23 after the Markdown writer mark-span
slice: `php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-markdown-review-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted
`==verify source caption==` while escaping literal `==audit tokens==` as
review text; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,269 assertions, and 0 failures. The focused file now
contains 213 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer mark-span
slice: the duplicate-root gate returned no active exact root harness process,
so `php tools/run-tests.php` was run once and did not emit a lock-wait message.
The pandoc tests passed inside the root run, but the root harness exited red
with 226 test files, 25,266 assertions, and 204 failures outside this lane.
The visible failures were in `lanes/difftastic/tests/TokenDifferTest.php`, all
showing `Call to undefined method PortLibs\Difftastic\TokenDiffer::isNixLanguage()`.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded raw-HTML fallback branch on `Link` and `Image`, and
`src/Text/Pandoc/Writers/HTML.hs` was inspected for the exact HTML writer
boundary that Pandoc delegates to when `Ext_raw_html` is enabled but
`Ext_link_attributes`/`Ext_attributes` are disabled. The native PHP writer now
maps five focused checks from that source boundary: attributed inline links
fall back to raw `<a>` HTML, attributed images fall back to raw `<img />` HTML,
nonstandard HTML attribute keys such as `source` are emitted with Pandoc-style
`data-` prefixes, `rawHtml=false` drops Markdown attributes instead of using
the HTML fallback, and the reference-link path omits reference-definition
attributes when Markdown link attributes are disabled.

Focused local verification on 2026-05-23 after the Markdown writer raw-HTML
fallback slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-raw-html-fallback.php`;
`php lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` emitted
an attributed reviewer edit link and media image as raw HTML with escaped
attributes and `data-source` metadata; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,272
assertions, and 0 failures. The focused file now contains 214 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer raw-HTML
fallback slice: the duplicate-root gate returned no active exact root harness
process, so `php tools/run-tests.php` was run once through the normal locked
root harness. It passed 226 test files, 26,723 assertions, and 0 failures.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
generic `Span attrs ils` branch. The PHP Markdown writer now maps four focused
checks from that source boundary: attributed spans fall back to a raw
`<span ...>` wrapper around Markdown-rendered inline content when
`bracketedSpans=false` and raw HTML is enabled, the same wrapper is emitted
when `nativeSpans=true` and raw HTML is disabled, the span wrapper is omitted
when both raw HTML and native spans are disabled, and null-attribute spans
remain plain content instead of creating an empty wrapper.

Focused local verification on 2026-05-23 after the Markdown writer generic
Span fallback slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-raw-html-fallback.php`;
`php lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` emitted
an attributed reviewer edit link, scoped review span, and media image as raw
HTML with escaped attributes and `data-source` metadata; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,276 assertions, and 0 failures. The focused file now contains 215
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer generic
Span fallback slice was left pending. The duplicate-root gate returned no
active exact `php tools/run-tests.php` process, but broad upstream runners were
active: Dolt BATS PID `237222` with child BATS processes and SQLite
`testrunner.tcl --jobs` PIDs `3854382`/`3854383`. Per lane instructions, no
additional no-argument root harness was started because focused pandoc tests
were already green.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Underline` and `SmallCaps` writer branches. `src/Text/Pandoc/Shared.hs`
was checked for `capitalize`, which uppercases `Str` nodes recursively, and
`src/Text/Pandoc/Writers/HTML.hs` was checked for the corresponding HTML
`<u>` and `smallcaps` span semantics. The PHP Markdown writer now maps eight
focused checks from that source boundary: underline raw-HTML fallback to
`<u>`, underline native-span fallback, underline emphasis fallback when raw
HTML/native spans are disabled, small-caps raw span output, small-caps native
span output, small-caps uppercase fallback, uppercasing of nested link label
text, and preserving code-span text while ordinary `Str` text is uppercased.

Focused local verification on 2026-05-23 after the Markdown writer
underline/small-caps fallback slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-raw-html-fallback.php`;
`php lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` emitted an
attributed reviewer edit link, scoped review span, raw `<u>` underline,
smallcaps span, and media image as raw HTML-compatible Markdown with escaped
attributes and `data-source` metadata; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,280
assertions, and 0 failures. The focused file now contains 216 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer
underline/small-caps fallback slice was left pending. The duplicate-root gate
returned no active exact `php tools/run-tests.php` process, but broad upstream
runners were active: Dolt BATS PID `237222` with child BATS processes and
SQLite `testrunner.tcl --jobs` PIDs `3854382`/`3854383`. Per lane
instructions, no additional no-argument root harness was started because
focused pandoc tests were already green.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Emph` and `Strong` writer branches. The PHP Markdown writer now maps
three focused checks from that source boundary: `Emph [Emph ils]` collapses to
the inner rendered content instead of becoming double emphasis, empty `Emph`
emits no delimiters, and empty `Strong` emits no delimiters.

Focused local verification on 2026-05-23 after the Markdown writer
nested/empty emphasis slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-markdown-review-handoff.php`;
`php lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted
`Reviewer emphasis normalization: source flag and empty source marks drop
before handoff.`; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,283 assertions, and 0 failures. The focused file now
contains 217 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer nested/empty
emphasis slice was left pending. The first duplicate-root gate sample found an
active focused PHP lane process, PID `497483`
(`php tools/run-tests.php lanes/rclone/tests lanes/syncthing/tests`), which
exited before owner sampling. A later exact-root sample was clear, but broad
SQLite `testrunner.tcl --jobs` processes were active: PIDs `3854382` and
`3854383`, owned by `claude`. Per lane instructions, no additional
no-argument root harness was started because focused pandoc tests were already
green.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Strikeout` writer branch when `Ext_strikeout` is disabled. The PHP
Markdown writer now maps two additional focused checks from that source
boundary: `Strikeout` falls back to raw `<s>...</s>` output when raw HTML is
enabled, and falls back to plain rendered inline content when raw HTML is also
disabled. The existing enabled-extension `~~...~~` branch remains covered.

Focused local verification on 2026-05-23 after the Markdown writer
Strikeout disabled-extension fallback slice: `php -l` passed for
`MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-markdown-raw-html-fallback.php`; `php
lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` emitted
`<s>legacy caption</s>` for a deleted source caption; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,286
assertions, and 0 failures. The focused file now contains 218 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer Strikeout
disabled-extension fallback slice was left pending. The duplicate-root gate
returned no active exact `php tools/run-tests.php` process, but broad upstream
runners were active: Dolt BATS PIDs `575005`, `575036`, and `575043`, plus
SQLite `testrunner.tcl --jobs` PIDs `3854382` and `3854383`, all owned by
`claude`. Per lane instructions, no additional no-argument root harness was
started because focused pandoc tests were already green.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Superscript` and `Subscript` writer branches when `Ext_superscript`
or `Ext_subscript` is disabled, and `src/Text/Pandoc/Writers/Shared.hs` was
checked for the `toSuperscript`, `toSubscript`, `toSuperscriptInline`, and
`toSubscriptInline` conversion tables. The PHP Markdown writer now maps four
additional focused checks from that source boundary: disabled script
extensions fall back to raw `<sup>`/`<sub>` output when raw HTML is enabled,
disabled raw HTML falls back to Unicode digit/symbol scripts when the
upstream conversion table can represent the content, `preferAscii` follows
Pandoc's later superscript-character fallback for digit content, and
non-convertible content falls back to `^(...)` or `_(...)` parenthesized text.
The mapped denominator is now 674 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer
Superscript/Subscript disabled-extension fallback slice: `php -l` passed for
`MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-markdown-raw-html-fallback.php`; `php
lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` emitted
`H<sub>2</sub>` and `x<sup>2</sup>` for disabled script syntax in a reviewer
fallback packet; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,289
assertions, and 0 failures. The focused file now contains 219 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer
Superscript/Subscript disabled-extension fallback slice was left pending. The
duplicate-root gate returned no active exact `php tools/run-tests.php`
process, but broad upstream runners were active: Dolt BATS PIDs `575005`,
`575036`, and `575043`; Syncthing Go test PIDs `744251` and `744254`; and
Gitoxide Cargo test PIDs `744335` and `744338`, all owned by `claude`. Per
lane instructions, no additional no-argument root harness was started because
focused pandoc tests were already green.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Quoted SingleQuote` and `Quoted DoubleQuote` writer branches. The PHP
Markdown writer now maps four additional focused checks from that source
boundary: smart-enabled output keeps Pandoc's straight single and double quote
delimiters, `smart=false` emits Unicode curly quote delimiters, `smart=false`
with `preferAscii=true` emits `&lsquo;`/`&rsquo;` and `&ldquo;`/`&rdquo;`
entity delimiters, and smart-disabled text stops escaping ordinary quote,
dash, and ellipsis punctuation as if `Ext_smart` were still enabled. The mapped
denominator is now 678 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer Quoted
smart-disabled fallback slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-raw-html-fallback.php`; `php
lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` emitted
`&lsquo;legacy reviewer quote&rsquo;` and `&ldquo;migration excerpt&rdquo;`
for disabled smart quote syntax in a reviewer fallback packet; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,293 assertions, and 0 failures. The focused file now contains 220
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer Quoted
smart-disabled fallback slice was left pending. The duplicate-root gate found
active exact root harness PID `938813`, owned by `claude`, command `php
tools/run-tests.php`. Per lane instructions, no additional no-argument root
harness was started because focused pandoc tests were already green.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `Str` writer branch, and `src/Text/Pandoc/XML.hs` plus the upstream
`tagsoup-0.14.8` `Text.HTML.TagSoup.Entity` table were checked for Pandoc's
`toHtml5Entities` behavior. The PHP Markdown writer now maps four additional
focused checks from that source boundary: `preferAscii` emits named HTML5
entities for mapped non-ASCII `Str` characters, falls back to decimal numeric
entities for unmapped code points, applies smart-enabled `unsmartify` before
entity conversion for typographic quotes/dashes/ellipses, and preserves the
same punctuation as HTML5 entities when smart punctuation is disabled. The
mapped denominator is now 682 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer Str
preferAscii slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-raw-html-fallback.php`; `php
lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` emitted
`R&eacute;sum&eacute;`, `&COPY;`, `&in;`, `&#128512;`,
`&ldquo;curly excerpt&rdquo;`, and `&mldr;` for a non-ASCII reviewer fallback
packet; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,295 assertions, and 0 failures. The focused file now
contains 221 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer Str
preferAscii slice was left pending. The duplicate-root gate found active exact
root harness PID `1035563`, owned by `claude`, command `php
tools/run-tests.php`; broad Dolt Go runners were also active. Per lane
instructions, no additional no-argument root harness was started because
focused pandoc tests were already green.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `LineBreak` writer branch, and `src/Text/Pandoc/Extensions.hs` was
checked for the `pandocExtensions` defaults that enable
`Ext_escaped_line_breaks`. The PHP Markdown writer now maps three additional
focused checks from that source boundary: default Markdown output emits an
escaped line break (`\` before newline), disabling escaped line breaks emits
Pandoc's two-space hard-break form, and enabling hard line breaks emits a plain
newline. The mapped denominator is now 685 focused Markdown/HTML/WordPress
checks.

Focused local verification on 2026-05-23 after the Markdown writer LineBreak
option slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-review-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted the
reviewer line-break paragraph with a trailing escaped-line-break backslash;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
test file, 2,298 assertions, and 0 failures. The focused file now contains 222
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer LineBreak
option slice was left pending. The duplicate-root gate returned no active exact
`php tools/run-tests.php` process, but broad upstream runners were active:
Dolt BATS PIDs `1036684`, `1036713`, `1036720`, `1036721`, and `1036722`, and
SQLite `testrunner.tcl --jobs` PIDs `1057838` and `1057839`, all owned by
`claude`. Per lane instructions, no additional no-argument root harness was
started because focused pandoc tests were already green.

`src/Text/Pandoc/Writers/Markdown/Inline.hs` was inspected again for the
bounded `RawInline` writer branch. The PHP Markdown writer now maps eight
additional focused checks from that source boundary: `raw_attribute` takes
precedence for HTML raw inlines, TeX raw inlines, and unknown raw formats;
disabling `raw_attribute` lets enabled `raw_html` and `raw_tex` formats pass
through literally; disabling those target raw extensions omits the inlines as
Pandoc's `InlineNotRendered` path does; unsupported non-target raw formats are
omitted when raw attributes are disabled; and markdown-family raw formats still
render literally even when all raw preservation toggles are disabled. The
mapped denominator is now 693 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer raw-inline
extension fallback slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-review-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted raw TeX,
OPML, and HTML reviewer markers as Pandoc raw-attribute Markdown; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,307 assertions, and 0 failures. The focused file now contains 223
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer raw-inline
extension fallback slice was left pending. The duplicate-root gate returned no
active exact `php tools/run-tests.php` process, but broad upstream Dolt BATS
runners were active: PIDs `1036684`, `1036713`, `1036720`, `1036721`,
`1036722`, and `1434434`, all owned by `claude`. Per lane instructions, no
additional no-argument root harness was started because focused pandoc tests
were already green.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected again for the bounded
block-level `RawBlock` and `Div` writer branches, and
`src/Text/Pandoc/Extensions.hs` was checked for Pandoc's default
`Ext_raw_html`, `Ext_raw_tex`, `Ext_raw_attribute`, `Ext_native_divs`, and
`Ext_fenced_divs` settings. The PHP Markdown writer now maps 12 additional
focused checks from that source boundary: markdown-family raw blocks pass
through literally, HTML and TeX raw blocks pass through while their target raw
extensions are enabled, disabled HTML/TeX raw blocks and unknown raw formats
fall back to fenced raw-attribute blocks when `raw_attribute` is enabled,
unsupported raw blocks are omitted when raw attributes are disabled, fenced
Div output uses Pandoc-style nesting-aware colon counts, disabled fenced Divs
fall back to native/raw HTML wrappers, and disabling fenced/native/raw HTML
Div output preserves the inner rendered content. The mapped denominator is now
705 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer RawBlock/Div
fallback slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-review-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-review-handoff.php` emitted a fenced
WordPress review Div, an OPML fenced raw block, and a literal TeX review
environment for native reviewer handoff; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,319
assertions, and 0 failures. The focused file now contains 225 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer RawBlock/Div
fallback slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected again for the bounded
block-level `Figure` writer branch, including the `combinedAttr` implicit
figure guard, the `fig:` title stripping path, the raw-HTML `figureToMarkdown`
fallback, and the `figureDiv` fallback from `src/Text/Pandoc/Shared.hs`. The
PHP Markdown writer now maps the focused checks from that source boundary:
implicit single-image figures render with caption text while moving
allowed figure ids to the image attrs, `fig:` title prefixes are stripped,
distinct image alt text is preserved as an `alt` attribute, attributed figures
fall back to raw HTML when `raw_html` is enabled, raw-disabled figures use
Pandoc-style fenced figure Divs including caption Divs, disabled implicit
figures take the same Div fallback, and constrained profiles can emit only the
figure body when raw HTML and Div output are unavailable. The mapped
denominator is now 711 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer Figure
fallback slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, `examples/wordpress-markdown-review-handoff.php`, and
`examples/wordpress-markdown-raw-html-fallback.php`; `php
lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` emitted an
attributed source-review `<figure>` with `data-source` and `figcaption`
metadata; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,324 assertions, and 0 failures. The focused file now
contains 226 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer Figure
fallback slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` and
`src/Text/Pandoc/Writers/Markdown/Table.hs` were inspected again for the
bounded table writer branch. The static source boundary covers Pandoc's order:
simple tables can use pipe/simple table output, complex tables with spans,
block cells, or footers fall back to HTML while `raw_html` is enabled, simple
spanned tables can degrade to approximate pipe tables when raw HTML is
disabled, and otherwise the writer reports an unrendered table as `[TABLE]`
with the caption boundary preserved. `src/Text/Pandoc/Writers/HTML.hs` was
checked for the corresponding HTML fallback structure: caption, colgroup,
thead/tbody/tfoot, colspan/rowspan, and text-alignment styles. The PHP writer
now maps five additional focused checks from that source boundary: pipe-table
rendering with alignment delimiters and caption inline content, raw HTML
fallback for spanned tables, raw HTML cell colspan/alignment preservation,
raw-disabled approximate pipe-table fallback, and disabled pipe/raw placeholder
output with caption attributes. The mapped denominator is now 716 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer table
fallback slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-raw-html-fallback.php`; `php
lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` emitted a
source-review `<table>` with `data-source`, caption, colgroup, colspan, and
alignment metadata; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,329
assertions, and 0 failures. The focused file now contains 227 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer table
fallback slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Markdown/Table.hs`, and
`src/Text/Pandoc/Writers/Shared.hs` were inspected again for the bounded
grid-table writer branch. The static source boundary covers Pandoc's table
selection order where grid-table output is chosen before raw HTML fallback when
`grid_tables` can represent a non-spanned table with block-rich cells or a
table footer. The PHP writer now maps six additional focused checks from that
source boundary: width-driven grid column sizing, header alignment markers,
block-level cell content, line-break rendering inside grid cells, table-footer
double borders, caption/attribute preservation, and the no-raw-HTML fallback
boundary for this representable grid-table shape. The mapped denominator is
now 722 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer grid-table
branch slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, `examples/wordpress-markdown-grid-table-handoff.php`,
and `examples/wordpress-markdown-raw-html-fallback.php`; `php
lanes/pandoc/examples/wordpress-markdown-grid-table-handoff.php` emitted a
Pandoc-style grid table with block-level cell content, footer totals,
width-driven columns, alignment markers, and caption/source attrs; `php
lanes/pandoc/examples/wordpress-markdown-raw-html-fallback.php` still emitted a
source-review raw HTML table for a spanned-table fallback; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,339 assertions, and 0 failures. The focused file now contains 228
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer grid-table
branch slice was left pending because this lane was not assigned no-argument
root verification. Per lane instructions, focused pandoc evidence was recorded
and root aggregate verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` and
`src/Text/Pandoc/Writers/Markdown/Table.hs` were inspected again for the
bounded multiline-table writer branch. The static source boundary covers
Pandoc's table selection order where `Ext_multiline_tables` is chosen for
width-bearing simple-cell tables before grid-table or raw HTML fallback, plus
the `pandocTable` headed and headless rule shapes. The PHP writer now maps six
additional focused checks from that source boundary: multiline-table selection
before pipe syntax, headed full-border output, headless per-column rule output,
width-derived cell alignment, multiline cell text preservation, caption/attr
preservation, and the pipe fallback when multiline tables are disabled. The
mapped denominator is now 728 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer
multiline-table branch slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-multiline-table-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-multiline-table-handoff.php` emitted a
Pandoc-style multiline table with wrapped WordPress reviewer notes,
width-derived columns, caption attrs, and no raw HTML fallback; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,352 assertions, and 0 failures. The focused file now contains 229
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer
multiline-table branch slice was left pending because this lane was not
assigned no-argument root verification. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Shared.hs`, and
`test/command/rst-writer-gridtable-if-rowspans.md` were inspected again for
the bounded row/column-span grid-table writer branch. The static source
boundary covers Pandoc's table selection order where `Ext_grid_tables` is
chosen before raw HTML fallback whenever `hasColRowSpans` is true, plus the
shared `gridTable` behavior that inserts row-span dummy cells, suppresses only
the covered horizontal-rule segment with `NoLine`, preserves double
head/footer boundaries, and gives colspanned cells a combined width without
interior separators. The PHP writer now maps eight additional focused checks
from that source boundary: spanned-table grid selection before raw HTML,
row-span continuation dummy cells, row-span border gaps, double header
boundaries, double footer boundaries, colspanned separator collapse, raw HTML
fallback only when `gridTables` is disabled, and the approximate pipe-table
fallback when grid and raw output are disabled. The mapped denominator is now
736 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer
grid-table span branch slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-spanned-grid-table-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-spanned-grid-table-handoff.php`
emitted a Pandoc-style spanned grid table with row-spanned review areas,
colspanned remediation summaries, caption/classes/source attrs, and no raw
HTML fallback; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,361
assertions, and 0 failures. The focused file now contains 230 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer
grid-table span branch slice was left pending because this lane was not
assigned no-argument root verification. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` and
`src/Text/Pandoc/Writers/Markdown/Table.hs` were inspected again for the
bounded simple-table writer branch. The static source boundary covers
Pandoc's table selection order where `isSimple && Ext_simple_tables` chooses
`pandocTable opts False` before `pipeTable`, plus the `pandocTable` headed and
headless simple-table rule shapes for widthless simple-cell tables. The PHP
writer now maps eight additional focused checks from that source boundary:
simple-table selection before pipe syntax, headed simple-table output,
headless simple-table output, right/left/center/default alignment padding,
caption/attribute preservation under the two-space Pandoc table indent, pipe
fallback when `simple_tables` is disabled, multiline fallback when both
`simple_tables` and `pipe_tables` are disabled, and no raw HTML fallback for
that bounded Markdown-native shape. The mapped denominator is now 744 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer
simple-table branch slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-simple-table-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-simple-table-handoff.php` emitted a
Pandoc-style simple table with widthless WordPress import totals, alignment
padding, caption/classes/source attrs, and no pipe/raw HTML fallback; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,370 assertions, and 0 failures. The focused file now contains 231
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer
simple-table branch slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown/Table.hs` and `test/pipe-tables.txt` were
inspected again for the bounded pipe-table width-constrained writer branch.
The static source boundary covers Pandoc's `pipeTable` behavior where content
widths drive cell padding only while they fit inside `writerColumns`, relative
`ColWidth` hints scale delimiter widths when content plus pipe separators would
overflow the configured column budget, and over-wide rows stop receiving
alignment padding. The PHP writer now maps eight additional focused checks
from that source boundary: relative delimiter-width selection, floor-based
writerColumns scaling, continued parsing of the upstream long relative-width
pipe-table fixture, unpadded over-wide rows, two-character delimiter fallback
for over-wide widthless tables, stable left/right/default delimiter markers in
narrow output, caption/attribute preservation in the new WordPress handoff
example, and no raw HTML fallback for that bounded pipe-table shape. The
mapped denominator is now 752 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer pipe-table
width branch slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-pipe-width-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-pipe-width-handoff.php` emitted a
narrow Pandoc-style pipe table with relative delimiter widths, over-wide review
notes without alignment padding, caption/classes/source attrs, and no raw HTML
fallback; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,374
assertions, and 0 failures. The focused file now contains 232 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer pipe-table
width branch slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown/Table.hs` was inspected again for the
bounded multiline-table `writerWrapText`/`minOffset` branch. The static source
boundary covers Pandoc's `pandocTable` `relWidth` behavior where non-simple
column widths use the greater of the relative `ColWidth` budget and
`minNumChars` under `WrapAuto`, so a long unbreakable word is not split while
ordinary text can wrap inside a narrow multiline-table column. The PHP writer
now maps nine additional focused checks from that source boundary:
relative-width expansion for an unbreakable source token, preservation of the
long token on one line, narrow reviewer-note wrapping at word boundaries,
wrapped header text, wrapped body text, multiline alignment padding after
wrapping, caption/classes/source attribute preservation, no raw HTML fallback,
and the WordPress import-token review handoff example. The mapped denominator
is now 761 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer multiline
WrapAuto/minOffset branch slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-multiline-wrap-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-multiline-wrap-handoff.php` emitted a
Pandoc-style multiline table with an unbroken long WordPress source token,
word-wrapped reviewer notes, caption/classes/source attrs, and no raw HTML
fallback; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,383 assertions, and 0 failures. The focused file now
contains 233 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer multiline
WrapAuto/minOffset branch slice was left pending because this lane was not
assigned no-argument root verification. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Options.hs`, `src/Text/Pandoc/Writers/Markdown.hs`, and
`src/Text/Pandoc/Writers/Markdown/Table.hs` were inspected for the bounded
multiline-table `writerWrapText`/`WrapNone` branch. The static source boundary
covers Pandoc's `WrapOption` names, the `writeMarkdown` guard that forces
`WrapNone` when hard line breaks are enabled, and `pandocTable` `relWidth`
behavior where non-`WrapAuto` modes use `numChars` instead of `minNumChars` for
relative-width columns. The PHP writer now maps eight additional focused checks
from that source boundary: full-width source-token sizing, full-width
reviewer-note sizing, no word-boundary soft wrapping under `WrapNone`,
hard-line-breaks forcing no-wrap table sizing, full row preservation for a
WordPress import review note, caption/classes/source attribute preservation,
native Markdown output without raw HTML fallback, and the
`wordpress-markdown-multiline-nowrap-handoff.php` example. The mapped
denominator is now 769 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer multiline
WrapNone branch slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-multiline-nowrap-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-multiline-nowrap-handoff.php` emitted
a Pandoc-style multiline table with full-width WordPress source and reviewer
note columns, caption/classes/source attrs, and no raw HTML fallback; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,391 assertions, and 0 failures. The focused file now contains 234
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer multiline
WrapNone branch slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown/Inline.hs`,
`src/Text/Pandoc/Writers/Markdown.hs`, and `src/Text/Pandoc/Options.hs` were
inspected again for the bounded Markdown writer `SoftBreak` wrap-option
branch. The static source boundary covers `inlineToMarkdown opts SoftBreak`,
where `WrapAuto` and `WrapNone` render a breaking space, `WrapPreserve`
renders a carriage return, `Options.FromJSON` accepts both `preserve` and
`wrap-preserve`, and `writeMarkdown` forces `WrapNone` when
`Ext_hard_line_breaks` is enabled. The PHP writer now maps five additional
focused checks from that source boundary: default `WrapAuto` spacing, explicit
`WrapNone` spacing, `WrapPreserve` newline preservation, the `wrap-preserve`
alias, and the hard-line-break guard forcing `WrapNone`. The mapped denominator
is now 774 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer SoftBreak
wrap-option slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-wrap-preserve-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-wrap-preserve-handoff.php` emitted a
Pandoc-style reviewer handoff preserving source paragraph line boundaries
under `wrap-preserve`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,396
assertions, and 0 failures. The focused file now contains 235 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer SoftBreak
wrap-option slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` and
`src/Text/Pandoc/Writers/Markdown/Inline.hs` were inspected for the bounded
Markdown writer `Header` attribute branch. The static source boundary covers
the `blockToMarkdown' opts (Header level attr inlines)` path where Pandoc
computes the auto identifier, elides an id-only attr tuple when it matches that
auto id, appends `attrsToMarkdown` to ATX headings, appends the same tuple to
the setext heading text before the underline, and omits heading attributes when
the header/attributes extension path is disabled. The PHP writer now maps six
additional focused checks from that source boundary: ATX heading attrs with id,
class, and key/value pairs; setext heading attr placement while keeping
underline width based on the heading text; custom id-only headings; inline
heading content with a custom id; auto-id and duplicate auto-id elision; and
disabled heading-attribute output. The mapped denominator is now 780 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer Header
attribute slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-heading-anchors-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-heading-anchors-handoff.php` emitted
custom Pandoc setext/ATX heading attrs for a WordPress review packet and
elided duplicate imported auto ids; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,400
assertions, and 0 failures. The focused file now contains 236 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer Header
attribute slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`, `src/Text/Pandoc/Extensions.hs`, and
`src/Text/Pandoc/Readers/Markdown.hs` were inspected for the bounded Markdown
writer `CodeBlock` attribute branch. The static source boundary covers
`classOrAttrsToMarkdown`, the `blockToMarkdown' opts (CodeBlock attribs str)`
path, `Ext_backtick_code_blocks`, `Ext_fenced_code_blocks`,
`Ext_fenced_code_attributes`, `getLangFromClasses`, and the block-list
separator guard that only inserts `<!-- -->` before indented code after a list.
The PHP writer now maps six additional focused checks from that source
boundary: class-only code block info strings with Pandoc's leading space;
full id/classes/key-value attr tuples; fence length growth when code contains
an all-backtick fence line; disabled fenced-code-attributes language fallback;
disabled fence-extension indented fallback; and list/code separator behavior
for fenced versus indented code. The mapped denominator is now 786 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer CodeBlock
attribute slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-fenced-code-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-fenced-code-handoff.php` emitted a
Pandoc-style fenced PHP shortcode cleanup snippet with id, classes, start
line, and source-batch attrs; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,406
assertions, and 0 failures. The focused file now contains 237 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer CodeBlock
attribute slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected for the bounded Markdown
writer `DefinitionList` branch. The static source boundary covers
`blockToMarkdown' opts (DefinitionList items)`, `definitionListItemToMarkdown`,
`Ext_definition_lists`, `writerTabStop`, the tight-vs-loose split where
`Plain` definitions use `vcat` and `Para` definitions use `vsep`, and the
disabled-extension fallback that emits label/body content without definition
markers. The PHP writer now maps seven additional focused checks from that
source boundary: tight `Plain` definitions render with repeated `:   `
markers, loose `Para` definitions keep a blank line after the term, multiple
definitions keep repeated markers, nested `CodeBlock` bodies are indented under
the marker, nested `BlockQuote` bodies remain block quotes under the marker,
`tabStop` controls marker spacing, and disabled `definitionLists` fallback
keeps term/body content instead of dropping the node. The mapped denominator is
now 793 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer
DefinitionList slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-definition-list-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-definition-list-handoff.php` emitted a
Pandoc-style WordPress import glossary/checklist with repeated definition
markers, a loose nested shortcode snippet body, and source attrs; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,409 assertions, and 0 failures. The focused file now contains 238
behavior tests.

Required root verification on 2026-05-23 after the Markdown writer
DefinitionList slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected for the adjacent
`DefinitionList` branch inside `blockListToMarkdown` `fixBlocks`. The static
source boundary covers the upstream guard that inserts `commentSep` between
consecutive `DefinitionList` blocks and the fallback choice where `commentSep`
is a raw HTML `<!-- -->` block when raw HTML is enabled, otherwise a raw
markdown `&nbsp;` block. The PHP writer now maps two additional focused checks:
adjacent definition lists stay separate with `<!-- -->`, and the raw-HTML-off
path uses `&nbsp;`. The mapped denominator is now 795 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the adjacent DefinitionList
separator slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-definition-list-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-definition-list-handoff.php` emitted a
Pandoc-style WordPress import glossary/checklist plus an adjacent reviewer
packet separated by `<!-- -->`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,411
assertions, and 0 failures. The focused file still contains 238 behavior tests.

Required root verification on 2026-05-23 after the adjacent DefinitionList
separator slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected for the full adjacent
same-type list branch inside `blockListToMarkdown` `fixBlocks`. The static
source boundary covers the 53-line upstream `fixBlocks` region, including the
three guards that insert `commentSep` between consecutive `BulletList`,
`OrderedList`, and `DefinitionList` blocks plus the fallback choice where
`commentSep` is a raw HTML `<!-- -->` block when raw HTML is enabled,
otherwise a raw markdown `&nbsp;` block. The cloned static inventory was
revalidated with `git ls-tree` counts: 2,276 inspected upstream
test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps four additional focused checks from that source boundary:
adjacent bullet lists stay separate with `<!-- -->`, adjacent bullet lists use
`&nbsp;` when raw HTML is disabled, adjacent ordered lists stay separate with
`<!-- -->`, and adjacent ordered lists use `&nbsp;` when raw HTML is disabled.
The mapped denominator is now 799 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the adjacent same-type list
separator slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-adjacent-list-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-adjacent-list-handoff.php` emitted
Pandoc-style WordPress reviewer bullet and ordered queues separated by
`<!-- -->`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,415
assertions, and 0 failures. The focused file now contains 239 behavior tests.

Required root verification on 2026-05-23 after the adjacent same-type list
separator slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected for the remaining
Plain/RawBlock portion of `blockListToMarkdown` `fixBlocks`. The static source
boundary was counted directly from the cached upstream commit at 42 lines and
15 Plain/RawBlock/Div/comment/code guard mentions. The cloned static inventory
was revalidated again with `git ls-tree` counts: 2,276 inspected upstream
test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps four additional focused checks from that source boundary:
Plain followed by RawBlock remains tight with no injected blank block, RawBlock
followed by Plain remains tight, consecutive RawBlock nodes remain tight, and
RawBlock followed by ordinary block content still receives normal blank block
separation. The mapped denominator is now 803 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the raw/plain `fixBlocks`
boundary slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-raw-boundary-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-raw-boundary-handoff.php` emitted a
WordPress reviewer handoff where plain source notes stay adjacent to raw HTML
review cards and the next heading remains separated; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,419
assertions, and 0 failures. The focused file now contains 240 behavior tests.

Required root verification on 2026-05-23 after the raw/plain `fixBlocks`
boundary slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected for the in-list
Plain-before-Div branch inside `blockListToMarkdown` `fixBlocks`. The targeted
source boundary is upstream lines 900-918 at cached commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`: `Plain` before `Div` is promoted
to `Para` when `Ext_fenced_divs` is enabled before the later `inlist`
Plain fallback can keep it tight. The cloned static inventory was revalidated
with `git ls-tree` counts: 2,276 inspected upstream test/data/benchmark
artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps two additional focused checks from that source boundary:
list-item `Plain` before a fenced `Div` renders as a real list item followed
by a separated fenced Div, and disabled fenced Div output remains on the
in-list Plain fallback. The mapped denominator is now 805 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the in-list Plain-before-Div
`fixBlocks` slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-list-div-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-list-div-handoff.php` emitted a
WordPress reviewer list item followed by a Pandoc-style separated fenced Div;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
test file, 2,421 assertions, and 0 failures. The focused file now contains
241 behavior tests.

Required root verification on 2026-05-23 after the in-list Plain-before-Div
`fixBlocks` slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected for the Plain/Para marker
escaping branch. The targeted source boundary is upstream lines 318-333 for
`olMarker`/`beginsWithOrderedListMarker` and lines 430-443 for
`blockToMarkdown' opts (Plain inlines)` at cached commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`: ordered-list-looking first
`Str` nodes are emitted as raw markdown with escaped marker punctuation, and
single-token `+`, `-`, and `%` starts receive an escape guard before normal
inline rendering. The cloned static inventory was revalidated with `git
ls-tree` counts: 2,276 inspected upstream test/data/benchmark artifacts, split
as 1,974 under `test/`, 54 under `pandoc-lua-engine/test/`, 247 under
`data/`, and 1 under `benchmark/`. The PHP writer now maps ten additional
focused checks from that source boundary: decimal marker escaping,
parenthesized decimal marker escaping, roman marker escaping, autonumber marker
escaping, dash marker escaping, plus marker escaping, percent/title-block-style
body escaping, and nested list-item paragraph marker escaping. The mapped
denominator is now 813 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Plain/Para marker-escaping
slice: `php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-markdown-plain-marker-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-plain-marker-handoff.php` emitted
escaped WordPress reviewer source markers including `1\.`, `\(2\)`, `\-`, and
`\%`; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,431 assertions, and 0 failures. The focused file now
contains 242 behavior tests.

Required root verification on 2026-05-23 after the Plain/Para marker-escaping
slice was left pending because this lane was not assigned no-argument root
verification. Per lane instructions, focused pandoc evidence was recorded and
root aggregate verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown/Table.hs` was inspected for the bounded
`pandocTable` `numChars`/`minNumChars` branch. The targeted source boundary is
upstream lines 93-118 at cached commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`: widthless simple tables use
`numChars = offset + 2`, while width-bearing multiline tables use
`minOffset` for `WrapAuto` and `offset` for no-wrap output. The cloned static
inventory was revalidated with `git ls-tree` counts: 2,276 inspected upstream
test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps four additional focused checks from that source boundary:
East Asian wide characters count as two display columns for widthless table
rules, zero-width joiner/non-joiner and combining marks do not inflate table
padding, the simple-table rule keeps Pandoc's `+ 2` alignment padding, and a
WordPress Unicode reviewer table handoff stays native Markdown. The mapped
denominator is now 817 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the pandocTable display-width
`numChars` slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-unicode-table-width-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-unicode-table-width-handoff.php`
emitted a native Pandoc simple table with CJK source labels and zero-width
import tokens aligned by display width; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,436
assertions, and 0 failures. The focused file now contains 243 behavior tests.

Required root verification on 2026-05-23 after the pandocTable display-width
`numChars` slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected for the ordered-list
paragraph-before-fenced-Div boundary. The targeted source boundary is
`blockListToMarkdown` `fixBlocks` plus `orderedListItemToMarkdown` at cached
commit `0640c4c9859aa5a3ede082c190fcd5883c24ac83`: paragraph blocks before a
following fenced `Div` keep a blank block boundary, and ordered-list item
continuation indentation is derived from the rendered marker width. The cloned
static inventory was revalidated with `git ls-tree` counts: 2,276 inspected
upstream test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The PHP
writer now maps four additional focused assertions from that source boundary,
covering:
paragraph-before-Div blank separation, fenced Div retention inside the list
item, three-digit ordered marker continuation indentation, and a WordPress
reviewer checklist handoff example. The mapped denominator is now 821 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the ordered-list
paragraph-before-fenced-Div slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-ordered-list-div-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-ordered-list-div-handoff.php` emitted
a native Pandoc Markdown ordered checklist item with a separated, marker-width
indented fenced Div; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,439
assertions, and 0 failures. The focused file now contains 244 behavior tests.

Required root verification on 2026-05-23 after the ordered-list
paragraph-before-fenced-Div slice was left pending because this lane was not
assigned no-argument root verification. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown/Table.hs` was inspected for the bounded
`pipeTable` positional width branch. The targeted source boundary is upstream
lines 37-74 at cached commit `0640c4c9859aa5a3ede082c190fcd5883c24ac83`:
`pipeWidths` checks `not (all (== 0) widths)` and maps the original `widths`
list directly, so default-width zero columns keep their slots instead of being
compacted before relative delimiter sizing. The cloned static inventory was
revalidated with `git ls-tree` counts: 2,276 inspected upstream
test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps four additional focused checks from that source boundary:
mixed zero/relative width hints are preserved positionally, the first
default-width pipe delimiter stays minimal, later relative-width delimiters
keep their own 25 percent and 75 percent slots, and a multilingual WordPress
reviewer handoff example stays Markdown-native. The mapped denominator is now
825 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the pipe default-width slice:
`php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-markdown-pipe-default-width-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-pipe-default-width-handoff.php`
emitted a narrow WordPress reviewer pipe table whose first default-width column
kept a minimal `--` delimiter and whose later weighted review columns kept
their relative delimiter widths; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,444
assertions, and 0 failures. The focused file now contains 245 behavior tests.

Required root verification on 2026-05-23 after the pipe default-width slice was
left pending because this lane was not assigned no-argument root verification.
Per lane instructions, focused pandoc evidence was recorded and root aggregate
verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`, `src/Text/Pandoc/Options.hs`, and
`src/Text/Pandoc/Writers/Markdown/Table.hs` were inspected for the bounded
table-caption wrapping branch. The targeted source boundary is upstream
`pandocToMarkdown` lines 213-216, where `WrapAuto` sets the DocLayout render
width to `writerColumns`, plus the `Table` writer lines 628-636 and 657-679 at
cached commit `0640c4c9859aa5a3ede082c190fcd5883c24ac83`, where caption
inlines and table attributes are combined into `caption'''` and emitted after
simple, pipe, multiline, and grid table output. `Text.Pandoc.Options` lines
267-280 and 412-413 were also checked for the `WrapAuto`, `WrapNone`, and
`WrapPreserve` option semantics and default `writerColumns = 72`. The cloned
static inventory was revalidated with `git ls-tree` counts: 2,276 inspected
upstream test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps six additional focused checks from that source boundary:
auto-wrapped pipe-table captions respect configured columns, continuation
caption lines remain inside the table-caption block, explicit `wrap=none`
keeps captions unwrapped, `hardLineBreaks` forces the same no-wrap behavior,
long inline-link captions wrap in simple and pipe table output, and a
WordPress pipe-table caption handoff example stays Markdown-native. The mapped
denominator is now 831 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the pipe caption-wrap slice:
`php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-markdown-pipe-caption-wrap-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-pipe-caption-wrap-handoff.php`
emitted a narrow WordPress reviewer pipe table whose caption wrapped before the
caption attribute tuple; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,450
assertions, and 0 failures. The focused file now contains 246 behavior tests.

Required root verification on 2026-05-23 after the pipe caption-wrap slice was
left pending because this lane was not assigned no-argument root verification.
Per lane instructions, focused pandoc evidence was recorded and root aggregate
verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`, `src/Text/Pandoc/Writers/Markdown/Table.hs`,
`src/Text/Pandoc/Extensions.hs`, and `src/Text/Pandoc/Options.hs` were inspected
for the disabled `Ext_table_captions` / CommonMark table-caption boundary. The
targeted source boundary is the `caption'''` branch in the Markdown writer:
caption text plus table attrs are still emitted after table output, but the
Pandoc `: ` caption marker is omitted when `Ext_table_captions` is disabled.
The `writeCommonMark` path and `commonmark`/`commonmark_x` extension defaults
were also checked: CommonMark profiles do not include `Ext_table_captions`,
while `WrapAuto`, `WrapNone`, and `hard_line_breaks` still follow the same
writer wrapping controls. The cloned static inventory was revalidated with
`git ls-tree` counts: 2,276 inspected upstream test/data/benchmark artifacts,
split as 1,974 under `test/`, 54 under `pandoc-lua-engine/test/`, 247 under
`data/`, and 1 under `benchmark/`. The PHP writer now maps five additional
focused checks from that source boundary: explicit disabled `tableCaptions`
keeps simple-table caption text without `: `, explicit disabled
`tableCaptions` keeps pipe-table caption text without `: `, caption attrs
remain attached to the no-colon caption text, `variant => commonmark` follows
the same no-colon path, and a WordPress CommonMark-flavored reviewer handoff
example stays Markdown-native. The mapped denominator is now 836 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the disabled table-caption /
CommonMark slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-commonmark-caption-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-commonmark-caption-handoff.php`
emitted a CommonMark-flavored WordPress reviewer pipe table whose caption text
remained visible without a Pandoc colon marker while keeping caption attrs;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
test file, 2,456 assertions, and 0 failures. The focused file now contains
247 behavior tests.

Required root verification on 2026-05-23 after the disabled table-caption /
CommonMark slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown/Inline.hs`,
`src/Text/Pandoc/Writers/Markdown.hs`, and `src/Text/Pandoc/Extensions.hs`
were inspected again for the CommonMark writer variant around `RawInline` and
`LineBreak`. The targeted upstream boundary is the `Commonmark` branch where
only `gfm`, `commonmark`, `commonmark_x`, and `markdown` raw inline formats
render literally, while other raw formats continue through raw-attribute,
raw-HTML, raw-TeX, or not-rendered fallbacks. The `LineBreak` branch also
forces backslash hard-break output for CommonMark unless `Ext_hard_line_breaks`
is enabled. The CommonMark extension defaults were checked: `commonmark`
defaults raw HTML on but raw attributes/raw TeX off, while `commonmark_x`
includes raw attributes but still not raw TeX. The cloned static inventory was
revalidated with `git ls-tree` counts: 2,276 inspected upstream
test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps nine additional focused checks from that source boundary:
CommonMark hard breaks ignore disabled `escapedLineBreaks`, `hardLineBreaks`
still emits a plain newline, `commonmark_x` and `gfm` raw inline formats render
literally, `markdown_github` is omitted by default under `commonmark`, raw HTML
renders literally, raw TeX is omitted by default, explicit `rawAttribute`
renders raw TeX as raw-attribute Markdown, and `variant=commonmark_x` defaults
raw attributes on for non-CommonMark raw inline formats. The mapped denominator
is now 845 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the CommonMark raw-inline /
linebreak slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-commonmark-raw-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-commonmark-raw-handoff.php` emitted
raw CommonMark/HTML reviewer spans and a backslash hard break despite
`escapedLineBreaks=false`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,465
assertions, and 0 failures. The focused file now contains 248 behavior tests.

Required root verification on 2026-05-23 after the CommonMark raw-inline /
linebreak slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Markdown/Inline.hs`, and
`src/Text/Pandoc/Extensions.hs` were inspected again for the CommonMark writer
variant around block-level `RawBlock` output. The targeted upstream boundary
is the `Commonmark` branch in `blockToMarkdown'`, where only `gfm`,
`commonmark`, `commonmark_x`, and `markdown` raw block formats render
literally; raw HTML renders literally through `removeBlankLinesInHTML`;
unsupported raw formats continue through raw-attribute or not-rendered
fallbacks; and CommonMark defaults raw HTML on while `commonmark_x` also
defaults raw attributes on. The cloned static inventory was revalidated with
`git ls-tree` counts: 2,276 inspected upstream test/data/benchmark artifacts,
split as 1,974 under `test/`, 54 under `pandoc-lua-engine/test/`, 247 under
`data/`, and 1 under `benchmark/`. The PHP writer now maps eight additional
focused checks from that source boundary: CommonMark/commonmark_x/GFM/markdown
raw block formats render literally, `markdown_github` is omitted by default
under `commonmark`, explicit `rawAttribute` and `commonmark_x` fence
unsupported raw block formats, raw HTML remains literal even when generic
`rawHtml` is disabled and converts blank source lines to `&#10;`, raw TeX is
omitted by default under `commonmark`, and raw TeX is fenced under
`commonmark_x`. The mapped denominator is now 855 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the CommonMark raw-block slice:
`php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-markdown-commonmark-raw-block-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-commonmark-raw-block-handoff.php`
emitted block-level CommonMark-compatible source HTML, Pandoc-style raw HTML
blank-line escaping, and no GitHub-only raw Markdown promotion under strict
CommonMark; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,475 assertions, and 0 failures. The focused file now
contains 249 behavior tests.

Required root verification on 2026-05-23 after the CommonMark raw-block slice
was left pending because this lane was not assigned no-argument root
verification. Per lane instructions, focused pandoc evidence was recorded and
root aggregate verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`, `src/Text/Pandoc/Shared.hs`, and
`src/Text/Pandoc/Extensions.hs` were inspected again for the Markdown writer
`LineBlock` branch. The targeted upstream boundary is `blockToMarkdown' opts
(LineBlock lns)`: when `Ext_line_blocks` is enabled, each rendered line is
emitted with a `| ` marker through a hanging indent; when the extension is
disabled, Pandoc falls back through `linesToPara`, which combines line entries
with hard `LineBreak` nodes. The CommonMark/default-extension boundary was also
checked: Pandoc markdown enables `Ext_line_blocks`, while `commonmark` and
`commonmark_x` default extension sets do not include it. The cloned static
inventory was revalidated with `git ls-tree` counts: 2,276 inspected upstream
test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The PHP
writer now maps four additional focused checks from that source boundary:
pipe-prefixed line-block output, bare pipe output for empty line entries,
nonbreaking indentation preservation, disabled-lineBlocks/CommonMark
hard-break fallback, and an explicit CommonMark `lineBlocks` opt-in. The mapped
denominator is now 859 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the Markdown writer LineBlock
slice: `php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-markdown-line-block-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-line-block-handoff.php` emitted a
pipe-prefixed WordPress reviewer stanza with nonbreaking source indentation and
an empty line entry; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,479
assertions, and 0 failures. The focused file now contains 250 behavior tests.

Required root verification on 2026-05-23 after the Markdown writer LineBlock
slice was left pending because this lane was not assigned no-argument root
verification. Per lane instructions, focused pandoc evidence was recorded and
root aggregate verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` and
`src/Text/Pandoc/Writers/Markdown/Inline.hs` were inspected again for the
PlainText writer boundary used by `writePlain`. The targeted upstream branch is
`blockToMarkdown' opts (LineBlock lns)`, where `PlainText` is handled before
`Ext_line_blocks`: each line is rendered through `inlineListToMarkdown`, empty
line entries become blank lines, and no `| ` markers are emitted. Targeted
inline reads covered the matching PlainText branches for spans/emphasis,
strong text, code, `Str`, and links: formatting is stripped, code is emitted as
literal text, and URI/e-mail autolinks render as text. The cloned static
inventory was revalidated with `git ls-tree` counts: 2,276 inspected upstream
test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps six additional focused checks from that source boundary:
PlainText line blocks omit pipe markers, preserve empty line entries as blank
lines, preserve nonbreaking indentation, strip emphasis/link Markdown around
inline content, keep code spans literal, emit URI autolinks as text, and ignore
`lineBlocks` extension toggles. The mapped denominator is now 865 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText line-block slice:
`php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-plain-line-block-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-line-block-handoff.php` emitted a
plain-text WordPress reviewer stanza without Pandoc pipe markers while
preserving nonbreaking source indentation and an empty line entry; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,481 assertions, and 0 failures. The focused file now contains 251
behavior tests.

Required root verification on 2026-05-23 after the PlainText line-block slice
was left pending because this lane was not assigned no-argument root
verification. Per lane instructions, focused pandoc evidence was recorded and
root aggregate verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs` was inspected again for the PlainText
block branches used by `writePlain`. The targeted upstream boundaries are
`blockToMarkdown' opts (Plain inlines)`, `RawBlock`, `HorizontalRule`,
`Header`, and `BlockQuote`, plus the PlainText `commentSep` branch in
`blockListToMarkdown`: plain/paragraph blocks render through PlainText inline
semantics, `Header` emits text without ATX/setext markers or attrs,
`BlockQuote` uses a two-space leader, `RawBlock (Format "plain")` renders
literally while other raw formats do not render in PlainText, horizontal rules
use a dash run sized by `writerColumns`, and adjacent same-type lists do not
receive raw HTML comments. The cloned static inventory was revalidated with
`git ls-tree` counts: 2,276 inspected upstream test/data/benchmark artifacts,
split as 1,974 under `test/`, 54 under `pandoc-lua-engine/test/`, 247 under
`data/`, and 1 under `benchmark/`. The PHP writer now maps eight additional
focused checks from that source boundary. The mapped denominator is now 873
focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText block slice:
`php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-plain-review-blocks-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-review-blocks-handoff.php` emitted
unmarked plain headings, source paragraph label text without Markdown link
markup, a two-space-indented quote note, a literal plain raw review packet, and
a writer-column dash separator; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,482
assertions, and 0 failures. The focused file now contains 252 behavior tests.

Required root verification on 2026-05-23 after the PlainText block slice was
left pending because this lane was not assigned no-argument root verification.
Per lane instructions, focused pandoc evidence was recorded and root aggregate
verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Markdown/Inline.hs`, and `test/writer.plain` were
inspected again for the PlainText writer list and DefinitionList boundary used
by `writePlain`. The targeted upstream branches are
`bulletListItemToMarkdown`, `orderedListItemToMarkdown`, and
`definitionListItemToMarkdown`: list item contents are rendered through
`blockListToMarkdown`, so inline emphasis/links/code use PlainText inline
semantics; DefinitionList labels are rendered as `Plain` blocks, the
PlainText branch uses a space leader instead of `:`, the effective leader is
two spaces in the upstream writer.plain fixture, loose definitions preserve
the blank term/body boundary, and nested code/blockquote bodies are indented
under that two-space definition leader. The cloned static inventory was
revalidated with `git ls-tree` counts: 2,276 inspected upstream
test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps eight additional focused checks from that source boundary:
PlainText bullet-list inline formatting stripping, ordered-list paragraph
inline formatting stripping, DefinitionList term formatting stripping,
two-space definition leaders without Markdown `:` markers, tight multiple
definition output, loose definition term/body spacing, nested code indentation,
and nested blockquote indentation without Markdown quote markers. The mapped
denominator is now 881 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText
list/DefinitionList slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-definition-list-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-definition-list-handoff.php` emitted a
plain import glossary with stripped term/link/emphasis text, two-space
definition leaders, a nested shortcode code block, and a visibly indented
plain quote note; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,485
assertions, and 0 failures. The focused file now contains 253 behavior tests.

Required root verification on 2026-05-23 after the PlainText
list/DefinitionList slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown/Inline.hs`,
`src/Text/Pandoc/Writers/Markdown.hs`, and `test/writer.plain` were inspected
again for the PlainText Image and Note boundary used by `writePlain`. The
targeted upstream branches are `inlineToMarkdown` for `Image`, `Link`, and
`Note`, `notesToMarkdown`/`noteToMarkdown`, and the `Figure` implicit-image
branch: PlainText images render the link text inside square brackets, source
URL labels collapse to an empty bracket guard, note references render as
numeric bracket labels, and plain note definitions use bracket labels without
Markdown footnote caret/colon syntax. The cloned static inventory was
revalidated with `git ls-tree` counts: 2,276 inspected upstream
test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps six additional focused checks from that source boundary:
implicit figure caption brackets, image source-label guard brackets, numeric
inline note references, numeric plain note definitions without Markdown
footnote markers, PlainText inline stripping inside note bodies, and
multi-block note body paragraph/code boundaries. The mapped denominator is now
887 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText image/note slice:
`php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-plain-media-note-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-media-note-handoff.php` emitted an
unmarked plain heading, a bracketed image caption, a numeric note reference, a
stripped source edit link label, and an indented code note body; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,488 assertions, and 0 failures. The focused file now contains 254
behavior tests.

Required root verification on 2026-05-23 after the PlainText image/note slice
was left pending because this lane was not assigned no-argument root
verification. Per lane instructions, focused pandoc evidence was recorded and
root aggregate verification remains pending for the supervisor/integrator.

`test/Tests/Writers/Plain.hs`, `src/Text/Pandoc/Writers/Markdown/Inline.hs`,
and `src/Text/Pandoc/Shared.hs` were inspected again for the PlainText
Gutenberg inline boundary used by `writePlain`. The targeted upstream test is
`strongly emphasized text to uppercase`, which enables `Ext_gutenberg` on the
plain writer and expects `Strong [Str "Straße"]` to render as `STRASSE`.
Targeted source reads covered the PlainText `Emph` and `Strong` branches:
`Emph` emits underscore delimiters only when Gutenberg is enabled, nested
`Emph [Emph ...]` collapses before delimiter output, and `Strong` renders
`inlineListToMarkdown` over `capitalize`d inline children. `capitalize`
uppercases only regular `Str` nodes recursively, expanding lowercase characters
such as German sharp-s correctly while preserving code-span source tokens. The
cloned static inventory was revalidated with `git ls-tree` counts: 2,276
inspected upstream test/data/benchmark artifacts, split as 1,974 under
`test/`, 54 under `pandoc-lua-engine/test/`, 247 under `data/`, and 1 under
`benchmark/`. The PHP writer now maps four additional focused checks from
that source boundary: Unicode-safe strong uppercase with `Straße` -> `STRASSE`,
recursive uppercase of link-label/text content inside strong while code tokens
stay literal, Gutenberg-only underscore output for PlainText emphasis, and
nested PlainText emphasis collapse. The mapped denominator is now 891 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText Gutenberg inline
slice: `php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-plain-gutenberg-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-gutenberg-handoff.php` emitted an
unmarked plain heading, uppercase `MEDIA PRÜFUNG` and `SOURCE EDITOR VIA`
review text, preserved `wp_update_post`, and underscore-delimited
`review-only emphasis`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,493
assertions, and 0 failures. The focused file now contains 255 behavior tests.

Required root verification on 2026-05-23 after the PlainText Gutenberg inline
slice was left pending because this lane was not assigned no-argument root
verification. Per lane instructions, focused pandoc evidence was recorded and
root aggregate verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Markdown/Inline.hs`,
`src/Text/Pandoc/Writers/Markdown/Table.hs`, and
`src/Text/Pandoc/Extensions.hs` were inspected again for the PlainText table
boundary used by `writePlain`. The targeted upstream branch is
`blockToMarkdown'` for `Table`: it computes `caption'` with
`inlineListToMarkdown`, appends table attrs through `attrsToMarkdown`, then
reuses that caption after simple, pipe, multiline, grid, approximate spanned
pipe, and `[TABLE]` fallback output. Under the PlainText writer environment,
`inlineListToMarkdown` strips strong/link/code Markdown syntax and link
destinations from cells and captions while preserving table attrs. The cloned
static inventory was revalidated with `git ls-tree` counts: 2,276 inspected
upstream test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps eight additional focused checks from that source boundary:
plain simple-table cell stripping, plain caption stripping, caption attrs,
stripped link destinations, stripped code ticks, approximate pipe fallback for
spanned cells, `[TABLE]` fallback caption emission, and unsupported table
handoff without raw HTML. The mapped denominator is now 899 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText table fallback
slice: `php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-plain-table-fallback-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-table-fallback-handoff.php` emitted a
plain paragraph, `[TABLE]`, and a plain source caption with attrs while
omitting Markdown link/code syntax and admin URLs; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,501
assertions, and 0 failures. The focused file now contains 256 behavior tests.

Required root verification on 2026-05-23 after the PlainText table fallback
slice was left pending because this lane was not assigned no-argument root
verification. Per lane instructions, focused pandoc evidence was recorded and
root aggregate verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`, `data/templates/default.plain`, and
`test/writer.plain` were inspected again for the PlainText template titleblock
boundary. The targeted upstream path is `pandocToMarkdown`: when
`writerTemplate` is present and the writer variant is `PlainText`, Pandoc uses
`plainTitleBlock` to render title, semicolon-joined authors, and date into the
template's `titleblock` field before body output. The corresponding default
plain template emits `titleblock`, a blank line, then `body`; `test/writer.plain`
starts with `Pandoc Test Suite`, `John MacFarlane; Anonymous`, and the date
before the suite body. The cloned static inventory was revalidated with
`git ls-tree` counts: 2,276 inspected upstream test/data/benchmark artifacts,
split as 1,974 under `test/`, 54 under `pandoc-lua-engine/test/`, 247 under
`data/`, and 1 under `benchmark/`. The PHP writer now maps six additional
focused checks from that source boundary: template-only plain titleblock
emission, unchanged body-only writePlain output without a template, title inline
PlainText stripping, semicolon author joining with link/emphasis stripping,
date code-span stripping, and `standalone` as an alias for template output. The
mapped denominator is now 905 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText template
titleblock slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and `examples/wordpress-plain-titleblock-handoff.php`;
`php lanes/pandoc/examples/wordpress-plain-titleblock-handoff.php` emitted a
plain metadata header with title, semicolon-joined authors, date, and body text
while omitting Markdown link/code syntax and admin URLs; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,507 assertions, and 0 failures. The focused file now contains 257
behavior tests.

Required root verification on 2026-05-23 after the PlainText template
titleblock slice was left pending because this lane was not assigned
no-argument root verification. Per lane instructions, focused pandoc evidence
was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Shared.hs`, `src/Text/Pandoc/Options.hs`, and
`data/templates/default.plain` were inspected again for the PlainText template
include-variable boundary. The targeted upstream path is `pandocToMarkdown`:
when `writerTemplate` is present it builds a template context with `body`,
`toc`/`table-of-contents`, `titleblock`, writer variables, and metadata.
`addVariablesToContext` gives writer variables precedence over metadata, while
metadata values are rendered through the same block/inline writer. The default
plain template is 21 lines and emits `header-includes`, `include-before`,
`table-of-contents`, `body`, and `include-after` in that order. The cloned
static inventory was revalidated with `git ls-tree` counts: 2,276 inspected
upstream test/data/benchmark artifacts, split as 1,974 under `test/`, 54 under
`pandoc-lua-engine/test/`, 247 under `data/`, and 1 under `benchmark/`. The
PHP writer now maps seven additional focused checks from that source boundary:
header-includes before body, multiple include-before entries before body,
include-after after body notes/references, writer-variable precedence over
metadata, raw writer-variable string output, metadata include-after rendering
through PlainText inline semantics, and unchanged body-only writePlain output
when no template is enabled. The mapped denominator is now 912 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText template
include-variable slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-include-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-include-handoff.php` emitted a
plain WordPress import audit packet with header-includes, two include-before
entries, plain body text, and a metadata-derived include-after footer while
omitting admin URLs and Markdown code ticks from metadata-rendered text; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,513 assertions, and 0 failures. The focused file now contains 258
behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText template
include-variable slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Shared.hs`, `src/Text/Pandoc/Chunks.hs`,
`src/Text/Pandoc/Options.hs`, `data/templates/default.plain`, and
`test/command/toc.md` were inspected again for the PlainText template
table-of-contents boundary. The targeted upstream path is `pandocToMarkdown`:
when `writerTableOfContents` is enabled it builds `toc` and
`table-of-contents` context fields from `toTableOfContents`; the default
plain template emits that field before `body`; `writerTOCDepth` bounds nested
entries; `tocEntryToLink` wraps heading text in links with `toc-...` anchors;
and PlainText output strips those link destinations and attrs while retaining
the heading labels. The cloned static inventory was revalidated with
`git ls-tree` counts: 2,276 inspected upstream test/data/benchmark artifacts,
split as 1,974 under `test/`, 54 under `pandoc-lua-engine/test/`, 247 under
`data/`, and 1 under `benchmark/`. The PHP writer now maps eight additional
focused checks from that source boundary: template-only TOC emission before
body, unchanged body-only writePlain output when no template is enabled,
TOC-depth limiting, heading-link destination stripping, generated TOC-anchor
stripping, source link attribute stripping, code tick stripping in TOC labels,
and unlisted heading omission when no section number is present. The mapped
denominator is now 920 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText template TOC
slice: `php -l` passed for `MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-plain-toc-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-toc-handoff.php` emitted a plain
WordPress import audit packet with a nested TOC before body text while omitting
source edit URLs, generated TOC anchors, and code ticks from TOC labels; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,520 assertions, and 0 failures. The focused file now contains 259
behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText template TOC
slice was not assigned to this lane worker, so no no-argument root harness was
started. Per lane instructions, focused pandoc evidence was recorded and root
aggregate verification remains pending for the supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Shared.hs`, `src/Text/Pandoc/Chunks.hs`,
`src/Text/Pandoc/Shared.hs`, `src/Text/Pandoc/Options.hs`,
`data/templates/default.plain`, and `test/command/toc.md` were inspected again
for the PlainText numbered table-of-contents boundary. The targeted upstream
path is `toTableOfContents`: it calls `makeSections writerNumberSections`,
then `toTOCTree`, then `tocToList`. `makeSections` adds `number` attributes
when numbering is enabled and the heading is not `unnumbered`; explicit
`number` attrs are preserved; unnumbered headings do not advance the section
counter; and `toTOCTree` excludes `unlisted` headings only when no section
number is present. `tocEntryToLink` emits the `toc-section-number` span only
when numbering is requested, so plain output includes labels such as `1.1`
only in numbered TOCs.

The PHP writer now maps six additional focused checks from that source
boundary: generated section numbers render in PlainText TOC labels when
`numberSections` is enabled, generated section numbers keep `unlisted` headings
visible, `unnumbered` headings stay visible without advancing counters,
explicit heading `number` attrs render when numbering is enabled,
explicit-number `unlisted` headings stay in the TOC when numbering labels are
disabled, and body heading output remains unnumbered/plain. The mapped
denominator is now 926 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText numbered template
TOC slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-numbered-toc-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-numbered-toc-handoff.php` emitted a
plain WordPress import audit packet with generated section numbers, a numbered
unlisted audit heading, an unnumbered appendix heading, and an explicit legacy
section number; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,526
assertions, and 0 failures. The focused file now contains 260 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText numbered
template TOC slice was not assigned to this lane worker, so no no-argument root
harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Shared.hs`, `data/templates/default.plain`, and
`MANUAL.txt` were inspected again for the PlainText template body context
boundary. The targeted upstream path is `pandocToMarkdown`: it builds `main`
from the rendered body plus notes/references, then inserts that value with
`defField "body"` after `addVariablesToContext`. Because `defField` only
fills missing fields, writer variables and metadata fields named `body` can
override the automatic body. The Pandoc manual also documents `body` as an
automatic variable that users can modify, and the default plain template emits
`$body$` between `table-of-contents` and `include-after`.

The PHP writer now maps seven additional focused checks from that source
boundary: metadata `body` fields replace automatic body output in PlainText
template rendering, metadata body block values render through PlainText
link/code/list semantics, writer `body` variables take precedence over metadata
body fields, writer body variables are emitted as raw template values,
`include-after` remains after the overridden body, the original document body
is suppressed when a body context override is present, and the shared context
lookup now also applies variable/option/metadata precedence to `titleblock`.
The titleblock path is covered by two focused checks for raw writer-variable
output and precedence over generated metadata titleblocks. The mapped
denominator is now 935 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText template body
context override slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-body-override-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-body-override-handoff.php` emitted a
plain WordPress import audit packet with a template-provided redacted body and
a metadata-derived footer; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,535
assertions, and 0 failures. The focused file now contains 261 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText template body
context override slice was not assigned to this lane worker, so no no-argument
root harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Shared.hs`, `data/templates/default.plain`, and
`MANUAL.txt` were inspected again for the PlainText custom template
`meta-json` boundary. The targeted upstream path is `pandocToMarkdown` plus
`addVariablesToContext`: Pandoc builds metadata context first, inserts a
metadata-only `meta-json` value, overlays writer variables so they override
ordinary metadata fields, then supplies default `toc`, `table-of-contents`,
`body`, and `titleblock` fields with `defField`. The native PHP slice maps the
bounded custom-template subset needed by WordPress plain audit packets:
`$if(...)$`, `$for(...)$`, and `$name$` substitutions, metadata-only JSON,
PlainText rendering of metadata block values inside that JSON, generated
titleblock/body defaults, and writer-variable precedence over metadata in the
rendered custom template. The mapped denominator is now 944 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
`meta-json` slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-meta-json-template-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-meta-json-template-handoff.php` emitted a
custom plain WordPress import audit packet with metadata JSON, a writer-variable
preface override, generated titleblock text, and a PlainText-rendered body;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
test file, 2,544 assertions, and 0 failures. The focused file now contains 262
behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
`meta-json` slice was not assigned to this lane worker, so no no-argument root
harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax plus the existing
`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Shared.hs`, and `data/templates/default.plain`
PlainText context slices were inspected again for bounded doctemplate branch
behavior. The upstream manual documents dotted variables for structured
values, matched `${...}` delimiters with optional whitespace, `$if$` blocks
with `$else$`, `$for$` loops, `$sep$` loop separators, and the anaphoric
`it` keyword inside loops. The native PHP slice maps ten additional focused
checks from that boundary: dotted metadata lookup, `${...}` variable
delimiters, `$if$ true branches, absent-variable `$else$ branches, `$for$`
iteration over list values, `$sep$ output only between loop values,
`$it.field$` lookup inside loops, map truthiness, map values rendering as
`true` when interpolated directly, and PlainText body substitution inside a
custom template. The mapped denominator is now 954 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
branch slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-branching-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-branching-handoff.php` emitted
a custom plain WordPress branch packet with dotted workflow metadata, an else
fallback, comma-separated reviewer recipients, and PlainText-rendered body
text; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,550 assertions, and 0 failures. The focused file now
contains 263 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
branch slice was not assigned to this lane worker, so no no-argument root
harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax plus the existing
`src/Text/Pandoc/Writers/Markdown.hs`,
`src/Text/Pandoc/Writers/Shared.hs`, and `data/templates/default.plain`
PlainText context slices were inspected again for bounded doctemplate nested
control behavior. The upstream manual documents matching `$...$` or `${...}`
delimiters, `$$` literal dollar output, `$--` comments through the end of a
line, `$if$`/`$elseif$`/`$else$`/`$endif$` conditionals, `$for$ loops,
top-level `$sep$` loop separators, dotted variables, and the anaphoric `it`
keyword. The native PHP slice maps thirteen additional focused checks from that
boundary: nested `for` blocks match their own `endfor` instead of an inner
loop terminator, outer loop separators ignore nested loop separators, nested
loop separators render only between inner values, scalar `it` values render
inside loops, true `if` branches inside loops render normally, `elseif`
fallback branches render when the initial conditional is false, final `else`
branches are suppressed after a true `elseif`, empty reviewer arrays select
the false branch, `$$` renders one
literal dollar, `$--` comments are omitted through end of line, template tokens
inside comments do not render, underscore-bearing variable names work in
conditionals, and braced whitespace-delimited variables still interpolate. The
mapped denominator is now 967 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
nested-control slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-nested-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-nested-handoff.php` emitted a
plain WordPress nested template packet with scalar labels through `it`,
phase/reviewer loops, an `elseif` fallback, literal dollar output, omitted
template comments, and a PlainText
body; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,561 assertions, and 0 failures. The focused file now
contains 265 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
nested-control slice was not assigned to this lane worker, so no no-argument
root harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax was inspected again for bounded doctemplate
partial behavior. The upstream manual documents partial calls as
`${ partial() }`, variable-applied partials as `${ variable:partial() }`,
bracket separators after variables or partials, the anaphoric `it` keyword
inside applied partials, omission of final newlines from included partials, and
partials including other partials. The native PHP slice maps seven additional
focused checks from that boundary: direct partial inclusion, variable-applied
reviewer partials, bracket separators for applied partial output, bracket
separators for array variables, map values rendered through `it` inside an
applied partial, nested partial inclusion, and final newline omission before
following template content. The mapped denominator is now 974 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
partial slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-partial-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-partial-handoff.php` emitted a
custom plain WordPress partial template packet with variable-applied reviewer
partials, a nested footer partial, bracket separators, workflow metadata, and a
PlainText-rendered body; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,568
assertions, and 0 failures. The focused file now contains 266 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
partial slice was not assigned to this lane worker, so no no-argument root
harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax was inspected again for bounded doctemplate pipe
behavior. The upstream manual documents slash pipes after variables and
partials, use of pipes in `for` loop expressions such as
`$for(metadata/pairs)$`, chained pipes such as `$it.key/alpha/uppercase$`,
and predefined no-parameter pipes including `pairs`, `uppercase`,
`lowercase`, `length`, `reverse`, `first`, `last`, `rest`, `allbutlast`,
`chomp`, `alpha`, and `roman`. `src/Text/Pandoc/Templates.hs` was also
rechecked to confirm Pandoc delegates template compilation/rendering to the
`doctemplates` package, so this slice is MANUAL/doctemplates-semantics
inventory rather than an upstream Haskell runner pass. The native PHP slice
maps fifteen additional focused checks from that boundary: slash pipes parse
on variables, loop expressions, conditionals, and applied partial output;
`pairs` converts reviewer arrays to key/value maps with one-based list keys;
`uppercase` and `lowercase` transform text; `length` counts array values;
`first` and `last` select boundary values; `rest` and `allbutlast` can be
chained before a loop; `reverse` preserves transformed-array separator
rendering; `chomp` trims trailing newlines from body substitution; `alpha`
and `roman` convert integer-like text; chained `alpha/uppercase` labels
render as reviewer letters; and piped body values still use PlainText link
semantics. The mapped denominator is now 989 focused Markdown/HTML/WordPress
checks.

Focused local verification on 2026-05-23 after the PlainText custom template
pipe slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-pipes-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-pipes-handoff.php` emitted a
custom plain WordPress pipe template packet with lower/uppercased workflow
metadata, label count/first-last/reverse output, alphabetic reviewer pair
labels, and a PlainText-rendered body; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,573
assertions, and 0 failures. The focused file now contains 267 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
pipe slice was not assigned to this lane worker, so no no-argument root
harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax was inspected again for bounded doctemplate
parameterized pipe behavior. The upstream manual documents
`left n "leftborder" "rightborder"`, `right n "leftborder" "rightborder"`,
and `center n "leftborder" "rightborder"` as textual-value pipes with
positive integer widths and optional quoted borders; it also states that
these pipes do not affect other values. `src/Text/Pandoc/Templates.hs` was
rechecked to confirm Pandoc delegates template compilation/rendering to the
`doctemplates` package, so this remains MANUAL/doctemplates-semantics
inventory rather than an upstream Haskell runner pass. The native PHP slice
maps twelve additional focused checks from that boundary: left alignment with
right padding, right alignment with left padding, centered padding, optional
left/right borders, quoted border escape handling, no truncation for over-wide
text, no effect on non-text map values, alignment after an `uppercase` chain,
fixed-width loop-row formatting, parameterized pipe parsing after variables,
PlainText body substitution before alignment, and reviewer-safe link stripping
inside aligned body output. The mapped denominator is now 1001 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
alignment-pipe slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-align-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-align-handoff.php` emitted a
custom plain WordPress alignment template packet with padded batch metadata,
a centered workflow queue, fixed-width reviewer rows using left/right/center
pipes, and a PlainText-rendered body; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,580
assertions, and 0 failures. The focused file now contains 268 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
alignment-pipe slice was not assigned to this lane worker, so no no-argument
root harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax was inspected again for breakable-space and
`nowrap` pipe behavior. The upstream manual documents `$~$...$~$` regions as
template text whose spaces can break when a document is rendered with a short
line length, `chomp` as removing trailing newlines and breakable space, and
`nowrap` as disabling line wrapping on breakable spaces.
`src/Text/Pandoc/Templates.hs` was rechecked to confirm Pandoc delegates
template rendering to the `doctemplates` package, while
`src/Text/Pandoc/Writers/Markdown.hs`
confirms `writerColumns` is passed to DocLayout rendering only when
`writerWrapText` is `WrapAuto`. The native PHP slice maps eight additional
focused checks from that boundary: `$~$` breakable-space toggles parse without
leaking directives, breakable template spaces wrap at `writerColumns`,
ordinary template spaces remain nonbreakable, partial output can carry
breakable spaces, `/nowrap` disables wrapping on those breakable spaces,
`/chomp` removes trailing newlines plus breakable space, custom PlainText body
substitution remains reviewer-safe, and source admin URLs are stripped from
body output. The mapped denominator is now 1009 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
breakable-space/nowrap slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-nowrap-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-nowrap-handoff.php` emitted a
custom plain WordPress packet with a wrapped breakable reviewer summary, a
protected no-wrap legal-hold line, a chomped readiness marker, and a
PlainText-rendered body; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,587
assertions, and 0 failures. The focused file now contains 269 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
breakable-space/nowrap slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax was inspected again for custom-template nesting.
The upstream manual documents the `$^$` directive as the way to nest subsequent
lines to the current output column, including multiline variable values and
following template lines aligned with the directive, and it documents automatic
nesting when a variable occurs by itself on an indented line and its value
contains multiple lines. `src/Text/Pandoc/Templates.hs` was rechecked to
confirm Pandoc delegates template rendering to the `doctemplates` package, so
this remains MANUAL/doctemplates-semantics inventory rather than an upstream
Haskell runner pass. The native PHP slice maps nine additional focused checks
from that boundary: `$^$` directives parse without leaking markers, multiline
variables indent continuation lines to the current output column, following
template lines aligned with the directive keep that nesting level, multiline
partials nest the same way, indented variables alone on a line nest
automatically, body substitution remains PlainText-rendered inside nested
templates, source admin URLs are stripped from body output, the WordPress
plain nesting handoff example emits aligned reviewer packet text, and the
static cloned denominator remains 2,276 upstream test/data/benchmark artifacts.
The mapped denominator is now 1018 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
nesting slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-nesting-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-nesting-handoff.php` emitted a
custom plain WordPress packet with a nested multiline review description,
aligned owner continuation, automatically nested summary variable, nested
legal-hold partial, and PlainText-rendered body; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,593
assertions, and 0 failures. The focused file now contains 270 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
nesting slice was not assigned to this lane worker, so no no-argument root
harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax and Pandoc's
`src/Text/Pandoc/Templates.hs` were inspected again at upstream pandoc commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`; Pandoc delegates compiled
template rendering to the `doctemplates >= 0.11 && < 0.12` package. A shallow
supplemental clone of `jgm/doctemplates` at
`2485bcbdb297d54d732006ca545a2a9e426f3817` was inspected for the dependency
boundary: 32 files under `test/`, including 22 `.test` fixture files, and
`test/nest.test` has 67 lines covering `$^$` nesting, automatic nesting,
nested control directives, blank lines, and indentation reset. The native PHP
slice maps eight additional focused checks from that fixture: automatic
nesting for whitespace-preceded multiline variables, explicit `$^$` multiline
variable nesting, aligned following template lines at the `$^$` level,
nested `if` directives inside a nested loop, loop output without doubled
source indentation, blank template lines that do not terminate nesting,
blank output lines without indentation-only spaces, and nesting termination
when the next nonblank template line is less indented. The mapped denominator
is now 1,026 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
blank-line nesting slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-nesting-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-nesting-handoff.php` emitted a
custom plain WordPress packet with a nested multiline review description that
contains an internal blank line, aligned owner continuation, automatically
nested summary variable, nested legal-hold partial, blank-line separated
legal-hold conditional, and PlainText-rendered body; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,598
assertions, and 0 failures. The focused file now contains 271 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
blank-line nesting slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax and Pandoc's `src/Text/Pandoc/Templates.hs` were
inspected again at upstream pandoc commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`; Pandoc still delegates template
rendering to the `doctemplates >= 0.11 && < 0.12` package. The supplemental
`jgm/doctemplates` clone at `2485bcbdb297d54d732006ca545a2a9e426f3817` was
inspected for `test/conditionals.test` (37 lines) and `test/elseif.test`
(30 lines), plus the parser branches where `pConditional` passes its
multiline flag into `pElse` and `pElseIf` consumes a following line ending
for its own branch body. The native PHP slice maps six additional focused
checks from that boundary: standalone `$else$` after a multiline `$if$`
swallows its following newline, a selected false branch does not gain a
leading blank line, empty false conditions omit their block without adding
extra blank output, standalone `$elseif$` swallows its following newline,
a selected first-elseif branch preserves the intentional blank between
adjacent conditionals, and chained false `$elseif$` branches fall through to
the final `$else$` without a leading blank. The mapped denominator is now
1,032 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
standalone branch newline slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-branching-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-branching-handoff.php` emitted
a custom plain WordPress branch packet with a standalone `$elseif$` escalation
block selecting the workflow queue without a leading blank line, plus a
PlainText-rendered body; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,602
assertions, and 0 failures. The focused file now contains 272 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
standalone branch newline slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax and Pandoc's `src/Text/Pandoc/Templates.hs` were
inspected again at upstream pandoc commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`; Pandoc still delegates template
rendering to the `doctemplates >= 0.11 && < 0.12` package. The supplemental
`jgm/doctemplates` clone at `2485bcbdb297d54d732006ca545a2a9e426f3817` was
inspected for `test/final-newline.test` (15 lines) and `test/boolean.test`
(13 lines). The native PHP slice maps seven additional focused checks from
that boundary: a scalar ending with one newline does not double the following
template line ending, a scalar ending with two newlines preserves one
intentional blank line, final custom PlainText output still omits a trailing
output newline, direct `true` renders as `true`, direct `false` renders as
`false`, truthy boolean conditionals select the true branch, and false boolean
conditionals select the false branch. The mapped denominator is now 1,039
focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
final-newline/boolean scalar slice: `php -l` passed for
`MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-final-newline-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-final-newline-handoff.php`
emitted a custom plain WordPress packet with newline-terminated review fields
without extra blank lines, one intentional blank from a double-newline field,
visible true/false metadata, and PlainText-rendered body output; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,609 assertions, and 0 failures. The focused file now contains 274
behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
final-newline/boolean scalar slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax and Pandoc's `src/Text/Pandoc/Templates.hs` were
inspected again at upstream pandoc commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`; Pandoc still delegates template
rendering to the `doctemplates >= 0.11 && < 0.12` package. The supplemental
`jgm/doctemplates` clone at `2485bcbdb297d54d732006ca545a2a9e426f3817` was
inspected for `test/space-in-loop.test` (27 lines) and `test/values.test`
(20 lines). The native PHP slice maps six additional focused checks from that
boundary: a loop body with a blank line after the interpolated value preserves
one blank line between rendered list values, an empty loop with a blank body
emits no whitespace, direct numeric/null interpolation follows the fixture's
text/empty-line behavior, direct scalar-list interpolation concatenates items
without implicit paragraph separators, direct map interpolation renders
`true`, and direct lists of maps concatenate those `true` markers. The mapped
denominator is now 1,045 focused
Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
space-in-loop/direct-list slice: `php -l` passed for `MarkdownWriter.php` and
`MarkdownReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,615
assertions, and 0 failures. The focused file now contains 275 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
space-in-loop/direct-list slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax and Pandoc's `src/Text/Pandoc/Templates.hs` were
inspected again at upstream pandoc commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`; Pandoc still delegates template
rendering to the `doctemplates >= 0.11 && < 0.12` package. The supplemental
`jgm/doctemplates` clone at `2485bcbdb297d54d732006ca545a2a9e426f3817` was
inspected for `test/loop-in-partial.test` (6 lines) plus its two partials
`test/loop1.txt` and `test/loop2.txt`. The native PHP slice maps two
additional focused checks from that boundary: mutually recursive partials now
emit Pandoc's literal `(loop)` sentinel instead of recursing forever or
disappearing, and a WordPress reviewer-packet partial loop keeps
PlainText-rendered body output without leaking source admin URLs. The mapped
denominator is now 1,047 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
partial-recursion slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-loop-guard-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-loop-guard-handoff.php` emitted
a custom plain WordPress packet with `Guard: (loop)` and PlainText-rendered
body output; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,619
assertions, and 0 failures. The focused file now contains 276 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
partial-recursion slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax and Pandoc's `src/Text/Pandoc/Templates.hs` were
inspected again at upstream pandoc commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`; Pandoc still delegates template
rendering to the `doctemplates >= 0.11 && < 0.12` package. The supplemental
`jgm/doctemplates` clone at `2485bcbdb297d54d732006ca545a2a9e426f3817` was
inspected for `test/pad.test` (53 lines), plus `Text.DocTemplates.Internal`
`Block` pipe handling where `left`, `right`, and `center` are rendered through
DocLayout block constructors with vertical-fill borders and `NullVal` aligned
as an empty simple value. The native PHP slice maps eight additional focused
checks from that boundary: multiline right/center/left alignment composes
adjacent block values line-by-line, trailing block padding is not emitted at
the end of output lines, `$^$` nesting still uses the untrimmed aligned column
width, `pairs/alpha/right` keeps multiline values nested under the list label,
bordered key/value blocks vertically fill shorter key columns, left-aligned
multiline value cells keep right borders on every row, missing salary fields
render as padded empty cells, and over-wide aligned fields are not truncated.
The mapped denominator is now 1,055 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
pad-alignment slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-pad-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-pad-handoff.php` emitted a
custom plain WordPress reviewer packet with multiline review notes, vertically
filled blank cells for missing metadata, over-wide note preservation, and
PlainText-rendered body output; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,622
assertions, and 0 failures. The focused file now contains 277 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
pad-alignment slice was not assigned to this lane worker, so no no-argument
root harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax and Pandoc's `src/Text/Pandoc/Templates.hs` were
inspected again at upstream pandoc commit
`0640c4c9859aa5a3ede082c190fcd5883c24ac83`; Pandoc still delegates template
rendering to the `doctemplates >= 0.11 && < 0.12` package. The supplemental
`jgm/doctemplates` clone at `2485bcbdb297d54d732006ca545a2a9e426f3817` was
inspected for `test/loop-in-object.test` (19 lines), `test/loop-in-partial.test`
(6 lines), and `src/Text/DocTemplates/Parser.hs` lines 317-318 where partial
resolution returns the literal `(loop)` only when nesting exceeds 50. The native
PHP slice maps six additional focused checks from that boundary: dotted object
loops iterate `worksite.workers`, anaphoric `it.name.last` and `it.name.first`
resolve inside that nested object loop, the three worker rows render in upstream
order, partial nesting through level 50 still resolves, level 51 emits the
Pandoc `(loop)` sentinel, and a WordPress reviewer-packet object loop keeps
PlainText-rendered body output without leaking source admin URLs. The mapped
denominator is now 1,061 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
object-loop/partial-depth slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-object-loop-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-object-loop-handoff.php` emitted
a custom plain WordPress reviewer packet with nested reviewer names/queues and
PlainText-rendered body output; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,626
assertions, and 0 failures. The focused file now contains 278 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
object-loop/partial-depth slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax, Pandoc's `src/Text/Pandoc/Templates.hs`, and the
supplemental `jgm/doctemplates` clone at
`2485bcbdb297d54d732006ca545a2a9e426f3817` were inspected again for
`test/test.hs` compile-failure unit cases and `src/Text/DocTemplates/Parser.hs`
error boundaries. The native PHP slice maps six additional focused checks from
that boundary: malformed `$if(...)$` syntax reports the unexpected closing
delimiter, the reserved `$sep$` keyword fails as a variable, unknown pipes report
the offending pipe name, `left`/`right`/`center` pipes require integer
parameters, invalid pipe arguments report the bad argument column, and syntax
errors inside partials report the derived partial path plus line/column. The
mapped denominator is now 1,067 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
compile-diagnostics slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-diagnostics-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-diagnostics-handoff.php` emitted
a `templates/reviewer-card.txt` line/column diagnostic before rendering the
source body; `php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 1 test file, 2,632 assertions, and 0 failures. The focused file now
contains 279 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
compile-diagnostics slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax, Pandoc's `src/Text/Pandoc/Templates.hs`, and the
supplemental `jgm/doctemplates` clone at
`2485bcbdb297d54d732006ca545a2a9e426f3817` were inspected again for
`test/basic-with-braces.test` (16 lines), `test/partials.test` (39 lines), and
`src/Text/DocTemplates/Parser.hs` delimiter/partial nesting behavior. The
native PHP slice maps eight additional focused checks from that boundary:
`${...}` delimiters drive the employee loop, braced variable interpolation
reads dotted fields, braced conditionals select missing-salary else branches,
`$$` followed by a braced variable emits a literal dollar plus the salary,
applied partials render employee rows through anaphoric `it`, bracket
separators render only between applied partial results, indented bare partials
nest every rendered line under the source indentation, and a WordPress reviewer
packet keeps PlainText-rendered body output without leaking source admin URLs.
The mapped denominator is now 1,075 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-23 after the PlainText custom template
brace-delimiter/indented-partial slice: `php -l` passed for
`MarkdownWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-brace-partial-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-brace-partial-handoff.php`
emitted a custom plain WordPress reviewer packet with braced delimiter reviewer
rows, literal dollar budget output, nested checklist partial output, and no
source admin URL; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,640
assertions, and 0 failures. The focused file now contains 281 behavior tests.

Root aggregate verification on 2026-05-23 after the PlainText custom template
brace-delimiter/indented-partial slice was not assigned to this lane worker, so
no no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`MANUAL.txt` Template syntax, Pandoc's `src/Text/Pandoc/Templates.hs`, and the
supplemental `jgm/doctemplates` clone at
`2485bcbdb297d54d732006ca545a2a9e426f3817` were inspected again for
`test/pipes.test` (109 lines), `test/partials.test` (39 lines), and the
`enum.txt`, `boilerplate.txt`, and `partial_foo.txt` partial fixtures. The
native PHP slice maps eight additional focused checks from that boundary:
applied partial source expressions apply `pairs/reverse` before rendering the
partial, enum-style applied partials preserve the remaining newline from a
double-final-newline partial, recursive `chomp` trims map/list values before
`pairs/uppercase`, list values render through `roman` and `alpha` pipes before
bracket separators, and bare partial names resolve upstream `.txt` partial
files while direct partial inclusion still omits final partial newlines before
following template content. A local comparison against the supplemental
doctemplates `.test` corpus now reports zero fixture mismatches. The mapped
denominator is now 1,083 focused Markdown/HTML/WordPress checks.

Focused local verification on 2026-05-24 after the PlainText custom template
pipes/partial-resolution slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-plain-template-pipe-partial-fixture-handoff.php`; `php
lanes/pandoc/examples/wordpress-plain-template-pipe-partial-fixture-handoff.php`
emitted a custom plain WordPress reviewer packet with `pairs/reverse` check
rows rendered through a `.txt` partial, chomped uppercase owner metadata, roman
milestone lists, and no source admin URL; the local supplemental doctemplates
fixture comparison reported `doctemplates failures: 0`; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,646 assertions, and 0 failures. The focused file now contains 282
behavior tests.

Root aggregate verification on 2026-05-24 after the PlainText custom template
pipes/partial-resolution slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

`test/Tests/Writers/Native.hs`, `test/writer.native`,
`test/lhs-test-markdown.native`, and the leading `test/testsuite.native`
constructor shape were inspected for Pandoc Native writer behavior. The
upstream property boundary is small but broad: `p_write_rt` requires full
`Pandoc` native output to read back into the same document when a template is
present, while `p_write_blocks_rt` requires block-list output to read back into
the same bounded block list. The native PHP slice maps fourteen additional
focused checks from that boundary: full-document `Pandoc` output with `Meta
{ unMeta = fromList ... }`, block-list output without the `Pandoc` wrapper,
title/author/date `MetaInlines` and `MetaList` values, `MetaBool`/`MetaString`
values, `Header`, `Para`, `Plain`, `HorizontalRule`, `BulletList`,
`OrderedList`, `BlockQuote`, `LineBlock`, and `CodeBlock` constructors, attr
tuples, `Strong`, `Link`, and `Code` inlines, and Haskell-style string escaping
for code-block newlines.

Focused local verification on 2026-05-24 after the NativeWriter common-AST
slice: `php -l` passed for `NativeWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-native-review-packet-handoff.php`; `php
lanes/pandoc/examples/wordpress-native-review-packet-handoff.php` emitted a
standalone Pandoc Native reviewer packet with metadata, a source link,
checklist blocks, and escaped PHP code-block fixture text; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,663 assertions, and 0 failures. The focused file now contains 284
behavior tests.

Root aggregate verification on 2026-05-24 after the NativeWriter common-AST
slice was not assigned to this lane worker, so no no-argument root harness was
started. Per lane instructions, focused pandoc evidence was recorded and root
aggregate verification remains pending for the supervisor/integrator.

`test/command/html-read-figure.md`, `test/command/cite-in-inline-note.md`,
`test/markdown-citations.native`, and the Images section of
`test/testsuite.native` were inspected for Pandoc Native figure/citation
constructor behavior. The native PHP slice maps eight additional focused
checks from those fixtures: `Figure` Attr tuples, `Caption (Just [...])` short
captions, long caption block lists, Plain-wrapped `Image` figure bodies,
`Cite` wrappers, `Citation` prefix/suffix inline lists, `AuthorInText` and
`SuppressAuthor` modes, and note-number/hash fields. The mapped denominator is
now 1,105 focused Markdown/HTML/Native/WordPress checks.

Focused local verification on 2026-05-24 after the NativeWriter Figure/Cite
slice: `php -l` passed for `NativeWriter.php`, `MarkdownReaderTest.php`,
`examples/wordpress-native-citation-figure-handoff.php`, and
`examples/wordpress-native-review-packet-handoff.php`; `php
lanes/pandoc/examples/wordpress-native-citation-figure-handoff.php` emitted a
standalone Pandoc Native reviewer packet with metadata, `Figure`/`Image`
constructors, short/long captions, and `Cite`/`Citation` records; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,666 assertions, and 0 failures. The focused file now contains 285
behavior tests.

Root aggregate verification on 2026-05-24 after the NativeWriter Figure/Cite
slice was not assigned to this lane worker, so no no-argument root harness was
started. Per lane instructions, focused pandoc evidence was recorded and root
aggregate verification remains pending for the supervisor/integrator.

`test/pipe-tables.native`, `test/tables.native`,
`test/markdown-reader-more.native`, and `test/Tests/Writers/Native.hs` were
inspected again for Pandoc Native table constructor behavior. The native PHP
slice maps nine additional focused checks from those fixtures: table Attr
tuples, `Caption` short/long blocks, `ColSpec` alignment and width fields,
`TableHead` row lists, `TableBody` `RowHeadColumns` and body-local head-row
lists, `TableFoot` row lists, `Cell` Attr/Align/RowSpan/ColSpan fields,
inline cell content as `Plain` blocks, and block-cell content preserving
`Para`/`BulletList` children. The mapped denominator is now 1,114 focused
Markdown/HTML/Native/WordPress checks.

Focused local verification on 2026-05-24 after the NativeWriter Table slice:
`php -l` passed for `NativeWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-native-table-handoff.php`; `php
lanes/pandoc/examples/wordpress-native-table-handoff.php` emitted a standalone
Pandoc Native reviewer packet with table metadata, caption, colspecs,
row-head columns, spanned cells, and nested block-cell content; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,674 assertions, and 0 failures. The focused file now contains 286
behavior tests.

Root aggregate verification on 2026-05-24 after the NativeWriter Table slice
was not assigned to this lane worker, so no no-argument root harness was
started. Per lane instructions, focused pandoc evidence was recorded and root
aggregate verification remains pending for the supervisor/integrator.

`test/Tests/Writers/Native.hs` was inspected again for the upstream Native
writer read-back boundary. The native PHP slice maps twelve additional
focused checks: full-document Native read-back, blocks-only list read-back,
title/author/date/bool/list metadata parsing, Attr tuple parsing, Header/Para
and list/code-block parsing, Figure/Image read-back, Cite/Citation read-back,
Table colspec/head/body/foot read-back, body-local head rows, row/column spans,
and WordPress block emission from a NativeReader-parsed packet. The mapped
denominator is now 1,126 focused Markdown/HTML/Native/WordPress checks.

Focused local verification on 2026-05-24 after the NativeReader read-back
slice: `php -l` passed for `NativeReader.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-native-reader-handoff.php`; `php
lanes/pandoc/examples/wordpress-native-reader-handoff.php` emitted WordPress
heading, paragraph/link, and table blocks from a NativeReader-parsed packet;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
test file, 2,704 assertions, and 0 failures. The focused file now contains 288
behavior tests.

Root aggregate verification on 2026-05-24 after the NativeReader read-back
slice was not assigned to this lane worker, so no no-argument root harness was
started. Per lane instructions, focused pandoc evidence was recorded and root
aggregate verification remains pending for the supervisor/integrator.

## 2026-05-24 NativeReader Structural Fixture Slice

`test/testsuite.native`, `test/html-reader.native`, and
`test/docx/tables_separated_with_rawblock.native` were inspected again for
upstream Native structural fixture shapes. The new copied lane fixture
`fixtures/upstream-native-structure-slice.native` focuses on the bounded
Native reader/writer surface needed for WordPress review packets:
`DefinitionList` terms and multi-block definitions, `CodeBlock` and
`BlockQuote` children inside a definition body, nested `Div` blocks, `RawBlock`
passthrough for `html` and `openxml` formats, and the parenthesized
`(TableHead ...)` / `(TableFoot ...)` constructor shape used by upstream table
fixtures. The mapped denominator is now 1,144 focused
Markdown/HTML/Native/WordPress checks.

NativeReader now accepts both the unwrapped table section form emitted by prior
lane code and the upstream parenthesized forms. NativeWriter now emits
parenthesized `TableHead` and `TableFoot` constructors in the upstream table
shape. WordPressBlockWriter now keeps non-inline blocks inside definition-list
definitions as block HTML, so Native definition bodies preserve code blocks
and block quotes instead of flattening them to plain text.

Focused local verification on 2026-05-24 after the NativeReader structural
fixture slice: `php -l` passed for `NativeReader.php`, `NativeWriter.php`,
`WordPressBlockWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-native-upstream-structure-handoff.php`; `php
lanes/pandoc/examples/wordpress-native-upstream-structure-handoff.php` emitted
WordPress definition-list HTML, grouped raw HTML, and a table block from a
NativeReader-parsed fixture; targeted upstream-cache smoke checks parsed
`test/html-reader.native` as 245 blocks,
`test/docx/tables_separated_with_rawblock.native` as 3 blocks, and
`test/testsuite.native` as 239 blocks via `git show`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 2,730
assertions, and 0 failures. The focused file now contains 290 behavior tests.

Root aggregate verification on 2026-05-24 after the NativeReader structural
fixture slice was not assigned to this lane worker, so no no-argument root
harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

## 2026-05-24 NativeReader DOCX Inline Formatting Wrapper Slice

`test/docx/inline_formatting.native` was inspected from the upstream cache for
the DOCX reader's Native wrapper and inline-formatting output. The copied lane
fixture `fixtures/upstream-native-docx-inline-formatting.native` keeps the
upstream `Pandoc (Meta {unMeta = fromList []}) [...]` shape that previously
failed the bounded NativeReader because the meta constructor is parenthesized.
The mapped slice covers six focused checks: parenthesized full-document meta
wrapper parsing, five top-level DOCX formatting paragraphs, nested
`Emph`/`Strong` inlines, `SmallCaps` and `Strikeout`, `Underline`,
`Superscript`/`Subscript`, and `LineBreak` emission to WordPress block markup.
The mapped denominator is now 1,150 focused Markdown/HTML/Native/WordPress
checks.

NativeReader now accepts both unwrapped `Pandoc Meta ...` packets emitted by
the lane's NativeWriter and upstream-style `Pandoc (Meta ...)` packets. The
new WordPress example
`examples/wordpress-native-docx-inline-formatting-handoff.php` reads the
copied DOCX Native fixture and emits WordPress paragraphs that preserve
emphasis, strong/emphasis nesting, small caps, strikeout, underline,
superscript/subscript, and hard line breaks without invoking upstream Pandoc.

Focused local verification on 2026-05-24 after the NativeReader DOCX inline
formatting wrapper slice: `php -l` passed for `NativeReader.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-native-docx-inline-formatting-handoff.php`; `php
lanes/pandoc/examples/wordpress-native-docx-inline-formatting-handoff.php`
emitted the expected WordPress paragraph markup from a NativeReader-parsed
DOCX fixture; a targeted upstream-cache smoke check parsed
`test/docx/inline_formatting.native` as 5 blocks via `git show`; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 2,749 assertions, and 0 failures. The focused file now contains 291
behavior tests.

Root aggregate verification on 2026-05-24 after the NativeReader DOCX inline
formatting wrapper slice was not assigned to this lane worker, so no
no-argument root harness was started. Per lane instructions, focused pandoc
evidence was recorded and root aggregate verification remains pending for the
supervisor/integrator.

## 2026-05-24 NativeReader DOCX Review Span Slice

The upstream DOCX fixture subinventory was counted with `git ls-tree` from
`.upstream-cache/pandoc` at `0640c4c9859aa5a3ede082c190fcd5883c24ac83`:
233 artifacts under `test/docx`, including 112 `.native` expectations, 121
`.docx` inputs/goldens, and 38 `test/docx/golden` outputs. The targeted
review-span fixture set includes `test/docx/comments.native`,
`test/docx/comments_no_comments.native`, `test/docx/raw-blocks.native`,
`test/docx/raw-bookmarks.native`, and ten track-change `.native` fixtures.

This slice maps `test/docx/comments.native`,
`test/docx/track_changes_insertion_all.native`, and
`test/docx/track_changes_deletion_all.native`. The copied fixtures cover five
`comment-start` spans, five `comment-end` spans, one tracked insertion span,
one tracked deletion span, comment id/author/date attrs, tracked-change
author/date attrs, and a `LineBreak` inside a comment body. That slice brought
the mapped denominator to 1,162 focused Markdown/HTML/Native/WordPress checks.

WordPressBlockWriter now recognizes upstream DOCX review-span classes. Comment
spans render with `data-pandoc-comment-id`, `data-pandoc-comment-author`, and
`data-pandoc-comment-date` instead of raw upstream `author`/`date` attributes.
Tracked insertions render as `<ins>` and deletions as `<del>`, preserving
`data-pandoc-change-author`, `data-pandoc-change-date`, and `datetime`
metadata for reviewer-facing WordPress handoffs. At that point the mapped
denominator was 1,162 focused Markdown/HTML/Native/WordPress checks; the later
raw OpenXML, paragraph-change, and custom-style slices above raise it to 1,203.

Focused local verification on 2026-05-24 after the NativeReader DOCX review
span slice: `php -l` passed for `WordPressBlockWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-native-docx-review-spans-handoff.php`; `php
lanes/pandoc/examples/wordpress-native-docx-review-spans-handoff.php` emitted
WordPress paragraph markup with comment metadata and tracked ins/del spans; a
targeted upstream-cache smoke check parsed `test/docx/comments.native` as 4
blocks, `test/docx/track_changes_insertion_all.native` as 1 block, and
`test/docx/track_changes_deletion_all.native` as 1 block via `git show`;
`php tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1
test file, 2,774 assertions, and 0 failures. The focused file then contained
293 behavior tests; the later raw OpenXML, paragraph-change, and custom-style
slices above raise it to 297 tests and 2,822 assertions.

Root aggregate verification on 2026-05-24 after the NativeReader DOCX review
span slice was not assigned to this lane worker, so no no-argument root
harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

## 2026-05-24 MarkdownWriter Wikilink Writer Slice

`test/command/wikilinks_title_after_pipe.md` and
`test/command/wikilinks_title_before_pipe.md` were inspected from the upstream
cache for the Markdown/CommonMark writer examples. The new native PHP slice
maps ten focused Markdown/WordPress checks: four upstream title-after-pipe
writer outputs, four upstream title-before-pipe writer outputs, one
class-preservation fallback guard for links that are not a pure `.wikilink`,
and one WordPress reviewer-packet handoff for migration runbook/checklist
shortcuts. The mapped denominator is now 1,459 focused
Markdown/HTML/Native/WordPress checks.

MarkdownWriter now renders class-only `wikilink` links as Pandoc wiki-link
shortcuts when `wikilinksTitleAfterPipe`, `wikilinksTitleBeforePipe`, or a
matching variant string is enabled. It keeps same-label links compact,
percent-encodes target spaces as `%20`, supports both `[[target|title]]` and
`[[title|target]]`, and falls back to ordinary Markdown link rendering when
extra classes or attributes would otherwise be dropped. The new WordPress
example `examples/wordpress-markdown-wikilink-writer-handoff.php` emits
reviewer Markdown for legacy wiki shortcuts without shelling out to Pandoc.

Focused local verification on 2026-05-24 after the MarkdownWriter wikilink
writer slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-wikilink-writer-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-wikilink-writer-handoff.php` emitted
title-after-pipe and title-before-pipe wiki-link reviewer Markdown; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test
file, 3,125 assertions, and 0 failures. The focused file now contains 315
behavior tests.

Root aggregate verification on 2026-05-24 after the MarkdownWriter wikilink
writer slice was not assigned to this lane worker, so no no-argument root
harness was started. Per lane instructions, focused pandoc evidence was
recorded and root aggregate verification remains pending for the
supervisor/integrator.

## 2026-05-24 NativeReader DOCX Notes Slice

`test/docx/notes.native` and `test/docx/link_in_notes.native` were inspected
from the upstream cache for bounded DOCX note/endnote behavior. The copied
lane fixtures `fixtures/upstream-native-docx-notes.native` and
`fixtures/upstream-native-docx-link-in-notes.native` map twelve focused
checks: two DOCX note nodes, one endnote node, the source heading id, one link
inside a note, NativeWriter round-trip guards for `Note` and `Link`
constructors, and WordPress endnote output with backlinks. The mapped
denominator is now 1,471 focused Markdown/HTML/Native/WordPress checks.

NativeReader already supports Pandoc Native `Note` and `Link` constructors;
this slice pins that behavior against upstream DOCX Native fixtures and
exercises the WordPress endnote handoff without invoking upstream Pandoc or
activating DOCX ZIP/OpenXML parsing. The new WordPress example
`examples/wordpress-native-docx-notes-handoff.php` reads the copied fixtures,
combines them into one Native AST document, and emits reviewer-safe WordPress
endnotes while preserving the link inside the final note.

Focused local verification on 2026-05-24 after the NativeReader DOCX notes
slice: `php -l` passed for `MarkdownReaderTest.php` and
`examples/wordpress-native-docx-notes-handoff.php`; `php
lanes/pandoc/examples/wordpress-native-docx-notes-handoff.php` emitted
WordPress endnotes with backlinks and a preserved note link; `diff -u` checks
against `git -C .upstream-cache/pandoc show HEAD:test/docx/notes.native` and
`HEAD:test/docx/link_in_notes.native` confirmed both copied fixtures match
upstream exactly; the first focused test run exposed one overly strict
NativeWriter line-wrapping assertion, which was corrected; `php
tools/run-tests.php lanes/pandoc/tests/MarkdownReaderTest.php` then passed 1
test file, 3,141 assertions, and 0 failures. The focused file now contains 316
behavior tests.

Root aggregate verification on 2026-05-24 after the NativeReader DOCX notes
slice was not assigned to this lane worker, so no no-argument root harness was
started. Per lane instructions, focused pandoc evidence was recorded and root
aggregate verification remains pending for the supervisor/integrator.

## 2026-05-24 MarkdownWriter Standalone TOC Slice

`test/command/toc.md` was inspected from the upstream cache for Pandoc's
standalone Markdown writer TOC behavior. The mapped slice covers fourteen
focused checks: standalone TOC insertion, six rendered TOC entries, generated
duplicate heading fragments such as `b-1` and `b-2`, TOC link ids such as
`toc-a`, exclusion of divs with multiple minimum-level headings, inclusion of
divs with one minimum-level heading, and a WordPress reviewer-packet example.
The mapped denominator is now 1,485 focused Markdown/HTML/Native/WordPress
checks.

MarkdownWriter now uses the existing TOC candidate logic for standalone
Markdown output when the template/standalone and TOC options are enabled. It
keeps the existing PlainText template TOC behavior, while Markdown output emits
Pandoc-style link attributes in the TOC and uses a local duplicate-id counter
so generated fragments match the body heading order without consuming the
body-writer heading-id state. The new WordPress example
`examples/wordpress-markdown-toc-handoff.php` emits a standalone Markdown
review packet for migration batches, keeping interior scratch sections in the
body but out of the TOC.

Focused local verification on 2026-05-24 after the MarkdownWriter standalone
TOC slice: `php -l` passed for `MarkdownWriter.php`,
`MarkdownReaderTest.php`, and
`examples/wordpress-markdown-toc-handoff.php`; `php
lanes/pandoc/examples/wordpress-markdown-toc-handoff.php` emitted a standalone
reviewer Markdown TOC with source/media/publish sections and omitted interior
scratch headings; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 3,147
assertions, and 0 failures; `git diff --check` passed for the touched Pandoc
files. The focused file now contains 317 behavior tests.

Root aggregate verification on 2026-05-24 after the MarkdownWriter standalone
TOC slice was not assigned to this lane worker, so no no-argument root harness
was started. Per lane instructions, focused pandoc evidence was recorded and
root aggregate verification remains pending for the supervisor/integrator.

## 2026-05-24 HTML Writer Styled Inline Constructor Slice

`src/Text/Pandoc/Writers/HTML.hs` was inspected from the upstream cache at
`0640c4c9859aa5a3ede082c190fcd5883c24ac83` for the bounded inline constructor
branches around `Underline`, `Strikeout`, `SmallCaps`, `Superscript`, and
`Subscript`. This slice maps twelve focused checks: five upstream HTML inline
constructor branches, nested inline/escaping behavior inside those wrappers,
link output inside superscript, and a WordPress review-preview handoff. The
mapped denominator is now 2,039 focused Markdown/HTML/Native/WordPress checks.

HtmlWriter now renders those constructors as native HTML: underline as `<u>`,
strikeout as `<del>`, small caps as `<span class="smallcaps">`, superscript as
`<sup>`, and subscript as `<sub>`. The nodes are also treated as inline nodes
when nested in figure/table/list contexts. This is a writer-only behavior slice:
it does not invoke upstream Pandoc, activate package ZIP/OpenXML/OpenDocument
parsing, fetch media, run external PDF/TeX engines, or open citation/CSL,
PlainMath/MathML, or syntax-highlighting dependency gates.

The new WordPress example
`examples/wordpress-html-writer-styled-inline-handoff.php` emits an HTML
preview block for editorial underline/deletion/small-caps marks plus H2O-style
subscript and source-note superscript links, then wraps that preview in a
WordPress HTML block for reviewer handoff.

Focused local verification on 2026-05-24 after the HTML writer styled-inline
slice: `php -l` passed for `HtmlWriter.php`, `MarkdownReaderTest.php`, and
`examples/wordpress-html-writer-styled-inline-handoff.php`; `php
lanes/pandoc/examples/wordpress-html-writer-styled-inline-handoff.php` emitted
the expected HTML preview and WordPress review block; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 test file, 3,798
assertions, and 0 failures. The focused file now contains 376 behavior tests.

Root aggregate verification on 2026-05-24 after this slice was not assigned to
this lane worker, so no no-argument root harness was started. Per lane
instructions, focused pandoc evidence was recorded and root aggregate
verification remains pending for the supervisor/integrator.

## 2026-06-21 EPUB3 SMIL Media Overlay Package Slice

The compact direct EPUB reader now preserves OPF manifest `media-overlay`
relationships and parses bounded SMIL `par` entries into document metadata.
For each referenced overlay, `EpubReader` records the content item, overlay
resource path, text fragment targets, audio targets, and clip begin/end
timings. The data remains review metadata; the PHP port does not attempt media
playback, audio decoding, external fetches, or EPUBCheck validation.

The bounded EPUB3 writer now preserves those same relationships on export when
metadata supplies `epubMediaOverlays`, `mediaOverlays`, or read-back
`epubSpineItemRefs`. `EpubWriter` emits OPF `media-overlay` attributes on
chapter manifest items, writes packaged SMIL resources as `application/smil+xml`,
preserves valid overlay manifest IDs, and round-trips the generated package back
through `EpubReader` for metadata verification.

Focused verification on 2026-06-21: `php -l` passed for
`lanes/pandoc/src/EpubWriter.php` and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 167 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests/PandocFormatRegistryTest.php
lanes/pandoc/tests/EpubWriterTest.php lanes/pandoc/tests/EpubReaderTest.php`
passed 3 files, 283 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,430 assertions, and 0 failures.

## 2026-06-21 EPUB3 HTML5 Media Resource Package Slice

`EpubReader` and `EpubWriter` now treat HTML5 `poster` attributes as packaged
resource references alongside `src` and `href`. This keeps video poster images
in the same bounded package-relative rewrite path as images, CSS, audio, video,
captions, and other local assets.

`EpubWriter` now recognizes common audio/video/caption extensions when building
the OPF manifest, including WebVTT captions, Ogg audio/video, WebM video, MP3,
M4A, AAC, FLAC, Opus, and WAV. The focused writer test packages a chapter with
`video`, nested `source`, `track`, `poster`, and `audio` references, verifies
the OPF media types, checks chapter-relative URL rewriting, and round-trips the
generated EPUB through `EpubReader` to confirm resource provenance.

Focused verification on 2026-06-21 after the HTML5 media resource slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 181 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,444 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, media playback, external
asset fetch, or browser media decoding was executed in this slice.

## 2026-06-21 EPUB3 Semantic Spine Footnote/Pagebreak Slice

`EpubWriter` now post-processes generated spine XHTML with EPUB-specific
semantics that the shared HTML reader already understands on import. Footnote
references get `epub:type="noteref"`, generated footnote containers get
`epub:type="footnotes"` and `role="doc-endnotes"`, generated footnote list
items get `epub:type="footnote"`, and classed pagebreak spans/divs get
`role="doc-pagebreak"` plus `epub:type="pagebreak"`.

The focused regression covers a generated EPUB chapter with a formatted
footnote and a classed pagebreak span. It verifies the XHTML semantics inside
the package and then reads the generated EPUB back through `EpubReader`,
confirming the footnote body is recovered as a native `note` node instead of a
reference with an empty note body.

Focused verification on 2026-06-21 after the semantic spine slice: `php -l`
passed for `lanes/pandoc/src/EpubWriter.php` and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 190 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,453 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, assistive-technology audit,
or full EPUB fixture corpus was executed in this slice.

## 2026-06-21 EPUB3 MathML Spine Package Slice

`HtmlWriter` now recognizes the `mathml` writer method for explicit MathML
payloads carried on `math` AST nodes. When a node supplies a `mathml` or `html`
attribute containing a `<math>` element, the writer preserves that MathML,
adds the MathML namespace when missing, and adds `display="block"` for display
math payloads when needed. Nodes without an explicit MathML payload continue to
fall back to visible TeX text; this slice does not claim generic TeX-to-MathML
conversion.

`EpubWriter` now detects MathML in chapter AST content and marks the
corresponding XHTML spine manifest item with `properties="mathml"`. The focused
round-trip test writes an EPUB3 package with an explicit MathML payload,
verifies the OPF manifest property and preserved MathML in the chapter, then
reads the EPUB back through `EpubReader` to confirm the embedded TeX annotation
is recovered as a native `math` node.

Focused verification on 2026-06-21 after the MathML spine package slice:
`php -l` passed for `lanes/pandoc/src/HtmlWriter.php`,
`lanes/pandoc/src/EpubWriter.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 196 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,459 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, generic TeX-to-MathML
conversion, or full EPUB fixture corpus was executed in this slice.

## 2026-06-21 EPUB3 Spine Manifest Property Slice

`EpubWriter` now derives OPF manifest properties for XHTML spine items from the
chapter AST before packaging. Chapters that contain explicit MathML still get
`mathml`; chapters that contain inline SVG now get `svg`; chapters that contain
raw script elements or event-handler attributes now get `scripted`; and chapters
that reference remote `http`/`https` media/object resources now get
`remote-resources`.

The focused regression writes a chapter with inline SVG, a remote image, and a
script element, verifies `properties="svg scripted remote-resources"` in the
OPF package item, verifies the XHTML payload remains present, and reads the EPUB
back through `EpubReader` to confirm the item properties are exposed on the
manifest resource metadata.

Focused verification on 2026-06-21 after the spine manifest property slice:
`php -l` passed for `lanes/pandoc/src/HtmlWriter.php`,
`lanes/pandoc/src/EpubWriter.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 201 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,464 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, remote-resource fetch,
script execution, SVG rendering audit, or full EPUB fixture corpus was executed
in this slice.

## 2026-06-21 EPUB3 Dublin Core Metadata Slice

`EpubReader` now recognizes the full core Dublin Core element set commonly used
inside OPF metadata: `title`, `creator`, `contributor`, `date`, `language`,
`identifier`, `subject`, `description`, `publisher`, `rights`, `source`,
`relation`, `coverage`, `type`, and `format`. The reader continues to map
`creator` to the shared `author` key and `language` to `lang` for the existing
Pandoc metadata shape.

`EpubWriter` now emits the additional OPF Dublin Core fields carried in document
metadata: `contributor`, `publisher`, `date`, `type`, `format`, `source`,
`relation`, `coverage`, and `rights`, alongside the existing identifier, title,
language, creators, subjects, and description. The focused regression verifies
the generated `package.opf` and then round-trips the EPUB through `EpubReader`
to prove the metadata is recoverable.

Focused verification on 2026-06-21 after the Dublin Core metadata slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 234 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,497 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, authority-file validation,
identifier normalization audit, or full EPUB fixture corpus was executed in this
slice.

## 2026-06-21 EPUB3 OPF Package Structure Slice

`EpubReader` now records bounded OPF package-structure metadata that was
previously dropped during direct EPUB ingestion: package `version`,
`unique-identifier`, `prefix`, `dir`, and `xml:lang` attributes; the selected
unique identifier value when multiple `dc:identifier` elements are present;
manifest `fallback` and `fallback-style` relationships; OPF `bindings`; and
nested EPUB3 `collection` entries with collection metadata and links.

`EpubWriter` now consumes the same metadata shape on export. It emits package
attributes, preserves manifest ids for metadata-carried resource payloads, emits
manifest fallback/fallback-style attributes, writes OPF `bindings`, and writes
nested `collection` records with relative package links. The focused writer
regression verifies the generated `package.opf` and then reads the EPUB back
through `EpubReader` to prove the structure survives round-trip.

Focused verification on 2026-06-21 after the OPF package-structure slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 277 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,540 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, full OPF vocabulary
validation, external linked-resource fetch, or full EPUB fixture corpus was
executed in this slice.

## 2026-06-21 EPUB3 XHTML Spine Normalization Slice

`EpubWriter` now applies EPUB-specific XHTML normalization to generated spine
documents after HTML rendering, EPUB semantics, and package-relative URL
rewriting. Raw HTML fragments inside the AST are normalized so common HTML void
elements (`br`, `img`, `input`, `source`, `track`, and related tags) become
XML self-closing tags, common HTML boolean attributes are expanded with explicit
values, HTML named entities are converted to XML-safe text, bare ampersands are
escaped, and raw `script`/`style` text that contains XML-sensitive characters is
escaped.

The focused regression writes an EPUB3 chapter containing browser-style raw
HTML media, form, named-entity, void-element, and script fragments. It verifies
the resulting `chapter.xhtml` contains normalized XHTML, parses the full spine
document with `DOMDocument::loadXML`, and reads the generated EPUB back through
`EpubReader` to confirm packaged resource references remain recoverable.

Focused verification on 2026-06-21 after the XHTML spine normalization slice:
`php -l` passed for `lanes/pandoc/src/EpubWriter.php` and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 288 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,551 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, arbitrary HTML5 tree
construction audit, JavaScript execution, browser rendering, or full EPUB
fixture corpus was executed in this slice.

## 2026-06-21 EPUB3 Fixed-Layout Viewport Slice

`EpubReader` now records XHTML spine `<meta name="viewport">` declarations as
fixed-layout viewport metadata on `epubSpineItemRefs`, `epubViewports`, and
`epubViewport`. `EpubWriter` now emits matching viewport declarations in
generated XHTML spine heads from per-spine or global metadata/options when
valid width/height dimensions are available.

The focused regression covers a fixed-layout, split-spine EPUB with separate
viewport dimensions on each XHTML page. It verifies generated split chapters
carry the expected viewport meta tags and that the package reads back through
`EpubReader` with normalized viewport metadata.

Focused verification on 2026-06-21 after the fixed-layout viewport slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 294 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,557 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, browser rendering, or full
EPUB fixture corpus was executed in this slice.

## 2026-06-21 EPUB3 Hierarchical Navigation Slice

`EpubWriter` now renders generated `nav.xhtml` TOC, landmarks, and page-list
entries through a shared level-aware list renderer. Heading-derived TOC entries
now preserve nested heading levels in nested EPUB3 `<ol>` lists, and
metadata-provided page-list or landmark entries can carry their level structure
through export and read-back.

The focused regression covers a split chapter EPUB whose `h2` section must nest
under its `h1` chapter in the generated TOC, plus a page-list entry with a
level-2 child. It verifies the generated `nav.xhtml` nesting and confirms the
page-list hierarchy reads back through `EpubReader` with the correct levels.

Focused verification on 2026-06-21 after the hierarchical navigation slice:
`php -l` passed for `lanes/pandoc/src/EpubWriter.php` and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 297 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,560 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, browser/a11y navigation audit,
or full EPUB fixture corpus was executed in this slice.

## 2026-06-21 EPUB3 Spine Language/Direction Slice

`EpubReader` now records valid XHTML spine root `lang`/`xml:lang` and `dir`
values on `epubSpineItemRefs` alongside the existing fixed-layout viewport
metadata. `EpubWriter` now emits per-spine `lang`, `xml:lang`, and `dir`
attributes for generated split chapters from metadata or options, with the
existing document-level language fallback preserved.

The focused regression extends the fixed-layout split-spine coverage with two
distinct language/direction pairs. It verifies imported XHTML pages preserve
`pl`/`ltr` and `ar`/`rtl` metadata, generated chapters emit those attributes on
the XHTML root, and read-back through `EpubReader` keeps the per-spine values.

Focused verification on 2026-06-21 after the spine language/direction slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 303 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,566 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, browser rendering,
language-tag conformance suite, or full EPUB fixture corpus was executed in
this slice.

## 2026-06-21 EPUB3 Non-Linear Spine Output Slice

`EpubWriter` now emits resource-backed non-linear spine itemrefs for XHTML/HTML
resources that are already present in the package manifest. The writer accepts
metadata-carried `epubSpineItemRefs` or explicit `nonLinearSpineItems`, resolves
package-relative `path`/`href` values, skips duplicate linear spine ids, writes
`linear="no"` plus itemref properties, and keeps those resources out of the
linear reading body on read-back.

The slice also narrows linear spine property extraction from `epubSpineItemRefs`
so scalar fields such as `idref` are not accidentally tokenized into OPF
`properties` attributes. The focused regression packages a synthetic appendix
XHTML resource, verifies the OPF manifest and non-linear itemref, verifies the
landmark nav link, reads the generated EPUB back through `EpubReader`, and
confirms the appendix remains non-linear provenance instead of body content.

Focused verification on 2026-06-21 after the non-linear spine output slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 226 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 317 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files, 6,580
assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, full OPF vocabulary
validation, or full EPUB fixture corpus was executed in this slice.

## 2026-06-21 EPUB3 Metadata Refinement/Link Slice

`EpubReader` now preserves bounded EPUB3 OPF package metadata records beyond
plain Dublin Core text. Package-level `<meta property="...">` records are
recorded as `epubMetadataProperties`, including `id`, `refines`, `scheme`,
`xml:lang`, and `dir` attributes when present, while the existing
`epubProperties` index remains available for property-value lookup. Package
metadata `<link>` records are recorded as `epubMetadataLinks` with
package-relative `href` values, `rel`, `mediaType`, `id`, `refines`, and
property tokens.

`EpubWriter` now consumes `epubMetadataProperties`/`metadataProperties` and
`epubMetadataLinks`/`metadataLinks` to emit those records back into
`package.opf`. This preserves identifier refinements, accessibility metadata,
and linked record metadata through read-back without invoking upstream Pandoc or
an external EPUB validator.

Focused verification on 2026-06-21 after the metadata refinement/link slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 336 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,599 assertions, and 0 failures, with
625 counted `PASS` lines in `/tmp/pandoc-full-epub-metadata-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full package metadata
vocabulary validation, authority-file validation, or full EPUB fixture corpus
was executed in this slice.

## 2026-06-21 EPUB3 Per-Spine Rendition Refinement Slice

`EpubReader` now scopes OPF `rendition:*` metadata refinements to the
referenced spine item instead of promoting them to global package rendition
metadata. Package records such as `<meta property="rendition:layout"
refines="#page1">pre-paginated</meta>` are still preserved in
`epubMetadataProperties`, but matching `epubSpineItemRefs` now also carry
`metadataProperties`, `renditionLayout`, `renditionOrientation`, and
`renditionSpread` fields where applicable.

`EpubWriter` now emits per-spine rendition refinements for generated chapter
manifest ids. Metadata carried on `epubSpineItemRefs` is written as
`<meta property="rendition:*" refines="#chapter-n">...</meta>`, and stale
source-package spine ids are suppressed when a read EPUB is written back with
new generated chapter ids.

Focused verification on 2026-06-21 after the per-spine rendition refinement
slice: `php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 354 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,617 assertions, and 0 failures, with
627 counted `PASS` lines in
`/tmp/pandoc-full-epub-spine-rendition-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full package metadata
vocabulary validation, full EPUB fixture corpus, or generic TeX-to-MathML
conversion was executed in this slice.

## 2026-06-22 EPUB OCF Mimetype Local Header Diagnostics Slice

`EpubReader` now parses the first ZIP local file header from the EPUB bytes and
records `localHeaderName` plus `localHeaderExtraBytes` on
`epubOcfMimetype`. The OCF mimetype diagnostics now emit
`ocf-mimetype-extra-field` when the required top-level `mimetype` entry has a
local ZIP extra field, while preserving the existing missing, not-first,
unreadable, exact-content, compressed, and encrypted mimetype checks.

The focused regression patches an otherwise readable EPUB ZIP so the first
local header keeps `mimetype` first, stored, and byte-exact, but includes a
four-byte local extra field. Read-back proves the diagnostic is sourced from
the archive bytes rather than invoice/demo strings or `ZipArchive` central
directory metadata.

Focused verification on 2026-06-22 after the OCF local-header slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php` and
`lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 1,212 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
lanes/pandoc/tests/EpubWriterTest.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 3 files, 6,435 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 8,245 assertions, and 0 failures, with
713 counted `PASS` lines in `/tmp/pandoc-full-epub-ocf-local-header.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
browser rendering audit, remote-resource fetching, generated multi-rendition
authoring beyond preserved OPF payloads, DRM decryption, XML signature
cryptographic validation, generic TeX-to-MathML conversion, or full EPUB2
output parity is claimed.

## 2026-06-22 EPUB3 HTML5 Semantic Descendant Metadata Slice

`EpubReader` now records additional EPUB3 spine descendant metadata for
`details`, `summary`, HTML5 media/resource elements (`audio`, `video`,
`source`, `track`, `object`, `embed`), inline data elements (`bdi`, `data`,
`meter`, `output`, `time`), ruby annotations, and MathML roots. Resource-bearing
attributes are normalized to package-relative paths, `srcset` candidates are
rewritten consistently, and stable element-specific attributes such as
`controls`, `poster`, `kind`, `srclang`, `value`, `for`, `datetime`,
`display`, and `alttext` are kept on `epubSpineItemRefs[*].semanticElements`.

The focused regressions extend the existing HTML5 inline and details fixtures
and add a media/math/ruby EPUB3 fixture covering video sources, caption tracks,
audio sources, object data, picture `srcset`, ruby readings, and MathML display
metadata. This keeps source spine semantics available to WordPress ingestion
layers even when the visible block body is represented as raw HTML or simplified
Pandoc inline nodes.

Focused verification on 2026-06-22 after the HTML5 semantic descendant metadata
slice: `php -l` passed for `lanes/pandoc/src/EpubReader.php` and
`lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 311 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
passed 1 file, 638 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-22 EPUB3 Package Rendition Viewport Fallback Slice

`EpubWriter` now treats package-level `epubRenditionViewport` and
`renditionViewport` metadata as valid XHTML chapter viewport fallbacks. A
document that declares only OPF `rendition:viewport` metadata now emits both the
package-level `<meta property="rendition:viewport">` record and a matching
chapter-level `<meta name="viewport">` tag, instead of requiring duplicate
`epubViewport` metadata.

The focused regression writes a fixed-layout EPUB3 document with only
`epubRenditionViewport`, verifies the OPF and XHTML viewport outputs, and
round-trips the package through `EpubReader` to confirm both
`epubRenditionViewport` and the spine item's `viewport` metadata preserve the
same extended viewport properties.

Focused verification on 2026-06-22 after the package rendition viewport
fallback slice: `php -l` passed for `lanes/pandoc/src/EpubWriter.php` and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubWriterTest.php` passed 1 file, 643 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
passed 1 file, 321 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-22 EPUB3 Path-Qualified Footnote Reference Slice

`MarkdownReader` now resolves semantic HTML/EPUB note references whose `href`
contains a local fragment behind a relative document path, such as
`chapter.xhtml#fn1` after EPUB package path rewriting. The shared HTML reader
also treats ARIA roles as token lists, recognizes namespace-aware `epub:type`
attributes, and removes footnote backlinks marked with `epub:type="backlink"`
or Pandoc-style `footnote-back` classes in addition to `role="doc-backlink"`.

The focused EPUB regression builds a package whose spine XHTML uses
`href="chapter.xhtml#fn1"` for the noteref and an `epub:type="backlink"`
return link inside a standalone `aside epub:type="footnote"`. Read-back now
converts that source into a native `note`, removes the standalone aside from the
visible body, and emits normalized WordPress footnote markup without leaking the
path-qualified source href or duplicate backlink text.

Focused verification on 2026-06-22 after the path-qualified footnote reference
slice: `php -l` passed for `lanes/pandoc/src/MarkdownReader.php`,
`lanes/pandoc/src/EpubReader.php`, and `lanes/pandoc/tests/EpubReaderTest.php`;
`php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php` passed 1 file,
321 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,540 assertions, and
0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
passed 1 file, 638 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-22 EPUB3 Table Rowspan Row-Head Fidelity Slice

`HtmlWriter` now tracks active rowspans while rendering table head, body, and
foot rows. Body rows whose first logical column is already covered by a spanning
row-head cell now skip that occupied logical column before deciding whether the
next physical cell should be `<th>` or `<td>`, keeping later cells aligned with
the original table grid.

`MarkdownReader` now mirrors that rowspan occupancy when inferring
`rowHeadColumns` from XHTML tables. A first-column `<th rowspan="2">` therefore
continues to count as the row-head column on the covered following row instead
of collapsing read-back row-head metadata to zero.

The focused EPUB3 writer regression extends the complex table fixture with a
body row header carrying `rowspan="2"`. Generated XHTML now emits the spanning
row header as `<th rowspan="2">`, keeps the following row's first physical cell
as `<td>`, and round-trips caption text, column widths, alignments, header
background styles, `rowHeadColumns`, `colspan`, `rowspan`, and review
`data-*` attributes through EPUB read-back.

Focused verification on 2026-06-22 after the table rowspan row-head fidelity
slice: `php -l` passed for `lanes/pandoc/src/MarkdownReader.php`,
`lanes/pandoc/src/HtmlWriter.php`, and `lanes/pandoc/tests/EpubWriterTest.php`;
`php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php` passed 1 file,
644 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,540 assertions, and
0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php`
passed 1 file, 321 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
browser table-layout rendering, multi-rendition authoring validation, DRM
decryption, XML signature validation, or generic TeX-to-MathML conversion was
executed in this slice.

## 2026-06-22 EPUB3 Nav Entry Link Attribute Slice

`EpubReader` now preserves link-level XHTML nav anchor metadata on TOC,
landmark, page-list, and auxiliary navigation entries. Source nav labels can
carry `rel`, `hreflang`, `media`, and `target` alongside the existing
EPUB/ARIA/id/class/title/language/direction/hidden metadata without those
attributes being flattened away during package ingestion.

`EpubWriter` now carries the same nav-entry fields through metadata
normalization and emits them back onto clickable `<a>` labels in generated
`nav.xhtml`. Non-clickable nav parent labels remain `<span>` labels and do not
receive anchor-only fields.

The focused regressions extend the nav-section reader fixture with a
source-authored TOC anchor carrying `rel`, `hreflang`, `media`, and `target`,
and extend the rich generated TOC writer fixture to prove those fields appear in
the packaged nav document and survive EPUB read-back.

Focused verification on 2026-06-22 after the nav entry link attribute slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, `lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 321 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests/EpubWriterTest.php`
passed 1 file, 644 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,294 assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
browser/a11y navigation audit, remote-resource fetching, multi-rendition
authoring validation, DRM decryption, XML signature validation, or generic
TeX-to-MathML conversion was executed in this slice.

## 2026-06-22 EPUB3 XHTML Head Base Slice

`EpubReader` now preserves XHTML spine `<head><base>` metadata on
`epubSpineItemRefs[*].headBases`. Local base `href` values are normalized to
package-relative directory paths, and XHTML body resources, head links, head
styles, `srcset` candidates, and CSS URL references resolve through that base
while still tracking package-relative `epubReferencedResources`.

`EpubWriter` now emits metadata-provided head bases into generated XHTML spine
heads. Package-relative base `href` values are mapped back to chapter-relative
paths, and trailing slash directory bases are preserved so generated packages
read back with the same resource-base behavior.

The focused reader regression builds a source EPUB3 package whose chapter head
contains `<base href="../assets/">` and verifies linked stylesheets, style-block
URLs, body images, and `srcset` candidates resolve against `OEBPS/assets/`.
The focused writer regression builds a generated EPUB3 package with
`headBases` metadata, verifies the emitted chapter-relative `<base>` element,
and proves read-back resolves a raw body image through the generated base.

Focused verification on 2026-06-22 after the XHTML head-base slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, `lanes/pandoc/tests/EpubReaderTest.php`,
and `lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 1,019 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,348 assertions, and 0 failures, with
674 counted `PASS` lines in
`/tmp/pandoc-full-epub-head-base-20260622T025312Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
browser base-url/resource rendering audit, remote-resource fetching,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-22 EPUB3 XHTML Head Script Slice

`EpubReader` now preserves XHTML spine `<head><script>` metadata on
`epubSpineItemRefs[*].headScripts`. Script records keep local or remote `src`,
inline script text, `type`, `charset`, `crossorigin`, `integrity`,
`referrerpolicy`, `nonce`, and `async`/`defer`/`nomodule` booleans. Local script
`src` values are normalized to package-relative metadata paths during read-back
and tracked by the existing resource-reference pass.

`EpubWriter` now emits metadata-provided head scripts into generated XHTML spine
heads, accepts inline script text via `script`/`content`/`text`, maps
package-relative script `src` values back to chapter-relative paths, XML-escapes
inline script text, and marks generated XHTML spine manifest items as
`scripted` when `headScripts` metadata is present.

The focused writer regression builds an EPUB3 package with an external module
script resource and an inline head script. It verifies OPF `scripted` manifest
properties, generated chapter-relative script `src`, XML-safe inline script
output, read-back `headScripts` metadata, referenced-resource tracking, and
extracted script payload round-trip.

Focused verification on 2026-06-22 after the XHTML head-script slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, `lanes/pandoc/tests/EpubReaderTest.php`,
and `lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 998 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,327 assertions, and 0 failures, with
672 counted `PASS` lines in
`/tmp/pandoc-full-epub-head-scripts-20260622T022401Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
browser script execution audit, remote-resource fetching, multi-rendition
authoring validation, DRM decryption, XML signature validation, or generic
TeX-to-MathML conversion was executed in this slice.

## 2026-06-22 EPUB3 Rich XHTML Head Link Slice

`EpubReader` now preserves a bounded richer XHTML spine `<head><link>` metadata
surface instead of keeping only the basic stylesheet/profile attributes. Head
links retain `as`, `sizes`, `crossorigin`, `integrity`, `referrerpolicy`,
`fetchpriority`, `blocking`, `color`, `imagesizes`, `imagesrcset`, and
`disabled` metadata. Package-relative `imagesrcset` candidates are normalized
to package paths during read-back and tracked in `epubReferencedResources`.

`EpubWriter` now emits that same bounded head-link attribute set from
`epubSpineItemRefs[*].links`, accepts camelCase aliases such as
`referrerPolicy`, `fetchPriority`, `imageSrcset`, and `imageSizes`, and maps
package-relative `imagesrcset` candidates back to chapter-relative XHTML paths.
This keeps responsive preload/icon/resource-hint provenance available to the
WordPress ingestion path and keeps generated EPUB spine heads package-relative
on read, chapter-relative on write.

The focused writer regression extends the existing head-link fixture with a
local image preload link carrying CORS/integrity/referrer/fetch/blocking/color,
`sizes`, `imagesizes`, `imagesrcset`, and `disabled` metadata. It verifies OPF
resource manifest output, generated chapter-relative `imagesrcset`, read-back
metadata, referenced-resource tracking for both candidates, and extracted image
payload round-trip.

Focused verification on 2026-06-22 after the rich XHTML head-link slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, and
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 990 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,319 assertions, and 0 failures, with
671 counted `PASS` lines in
`/tmp/pandoc-full-epub-head-link-rich-20260622T021848Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
browser preload/resource rendering audit, remote-resource fetching,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-22 EPUB3 External Stylesheet Resource URL Slice

`EpubReader` now normalizes retained CSS resource payloads during opt-in EPUB
asset extraction. External `text/css` payload references written as `url(...)`
or string `@import "..."` are converted from stylesheet-relative paths into
package-relative metadata, and those referenced resources are recorded in
`epubReferencedResources` while preserving query/fragment suffixes.

`EpubWriter` now performs the inverse mapping when packaging CSS resources:
package-relative `url(...)` and string `@import` references are emitted relative
to the CSS resource directory, while existing XHTML body/head style rewriting
continues to emit chapter-relative paths. This keeps linked stylesheets,
imported stylesheets, font files, and background image resources round-trippable
through native PHP EPUB3 read/write.

The focused reader regression extends the extracted asset fixture with
`EPUB/styles/book.css` importing `theme.css`, referencing a font with
query/fragment suffixes, and referencing a background image. It verifies
package-relative payload metadata, referenced-resource tracking, and generated
stylesheet-relative EPUB output. The writer head-style regression now also
verifies metadata-carried CSS payload `@import` and `url(...)` references are
rewritten in the generated package and normalized again on read-back.

Focused verification on 2026-06-22 after the external stylesheet resource URL
slice: `php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, `lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 981 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,310 assertions, and 0 failures, with
671 counted `PASS` lines in
`/tmp/pandoc-full-epub-css-payload-20260622T020923Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
browser CSS rendering audit, remote-resource fetching, multi-rendition
authoring validation, DRM decryption, XML signature validation, or generic
TeX-to-MathML conversion was executed in this slice.

## 2026-06-22 EPUB3 CSS Resource URL Slice

`EpubReader` now rewrites CSS `url(...)` references in XHTML spine `style`
attributes and `<head><style>` blocks through the same package-relative
resource path normalization used for `src`, `href`, `poster`, `data`, and
`srcset`. The reader records referenced CSS resources in
`epubReferencedResources`, preserves query/fragment suffixes, skips absolute,
`data:`, `mailto:`, and fragment-only URLs, and stores head-style metadata with
package-relative CSS URLs instead of chapter-relative paths.

`EpubWriter` now maps package-relative CSS `url(...)` references back to
chapter-relative XHTML paths when emitting generated body fragments and
metadata-provided spine head styles. This keeps packaged fonts, background
images, and other CSS resources round-trippable through EPUB read/write without
leaving OPF-relative URLs inside chapter XHTML.

The focused regressions extend the EPUB asset fixture with a head style font
URL and inline background URL, then verify referenced-resource tracking,
head-style metadata normalization, generated chapter-relative CSS output, and
read-back payload preservation. The writer head-style regression now covers
package-relative font/background URLs with query/fragment preservation and
round-trip read-back.

Focused verification on 2026-06-22 after the CSS resource URL slice: `php -l`
passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, `lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 973 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,302 assertions, and 0 failures, with
671 counted `PASS` lines in
`/tmp/pandoc-full-epub-css-url-20260622T015126Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
browser CSS rendering audit, remote-resource fetching, multi-rendition
authoring validation, DRM decryption, XML signature validation, or generic
TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 Aside Footnote Note Reference Slice

`MarkdownReader` now records local HTML footnote reference targets before
collecting definitions. Referenced standalone semantic footnote items such as
`<aside id="fn1" epub:type="footnote">` are parsed as native Pandoc note
definitions, original `role="doc-backlink"` anchors are stripped, and only
referenced definition wrappers are suppressed from body output. Unreferenced
semantic EPUB asides remain visible as normal native div content. The same slice
accepts whitespace-separated `epub:type` tokens for note references,
footnotes/rearnotes containers, and footnote/rearnote items.

`EpubReader` now has a package-level regression proving a spine XHTML
`doc-noteref` anchor pointing to a standalone EPUB footnote aside is converted
into a native Pandoc `note`, emitted by `WordPressBlockWriter` as a WordPress
footnotes section, and still preserved in `epubSpineItemRefs[*].semanticElements`
as the original `aside` provenance record.

Focused verification on 2026-06-21 after the aside footnote slice: `php -l`
passed for `lanes/pandoc/src/MarkdownReader.php`,
`lanes/pandoc/tests/MarkdownReaderTest.php`, and
`lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php lanes/pandoc/tests/EpubReaderTest.php`
passed 2 files, 4,771 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 839 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,168 assertions, and 0 failures, with
666 counted `PASS` lines in
`/tmp/pandoc-full-epub-aside-footnote-20260621T235014Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 SMIL Sequence Metadata Slice

`EpubReader` now preserves bounded EPUB3 media-overlay sequence metadata from
SMIL `<seq>` elements. The reader records sequence `id`, `epub:textref`,
`epub:type`, valid `xml:lang`/`lang`, and descendant `par` id membership on
`epubMediaOverlays[*].sequences` while continuing to expose the existing
text/audio/clip pair records.

`EpubWriter` now consumes the first metadata-provided overlay sequence when it
generates SMIL from `epubMediaOverlays[*].pairs`, emitting `<seq>` attributes
for `id`, package-relative `epub:textref`, `epub:type`, and `xml:lang`, and
declaring the EPUB namespace when sequence attributes need it. Explicit SMIL
payload resources still take precedence over generated SMIL.

Focused verification on 2026-06-21 after the SMIL sequence metadata slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, `lanes/pandoc/tests/EpubReaderTest.php`,
and `lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 815 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,127 assertions, and 0 failures, with
661 counted `PASS` lines in
`/tmp/pandoc-full-epub-smil-sequence-20260621T231503Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 Generated SMIL Media Overlay Slice

`EpubWriter` now synthesizes SMIL media-overlay resources from
`epubMediaOverlays[*].pairs` when no explicit SMIL resource payload or writer
resource is supplied. Generated overlays default to
`overlays/{overlayId}.smil`, use package-relative text/audio targets, retain
`par` ids and `clipBegin`/`clipEnd` ranges, and are linked from generated OPF
spine manifest items through `media-overlay="{overlayId}"`. Existing supplied
SMIL payloads remain authoritative and are copied unchanged.

The regression builds an EPUB3 document with overlay metadata, audio payload,
and no SMIL payload. It verifies the generated `OEBPS/overlays/mo-generated.smil`
file, OPF manifest/linkage, overlay metadata refinements, and EpubReader
round-trip recovery of overlay path, par count, text/audio targets, and pair
clip ranges.

Focused verification on 2026-06-21 after the generated SMIL media-overlay
slice: `php -l` passed for `lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubWriterTest.php`, `lanes/pandoc/src/EpubReader.php`,
and `lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 812 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,124 assertions, and 0 failures, with
661 counted `PASS` lines in
`/tmp/pandoc-full-epub-generated-smil-20260621T230526Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 HTML5 Inline Data/Meter/Output Slice

`MarkdownReader` now treats HTML5 `data`, `meter`, and `output` elements as
generic raw inline XHTML when raw HTML is enabled. This keeps their tag names
and attributes intact during EPUB3 XHTML spine conversion instead of flattening
them to child text. `EpubWriter` already emits raw HTML inline boundaries into
the generated XHTML spine; the new writer regression verifies those elements
are XML-valid in `chapter.xhtml` and survive an `EpubReader` round trip.

The reader regression builds a temporary EPUB3 package whose linear XHTML spine
contains a product identifier in `<data value="...">`, a stock level in
`<meter min="..." max="..." value="...">`, and a calculated line total in
`<output for="..." name="...">`. Read-back verifies the paragraph contains the
six raw inline opening/closing boundaries and that WordPress block output keeps
the original semantic tags.

Focused verification on 2026-06-21 after the HTML5 inline slice: `php -l`
passed for `lanes/pandoc/src/MarkdownReader.php`,
`lanes/pandoc/src/EpubReader.php`, `lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 783 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 7,071 assertions, and 0 failures, with
658 counted `PASS` lines in
`/tmp/pandoc-full-epub-html5-inline-20260621T223617Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 HTML5 Standalone Inline Fragment Slice

The standalone HTML inline-fragment reader now recognizes leading HTML5
`data`, `meter`, and `output` elements. Those elements were already preserved
when they appeared inside paragraph-wrapped EPUB spine XHTML, but a fragment
whose first source token was one of those tags could miss the raw-inline DOM
path. The allowlist now routes those fragments through the same balanced
element parser used for other EPUB-relevant inline tags.

The focused regression starts a paragraph directly with
`<data value="...">`, continues with `<meter>` and `<output>`, and verifies
raw opening/closing boundaries, attributes, nested `<strong>` content, and
WordPress block output are preserved.

Focused verification on 2026-06-21 after the standalone HTML5 inline fragment
slice: `php -l` passed for `lanes/pandoc/src/MarkdownReader.php` and
`lanes/pandoc/tests/MarkdownReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,523 assertions,
and 0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 785 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files, 7,097
assertions, and 0 failures, with 660 counted `PASS` lines in
`/tmp/pandoc-full-epub-html5-standalone-inline-20260621T224602Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 BDI Raw Inline Slice

`MarkdownReader` now treats standalone and DOM-converted HTML5 `bdi` elements
as raw inline XHTML boundaries, matching the existing EPUB spine strategy for
semantic inline tags whose element name carries behavior that cannot be reduced
to plain span metadata. This preserves bidirectional-isolation semantics and
keeps `id`, `class`, `dir`, and nested inline content intact in both regular
HTML fragments and EPUB3 spine XHTML.

The EPUB reader regression extends the HTML5 inline spine fixture with
`<bdi id="buyer" dir="auto">...`, verifies the raw opening and closing
boundaries survive beside `data`, `meter`, and `output`, and checks WordPress
block output keeps the isolation element. The writer regression emits the same
raw inline boundaries into `chapter.xhtml`, then reads the generated package
back through `EpubReader` and verifies the boundary sequence is preserved.

Focused verification on 2026-06-21 after the BDI raw inline slice: `php -l`
passed for `lanes/pandoc/src/MarkdownReader.php`,
`lanes/pandoc/tests/MarkdownReaderTest.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,507 assertions,
and 0 failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 785 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files, 7,081
assertions, and 0 failures, with 659 counted `PASS` lines in
`/tmp/pandoc-full-epub-html5-inline-bdi-20260621T223946Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 Semantic Attribute WordPress Output Slice

`WordPressBlockWriter` now preserves EPUB semantic attributes through the
WordPress block handoff instead of dropping them at the HTML allowlist boundary.
The allowed block, inline, image, table, and table-cell attributes now include
`epub:type`, `xml:lang`, and role-bearing EPUB/ARIA metadata where applicable,
while retaining the existing `data-*` and `aria-*` safety boundaries.

The focused EPUB spine fixture verifies that a linear XHTML section, aside, and
pagebreak carrying `epub:type`, `role`, `aria-label`, `title`, `xml:lang`, and
`dir` stay visible in WordPress block output. A table-specific regression covers
the table boundary directly: a source table with `epub:type="table"`,
`role="doc-table"`, and `xml:lang`, plus semantic `<th>` and `<td>` attributes,
round-trips to core table block markup without losing those semantic attributes.

Focused verification on 2026-06-21 after the semantic attribute WordPress output
slice: `php -l` passed for `lanes/pandoc/src/WordPressBlockWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/MarkdownReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 2 files, 4,698 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,962 assertions, and 0 failures, with
651 counted `PASS` lines in
`/tmp/pandoc-full-epub-semantic-table-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 Fixed-Layout Page-Spread Spine Slice

`EpubReader` now derives structured `pageSpread` metadata from fixed-layout
spine itemref properties such as `rendition:page-spread-left`,
`rendition:page-spread-right`, and `rendition:page-spread-center`, while still
preserving the original raw property tokens. This makes fixed-layout page
placement inspectable in `epubSpineItemRefs` alongside the existing structured
layout, orientation, spread, and flow metadata.

`EpubWriter` can now emit `rendition:page-spread-*` itemref properties from
structured `pageSpread`, `epubPageSpread`, or `spreadPage` metadata. The
round-trip writer path proves that callers no longer need to prebuild raw
`properties` strings to preserve left/right page placement; the writer derives
the proper itemref property and the reader reconstructs `pageSpread` on import.

Focused verification on 2026-06-21 after the fixed-layout page-spread slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, `lanes/pandoc/tests/EpubReaderTest.php`,
and `lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 680 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,964 assertions, and 0 failures, with
651 counted `PASS` lines in `/tmp/pandoc-full-epub-page-spread-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 Ruby Annotation Spine Slice

`MarkdownReader` now keeps `ruby`, `rt`, and `rp` elements as raw HTML inline
tag boundaries when raw HTML is enabled, matching the existing fallback path for
inline HTML tags that have no native Pandoc AST constructor. This preserves
EPUB3/XHTML reading annotations through `EpubReader` because linear spine XHTML
is converted through the same HTML-capable Markdown reader path.

Regression coverage now includes a direct HTML reader case for
`<ruby><rp>...</rp><rt>...</rt></ruby>` and an EPUB3 spine package smoke that
proves ruby annotation markup survives into `WordPressBlockWriter` output. The
disabled raw-HTML branch still flattens the ruby text content without leaking
raw tag boundaries.

Focused verification on 2026-06-21 after the ruby annotation slice: `php -l`
passed for `lanes/pandoc/src/MarkdownReader.php`,
`lanes/pandoc/tests/MarkdownReaderTest.php`, and
`lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/MarkdownReaderTest.php`
passed 2 files, 4,692 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,956 assertions, and 0 failures, with
650 counted `PASS` lines in `/tmp/pandoc-full-epub-ruby-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
browser rendering check for ruby annotations, DRM decryption, XML signature
validation, multi-rendition authoring validation, or generic TeX-to-MathML
conversion was executed in this slice.

## 2026-06-21 EPUB3 NCX Document Label Language Slice

`EpubReader` now preserves NCX document-level language metadata for `docTitle`
and `docAuthor` records. The primary NCX metadata promotion exposes
`epubNcxDocTitleLang` and `epubNcxDocAuthorRecords` while keeping the existing
string `epubNcxDocTitle` and `epubNcxDocAuthors` surfaces backward compatible.

`EpubWriter` now consumes `epubNcxDocTitleLang` and
`epubNcxDocAuthorRecords` / `ncxDocAuthorRecords` / NCX metadata records when
generating optional `toc.ncx`, emitting `xml:lang` on `docTitle` and
`docAuthor` elements without requiring callers to abandon scalar author lists.

Focused verification on 2026-06-21 after the NCX document-label language slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 674 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,940 assertions, and 0 failures, with
649 counted `PASS` lines in
`/tmp/pandoc-full-epub-ncx-doc-language-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 NCX Page-List Label Slice

`EpubReader` now records a direct NCX `pageList` `navLabel` as
`epubNcxPageListLabel` plus `epubNcxPageListLabelLang`, and stores the same
fields inside `epubNcxMetadata[*]`. The reader deliberately takes only the
direct `pageList` label so a missing list label does not accidentally inherit
the first nested `pageTarget` label.

`EpubWriter` no longer hardcodes the NCX page-list label to `Pages`. Generated
NCX output now prefers `epubNcxPageListLabel` /
`epubNcxPageListLabelLang`, then source `epubNcxMetadata`, and falls back to
`Pages` only when no source label is available. This keeps localized page-list
labels stable through EPUB3 import/export and leaves page-target labels and
their language metadata intact.

Focused verification on 2026-06-21 after the NCX page-list label slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, `lanes/pandoc/tests/EpubReaderTest.php`,
and `lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 662 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,928 assertions, and 0 failures, with
649 counted `PASS` lines in
`/tmp/pandoc-full-epub-ncx-page-list-label-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 NCX NavList Slice

`EpubReader` now preserves NCX `navList` sections separately from EPUB3
`nav.xhtml` auxiliary navigation. Imported metadata records the list label,
label language, `id`, `class`/type, and `navTarget` entries with target text,
href, id, class/type, value, label language, and `playOrder`.

`EpubWriter` now accepts `epubNcxNavLists` metadata and emits NCX
`<navList>` / `<navTarget>` output into `toc.ncx`, reusing package-relative href
rewriting, stable NCX id generation, language-qualified `navLabel` output, and
source `playOrder` values. This closes another NCX vocabulary round-trip gap
for EPUB packages that still carry NCX navigation beyond the primary table of
contents and page list.

Focused verification on 2026-06-21 after the NCX navList slice: `php -l`
passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`, `lanes/pandoc/tests/EpubReaderTest.php`,
and `lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 668 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,934 assertions, and 0 failures, with
649 counted `PASS` lines in `/tmp/pandoc-full-epub-ncx-navlist-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 OCF Container Link Language Slice

`EpubReader` now preserves `hreflang` on OCF `META-INF/container.xml`
`<links><link .../></links>` entries. The existing container link metadata path
already carried `href`, `id`, `rel`, `media-type`, `refines`, and `properties`;
this closes the language-qualified OCF link round-trip gap so container-level
records such as ONIX sidecars keep their declared language.

`EpubWriter` now emits metadata-provided container link `hreflang` attributes
back into `META-INF/container.xml`, accepting both `hreflang` and `hrefLang`
metadata keys. The OCF sidecar round-trip regression now verifies that a
container record link with `hreflang="en-GB"` is read, written, and read back.

Focused verification on 2026-06-21 after the OCF container link language slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 651 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,917 assertions, and 0 failures, with
649 counted `PASS` lines in
`/tmp/pandoc-full-epub-container-link-hreflang-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 Nav Label Language Direction Slice

`EpubReader` now preserves language and direction metadata on individual EPUB3
navigation labels. XHTML nav `<a>` and `<span>` labels now carry valid
`xml:lang`/`lang` and `dir` values into `epubTocEntries`,
`epubLandmarkEntries`, `epubPageListEntries`, and auxiliary nav entries instead
of only preserving those attributes on the containing `<nav>` sections.

`EpubWriter` now accepts `lang`, `xml:lang`, or `xmlLanguage` plus `dir` on
metadata-provided nav entries. Generated `nav.xhtml` emits valid label language
as both `lang` and `xml:lang`, emits valid `dir`, and round-trips those fields
through the reader. This applies to clickable `<a>` entries and non-clickable
parent `<span>` entries.

Focused verification on 2026-06-21 after the nav label language/direction
slice: `php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 651 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,917 assertions, and 0 failures, with
649 counted `PASS` lines in
`/tmp/pandoc-full-epub-container-link-nav-label-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 Local Iframe Resource Slice

`EpubReader` now builds a bounded HTML-reader iframe resource map for
package-local XHTML spine content. Package-relative `<iframe src="...">`
targets are resolved through the OPF manifest path table, `application/xhtml+xml`
iframe bodies are parsed through the existing HTML reader, iframe image targets
remain image fallbacks, generic package resources remain inspectable iframe
wrappers, nested embedded resource URLs are rewritten to package-relative paths,
and read metadata records `epubIframeResources` plus
`epubIframeResourceCount`.

`MarkdownReader` now treats `application/xhtml+xml` iframe resources as parseable
HTML alongside `text/html`, which lets EPUB spine conversion reuse the existing
HTML reader iframe flow instead of preserving empty iframe shells for normal EPUB
XHTML resources.

The focused regression builds an EPUB3 package with a linear spine XHTML chapter
that embeds a package-local XHTML review document, an image iframe, and a generic
binary iframe. The embedded XHTML document itself contains an image and a nested
XHTML iframe. Read-back verifies the embedded heading/paragraph/strong/image
content, nested iframe body, image fallback, generic source metadata, tracked
iframe resources, tracked referenced package resources, and WordPress block
handoff for the expanded iframe content.

Focused verification on 2026-06-21 after the local iframe resource slice:
`php -l` passed for `lanes/pandoc/src/EpubReader.php`,
`lanes/pandoc/src/MarkdownReader.php`, and
`lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 636 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php` passed 1 file, 4,481 assertions, and
0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files, 6,902
assertions, and 0 failures.

No upstream Haskell runner, EPUBCheck validation, browser iframe rendering,
remote iframe fetching, full EPUB fixture corpus, DRM decryption, XML signature
validation, multi-rendition authoring validation, or generic TeX-to-MathML
conversion was executed in this slice.

## 2026-06-21 EPUB3 Details/Summary Spine Preservation Slice

`MarkdownReader` now treats XHTML `details` elements as block-level DOM nodes
and preserves them as raw HTML containers when raw HTML is enabled. This keeps
`<summary>` labels, `open`, EPUB/ARIA attributes, and nested block content from
being flattened into one paragraph during EPUB spine conversion.

`EpubReader` now has a regression for EPUB3 spine XHTML with package-relative
links inside a `<details>` body, verifying the body link remains tracked as an
EPUB referenced resource and WordPress block output uses one html block rather
than `Answer keyVisible explanation`. `EpubWriter` now has a matching
write/read-back regression proving raw details blocks are emitted as XML-valid
XHTML, survive EPUB read-back as `raw_html`, and remain active in WordPress html
block output.

Focused verification on 2026-06-21 after the details/summary spine slice:
`php -l` passed for `lanes/pandoc/src/MarkdownReader.php`,
`lanes/pandoc/src/EpubReader.php`, `lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/MarkdownReaderTest.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/MarkdownReaderTest.php lanes/pandoc/tests/EpubReaderTest.php
lanes/pandoc/tests/EpubWriterTest.php` passed 3 files, 5,355 assertions, and
0 failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files,
7,144 assertions, and 0 failures, with 664 counted `PASS` lines in
`/tmp/pandoc-full-epub-details-20260621T233427Z.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 Semantic Spine Descendant Metadata Slice

`EpubReader` now records bounded semantic XHTML descendants from linear spine
documents on `epubSpineItemRefs[*].semanticElements`. The reader preserves
semantic source hints from `section`, `article`, `aside`, `figure`,
`figcaption`, `nav`, `main`, `header`, `footer`, `span`, `div`, and semantic
`a` descendants when they carry EPUB/ARIA/role/id/class/title/lang/dir/hidden
attributes, plus a normalized text excerpt. This keeps chapter/footnote/page
break provenance inspectable even when body conversion flattens those source
wrappers into normal Pandoc blocks.

The focused regression builds an EPUB3 package whose linear XHTML spine item
contains a `section epub:type="chapter" role="doc-chapter"`, an
`aside epub:type="footnote" role="doc-footnote"`, and a
`span epub:type="pagebreak" role="doc-pagebreak"`. Read-back verifies the
body-level `epub:type` remains in `bodyAttributes` and the descendant semantic
records are attached to the matching spine item.

Focused verification on 2026-06-21 after the semantic spine descendant metadata
slice: `php -l` passed for `lanes/pandoc/src/EpubReader.php` and
`lanes/pandoc/tests/EpubReaderTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php` passed 1 file, 150 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests/EpubReaderTest.php
lanes/pandoc/tests/EpubWriterTest.php` passed 2 files, 581 assertions, and 0
failures; `php tools/run-tests.php lanes/pandoc/tests` passed 11 files, 6,844
assertions, and 0 failures, with 645 counted `PASS` lines in
`/tmp/pandoc-epub3-spine-semantics-full-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full EPUB fixture corpus,
multi-rendition authoring validation, DRM decryption, XML signature validation,
or generic TeX-to-MathML conversion was executed in this slice.

## 2026-06-21 EPUB3 Media Overlay Metadata And Rendition Flow Slice

`EpubReader` now preserves package-level media overlay metadata
(`media:duration`, `media:narrator`, `media:active-class`, and
`media:playback-active-class`) and attaches refined media overlay metadata to
the matching manifest item. The same reader slice also records package-level
`rendition:flow` and derives single spine itemref flow properties such as
`rendition:flow-scrolled-doc` into `epubSpineItemRefs[*].renditionFlow`.

`EpubWriter` now emits those package-level media overlay metadata records,
refined media overlay item metadata, package-level `rendition:flow`, and
per-itemref `rendition:flow-*` properties without overwriting explicit source
properties. This keeps narrated EPUB playback-state metadata and scrolling or
pagination intent visible through native PHP import/export.

Focused verification on 2026-06-21 after the media overlay metadata and
rendition flow slice: `php -l` passed for
`lanes/pandoc/src/EpubReader.php`, `lanes/pandoc/src/EpubWriter.php`,
`lanes/pandoc/tests/EpubReaderTest.php`, and
`lanes/pandoc/tests/EpubWriterTest.php`; `php tools/run-tests.php
lanes/pandoc/tests/EpubReaderTest.php lanes/pandoc/tests/EpubWriterTest.php`
passed 2 files, 419 assertions, and 0 failures; `php tools/run-tests.php
lanes/pandoc/tests` passed 11 files, 6,682 assertions, and 0 failures, with
630 counted `PASS` lines in `/tmp/pandoc-full-epub-flow-20260621.log`.

No upstream Haskell runner, EPUBCheck validation, full package metadata
vocabulary validation, full EPUB fixture corpus, or generic TeX-to-MathML
conversion was executed in this slice.
