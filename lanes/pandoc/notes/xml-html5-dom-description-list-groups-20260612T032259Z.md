# XML/HTML5 DOM Description List Groups

Mapped one bounded XML/HTML5 DOM handoff slice for HTML description lists.

- Added `dl` summary metadata for grouped `dt`/`dd` terms and details.
- Preserved direct `dl` groups and HTML5 `div`-wrapped groups with source element provenance.
- Added `dt` and `dd` child summaries for reviewer handoff text.
- Verified deterministic raw HTML and WordPress raw-block handoff without invoking Pandoc, browser renderers, online sanitizers, external validators, online services, live provider tests, or live-service provider tests.
