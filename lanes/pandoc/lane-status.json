{
    "schemaVersion": 1,
    "library": "pandoc",
    "estimatedProgress": 71,
    "suiteProgress": "cloned static upstream inventory; 2,028 upstream test files/artifacts inspected; 426 focused Markdown/HTML/WordPress checks mapped. Latest mapped slice covers six Tests.Readers.Markdown bare URI autolink extension cases: leading http(s) URLs, query strings with trailing period, surrounding punctuation, uppercase schemes, balanced parentheses, and bracketed paths whose destinations percent-encode square brackets. Full Haskell runner not executed.",
    "phpPass": 164,
    "phpFail": 0,
    "wordpressScenarios": "Data Liberation import preserving Markdown links/media/footnotes/tables plus HTML-reader full-document exports, tables, blockquotes, lists, definition lists, inline marks, literal punctuation, LaTeX-looking source, special characters, explicit HTML links/images, footnote-link exports, and table section/row metadata. The WordPress fixture now includes space-containing legacy media and manifest links, bare pasted source URLs with trailing punctuation and balanced parenthesized media paths, entity-decoded umlaut link destinations/titles plus URI/e-mail autolinks, attributed autolinks, link labels containing autolink-looking source URLs, nested Markdown link syntax, and bare URI-looking source notation that must remain literal inside reviewer labels, parenthesized source URLs and nested parenthesized reference destinations, backslash/raw-TeX link labels, unresolved reference-looking fallback markers, citation-adjacent source links, empty-reference placeholders, bracketed review spans with stable ids/classes/data attributes around emphasized edit links, immediate inline code attributes for reviewer/source tokens, implicit intra-document section links, explicit Markdown hard-break plus multiline code-span handoff text, multilingual source audit links/e-mail contacts, numbered review examples, Pandoc-style line blocks, raw TeX macro definitions, rectangular grid-table import queues, block-rich grid cells, and Markdown grid-table span queues.",
    "phase": "cloned inventory plus native Markdown/HTML reader and WordPress writer slices; latest slice maps bounded Tests.Readers.Markdown bare URI autolink extension semantics after the link-label boundary slice",
    "audit": "audited 2026-05-23: pandoc-local PHP lane coverage and required root harness are green after the bare URI slice; no upstream runner parity",
    "currentWork": "Native MarkdownReader now recognizes bare http(s) URI autolinks at inline boundaries, assigns Pandoc-style uri link metadata, keeps trailing sentence punctuation outside the anchor, preserves balanced parenthesized paths, accepts uppercase schemes, and percent-encodes square/curly brackets in destinations. The WordPress import fixture now covers pasted source URLs without angle brackets or Markdown link syntax.",
    "blocker": "Pandoc-local PHP blocker: none; focused pandoc behavior count is 164 tests with 1,699 assertions and 0 failures. The required root php tools/run-tests.php command was run on 2026-05-23 after this slice and passed with 183 test files, 18,316 assertions, and 0 failures. Full upstream Pandoc runner remains unexecuted because upstream test-pandoc/test-pandoc-lua-engine must be built as Haskell Tasty executables from a full checkout, the upstream cache is blob-filtered/no-checkout with mass working-tree deletions, and running Cabal would require hydrating the broad checkout plus downloading/building Pandoc dependency graph. ghc 9.10.3 and cabal 3.12.1.0 are on PATH, stack is not.",
    "latestCommit": "1261afe Map Pandoc bare URI autolinks",
    "nextTask": "Map the remaining broader Tests.Readers.Markdown bare URI extension families: doi/git/file/mailto schemes, semicolon/query/fragment variants, and additional trailing punctuation boundaries."
}
