{
    "schemaVersion": 1,
    "library": "pandoc",
    "estimatedProgress": 70,
    "suiteProgress": "cloned static upstream inventory; 2,028 upstream test files/artifacts inspected; 420 focused Markdown/HTML/WordPress checks mapped. Latest mapped slice covers Tests.Readers.Markdown link-label recursion boundaries: autolinks, nested inline links, and bare URI-looking text inside ordinary link labels stay literal, while non-link inline markup such as emphasis still parses. Full Haskell runner not executed.",
    "phpPass": 163,
    "phpFail": 0,
    "wordpressScenarios": "Data Liberation import preserving Markdown links/media/footnotes/tables plus HTML-reader full-document exports, tables, blockquotes, lists, definition lists, inline marks, literal punctuation, LaTeX-looking source, special characters, explicit HTML links/images, footnote-link exports, and table section/row metadata. The WordPress fixture now includes space-containing legacy media and manifest links, entity-decoded umlaut link destinations/titles plus URI/e-mail autolinks, attributed autolinks, link labels containing autolink-looking source URLs, nested Markdown link syntax, and bare URI-looking source notation that must remain literal inside reviewer labels, parenthesized source URLs and nested parenthesized reference destinations, backslash/raw-TeX link labels, unresolved reference-looking fallback markers, citation-adjacent source links, empty-reference placeholders, bracketed review spans with stable ids/classes/data attributes around emphasized edit links, immediate inline code attributes for reviewer/source tokens, implicit intra-document section links, explicit Markdown hard-break plus multiline code-span handoff text, multilingual source audit links/e-mail contacts, numbered review examples, Pandoc-style line blocks, raw TeX macro definitions, rectangular grid-table import queues, block-rich grid cells, and Markdown grid-table span queues.",
    "phase": "cloned inventory plus native Markdown/HTML reader and WordPress writer slices; latest slice maps bounded Tests.Readers.Markdown no-links-inside-link-label semantics after the autolink attribute slice",
    "audit": "audited 2026-05-23: pandoc-local PHP lane coverage and required root harness are green after the link-label slice; no upstream runner parity",
    "currentWork": "Native MarkdownReader now names and uses a link-label inline parsing boundary that disables recursive link parsing for link and image labels. The bounded upstream Tests.Readers.Markdown cases for `[<https://example.org>](url)`, `[[a](url2)](url)`, and `[https://example.org(](url)` are covered, plus a WordPress import fixture where reviewer labels preserve source notation without emitting nested anchors.",
    "blocker": "Pandoc-local PHP blocker: none; focused pandoc behavior count is 163 tests with 1,680 assertions and 0 failures. The required root php tools/run-tests.php command was run on 2026-05-23 after this slice and passed with 183 test files, 18,129 assertions, and 0 failures. Full upstream Pandoc runner remains unexecuted because upstream test-pandoc/test-pandoc-lua-engine must be built as Haskell Tasty executables from a full checkout, the upstream cache is blob-filtered/no-checkout with mass working-tree deletions, and running Cabal would require hydrating the broad checkout plus downloading/building Pandoc dependency graph. ghc 9.10.3 and cabal 3.12.1.0 are on PATH, stack is not.",
    "latestCommit": "pending lane commit for the link-label slice; previous accepted 7f46f84 Map Pandoc reference link edge cases",
    "nextTask": "Map the bounded Tests.Readers.Markdown backslash-escape cases in link URLs and titles: escaped closing parentheses in inline destinations, escaped quote characters in titles, and escaped characters in reference-link URLs/titles."
}
