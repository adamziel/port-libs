# Readability/content rewrite engine WordPress Scenario

Migration-aware article cleanup into clean WordPress blocks with page-builder navigation, comment widgets, share UI, in-article ad slots, and surrounding theme chrome removed.

## Current Native Slice

Native DOM extractor removes chrome and upstream-style unlikely candidates, removes invisible `display:none`/`visibility:hidden`/`hidden`/`aria-hidden` nodes plus `aria-modal` dialogs while preserving `fallback-image` media, promotes one substantial article out of surrounding body/main/div wrappers, removes short leading timestamp chrome, removes generic unsafe embeds, removes `link` and `fieldset` source fragments plus `script`/`style` nodes, removes empty paragraphs and empty visual `h1`-`h6` spacer headings with no media/embed payload, deduplicates repeated inline SVG symbol sprites while retaining ordinary inline SVG content, removes compact trailing link/media footer bars after substantial article content, removes ad wrappers such as `ad-container`, `ad-mobile`, `dfp-slot`, and `js_ad`, removes explicit Sharedaddy/Jetpack social widgets, removes layout-only full-width figure wrappers while keeping editorial in-column figures and Medium-style `postField--fillWidthImage` editorial full-width figures, removes link-heavy credit-only wrappers inside figure captions, removes one-cell layout table wrappers while preserving multi-cell and upstream-marked data tables, normalizes legacy `font` tags to spans, removes article comments before paragraph wrapping so React/Next hydration markers do not split phrasing text, inserts separator whitespace at text-bearing block/table/list boundaries so adjacent paragraphs, headings, generated `br` paragraphs, and table cells do not concatenate in article text, trims whitespace-only selected-root text nodes after cleanup with NBSP treated as boundary whitespace, strips presentational/style attributes from retained article markup, removes interactive article controls and leading byline/follow/read-time/share action bars before the first content heading while preserving avatar media, removes leading timestamp and inline-byline wrappers before the first substantial paragraph on heading-less news bodies, scores semantic content containers and article-body containers including body-only fallback pages, applies partial upstream class/id candidate weighting, strongly weights WordPress `itemprop=articleBody` microdata and RDFa `property=articleBody` candidates over trailing tag/theme chrome, parses UTF-8 HTML safely, implements Mozilla-style `isProbablyReaderable` thresholds, extracts and cleans document titles before in-article headings, disambiguates JSON-LD `name` and `headline` against the cleaned source document title, removes source site suffixes separated by upstream title separators before WordPress post title selection, uses first paragraphs for metadata-free fallback excerpts, removes duplicate title headings from content, removes scaffold `h1`/`h2` nodes that only surround paragraph-bearing section containers, demotes remaining `h1` body headings to `h2` before block serialization, simplifies nested `div`/`section` wrappers, unwraps transparent section wrappers whose only direct element children are containers after source class cleanup, can opt into serializing cleaned fixture content inside `div#readability-page-1.page`, reruns wrapper simplification after empty paragraph cleanup, wraps consecutive phrasing children inside `div` nodes into paragraphs, preserves media-bearing single-paragraph `div` wrappers, converts text/phrasing-only `div` blocks to paragraphs, converts repeated `br` chains into paragraph boundaries while preserving single soft breaks, collapses low-link-density non-media `div` wrappers around a single paragraph, strips source CSS classes during post-processing while preserving caller-requested class tokens, trims and resolves relative `href`, `src`, `poster`, and `srcset` URLs against the source document URL and base element, replaces `javascript:` links with inert retained content, gives upstream-ordered Dublin Core/OpenGraph/Twitter/Parse.ly metadata precedence for titles, bylines, excerpts, and publication times while skipping meta tags with missing `content`, exposes byline/site/published/dir/lang metadata fields from meta tags and JSON-LD, derives article direction from selected content ancestors, preserves allowed video embeds while removing generic embedded widgets, promotes lazy `data-src`/`data-srcset` images including short base64 placeholder cases, removes post-article recommendation/signup chrome after selected article wrappers, maps Mozilla fixture pages including `bbc-1`, `telegraph`, `ars-1`, `heise`, `normalize-spaces`, `parsely-metadata`, `metadata-content-missing`, `003-metadata-preferred`, `004-metadata-space-separated-properties`, `embedded-videos`, `videos-2`, `cnet-svg-classes`, `medium-1`, `lazy-image-1`, `lazy-image-2`, `lazy-image-3`, `keep-images`, `base-url`, `base-url-base-element`, `base-url-base-element-relative`, `js-link-replacement`, `clean-links`, `table-style-attributes`, `replace-font-tags`, `schema-org-context-object`, `wordpress`, `rtl-1`, `rtl-2`, `rtl-3`, `rtl-4`, `links-in-tables`, `keep-tabular-data`, `remove-aria-hidden`, `hidden-nodes`, `visibility-hidden`, `replace-brs`, `remove-extra-brs`, `basic-tags-cleaning`, `remove-extra-paragraphs`, `style-tags-removal`, `remove-script-tags`, `social-buttons`, `title-en-dash`, and `title-and-h1-discrepancy`, and emits WordPress paragraph, heading, image, and table blocks while flattening block-only layout containers.

This slice also tightens Mozilla `clean-links` fixture coverage to exact footer link/image parity and maps Mozilla `title-and-h1-discrepancy` fixture coverage, Mozilla `schema-org-context-object` full paragraph coverage, Mozilla `wordpress` fixture coverage, Mozilla `ol` fixture coverage, Mozilla `001` body-byline fixture coverage, Mozilla `005-unescape-html-entities` metadata fixture coverage, native `_getJSONLD` title disambiguation, native `_getClassWeight` class/id candidate weighting including the Medium `section-content` boundary, native `_getLinkDensity` hash-only link weighting, native body byline extraction/removal, native metadata numeric entity replacement, native heading-less news timestamp/byline chrome cleanup, native comment-delimited phrasing cleanup, and native RTL direction metadata from selected article ancestors, so legacy source footer bars do not become imported blocks, WordPress BlogPosting `articleBody` microdata and BBC RDFa `property=articleBody` roots win over trailing tag/theme chrome, plugin-injected JSON-LD headlines do not replace canonical WordPress import titles, top news timestamp/byline wrappers do not become imported paragraph blocks, React/Next hydration comments do not split contributor text into fragment blocks, local footnote/citation links do not prevent low-density paragraph wrappers from collapsing, metadata-free author bylines do not become imported paragraph blocks, double-escaped invalid excerpt entities do not survive post metadata import, right-to-left source wrappers remain available as article metadata, and optional oracle output can preserve named Medium section wrappers without importing those shells into WordPress block output.

The current block-boundary spacing slice maps Mozilla `keep-tabular-data`, `replace-brs`, and the shared copied-fixture spacing family: native article text no longer joins paragraph-to-heading, paragraph-to-paragraph, generated hard-break paragraph, or table-cell text when the source HTML lacks whitespace between adjacent tags.

The current selected-root NBSP slice maps Mozilla `clean-links` full normalized content text parity and classic WordPress table-wrapped exports: source `&nbsp;` padding at the chosen root is dropped while internal nonbreaking editorial spacing remains intact.

The current data URL image slice maps Mozilla `data-url-image` behavior for WordPress media migrations: real inline SVG/base64/JPEG payloads survive extraction, placeholder GIF sources are removed when responsive candidates exist, and promoted `srcset` values remain available for media sideloading.

The current full-width media slice maps Mozilla `keep-images` behavior for media-heavy Medium exports: all expected editorial figures and captions survive when the source carries `postField--fillWidthImage` evidence, while unrelated decorative wrappers can still be removed before WordPress block output.

The current empty-heading slice maps Mozilla `medium-1` behavior for Medium exports: blank heading spacer nodes such as `<h4><br></h4>` are removed before text-boundary insertion, preventing lead heading text from concatenating with the first paragraph and preventing empty WordPress heading blocks.

The current SVG sprite slice maps Mozilla `cnet-svg-classes` behavior and WordPress theme exports: duplicate inline symbol sprites are removed by repeated symbol-id signature while the first reusable sprite and ordinary editorial inline SVG diagrams remain.

The current visible-date metadata slice maps Mozilla `v8-blog` behavior: a visible body/header `<time datetime>` does not populate `publishedTime` unless upstream-supported JSON-LD, `article:published_time`, or `parsely-pub-date` metadata supplies the date. For WordPress imports, this keeps theme template dates out of trusted post metadata until the import layer decides how to handle them.

The current Medium page-break slice maps Mozilla `medium-3` behavior: source `<hr>` separators between Medium page sections are removed while the content sections remain separate for oracle output, preventing synthetic page rules from becoming migrated WordPress paragraph blocks.

The current class-preservation and single-article promotion slice maps Mozilla `_cleanClasses` option behavior plus the copied `heise` fixture: source classes are stripped by default, callers can explicitly preserve class tokens such as `caption`, `wp-caption`, `aligncenter`, and `wp-caption-text`, and one real article can be promoted out of a navigation-heavy page shell before WordPress block serialization.

The current ARS figure-credit slice maps Mozilla `ars-1` caption cleanup: link-heavy credit-only wrappers inside `figcaption` are removed, the hero image source remains available, and requested caption classes can still survive for WordPress media review workflows.

The current Guardian media slice maps Mozilla `guardian-1` from `.upstream-cache/readability/test/test-pages/guardian-1/`: the optional oracle wrapper preserves the selected Guardian `itemprop="articleBody"` media root, 14 figures, 13 image payloads, 112 responsive source rows, and 8 caption list items survive, Guardian navigation/contribution/byline chrome is excluded from article text, and retained image figures now serialize as WordPress image blocks.

The current NYT caption slice maps Mozilla `nytimes-1` from `.upstream-cache/readability/test/test-pages/nytimes-1/`: the rich NYT figure, image source, `data-mediaviewer` caption/credit attributes, caller-preserved `caption` class, and caption/credit text survive, the CSS-hidden reader feedback prompt is removed before article text and block serialization, and the retained figure serializes as one WordPress image block.

The current NYT continuation slice maps Mozilla `nytimes-2` from `.upstream-cache/readability/test/test-pages/nytimes-2/`: the optional oracle wrapper preserves `article#story`, the lead figure and credit survive, three upstream continuation anchors remain available for expected-content parity, hidden story-ad interrupters are removed, the related story rail is excluded from imported text, and WordPress output emits one image block plus 23 paragraph blocks.

The current NYT utility media slice maps Mozilla `nytimes-3` from `.upstream-cache/readability/test/test-pages/nytimes-3/`: the optional oracle wrapper preserves `article#story`, figures whose image URL only appears on the `figure itemid` attribute receive native image payloads, 7 media figures and 8 expected image sources survive, related interactive-card chrome and the bottom advertisement slug are removed, and WordPress output emits 7 image blocks plus 43 paragraph blocks.

The current NYT debt article slice maps Mozilla `nytimes-4` from `.upstream-cache/readability/test/test-pages/nytimes-4/`: the optional oracle wrapper promotes the selected `section[itemprop=articleBody]` to `article#story`, the lead image and caption survive, 48 expected paragraphs and 4 h2 sections remain, publisher share tools plus interactive debt chart chrome are removed, related-link cards are excluded while the upstream-retained `More about` label boundary remains, and WordPress output emits 1 image block plus 47 paragraph blocks.

The current BBC RDFa slice maps Mozilla `bbc-1` from `.upstream-cache/readability/test/test-pages/bbc-1/`: the optional oracle wrapper preserves the selected `div[property=articleBody]`, metadata and readerable values match upstream, unsupported video placeholder shells are removed after iframe cleanup, navigation chrome is excluded, and WordPress output emits 5 image blocks plus article paragraphs.

The current CNN storytext slice maps Mozilla `cnn` from `.upstream-cache/readability/test/test-pages/cnn/`: a semantic `main` container with social-anchor class is not discarded as share chrome, `div#storytext` wins content selection and survives inside the optional oracle wrapper, the SmartAsset powered label is retained, CNN video player, Teads ad, disclosure widget, and masthead chrome are removed, and WordPress output emits one heading block plus 14 paragraph blocks.

The current Wapo inline gallery slice maps Mozilla `wapo-1` from `.upstream-cache/readability/test/test-pages/wapo-1/`: Washington Post PageBuilder inline gallery widgets are removed, linked graphic preview chrome is stripped while its caption remains, PostTV embed chrome is dropped while the editorial caption text survives, the final inline map image remains in paragraph content, and WordPress output emits 39 paragraph blocks without gallery controls.

The current Wapo author/media slice maps Mozilla `wapo-2` from `.upstream-cache/readability/test/test-pages/wapo-2/`: the selected article-body envelope keeps a lead inline-photo sibling before the article and a post-body author bio after it, top `pb-sig-line` byline/share chrome is removed, metadata and readerable values match upstream, and WordPress output emits 28 paragraph blocks without share, comment, or most-read chrome.

The current Yahoo Japan articleBody slice maps Mozilla `yahoo-4` from `.upstream-cache/readability/test/test-pages/yahoo-4/`: the generic `paragraph` wrapper containing `ynDetailText` wins content selection, Japanese metadata and expected article text match upstream, and WordPress output emits 9 paragraph blocks with no navigation, ranking, share, or footer chrome.

The current Telegraph text-section slice maps Mozilla `telegraph` from `.upstream-cache/readability/test/test-pages/telegraph/`: dateCreated-only JSON-LD no longer populates `publishedTime`, six upstream text-section wrappers and 13 editorial paragraphs survive, publisher image interrupter sections and media credits are dropped, related/comment/share chrome is excluded from article text, and WordPress output emits 13 paragraph blocks with 0 image blocks.

The current BuzzFeed print/bio cleanup slice maps Mozilla `buzzfeed-1` from `.upstream-cache/readability/test/test-pages/buzzfeed-1/`: upstream null-byline parity is preserved, print-only image helpers, author bio/contact copy, bottom share controls, and promoted ad byline fragments are removed, while story headings, paragraphs, and two inline grid images remain available for WordPress review output.

The current Le Monde French video slice maps Mozilla `lemonde-1` from `.upstream-cache/readability/test/test-pages/lemonde-1/`: the selected `articleBody` root, French metadata/lang values, 28 paragraphs, 9 section headings, and trusted Dailymotion iframe survive while subscription, navigation, social, ad, and recommendation chrome stay out of WordPress block output.

The current The Verge slice maps Mozilla `theverge` from `.upstream-cache/readability/test/test-pages/theverge/`: the optional oracle wrapper preserves `div#content`, the pullquote wrapper, responsive Vision Pro image/srcset, figcaption, and newsletter pricing copy match upstream text boundaries, and WordPress output emits 19 paragraph blocks, 3 heading blocks, and 1 image block without subscribe buttons, comment actions, sponsor labels, ad rails, or most-popular/sidebar chrome.

The current CityLab slice maps Mozilla `citylab-1` from `.upstream-cache/readability/test/test-pages/citylab-1/`: metadata, readerable, excerpt, and normalized expected text match upstream; the author RSS feed list is removed; the author bio and editorial image/link boundaries survive; and WordPress output emits 20 paragraph blocks, 4 heading blocks, and 3 image blocks without author feed chrome.

The current Mozilla Firefox slice maps Mozilla `mozilla-1` from `.upstream-cache/readability/test/test-pages/mozilla-1/`: metadata, readerable, excerpt, and normalized expected text match upstream; the optional oracle output preserves the `main-content` role wrapper; the trailing Firefox Sync sign-in CTA is removed; and WordPress output emits 19 paragraph blocks without Sync CTA chrome.

The current ACLU slice maps Mozilla `aclu` from `.upstream-cache/readability/test/test-pages/aclu/`: metadata, readerable, excerpt, and normalized expected text match upstream; the article body nested under Drupal panel/sidebar layout wrappers survives unlikely-candidate cleanup; comments, share links, conference banners, and hero/theme images are removed; and WordPress output emits 33 paragraph blocks plus 7 heading blocks.

The current parse-options slice maps upstream Readability API option boundaries: `keepClasses` skips default class cleanup for review-mode imports, a custom allowed-video regex can retain trusted non-default oEmbed iframe hosts while unrelated widgets are removed, `maxElemsToParse` aborts oversized documents before cleanup work begins, and `charThreshold` retries can recover short nonempty article candidates or return null for chrome-only documents.

## Scenario Fixture

- `lanes/readability/fixtures/wordpress-page-builder.html` models a legacy WordPress page-builder article with sidebar navigation, in-article ad slots, comments, share widgets, retained media, and article paragraphs.
- `lanes/readability/fixtures/mozilla/001/` copies Mozilla's body-byline fixture to keep metadata-free `itemprop=author` extraction, nested `itemprop=name` preference, and byline-node removal tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/005-unescape-html-entities/` copies Mozilla's metadata entity fixture to keep numeric entity decoding and invalid codepoint replacement tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/title-and-h1-discrepancy/` copies Mozilla's JSON-LD title fixture to keep `name`/`headline` disambiguation and h1 cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/schema-org-context-object/` copies Mozilla's NBC News fixture to keep object-valued JSON-LD `@context`, NewsArticle metadata, and leading timestamp/inline-byline cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/wordpress/` copies Mozilla's WordPress Tavern fixture to keep real WordPress BlogPosting/articleBody extraction, wp.com responsive images, and Jetpack chrome cleanup tied to upstream.
- `lanes/readability/fixtures/mozilla/mozilla-1/` copies Mozilla's Firefox customize-page fixture to keep main-content wrapper parity and trailing Sync CTA cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/aclu/` copies Mozilla's ACLU Drupal fixture to keep panel/sidebar wrapper survival and comment/share chrome cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/normalize-spaces/` copies Mozilla's `source.html`, `expected.html`, and `expected-metadata.json` fixture to keep whitespace-normalized article extraction tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/003-metadata-preferred/` and `lanes/readability/fixtures/mozilla/004-metadata-space-separated-properties/` copy Mozilla's compact metadata fixtures to keep upstream metadata precedence and space-separated property matching tied to named upstream pages.
- `lanes/readability/fixtures/mozilla/metadata-content-missing/` copies Mozilla's metadata fixture to keep space-separated `property` matching, missing `content` skipping, and Dublin Core title/byline/excerpt precedence tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/embedded-videos/` copies Mozilla's video fixture to keep YouTube, YouTube-nocookie, and Vimeo embeds available during migration while unrelated widgets are stripped by the native cleanup slice.
- `lanes/readability/fixtures/mozilla/videos-2/` copies Mozilla's video-heavy Liberation fixture to keep JSON-LD byline/site/date metadata, UTF-8 article text, article-body selection, and seven editorial video embeds tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/lazy-image-1/` copies Mozilla's Medium lazy image fixture to keep meta description precedence, promoted `data-old-src` article images, and post-article recommendation/signup cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/lazy-image-2/` copies Mozilla's Kinja lazy image fixture to keep entity-decoded excerpts, in-article ad cleanup, and 56 responsive image rows tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/lazy-image-3/` copies Mozilla's small lazy image fixture to keep JavaScript-free `data-src` image promotion tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/data-url-image/` copies Mozilla's data URI image fixture to keep standalone data URI media retention, placeholder GIF cleanup before `srcset` promotion, inline SVG serialization parity, and base64 SVG/JPEG payload preservation tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/keep-images/` copies Mozilla's media-heavy Medium fixture to keep full-width editorial image/caption retention tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/medium-1/` copies Mozilla's Medium fixture to keep empty spacer-heading cleanup and heading-to-paragraph text boundary behavior tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/medium-2/` copies Mozilla's Medium fixture to keep trailing syndication/source-note cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/medium-3/` copies Mozilla's Medium fixture to keep hr-separated page-section cleanup, long article text, blockquotes, ordered lists, links, images, and readability-page child structure tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/heise/` copies Mozilla's Heise fixture to keep single-article promotion, timestamp cleanup, caption class preservation, and German metadata tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/ars-1/` copies Mozilla's ARS fixture to keep metadata, readerable classification, retained hero image source, empty caption text, and credit-only caption-link removal tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/guardian-1/` copies Mozilla's Guardian fixture from upstream `test/test-pages/guardian-1` to keep articleBody wrapper parity, dense image/picture/source retention, caption list retention, and theme-chrome cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/nytimes-1/` copies Mozilla's NYT fixture from upstream `test/test-pages/nytimes-1` to keep rich figure/caption/credit retention and hidden reader-feedback prompt cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/nytimes-2/` copies Mozilla's NYT continuation fixture from upstream `test/test-pages/nytimes-2` to keep continuation anchors, story-ad interrupter cleanup, related story rail exclusion, and lead figure retention tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/nytimes-3/` copies Mozilla's NYT utility-media fixture from upstream `test/test-pages/nytimes-3` to keep figure `itemid` lazy-image repair, related-card cleanup, bottom advertisement cleanup, and multi-figure WordPress image block output tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/nytimes-4/` copies Mozilla's NYT debt article fixture from upstream `test/test-pages/nytimes-4` to keep lead image retention, promoted `article#story` wrapper parity, debt chart/interactive cleanup, share-tool removal, related-link cleanup, and paragraph-heavy WordPress import output tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/bbc-1/` copies Mozilla's BBC fixture from upstream `test/test-pages/bbc-1` to keep RDFa articleBody root selection, unsupported video-placeholder cleanup, image/data-src retention, and BBC navigation removal tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/cnn/` copies Mozilla's CNNMoney fixture from upstream `test/test-pages/cnn` to keep storytext root selection, SmartAsset label retention, video/ad/disclosure widget cleanup, and masthead exclusion tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/wapo-1/` copies Mozilla's Washington Post fixture from upstream `test/test-pages/wapo-1` to keep inline gallery cleanup, linked graphic preview cleanup, retained video/map caption text, final inline map image retention, and expected paragraph/link/image counts tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/wapo-2/` copies Mozilla's Washington Post fixture from upstream `test/test-pages/wapo-2` to keep lead inline-photo envelope retention, top byline/share cleanup, post-body author bio retention, and expected paragraph/link/image counts tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/yahoo-4/` copies Mozilla's Yahoo Japan fixture from upstream `test/test-pages/yahoo-4` to keep `ynDetailText` articleBody selection, Japanese metadata parity, 9 article paragraphs, and navigation/ranking/share/footer cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/telegraph/` copies Mozilla's Telegraph fixture from upstream `test/test-pages/telegraph` to keep text-section wrapper parity, null `publishedTime` for dateCreated-only JSON-LD, image interrupter removal, and publisher chrome cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/theverge/` copies Mozilla's The Verge fixture from upstream `test/test-pages/theverge` to keep `div#content` wrapper parity, pullquote wrapper retention, responsive image/srcset retention, newsletter pricing boundaries, and action/ad/rail cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/wikipedia-4/` copies Mozilla's Wikipedia film-list fixture from upstream `test/test-pages/wikipedia-4` to keep long sortable-table extraction, Wikimedia metadata, category/maintenance/tracking chrome cleanup, and table-heavy WordPress output tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/wikipedia/` copies Mozilla's standard Mozilla article fixture from upstream `test/test-pages/wikipedia` to keep long article extraction, infobox/media/table/link parity, and MediaWiki siteSub/jump/hatnote/printfooter cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/wikipedia-2/` copies Mozilla's New Zealand country-page fixture from upstream `test/test-pages/wikipedia-2` to keep large country infobox/table extraction, Wikimedia metadata, expected image-source retention, and MediaWiki status-indicator cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/wikipedia-3/` copies Mozilla's Wikipedia Hermitian-matrix fixture from upstream `test/test-pages/wikipedia-3` to keep math-heavy article extraction, Wikimedia metadata, section headings, formula image fallback retention, and MediaWiki shell cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/cnet-svg-classes/` copies Mozilla's CNET fixture to keep duplicate SVG symbol-sprite cleanup, article image parity, and Spanish metadata tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/v8-blog/` copies Mozilla's V8 fixture to keep the null publishedTime boundary for visible article header time elements tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/base-url/` copies Mozilla's no-base-element URL fixture to keep source-page relative links, root-relative links, hash links, and image sources tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/base-url-base-element/` copies Mozilla's root-base-element URL fixture to keep base href resolution tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/base-url-base-element-relative/` copies Mozilla's base URL fixture to keep relative link and image URL cleanup tied to the upstream `http://fakehost/test/page.html` fixture URL.
- `lanes/readability/fixtures/mozilla/js-link-replacement/` copies Mozilla's `javascript:` link replacement fixture to keep inert span replacement and child-node preservation tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/clean-links/` copies Mozilla's popup-link fixture to keep note-anchor JavaScript cleanup and whitespace-trimmed URI attributes tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/table-style-attributes/` copies Mozilla's legacy table/style fixture to keep `font` to `span` normalization, comment removal, presentational attribute cleanup, retained multi-cell table structure, and table/cell size cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/replace-font-tags/` copies Mozilla's focused legacy font fixture to keep `font` to `span` conversion, retained `face`/`size` attributes, paragraphized text divs, and heading cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/rtl-1/` through `lanes/readability/fixtures/mozilla/rtl-4/` copy Mozilla's direction metadata fixtures to keep upstream `html`/`body`/`main` wrapper direction semantics tied to named upstream pages.
- `lanes/readability/fixtures/mozilla/links-in-tables/` copies Mozilla's Blogger fixture to keep table links, row structure, and colgroup/col retention tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/keep-tabular-data/` copies Mozilla's Factorio fixture to keep a large GUI status table, status images, row counts, and stripped style/class cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/remove-aria-hidden/` copies Mozilla's hidden-node fixture to keep `aria-hidden` text removal and first-paragraph fallback excerpts tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/hidden-nodes/` copies Mozilla's hidden-node fixture to keep inline `display:none` and hidden-attribute cleanup tied to a named upstream page while preserving visible sibling headings.
- `lanes/readability/fixtures/mozilla/visibility-hidden/` copies Mozilla's visibility-hidden fixture to keep `visibility:hidden` cleanup, generic embed removal, and scaffold heading cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/replace-brs/` copies Mozilla's repeated-break fixture to keep br-chain paragraphization tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/remove-extra-brs/` copies Mozilla's trailing-break fixture to keep br-before-paragraph cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/basic-tags-cleaning/` copies Mozilla's generic tag cleanup fixture to keep unsafe embed removal, scaffold heading cleanup, metadata, readerable classification, and paragraph sequence parity tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/remove-extra-paragraphs/` copies Mozilla's empty paragraph fixture to keep whitespace-only paragraph removal, metadata, readerable classification, and paragraph sequence parity tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/style-tags-removal/` copies Mozilla's style-tag fixture to keep source CSS removal tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/remove-script-tags/` copies Mozilla's script-tag fixture to keep JavaScript/VBScript source fragment removal tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/social-buttons/` copies Mozilla's WordPress.com social button fixture to keep Sharedaddy/Jetpack like-widget cleanup tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/title-en-dash/` copies Mozilla's title cleanup fixture to keep document-title separator behavior tied to a named upstream page.
- `lanes/readability/fixtures/mozilla/ol/` copies Mozilla's ordered-list fixture to keep list content extraction tied to a named upstream page while readerable preflight remains false for `li p` candidates.
- `lanes/readability/examples/wordpress-migration-blocks.php` extracts that fixture and emits core block comments for migration workflows.
- `lanes/readability/examples/wordpress-single-article-promotion.php` demonstrates promoting one substantial post article out of a chrome-heavy source shell before block serialization.
- `lanes/readability/examples/wordpress-absolute-uri-migration.php` demonstrates resolving legacy relative links, image `src`, and responsive `srcset` candidates before WordPress block output.
- `lanes/readability/examples/wordpress-popup-link-cleanup.php` demonstrates neutralizing popup-note JavaScript anchors while trimming whitespace-padded editorial link and media URLs before WordPress block output.
- `lanes/readability/examples/wordpress-layout-table-cleanup.php` demonstrates removing a one-cell source layout table, absolutizing links inside the recovered paragraph, and preserving a real multi-cell table as a core table block.
- `lanes/readability/examples/wordpress-data-table-summary.php` demonstrates preserving an upstream-marked summarized one-cell data table as a core table block while collapsing a presentational one-cell layout table.
- `lanes/readability/examples/wordpress-line-break-cleanup.php` demonstrates turning hard-break paragraph boundaries from a legacy export into separate core paragraph blocks while keeping a single soft break.
- `lanes/readability/examples/wordpress-inline-junk-cleanup.php` demonstrates dropping inline source stylesheet links and fieldset controls before WordPress block serialization.
- `lanes/readability/examples/wordpress-social-widget-cleanup.php` demonstrates dropping inline executable fragments plus Jetpack social widgets before WordPress block serialization.
- `lanes/readability/examples/wordpress-metadata-precedence.php` demonstrates selecting portable Dublin Core title, byline, and excerpt metadata over incomplete or wrong theme fallback tags before WordPress block serialization.
- `lanes/readability/examples/wordpress-metadata-entity-cleanup.php` demonstrates decoding double-escaped metadata excerpt entities and replacing invalid numeric references before WordPress post metadata import.
- `lanes/readability/examples/wordpress-font-tag-cleanup.php` demonstrates converting Classic Editor-era `font` tags to spans before WordPress block serialization.
- `lanes/readability/examples/wordpress-rtl-direction-metadata.php` demonstrates preserving right-to-left article direction metadata from source wrappers while serializing clean WordPress blocks.
- `lanes/readability/examples/wordpress-title-cleanup.php` demonstrates removing a source site suffix from the selected WordPress post title while preserving body heading blocks.
- `lanes/readability/examples/wordpress-jsonld-title-disambiguation.php` demonstrates keeping a canonical JSON-LD `name` title when a plugin-injected JSON-LD `headline` disagrees with the source document title.
- `lanes/readability/examples/wordpress-footnote-wrapper-cleanup.php` demonstrates collapsing a legacy wrapper around internal citation links while preserving the local hash link and target.
- `lanes/readability/examples/wordpress-body-byline-cleanup.php` demonstrates extracting a body `itemprop=author` byline into metadata and removing the byline paragraph before block output.
- `lanes/readability/examples/wordpress-news-byline-chrome-cleanup.php` demonstrates preserving structured NewsArticle metadata while starting WordPress blocks after leading timestamp and inline-byline chrome in a heading-less news body.
- `lanes/readability/examples/wordpress-section-wrapper-cleanup.php` demonstrates dropping transparent page-builder section shells while retaining editorial paragraph blocks.
- `lanes/readability/examples/wordpress-readability-page-wrapper-cleanup.php` demonstrates preserving Mozilla's `readability-page-1` wrapper for oracle comparison while WordPress block serialization flattens that wrapper and keeps editorial paragraphs.
- `lanes/readability/examples/wordpress-medium-section-wrapper-parity.php` demonstrates preserving a named Medium section wrapper in optional oracle output while WordPress block serialization still flattens opaque source section shells.
- `lanes/readability/examples/wordpress-articlebody-microdata-cleanup.php` demonstrates selecting schema.org `articleBody` content when the source theme omits `entry-content`, so trailing tag/theme chrome is not serialized.
- `lanes/readability/examples/wordpress-data-uri-image-cleanup.php` demonstrates preserving real inline SVG data URI media while replacing a tiny placeholder GIF with responsive `data-srcset` candidates before block serialization.
- `lanes/readability/examples/wordpress-full-width-media-preservation.php` demonstrates keeping editorial full-width source figures while dropping decorative crop wrappers before block serialization.
- `lanes/readability/examples/wordpress-react-comment-text-cleanup.php` demonstrates keeping contributor text split by React/Next `<!-- -->` hydration markers as one WordPress paragraph block.
- `lanes/readability/examples/wordpress-block-boundary-spacing.php` demonstrates keeping article text readable across paragraph-heading and table-cell boundaries before emitting clean WordPress blocks.
- `lanes/readability/examples/wordpress-leading-nbsp-cleanup.php` demonstrates dropping selected-root `&nbsp;` padding from a classic table-wrapped export while preserving internal nonbreaking editorial spacing.
- `lanes/readability/examples/wordpress-empty-heading-cleanup.php` demonstrates dropping visual spacer headings before block serialization while retaining real headings and readable article text boundaries.
- `lanes/readability/examples/wordpress-svg-sprite-dedupe.php` demonstrates removing duplicate inline theme symbol sprites while retaining the first sprite sheet and an editorial inline SVG diagram.
- `lanes/readability/examples/wordpress-visible-time-metadata-boundary.php` demonstrates leaving visible template dates out of trusted publishedTime metadata unless upstream-supported metadata fields supply a date.
- `lanes/readability/examples/wordpress-syndication-footer-cleanup.php` demonstrates removing a trailing `Originally published at` source note before WordPress block serialization.
- `lanes/readability/examples/wordpress-medium-page-break-cleanup.php` demonstrates removing Medium page-break `<hr>` separators before WordPress block serialization while keeping each editorial section as paragraph content.
- `lanes/readability/examples/wordpress-caption-class-preservation.php` demonstrates opt-in preservation of WordPress caption classes while unrelated source theme classes are still stripped.
- `lanes/readability/examples/wordpress-figure-credit-cleanup.php` demonstrates removing link-only source photo-credit wrappers inside media captions while preserving the requested WordPress caption class contract.
- `lanes/readability/examples/wordpress-guardian-media-caption-import.php` demonstrates importing the Guardian fixture as 13 WordPress image blocks while retaining 8 adjacent caption list items for media review.
- `lanes/readability/examples/wordpress-nytimes-caption-import.php` demonstrates importing the NYT fixture as one image block with caption/credit text retained and hidden feedback chrome removed.
- `lanes/readability/examples/wordpress-nytimes-continuation-import.php` demonstrates importing the NYT continuation fixture as one image block and 23 paragraph blocks while removing story-ad and related-story chrome.
- `lanes/readability/examples/wordpress-nytimes-utility-media-import.php` demonstrates importing the NYT utility-media fixture as 7 image blocks and 43 paragraph blocks while repairing itemid-only figure images and removing related-card/ad chrome.
- `lanes/readability/examples/wordpress-nytimes-debt-article-import.php` demonstrates importing the NYT debt article fixture as 1 image block and 47 paragraph blocks while removing interactive debt chart chrome, related-link cards, and publisher share tools.
- `lanes/readability/examples/wordpress-nytimes-section-front-import.php` demonstrates importing the NYT Spanish section-front fixture with 22 retained media cards and 12 retained story summaries while latest-stream cards, secondary highlight rails, and tab navigation stay out of WordPress block output.
- `lanes/readability/examples/wordpress-bbc-rdfa-articlebody-import.php` demonstrates importing the BBC RDFa articleBody fixture as 5 image blocks while unsupported video placeholders and navigation stay out of WordPress block output.
- `lanes/readability/examples/wordpress-cnn-storytext-import.php` demonstrates importing the CNN storytext fixture as one heading block and 14 paragraph blocks while video, ad, disclosure, and masthead chrome stay out of WordPress block output.
- `lanes/readability/examples/wordpress-wapo-inline-gallery-import.php` demonstrates importing the Washington Post fixture as 39 paragraph blocks while gallery controls, Buy Photo links, and linked graphic preview chrome stay out of WordPress block output.
- `lanes/readability/examples/wordpress-wapo-author-media-import.php` demonstrates importing the Washington Post lead-media fixture as 28 paragraph blocks while the lead photo/caption and author bio remain available and share/comment/most-read chrome stays out of WordPress block output.
- `lanes/readability/examples/wordpress-yahoo-application-name-boundary.php` demonstrates importing the Yahoo fixture with null publisher/site metadata when only `application-name` repeats the page title, while 16 paragraph blocks and one review heading block survive and share/ad chrome stays out of WordPress block output.
- `lanes/readability/examples/wordpress-yahoo-gma-provider-chrome-cleanup.php` demonstrates importing the Yahoo/GMA fixture with retained editorial related links and no provider masthead, save/like controls, recipe promo, or share-action chrome.
- `lanes/readability/examples/wordpress-yahoo-japan-articlebody-import.php` demonstrates importing the Yahoo Japan fixture with Japanese language/site metadata, 9 paragraph blocks, 0 image blocks, and no Yahoo navigation, ranking, share, or footer chrome.
- `lanes/readability/examples/wordpress-buzzfeed-print-bio-cleanup.php` demonstrates importing the BuzzFeed fixture with null byline, two heading blocks, 17 paragraph blocks, retained inline grid images, and no print/bio/share chrome.
- `lanes/readability/examples/wordpress-lemonde-french-video-import.php` demonstrates importing the Le Monde fixture with French language metadata, 28 paragraph blocks, 9 heading blocks, a retained Dailymotion iframe, and no navigation/ad chrome.
- `lanes/readability/examples/wordpress-theverge-newsletter-media-import.php` demonstrates importing The Verge fixture with the content wrapper retained for oracle comparison, 19 paragraph blocks, 3 heading blocks, 1 image block, newsletter copy retained, and no subscribe/comment/ad chrome.
- `lanes/readability/examples/wordpress-citylab-author-feed-cleanup.php` demonstrates importing the CityLab fixture with the author bio retained, 20 paragraph blocks, 4 heading blocks, 3 image blocks, and no author RSS feed chrome.
- `lanes/readability/examples/wordpress-mozilla-main-content-cta-cleanup.php` demonstrates importing the Mozilla Firefox fixture with the main-content wrapper retained for oracle comparison, 19 paragraph blocks, and no Firefox Sync sign-in CTA chrome.
- `lanes/readability/examples/wordpress-mozilla-developer-edition-import.php` demonstrates importing the Mozilla Developer Edition fixture with feature copy retained, 4 paragraph blocks, 2 heading blocks, and head-comment chrome excluded from WordPress block output.
- `lanes/readability/examples/wordpress-aclu-drupal-panel-import.php` demonstrates importing the ACLU Drupal fixture with 33 paragraph blocks, 7 heading blocks, no image blocks, and no comment/panel chrome.
- `lanes/readability/examples/wordpress-telegraph-text-section-import.php` demonstrates importing the Telegraph fixture as 13 paragraph blocks with null published time, 0 image blocks, and publisher chrome removed.
- `lanes/readability/examples/wordpress-custom-video-embed-preservation.php` demonstrates preserving a trusted custom oEmbed iframe provider and review-mode classes while unrelated widget embeds are removed before block serialization.
- The focused standalone-video block test covers migrated posts where Mozilla retains trusted video `iframe`/`object`/`embed` media: standalone embeds become core HTML blocks for review instead of paragraph-wrapped iframe markup, while inline paragraph iframes keep their surrounding text boundary.
- `lanes/readability/examples/wordpress-char-threshold-import-boundary.php` demonstrates recovering short editorial copy from a comment-like source wrapper through `charThreshold` retry behavior while still allowing empty chrome-only sources to be skipped as null.
- The focused WordPress test covers migration output where `post_title` stores the article title separately: duplicate source `h1` content is removed, while real body section headings remain as `h2` block headings.
- The focused WordPress full-width media test covers imports where a theme/page builder emits a layout-only crop outside the paragraph column: the decorative wrapper is removed while the editorial figure remains available for image block serialization.
- The focused WordPress editorial media test covers imports where a Medium-style `postField--fillWidthImage` figure is editorial content: the figure and caption remain available for block output while the source class is stripped after the keep decision.
- The focused WordPress class-cleanup test covers imports where legacy themes or block wrappers carry source CSS classes: nested layout wrappers are simplified, class attributes are stripped like upstream Readability default post-processing, and IDs plus promoted image sources remain available for block output.
- The focused WordPress leading-action test covers imports where a source platform places author, follow, read-time, and share controls before the first content heading: the controls are removed while avatar media and editorial body content remain available for block output.
- The focused WordPress quote-wrapper test covers imports where a source theme wraps a pull quote paragraph in a layout `div`: the quote text remains while the wrapper collapses to match upstream Medium blockquote output.
- The focused WordPress phrasing-media wrapper test covers imports where source layouts put images or avatar links directly inside `div` wrappers: media is wrapped in paragraphs like upstream Readability preprocessing, and media-bearing wrappers are not flattened away.
- The focused WordPress URL cleanup test covers imports where legacy relative links and images must remain usable after leaving the source domain: URLs are made absolute and `javascript:` links are neutralized while their child content is retained.
- The focused WordPress popup-link cleanup test covers imports where older themes store annotations as `javascript:` anchors with event-handler attributes and whitespace-padded media URLs: the JavaScript is neutralized while clean absolute editorial links and image sources remain available.
- The focused WordPress trailing footer-bar test covers imports where an old source theme appends compact navigation bars with multiple links and images after the article body: the footer bar is removed while editorial content links remain absolute.
- The focused WordPress wire source-credit test covers syndicated publisher imports where a short source credit is appended after the real story body: the source link is removed before block output while a trusted metadata byline remains available separately.
- The focused footnote wrapper cleanup test covers imports where local citation links should not make a wrapper look too link-heavy: hash-only links use Mozilla's lower link-density coefficient, the wrapper collapses, and the citation link plus target remain.
- The focused body byline cleanup test covers imports where older themes place the author byline inside article content instead of metadata: the byline is exposed as metadata and the byline paragraph is not serialized as a WordPress block.
- The focused metadata entity cleanup test covers imports where old feeds or templates double-escape excerpt metadata: valid numeric references decode and invalid zero/out-of-range numeric references become replacement characters before the excerpt is stored.
- The focused div-to-paragraph and body-fallback slice keeps legacy exports that lack semantic article wrappers from importing document-head markup, and makes text-only layout `div` blocks arrive as normal paragraph blocks.
- The focused single-article cleanup tests cover exports where the source body or a broader page shell contains one real article plus empty placeholders, navigation, sidebar text, and site chrome: the article wrapper is not imported, short leading timestamp chrome is dropped, empty paragraphs are removed like upstream `_prepArticle`, and blank WordPress paragraph blocks are not emitted.
- The focused layout-table cleanup test covers exports where an old theme wraps article text or media in a one-cell table: the layout table is replaced with a `p` or `div` according to upstream `_prepArticle`, while real multi-cell tables are retained and serialized as WordPress table blocks.
- The focused presentational-markup cleanup test covers imports where old themes carry `font`, `align`, `bgcolor`, `cellpadding`, `cellspacing`, `border`, `width`, and inline `style` attributes: the editorial text and multi-cell table survive, but obsolete source styling does not enter WordPress table blocks.
- The focused font-tag cleanup test covers imports where old Classic Editor or WYSIWYG exports carry nested `font` tags: the editorial text and review-visible `face`/`size` attributes survive as spans, but obsolete `font` tags do not enter WordPress block output.
- The focused RTL direction metadata test covers imports where a source theme uses a right-to-left body/main wrapper: the article `dir` metadata remains available for downstream post handling while duplicate source title chrome is removed.
- The focused data-table marker test covers compact WordPress compatibility matrices where a source table has only one visible cell but carries upstream data-table evidence such as `summary`: the data table remains a table block, while a presentational one-cell table still collapses to paragraph copy.
- The focused hidden export cleanup test covers WordPress imports where source themes duplicate content in visually hidden or modal UI: invisible duplicate text is removed before block output, while `fallback-image` media remains available.
- The focused hidden fixture tests cover WordPress imports where page builders hide duplicate content with inline styles or wrap the real article section with short scaffold headings: hidden content is removed, visible sibling headings remain when upstream keeps them, and paragraph-section scaffolds do not become imported block headings.
- The focused line-break cleanup test covers legacy WordPress exports that encode paragraph boundaries as repeated `<br>` tags: the content is split into separate paragraph blocks while a single editorial soft break remains.
- The focused inline junk cleanup test covers legacy WordPress exports that embed theme stylesheet `link` tags and subscription `fieldset` controls inside the article body: those source fragments are removed before block output while editorial paragraphs remain.
- The focused social-widget cleanup test covers WordPress.com and Jetpack exports where `sharedaddy` like widgets, inline scripts, and inline styles are embedded in post content: runtime chrome is removed while editorial paragraphs serialize to core paragraph blocks.
- The focused metadata precedence test covers WordPress imports where source themes provide incomplete OpenGraph/Twitter tags plus wrong fallback author/title values: Dublin Core metadata wins and the article body still serializes to core blocks.
- The focused title-cleanup test covers WordPress imports where the source `<title>` combines the article title and site name with an en dash: the site suffix is removed before `post_title` selection, and duplicate source `h1` content does not become a block.
- The focused JSON-LD title disambiguation test covers WordPress imports where plugin-generated structured data contains a non-canonical `headline`: the JSON-LD `name` that matches the cleaned source document title remains the imported title, while the duplicate body `h1` is still removed from block content.
- The focused news chrome cleanup test covers WordPress imports where a heading-less news template puts timestamp and inline byline wrappers before article copy: the wrappers remain metadata/chrome, and block output starts at the first editorial paragraph.
- The focused section wrapper cleanup test covers WordPress imports where page builders wrap article copy in layout-only section shells: classes and the section wrapper are removed, while editorial paragraphs remain in block output.
- The focused readability-page wrapper cleanup test covers import pipelines that need upstream expected-HTML comparison: the optional wrapper preserves `div#readability-page-1.page`, but block output still flattens it and emptied Medium author/action wrappers collapse toward upstream shape.
- The focused Medium section-wrapper parity test covers import pipelines that compare against upstream expected HTML: a named Medium section boundary survives under `div#readability-page-1.page` for oracle output, but WordPress blocks still flatten source section shells and strip `section-content` classes.
- The focused articleBody microdata cleanup test covers WordPress themes that expose `itemprop="articleBody"` but omit expected content classes: the microdata body wins over trailing tag wrappers and serializes clean paragraph blocks.
- The focused data URI image cleanup test covers WordPress media imports where inline SVG/JPEG data payloads should survive, but tiny placeholder GIF sources should give way to responsive candidates from `data-srcset`.
- The focused block-boundary spacing test covers WordPress imports where adjacent block tags or table cells have no source whitespace: article text remains usable for search excerpts and review logs while the block serializer still emits heading, paragraph, and table blocks.
- The focused selected-root NBSP test covers classic WordPress exports where body-level table wrappers are preceded or followed by nonbreaking layout padding: padding-only root text is removed, internal nonbreaking spaces remain, and paragraph blocks serialize cleanly.
- The focused empty-heading cleanup test covers source editors that use blank heading tags as visual spacers: empty `h1`-`h6` nodes without media payload are removed before block serialization, while real headings remain and paragraph text boundaries stay readable.
- The current Mozilla Hacks `002` slice maps developer-content imports: the upstream `div#content-main`/`article role="article"` oracle boundary is retained for fixture comparison, 17 syntax-highlighted code samples survive extraction, bare absolute origins are canonicalized with a trailing slash like Mozilla's URL cleanup, and WordPress block output emits code samples as `core/code` blocks while comments, author sidebar, article navigation, and legal footer chrome are excluded.
- `lanes/readability/examples/wordpress-mozilla-hacks-code-import.php` demonstrates importing the copied Mozilla Hacks Fetch API fixture with 57 paragraph blocks, 17 code blocks, retained title/byline/site metadata, and no comment/sidebar/legal footer chrome.
- The current Atlas Obscura `article-author-tag` slice maps long-form article imports where `article:author` metadata and a selected `section#article-body` must survive for oracle comparison: six image payloads remain, NBSP-only spacer paragraphs after editorial rules are removed, and source header/navigation chrome stays out of migrated blocks.
- `lanes/readability/examples/wordpress-atlas-obscura-article-author-import.php` demonstrates importing the copied Atlas Obscura fixture with retained title/byline/site/date metadata, 6 image payloads, 30 paragraph blocks, 2 separator blocks, and no header/navigation chrome.
- The current Engadget `engadget` slice maps product-review imports with gallery and commerce controls: the upstream gallery lead images, 10 image payloads, review score/price/pros/cons summary, and one trusted YouTube iframe remain, while thumbnail strips, grid-count overlays, product identity links, and buy buttons are excluded from article HTML and WordPress blocks.
- `lanes/readability/examples/wordpress-engadget-review-gallery-import.php` demonstrates importing the copied Engadget fixture with 10 image payloads, 1 iframe payload, 32 paragraph blocks, 6 heading blocks, 5 image blocks, and no gallery/product chrome.
- The current Google SRE book `google-sre-book-1` slice maps long technical-book imports with a source table of contents around the article: the native extractor promotes the single `section#maia-main[role=main]` chapter root, preserves the symptom/cause table and editorial links, and excludes book navigation, header title, and logo media from article HTML and WordPress blocks.
- `lanes/readability/examples/wordpress-google-sre-book-import.php` demonstrates importing the copied Google SRE book fixture with 53 paragraph blocks, 10 heading blocks, 1 table block, 0 image blocks, and no book navigation/header chrome.
- The current Wikipedia `wikipedia-4` slice maps table-heavy encyclopedia imports: the long sortable film list remains one reviewable table while dynamic-list maintenance notes, category links, portal/tracking images, and CentralAutoLogin pixels are excluded from article HTML and WordPress blocks.
- `lanes/readability/examples/wordpress-wikipedia-film-list-import.php` demonstrates importing the copied Wikipedia fixture with 73 table rows, 6 paragraph blocks, 2 heading blocks, 1 table block, 0 image blocks, and no Wikipedia chrome retained.
- The current Wikipedia `wikipedia` slice maps long-form Mozilla article imports: upstream paragraph, heading, table, image, and link parity survives while MediaWiki `siteSub`, jump links, main-article hatnotes, print footer, category links, and CentralAutoLogin pixels are excluded from article HTML and WordPress blocks.
- `lanes/readability/examples/wordpress-wikipedia-mozilla-import.php` demonstrates importing the copied Wikipedia fixture with 8 retained images, 73 paragraph blocks, 37 heading blocks, 2 table blocks, 1 quote block, 0 standalone image blocks, and no MediaWiki shell chrome retained.
- The current Wikipedia `wikipedia-2` slice maps country-page imports: New Zealand metadata, headings, infobox/data tables, and expected image sources survive while MediaWiki good-article/protection status indicators, siteSub, jump links, category links, and CentralAutoLogin pixels are excluded from article HTML and WordPress blocks.
- `lanes/readability/examples/wordpress-wikipedia-country-import.php` demonstrates importing the copied Wikipedia fixture with 46 retained images, 151 paragraph blocks, 30 heading blocks, 4 table blocks, 0 standalone image blocks, and no MediaWiki status/shell chrome retained.
- The current Wikipedia `wikipedia-3` slice maps math-heavy encyclopedia imports: Hermitian-matrix metadata, excerpt, paragraphs, headings, expected formula image sources, and a review table survive while MediaWiki `siteSub`, jump links, category links, print footer, and CentralAutoLogin pixels are excluded from article HTML and WordPress blocks.
- `lanes/readability/examples/wordpress-wikipedia-hermitian-math-import.php` demonstrates importing the copied Wikipedia fixture with 67 retained math/editorial images, 62 paragraph blocks, 12 heading blocks, 1 table block, and no MediaWiki shell chrome retained.
- The current Firefox Nightly `firefox-nightly-blog` slice maps WordPress article-header author imports: an `address a[rel=author]` inside `article > header` becomes byline metadata like upstream, while the older WordPress Tavern fixture still keeps its upstream null-byline boundary.
- `lanes/readability/examples/wordpress-firefox-nightly-author-import.php` demonstrates importing the copied Firefox Nightly WordPress fixture with byline `Mike Conley`, 3 retained article images, 16 paragraph blocks, 19 heading blocks, and no comment/sidebar/download CTA chrome retained.
- The current Libération `liberation-1` slice maps French wire-service imports where a source appends a trailing AFP author/source-credit container after the article body: the native extractor preserves the byline/date/lang/site metadata and Dailymotion iframe, but removes that final source link before WordPress block output.
- `lanes/readability/examples/wordpress-liberation-wire-credit-cleanup.php` demonstrates importing the copied Libération fixture with byline `Par Sébastien Farcis`, language `fr`, 5 paragraph blocks, retained Dailymotion embed, and no trailing wire credit retained.
- The current SimplyFound `simplyfound-1` slice maps publisher imports where a Bootstrap account-approval modal and empty adsbygoogle container are appended after the real article body: the native extractor preserves title/site/lang metadata and expected text parity, but removes the modal and ad container before WordPress block output.
- `lanes/readability/examples/wordpress-simplyfound-account-modal-cleanup.php` demonstrates importing the copied SimplyFound fixture with 11 paragraph blocks, site name `SIMPLYFOUND.COM | BY: JOE WEE`, language `en`, no retained account approval modal, and no retained trailing ad slot.
- The current La Nacion `la-nacion` slice maps publisher imports where exported HTML begins with a UTF-8 BOM and the article summary lives outside the `articleBody` section as `itemprop=description`: the native extractor ignores the BOM before parsing, promotes the described NewsArticle parent, keeps the lead summary paragraph, and excludes navigation/compatibility-warning chrome from WordPress block output.
- `lanes/readability/examples/wordpress-la-nacion-bom-description-import.php` demonstrates importing the copied La Nacion fixture with 12 paragraph blocks, 1 image block, retained lead description, no retained BOM, and no navigation/compatibility chrome.
- The current dev418 `dev418` slice maps mixed-media publisher/tutorial imports where standalone images, figures, separators, and image lists are all meaningful article content: the native extractor preserves 8 image payloads, 4 figures, 4 separators, 2 media lists, and 6 media list items while matching upstream title/excerpt/readerable boundaries.
- `lanes/readability/examples/wordpress-dev418-media-list-import.php` demonstrates importing the copied dev418 fixture with 4 separator blocks, 1 image block, 2 media-list blocks, no paragraph-wrapped image list markup, and retained absolute image URLs for media review.
- The current `toc-missing` slice maps long technical article imports where an in-article table of contents should survive but a promotional interactive editor CTA should not: the native extractor keeps upstream text/metadata parity, preserves the TOC and SQL examples, and prunes the CTA body while retaining the upstream `Interactive Editor` heading boundary.
- `lanes/readability/examples/wordpress-toc-interactive-editor-cleanup.php` demonstrates importing the copied Haki Benita fixture with the TOC retained, the external editor CTA excluded, 96 paragraph blocks, 18 heading blocks, and 26 code blocks.
- The current TMZ `tmz-1` slice maps legacy publisher/WordPress-style posts where a compact `post-*` envelope supplies a split headline/date immediately before the selected `itemprop=articleBody`: the native extractor keeps the upstream subheadline/date and body text, removes the duplicate headline prefix, and unwraps invalid heading paragraph wrappers only for that legacy envelope before block serialization.
- `lanes/readability/examples/wordpress-tmz-legacy-post-envelope-import.php` demonstrates importing the copied TMZ fixture with the legacy date retained, article body retained, duplicate split headline excluded, three inline images retained, and seven paragraph blocks.
- The current `invalid-attributes` slice maps malformed legacy HTML where a div carries empty invalid attribute syntax: the native extractor marks the wrapper before libxml drops the syntax, preserves the wrapper boundary for oracle comparison, removes the marker before serialization, and keeps the invalid attribute out of migrated content.
- `lanes/readability/examples/wordpress-invalid-attribute-wrapper-cleanup.php` demonstrates importing the copied invalid-attributes fixture with the wrapper retained for oracle review, invalid attribute syntax absent, and one clean paragraph block emitted.
- The current `tumblr` slice maps legacy Tumblr theme imports where a single post is wrapped in a table/sidebar shell: the native extractor boosts the single-post `div#posts`, ignores bare string JSON-LD authors like upstream so byline stays null, keeps site/language/published metadata, and excludes official/community link chrome.
- `lanes/readability/examples/wordpress-tumblr-single-post-import.php` demonstrates importing the copied Tumblr fixture with release notes retained, theme sidebar chrome excluded, one heading block, and one paragraph block.
- The current `medicalnewstoday` slice maps publisher imports where a real article byline is nested in the article body but an outer layout wrapper includes `site_header`: the native extractor stops unlikely-byline ancestor rejection at article/main boundaries, keeps `By Ana Sandoiu` as metadata, and removes that source byline from block content.
- `lanes/readability/examples/wordpress-medicalnewstoday-byline-import.php` demonstrates importing the copied Medical News Today fixture with byline metadata retained, one article image retained for review, 26 paragraph blocks, 3 heading blocks, and no publisher ad/history chrome.
- The current `iab-1` slice maps WordPress-style publisher imports where a compact date and hero image live in source header chrome before the actual post body but the author box remains useful review content: the native extractor drops `10.15.15` and the header hero image while retaining Scott Cunningham's post author bio.
- `lanes/readability/examples/wordpress-iab-lean-author-import.php` demonstrates importing the copied IAB fixture with trusted byline/date metadata, no header date block text, no header hero image block, retained author bio, 21 paragraph blocks, and 1 author image block.
- The current `bug-1255978` slice maps Drupal/Independent-style publisher imports where the useful `itemprop=articleBody` container has a share-like `gigya-share-btns` id inside a larger column with push-notification, gallery, and Taboola chrome: the native extractor preserves the article body, removes the chrome, keeps upstream text parity, and retains the source reuse link for review.
- `lanes/readability/examples/wordpress-independent-articlebody-taboola-cleanup.php` demonstrates importing the copied Independent fixture with title/byline/date metadata, the articleBody id retained, no Taboola recommendations, no gallery promo, a retained reuse link, and 32 paragraph blocks.
- The current `comment-inside-script-parsing` slice maps legacy parser-boundary imports where a script block carries old HTML comment delimiters and a nested pseudo script tag: the native extractor keeps the upstream article text while excluding script payload text and pseudo script URLs from content.
- `lanes/readability/examples/wordpress-script-comment-parser-cleanup.php` demonstrates importing the copied parser fixture with title `Test script parsing`, no imported script comment payload, five paragraph blocks, and zero heading blocks.
- The current `lifehacker-post-comment-load` slice maps Kinja/Lifehacker imports where retained editorial lists use `data-textannotation-id` list items and comment UI is appended around the article: the native extractor preserves the story metadata, 37 paragraphs, 8 headings, 16 list items, and 9 image payloads while removing comment/ad/follow/navigation chrome.
- `lanes/readability/examples/wordpress-lifehacker-kinja-list-import.php` demonstrates importing the copied Lifehacker fixture with title/byline/site metadata, 36 paragraph blocks, 8 heading blocks, 4 list blocks, 1 quote block, no retained Kinja chrome, and no paragraph-wrapped `<ul>` lists.
- The current compact ordered-list and marked-list slice maps the copied Mozilla `ol` fixture plus an explicit WordPress migration marker boundary into WordPress output: a single retained ordered editorial item serializes as a core ordered list block, and a source list marked with `data-wp-block-list` serializes as a core unordered list block, while long encyclopedia/book/release-note list fixtures keep their existing paragraph-review block behavior.
- `lanes/readability/examples/wordpress-editorial-list-import.php` demonstrates importing marked unordered and compact ordered editorial lists with 1 unordered list block, 1 ordered list block, and no paragraph-wrapped list markup.
- The current quote-block slice maps retained upstream blockquotes in copied Mozilla fixtures and WordPress migration output: blockquotes now serialize as core quote blocks with `wp-block-quote` instead of paragraph-wrapped source markup, while surrounding article paragraphs remain ordinary paragraph blocks.
- `lanes/readability/examples/wordpress-quote-block-import.php` demonstrates importing a retained editorial quote with 1 quote block, no paragraph-wrapped quote markup, and the expected WordPress quote class.
- The current standalone native media slice maps retained `video` and `audio` elements in WordPress migration output: standalone media children serialize as core HTML blocks with resolved media URLs instead of paragraph-wrapped media markup, while nested media-wrapper broadening remains a deliberate follow-up.
- `lanes/readability/examples/wordpress-native-media-element-import.php` demonstrates importing native video/audio elements with 2 HTML blocks, retained video/audio URLs, and no paragraph-wrapped media.
- The current class-weight rearm slice maps threshold retries for legacy imports where a short high-weight wrapper can beat the real article body: after strict and unlikely-candidate-relaxed attempts remain below `charThreshold`, the extractor retries without class/id weighting before returning the longest nonempty candidate.
- `lanes/readability/examples/wordpress-class-weight-rearm-import.php` demonstrates importing a legacy theme where the longer article body is recovered, teaser copy is excluded, and two paragraph blocks are emitted.
- Verification delta for this isolated micro-slice: syntax checks passed for changed PHP files; `php tools/run-tests.php lanes/readability/tests/ArticleExtractorTest.php` passed 1 selected test file, 1861 assertions, and 0 failures; `php lanes/readability/examples/wordpress-class-weight-rearm-import.php` reported `Recovered body: yes`, `Teaser retained: no`, and `Paragraph blocks: 2`; root harness not run - isolated micro-slice.
- Dependency closure: no new support component is needed; this reuses the existing bounded DOM scoring, retry orchestration, and WordPress block serializer.
- The focused SVG sprite cleanup test covers WordPress themes that repeat inline symbol sprite sheets around article content: duplicate sprites are removed by symbol signature, but reusable symbols and editorial inline SVG diagrams remain available to block output.
- The focused visible-date metadata test covers WordPress themes that expose a visible `<time datetime>` in article chrome: the date does not become trusted published metadata without upstream-supported JSON-LD, `article:published_time`, or `parsely-pub-date` evidence.
- The focused syndication footer cleanup test covers syndicated WordPress imports where a source platform appends an `Originally published at` note after the article body: the source note and link are removed before paragraph block serialization.
- The focused Medium page-break cleanup test covers source exports that split a long Medium article with `<hr>` separators: editorial sections remain in order, but separator rules are not emitted as WordPress paragraph blocks.
- The focused caption class preservation test covers migration pipelines that need selected WordPress media classes for review: configured class tokens survive, while unconfigured source theme classes are removed like upstream Readability default cleanup.
- The focused figure credit cleanup test covers media migrations where a source theme embeds a photo-credit-only link inside `figcaption`: the credit wrapper and text are removed before block serialization, while the image and requested caption classes remain available.
- The focused Guardian media test covers publisher imports where captions are emitted as list items immediately after media figures: the media root and image payloads match the upstream fixture, theme chrome is removed, and retained figures serialize as image blocks instead of paragraph blocks.
- The focused NYT media test covers publisher imports where caption and credit are embedded in a rich schema.org figure: media-viewer caption/credit attributes, figcaption class/text, and image source survive for review, the hidden reader-feedback prompt is removed, and the retained figure serializes as an image block.
- The focused NYT continuation test covers publisher imports where hidden ad interrupters include "continue reading" anchors between body sections: upstream continuation anchors and paragraph ids survive, the hidden ad and related story rail are removed, and block output keeps one image block plus 23 paragraph blocks.
- The focused NYT utility media test covers publisher imports where several figures have no child `img` because the useful image URL is only on the figure `itemid`: native cleanup creates image payloads like upstream, preserves captions/credits, removes related-card and advertisement chrome, and block output keeps 7 image blocks plus 43 paragraph blocks.
- The focused NYT debt article test covers publisher imports where the upstream-selected article body is nested below the story wrapper and includes interactive chart/share/link chrome: the wrapper is promoted, the lead image and dense article paragraphs survive, share tools and chart chrome are removed, related-link cards are excluded, and block output keeps 1 image block plus 47 paragraph blocks.
- The focused NYT section-front test covers publisher collection pages where many story cards are recirculation rather than an article body: primary media cards and selected summaries survive for review, while latest-stream cards, secondary highlight rails, ad wrappers, and tab navigation are removed before WordPress block output.
- The focused BBC RDFa test covers publisher imports where the useful article root is marked with `property=articleBody`: the root wins selection, upstream paragraph/heading/image boundaries match, unsupported video placeholder shells are removed, and block output keeps 5 image blocks without navigation chrome.
- The focused CNN storytext test covers publisher imports where a `main` container has social-anchor classes but contains the actual story: the storytext root wins selection, SmartAsset attribution survives, and video/ad/disclosure/masthead chrome is removed before block output.
- The focused Wapo inline gallery test covers publisher imports where Washington Post PageBuilder injects gallery controls and linked graphic preview wrappers into article flow: gallery UI and preview image chrome are removed, while editorial video captions, map captions, article links, and the final inline map image remain in paragraph block output.
- The focused Wapo author/media test covers publisher imports where Washington Post places lead media before the selected article and author bio after it: the article-body envelope is retained for oracle output, top byline/share chrome is removed, and block output keeps 28 paragraph blocks without share/comment/most-read chrome.
- The focused Yahoo application-name test covers publisher/homepage imports where a source provides `application-name` as a page-title string rather than a publisher name: native siteName remains null like upstream unless JSON-LD publisher or `og:site_name` exists, preventing title strings from being stored as source-site metadata.
- The focused Yahoo/GMA provider chrome test covers syndicated Yahoo article pages where the provider masthead, save/like toolbar, topic bar, share controls, and breaking-news promo should not become migrated post content while upstream-retained related editorial links remain.
- The focused Yahoo Japan articleBody test covers Japanese publisher pages where the useful story copy is nested in `ynDetailText` under a generic `paragraph` wrapper: that wrapper wins candidate selection, 9 editorial paragraphs survive, and top navigation, ranking, share, and footer chrome stay out of WordPress block output.
- The focused BuzzFeed print/bio test covers listicle imports where print-only image links, source author bios, contact blocks, bottom share controls, and promoted ad bylines should not become migrated blocks while inline grid images and story text remain.
- The focused Le Monde French video test covers non-English publisher imports where a trusted Dailymotion iframe and articleBody section structure should survive while subscription/navigation/ad chrome is removed.
- The focused The Verge test covers publisher imports where a Next/Vox article body uses a generic `div#content`, a floated pullquote wrapper, responsive image markup, and an embedded paid-newsletter card: the content and pullquote wrappers remain for oracle parity, reviewable newsletter text survives, and subscribe/comment/sponsor/rail chrome is excluded from block output.
- The focused CityLab test covers publisher imports where an author bio section includes a source RSS feed list: the author bio remains available for review, but the feed link/list is removed before WordPress block serialization.
- The focused Mozilla Firefox test covers product/support pages where the useful article body sits in `main#main-content` and a trailing Sync CTA follows the article: the main-content boundary remains available for oracle parity while sign-in CTA links are excluded from block output.
- The focused ACLU test covers Drupal panel layouts where the article body is nested below a sidebar-labeled wrapper: the native extractor keeps the body text and headings while dropping comments, share links, banners, and theme media from block output.
- The focused Telegraph text-section test covers publisher imports where the upstream fixture excludes image interrupters: dateCreated-only JSON-LD does not become trusted publish metadata, text-section wrappers survive for oracle output, and block output contains paragraphs without media-credit image blocks.
- The focused SimplyFound account-modal test covers publisher pages where account-approval or login workflow dialogs are appended near the article tail: trailing modal UI and ad slots are removed after substantial content, while editorial article paragraphs remain serialized as WordPress blocks.
- The focused La Nacion BOM/description test covers publisher exports where a BOM appears before doctype and a schema.org lead summary is a sibling of the selected article body: title/excerpt extraction remains clean, the summary becomes an importable paragraph block, and source navigation/compatibility chrome is excluded.
- The focused parse-options tests cover review-mode class retention, custom trusted iframe host preservation, unknown widget iframe removal, upstream-shaped `maxElemsToParse` aborts for overly large source documents, `charThreshold` longest-nonempty retry recovery, and null results for chrome-only imports.
- The focused Tumblr single-post test covers WordPress imports from older Tumblr exports where source theme sidebars and official/community link lists are larger than the post chrome: the release-note post body wins, bare string JSON-LD author values do not become bylines, and block output keeps only the post title plus release-note paragraph.
- The focused Medical News Today byline test covers publisher imports where source layout classes contain `site_header` above the article: article-scoped bylines still become post metadata, while source byline and ad/history chrome stay out of paragraph blocks.
- The focused Independent articleBody test covers Drupal-era publisher exports where the article body is misidentified as sharing chrome by id: articleBody evidence wins, Video.js hidden controls and in-article gallery promos are pruned, and Taboola recommendations do not enter block output.
- The focused Mozilla social-buttons test covers WordPress-style share widget imports where source sharing controls are adjacent to real article copy: five expected editorial paragraphs become paragraph blocks while Facebook/Twitter/mail share UI stays out of article HTML and block output.
- The focused definition-list block test covers encyclopedia and technical imports where upstream retained `dl` structures should remain semantically reviewable: standalone definition lists now serialize as WordPress HTML blocks instead of paragraph-wrapped `dl` markup, and the affected copied Wikipedia fixtures record the expected paragraph-to-HTML-block shift.
- `lanes/readability/examples/wordpress-definition-list-import.php` demonstrates importing a retained definition list with one HTML block, no paragraph-wrapped `dl` markup, and two surrounding paragraph blocks.
- Verification delta for this isolated micro-slice: syntax checks passed for changed PHP files; `php tools/run-tests.php lanes/readability/tests/ArticleExtractorTest.php` passed 1 selected test file, 151 tests, 1891 assertions, and 0 failures; `php lanes/readability/examples/wordpress-definition-list-import.php` reported `Definition list HTML block: yes`, `Paragraph-wrapped definition list: no`, and `Paragraph blocks: 2`; root harness not run - isolated micro-slice.
- Dependency closure: no new support component is needed; this reuses the existing native DOM post-processing and WordPress block serializer.
- The focused captioned embed-wrapper block test covers legacy WordPress and publisher imports where a retained provider iframe/video sits in a div/section with short caption text: the wrapper now serializes as a single HTML review block, and the copied Independent `bug-1255978` fixture records the paragraph-to-HTML-block shift for its retained publisher video wrapper.
- The focused nested embed-wrapper block test covers legacy WordPress and publisher imports where a retained provider iframe/video is tightly nested inside one wrapper layer with short caption text: the full wrapper now serializes as a single HTML review block, while inline paragraph embeds stay in paragraph context.
- `lanes/readability/examples/wordpress-captioned-embed-wrapper-import.php` demonstrates importing direct and nested retained captioned iframe wrappers with two HTML blocks, no paragraph-wrapped embed wrapper, one inline embed paragraph retained, and three paragraph blocks.
- Verification delta for this isolated micro-slice: syntax checks passed for changed PHP files; `php tools/run-tests.php lanes/readability/tests/ArticleExtractorTest.php` passed 1 selected test file, 153 tests, 1901 assertions, and 0 failures; `php lanes/readability/examples/wordpress-captioned-embed-wrapper-import.php` reported `Captioned embed HTML blocks: 2`, `Embed wrapper retained: yes`, `Nested wrapper retained: yes`, `Inline embed paragraph retained: yes`, `Paragraph-wrapped embed wrapper: no`, and `Paragraph blocks: 3`; root harness not run - isolated micro-slice.
- Dependency closure: no new support component is needed; this reuses the existing native DOM cleanup, media-preservation, and WordPress block serializer.
- The focused empty-paragraph cleanup test covers classic and migrated editor exports where blank `<p>` nodes appear between real paragraphs: the copied Mozilla `remove-extra-paragraphs` fixture now has exact expected-text and retained nonempty paragraph parity, while empty and whitespace-only source paragraphs are removed before WordPress block serialization.
- `lanes/readability/examples/wordpress-empty-paragraph-cleanup.php` demonstrates importing the copied remove-extra-paragraphs fixture with five paragraph blocks, no blank source paragraphs retained, and no blank WordPress paragraph blocks emitted.
- Verification delta for this isolated micro-slice: syntax checks passed for changed PHP files; `php tools/run-tests.php lanes/readability/tests/ArticleExtractorTest.php` passed 1 selected test file, 154 tests, 1915 assertions, and 0 failures; `php lanes/readability/examples/wordpress-empty-paragraph-cleanup.php` reported `Paragraph blocks: 5`, `Blank source paragraphs retained: no`, and `Blank WordPress blocks emitted: no`; root harness not run - isolated micro-slice.
- Dependency closure: no new support component is needed; this reuses the existing native DOM extraction, empty-paragraph cleanup, Mozilla fixture comparison helpers, and WordPress paragraph block serializer.
- The focused script/style tag cleanup test covers legacy exports where executable or stylesheet payloads are interleaved with real article paragraphs: the copied Mozilla `style-tags-removal` and `remove-script-tags` fixtures now assert that retained upstream paragraphs/headings serialize to matching WordPress blocks while raw `<script>` and `<style>` tags are absent from block output.
- `lanes/readability/examples/wordpress-script-style-tag-cleanup.php` demonstrates importing both copied fixtures with style-tags-removal emitting 2 paragraph blocks and 2 heading blocks, remove-script-tags emitting 5 paragraph blocks and 0 heading blocks, and script/style tags imported `no` for both.
- Verification delta for this isolated micro-slice: syntax checks passed for changed PHP files; `php tools/run-tests.php lanes/readability/tests/ArticleExtractorTest.php` passed 1 selected test file, 154 tests, 1927 assertions, and 0 failures; `php lanes/readability/examples/wordpress-script-style-tag-cleanup.php` reported `Script/style tags imported: no` for both copied fixtures; root harness not run - isolated micro-slice.
- Dependency closure: no new support component is needed; this reuses the existing native DOM script/style stripping, Mozilla fixture comparison helpers, and WordPress paragraph/heading block serializer.

## Next Task

Map `lifehacker-working` or another remaining Kinja/comment-heavy fixture with a targeted upstream oracle, or map another already-copied cleanup fixture with exact expected-content and WordPress block evidence.
