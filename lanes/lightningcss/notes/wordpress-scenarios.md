# LightningCSS WordPress Scenario

Shared-hosting CSS transforms for themes, blocks, Playground, and plugin build-free delivery.

## Current Native Slice

Native CSS comment/whitespace minifier, declaration block parser, CSSOM-style direct declaration plus margin/padding, logical margin/padding set ordering across physical fallbacks, background, mask-border-source, border, flex-flow, animation-name, grid-area, grid-template, and grid shorthand access, focused animation name/timing/duration/delay/iteration/fill-mode longhand minification, focused animation shorthand minification with keyword-name disambiguation and scroll/view timeline cleanup, focused animation longhand-to-shorthand declaration composition with list-length guardrails and prefixed group isolation, focused animation-range start/end value minification, range shorthand composition, var() guard preservation, and later animation shorthand reset behavior, focused transition duration/delay/timing-function longhand minification, focused transition shorthand value minification, focused transition longhand-to-shorthand declaration composition, focused transition logical block-axis expansion, legacy Safari transition prefixing for direction-sensitive inline logical properties, transform transitions, transform-list math-function minification, transform translate/scale function normalization, Safari 12 `clamp()` lowering, mask/mask-border transition property names, filter value minification, target-aware `filter`/`backdrop-filter` prefixing including `@supports` backdrop-filter condition prefix/removal, box-shadow value minification, target-specific box-shadow WebKit prefixing/removal and advanced-color fallback/support layering including oklch alpha shadows, text-shadow value minification and advanced-color fallback/support layering, text-decoration WebKit/Mozilla prefixing plus advanced-color fallback/support layering, text-emphasis minification/composition/WebKit prefixing plus advanced-color fallback/support layering, caret color/shape minification plus advanced-color fallback/support layering, list-style marker/image/position minification, shorthand composition, old WebKit gradient prefixing, and advanced-color fallback/support layering, `image-set()` URL/string/type descriptor normalization plus conservative `-webkit-image-set()` target prefixing, focused `@import` URL/supports/media minification, focused `@supports` declaration/not/AND/OR/selector/unknown condition minification, declaration-body `calc()`/`hsl()` value minification while preserving supports-condition values, and adjacent `@supports` block merging, focused `@container` name/range/not/style/scroll-state prelude minification, adjacent identical `@container`/style-rule merging, container query unit `calc()` folding, and `container-name`/`container-type` shorthand composition, direct mask-border shorthand prefixing, mask-border longhand composition, prefixed `-webkit-mask-box-image` longhand composition, focused LCH gradient fallback layers for direct and composed mask-border values, lab `@supports` fallback emission for advanced-color mask, mask-border, filter, box-shadow, text-shadow, text-decoration, text-emphasis, caret, and list-style values that include CSS custom properties, standalone mask-composite/mask-mode fallbacks, direct `mask-image` prefixing, `mask` luminance source-type fallback emission, mask longhand composition to modern and `-webkit-mask` shorthands, and focused LCH gradient fallback layers for direct and composed mask values, value-level color keyword and hsl/rgb function shortening, focused linear `calc()` arithmetic with nested parentheses, scalar multiplication/division, signed operands, exponent notation, mixed-unit like-term reduction, conservative unknown-unit/var() preservation, focused `min()`/`max()`/`clamp()`/`round()`/`rem()`/`mod()`/`hypot()`/`sqrt()`/`pow()`/`log()`/`exp()`/`abs()`/`sign()` math-function minification, compact nested math-function serialization inside `calc()`, math-function operator spacing, a small stylesheet parser that distinguishes style rules, at-rule statements/blocks, declarations, selectors, and nested WordPress rules, selector specificity packing/comparison for override ordering, media query range/feature/list normalization, and a focused `@custom-media` transformer with upstream-style error locations.

`examples/wordpress-stylesheet-parser.php` parses the block-theme CSS fixture and reports the top-level rule count, first selector list, and declaration count. `examples/wordpress-border-cssom.php` models a theme.json-style block border where `border-color` recomposes a full physical border shorthand while side-specific overrides remain guarded. `examples/wordpress-animation-cssom.php` rewrites keyframe animation names from a shorthand while preserving duration/easing/fill-mode tokens, and falls back to a separate `animation-name` list when a block style needs multiple names. `examples/wordpress-animation-longhand-minifier.php` minifies block animation preset longhands so quoted keyframe identifiers, reserved keyword strings, easing aliases, duration/delay values, and list-valued controls serialize consistently with LightningCSS. `examples/wordpress-animation-shorthand-minifier.php` canonicalizes a block animation shorthand with a quoted keyframe name, cubic-bezier alias, delay, iteration count, fill mode, and scroll timeline without Node. `examples/wordpress-animation-composition.php` folds a cover block entrance preset from longhands into the upstream animation shorthand while preserving scroll timeline semantics. `examples/wordpress-animation-range.php` composes scroll-linked cover block `animation-range-start`/`animation-range-end` declarations into the upstream shorthand while preserving a preceding animation shorthand. `examples/wordpress-transition-longhand-minifier.php` minifies block button transition presets, including simple time `calc()` values and common easing aliases. `examples/wordpress-transition-shorthand-minifier.php` minifies block interaction transition shorthands, preserving author intent while canonicalizing property/duration/timing/delay order and omitting default `ease`. `examples/wordpress-transition-composition.php` folds block pagination interaction transition longhands into compact shorthand while preserving list cycling and non-default delay semantics. `examples/wordpress-logical-transition.php` expands block-axis logical transition targets such as `margin-block` into physical top/bottom transition properties for block spacing animations. `examples/wordpress-legacy-transition-prefixer.php` emits legacy Safari transition fallbacks for navigation block spacing animations that combine `margin-inline-start` with `transform`. `examples/wordpress-filter-prefixer.php` emits `-webkit-backdrop-filter` and `-webkit-filter` fallbacks for sticky glass-header template parts that use block theme variables. `examples/wordpress-box-shadow-minifier.php` minifies query-grid card shadow presets by normalizing inset ordering, zero lengths, default blur/spread values, and rgba black alpha colors without Node. `examples/wordpress-box-shadow-prefixer.php` emits old Chrome/WebKit `-webkit-box-shadow` fallbacks plus lab `@supports` guards for query-card shadow tokens without Node. `examples/wordpress-text-shadow-prefixer.php` emits sRGB base text-shadow values plus a lab `@supports` guard for headline shadow tokens without Node. `examples/wordpress-text-decoration-prefixer.php` emits sRGB base link underline decoration values plus a lab `@supports` guard and composes dotted underline fallbacks for Safari without Node. `examples/wordpress-text-emphasis-prefixer.php` emits sRGB/lab fallbacks plus legacy WebKit position prefixes for editorial annotation emphasis marks without Node. `examples/wordpress-caret-prefixer.php` emits sRGB/lab fallbacks for editor and search input caret styles without Node. `examples/wordpress-list-style-prefixer.php` emits sRGB/lab fallbacks for list block marker gradients without Node. `examples/wordpress-image-set-prefixer.php` emits responsive cover image and retina list marker `-webkit-image-set()` fallbacks plus modern `image-set()` declarations without Node. `examples/wordpress-container-query-minifier.php` composes a query-card `container` shorthand and minifies a named `@container` style query for block layouts without Node. `examples/wordpress-container-merge-cq-units.php` merges adjacent query-card `@container` rules and folds `calc(1cqw + 2cqw)` gap math for block layouts without Node. `examples/wordpress-mask-transition-prefixer.php` emits legacy WebKit transition property names for framed cover block styles that animate `mask-border` and `mask` without Node. `examples/wordpress-mask-border-prefixer.php` composes decorative cover-frame mask-border longhands into compact modern and `-webkit-mask-box-image` fallbacks with sRGB fallback layers before the LCH source layers for shared hosting. `examples/wordpress-mask-border-var-supports.php` emits the upstream-style sRGB base declarations plus lab `@supports` fallback for a cover-frame mask-border whose slice is controlled by a CSS custom property. `examples/wordpress-mask-image-prefixer.php` composes an LCH gradient media-cover mask-image/position/size/repeat/origin/clip/mode stack into sRGB fallback and modern advanced-color `-webkit-mask`/`mask` layers for themes that cannot run a Node build step. `examples/wordpress-background-color-fallback.php` emits upstream-style sRGB and display-p3 fallback layers before an LCH cover-block background gradient for shared hosting. `examples/wordpress-custom-property-color-fallback.php` emits upstream-style sRGB base custom properties plus display-p3 and lab `@supports` overrides for theme color tokens that use Oklab or display-p3 values. `examples/wordpress-logical-spacing-cssom.php` models logical block spacing controls where a later physical fallback must stay in place and a new logical declaration is appended. `examples/wordpress-grid-area-cssom.php` reads row and column placement from a four-part `grid-area` shorthand for block layout migrations. `examples/wordpress-grid-template-cssom.php` extracts explicit block-template area rows, tracks, and initial auto-placement from longhands as `grid-template`/`grid` shorthand values. `examples/wordpress-mask-border-cssom.php` extracts the image source from decorative mask-border declarations used by block or theme frame styles. `examples/wordpress-custom-media-transformer.php` expands block-theme breakpoint and motion aliases from `@custom-media` before minification so shared-hosting tooling can consume modern theme CSS without Node. `examples/wordpress-custom-media-boolean-guard.php` models an incompatible print alias used inside screen styles and now returns structured source locations for the rejected @media rule and referenced @custom-media rule. The declaration helper now exposes background color/image/repeat/position/size CSSOM behavior needed by block background controls and theme style migration tools, mask-border source behavior for decorative frame migration, border shorthand behavior used by block style controls, logical margin/padding ordering behavior for fallback-preserving spacing migrations, prefixed and unprefixed `flex-flow` direction/wrap behavior used by block layout migrations, animation-name updates for keyframe migration tools, animation longhand/shorthand minification, declaration composition, and animation-range composition for block style presets, transition longhand/shorthand value minification, transition declaration composition, block-axis logical transition expansion, direction-sensitive inline transition prefixing, transform transition prefixing, filter/backdrop-filter prefixing, box-shadow value minification and target-specific prefix/fallback generation, text-shadow value minification and target-specific advanced-color fallback generation, text-decoration prefix/fallback generation for link underline styles, text-emphasis minification/composition/prefix/fallback generation for editorial annotation marks, caret color/shape minification and target-specific advanced-color fallback generation for editor controls, list-style marker/image minification, composition, old WebKit gradient prefixing, target-specific advanced-color fallback generation for list blocks, image-set responsive asset prefixing for cover images and marker graphics, container query/shorthand minification, adjacent container block merging, and cqw/cqh/cqi/cqb/cqmin/cqmax calc folding for query-card block layouts, mask/mask-border transition prefixing, mask-border shorthand/longhand prefix composition, mask-image/mask longhand composition, and focused advanced-color fallback layering including lab `@supports` emission when variables prevent a single advanced-color fallback layer, Oklab/Oklch-to-Lab target values, custom-property sRGB/display-p3/lab supports layering, plus background gradient sRGB/display-p3 fallback layering, and grid placement/template access for layout tooling. The specificity helper covers WordPress override selectors such as block-button hover styles and ID-scoped navigation rules. The media query helper normalizes responsive block-theme breakpoints such as min/max width aliases, comparison ranges, hover/update features, ratios, and simple same-unit calc values. The custom media transformer maps focused upstream `src/lib.rs::test_custom_media` cases for list substitution, recursive and later declarations, media type factoring, negation simplification, undefined/circular reference errors, unsupported media-type boolean-logic errors, upstream-style diagnostic line/column reporting, and preserving declarations when requested. This gives shared-hosting and Playground tooling a native AST boundary before adding bundling or broader transformer semantics.

`examples/wordpress-fluid-spacing-calc.php` models fluid block stack spacing and cover offsets whose calc expressions fold without Node while preserving mixed-unit expressions. `examples/wordpress-fluid-spacing-math-functions.php` models query-card spacing tokens whose `round()`, `rem()`, `mod()`, nested `round()` inside `calc()`, and `clamp()` expressions fold without Node. `examples/wordpress-advanced-math-functions.php` models a depth-styled cover block whose `abs()`, `hypot()`, `pow()`, and `sign()` expressions fold without Node. `examples/wordpress-transform-math-minifier.php` models a tilted cover block hover style whose `mod()`/`rem()` angle math folds inside adjacent transform functions and whose `clamp()` border width lowers for Safari 12 without Node. `examples/wordpress-transform-function-minifier.php` models lift/press cover block transforms whose `translate3d()`/`translateX()` and `scale()`/`scale3d()` functions reduce to compact upstream-style transform functions without Node. `examples/wordpress-container-query-validation.php` models a block layout build step that rejects an invalid `@container none (...)` alias and reports a structured diagnostic instead of silently emitting CSS that upstream LightningCSS rejects. `examples/wordpress-import-supports.php` models a layered block stylesheet import guarded by `supports(display:grid)` and responsive media, with `@charset` removed and the import URL normalized for build-free delivery. `examples/wordpress-supports-rule-minifier.php` models a supports-gated query block layout fallback where redundant grouping is removed but the `not` branch remains correctly parenthesized. `examples/wordpress-supports-value-minifier.php` models supports-gated gallery width math and heading color values where declarations are minified while the supports conditions remain semantic guards. `examples/wordpress-supports-backdrop-filter-prefixer.php` models a glass header guarded by `@supports (backdrop-filter: ...)` and emits the Safari-targeted `-webkit-backdrop-filter` condition and declaration fallback without Node.

## Next Task

Broaden transform function minification to rotate/skew/matrix and transform longhand properties, continue `@container` error recovery, or continue the unresolved percentage/full-precision edge cases around `test_exp`/`test_sign`.
