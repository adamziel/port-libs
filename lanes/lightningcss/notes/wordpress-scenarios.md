# LightningCSS WordPress Scenario

Shared-hosting CSS transforms for themes, blocks, Playground, and plugin build-free delivery.

## Current Native Slice

Native CSS comment/whitespace minifier, declaration block parser, CSSOM-style direct declaration plus margin/padding, logical margin/padding set ordering across physical fallbacks, background, mask-border-source, border, flex-flow, animation-name, grid-area, grid-template, and grid shorthand access, focused font-family/font-stretch minification, focused font shorthand value minification and longhand/shorthand composition, focused `@font-face` source/range minification, focused `@font-palette-values` color override minification and advanced-color fallback/support layering, focused `@font-feature-values` OpenType alias minification/merging, focused `@property` registration minification/validation for typed design tokens, focused `@keyframes` rule minification for block animation names/selectors/duplicate rules, focused animation name/timing/duration/delay/iteration/fill-mode longhand minification, focused animation shorthand minification with keyword-name disambiguation and scroll/view timeline cleanup, focused animation longhand-to-shorthand declaration composition with list-length guardrails and prefixed group isolation, focused animation-range start/end value minification, range shorthand composition, var() guard preservation, and later animation shorthand reset behavior, focused transition duration/delay/timing-function longhand minification, focused transition shorthand value minification, focused transition longhand-to-shorthand declaration composition, focused transition logical block-axis expansion, legacy Safari transition prefixing for direction-sensitive inline logical properties, transform transitions, transform-list math-function minification, transform translate/scale/rotate/skew/matrix function normalization, transform translate/rotate/scale longhand normalization, Safari 12 `clamp()` lowering, color-scheme fallback flags, top-level and nested `light-dark()` fallback lowering for mapped variable, advanced-color, relative `rgb()`/`color()`, and `color-mix()` cases, Firefox advanced-color serialization inside `light-dark()`, Safari 13 LightDark feature-exclusion preservation, mask/mask-border transition property names, filter value minification, target-aware `filter`/`backdrop-filter` prefixing including `@supports` backdrop-filter condition prefix/removal, box-shadow value minification, target-specific box-shadow WebKit prefixing/removal and advanced-color fallback/support layering including oklch alpha shadows, text-shadow value minification and advanced-color fallback/support layering, text-decoration WebKit/Mozilla prefixing plus advanced-color fallback/support layering, text-emphasis minification/composition/WebKit prefixing plus advanced-color fallback/support layering, caret color/shape minification plus advanced-color fallback/support layering, list-style marker/image/position minification, shorthand composition, old WebKit gradient prefixing, and advanced-color fallback/support layering, `image-set()` URL/string/type descriptor normalization plus conservative `-webkit-image-set()` target prefixing, focused `@namespace` URI serialization, ordering validation, namespace-qualified selector preservation, and namespace attribute selector normalization, focused `@import` URL/supports/media minification, focused `@supports` declaration/not/AND/OR/selector/unknown condition minification, declaration-body `calc()`/`hsl()` value minification while preserving supports-condition values, and adjacent `@supports` block merging, focused `@container` name/range/not/style/scroll-state prelude minification, adjacent identical `@container`/style-rule merging, container query unit `calc()` folding, `container-name`/`container-type` shorthand composition, and errorRecovery-style omission/warning reporting for invalid `@media unknown(foo)` and `@container unknown(foo)` rules, focused CSS nesting lowering for simple `&` selectors, leading-combinator implicit parent insertion, complex parent placement through descendant `:is(...)` wrappers, attached type and namespace-attached selectors, explicit nesting include/exclude target API behavior, target-gated attached selector-list suffix distribution, selector-list `:is(...)` parent references, top-level `:scope` attachment, `@nest` suffix selectors, recursive nested style rules, nested pseudo-element selectors compacted to `:before`/`:after`, nested `@media`/`@supports`/`@container`/`@layer` blocks, nested `@scope` boundary/root lowering, styled-jsx placeholder media-query and declaration-value recovery, no-target nested parent-reference whitespace preservation, no-target attached selector preservation, and no-target implicit nested selector canonicalization for preserved modern block CSS, direct mask-border shorthand prefixing, mask-border longhand composition, prefixed `-webkit-mask-box-image` longhand composition, focused LCH gradient fallback layers for direct and composed mask-border values, lab `@supports` fallback emission for advanced-color mask, mask-border, filter, box-shadow, text-shadow, text-decoration, text-emphasis, caret, and list-style values that include CSS custom properties, standalone mask-composite/mask-mode fallbacks, direct `mask-image` prefixing, `mask` luminance source-type fallback emission, mask longhand composition to modern and `-webkit-mask` shorthands, and focused LCH gradient fallback layers for direct and composed mask values, value-level color keyword and hsl/rgb function shortening, focused linear `calc()` arithmetic with nested parentheses, scalar multiplication/division, signed operands, exponent notation, mixed-unit like-term reduction, conservative unknown-unit/var() preservation, focused `min()`/`max()`/`clamp()`/`round()`/`rem()`/`mod()`/`hypot()`/`sqrt()`/`pow()`/`log()`/`exp()`/`abs()`/`sign()` math-function minification including `exp(1)` constant cancellation and unresolved `sign(1%)` percentage guard preservation, compact nested math-function serialization inside `calc()`, math-function operator spacing, a small stylesheet parser that distinguishes style rules, at-rule statements/blocks, declarations, selectors, and nested WordPress rules, selector specificity packing/comparison for override ordering, media query range/feature/list normalization, and a focused `@custom-media` transformer with upstream-style error locations.

This run adds focused upstream `font-family` minification for theme typography: safe quoted author family names unquote, quoted generic/CSS-wide family names stay strings, empty family names serialize as `""`, `@font-face` keeps reserved names quoted, and `font-stretch: expanded` becomes `125%`.

This run adds focused upstream `font` shorthand composition for theme typography: compatible longhands fold into one compact shorthand, `all-small-caps` remains a guarded longhand, a simple later `line-height` folds into an existing shorthand, `var(--lh)` remains separate, and default `normal` components are omitted while explicit numeric weights are preserved.

This run adds focused upstream `@font-face` source and range minification for self-hosted theme fonts: safe font URLs unquote, `local(...)` names normalize, `format(...)`/`tech(...)` descriptors compact, and `unicode-range` values collapse to compact uppercase wildcard ranges where possible.

This run adds focused upstream `@font-palette-values` minification and advanced-color fallback handling for color-font theme styles: palette override colors compact from hex, rgb(), and named colors, variable-backed override colors preserve the upstream separator boundary, `font-palette` custom identifiers serialize without declaration whitespace, Chrome 90 targets receive an sRGB fallback descriptor before the original advanced-color descriptor, variable-backed advanced-color overrides move the Lab value into a guarded `@supports` rule, and existing Lab-support contexts remain unchanged.

This run adds focused upstream `@font-feature-values` minification for block typography: font family lists are safely unquoted, repeated feature blocks merge, duplicate feature declarations keep the later value, and adjacent same-family rules collapse into one compact OpenType feature alias rule.

This run adds focused upstream `@property` minification/validation for registered block design tokens: descriptor order canonicalizes, syntax grammar strings compact, typed color/length/string/time/url/image initial values minify, duplicate same-name registrations keep the later descriptor values, and upstream invalid descriptor/name cases are rejected. `examples/wordpress-property-registration-minifier.php` models registered color and motion-duration block tokens without Node.

This run adds focused upstream position inset composition for cover and media overlays: physical `top`/`right`/`bottom`/`left` offsets collapse to the shortest `inset` shorthand, logical block/inline inset longhands collapse to axis shorthands, and earlier logical longhands are dropped when an intervening `inset` shorthand resets them. `examples/wordpress-position-inset-minifier.php` models cover block background and inner-container offsets without Node.

This run adds focused upstream `@page` minification and validation for WordPress print/PDF export styles: page selectors and pseudo-classes compact, named page lists collapse to comma-separated syntax, page margin boxes remain nested with minified declarations, `0.5cm`/`0.5in` margin lengths shorten, and invalid nested page at-rules are rejected. `examples/wordpress-page-rule-minifier.php` models chapter, toc, and index export page rules without Node.

This run completes focused upstream `@namespace` serialization for namespaced SVG selectors used by block icon and social/navigation markup: namespace `url(...)` sources serialize as quoted URI strings, prefixed namespace declarations and type selectors are preserved, namespace-qualified attribute selector values are quoted, empty attribute namespaces collapse to ordinary attributes, and late namespace declarations after style rules are rejected. `examples/wordpress-namespace-svg-selectors.php` models SVG and xlink namespace selectors without Node.

This run adds focused upstream errorRecovery behavior for block CSS ingestion: invalid `@container unknown(foo)` and `@media unknown(foo)` rules can be omitted with structured warnings while following valid block CSS still minifies. The recovery slice now also drops compound invalid `@container ... and unknown(foo)` and `@media ... and unknown(foo)` experimental condition blocks while keeping later valid CSS. `examples/wordpress-container-error-recovery.php` models preserving a query block rule after dropping invalid experimental container and media condition rules without Node.

This run adds focused upstream `border-radius` shorthand value compaction for rounded block/card styles: repeated corner lists collapse to the shortest equivalent value, slash-separated vertical radii compact independently, zero lengths lose units, and legacy `-webkit-`/`-moz-` radius declarations get the same value-level minification. `examples/wordpress-border-radius-minifier.php` models rounded group and image block CSS without Node. Dependency closure: no new support component is needed; the slice reuses `CssMinifier`'s bounded declaration-value scanner and does not add a shared parser/runtime dependency.

This run adds focused `@scope` plus `@nest` lowering for scoped block boundaries: top-level scoped rule bodies now lower `@nest` selectors against `:scope`, media-wrapped scoped blocks get the same treatment, and the minifier preserves descendant spacing before `:scope`. `examples/wordpress-nesting-transformer.php` now includes a scoped query-grid variant that lowers without Node. Dependency closure: no new support component is needed; this reuses the bounded native `NestingTransformer` scope-body lowering and `CssMinifier` selector spacing scanner.

This run extends focused upstream `border-radius` minification for rounded block/card styles: four corner longhands now compose to compact `border-radius` shorthands, elliptical corner pairs serialize with slash syntax, later longhand sets compose after an intervening shorthand reset, and legacy `-webkit-`/`-moz-` corner longhands compose to matching prefixed shorthands. `examples/wordpress-border-radius-minifier.php` now models a cover block with per-corner elliptical radii without Node. Dependency closure: no new support component is needed; the slice reuses `CssMinifier`'s bounded declaration-entry composer and border-radius side-list helpers.

This run extends focused upstream `border-radius` prefixing for rounded block/card styles: old Chrome/Safari targets receive `-webkit-border-radius`, old Firefox targets receive `-moz-border-radius` and matching prefixed physical corner longhands, and modern targets drop duplicate stale prefixed radius declarations when an identical unprefixed declaration is present. `examples/wordpress-border-radius-minifier.php` now models legacy Chrome radius prefix output for rounded group, image, and cover blocks without Node. Dependency closure: no new support component is needed; the slice reuses `TransitionPrefixer`'s bounded declaration scanner and the existing `CssMinifier` radius value/composition support.

This run extends focused upstream `border-radius` minification for logical corner reset boundaries in rounded block styles: earlier `border-start-start-radius` declarations are dropped when a later `border-radius` shorthand resets all corners, later logical corners survive after shorthand compaction, and physical longhand composition is skipped when an intervening logical corner would make the rewrite unsafe. `examples/wordpress-border-radius-minifier.php` now models a pill button style whose final shorthand intentionally resets an earlier logical corner without Node. Dependency closure: no new support component is needed; the slice reuses `CssMinifier`'s bounded declaration-entry composer and border-radius side-list helpers.

This run extends focused upstream `border-radius` prefixing for logical corner fallback delivery in rounded navigation and block styles: Safari 12 targets split logical corner radius declarations into LTR and RTL selector variants with physical corner longhands, including paired start/end declarations and variable-backed radius tokens. `examples/wordpress-border-radius-minifier.php` now models logical navigation item corner tokens shipping as physical fallbacks without Node. Dependency closure: no new support component is needed; the slice reuses `TransitionPrefixer`'s bounded target-option flow, RTL selector helper, declaration scanner, and existing radius minification support.

This run extends focused upstream `border-radius` minification for rounded cover/card styles with shorthand-plus-physical override compaction: a cover block fallback `border-radius` is removed when four later physical corner longhands fully replace it and compose to an elliptical shorthand, while partial overrides remain as a shorthand plus longhand. `examples/wordpress-border-radius-minifier.php` now models the superseded cover shorthand path without Node. Dependency closure: no new support component is needed; the slice reuses `CssMinifier`'s bounded declaration-entry composer, shorthand reset tracking, and border-radius side-list helpers.

This run completes the focused upstream `border-radius` helper cluster for rounded block/card styles: concrete partial physical corner overrides after a shorthand now fold into the shortest updated shorthand, while variable-backed partial overrides remain separate so theme tokens are not over-composed. `examples/wordpress-border-radius-minifier.php` now models a media-text soft-corner override that ships compact modern plus legacy WebKit CSS without Node. Dependency closure: no new support component is needed; the slice reuses `CssMinifier`'s bounded declaration-entry composer, shorthand reset tracking, border-radius side-list helpers, and conservative `var()` guard.

This run extends the focused custom-media WordPress path for build-free theme delivery: aliases now resolve inside `@import` media query tails after CSS comments, `layer`/`layer(...)`, and `supports()` modifiers, so layered block stylesheet imports and ordinary `@media` rules can share the same breakpoint tokens. `examples/wordpress-custom-media-transformer.php` now models a responsive query-card stylesheet import assigned to a theme block layer and guarded by a custom-media breakpoint alias without Node. Dependency closure: no new support component is needed; the slice reuses `CustomMediaTransformer`'s bounded alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware top-level scanner.

This run tightens focused custom-media scanner behavior for migrated block/theme CSS: custom-media-looking aliases inside CSS comments in `@media` preludes and `@import` media tails are ignored rather than treated as undefined references, while live aliases in the same constructs still resolve. `examples/wordpress-custom-media-transformer.php` now models a layered query-card import and animated cover media rule that carry stale migration alias comments without Node. Dependency closure: no new support component is needed; the slice reuses `CustomMediaTransformer`'s bounded alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware scanner logic.

This rework/refill slice extends that migrated block/theme CSS path to stale comment text that also contains unmatched-looking `)` characters: the custom-media delimiter scanner skips CSS comments in `@media` preludes and `@import` media tails, so old migration notes cannot truncate a live query before breakpoint aliases resolve. `examples/wordpress-custom-media-transformer.php` now models a layered query-card import and animated cover media rule with stale alias/parenthesis comments without Node. Dependency closure: no new support component is needed; the slice reuses `CustomMediaTransformer`'s bounded alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware scanner/delimiter logic.

This rework/refill slice also covers stale migration comments that contain commas inside comma-separated media lists: the custom-media splitter skips CSS comments in `@media` preludes and `@import` media tails, so old notes cannot split live query lists or trigger undefined alias diagnostics. `examples/wordpress-custom-media-transformer.php` now models a layered query-card import and animated cover media rule with stale alias, parenthesis, and comma comments while adjacent live breakpoint/motion aliases still resolve without Node. Dependency closure: no new support component is needed; the slice reuses `CustomMediaTransformer`'s bounded alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware scanner/delimiter/media-list splitting logic.

This rework/refill slice extends the same migrated block/theme CSS path into `@custom-media` definition bodies: stale comments embedded inside breakpoint and motion aliases can contain old alias names, unmatched-looking `)`, and commas without being collected as references, changing delimiter matching, or splitting the definition list. `examples/wordpress-custom-media-transformer.php` now models those stale definition comments in addition to import-tail and media-prelude comments without Node. Dependency closure: no new support component is needed; the slice reuses `CustomMediaTransformer`'s bounded alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware scanner/delimiter/media-list splitting logic.

This rework slice further hardens migrated block/theme CSS imports: stale comments between an `@import` source and its real `layer(...)`/`supports()` modifiers can mention old modifier-looking tokens, aliases, commas, and unmatched-looking parentheses without changing where the media tail starts or how aliases resolve. `examples/wordpress-custom-media-transformer.php` now includes those stale import modifier notes while still producing a layered query-card import and motion media rule without Node. Dependency closure: no new support component is needed; the slice reuses `CustomMediaTransformer`'s bounded alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware scanner/delimiter/media-list splitting logic.

This run adds focused upstream `color-scheme` minification for editor/theme dark-mode handoff: `dark light` canonicalizes to `light dark`, `only light` and `only dark` serialize as `light only` and `dark only`, and invalid mixed lists stay in authored order. `examples/wordpress-color-scheme-minifier.php` models editor stylesheet dark/light color-scheme tokens without Node.

This run adds the adjacent focused upstream `color-scheme` fallback-prefix and `light-dark()` minifier slice for editor/theme dark-mode handoff: Chrome 90 targets receive `--lightningcss-light`/`--lightningcss-dark` flags, Firefox 120 targets leave the declaration alone, and nested identical `light-dark()` functions collapse after color arm minification. The slice now also lowers top-level `light-dark()` color declarations for Chrome 90 when the arms are mapped advanced colors or `var(...)` tokens, maps nested `light-dark()` inside relative `rgb()`/`color()` syntax, maps `color-mix(in srgb, light-dark(...), light-dark(...))`, serializes Firefox 120 Oklch/Oklab arms without fallback lowering, and honors the upstream Safari 13 `exclude: Features::LightDark` boundary by preserving `color-scheme` and `light-dark()` without fallback lowering. `examples/wordpress-color-scheme-fallback-prefixer.php` models editor scheme fallback flags, variable-backed editor accent colors, warning background `color-mix()` fallbacks, and relative alpha-token color fallbacks without Node.

This run adds focused upstream `print-color-adjust` prefixing for WordPress print/PDF export fidelity: Chrome 135 targets emit `-webkit-print-color-adjust:exact` before the modern declaration, while Chrome 137 targets preserve the unprefixed declaration. `examples/wordpress-print-color-adjust-prefixer.php` models exact-color cover print-export CSS without Node.

This run adds focused upstream `all` reset minification for block style resets: stale resettable declarations before `all` are pruned, duplicate reset declarations collapse to the latest value, CSS custom properties survive before the reset, and `direction`/`unicode-bidi` serialize after the reset. `examples/wordpress-all-reset-minifier.php` models a plain quote block reset that preserves a custom design token and RTL direction while dropping stale pre-reset spacing/background declarations without Node.

This run adds focused upstream `@starting-style` minification and nesting lowering for block entrance transitions: non-empty `@starting-style` rules minify, empty rules are dropped, and nested block declarations lower to a top-level `@starting-style` rule with the parent selector. `examples/wordpress-starting-style.php` models a fade-in button block without Node.

This run adds focused upstream `@view-transition` minification for page and block navigation transitions: `navigation` and `types` descriptors compact at top level and inside `@layer`, `types: none` remains intact, custom-ident type lists preserve their spacing boundary, and custom-property token streams containing `@view-transition` are not treated as at-rules. `examples/wordpress-view-transition-minifier.php` models page navigation and navigation block transition types without Node.

This run adds focused upstream `@keyframes` minification for build-free block animations: safe quoted animation names unquote, quoted reserved names remain quoted, unquoted reserved names are rejected, `from` and bare `100%` selectors normalize to upstream forms, invalid named-range `entry to` frames are omitted, vendor-prefixed keyframes minify, and duplicate unprefixed keyframes keep the later rule. `examples/wordpress-keyframes-minifier.php` models compact cover reveal keyframes plus its animation shorthand without Node.

This run adds focused upstream `@keyframes` prefixing for legacy shared-hosting browser targets: `TransitionPrefixer` emits `@-webkit-keyframes` for old Safari/Chrome targets, emits `@-moz-keyframes` for old Firefox targets, emits both prefix families when both target groups are requested, and drops stale prefixed keyframes for modern targets. `examples/wordpress-keyframes-prefixer.php` models a cover reveal animation that ships legacy WebKit plus modern keyframes without Node. Dependency closure: no new support component is needed; the slice reuses the existing bounded target-option support inside `TransitionPrefixer` and the native keyframes minifier output.

This run adds focused upstream `@counter-style` pretty-print formatting for custom list markers: declaration-only fixed marker definitions format with LightningCSS-style indentation and preserve quoted suffix tokens. `examples/wordpress-counter-style-formatter.php` formats custom ordered-list marker `@counter-style` CSS for block patterns without Node. Dependency closure: no new support component is needed; the slice reuses `CssFormatter`'s bounded declaration parser.

This run adds focused upstream-aligned `@scope` rule minification for block-boundary styles: root scopes, root-plus-limit scopes, case-insensitive TO limit keywords, and selector-list roots/limits compact while preserving the required `to (...)` spacing. `examples/wordpress-scope-rule-minifier.php` models scoped card block CSS that stops before nested button blocks without Node. Dependency closure: no new support component is needed; the slice reuses the existing bounded `CssMinifier` scanner.

This run adds focused native bundle/import graph resolution for build-free block-theme delivery: `CssBundler` recursively resolves token, shared component, responsive block, and print CSS imports from an in-memory source map, preserves upstream dependency ordering, wraps imported styles in authored `@supports`, `@media`, and `@layer` conditions, keeps custom-media aliases available across imported files, and reports unsupported external-import ordering with source locations. `examples/wordpress-bundle-import-graph.php` models a theme stylesheet graph for tokens, shared buttons, query-card styles, and print styles without Node. Dependency closure: no new support component is needed; the slice reuses the bounded native `CssMinifier`, `CustomMediaTransformer`, media-query normalization, scanner, and path helpers.

This isolated bundle resolver slice extends that build-free delivery path to file-backed theme directories: `CssBundler::bundleFile()` reads resolved stylesheet files from disk while a custom resolver maps package-style specifiers to vendor token CSS. `examples/wordpress-bundle-import-graph.php` now creates a temporary theme tree, resolves `pkg:tokens.css` to a vendor token stylesheet, bundles a navigation block stylesheet from disk, and verifies `filesystem-provider: resolved` without Node or a filesystem crawler. Dependency closure: no new support component is needed; this uses PHP file reads inside the existing bounded bundler/resolver/minifier path.

This integration also adds focused native CSS Modules and source-map support for build-free block delivery. `examples/wordpress-css-modules-transformer.php` models a block card module that scopes local class names, keeps global utility selectors public, removes `composes` declarations from emitted CSS, and returns export metadata for local, global, and dependency composes references without Node. `examples/wordpress-source-map-vlq-offsets.php` models a distributed theme/block stylesheet that preserves license comments while emitting source-map v3 JSON with LightningCSS-compatible Base64 VLQ mappings. Dependency closure: no external support component is needed; the slice reuses bounded selector/declaration scanning, `CssMinifier`, and lane-local source-map v3/VLQ support.

This isolated CSS Modules micro-slice extends the same build-free block delivery path: `examples/wordpress-css-modules-transformer.php` now models a block card module with a nested `.cardIcon` selector inside a parent card that also composes dependency and global utility classes. Native PHP scopes and lowers the nested child selector while retaining dependency/global composes export metadata on the parent, matching the pinned upstream CSS Modules nested selector/composes behavior without Node. Dependency closure: no new support component is needed; the slice reuses `CssModulesTransformer`, `NestingTransformer`, selector/declaration scanning, and `CssMinifier`.

`examples/wordpress-all-reset-minifier.php` models a plain quote block reset whose custom design token and RTL direction survive while resettable spacing/background declarations before `all` are pruned without Node. `examples/wordpress-starting-style.php` models nested block entrance CSS whose starting style is lowered to a top-level at-rule without Node. `examples/wordpress-keyframes-minifier.php` models compact cover reveal keyframes plus its animation shorthand without Node. `examples/wordpress-font-family-minifier.php` models block-theme typography CSS whose quoted author font families are safely unquoted, whose reserved `@font-face` family name stays quoted, and whose `font-stretch: expanded` is minified to `125%` without Node. `examples/wordpress-font-shorthand-composition.php` models block title/navigation typography whose longhands and verbose shorthands collapse to compact upstream-style `font` declarations without Node. `examples/wordpress-font-face-src-range-minifier.php` models a self-hosted Inter variable-font face whose `local(...)`, URL, `format()`, `tech()`, and `unicode-range` descriptors collapse to upstream-style CSS without Node. `examples/wordpress-font-palette-values-minifier.php` models a block heading color-font palette whose `override-colors` and `font-palette` custom ident collapse to upstream-style CSS without Node. `examples/wordpress-font-palette-prefixer.php` models a variable-backed color-font heading palette whose LCH color emits an sRGB base descriptor plus a Lab `@supports` fallback without Node. `examples/wordpress-font-feature-values-minifier.php` models Inter OpenType feature aliases whose repeated `@styleset` blocks and adjacent family rules collapse to compact upstream-style CSS without Node. `examples/wordpress-property-registration-minifier.php` models registered block design tokens whose duplicate color registration collapses to the later value and whose motion duration token shortens `1000ms` to `1s` without Node. `examples/wordpress-page-rule-minifier.php` models chapter, toc, and index print/PDF export `@page` rules plus page margin boxes without Node. `examples/wordpress-print-color-adjust-prefixer.php` models exact-color print/PDF export styles that need `-webkit-print-color-adjust` on Chrome 135 without Node. `examples/wordpress-namespace-svg-selectors.php` models SVG and xlink namespace selectors for block icons and social/navigation links without Node. `examples/wordpress-color-scheme-minifier.php` models editor dark/light color-scheme tokens without Node. `examples/wordpress-color-scheme-fallback-prefixer.php` models Chrome-targeted editor color-scheme fallback flags, variable-backed `light-dark()` accent colors, per-scheme `color-mix()` warning backgrounds, and relative `rgb(from light-dark(...))` alpha-token colors without Node. `examples/wordpress-fluid-spacing-calc.php` models fluid block stack spacing and cover offsets whose calc expressions fold without Node while preserving mixed-unit expressions. `examples/wordpress-fluid-spacing-math-functions.php` models query-card spacing tokens whose `round()`, `rem()`, `mod()`, nested `round()` inside `calc()`, and `clamp()` expressions fold without Node. `examples/wordpress-advanced-math-functions.php` models a depth-styled cover block whose `abs()`, `hypot()`, `pow()`, and safe `sign()` expressions fold while percentage-based `sign()` remains a CSS guard without Node. `examples/wordpress-transform-math-minifier.php` models a tilted cover block hover style whose `mod()`/`rem()` angle math folds inside adjacent transform functions and whose `clamp()` border width lowers for Safari 12 without Node. `examples/wordpress-transform-function-minifier.php` models lift/press cover block transforms whose `translate3d()`/`translateX()` and `scale()`/`scale3d()` functions reduce to compact upstream-style transform functions without Node. `examples/wordpress-transform-rotate-longhand-minifier.php` models a gallery tilt effect whose `translateX(calc(2in + 50px))`, `rotate3d()`, `skew()`, `rotate` longhand, and `scale` longhand values reduce to compact upstream-style CSS without Node. `examples/wordpress-nesting-transformer.php` models nested query block title, `:before`/`:after` generated-content hooks, hover, featured-title scoping that preserves `.is-featured :is(.wp-block-query .wp-block-post-title)`, responsive rules, `article.wp-block-query` attached selectors, nested `@supports`/`@container`/`@layer` rules, and scoped post-template boundaries lowered to flat CSS for shared-hosting delivery without Node. The nesting tests also model styled-jsx placeholders inside nested cover block custom properties, gradients, transform calc values, media queries, and spacing calc values so WordPress build-free delivery can recover placeholder-heavy CSS without shelling out. `examples/wordpress-container-query-validation.php` models a block layout build step that rejects an invalid `@container none (...)` alias and reports a structured diagnostic instead of silently emitting CSS that upstream LightningCSS rejects. `examples/wordpress-container-error-recovery.php` models a tolerant import path where an invalid `@container unknown(foo)` block is omitted with an upstream-style warning while the following query block style still minifies without Node. `examples/wordpress-import-supports.php` models a layered block stylesheet import guarded by `supports(display:grid)` and responsive media, with `@charset` removed and the import URL normalized for build-free delivery. `examples/wordpress-supports-rule-minifier.php` models a supports-gated query block layout fallback where redundant grouping is removed but the `not` branch remains correctly parenthesized. `examples/wordpress-supports-value-minifier.php` models supports-gated gallery width math and heading color values where declarations are minified while the supports conditions remain semantic guards. `examples/wordpress-supports-backdrop-filter-prefixer.php` models a glass header guarded by `@supports (backdrop-filter: ...)` and emits the Safari-targeted `-webkit-backdrop-filter` condition and declaration fallback without Node.

`examples/wordpress-cssom-priority-buckets.php` models editor and migration tooling that edits declaration blocks with forced theme tokens. Native CSSOM reads now report an earlier `!important` preset color as active even when a later normal color exists, normal editor overrides remove opposite-priority direct color declarations, spacing edits preserve opposite-priority shorthands instead of mutating them, and color removal deletes both normal and important declarations while keeping background declarations printable without Node. Dependency closure: no new support component is needed; this uses the bounded `DeclarationBlock` parser/serializer and priority flags already in the LightningCSS lane.

`examples/wordpress-background-cssom.php` models editor and migration tooling that rewrites block background declarations with theme tokens. Native CSSOM writes now update compatible `background` shorthands when setting supported `background-*` longhands, append a separate longhand when multi-layer counts do not match, and remove the supported background longhand set when `background` is removed. Dependency closure: no new support component is needed; this uses the bounded `DeclarationBlock` parser/serializer, priority buckets, and background layer scanner already in the LightningCSS lane.

`examples/wordpress-custom-at-rules-transformer.php` now also models a build-free block stylesheet that collects custom design tokens through composed visitors only: `token()` replacement is supplied by a composed `Function` visitor, an unknown `@token --wp-ring #056ef0` at-rule exposes a dashed-ident prelude consumed by a later `Token.at-keyword` visitor for `@--wp-ring`, and a composed `Declaration.custom.size` visitor expands a `size: 48px` block utility into width/height declarations before the `Length` visitor converts them to rem values. Dependency closure: no new support component is needed; this uses the bounded `CustomAtRuleTransformer`, declaration parser, minifier, and native scanner helpers already in the LightningCSS lane.

This isolated filter-boundary run updates `examples/wordpress-filter-prefixer.php` to model a sticky glass header compiled for Chrome 52/Safari 14 versus Chrome 53/Safari 14: `backdrop-filter` still gets the Safari `-webkit-` guard, while `filter` drops its stale WebKit prefix exactly at the Chrome 53 upstream boundary. No new support component is needed; it reuses `TransitionPrefixer` target-version routing and declaration scanning.

This isolated object-fit boundary run adds `examples/wordpress-object-fit-prefixer.php` to model featured-image and cover-video CSS compiled for Opera 10.5, 10.6, 12.1, and 13: the legacy Opera 10.6 through 12.1 range receives `-o-object-fit` and `-o-object-position`, while Opera 10.5 and 13 keep only modern declarations. No new support component is needed; it reuses `TransitionPrefixer` target-version routing, declaration scanning, and existing stale-prefix cleanup.

This isolated property-values run extends `examples/wordpress-gradient-value-minifier.php` so build-free block CSS models linear, radial, and conic gradients without Node: radial focus gradients drop default `farthest-corner` and center components, sized radial gradients fold calc-derived radii, and conic accent gradients drop default `from 0deg at center` preludes while preserving compact color stops. No new support component is needed; it reuses `CssMinifier` declaration-value scanning, math folding, color serialization, and top-level function/list helpers.

This isolated bundle/import graph run extends `examples/wordpress-bundle-import-graph.php` for supports-gated block layout delivery: a parent stylesheet import guarded by `supports((display: grid) or (display: flex))` and a child import guarded by `supports(container-type: inline-size)` now preserve the nested `or` group when bundled and minified. No new support component is needed; it reuses `CssBundler`, `CssMinifier`, the bounded supports-condition scanner/minifier helpers, and the native resolver/import graph model.

## Next Task

Continue current-base LightningCSS handoffs for remaining CSSOM shorthand splitting/removal parity, custom at-rule parser/visitor parity, target-prefix browser boundaries, media-query range/layer handling, non-overlapping property-value color/font/grid coverage, and source-map/bundler integration. Reject status-only markers and avoid repeating accepted CSSOM priority-bucket, background CSSOM read/write, linear/radial/conic gradient minifier, or bundle supports-condition grouping slices.
