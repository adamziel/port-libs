# LightningCSS Upstream Inventory

Source: `.upstream-cache/lightningcss` at upstream commit `22bdda3d190f1cd321d98026225cfc964af64ad9`.

The lane denominator is no longer the seed manifest. It is a static upstream inventory plus runner evidence:

- 3,332 `src/lib.rs` helper invocations counted from pristine `git show HEAD:src/lib.rs` output (`minify_test`, `prefix_test`, `test`, `custom_media_test`, `error_test`, and related helpers), excluding helper function definitions.
- 74 inspected CSSOM helper invocations in `tests/test_cssom.rs`.
- 45 Rust `#[test]` functions counted outside `src/lib.rs` and `tests/test_cssom.rs`.
- 81 Node `uvu` tests.
- 8 CSS fixture files tracked separately from the behavior denominator.

Runner evidence already recorded in `UPSTREAM_TEST_MANIFEST.json`: native Node tests, Rust `cargo test --all-features`, TypeScript checks, and WASM Node/browser runners have local passing evidence. The upstream cache currently contains local compatibility edits for the WASM runner (`Cargo.lock` and `src/lib.rs` dirty), so inventory reads should use `git show HEAD:<path>` for pristine upstream content unless the cache is restored.

This run maps 18 focused upstream `src/lib.rs::test_font` minifier cases. Native PHP now serializes quoted `font-family` strings using upstream boundaries: quoted generic/CSS-wide family names such as `'sans-serif'`, `'inherit'`, `'revert'`, and `'revert-layer'` stay quoted, safe author family strings such as `"Helvetica"` and `"Times New Roman"` become unquoted family names, empty family strings become `""`, the same reserved-name rules apply inside `@font-face`, and `font-stretch: expanded` minifies to `125%`.

This run adds 8 focused upstream `src/lib.rs::test_font` shorthand/composition cases. Native PHP now composes compatible `font-family`/`font-size`/`font-weight`/`font-style`/`font-stretch`/`font-variant-caps`/`line-height` longhand groups to compact `font` shorthands, leaves `all-small-caps` as a separate guarded `font-variant-caps` longhand, folds a later simple `line-height` into an existing `font` shorthand, preserves a `var(--lh)` line-height guard as a longhand, omits default `normal` shorthand components, and preserves explicit numeric weights such as `400`.

This run adds 20 focused upstream `src/lib.rs::test_font_face` minifier cases. Native PHP now normalizes `@font-face src` descriptors by unquoting safe `url(...)` sources, unquoting non-empty `local(...)` names while preserving `local("")`, quoting `format(...)` descriptors, lowercasing/trimming `tech(...)` descriptor lists, preserving the upstream invalid tech-before-format spacing boundary, and compacting `unicode-range` values including uppercase `U+`, redundant endpoint-zero trimming, and wildcard conversion for full ranges such as `U+400-4FF`, `U+0000-FFFF`, and `U+100000-10FFFF`.

Current focused slice maps 14 upstream `src/lib.rs::test_filter` cases: `filter` URL quote removal, `blur(0px)` to `blur()`, `brightness(100%)` to `brightness()`, `hue-rotate(0)` to `hue-rotate()`, adjacent `contrast()`/`brightness()` filter functions without whitespace, `drop-shadow(... yellow)` color shortening, `-webkit-filter` prefix emission, `-webkit-backdrop-filter` prefix emission, preserving an existing `-webkit-backdrop-filter` pair, `filter: var(--foo)` prefixing, and `drop-shadow(... lab(...))` sRGB fallback layers including a lab `@supports` guard when a custom property prevents direct replacement. The box-shadow slice now maps 17 upstream `src/lib.rs` cases: 6 minifier cases for rgba black alpha compaction to `#0006`, canonical `inset` ordering, zero length normalization, default spread/blur omission, and comma-separated normal/inset layer preservation; 9 target-specific `test_box_shadow` prefix/fallback cases for Chrome 90 advanced-color fallback layers, Chrome 4 `-webkit-box-shadow` emission and alpha-hex expansion, Chrome 95 legacy prefix removal, Chrome 90 lab `@supports` guards for `var(...)` shadows, and Safari 16 fallback pruning while preserving custom-property fallbacks; plus 2 `test_light_dark` oklch-alpha shadow cases that lower to sRGB or display-p3 fallback layers followed by Lab target shadows while omitting the default `currentColor` second-layer color.

This run adds a native `text-shadow` effects slice from upstream `src/lib.rs::test_text_shadow`: 5 minifier cases for color shortening, zero-blur omission, and comma-separated layer preservation, plus 5 prefix/fallback cases for Chrome 4 sRGB+Lab layers, Chrome 90 plus Safari 14 sRGB/display-p3/Lab layers, comma-separated advanced-color lists, custom-property lab `@supports` guards, and no-op preservation inside an existing Lab `@supports` guard.

This run also adds a native `text-decoration` effects slice from upstream `src/lib.rs::test_text_decoration`: 12 prefix/fallback cases covering `text-decoration-line`, `text-decoration-style`, and `text-decoration-color` WebKit/Mozilla prefixes, shorthand canonicalization from `double underline` to `underline double`, longhand style/color composition into prefixed shorthands, `var(--test)` shorthand prefixing, LCH-to-sRGB fallback layers, LCH custom-property `@supports (color: lab(...))` guards, and no-op preservation inside an existing Lab `@supports` guard.

This run adds a native `text-emphasis` effects slice from upstream `src/lib.rs::test_text_emphasis`: all 30 helper cases are now mapped, covering `text-emphasis-style` and `text-emphasis-position` canonicalization, `text-emphasis-color` color shortening, shorthand style/color composition and var() guard preservation, Chrome-era `-webkit-text-emphasis-*` prefix emission/removal, LCH-to-sRGB fallback layers, LCH custom-property `@supports (color: lab(...))` guards, and no-op preservation inside an existing Lab `@supports` guard.

This run adds a native `caret` effects slice from upstream `src/lib.rs::test_ui`: 15 helper cases are now mapped, covering `caret-color` color shortening, `caret-shape` preservation, `caret` shorthand color/shape ordering, default `auto` component omission, LCH-to-sRGB/display-p3 fallback layers, LCH custom-property `@supports (color: lab(...))` guards, and no-op preservation inside an existing Lab `@supports` guard.

This run adds a native `list-style` slice from upstream `src/lib.rs::test_list`: 26 helper cases are now mapped, covering `list-style-type` string and `symbols()` marker normalization, safe URL normalization for `list-style-image`, shorthand reset semantics for `none`/`inside`/`outside`, longhand-to-shorthand composition, custom-property guard preservation, Chrome 8 `-webkit-gradient` and `-webkit-linear-gradient` fallbacks for `list-style-image`, LCH-to-sRGB fallback layers for `list-style`, LCH custom-property `@supports (color: lab(...))` guards, and no-op preservation inside an existing Lab `@supports` guard.

This run adds a native `image-set()` slice from upstream `src/lib.rs::test_image_set`: all 13 helper invocation sites are now mapped, including six minifier cases for string/url/type descriptor normalization, two transform/minifier checks for paired prefixed/unprefixed declarations and gradient candidates, and five prefix helper sites covering WebKit image-set emission, existing prefixed declarations, IE URL fallback retention, and the property loop for `background`, `background-image`, `border-image*`, `-webkit-mask*`, `list-style-image`, and `list-style`.

This run adds a native `@container`/container shorthand slice from upstream `src/lib.rs::test_container_queries`: 17 focused helper cases are now mapped, covering name-only container blocks, named and unnamed range conditions, `not` grouping, value-first `calc()` comparisons, `style()` query value minification including color shortening and same-unit `calc()` folding, `scroll-state()` feature minification, `container-name` plus `container-type` shorthand composition, and `container: foo / normal` default-type omission.

This run extends the same upstream `src/lib.rs::test_container_queries` slice to 24 focused helper cases. The native PHP minifier now merges adjacent identical `@container` blocks, also merging adjacent nested style rules with the same selector, and folds simple same-unit container query units: `calc(1cqw + 2cqw)`, `calc(1cqh + 2cqh)`, `calc(1cqi + 2cqi)`, `calc(1cqb + 2cqb)`, `calc(1cqmin + 2cqmin)`, and `calc(1cqmax + 2cqmax)` to their compact `3*` forms while leaving URL tokens and mixed-unit calc values untouched.

This run maps the adjacent upstream `src/lib.rs::test_container_queries` validation cluster: all 15 `error_test` cases for disallowed container names (`none`, `and`, `or`, CSS-wide keywords), multi-ident names, invalid range comparisons, nested `style()`/`scroll-state()` functions, unknown condition functions, empty `@container` preludes, and empty condition brackets. The neighboring upstream `error_recovery_test("@container unknown(foo) {}")` is now mapped through `CssMinifier::minifyWithErrorRecovery()`.

This run also adds a focused native `@import` minifier slice from upstream `src/lib.rs::test_import`: 11 minifier cases now map URL-to-string import sources, quoted source preservation, media query compaction, `supports(display: flex)` and `supports(not (display: flex))` compaction, redundant supports parentheses, `@charset` omission before imports, and preserving a preceding `@layer foo;` statement while minifying the import URL.

This run adds a focused native `@supports` rule minifier slice from upstream `src/lib.rs::test_supports_rule` and `src/lib.rs::test_merge_supports`: 11 focused cases now map declaration conditions, `not` conditions, redundant `and`/`or` grouping, nested `and` plus parenthesized `or`, `not` plus parenthesized `and`, `selector(a > b)`, unknown function conditions, parenthesized unknown conditions, display-grid plus not-inline-grid conditions, and adjacent identical `@supports` block merging including matching nested style rules. This lane batch maps the neighboring upstream `width: calc(10px * 2)` and `hsl(0deg, 0%, 0%)` value-minifier cases: the `@supports` condition values remain intact, while nested declarations now minify to `width:20px` and `color:#000`.

This run maps the neighboring upstream `src/lib.rs::test_supports_rule` backdrop-filter prefix/removal cluster: Safari 14 targets now rewrite `@supports (backdrop-filter: blur(...))` to include the matching `-webkit-backdrop-filter` OR branch, preserve existing matching prefixed/unprefixed OR conditions, add a missing matching prefixed branch when an older prefixed condition has a different value, and collapse matching prefixed/unprefixed OR conditions back to the unprefixed condition for Chrome 80 targets. The direct `src/lib.rs::test_filter` Chrome 80 `backdrop-filter` no-prefix case is also mapped, so declaration prefixing and supports-condition prefixing share target-aware behavior.

Recent focused slices add native `TransitionPrefixer` coverage for 23 focused `src/lib.rs::test_mask` prefix cases: `transition: mask 200ms`, `transition: mask-border 200ms`, `transition-property: mask`, `transition-property: mask-border`, `transition-property: mask-composite, mask-mode`, direct `mask-border` shorthand prefixing, direct `mask-border` LCH gradient fallback layers, direct `mask-border-source` LCH gradient fallback layers, `mask-border-*` longhand composition, prefixed `-webkit-mask-box-image-*` longhand composition, `mask-border-slice: 10 40 10 40` compression, `mask-border-slice: var(--foo)`, `mask-border: linear-gradient(lch(...)) var(--foo)` sRGB base plus lab `@supports` fallback emission, standalone `mask-composite`/`mask-mode` fallbacks, direct `mask-image` gradient/url prefixing, existing paired `-webkit-mask-image` normalization, `mask: url(...) luminance` source-type fallback emission, `mask-image`/position/size/repeat/origin/clip/composite/mode longhand composition, `mask-image` LCH gradient fallback layers, `mask` LCH gradient fallback layers, `mask: linear-gradient(lch(...)) 40px var(--foo)` sRGB base plus lab `@supports` fallback emission, and composed mask longhand LCH gradient fallback layers. Previous background work mapped 4 focused upstream advanced-color background fallback cases from `src/lib.rs`: `background` LCH gradient fallback layers, `background-image` LCH gradient fallback layers, `background: lab(...) url(foo.png)` sRGB fallback plus URL normalization, and skipping duplicate fallback generation inside existing color `@supports` guards. Recent advanced-color work maps 5 focused upstream advanced-color target/custom-property cases: `background-image` Oklab gradients targeting Lab fallback values, `background-color` Oklch targeting Lab fallback values, custom properties with Oklab/Lab values emitting sRGB base plus display-p3 and Lab `@supports` overrides, display-p3 custom properties emitting an sRGB base plus display-p3 `@supports` override, and skipping custom-property fallback generation inside existing advanced-color `@supports` guards. Recent animation work maps 29 focused `src/lib.rs::test_animation` animation-range cases: `animation-range-start`/`animation-range-end` default percentage omission, `animation-range` shorthand compaction, start/end longhand composition, parseable shorthand plus later longhand composition, `var(--end)` guard preservation, list-length mismatch preservation, equal-list composition, later `animation` shorthand reset behavior, and `animation` followed by range composition. Recent custom-media work maps 3 upstream `src/lib.rs::test_custom_media` diagnostic cases: unsupported boolean logic reports both @media and @custom-media line/column locations, undefined custom media reports the @media location, and circular custom media reports the @media location. Earlier slices mapped 10 animation declaration-composition cases, 17 focused animation shorthand minification cases for quoted-name disambiguation, default component omission, canonical layer ordering, cubic-bezier alias omission, string-token whitespace preservation, and scroll/view timeline cleanup; animation longhand cases for quoted `animation-name` serialization and `animation-timing-function` cubic-bezier/steps aliases; custom-media boolean-logic cases; transition declaration composition; logical block-axis transition expansion; direction-sensitive inline transition prefixing; and `transition: transform` prefixing.

This run maps 25 focused upstream `src/lib.rs::test_calc` cases. Native PHP now folds linear `calc()` arithmetic with nested parentheses, unitless scalar multiplication/division, signed operands, scientific-notation lengths, same-unit and selected mixed-unit like-term reductions, cancellation to a single unit, and conservative preservation of unknown-unit or `var()` expressions.

This run extends `src/lib.rs::test_calc` by 55 focused math-function cases. Native PHP now folds and prunes `min()`/`max()` arms, compares `clamp()` bounds where safe, reduces `round()` with nearest/down/up/to-zero strategies, implements `rem()` dividend-sign remainder and `mod()` divisor-sign modulo behavior, folds nested unitless `round()`/`rem()`/`mod()` inside `calc()`, and compacts nested `clamp()`/`min()`/`max()` inside `calc()` while preserving non-comparable units and variables.

This run extends the same upstream `src/lib.rs::test_calc` slice by 3 focused cases. Native PHP now folds `mod()` and `rem()` angle math inside adjacent transform functions and serializes the transform list without a separating space (`rotateX(...)rotateY(...)`). It also lowers `clamp(min, preferred, max)` to `max(min,min(preferred,max))` for Safari 12 targets while preserving `clamp()` for Safari 14, matching the neighboring upstream prefix tests.

This run extends the focused upstream `src/lib.rs::test_exp` and `src/lib.rs::test_sign` slice to 20 cases. Native PHP now folds `hypot()`, `sqrt()`, `pow()`, `log()`, `exp()`, `abs()`, and `sign()` when the arguments are safely numeric, supports `e` and `pi` as numeric constants inside these bounded math expressions, preserves incompatible-unit `hypot(...)` values, keeps percentage-based `abs(1%)` unresolved because percentages are context-dependent, collapses `calc(1px * (e - exp(1)))` to `0`, and preserves the unresolved percentage guard `calc(10px * sign(1%))` with upstream-style calc spacing.

This run adds 27 focused upstream `src/lib.rs::test_transform` translate/scale minifier cases. Native PHP now normalizes `translate()`, `translateX()`, `translateY()`, `translateZ()`, and `translate3d()` by folding same-unit calc values, dropping zero axes, and reducing 3D forms to 2D or axis-specific functions where upstream does. It also normalizes `scale()`, `scaleX()`, `scaleY()`, `scaleZ()`, and `scale3d()` by converting percentage scale inputs to numbers, folding percentage calc inputs, reducing identity axes, preserving non-identity 3D scale tuples, and applying the same behavior to `-webkit-transform` while preserving other transform function casing such as `rotateX()`.

This run adds 99 focused upstream `src/lib.rs::test_transform` cases on top of that translate/scale slice. Native PHP now normalizes `rotate()`, `rotateX()`, `rotateY()`, `rotateZ()`, and `rotate3d()` with upstream-style z-axis canonicalization, single-axis reduction, mixed deg/rad/turn calc folding, and var() preservation; normalizes `skew()`, `skewX()`, and `skewY()` by dropping zero axes; compacts `perspective()`, `matrix()`, and `matrix3d()` arguments; folds absolute-length translate calc values such as `2in + 50px` to px while preserving mixed percentage/length calc values; and serializes adjacent transform functions without whitespace. The same slice adds `translate`, `rotate`, and `scale` longhand minification for trailing translate zero axes, rotate axis/angle canonicalization with negative-axis sign handling, percentage-to-number scale conversion, identity z-axis trimming, and equal x/y scale compression.

This run adds and extends a focused native CSS nesting lowering slice from upstream `src/lib.rs::test_nesting`: 30 focused lowering/recovery cases now map direct `& > .bar`, compound `&.bar`, duplicate `&&`, selector-list `:is(.foo, .bar)` parent references, explicit `include: Features::Nesting` lowering and `exclude: Features::Nesting` preservation through `NestingTransformer::transformForTargets()`, target-gated attached selector-list suffix distribution from `.a { &.b, &.c { &.d { ... } } }` to `.a.b.d, .a.c.d`, implicit parent placement before leading combinators such as `+ .bar + &`, complex parent placement through `:is(...)` wrappers for `.baz &` and `@nest .baz &`, attached type selectors such as `&article`, `&span`, and `&h1`, namespace-attached selectors such as `&|x`, `&*|x`, and `&toto|x`, top-level `:scope` attachment, `@nest h1&` suffix attachment, recursive nested style rules, nested `&::before`/`&::after` pseudo-element selectors compacted to `:before`/`:after` while preserving nested `@media`, nested `@supports`, nested `@container`, named and anonymous nested `@layer`, nested `@scope` body/root boundary lowering where inner `&` resets to `:scope`, `@nest .parent &` lowering, and styled-jsx placeholder media-query recovery. This is implemented as a separate `NestingTransformer` so the existing default minifier semantics remain intact while legacy/shared-hosting targets can explicitly lower nested block CSS.

This run maps the upstream `src/lib.rs::test_css_variable_error_recovery` styled-jsx placeholder case. Native PHP now preserves `--styled-jsx-placeholder-*` token streams in custom-property values, gradient stops, transform `calc()` values, and margin `calc()` values while lowering the nested `.item` and `div` selectors. A WordPress-focused test covers the same recovery for nested cover block CSS using placeholder-driven background, transform, media query, and spacing values.

This run also maps 6 no-target nested selector preservation cases from upstream `src/lib.rs::test_nesting`. The generic minifier now keeps the required descendant space before `&` in selectors such as `@nest .bar &`, preserves attached `&div` and `&span` selectors without lowering them, continues to compact `&:hover` and `:not(&)` parent-reference selectors without changing token boundaries, canonicalizes implicit preserved nested selectors such as `.bar {}` to `& .bar {}`, and still minifies declarations that follow preserved nested rule blocks.

This run maps all 7 upstream `src/lib.rs::test_font_palette_values` helper cases after a targeted pristine upstream read counted 7 helper invocations in that function: 4 minify helpers and 3 prefix helpers. Native PHP now minifies `@font-palette-values` `override-colors` entries by lowercasing/compressing hex colors, folding `rgb(43, 12, 9)` to `#2b0c09`, shortening `lime` to `#0f0`, preserving the upstream comma-space before a variable-backed override color, and serializing `font-palette: --Custom` without whitespace. The new prefix slice emits an sRGB fallback `override-colors` descriptor before the original advanced-color descriptor for Chrome 90 targets, moves variable-backed advanced colors into a Lab `@supports`-wrapped `@font-palette-values` rule, and leaves `@font-palette-values` rules that are already inside `@supports (color: lab(...))` unchanged except for minified serialization.

This run maps all 7 upstream `src/lib.rs::test_font_feature_values` minifier cases after a targeted pristine upstream read counted 7 helper invocations in that function. Native PHP now minifies `@font-feature-values` family lists by unquoting safe font family strings, compacts feature blocks such as `@styleset`, `@character-variant`, `@stylistic`, `@swash`, `@ornaments`, and `@annotation`, applies last-declaration-wins behavior for duplicate feature names, merges repeated feature blocks, and merges adjacent same-family `@font-feature-values` rules.

This run maps 21 focused upstream `src/lib.rs::test_property` cases after a targeted pristine upstream read counted 26 non-comment helper invocations in that function: 15 `minify_test`, 6 `error_test`, and 5 non-minifying pretty-printer `test` cases. Native PHP now minifies `@property` rules by validating dashed custom-property names, canonicalizing descriptor order to `syntax`, `inherits`, and `initial-value`, serializing syntax strings with compact grammar whitespace, minifying typed `<color>`, `<length>`, `<string>`, `<time>`, `<url>`, and `<image>` initial values, preserving empty/arbitrary `syntax: "*"` initial values, collapsing duplicate same-name registrations to the later descriptor values at the first rule position, and rejecting the upstream invalid color, length, missing-descriptor, and non-dashed-name cases. The 5 upstream pretty-printer cases remain out of scope for this minifier slice.

This run maps 6 focused upstream `src/lib.rs::test_position` physical/logical inset composition cases after a targeted pristine upstream read of the pre-prefix position cluster. Native PHP now composes `top`/`right`/`bottom`/`left` longhands to the shortest valid `inset` shorthand, composes `inset-block-start`/`inset-block-end` and `inset-inline-start`/`inset-inline-end` to axis shorthands, and drops logical longhands that are reset by an intervening `inset` shorthand before composing later inline offsets.

This run maps 12 focused upstream `src/lib.rs::test_page_rule` helper cases after a targeted pristine upstream read counted 14 helper invocations in that function: 10 `minify_test`, 2 non-minifying pretty-printer `test` cases, and 2 `error_test`. Native PHP now minifies `@page` selectors and page pseudo-classes, comma-separated named pages, page margin boxes such as `@bottom-left`, simple page margin lengths like `0.5cm` to `.5cm`, and rejects unknown nested page at-rules or margin boxes nested inside another margin box. The 2 upstream pretty-printer cases remain out of scope for this minifier slice.

This run completes all 9 focused upstream `src/lib.rs::test_namespace` helper cases after a targeted pristine upstream read counted 9 helper invocations in that function: 4 `minify_test`, 4 non-minifying pretty-printer `test` cases, and 1 `error_test`. Native PHP now minifies `@namespace url(...)` sources to quoted namespace URI strings, preserves prefixed namespace declarations and namespace-qualified type selectors, quotes namespace-qualified attribute selector values such as `[toto|att=val]`, collapses empty attribute namespaces such as `[|att=val]` to `[att="val"]`, preserves universal namespace attribute selectors such as `[*|att="val"]`, and rejects `@namespace` after a top-level style rule.

This run maps 2 upstream `error_recovery_test` cases from `src/lib.rs::test_media_queries` and `src/lib.rs::test_container_queries`. Native PHP now exposes `CssMinifier::minifyWithErrorRecovery()` for the focused LightningCSS `errorRecovery` semantics: invalid `@media unknown(foo)` and `@container unknown(foo)` rules are omitted, following valid rules still minify, and warnings report `UnexpectedToken` with filename, one-based line, and zero-based column locations matching the upstream Node/NAPI oracle columns.

This run maps 16 focused upstream `src/lib.rs::test_color_scheme` minifier cases after a targeted pristine upstream read counted the first 16 `minify_test` cases before the prefix and `light-dark()` cluster. Native PHP now canonicalizes `color-scheme: dark light` to `light dark`, moves `only` after `light` or `dark`, preserves `normal`/`inherit`/`unset`/unknown single-token values, and leaves invalid mixed token lists such as `dark foo`, `normal dark`, and `only foo dark bar` in authored order.

This run extends the same upstream `src/lib.rs::test_color_scheme` inventory again. A targeted pristine read of `git -C .upstream-cache/lightningcss show HEAD:src/lib.rs | sed -n '30965,31225p'` still counts 33 helper invocations in the full function. Native PHP now maps 31 of them in this focused inventory: the previous 16 color-scheme minifier cases, 4 adjacent `prefix_test` cases for Chrome 90 `--lightningcss-light`/`--lightningcss-dark` fallback flags and Firefox 120 no-op prefixing, 3 immediate `light-dark()` minifier cases, 2 Chrome 90 top-level `light-dark()` fallback cases for mapped advanced-color and `var()` arms, 5 helper sites for Firefox 120 advanced-color numeric serialization, nested relative `rgb(from light-dark(...))` fixed-alpha and var-alpha fallbacks, nested `color(from light-dark(...) srgb ...)` fallback layering, and `color-mix(in srgb, light-dark(...), light-dark(...))` fallback lowering, plus the explicit `nesting_test_with_targets` feature boundary where Safari 13 preserves `color-scheme` and `light-dark()` when `Features::LightDark` is excluded. The two remaining in-function Oklch alpha box-shadow helpers remain covered by the box-shadow inventory.

This run maps all 2 upstream `src/lib.rs::test_print_color_adjust` helper cases after a targeted pristine upstream read counted 2 `prefix_test` invocations in that function. Native PHP now emits `-webkit-print-color-adjust:exact` before `print-color-adjust:exact` for Chrome 135 targets, and preserves the unprefixed declaration for Chrome 137 targets.

This run maps all 11 upstream `src/lib.rs::test_all` helper cases after a targeted pristine upstream read counted 11 `minify_test` invocations in that function. Native PHP now drops earlier resettable declarations when a later `all` reset appears, collapses duplicate `all` declarations to the latest reset value, preserves custom properties before `all`, restores `direction` and `unicode-bidi` after `all`, keeps resettable declarations that appear after `all`, and handles `var(...)` values before/after reset in the upstream minifier order.

This run maps all 3 upstream `src/lib.rs::test_starting_style` helper cases after a targeted pristine upstream read counted 2 `minify_test` helpers and 1 `nesting_test` helper. Native PHP now preserves and minifies non-empty `@starting-style` rules, removes empty `@starting-style {}` rules, and lowers nested `@starting-style` declarations to a top-level at-rule with the parent selector.

This run maps all 4 upstream `src/lib.rs::test_view_transition` helper cases after a targeted pristine upstream read counted 4 `minify_test` helpers in that function. Native PHP now minifies top-level `@view-transition` `navigation` and `types` descriptors, preserves `types: none`, preserves space-separated custom-ident type lists such as `foo bar`, and applies the same rule-body minification to `@view-transition` nested inside `@layer`. A custom-property guard keeps declaration values like `--wp--custom--view-transition: @view-transition{...}` from being rewritten as at-rules.

This run maps the remaining 4 upstream `src/lib.rs::test_keyframes` prefix helper cases after the prior targeted pristine upstream read counted 19 helper invocations in that function: 11 `minify_test`, 4 `error_test`, and 4 `prefix_test`. Native PHP now covers all 19 focused keyframes helper cases: it unquotes safe quoted keyframes names, preserves quotes for reserved names such as `revert` and `none`, rejects unquoted reserved names, normalizes `from` to `0%` and bare `100%` to `to`, omits invalid named-range frames such as `entry to`, minifies `@-webkit-keyframes` and `@-moz-keyframes`, folds a simple `background-color` override into `background` inside keyframe bodies, keeps duplicate unprefixed `@keyframes` last-wins while leaving vendor-prefixed rules distinct, emits `@-webkit-keyframes` for legacy Safari/Chrome targets, emits `@-moz-keyframes` for legacy Firefox targets, emits both prefix families when both target sets require them, and drops stale prefixed keyframes for modern targets.

This run maps the focused upstream `src/lib.rs::test_counter_style` pretty-printer helper case. Native PHP `CssFormatter` now formats declaration-only `@counter-style` rules such as fixed circled-alpha marker definitions with two-space declaration indentation and preserves quoted-space suffix values. `examples/wordpress-counter-style-formatter.php` models custom ordered-list marker CSS for block patterns without Node. Dependency closure: no new support component is needed; the slice reuses the existing bounded formatter declaration parser and does not add a shared parser/runtime dependency.

This run adds focused upstream-aligned `@scope` prelude minification for scoped block CSS: root-only scopes compact to `@scope(...)`, root-plus-limit scopes preserve the required whitespace before `to (...)`, case-insensitive `TO` limit keywords normalize to `to`, and selector-list roots/limits compact their comma separators. `examples/wordpress-scope-rule-minifier.php` models card block styles scoped until nested button blocks without Node. Dependency closure: no new support component is needed; the slice reuses the existing bounded `CssMinifier` at-rule/value scanner and does not add a shared parser/runtime dependency.

Current PHP lane evidence: focused `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 678 assertions, 0 failures. The scope-rule minifier example `php lanes/lightningcss/examples/wordpress-scope-rule-minifier.php` passes and emits scoped card block CSS without Node. Syntax checks pass for `CssMinifier.php`, `CssMinifierTest.php`, and `wordpress-scope-rule-minifier.php`. Dependency closure: no new support component is needed; this reuses the existing bounded `CssMinifier` at-rule/value scanner, with no external formatter, browser service, or shell-out introduced.

This run broadens the existing upstream-aligned errorRecovery slice from whole-rule `@media unknown(foo)` and `@container unknown(foo)` to compound top-level condition functions. Native PHP now omits invalid `@container card (width > 30em) and unknown(foo)` and `@media screen and unknown(foo)` rule blocks in recovery mode, reports structured `UnexpectedToken Function("unknown")` warnings at the offending function columns, and keeps following valid CSS. This is intentionally bounded to top-level condition functions so nested declaration values such as `calc()`/`url()` and allowed `style()`/`scroll-state()` container functions are not treated as recoverable parser errors. Dependency closure: no new support component is needed; the slice reuses the existing bounded `CssMinifier` at-rule scanner and source-location helper.

Current PHP lane evidence for this slice: focused `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 680 assertions, 0 failures. The updated recovery example `php lanes/lightningcss/examples/wordpress-container-error-recovery.php` passes and emits minified valid query CSS plus two `UnexpectedToken` warnings without Node. Syntax checks pass for `CssMinifier.php`, `CssMinifierTest.php`, and `wordpress-container-error-recovery.php`; `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice.

This run maps 3 focused upstream `src/lib.rs::test_border_radius` minifier cases after a targeted pristine upstream read counted 21 helper invocations in that function. Native PHP now compacts `border-radius`, `-webkit-border-radius`, and `-moz-border-radius` side lists by dropping repeated fourth, third, and second components, and applies the same compaction independently to slash-separated vertical radii. Prefix generation/removal and physical/logical radius longhand composition remain future work.

Current PHP lane evidence for this slice: focused `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 684 assertions, 0 failures. The new rounded block example `php lanes/lightningcss/examples/wordpress-border-radius-minifier.php` passes and emits compact modern plus prefixed radius CSS without Node. Syntax checks pass for `CssMinifier.php`, `CssMinifierTest.php`, and `wordpress-border-radius-minifier.php`; `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; the slice reuses the existing bounded `CssMinifier` declaration-value scanner and side-list helpers, with no external CSS parser, browser service, or shell-out introduced.

This run adds 2 focused upstream-aligned `@scope` nesting-lowering cases. Native PHP now routes top-level `@scope` bodies through scope-body lowering so `@nest .wp-block-theme &` resolves against `:scope` instead of being preserved, and the same scoped `@nest` behavior works when the `@scope` block is nested under `@media`. The minifier also preserves the descendant combinator before a scoped `:scope` pseudo-class. `examples/wordpress-nesting-transformer.php` now models a scoped query block grid variant using `@nest` inside the scoped post-template boundary. Dependency closure: no new support component is needed; the slice reuses the bounded native `NestingTransformer` scope lowering and `CssMinifier` selector spacing scanner.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/NestingTransformerTest.php` passes with 1 test file, 42 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1063 assertions, 0 failures. Syntax checks pass for `CssMinifier.php`, `NestingTransformer.php`, `NestingTransformerTest.php`, and `wordpress-nesting-transformer.php`. The updated `php lanes/lightningcss/examples/wordpress-nesting-transformer.php` smoke passes and emits scoped query block CSS with the lowered `@nest` variant. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice.

This run extends the focused upstream `src/lib.rs::test_border_radius` slice by 4 upstream-aligned cases. Native PHP now composes four `border-*-radius` longhands into the shortest `border-radius` shorthand, including equal corner collapse, elliptical horizontal/vertical slash syntax, later longhand sets after an intervening shorthand reset, and matching `-webkit-`/`-moz-` prefixed longhand groups. The focused border-radius inventory now maps 7 of the 21 counted upstream helper invocations; physical/logical corner prefix generation and removal remain future work.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 688 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1067 assertions, 0 failures. Syntax checks pass for `CssMinifier.php`, `CssMinifierTest.php`, and `wordpress-border-radius-minifier.php`. The updated `php lanes/lightningcss/examples/wordpress-border-radius-minifier.php` smoke passes and emits compact rounded block CSS with longhand-composed elliptical radii. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CssMinifier` declaration-entry composer and border-radius side-list helpers.

This run extends the focused upstream `src/lib.rs::test_border_radius` slice by 3 upstream-aligned prefixer cases. Native PHP now emits `-webkit-border-radius` for old Chrome/Safari targets, emits `-moz-border-radius` plus matching prefixed physical corner longhands for old Firefox targets, and drops stale duplicate `-webkit-`/`-moz-` radius declarations for modern targets when an identical unprefixed declaration exists. The focused border-radius inventory now maps 10 of the 21 counted upstream helper invocations; logical corner mapping and additional target-removal edge cases remain future work.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/TransitionPrefixerTest.php` passes with 1 test file, 150 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1070 assertions, 0 failures. Syntax checks pass for `TransitionPrefixer.php`, `TransitionPrefixerTest.php`, and `wordpress-border-radius-minifier.php`. The updated `php lanes/lightningcss/examples/wordpress-border-radius-minifier.php` smoke passes and emits compact rounded block CSS with legacy WebKit radius prefixes. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `TransitionPrefixer` declaration scanner and `CssMinifier` border-radius value/composition support.

This run extends the focused upstream `src/lib.rs::test_border_radius` slice by 3 upstream-aligned logical corner reset/interleaving cases. Native PHP now drops earlier `border-start-start-radius` declarations when a later `border-radius` shorthand resets all corners, preserves logical corner declarations that follow a compacted shorthand, and refuses to compose physical `border-*-radius` longhands across an intervening logical corner declaration. The focused border-radius inventory now maps 10 of the 21 counted upstream helper invocations; target-gated physical/logical corner prefix generation and modern prefix removal remain future work.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 691 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1070 assertions, 0 failures. Syntax checks pass for `CssMinifier.php`, `CssMinifierTest.php`, and `wordpress-border-radius-minifier.php`. The updated `php lanes/lightningcss/examples/wordpress-border-radius-minifier.php` smoke passes and emits compact rounded block CSS with an earlier logical corner reset by a final pill radius. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CssMinifier` declaration-entry composer and border-radius side-list helpers.

This run extends the focused upstream `src/lib.rs::test_border_radius` slice by 4 upstream-aligned Safari 12 logical-corner fallback cases. Native PHP now lowers `border-start-start-radius`, `border-start-end-radius`, `border-end-end-radius`, and `border-end-start-radius` declarations to LTR/RTL selector variants with physical corner longhands, including paired start/end groups and variable-backed values. The focused border-radius inventory now maps 17 of the 21 counted upstream helper invocations; shorthand-plus-physical override compaction and additional stale-prefix edge cases remain future work.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/TransitionPrefixerTest.php` passes with 1 test file, 154 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1077 assertions, 0 failures. Syntax checks pass for `TransitionPrefixer.php`, `TransitionPrefixerTest.php`, and `wordpress-border-radius-minifier.php`. The updated `php lanes/lightningcss/examples/wordpress-border-radius-minifier.php` smoke passes and emits compact rounded block CSS with legacy WebKit radius prefixes plus Safari logical navigation corner fallbacks. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `TransitionPrefixer` target-option flow, RTL selector helper, declaration scanner, and `CssMinifier` radius value/composition support.

This run extends the focused upstream `src/lib.rs::test_border_radius` slice by 2 upstream-aligned shorthand-plus-physical override cases. Native PHP now drops a `border-radius` shorthand when four later physical corner longhands fully supersede it and compose into a new shorthand, while preserving a partial physical override as the original shorthand plus surviving longhand. The focused border-radius inventory now maps 19 of the 21 counted upstream helper invocations; additional stale-prefix edge cases remain future work.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 693 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1079 assertions, 0 failures. Syntax checks pass for `CssMinifier.php`, `CssMinifierTest.php`, and `wordpress-border-radius-minifier.php`. The updated `php lanes/lightningcss/examples/wordpress-border-radius-minifier.php` smoke passes and emits compact rounded block CSS with a superseded cover radius shorthand removed before longhand-composed elliptical radii. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CssMinifier` declaration-entry composer, shorthand reset tracking, and border-radius side-list helpers.

This run completes the focused upstream `src/lib.rs::test_border_radius` helper cluster by mapping the final 2 partial physical-corner override cases. Native PHP now folds concrete `border-radius` plus later `border-top-left-radius` into the shortest updated shorthand, while preserving a variable-backed partial corner override as a separate guarded longhand. The focused border-radius inventory now maps all 21 of the 21 counted helper invocations.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 695 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1081 assertions, 0 failures. Syntax checks pass for `CssMinifier.php`, `CssMinifierTest.php`, and `wordpress-border-radius-minifier.php`. The updated `php lanes/lightningcss/examples/wordpress-border-radius-minifier.php` smoke passes and emits compact rounded block CSS with a media-text soft-corner override folded into a shorthand plus legacy WebKit output. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CssMinifier` declaration-entry composer, shorthand reset tracking, border-radius side-list helpers, and conservative `var()` guard.

This run extends the focused custom-media transformer slice with import media-tail substitution after comments and `layer` modifiers. Native PHP now resolves custom-media aliases in `@import` media query tails after the import source, CSS comments, bare `layer`, named `layer(...)`, and `supports()` modifiers, so breakpoint aliases shared with `@media` rules also apply to layered conditional block stylesheet imports. The implementation intentionally preserves import-source/modifier text and only feeds the trailing media query list through the existing custom-media resolver. Dependency closure: no new support component is needed; this reuses the existing bounded `CustomMediaTransformer` alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware top-level scanner.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CustomMediaTransformerTest.php` passes with 1 test file, 33 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1083 assertions, 0 failures. Syntax checks pass for `CustomMediaTransformer.php`, `CustomMediaTransformerTest.php`, and `wordpress-custom-media-transformer.php`. The updated `php lanes/lightningcss/examples/wordpress-custom-media-transformer.php` smoke passes and emits a responsive layered custom-media-backed `@import` plus motion media output. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice.

This rework/refill slice tightens the same focused custom-media scanner cluster after the current-base rebase conflict around `@import` media-tail handling. Native PHP now skips CSS comments while matching parentheses for `@media` preludes and `@import` media tails, so migration comments containing stale aliases and unmatched-looking `)` text do not truncate the media query or trigger undefined custom-media diagnostics. Live aliases in the same prelude/tail still resolve through the existing custom-media resolver.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CustomMediaTransformerTest.php` passes with 1 test file, 35 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1085 assertions, 0 failures. Syntax checks pass for `CustomMediaTransformer.php`, `CustomMediaTransformerTest.php`, and `wordpress-custom-media-transformer.php`. The updated `php lanes/lightningcss/examples/wordpress-custom-media-transformer.php` smoke passes and emits a responsive layered custom-media-backed `@import` plus motion media output while stale comment parentheses remain inert. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CustomMediaTransformer` alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware scanner/delimiter logic.

This rework/refill slice keeps the same current-base conflict area and tightens custom-media media-list splitting. Native PHP now skips CSS comments while splitting comma-separated `@media` preludes and `@import` media tails, so stale migration comments containing commas and alias-looking tokens do not create bogus query-list items or undefined custom-media diagnostics. Live aliases in adjacent media-list items still resolve normally.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CustomMediaTransformerTest.php` passes with 1 test file, 36 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1086 assertions, 0 failures. Syntax checks pass for `CustomMediaTransformer.php`, `CustomMediaTransformerTest.php`, and `wordpress-custom-media-transformer.php`. The updated `php lanes/lightningcss/examples/wordpress-custom-media-transformer.php` smoke passes and emits a responsive layered custom-media-backed `@import` plus motion media output while stale comment parentheses and commas remain inert. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CustomMediaTransformer` alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware scanner/delimiter/media-list splitting logic.

This rework/refill slice extends the current-base custom-media scanner conflict area to comments inside `@custom-media` definition bodies. Native PHP already routes definition values through the same comment-aware reference collector, delimiter matcher, and media-list splitter; the added focused coverage preserves that behavior so stale alias names, unmatched-looking `)`, and commas in definition comments stay inert while live aliases still resolve.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CustomMediaTransformerTest.php` passes with 1 test file, 37 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1087 assertions, 0 failures. Syntax checks pass for `CustomMediaTransformerTest.php` and `wordpress-custom-media-transformer.php`. The updated `php lanes/lightningcss/examples/wordpress-custom-media-transformer.php` smoke passes and emits a responsive layered custom-media-backed `@import` plus motion media output while stale definition, prelude, and import-tail comments remain inert. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CustomMediaTransformer` alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware scanner/delimiter/media-list splitting logic.

This rework slice hardens the same current-base custom-media scanner conflict area by replacing regex-only custom-media stripping with a comment/string-aware delimiter walk. Stale migration comments in `@import` modifier positions can now mention old `layer(...)`, `supports(...)`, alias-looking tokens, commas, and unmatched-looking `)` characters without influencing modifier parsing, media-list splitting, or media-type validation; live aliases after the comments still resolve.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CustomMediaTransformerTest.php` passes with 1 test file, 38 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 9 test files, 1088 assertions, 0 failures. Syntax checks pass for `CustomMediaTransformer.php`, `CustomMediaTransformerTest.php`, and `wordpress-custom-media-transformer.php`. The updated `php lanes/lightningcss/examples/wordpress-custom-media-transformer.php` smoke passes and emits a responsive layered custom-media-backed `@import` plus motion media output while stale import modifier-looking comment text remains inert. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CustomMediaTransformer` alias collector, media-query resolver, at-rule scanner, source-location mapper, and comment-aware scanner/delimiter/media-list splitting logic.

This run adds a focused native bundle/import graph slice from upstream `src/bundler.rs::test_bundle` and `node/test/bundle.test.mjs`. Native PHP now resolves recursive `@import` graphs from an in-memory source map, supports resolver callbacks that rewrite specifiers, applies default relative path resolution, preserves dependency-before-importer order, keeps the last browser-evaluated import position for repeated imports, suppresses active import cycles, OR-merges repeated media/supports imports of the same file, wraps imported rules in parent `@supports`, `@media`, and `@layer` conditions, prefixes imported layer statements under parent layers, preserves external imports before bundled imports, rejects external imports after bundled imports with source locations, and makes `@custom-media` declarations from one imported file available to later imported files before minification. This maps 9 focused upstream-aligned bundler checks while leaving CSS modules `composes/from`, source-map remapping, filesystem reads, and custom at-rule visitor integration for later slices.

This isolated bundle resolver slice maps the adjacent upstream Node `bundle.test.mjs` `only custom resolve` behavior. Native PHP now exposes `CssBundler::bundleFile()` so a custom resolver callback can rewrite specifiers while the default source provider reads the resolved stylesheet files from disk, then feeds those files through the same recursive import graph ordering and minification path. Focused evidence: `php tools/run-tests.php lanes/lightningcss/tests/CssBundlerTest.php` passes with 1 file, 112 assertions, 0 failures; full LightningCSS lane passes with 13 files, 2315 assertions, 0 failures; `wordpress-bundle-import-graph.php --self-test` exits 0 and prints `filesystem-provider: resolved`. Conservative mapped coverage moves from 1446 to 1447 / 3532. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the native bundler resolver boundary, PHP file reads, path normalizer, minifier, and custom-media transformer.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CssBundlerTest.php` passes with 1 test file, 27 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 10 test files, 1115 assertions, 0 failures. Syntax checks pass for `CssBundleException.php`, `CssBundler.php`, `CssBundlerTest.php`, and `wordpress-bundle-import-graph.php`. The new `php lanes/lightningcss/examples/wordpress-bundle-import-graph.php` smoke passes and emits a recursively bundled block-theme import graph with token, shared button, responsive card, and print stylesheets. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CssMinifier`, `CustomMediaTransformer`, media-query normalization, and native scanner/path helpers.

This integration also accepts the sibling CSS Modules and source-map slices from the same LightningCSS focus burst. Native PHP now maps 9 focused upstream CSS Modules checks for `:local`/`:global` selector handling, local/global/dependency `composes` export metadata, selector-list propagation, and repeated declaration ordering. It also maps 3 focused upstream source-map checks for input-source-map remapped VLQ columns, preserved license-comment line offsets, and CLI CSS-module semicolon offsets. Combined focused verification passes with `php tools/run-tests.php lanes/lightningcss/tests/CssBundlerTest.php lanes/lightningcss/tests/CssModulesTransformerTest.php lanes/lightningcss/tests/SourceMapTest.php`: 3 files, 66 assertions, 0 failures. Full lane-focused verification passes with `php tools/run-tests.php lanes/lightningcss/tests`: 12 files, 1154 assertions, 0 failures. The three new examples pass, and `git diff --check -- lanes/lightningcss` passes.

This isolated micro-slice extends the focused upstream `src/lib.rs::test_css_modules` cluster by 1 mapped helper case: a parent local class with `composes: test from "foo.css"` after a nested local selector keeps the dependency composes export on the parent, localizes the nested class, and emits a flattened descendant selector. Native PHP now routes minified CSS Modules output through the existing `NestingTransformer` after selector/export rewriting, preserving parent composes metadata while matching the upstream nested local selector output. The focused CSS Modules inventory now maps 10 upstream-aligned helper cases.

Current focused slice evidence: `php tools/run-tests.php lanes/lightningcss/tests/CssModulesTransformerTest.php` passes with 1 test file, 30 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 12 test files, 1157 assertions, 0 failures. Syntax checks pass for `CssModulesTransformer.php`, `CssModulesTransformerTest.php`, and `wordpress-css-modules-transformer.php`. The updated `php lanes/lightningcss/examples/wordpress-css-modules-transformer.php` smoke passes and emits scoped parent/child block module CSS plus dependency/global composes metadata. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CssModulesTransformer`, `NestingTransformer`, selector/declaration scanning, and `CssMinifier` support.

This isolated CSSOM declaration read/write micro-slice deepens the already-counted upstream `tests/test_cssom.rs` / `src/declaration.rs` DeclarationBlock cluster without increasing mapped denominator coverage. Native PHP now mirrors upstream `DeclarationBlock::set` behavior for supported `background-*` longhands by updating an existing `background` shorthand when the longhand layer count matches the shorthand layer count, while appending a separate longhand when the layer counts differ. It also mirrors upstream `DeclarationBlock::remove` shorthand behavior for the supported background set by removing `background` plus supported background longhands from both normal and `!important` priority buckets.

Current focused slice evidence: red-first `php tools/run-tests.php lanes/lightningcss/tests/DeclarationBlockTest.php` failed with 1 file, 97 assertions, 2 failures before implementation. After implementation, focused `php tools/run-tests.php lanes/lightningcss/tests/DeclarationBlockTest.php` passes with 1 test file, 103 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 13 test files, 1221 assertions, 0 failures. Syntax checks pass for `DeclarationBlock.php`, `DeclarationBlockTest.php`, and `wordpress-background-cssom.php`. The new `php lanes/lightningcss/examples/wordpress-background-cssom.php --self-test` smoke passes. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `DeclarationBlock` parser, priority buckets, and background layer scanner.

This isolated micro-slice extends the focused upstream Node visitor/custom parser cluster by 2 mapped checks from pinned `node/test/customAtRules.mjs`, `node/test/composeVisitors.test.mjs`, `node/test/visitor.test.mjs`, and `node/ast.d.ts`. Native PHP now composes `Function` visitors alongside custom, unknown, and token visitors, so declaration-list custom at-rule collection can feed later function replacements inside a composed visitor. Unknown at-rule preludes now expose dashed identifiers as upstream-shaped `dashed-ident` token-or-value entries, and declaration-value at-keyword scanning now accepts dashed at-keywords such as `@--wp-accent`.

Current focused slice evidence: baseline `php tools/run-tests.php lanes/lightningcss/tests/CustomAtRuleTransformerTest.php` passed with 1 test file, 22 assertions, 0 failures before this slice. After implementation, focused `php tools/run-tests.php lanes/lightningcss/tests/CustomAtRuleTransformerTest.php` passes with 1 test file, 28 assertions, 0 failures. Full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 13 test files, 1765 assertions, 0 failures. Syntax checks pass for `CustomAtRuleTransformer.php`, `CustomAtRuleTransformerTest.php`, and `wordpress-custom-at-rules-transformer.php`. The updated `php lanes/lightningcss/examples/wordpress-custom-at-rules-transformer.php --self-test` smoke passes and exercises composed `Function` visitors plus dashed design-token aliases. `git diff --check -- lanes/lightningcss` passes. Root harness status: not run - isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded `CustomAtRuleTransformer`, visitor composition, declaration parser, minifier, and native scanner helpers.

This isolated custom at-rule visitor slice maps six focused upstream Node declaration visitor cases from `node/test/composeVisitors.test.mjs` and `node/test/visitor.test.mjs`: custom `Declaration` visitors expand `size` and `bg` declarations, structured declaration replacements feed `Length` visitors, unparsed known declarations such as `width: test` are exposed with `propertyId`, returned unparsed `var()` values remain composable into `calc()`, and generic `Declaration` / `DeclarationExit` all-property handlers can replace width/height declarations. `examples/wordpress-custom-at-rules-transformer.php` now models a `size: 48px` block media utility expanded without Node. Focused `php tools/run-tests.php lanes/lightningcss/tests/CustomAtRuleTransformerTest.php` passes with 1 test file, 51 assertions, 0 failures; full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 13 test files, 2176 assertions, 0 failures. Dependency closure: no new support component is needed; this reuses the bounded `CustomAtRuleTransformer`, `DeclarationBlock`, native scanner helpers, and lane-local value serialization.

This isolated bundle/import graph slice maps 2 focused upstream Node checks from `node/test/bundle.test.mjs`: a resolver-provided source ending in `@import "bar.css"` without a semicolon still resolves the imported stylesheet and reports read failures at the import location, and unterminated resolver-provided source reports `Unexpected end of input` with filename and source column. Native PHP now treats EOF-terminated top-level `@import` as an import rule and turns other trailing unterminated top-level input into a `parser-error` diagnostic. Focused `php tools/run-tests.php lanes/lightningcss/tests/CssBundlerTest.php` passes with 1 test file, 119 assertions, 0 failures; full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 13 test files, 2368 assertions, 0 failures. `examples/wordpress-bundle-import-graph.php --self-test` now includes `reader-eof-import: resolved`. Dependency closure: no new support component is needed; this reuses the bounded `CssBundler` top-level scanner, SourceProvider/read callback boundary, path resolver, minifier, and bundle exception model.

This isolated target-prefixing slice maps 4 focused upstream `src/prefixes.rs` checks for `Feature::ObjectFit | Feature::ObjectPosition`: Opera 10.5 and Opera 13 do not receive `-o-` prefixes, while Opera 10.6 through 12.1 receive `-o-object-fit` and `-o-object-position` before the modern declarations. Focused `php tools/run-tests.php lanes/lightningcss/tests/TransitionPrefixerTest.php` passes with 1 test file, 434 assertions, 0 failures; full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 13 test files, 2802 assertions, 0 failures. `examples/wordpress-object-fit-prefixer.php` passes and models featured-image/cover-video object-fit delivery for Opera 10.5, 10.6, 12.1, and 13. Conservative mapped coverage moves from 1601 to 1605 / 3532. Dependency closure: no new support component is needed; this reuses `TransitionPrefixer` target-version routing and declaration-prefix rewriting.

This isolated property-values slice maps 33 focused upstream `src/lib.rs::test_gradients` radial/conic minifier helper cases after the accepted linear-gradient cluster. Native PHP now canonicalizes radial shape/extent/position preludes, prefixed radial and repeating-radial functions, legacy `-webkit-gradient(radial, ...)` point/color stops, conic default `from 0deg` and `at 50% 50%` prelude elision, and conic stop color/position serialization. Focused `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 1106 assertions, 0 failures; full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 13 test files, 2915 assertions, 0 failures. `examples/wordpress-gradient-value-minifier.php` passes and now models linear, radial, and conic block gradients without Node. Conservative mapped coverage moves from 1637 to 1670 / 3532. Dependency closure: no new support component is needed; this reuses `CssMinifier` declaration-value scanning, math folding, color serialization, and top-level function/list helpers.

This isolated property-values slice maps seven focused upstream `src/lib.rs::test_custom_properties` typed `attr()` minifier helper cases. Native PHP now preserves upstream `attr(data-color type(<color>))`, `attr(data-width type(<length>), 100px)`, `%` type spacing, bare-comma empty fallback serialization, and `px`/`number`/`raw-string` attr arguments after the generic whitespace pass. Focused `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 1535 assertions, 0 failures; full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 13 test files, 4269 assertions, 0 failures. `examples/wordpress-attr-typed-value-minifier.php` passes and models typed data-attribute color/length delivery for block CSS without Node. Conservative mapped coverage moves from 2093 to 2100 / 3532. Dependency closure: no new support component is needed; this reuses `CssMinifier` declaration scanning, function parsing, list splitting, and token helpers.

This isolated CSS Modules slice deepens the already represented `src/lib.rs::test_css_modules` local/global/composes cluster with upstream attribute selector serialization parity. Native PHP now minifies quoted attribute selector values inside scoped selectors and nested selector functions, preserves required quoted strings, keeps escaped-space identifiers intact after minification, preserves authored attribute strings when CSS Modules minification is disabled, and rejects invalid unquoted delimiter values such as `[data-state=.wide]`. Focused `php tools/run-tests.php lanes/lightningcss/tests/CssModulesTransformerTest.php` passes with 1 test file, 273 assertions, 0 failures; full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 13 test files, 4395 assertions, 0 failures. `examples/wordpress-css-modules-attribute-selectors.php --self-test` passes and models block CSS Modules selectors using WordPress global block attributes plus local `composes`. Conservative mapped coverage remains `2117 / 3532` because this is an already represented CSS Modules cluster. Dependency closure: no new support component is needed; this reuses `CssModulesTransformer`, `CssMinifier`, `NestingTransformer`, selector scanning, and CSS identifier/string token helpers.

This isolated property-values slice deepens the already represented upstream `src/lib.rs::test_font` target fallback cluster with `font` shorthand oblique-angle parity. Native PHP now preserves `font` shorthand fallbacks containing `oblique <angle>` before target-prefix evaluation, removes them only when selected targets support oblique-angle font styles, avoids treating angle dimensions such as `40deg` as font-size tokens, and expands `system-ui` fallback families inside oblique-angle font shorthands. Focused `php tools/run-tests.php lanes/lightningcss/tests/CssMinifierTest.php` passes with 1 test file, 1737 assertions, 0 failures; focused `php tools/run-tests.php lanes/lightningcss/tests/TransitionPrefixerTest.php` passes with 1 test file, 935 assertions, 0 failures; full lane-focused `php tools/run-tests.php lanes/lightningcss/tests` passes with 13 test files, 5721 assertions, 0 failures. `examples/wordpress-font-target-fallback.php --self-test` passes and now models a slanted block heading `font` shorthand fallback without Node. Conservative mapped coverage remains `2315 / 3532`; this is an already represented font target fallback cluster. Dependency closure: no new support component is needed; this reuses `CssMinifier`, `TransitionPrefixer`, target-version routing, font shorthand token scanning, and the existing lane-local WordPress font fallback smoke.
