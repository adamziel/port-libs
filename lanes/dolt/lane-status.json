{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 40,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; 153 focused upstream behavior mappings now recorded. Bounded upstream Go/BATS evidence remains green for the existing serialized Go/BATS slices, and this slice adds a fresh focused upstream `TestPatchTableFunction(Prepared)?` pass in 0.598s covering 13 `dolt_patch()` script tests and 70 query assertions. Fresh 23:26-23:52 UTC runner evidence also reran the serialized 16-package Go batch, focused sqle/enginetest diff/schema/stat/log/branch/status/conflict coverage, schema/procedure/history and ancestor slices, broad 319-plan local BATS, branch/sql-branch BATS plan 1..39, the status helper negative-control/fixed-helper pair, and direct keyless diff-stat CLI/JSON probes. Current PHP slice maps native `dolt_patch()` schema/data patch rows plus table-function argument parsing for explicit refs, two-dot ranges, merge-base-backed three-dot ranges, case-insensitive table lookup, known unchanged tables, table-not-found errors, and non-literal argument rejection. Full Go/BATS runners not executed.",
    "phpPass": 136,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, native `dolt diff -r sql` and tabular review queues, keyless wp_import_log duplicate/cardinality rows, native `dolt_patch()` schema/data patch queues for wp_posts column rename/addition plus keyless import-log SQL statements, dot-range/three-dot patch-call review queues, known unchanged table empty results, missing-table errors, multiline block diffs, table summary/stat review output, schema/procedure history, commit diff/log/ancestors/ancestry, branch review rows, ignore-aware summaries/conflicts, primary-key-change warnings, and status/merge review queues.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors/has_ancestor/branch/log-graph/log-number/diff-summary/diff-filter/row-sql/row-tabular/keyless-row-sql-tabular/diff-mode/diff-summary-table-args/diff-stat-table-args/diff-stat-json/diff-stat-keyless/dolt-patch upstream evidence + native schema-aware, skinny, where/limit, summary/stat, dolt_ignore, CLI summary/stat, row SQL/tabular, status, merge/conflict, schema/procedure history, commit diff/log/ancestors/ancestry, branch projection, patch-row, and patch function-call boundary slices",
    "audit": "audited 2026-05-23: 136 Dolt PHP behavior tests pass with 710 assertions across 16 Dolt test files. Current implementation slice maps `dolt_patch()` table-function argument boundaries against focused upstream evidence: `TestPatchTableFunction(Prepared)?` passed in 0.598s, covering 13 script tests and 70 query assertions. Native PHP now validates literal text arguments, rejects non-literal function-expression arguments, parses explicit two-ref calls plus `A..B` and merge-base-backed `A...B` ranges, filters requested tables case-insensitively with `to` names before `from` names, returns no rows for known unchanged tables, and reports missing tables. Prior patch-row behavior still emits schema-before-data patch rows, restarts statement_order for schema/data filters, maps focused CREATE/DROP/ALTER DDL, suppresses data after DROP TABLE, and reuses keyed/keyless SQL row statements. Required root `php tools/run-tests.php` passed with 157 test files, 14,270 assertions, and 0 failures. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* rows including keyless multiset duplicate/cardinality rows, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), CLI summary/stat rendering, row-level SQL/tabular keyed and keyless rendering, diff modes, native dolt_patch() schema/data patch rows with focused DDL and keyed/keyless SQL statements plus function-call argument parsing and table lookup, dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF, dolt_log/dolt_commits, CLI log rendering, dolt_commit_ancestors, has_ancestor, dolt_branches, dolt_remote_branches, active_branch, and dolt_branch_activity projection rows.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. Broad Go package batches should use runner-local TMPDIR and `go test -p 1` to avoid linker temp/quota failures. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. branch-activity.bats was skipped because it starts a Dolt SQL server; local in-process TestBranchActivity is the bounded branch-activity evidence.",
    "latestCommit": "pending (Add Dolt patch function call boundaries)",
    "nextTask": "Map `dolt_patch()` authorization boundaries and broader revision/table edge cases such as WORKING/STAGED no-op rows and binary hex SQL values."
}
