{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 28,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; bounded upstream Go diff/table/schema/dtables/dtablefunctions/sqlfmt/expreval/enginetest tests reran and pass. Fresh tooling/quota evidence confirmed Go/BATS/expect are installed, reproduced a broad Go-batch linker `disk quota exceeded` failure without temp isolation, then passed the same 16-package batch with runner-local TMPDIR and `go test -p 1`; focused enginetest, integration, dtablefunction, ancestor/status, 319-plan local BATS, pristine-status negative-control, patched-status, and 39-plan branch/sql-branch evidence also ran. Current slice passed targeted upstream `log.bats` dense multi-branch graph evidence with plan 1..1 and an exact cache-local `dolt log --graph --oneline --decorate=short` dense fan-in probe that confirmed post-hash spacing/ref-placement quirks. Prior evidence includes `dolt log --graph`/decorate evidence, `dolt log --oneline`/`--stat`, `dolt log --all`, merge/min-parent Go/BATS coverage, log table-filter Go/BATS evidence, status/sql-status, commit-ancestor/has_ancestor/branch engine evidence, and a 319-plan patched-copy local BATS pass with 303 runnable passes and 16 upstream skips. Full Go/BATS runners not executed.",
    "phpPass": 98,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, wp_posts to wp_content_posts table rename summary, wp_plugin_events schema-drift projection, skinny post-review diffs, filtered publish-impacting review rows, aggregate wp_posts diff-stat counts, DOLT_COMMIT_DIFF-style wp_posts review rows, commit log review rows with branch refs and merge parents plus import-base/media-promotion revision ranges, wp_posts/wp_postmeta table-filtered histories, all branch-head review history that excludes abandoned scratch checkpoints, merge-only import review rows, non-root checkpoint histories, compact `dolt log --oneline --stat` import-review text, compact `dolt log --graph --oneline` import branch graph text with upstream post-hash spacing/ref placement, dense default `dolt log --graph` import fan-in text for multiple WordPress import branches, dolt_commit_ancestors parent rows that link reviewed import merge commits back to parent log messages, has_ancestor branch/tag checks for reviewed import merges, branch-review rows with active/dirty migration branches and activity timestamps, ignore-aware summaries/conflicts, primary-key-change warnings, status/merge review queues, schema history, and procedure history.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors/has_ancestor/branch/log-graph upstream runner + schema-aware, skinny, where/limit, summary/stat primary-key-warning, dolt_ignore summary-filter/conflict, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, dolt_schemas history/diff, dolt_procedures history/diff, DOLT_COMMIT_DIFF, dolt_log revision-range, all branch-head traversal, table filtering, merge/min-parent filtering, CLI oneline/stat rendering, CLI graph rendering including dense default fan-in and exact dense graph-oneline spacing/ref placement, TTY-sensitive decorate-auto rendering, dolt_commits, dolt_commit_ancestors, has_ancestor, dolt_branches, dolt_remote_branches, active_branch, and dolt_branch_activity projection slice",
    "audit": "audited 2026-05-22: 98 Dolt PHP behavior tests pass with 524 assertions across 10 Dolt test files. Fresh runner/tooling evidence confirmed installed `golang-1.26.3`, `bats-1.13.0`, `expect-5.45.4`, and `libicu-devel`; cache-local dolt/noms/remotesrv rebuilt with `dolt version 2.0.5`. Current 21:24 UTC runner refresh passed the serialized 16-package Go batch with runner-local TMPDIR and `go test -p 1`, focused diff/schema/stat/commit-diff/log/branch/branch-activity enginetest coverage, schema/procedure/history integration coverage, status/conflict/has_ancestor/dtablefunction focused Go slices, the combined 15-file local BATS slice with plan 1..319 (303 runnable passes and 16 upstream-declared skips), and branch/sql-branch BATS with plan 1..39; the pristine `status.bats` negative-control still fails due fixed-width commit-hash truncation. Current implementation slice also passed targeted upstream `log.bats` dense multi-branch graph evidence with plan 1..1 and an exact cache-local `dolt log --graph --oneline --decorate=short` dense fan-in probe, covering the compact graph spacing/ref-placement shape for four side branches merged into main. Prior bounded evidence includes graph/decorate BATS, oneline/stat log BATS, all-branch log, table-filter and merge/min-parent log Go/BATS evidence, commit-ancestor and has_ancestor evidence, branch table/activity evidence, focused enginetest/integration/dtablefunction/status runs, 319-plan local BATS evidence, and earlier 319-plan patched-copy local BATS evidence. Required root `php tools/run-tests.php` passed after the current metadata update with 127 test files, 11,352 assertions, and 0 failures. Dolt tests reached by the root run passed. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* row projection, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF commit snapshot projection, dolt_log/dolt_commits commit metadata projection with revisionSpecs/notRevisionSpecs for caret exclusions, two-dot ranges, three-dot merge-base ranges, multi-ref unions, upstream-shaped invalid range boundaries, all branch-head traversal from current HEAD plus local/remote branch refs, table filtering over changedTables or root-value-style tableHashes metadata, `--merges` / `--min-parents` parent-count filtering, and native `dolt log` CLI rendering for `--oneline`, `--parents`, `--stat`, `--stat --oneline`, `--graph` including dense default merge fan-in formatting, `--graph --oneline` including the upstream first-line vs later-line post-hash spacing/ref-placement quirk, and `--decorate=auto` captured-vs-TTY boundaries, native dolt_commit_ancestors rows with root null parents, merge parent indexes, commit_hash filtering, native has_ancestor commit graph checks with branch/tag/HEAD/hash refs plus ^, ^N, and ~N suffixes, and native dolt_branches/dolt_remote_branches/active_branch/dolt_branch_activity branch projection rows.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. Broad Go package batches should use runner-local TMPDIR and `go test -p 1` to avoid linker temp/quota failures. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. branch-activity.bats was skipped because it starts a Dolt SQL server; local in-process TestBranchActivity is the bounded branch-activity evidence. Dolt-local PHP and the latest required root PHP harness are green; the remaining blocker is full upstream runner parity only.",
    "latestCommit": "pending lane commit for native dense multi-branch dolt log graph-oneline spacing after green root PHP: 127 test files, 11,352 assertions, 0 failures",
    "nextTask": "Map another bounded Dolt CLI/system-table behavior, such as `dolt log -n` / `--limit` argument handling or a focused `dolt diff --summary` CLI text boundary, against upstream BATS evidence."
}
