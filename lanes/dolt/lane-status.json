{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 42,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; 165 focused upstream behavior mappings now recorded. Fresh inspected sparse inventory counted 1,420 Go test/benchmark functions in 377 checked-out Go test files and 3,630 hydrated sparse BATS @test cases in 203 BATS files, while the broader static denominator remains 399 Go _test.go files and 214 BATS files. Bounded upstream Go/BATS evidence remains green for the existing serialized Go/BATS slices. Fresh focused `Test(PatchTableFunction|PatchTableFunctionPrepared|DoltUserPrivileges)$` passed in 0.648s, covering `dolt_patch()` prepared/unprepared patch rows, binary hex SQL patch statements, WORKING/STAGED same-ref no-op rows, and table-function authorization boundaries. Full Go/BATS runners not executed.",
    "phpPass": 142,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, native `dolt diff -r sql` and tabular review queues, keyless wp_import_log duplicate/cardinality rows, native `dolt_patch()` schema/data patch queues for wp_posts column rename/addition plus keyless import-log SQL statements, binary wp_attachment_hashes media fingerprint patch statements, dot-range/three-dot patch-call review queues, permission-gated revision-database patch review for limited wp_posts reviewers, known unchanged table empty results, missing-table errors, multiline block diffs, table summary/stat review output, schema/procedure history, commit diff/log/ancestors/ancestry, branch review rows, ignore-aware summaries/conflicts, primary-key-change warnings, and status/merge review queues.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors/has_ancestor/branch/log-graph/log-number/diff-summary/diff-filter/row-sql/row-tabular/keyless-row-sql-tabular/diff-mode/diff-summary-table-args/diff-stat-table-args/diff-stat-json/diff-stat-keyless/dolt-patch/dolt-patch-authorization upstream evidence + native schema-aware, skinny, where/limit, summary/stat, dolt_ignore, CLI summary/stat, row SQL/tabular including binary hex literals, status, merge/conflict, schema/procedure history, commit diff/log/ancestors/ancestry, branch projection, patch-row, patch function-call, and patch authorization slices",
    "audit": "audited 2026-05-23: 142 Dolt PHP behavior tests pass with 733 assertions across 16 Dolt test files. Required root `php tools/run-tests.php` passed with 162 test files, 14,732 assertions, and 0 failures after this status stamp. Current implementation slice maps `dolt_patch()` SELECT authorization boundaries against focused upstream evidence: `Test(PatchTableFunction|PatchTableFunctionPrepared|DoltUserPrivileges)$` passed in 0.648s, covering prepared/unprepared patch rows plus database access denial, table-level SELECT, database-level SELECT, revokes, two-dot and three-dot permission boundaries, binary hex SQL patch statements, and WORKING/STAGED no-op rows. Native PHP now strips revision database names to their base database, checks requested table literals before rendering, requires unscoped calls to have SELECT on every supplied database table, accepts `db.table`, `db.*`, and `*.*` grants, and checks privileges before same-ref no-op calls return empty rows. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* rows including keyless multiset duplicate/cardinality rows, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), CLI summary/stat rendering, row-level SQL/tabular keyed and keyless rendering, binary/varbinary/vector SQL hex literal rendering, diff modes, native dolt_patch() schema/data patch rows with focused DDL and keyed/keyless SQL statements plus function-call argument parsing, same-ref no-op rows, table lookup, and SELECT privilege checks, dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF, dolt_log/dolt_commits, CLI log rendering, dolt_commit_ancestors, has_ancestor, dolt_branches, dolt_remote_branches, active_branch, and dolt_branch_activity projection rows.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. Broad Go package batches should use runner-local TMPDIR and `go test -p 1` to avoid linker temp/quota failures. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. branch-activity.bats was skipped because it starts a Dolt SQL server; local in-process TestBranchActivity is the bounded branch-activity evidence.",
    "latestCommit": "4978a39 (Add Dolt patch privilege checks)",
    "nextTask": "Map broader `dolt_patch()` revision/table edge cases such as branch/tag resolution failures and staged/worktree materialization beyond same-ref no-op rows."
}
