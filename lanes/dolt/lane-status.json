{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 34,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; bounded upstream Go diff/table/schema/dtables/dtablefunctions/sqlfmt/expreval/enginetest tests reran and pass. Current slice added a direct cache-local CLI probe and native PHP mapping for `dolt diff --summary [tables...]` table-argument short-circuit behavior: when an earlier changed table is outside the requested table set, upstream exits successfully with empty output for a later requested table. Fresh 2026-05-22 22:22-22:43 UTC runner refresh rechecked Go/BATS/expect/libicu tooling, rebuilt cache-local dolt/noms/remotesrv, passed the serialized 16-package Go batch, focused sqle/enginetest diff/schema/stat/commit-diff/log/branch/status/conflict coverage in 15.411s, focused schema/procedure/history and ancestor Go slices, the broad 319-plan local BATS slice with 303 runnable passes and 16 upstream skips, branch/sql-branch BATS with plan 1..39, a pristine status.bats negative-control failure for the fixed-width commit-hash helper, and the runner-local status-local-fixed repro pass. Prior focused evidence includes tabular `dolt diff --diff-mode=row|line|in-place|context`, row-mode tabular `dolt diff --filter`, row SQL diff-filter evidence with plan 1..1, diff-filter plan 1..3 for invalid/renamed/dropped/removed boundaries, `diff: table-only option` plan 1..1 for fixed-width `dolt diff --summary`, `diff-stat.bats` table-specific summary coverage, `log.bats` `-n` plan 1..2, direct cache-local log/graph/decorate probes, status/sql-status, commit-ancestor/has_ancestor/branch engine evidence, and a 118-plan local BATS branch/log/status slice. Full Go/BATS runners not executed.",
    "phpPass": 115,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, native `dolt diff -r sql` INSERT/UPDATE/DELETE review queues for wp_posts, native row-mode tabular `dolt diff` review queues for added/modified/removed wp_posts rows, multiline `post_content` block review through `--diff-mode=row|line|in-place|context`, wp_posts to wp_content_posts table rename summary plus fixed-width, diff-type filtered, and table-argument `dolt diff --summary` CLI rendering for migration reviewers, wp_plugin_events schema-drift projection, skinny post-review diffs, filtered publish-impacting review rows, aggregate wp_posts diff-stat counts, DOLT_COMMIT_DIFF-style wp_posts review rows, commit log review rows and import branch graphs, dolt_commit_ancestors parent rows, has_ancestor branch/tag checks, branch-review rows, ignore-aware summaries/conflicts, primary-key-change warnings, status/merge review queues, schema history, and procedure history.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors/has_ancestor/branch/log-graph/log-number/diff-summary/diff-filter/row-sql/row-tabular/diff-mode/diff-summary-table-args upstream evidence + native schema-aware, skinny, where/limit, summary/stat, dolt_ignore, fixed-width CLI diff-summary, CLI table-argument summary short-circuiting, row-level SQL and tabular diff rendering, tabular diff-mode rendering, status, merge/conflict, schema/procedure history, commit diff/log/ancestors/ancestry, and branch projection slice",
    "audit": "audited 2026-05-22: 115 Dolt PHP behavior tests pass with 602 assertions across 13 Dolt test files. Current implementation slice maps `dolt diff --summary [tables...]` table-argument behavior against direct cache-local upstream evidence: unscoped summary printed two changed tables, `--summary zzz` returned successful empty output because earlier `aaa` was outside the requested table set, and `--summary aaa` printed only the first table before stopping. Fresh 22:22-22:43 UTC bounded upstream runner evidence confirms installed `golang-1.26.3`, `bats-1.13.0`, `expect-5.45.4`, `libicu-devel`, cache-local dolt/noms/remotesrv at `dolt version 2.0.5`, a passing serialized 16-package Go batch, focused enginetest/integration/ancestor/status/branch/conflict slices, `TestDoltLog`, broad 319-plan local BATS with 303 runnable passes and 16 skips, branch/sql-branch BATS with plan 1..39, and a fresh pristine `status.bats` negative control that still fails because fixed-width commit-hash extraction truncates current `dolt log` output; the runner-local fixed helper passes that exact repro. Required root `php tools/run-tests.php` after this slice passed on the latest captured rerun with 143 test files, 12,874 assertions, and 0 failures after one unrelated Readability failure while the shared tree was moving. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* row projection, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), fixed-width `dolt diff --summary` CLI text rendering, CLI table-argument summary short-circuiting, name-only output with diff-type filters, native `dolt diff -r sql` rendering, native tabular `dolt diff` rendering with fixed-width `+`/`-`/`<`/`>` rows, diff-type filters, and `row`/`line`/`in-place`/`context` diff modes, dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF, dolt_log/dolt_commits, CLI log rendering, dolt_commit_ancestors, has_ancestor, dolt_branches, dolt_remote_branches, active_branch, and dolt_branch_activity projection rows.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. Broad Go package batches should use runner-local TMPDIR and `go test -p 1` to avoid linker temp/quota failures. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. branch-activity.bats was skipped because it starts a Dolt SQL server; local in-process TestBranchActivity is the bounded branch-activity evidence.",
    "latestCommit": "pending (Dolt summary table-argument slice)",
    "nextTask": "Map `dolt diff --stat <table>` table-specific output and empty-output boundaries, reusing the existing diff-stat counters."
}
