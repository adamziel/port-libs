{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 33,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; bounded upstream Go diff/table/schema/dtables/dtablefunctions/sqlfmt/expreval/enginetest tests reran and pass. Current slice added a cache-local direct CLI probe and native PHP mapping for tabular `dolt diff --diff-mode=row|line|in-place|context`, including upstream's default context behavior: multiline modified rows collapse into `*` line diffs, while single-line context rows remain `<`/`>`. Prior focused evidence includes row-mode tabular `dolt diff --filter`, row SQL diff-filter evidence with plan 1..1, diff-filter plan 1..3 for invalid/renamed/dropped/removed boundaries, `diff: table-only option` plan 1..1 for fixed-width `dolt diff --summary`, `log.bats` `-n` plan 1..2, direct cache-local log/graph/decorate probes, status/sql-status, commit-ancestor/has_ancestor/branch engine evidence, a 118-plan local BATS branch/log/status slice, and a broad 319-plan local BATS slice with 303 runnable passes and 16 upstream skips. Full Go/BATS runners not executed.",
    "phpPass": 113,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, native `dolt diff -r sql` INSERT/UPDATE/DELETE review queues for wp_posts, native row-mode tabular `dolt diff` review queues for added/modified/removed wp_posts rows, multiline `post_content` block review through `--diff-mode=row|line|in-place|context`, wp_posts to wp_content_posts table rename summary plus fixed-width and diff-type filtered `dolt diff --summary` CLI rendering for migration reviewers, wp_plugin_events schema-drift projection, skinny post-review diffs, filtered publish-impacting review rows, aggregate wp_posts diff-stat counts, DOLT_COMMIT_DIFF-style wp_posts review rows, commit log review rows and import branch graphs, dolt_commit_ancestors parent rows, has_ancestor branch/tag checks, branch-review rows, ignore-aware summaries/conflicts, primary-key-change warnings, status/merge review queues, schema history, and procedure history.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors/has_ancestor/branch/log-graph/log-number/diff-summary/diff-filter/row-sql/row-tabular/diff-mode upstream evidence + native schema-aware, skinny, where/limit, summary/stat, dolt_ignore, fixed-width CLI diff-summary, row-level SQL and tabular diff rendering, tabular diff-mode rendering, status, merge/conflict, schema/procedure history, commit diff/log/ancestors/ancestry, and branch projection slice",
    "audit": "audited 2026-05-22: 113 Dolt PHP behavior tests pass with 594 assertions across 13 Dolt test files. Current implementation slice maps tabular `dolt diff --diff-mode=row|line|in-place|context` output against a direct cache-local upstream probe, including line-level `*` rows, in-place uncolored text merging, and default context mode using line diffs only for multiline modified rows. Latest bounded upstream runner evidence still includes installed `golang-1.26.3`, `bats-1.13.0`, `expect-5.45.4`, `libicu-devel`, cache-local dolt/noms/remotesrv at `dolt version 2.0.5`, serialized 16-package Go batches, focused enginetest/integration/status/ancestor slices, 118-plan local BATS, broad 319-plan local BATS with 303 runnable passes and 16 skips, and the focused upstream `diff: row, line, in-place, context diff modes` BATS case. Required root `php tools/run-tests.php` was run after this slice: the first run failed outside Dolt in Readability while the tree was moving, and the final captured rerun passed with 142 test files, 12,668 assertions, and 0 failures. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* row projection, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), fixed-width `dolt diff --summary` CLI text rendering and name-only output with diff-type filters, native `dolt diff -r sql` rendering, native tabular `dolt diff` rendering with fixed-width `+`/`-`/`<`/`>` rows, diff-type filters, and `row`/`line`/`in-place`/`context` diff modes, dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF, dolt_log/dolt_commits, CLI log rendering, dolt_commit_ancestors, has_ancestor, dolt_branches, dolt_remote_branches, active_branch, and dolt_branch_activity projection rows.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. Broad Go package batches should use runner-local TMPDIR and `go test -p 1` to avoid linker temp/quota failures. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. branch-activity.bats was skipped because it starts a Dolt SQL server; local in-process TestBranchActivity is the bounded branch-activity evidence.",
    "latestCommit": "78dc7cf (Add Dolt tabular diff modes)",
    "nextTask": "Explicitly document and decide how to handle the current upstream `dolt diff --summary <table>` short-circuit boundary, then map another focused table-specific CLI summary case."
}
