{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 41,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; 157 focused upstream behavior mappings now recorded. Fresh inspected sparse inventory counted 1,420 Go test/benchmark functions in 377 checked-out Go test files and 3,630 hydrated sparse BATS @test cases in 203 BATS files, while the broader static denominator remains 399 Go _test.go files and 214 BATS files. Bounded upstream Go/BATS evidence remains green for the existing serialized Go/BATS slices. Fresh focused `Test(PatchTableFunction|PatchTableFunctionPrepared)$` passed in 0.676s, covering upstream binary hex SQL patch statements and WORKING/STAGED same-ref no-op rows. Current PHP slice maps native `dolt_patch()` schema/data patch rows, binary/varbinary/vector SQL hex literals, and table-function argument parsing for explicit refs, two-dot ranges, merge-base-backed three-dot ranges, same-ref no-op rows, case-insensitive table lookup, known unchanged tables, table-not-found errors, and non-literal argument rejection. Full Go/BATS runners not executed.",
    "phpPass": 140,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, native `dolt diff -r sql` and tabular review queues, keyless wp_import_log duplicate/cardinality rows, native `dolt_patch()` schema/data patch queues for wp_posts column rename/addition plus keyless import-log SQL statements, binary wp_attachment_hashes media fingerprint patch statements, dot-range/three-dot patch-call review queues, known unchanged table empty results, missing-table errors, multiline block diffs, table summary/stat review output, schema/procedure history, commit diff/log/ancestors/ancestry, branch review rows, ignore-aware summaries/conflicts, primary-key-change warnings, and status/merge review queues.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors/has_ancestor/branch/log-graph/log-number/diff-summary/diff-filter/row-sql/row-tabular/keyless-row-sql-tabular/diff-mode/diff-summary-table-args/diff-stat-table-args/diff-stat-json/diff-stat-keyless/dolt-patch upstream evidence + native schema-aware, skinny, where/limit, summary/stat, dolt_ignore, CLI summary/stat, row SQL/tabular including binary hex literals, status, merge/conflict, schema/procedure history, commit diff/log/ancestors/ancestry, branch projection, patch-row, and patch function-call boundary slices",
    "audit": "audited 2026-05-23: 140 Dolt PHP behavior tests pass with 721 assertions across 16 Dolt test files. Current implementation slice maps `dolt_patch()` binary SQL and same-ref no-op boundaries against focused upstream evidence: `Test(PatchTableFunction|PatchTableFunctionPrepared)$` passed in 0.676s, covering 13 script tests and 70 query assertions including binary hex literals and WORKING/STAGED no-row cases. Native PHP now validates literal text arguments, rejects non-literal function-expression arguments, parses explicit two-ref calls plus `A..B` and merge-base-backed `A...B` ranges, returns no rows when parsed from/to refs are equal, filters requested tables case-insensitively with `to` names before `from` names, returns no rows for known unchanged tables, reports missing tables, and renders binary/varbinary/vector patch values as `0x...`. Prior patch-row behavior still emits schema-before-data patch rows, restarts statement_order for schema/data filters, maps focused CREATE/DROP/ALTER DDL, suppresses data after DROP TABLE, and reuses keyed/keyless SQL row statements. Required root `php tools/run-tests.php` passed with 159 test files, 14,515 assertions, and 0 failures after this status stamp. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* rows including keyless multiset duplicate/cardinality rows, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), CLI summary/stat rendering, row-level SQL/tabular keyed and keyless rendering, binary/varbinary/vector SQL hex literal rendering, diff modes, native dolt_patch() schema/data patch rows with focused DDL and keyed/keyless SQL statements plus function-call argument parsing, same-ref no-op rows, and table lookup, dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF, dolt_log/dolt_commits, CLI log rendering, dolt_commit_ancestors, has_ancestor, dolt_branches, dolt_remote_branches, active_branch, and dolt_branch_activity projection rows.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. Broad Go package batches should use runner-local TMPDIR and `go test -p 1` to avoid linker temp/quota failures. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. branch-activity.bats was skipped because it starts a Dolt SQL server; local in-process TestBranchActivity is the bounded branch-activity evidence.",
    "latestCommit": "27eb349 (Add Dolt binary patch SQL slice)",
    "nextTask": "Map `dolt_patch()` authorization boundaries and broader revision/table edge cases such as branch/tag resolution failures, staged/worktree materialization beyond same-ref no-op rows, and permission checks."
}
