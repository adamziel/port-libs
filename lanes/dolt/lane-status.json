{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 19,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; bounded upstream Go diff/table/schema/dtables/dtablefunctions/sqlfmt/expreval/enginetest tests reran and pass. Fresh commit-ancestor evidence passed focused `TestHistorySystemTable` and `TestHistorySystemTablePrepared` subtests for sorting `dolt_commit_ancestors` and commit_hash-filtered merge parent rows. Prior BATS evidence includes a 12-file local diff/schema/merge/conflict/sql-commit-diff slice with 253 planned tests, 238 runnable passes, and 15 upstream skips plus a patched-copy status/sql-status/log slice with 68 planned tests, 67 runnable passes, and 1 upstream skip. Full Go/BATS runners not executed.",
    "phpPass": 74,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, wp_posts to wp_content_posts table rename summary, wp_plugin_events schema-drift projection, skinny post-review diffs, filtered publish-impacting review rows, aggregate wp_posts diff-stat counts, DOLT_COMMIT_DIFF-style wp_posts review rows, commit log review rows with branch refs and merge parents, dolt_commit_ancestors parent rows that link reviewed import merge commits back to parent log messages, ignore-aware summaries/conflicts, primary-key-change warnings, status/merge review queues, schema history, and procedure history.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors upstream runner + schema-aware, skinny, where/limit, summary/stat primary-key-warning, dolt_ignore summary-filter/conflict, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, dolt_schemas history/diff, dolt_procedures history/diff, DOLT_COMMIT_DIFF, dolt_log, dolt_commits, and dolt_commit_ancestors projection slice",
    "audit": "audited 2026-05-22: 74 Dolt PHP behavior tests pass with 323 assertions across 8 Dolt test files. Fresh runner evidence for this slice includes focused `TestHistorySystemTable/(can sort by dolt_log.commit|dolt_commit_ancestors table with commit_hash filter ignored for max1row optimization)` and prepared variants, passing in 0.180s. Existing bounded evidence also includes cache-local dolt/noms/remotesrv, dolt version 2.0.5, focused status/conflict Go tests, commit-diff/log Go tests, and patched-copy BATS status/log evidence. Root `php tools/run-tests.php` passed after the finished Dolt batch and current concurrent-lane updates with 108 test files, 7,782 assertions, and 0 failures. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* row projection, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF commit snapshot projection, dolt_log/dolt_commits commit metadata projection, and native dolt_commit_ancestors rows with root null parents, merge parent indexes, commit_hash filtering, and WordPress import-merge parent message fixtures.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. Dolt lane PHP and the latest root PHP harness rerun are green.",
    "latestCommit": "HEAD (Update Dolt final aggregate evidence)",
    "nextTask": "Port a narrow native `has_ancestor()`/branch ancestry predicate slice using the same commit graph model, or broaden commit-ancestor filters."
}
