{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 20,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; bounded upstream Go diff/table/schema/dtables/dtablefunctions/sqlfmt/expreval/enginetest tests reran and pass. Fresh consolidated runner evidence passed the 16-package Go batch, focused diff/schema/system enginetest coverage, schema/procedure/history integration coverage, commit-diff/log/status/conflict engine coverage, dolt_status_ignored, dolt_log dtablefunction tests, focused `TestHistorySystemTable`/prepared commit-ancestor subtests, focused ancestor-spec parser tests, and the focused `TestDoltScripts/test has_ancestor` engine script. Fresh BATS evidence passed a 15-file local diff/schema/merge/conflict/sql-commit-diff/log/status/sql-status slice with 319 planned tests, 303 runnable passes, and 16 upstream skips using runner-local `status-local-fixed.bats`. Full Go/BATS runners not executed.",
    "phpPass": 78,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, wp_posts to wp_content_posts table rename summary, wp_plugin_events schema-drift projection, skinny post-review diffs, filtered publish-impacting review rows, aggregate wp_posts diff-stat counts, DOLT_COMMIT_DIFF-style wp_posts review rows, commit log review rows with branch refs and merge parents, dolt_commit_ancestors parent rows that link reviewed import merge commits back to parent log messages, has_ancestor branch/tag checks for reviewed import merges, ignore-aware summaries/conflicts, primary-key-change warnings, status/merge review queues, schema history, and procedure history.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors/has_ancestor upstream runner + schema-aware, skinny, where/limit, summary/stat primary-key-warning, dolt_ignore summary-filter/conflict, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, dolt_schemas history/diff, dolt_procedures history/diff, DOLT_COMMIT_DIFF, dolt_log, dolt_commits, dolt_commit_ancestors, and has_ancestor projection slice",
    "audit": "audited 2026-05-22: 78 Dolt PHP behavior tests pass with 380 assertions across 9 Dolt test files. Fresh runner evidence for this slice includes cache-local dolt/noms/remotesrv, dolt version 2.0.5, the 16-package Go batch, focused diff/schema/system enginetest coverage in 3.845s, schema/procedure/history integration coverage in 0.266s, commit-diff/log/status/conflict engine coverage in 1.444s, focused `TestHistorySystemTable/(can sort by dolt_log.commit|dolt_commit_ancestors table with commit_hash filter ignored for max1row optimization)` and prepared variants in 0.200s, ancestor-spec parser tests in 0.049s, focused `TestDoltScripts/test has_ancestor` in 0.354s, and a 319-plan patched-copy local BATS pass with 303 runnable passes and 16 upstream skips. The pristine upstream `status.bats` one-test repro still fails because `cut -c 13-44` truncates current `dolt log` hashes; the copied runner file changes only `get_head_commit()` to extract the full hash. Root `php tools/run-tests.php` passed after this batch with 110 test files, 8,021 assertions, and 0 failures. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* row projection, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF commit snapshot projection, dolt_log/dolt_commits commit metadata projection, native dolt_commit_ancestors rows with root null parents, merge parent indexes, commit_hash filtering, and native has_ancestor commit graph checks with branch/tag/HEAD/hash refs plus ^, ^N, and ~N suffixes.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. Dolt lane PHP and the latest root PHP harness rerun are green.",
    "latestCommit": "HEAD (Add native has_ancestor commit graph slice)",
    "nextTask": "Broaden branch history coverage with `dolt_branches` / branch activity table projections, or add revision-range filtering for native `dolt_log()`."
}
