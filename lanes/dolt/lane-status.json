{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 43,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; 172 focused upstream behavior mappings now recorded. Fresh inspected sparse inventory counted 1,420 Go test/benchmark functions in 377 checked-out Go test files and 3,630 hydrated sparse BATS @test cases in 203 BATS files, while the broader static denominator remains 399 Go _test.go files and 214 BATS files. Bounded upstream Go/BATS evidence remains green for the existing serialized Go/BATS slices. Fresh focused `Test(PatchTableFunction|PatchTableFunctionPrepared)$` passed in 0.724s, and a direct cache-local CLI probe confirmed `dolt_patch('tag_main', 'branch1', 't')` emits resolved commit hashes plus upstream-shaped missing branch/table errors. Full Go/BATS runners not executed.",
    "phpPass": 144,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, native `dolt diff -r sql` and tabular review queues, keyless wp_import_log duplicate/cardinality rows, native `dolt_patch()` schema/data patch queues for wp_posts column rename/addition plus keyless import-log SQL statements, binary wp_attachment_hashes media fingerprint patch statements, dot-range/three-dot patch-call review queues, resolved review branch/tag hash rows, WORKING patch targets, permission-gated revision-database patch review for limited wp_posts reviewers, known unchanged table empty results, missing-table/ref errors, multiline block diffs, table summary/stat review output, schema/procedure history, commit diff/log/ancestors/ancestry, branch review rows, ignore-aware summaries/conflicts, primary-key-change warnings, and status/merge review queues.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors/has_ancestor/branch/log-graph/log-number/diff-summary/diff-filter/row-sql/row-tabular/keyless-row-sql-tabular/diff-mode/diff-summary-table-args/diff-stat-table-args/diff-stat-json/diff-stat-keyless/dolt-patch/dolt-patch-authorization/dolt-patch-ref-resolution upstream evidence + native schema-aware, skinny, where/limit, summary/stat, dolt_ignore, CLI summary/stat, row SQL/tabular including binary hex literals, status, merge/conflict, schema/procedure history, commit diff/log/ancestors/ancestry, branch projection, patch-row, patch function-call, patch authorization, and patch ref-resolution slices",
    "audit": "audited 2026-05-23: 144 Dolt PHP behavior tests pass with 744 assertions across 16 Dolt test files. Required root `php tools/run-tests.php` passed with 162 test files, 14,874 assertions, and 0 failures after this status stamp. Current implementation slice maps `dolt_patch()` ref resolution boundaries against upstream source and probe evidence: `Test(PatchTableFunction|PatchTableFunctionPrepared)$` passed in 0.724s, and a direct cache-local Dolt CLI probe confirmed branch/tag refs render resolved commit hashes while missing refs/tables report upstream-shaped errors. Native PHP now resolves branch, tag, HEAD, and ancestor specs through a supplied commit graph, keeps WORKING and STAGED as materialized working-set labels, returns no rows when distinct refs resolve to the same commit hash, and reports missing branch/hash specs as `branch not found: ...` or `target commit not found`. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* rows including keyless multiset duplicate/cardinality rows, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), CLI summary/stat rendering, row-level SQL/tabular keyed and keyless rendering, binary/varbinary/vector SQL hex literal rendering, diff modes, native dolt_patch() schema/data patch rows with focused DDL and keyed/keyless SQL statements plus function-call argument parsing, branch/tag/HEAD/ancestor ref resolution through a supplied commit graph, WORKING/STAGED root labels, same-resolved-ref no-op rows, table lookup, and SELECT privilege checks, dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF, dolt_log/dolt_commits, CLI log rendering, dolt_commit_ancestors, has_ancestor, dolt_branches, dolt_remote_branches, active_branch, and dolt_branch_activity projection rows.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. Broad Go package batches should use runner-local TMPDIR and `go test -p 1` to avoid linker temp/quota failures. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. branch-activity.bats was skipped because it starts a Dolt SQL server; local in-process TestBranchActivity is the bounded branch-activity evidence.",
    "latestCommit": "pending (Dolt patch ref resolution slice)",
    "nextTask": "Map broader `dolt_patch()` staged/worktree materialization where HEAD, STAGED, and WORKING each carry distinct table snapshots, then compare those rows against focused upstream `WORKING and STAGED` assertions."
}
