{
    "schemaVersion": 1,
    "library": "dolt",
    "estimatedProgress": 22,
    "suiteProgress": "cloned static inventory: 613 upstream executable test files counted; bounded upstream Go diff/table/schema/dtables/dtablefunctions/sqlfmt/expreval/enginetest tests reran and pass. Fresh consolidated runner evidence passed the 16-package Go batch, focused diff/schema/system enginetest coverage, schema/procedure/history integration coverage, commit-diff/log/status/conflict engine coverage, dolt_status_ignored, focused dolt_log table-function engine tests in 0.623s, dolt_log dtablefunction tests in 0.043s, focused `TestHistorySystemTable`/prepared commit-ancestor subtests, focused ancestor-spec parser tests, the focused `TestDoltScripts/test has_ancestor` engine script, and focused `Test(DoltBranchesSystemTable|DoltBranchesSystemTablePrepared|BranchActivity)` branch table/activity engine tests in 10.400s. Fresh BATS evidence passed a 15-file local diff/schema/merge/conflict/sql-commit-diff/log/status/sql-status slice with 319 planned tests, 303 runnable passes, and 16 upstream skips using runner-local `status-local-fixed.bats`. Full Go/BATS runners not executed.",
    "phpPass": 87,
    "phpFail": 0,
    "wordpressScenarios": "Versioned WordPress content/data migrations and inspectable database change sets; current fixtures show wp_posts publish/remove/import row changes, wp_posts to wp_content_posts table rename summary, wp_plugin_events schema-drift projection, skinny post-review diffs, filtered publish-impacting review rows, aggregate wp_posts diff-stat counts, DOLT_COMMIT_DIFF-style wp_posts review rows, commit log review rows with branch refs and merge parents plus import-base and media-promotion revision ranges, dolt_commit_ancestors parent rows that link reviewed import merge commits back to parent log messages, has_ancestor branch/tag checks for reviewed import merges, branch-review rows with active/dirty migration branches and activity timestamps, ignore-aware summaries/conflicts, primary-key-change warnings, status/merge review queues, schema history, and procedure history.",
    "phase": "cloned static upstream inventory + bounded diff/schema/table/dtablefunctions/status/merge/schema-history/procedure-history/commit-diff/log/commit-ancestors/has_ancestor/branch upstream runner + schema-aware, skinny, where/limit, summary/stat primary-key-warning, dolt_ignore summary-filter/conflict, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, dolt_schemas history/diff, dolt_procedures history/diff, DOLT_COMMIT_DIFF, dolt_log revision-range filtering, dolt_commits, dolt_commit_ancestors, has_ancestor, dolt_branches, dolt_remote_branches, active_branch, and dolt_branch_activity projection slice",
    "audit": "audited 2026-05-22: 87 Dolt PHP behavior tests pass with 438 assertions across 10 Dolt test files. Fresh runner evidence for this slice includes focused upstream `Test(LogTableFunction|LogTableFunctionPrepared)` in 0.623s and focused upstream `TestDoltLog` dtablefunction tests in 0.043s, plus cache-local dolt/noms/remotesrv, dolt version 2.0.5, the 16-package Go batch, focused diff/schema/system enginetest coverage in 3.931s, schema/procedure/history integration coverage in 0.320s, commit-diff/log/status/conflict engine coverage in 1.377s, focused `TestHistorySystemTable/(can sort by dolt_log.commit|dolt_commit_ancestors table with commit_hash filter ignored for max1row optimization)` and prepared variants in 0.195s, ancestor-spec parser tests in 0.045s, focused `TestDoltScripts/test has_ancestor` in 0.395s, focused `Test(DoltBranchesSystemTable|DoltBranchesSystemTablePrepared|BranchActivity)` in 10.400s, and a 319-plan patched-copy local BATS pass with 303 runnable passes and 16 upstream skips. The pristine upstream `status.bats` one-test repro still fails because `cut -c 13-44` truncates current `dolt log` hashes; the copied runner file changes only `get_head_commit()` to extract the full hash. Required root `php tools/run-tests.php` passed with 115 files, 8,634 assertions, and 0 failures. This is bounded runner evidence, not full upstream runner parity.",
    "currentWork": "Native table diff classification, Dolt-style DOLT_DIFF_* row projection, schema/tag diffs, primary-key diffability, table delta matching/filtering, schema-aware target row projection, skinny diffs, projected where/limit filtering, dolt_diff_summary()/dolt_diff_stat(), dolt_ignore filtering/conflicts, dolt_status/dolt_status_ignored, dolt_merge_status, dolt_conflicts, schema/procedure history and diff rows, DOLT_COMMIT_DIFF commit snapshot projection, dolt_log/dolt_commits commit metadata projection with revisionSpecs/notRevisionSpecs for caret exclusions, two-dot ranges, three-dot merge-base ranges, multi-ref unions, and upstream-shaped invalid range boundaries, native dolt_commit_ancestors rows with root null parents, merge parent indexes, commit_hash filtering, native has_ancestor commit graph checks with branch/tag/HEAD/hash refs plus ^, ^N, and ~N suffixes, and native dolt_branches/dolt_remote_branches/active_branch/dolt_branch_activity branch projection rows.",
    "blocker": "No full upstream runner parity: full go test ./... and the full upstream BATS directory still require hydrating/compiling the full Dolt workspace and running Python, parquet, Hadoop, server, compatibility, client integration, and benchmark-style dependencies beyond this modest lane slice. The pristine upstream status.bats helper still truncates commit hashes with fixed-width cut over current dolt log output; runner-local status-local-fixed.bats documents a one-helper patched copy where full status plus sql-status passes. Dolt lane PHP is green and the latest root PHP harness rerun is green after transient unrelated esbuild failures cleared.",
    "latestCommit": "cda8a77 (contains Dolt log revision-range slice; shared commit also includes readability lane changes)",
    "nextTask": "Add native dolt_log --tables filtering over per-commit changed table metadata."
}
