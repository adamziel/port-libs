# quadrable WordPress Scenario

Authenticated local-first state sync for Playground snapshots and content databases.

## Current Native Slice

Pure-PHP BLAKE2s-256 hash/key primitives plus deterministic upstream-shaped `quadb mineHash <prefix>` bit-prefix mining, an in-memory sparse tree for get/put/delete, update batching, path-independent roots, `getMulti`, upstream-shaped raw-key `getMultiRaw`, `FullKeys` proof transport with tracked string-key enumeration, direct upstream `big proof test` 1000-query proof/import/getMulti behavior, an exact upstream C++ `big proof test` transport-byte digest fixture, empty-head restoration, delete bubbling equivalence, ordered raw integer keys for WordPress option/post records, PHP-native large integer key overflow rejection, composite integer/hash keys for post-meta style rows, an exact upstream-digest snapshot root, iterator windows for incremental sync chunks, compact authenticated range proofs for partial snapshot verification, delegated sub-range proofs exported from already imported partial range proofs, shared empty witness proofs for multiple absent keys in the same empty subtree, proof-backed partial-tree updates for narrow content changes, merged partial proofs for independently requested records, proof-backed partial `quadb export` and `export --int` lines with upstream-shaped witness placeholders, retained delegated proof-import nodes after `mergeProof` and upstream-shaped `quadb gc` pruning, delegated sub-proofs exported from already imported partial proofs, bounded sync proof fragments with diff reconstruction, upstream-compatible sync fragment path-order/same-path guards, scan-time diff callbacks that match the final authenticated diff, imported proof-fragment shadow leaf node ids that match final sync diffs, exposed memStore-range sync shadow root node ids, upstream-MT19937-shaped sync-fuzz dimensions and byte budgets, full native upstream-shaped sync-fuzz convergence, full persisted tracked-node snapshot sync-fuzz convergence, cached full proof-tree reuse for repeated proof-fragment export, partial-shadow diffs that skip unchanged witness branches while reconstructing changed paths, tracked leaf node-id reuse for compact rebuilds of unchanged snapshot records, saved branch-head checkout for old/new snapshot forks, tracked node-store snapshots that reload named heads and branch node ids from JSON, tracked scan/final diffs that report identical leaf node ids for changed/deleted/added records, tracked diff application that reconstructs changed snapshots from final diffs, memStore-range detached overlays for volatile preview edits, named published heads that reject volatile MemStore writes until a preview fork detaches the head, native file-backed `quadb`-style current-head reloads for WordPress snapshot previews, upstream-shaped bare `quadb` docopt output and `quadb get` stdout/stderr/exit-code behavior for option-read guards, upstream-shaped `quadb put`/`del` missing-store, empty-stream write/overwrite/delete, and missing-delete no-op behavior for snapshot write guards, upstream-shaped `quadb import`/`export` missing-store, empty-store export, silent import, malformed-line rejection, and sparse-order tracked row output for snapshot round trips, upstream-shaped `quadb checkout`/`fork [--from]` missing-store, silent success, detached-empty, current-root fork, and missing-source-head behavior for preview-head guards, upstream-shaped `quadb import --int` missing-store and `std::stoi`-style integer input output for snapshot import guards, upstream-shaped invalid proof format, malformed proof hex, command-level `importProof --root` root input behavior, binary `importProof --dump` dump-before-root precedence, and binary `mergeProof` missing-store/malformed/full-head/success output for delegated-proof guards, upstream-shaped `quadb init` stdout/stderr and advertised `quadb length` no-op output for snapshot-store bootstrap checks, upstream-shaped `quadb root` command startup behavior for missing snapshot stores and empty precreated stores, upstream-shaped tracked string-key `quadb diff`/`patch` lines for applying preview edits to another WordPress snapshot head, file-backed composite integer+hash import/export/proof lines for delegated postmeta reads, file-backed `quadb exportProof --hex` FullKeys proof bytes for delegated preview reads, upstream-shaped `quadb exportProof --stdin` newline-delimited key lists for string and raw integer delegated proofs, binary proof-byte import and merge for default `quadb` proof transport, upstream-shaped `quadb --noTrackKeys` export/diff/dump output with unknown rendered keys, HashedKeys proofs that still authenticate no-track snapshots, FullKeys rejection when original keys are unavailable, empty raw `quadrable_key` bucket parity for noTrack full/proof heads, upstream-shaped proof dump text for delegated proof debugging, file-backed `quadb importProof --hex`/`mergeProof --hex` proof-backed heads that persist delegated partial reads across reopen, unauthenticated importProof root-warning output for callers that defer trust establishment, file-backed proof-head string-key writes/deletes/forks that replay across reopen and can export updated delegated proofs, ordered proof/update replay for merging updated-root proofs after persisted delegated proof-head edits, raw integer proof-head imports/updates/deletes for delegated WordPress snapshot rows, upstream-shaped `quadb stats` output for full and delegated proof heads, upstream-shaped full-head and proof-backed LMDB bucket snapshots for backup/debug inspection, raw LMDB entry key/value snapshots for backup manifests, binary/non-UTF-8 serialized option keys and values preserved through full and delegated proof-head reopen, byte-for-byte native raw cursor parity against C++ upstream LMDB oracles for binary tracked keys/values, detached proof-head state, noTrack key-bucket suppression, retained/pruned `mergeProof`/`quadb gc` proof buckets, and one mixed tracked/noTrack/full/proof/detached database after `mdb_dump`/`mdb_load`, native raw-entry-only restore for full tracked/noTrack heads plus named and detached proof-backed heads without the PHP portable state payload, exact raw cursor preservation before edits, proven-key writes on raw-restored delegated preview heads with updated FullKeys proof export after reopen, same-root/update-root `mergeProof` on named, detached, noTrack, and larger mixed raw-restored delegated heads while retaining GC-prunable proof-import projections and empty noTrack key buckets, numeric/date-shaped full and delegated preview head names preserved as LMDB string keys across reopen, independent delegated proof imports that allocate distinct projected LMDB node ids while proof-head forks share the imported root id, divergent delegated proof-head preview forks that share unchanged imported proof storage, upstream-shaped `quadb dumpTree` output for inspecting full and delegated proof-tree witnesses, upstream-shaped `quadb gc` sweeps that free discarded preview-head and retained proof-import nodes while preserving approved named or detached roots, and native portable dump/restore for mixed full, named proof-backed, detached proof-backed, and noTrackKeys preview stores with raw cursor-byte digest verification after restore.

## Fixture And Example

- `fixtures/wordpress-ordered-snapshot.json` stores ordered `wp_options`, `wp_posts`, and `wp_postmeta` records under raw integer keys.
- `examples/wordpress-iterator-window.php` loads the fixture and emits a three-record window beginning at key 2, modeling a bounded Playground snapshot sync page.
- `examples/wordpress-raw-multiget.php` batch-reads raw integer WordPress snapshot keys, including an absent record id, without rehashing already encoded sparse keys.
- `examples/wordpress-full-key-proof.php` exports a `FullKeys` proof for string-keyed WordPress option/post records, imports it, and enumerates only the authenticated original keys present in the partial tree.
- `examples/wordpress-large-proof-window.php` maps the upstream big-proof shape to a 1000-key WordPress post snapshot window, proving 500 existing post IDs and 500 missing external IDs while leaving an unproven next post unavailable.
- `examples/wordpress-proof-oracle-digest.php` verifies a 1000-key delegated WordPress post-id proof window against the exact upstream C++ `big proof test` proof-byte digest fixture.
- `examples/wordpress-quadb-minehash-prefix.php` maps upstream-shaped `quadb mineHash <prefix>` command output to deterministic fixture generation, mines decimal-string WordPress keys with a shared hash-bit prefix, checks invalid-prefix command failure, and exports a compact proof-depth stress proof for backup/sync tests.
- `examples/wordpress-integer-key-boundary.php` writes the largest PHP-native sparse integer key and shows that the next ID is rejected with the upstream-shaped `int range exceeded` guard.
- `examples/wordpress-composite-meta-key.php` uses `Key::fromIntegerAndHash` to group multiple hashed `wp_postmeta` names under a stable post-id prefix.
- `examples/wordpress-proof-range.php` exports and imports a compact range proof for keys 2 through 4, then reads the authenticated partial tree using only the trusted root and encoded proof bytes.
- `examples/wordpress-sub-range-proof.php` imports a wider range proof, exports a narrower delegated range proof from that partial tree, and verifies that a record outside the delegated range remains unavailable.
- `examples/wordpress-proof-update.php` exports a narrow proof for a single `wp_posts` record, applies an authenticated update to the imported partial tree, and verifies that the partial update root matches the full snapshot update root.
- `examples/wordpress-proof-merge.php` imports one proof for `siteurl`, merges a second proof for a post record with the same trusted root, and reads both records from the expanded authenticated partial tree.
- `examples/wordpress-sub-proof.php` imports a wider option/post proof, exports a narrower delegated sub-proof from that partial tree, and verifies that a record outside the delegated proof remains unavailable.
- `examples/wordpress-sync-diff.php` uses upstream-shaped sync request/response transport round trips to fetch bounded proof fragments from a changed snapshot, diff the authenticated shadow tree, and reconstruct updated/deleted/added WordPress records locally.
- `examples/wordpress-sync-scan-diff.php` streams scan-time diff callbacks while sync requests converge, then shows that those callbacks match the final authenticated WordPress option/post diff.
- `examples/wordpress-sync-node-id-parity.php` shows scan-time and final sync diffs carrying identical node ids, with changed/added records coming from imported memStore-range shadow leaves and deletes carrying local snapshot node ids.
- `examples/wordpress-sync-shadow-node.php` reports the memStore-range shadow root node id as proof fragments expand toward the authenticated remote snapshot root.
- `examples/wordpress-sync-request-guard.php` demonstrates rejecting overlapping same-path proof fragment requests from a malformed snapshot sync peer before any response proof is exported.
- `examples/wordpress-sync-fuzz-budget.php` runs deterministic upstream-shaped sync-fuzz trials and reports request/response budgets for a Playground snapshot preflight; a separate full 500-trial native probe now passes outside the fast suite.
- `examples/wordpress-persisted-sync-fuzz.php` runs deterministic upstream-shaped sync-fuzz trials while persisting and reloading named tracked heads, then reports restored head continuity, shared tracked nodes, request/response budgets, and authenticated roots for a Playground snapshot preflight. The full 500-trial persisted tracked-node probe now also passes outside the fast suite in 236.484s with the same 25,537 request and 22,591 response budget as the in-memory fuzzer.
- `examples/wordpress-sync-fuzz-watchdog.php` emits optional sync-fuzzer watchdog summaries and budget reports for both in-memory and persisted tracked Playground snapshots, including deterministic expected-root-digest checks, max diff/shadow-node pressure fields, persisted snapshot-byte/shared-node maxima, pass/fail status, and overrun details while keeping the fast suite on tiny deterministic trial counts.
- `examples/wordpress-node-id-reuse.php` rebuilds the ordered snapshot from reused leaf node ids, preserving unchanged record node ids while producing the same trusted root and a new branch head id.
- `examples/wordpress-snapshot-fork.php` saves the branch head id for an ordered snapshot, applies a post update on a fork, then checks out both old and new branch heads to read the authenticated record versions.
- `examples/wordpress-node-id-diff.php` compares a saved snapshot with a changed fork and reports matching scan-time and final diff leaf node ids for changed, deleted, and added records.
- `examples/wordpress-tracked-diff-reconstruct.php` applies final tracked diffs to reconstruct a changed WordPress snapshot and shows scan-time node ids matching the final diff node ids.
- `examples/wordpress-memstore-overlay.php` starts from an authenticated ordered snapshot, writes preview-only post changes into upstream's memStore node-id range, and leaves the base snapshot root unchanged.
- `examples/wordpress-named-memstore-fork.php` starts from a named published snapshot head, demonstrates the upstream memStore guard for direct volatile writes, then forks into a detached preview overlay that leaves the published head untouched.
- `examples/wordpress-persisted-node-store.php` exports a named tracked node store to JSON, reloads it, verifies the same published head/root/leaf node ids, then forks a memStore-range preview edit from the restored snapshot.
- `examples/wordpress-quadb-file-store.php` persists the ordered snapshot through native `QuadbStore` integer import lines, reopens the current `preview-snapshot` head from a file-backed directory, and verifies that checking out `master` restores the published post while hiding preview-only records.
- `examples/wordpress-quadb-cli-metadata.php` preflights upstream-shaped `quadb --help` and tagless `quadb --version` metadata for a WordPress snapshot CLI without opening or creating a store.
- `examples/wordpress-quadb-get-guard.php` checks upstream-shaped bare `quadb` docopt output and `quadb get` option-read output for missing stores, empty precreated stores, found options, and absent options so a WordPress snapshot tool can fail closed before trusting reads.
- `examples/wordpress-quadb-put-del-guard.php` checks upstream-shaped `quadb put` and `quadb del` output for missing stores, empty-stream writes, overwrites, missing-delete no-ops, deletes, and post-delete missing-key reads so a WordPress snapshot tool can fail closed before mutating records.
- `examples/wordpress-quadb-import-export.php` round trips WordPress option/post rows through upstream-shaped plain `quadb import`/`export` command wrappers, including missing-store fail-closed behavior, empty existing-store export bootstrap, sparse-order tracked row output, malformed import-line rejection, and empty-separator validation.
- `examples/wordpress-quadb-checkout-fork-guard.php` checks upstream-shaped `quadb checkout` and `quadb fork [--from]` output for missing stores, silent named checkout, silent named fork, detached empty checkout, and missing-source-head empty forks so a WordPress snapshot tool can safely manage preview heads without shelling out.
- `examples/wordpress-quadb-import-int-guard.php` checks upstream-shaped `quadb import --int` and `quadb export --int` output for missing stores, empty-store export bootstrap, accepted numeric-prefix keys such as `1x`, populated integer-key export output, nonnumeric `stoi` failures, negative `int range exceeded` failures, and out-of-32-bit-range `stoi` failures so a WordPress integer snapshot importer/exporter can fail closed before trusting snapshot rows.
- `examples/wordpress-quadb-int-read-write-guard.php` checks upstream-shaped `quadb get --int`, `put --int`, and `del --int` output for missing stores, `std::stoi`-style bad keys, accepted numeric-prefix keys such as `1x`, missing-delete no-ops, deletes, and post-delete missing-key reads so WordPress integer snapshot tools can fail closed around single-row mutations.
- `examples/wordpress-quadb-proof-input-guard.php` checks upstream-shaped `quadb exportProof` invalid format output plus direct and stdin dump-before-format precedence, `importProof --hex` malformed proof and root boundaries for odd hex, uppercase `0X`, empty proofs, invalid `--root` hex, short roots, uppercase accepted root hex, empty roots with no unauthenticated warning, missing stores, hex and binary `importProof --dump` precedence before invalid root parsing, trusted hex/binary delegated proof imports, and binary `mergeProof` expansion so a WordPress preview verifier can fail closed before trusting proof bytes or root arguments.
- `examples/wordpress-quadb-init-length.php` bootstraps a native file-backed WordPress snapshot store, records upstream-shaped first and repeated `quadb init` stdout/stderr, and confirms the advertised `quadb length` command-output wrapper returns no output before and after imported records.
- `examples/wordpress-quadb-root-command-guard.php` runs upstream-shaped `quadb root` command checks for WordPress snapshot stores, proving a missing store fails closed without creating a directory while a precreated empty store bootstraps and later reports the populated snapshot root.
- `examples/wordpress-quadb-inspection-command-guard.php` runs upstream-shaped `quadb status`, `head`, `stats`, `dumpTree`, `head rm`, and `gc` command-output checks for WordPress snapshot stores, proving missing stores fail closed while populated preview stores can be inspected and cleaned without shelling out.
- `examples/wordpress-quadb-head-cleanup.php` starts from a native file-backed preview workflow, removes a discarded preview head, runs upstream-shaped `quadb gc`, and verifies garbage nodes are collected while the approved preview root remains current.
- `examples/wordpress-quadb-patch.php` uses command-style `quadb diff <head>`/`quadb patch` wrappers with tracked string keys to fail closed on missing stores, generate upstream tree-walk ordered patch lines for a WordPress preview snapshot, apply them to a replica fork, and verify that the patched replica root matches the preview root.
- `examples/wordpress-quadb-proof-export.php` exports an upstream-shaped `0x` FullKeys proof from a file-backed WordPress snapshot head, imports it into a native partial tree, and verifies delegated option/post reads plus a proven-absent draft key against the trusted root.
- `examples/wordpress-quadb-proof-import-merge.php` imports a delegated FullKeys proof into an empty file-backed preview head, reopens that proof-backed head, merges a second proof, and verifies persistent authenticated option/post reads without the full snapshot.
- `examples/wordpress-quadb-partial-export.php` exports a delegated proof-backed WordPress head before and after `mergeProof`, showing proven option/post values alongside upstream-shaped `H(?)=0x...` placeholders for witness leaves in backup manifests.
- `examples/wordpress-quadb-proof-merge-gc.php` merges two delegated FullKeys proofs, shows retained proof-import LMDB-shaped nodes before `quadb gc`, sweeps those unreferenced nodes while preserving authenticated delegated option/post reads, and reports raw cursor node keys for backup manifests before and after pruning.
- `examples/wordpress-quadb-unauthenticated-proof-import.php` prints upstream-shaped proof dump text, imports a delegated FullKeys proof without a trusted root, and reports the unauthenticated root warning that a WordPress caller should verify through a trusted channel before serving preview data.
- `examples/wordpress-quadb-proof-stdin-binary.php` exports delegated FullKeys proofs through the upstream-shaped `quadb exportProof --stdin` command wrapper, imports and merges raw binary proof bytes like default `quadb importProof`/`mergeProof`, reports raw integer proof-byte size for WordPress snapshot row IDs, covers direct `exportProof --int` binary/hex/dump/numeric-prefix/whitespace/signed-zero parser evidence, and shows bad integer stdin plus negative direct integer keys failing closed, including the `-2147483648` range guard versus `-2147483649` `stoi` underflow split.
- `examples/wordpress-quadb-composite-meta.php` imports and exports file-backed composite integer+hash postmeta rows, then delegates a proof for one postmeta key plus a proven-absent key using upstream `Key::fromIntegerAndHash()` semantics.
- `examples/wordpress-quadb-lmdb-layout.php` exposes native file-backed full and delegated proof WordPress snapshot heads through upstream-shaped `quadrable_head`, `quadrable_nodesLeaf`, `quadrable_nodesInterior`, `quadrable_key`, and `quadrable_quadb_state` buckets so backup/migration tooling can inspect raw leaf, witness-leaf, witness, and branch node bytes without an LMDB extension; it also demonstrates that two independent delegated preview imports allocate separate projected node ids while a forked delegated proof head shares the first imported root id.
- `examples/wordpress-quadb-lmdb-raw-entries.php` emits a backup-manifest view of raw LMDB entry key/value bytes for full and delegated proof heads, including little-endian uint64 node-id keys for integer-key buckets, LMDB string cursor ordering for heads, and non-UTF-8 serialized option key/value bytes rendered as hex. `fixtures/upstream-lmdb-cursor-oracle.json` captures the same kind of WordPress-shaped binary tracked-key/proof-head scenario from upstream C++ LMDB, now including a detached proof head whose raw entries have no head table row and use `detachedHead` state plus noTrackKeys full/proof heads whose raw `quadrable_key` buckets stay empty, and the PHP suite compares native raw cursor bytes against it exactly.
- `examples/wordpress-quadb-raw-entry-import.php` restores a WordPress full-head preview store and the upstream mixed proof-backed dump/load oracle from raw LMDB cursor entries alone, without the PHP portable `state` payload, then verifies detached/named/full reads, raw-entry digests, a raw-restored delegated preview edit, an updated proof that authenticates the edited root, a post-edit `mergeProof` that adds another option read to the raw-restored delegated head, and a noTrack raw-restored merge/GC flow that keeps the raw key bucket empty.
- `fixtures/upstream-lmdb-raw-restored-merge-oracle.json` captures a focused upstream C++ `mdb_dump -a`/`mdb_load -a` restore of a named delegated WordPress proof head, followed by a preview `siteurl` write, a same-root `home` proof merge, and exact `quadb gc` raw cursor bytes.
- `fixtures/upstream-lmdb-sequential-raw-restored-merge-oracle.json` captures a named delegated WordPress proof head restored through upstream C++ `mdb_dump -a`/`mdb_load -a`, followed by a preview `siteurl` write, sequential same-root `home` and `wp_posts:1` proof merges, upstream `Collected 14/24 nodes` GC output, and exact after-GC cursor bytes.
- `examples/wordpress-quadb-sequential-raw-merge.php` runs the sequential raw-entry restore/merge/GC flow end to end and emits the two merge roots, delegated option/post reads, bucket counts before/after GC, and oracle-match booleans for a backup verifier.
- `fixtures/upstream-lmdb-detached-raw-restored-merge-oracle.json` captures the same upstream C++ dump/load, preview write, proof merge, and `quadb gc` cursor-byte sequence for a detached delegated proof head whose active root is stored through `quadrable_quadb_state.detachedHead`.
- `fixtures/upstream-lmdb-notrack-raw-restored-merge-oracle.json` captures the same upstream C++ dump/load, preview write, proof merge, and `quadb gc` cursor-byte sequence for a noTrack delegated proof head whose `quadrable_key` bucket stays empty before update, after merge, after GC, and after PHP reopen.
- `fixtures/upstream-lmdb-mixed-raw-restored-merge-oracle.json` captures a larger upstream C++ dump/load store with tracked full heads, noTrack full/proof heads, a named tracked proof head, and an active detached tracked proof head. PHP restores those raw cursor entries, applies a detached serialized-option preview edit, merges an updated-root post proof, reports upstream `Collected 38/85 nodes`, and preserves the surrounding named/full heads after GC.
- `fixtures/upstream-lmdb-mixed-named-raw-restored-merge-oracle.json` starts from the same upstream dump/load store, checks out the named `binary-proof` delegated preview head, applies the serialized-option edit, merges the updated-root post proof, prunes the now-orphaned detached proof nodes with upstream `Collected 49/85 nodes` cursor parity, and keeps the surrounding full/noTrack/proof heads readable after reopen.
- `examples/wordpress-quadb-mixed-raw-merge.php` runs that mixed raw-entry restore/merge/GC flow end to end and emits bucket counts plus detached, named delegated, noTrack delegated, and master reads for a WordPress backup verifier.
- `examples/wordpress-quadb-mixed-named-raw-merge.php` runs the named-head mixed raw-entry restore/merge/GC flow end to end and emits bucket counts plus named delegated, private delegated, and master reads for a WordPress backup verifier.
- `fixtures/upstream-lmdb-mixed-notrack-raw-restored-merge-oracle.json` starts from the same upstream dump/load store with current `private-proof`, applies a noTrack private option edit, merges the updated-root private post proof, preserves surrounding tracked/full/proof heads, keeps the private key out of `quadrable_key`, retains stale tracked-key rows left by upstream noTrack GC, and matches upstream `Collected 43/79 nodes` cursor parity.
- `examples/wordpress-quadb-mixed-notrack-raw-merge.php` runs the noTrack private-head mixed raw-entry restore/merge/GC flow end to end and emits privacy checks proving FullKeys private proof export is rejected and no private key string is added to the raw key bucket.
- `examples/wordpress-quadb-portable-dump-restore.php` exports and restores mixed WordPress preview stores with full heads, named delegated proof heads, a detached proof-backed head, and a noTrackKeys private proof store, then verifies the restored raw cursor bytes and raw-entry digests match the portable dumps. `fixtures/upstream-lmdb-dump-restore-oracle.json` captures a real upstream C++ mixed database after `mdb_dump -a` and `mdb_load -a`, and the PHP suite compares native raw cursor bytes and digest against the restored upstream entries.
- `examples/wordpress-quadb-numeric-heads.php` persists date/id-shaped WordPress preview heads such as `20260523` and delegated proof heads such as `404`, showing that raw LMDB head/state entries remain text keys across reopen.
- `examples/wordpress-quadb-notrackkeys.php` imports a WordPress snapshot with native `quadb --noTrackKeys` behavior, exports unknown rendered keys, authenticates a delegated read through a HashedKeys proof, shows FullKeys proof transport is unavailable without stored original key strings, and reports empty raw LMDB key buckets for both full and delegated proof heads.
- `examples/wordpress-quadb-proof-update.php` imports and forks a delegated FullKeys proof head, applies a persisted preview `siteurl` edit inside the authenticated partial tree, reopens the edited proof head, and exports an updated delegated proof for the new root.
- `examples/wordpress-quadb-proof-fork-sharing.php` forks a delegated WordPress proof head, edits the preview `siteurl`, and shows the unchanged `home` proof leaf stored once across base and preview heads while the edited `siteurl` has distinct base/preview leaves.
- `examples/wordpress-quadb-proof-merge-after-update.php` imports a delegated proof head, persists a preview `siteurl` edit, reopens the partial head, merges a second proof exported from the updated root, and verifies the merged post read persists.
- `examples/wordpress-quadb-integer-proof-update.php` imports a raw integer proof head, applies delegated WordPress preview edits with `importIntegerLines()`, reopens the partial head, and exports a delegated raw integer proof for the updated root.
- `examples/wordpress-quadb-stats.php` reports upstream-shaped `quadb stats` text for a full WordPress snapshot head and for a persisted delegated proof head with witness nodes.
- `examples/wordpress-quadb-dump-tree.php` reports upstream-shaped `quadb dumpTree` lines for a full WordPress snapshot head and a delegated proof head, making witness branches visible during preview debugging.

## Next Task

If accepted, decide whether to wire the optional 500-trial sync-fuzzer watchdog into a manual CI job using the summarized rootDigest, trialDigest, pressure, and budget-report fields, or leave it as lane-local smoke evidence. Dependency closure for the 2026-05-25 `watchdog-next` slice: no new support component is needed; the existing bounded `SyncFuzzer`, tracked-node-store persistence, and WordPress watchdog example components were reused. Focused evidence: `php tools/run-tests.php lanes/quadrable/tests/SyncTest.php` passed 1 file, 278 assertions, and 0 failures after adding `maxDiffs`, `maxScanDiffs`, and `maxShadowNodeId` budget enforcement; `php lanes/quadrable/examples/wordpress-sync-fuzz-watchdog.php 2 0` passed and emitted `ok: true` in-memory/persisted tracked reports with matching expected root/trial digests, pressure fields under enforced budgets, snapshot-byte/shared-node maxima, and empty failure lists. Root harness not run - isolated micro-slice.
