# Quadrable Upstream Inventory

Upstream checkout: `hoytech/quadrable` at `4f44437dc9b951a91986ad69e2856938387be614`.

## Cloned Source Inventory

The local upstream cache is a checkout, not a seed placeholder. A targeted `git ls-tree -r --name-only HEAD` inventory counts 55 tracked upstream paths:

- 3 C++ entrypoints: `check.cpp`, `quadb.cpp`, `syncBench.cpp`.
- 22 Quadrable public/internal headers under `include/quadrable*`.
- 20 documentation assets under `docs/`.
- 3 external submodule gitlinks: `external/docopt.cpp`, `external/hoytech-cpp`, `external/lmdbxx`.
- 7 root metadata or support files: `.gitignore`, `.gitmodules`, `LICENSE`, `Makefile`, `README.md`, `TODO`, and the aggregate `include/quadrable.h`.

Latest 2026-05-25 CRLF stdin FullKeys proof-command refill evidence: native PHP keeps the rebased `quadb exportProof --stdin --format=FullKeys` behavior additive on accepted base `8d18eb6576edf6c3fa99b76aa42ed252756ba8d0` by preserving carriage returns in string-key stdin lines, matching C++ `std::getline` rather than normalizing Windows CRLF input. Focused coverage proves `wp_options:siteurl\r\nwp_posts:1\r\nwp_posts:404\r\n` exports the same FullKeys proof as direct keys containing trailing `\r`, and a different proof from LF-only keys. `examples/wordpress-quadb-proof-stdin-binary.php` reports `crlfStringProofPreservesCarriageReturnKeys: true` and `crlfStringProofDiffersFromLf: true`. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level stdin line splitting, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 EOF-terminated stdin proof-command rework evidence: native PHP keeps the rebased `quadb exportProof --stdin` proof-command behavior additive on accepted base `03deaa9312057d14e792f7e280fe3c7bd43b0335` by covering the final stdin key at EOF without a trailing newline for both FullKeys string proofs and raw integer proofs. Focused coverage proves `wp_options:siteurl\nwp_posts:1\nwp_posts:404` exports the same FullKeys proof bytes as the newline-terminated input, and `2\n4\n99` exports the same raw integer proof bytes as `2\n4\n99\n`, preserving the upstream `std::getline` final-line behavior alongside the accepted whitespace/NUL/overflow parser boundaries. `examples/wordpress-quadb-proof-stdin-binary.php` reports `eofFinalKeyProofMatchesLf: true` and `integerEofProofMatchesLf: true`. Focused `QuadbStoreTest.php` passed 1 file, 1014 assertions, and 0 failures; syntax checks passed for changed PHP files; the WordPress proof stdin/binary smoke passed. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 stdin whitespace/NUL/overflow integer proof-command refill evidence: native PHP keeps the rebased `quadb exportProof --int --stdin` proof-command behavior additive on accepted base `740ee9588b97ef9780909873b03e0dc647b8bfd0` by covering stdin-only parser boundaries. Focused coverage proves whitespace-only stdin lines, NUL-prefixed stdin lines, and plus-prefixed positive overflow stdin lines fail through the shared C `std::stoi`-style parser with `quadb error: stoi`, alongside the already accepted empty-input versus explicit blank-line split. `examples/wordpress-quadb-proof-stdin-binary.php` reports `whitespaceOnlyStdinIntegerProofFailsStoi: true`, `stdinNulPrefixedIntegerProofFailsStoi: true`, and `stdinPlusOverflowIntegerProofFailsStoi: true`. Focused `QuadbStoreTest.php` passed 1 file, 1012 assertions, and 0 failures; syntax checks passed for changed PHP files; the WordPress proof stdin/binary smoke passed. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 stdin empty integer proof-command rework evidence: native PHP keeps the stale `quadb exportProof --int` proof-command rework additive on accepted base `b168fa0a8f5685c217839da8e2570a5487e582f9` by covering the newline-driven empty-input boundary separately from an explicit blank key line. Focused coverage proves empty stdin exports the same empty raw integer proof as the direct no-key path, while a single blank stdin line fails through the shared C `std::stoi`-style parser with `quadb error: stoi`. `examples/wordpress-quadb-proof-stdin-binary.php` reports `emptyStdinIntegerProofMatchesDirectEmpty: true` and `blankLineStdinIntegerProofFailsStoi: true` alongside the existing direct and stdin integer proof command fields. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 stdin sign-only integer proof-command rework evidence: native PHP keeps the stale `quadb exportProof --int` proof-command rework additive on accepted base `867f012ad03437688c83813c4dc7520bc232b698` by extending the same C `std::stoi`-style sign-only rejection to newline-driven `quadb exportProof --int --stdin`. Focused coverage proves stdin lines `+` and `-` fail with `quadb error: stoi` before proof export, matching the direct argument path. `examples/wordpress-quadb-proof-stdin-binary.php` reports `stdinSignOnlyIntegerProofFailsStoi: true` alongside existing direct and stdin integer proof command fields. Focused `QuadbStoreTest.php` passed 1 file, 1005 assertions, and 0 failures; syntax checks passed for changed PHP files; the WordPress proof stdin/binary smoke passed. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 direct integer proof-command sign-only rework evidence: native PHP keeps the stale direct `quadb exportProof --int` rework additive on accepted base `641e461db5f891513d13c9c43ec0c185b5749620` by covering the remaining sign-only `std::stoi` boundary. Focused coverage now proves direct integer proof keys `+` and `-` fail with `quadb error: stoi` before proof export. `examples/wordpress-quadb-proof-stdin-binary.php` reports `signOnlyIntegerProofFailsStoi: true` alongside the existing direct integer proof command smoke fields. Focused `QuadbStoreTest.php` passed 1 file, 1004 assertions, and 0 failures. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 stdin integer proof-command finisher evidence: native PHP keeps the accepted direct `quadb exportProof --int` parser parity and extends the newline-driven `quadb exportProof --int --stdin` command path through the same C `std::stoi`-style parser boundary. Focused coverage proves stdin lines `  +2suffix`, `-0suffix`, and `0000000004ignored` export the same raw integer proof as keys `[2, 0, 4]`, while `-2147483649suffix` fails with `quadb error: stoi`. `examples/wordpress-quadb-proof-stdin-binary.php` reports `stdinNumericPrefixIntegerProofMatchesDirect: true` and `stdinNegativeUnderflowIntegerProofFailsStoi: true` alongside existing delegated binary proof fields. Focused `QuadbStoreTest.php` passed 1 file, 1003 assertions, and 0 failures; syntax checks passed for changed PHP files; root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 direct integer proof-command underflow rework evidence: native PHP keeps the stale direct `quadb exportProof --int` rework additive on accepted base `888ca10fd920c524eef5f5fbb396a7bca680e69e` by covering the remaining negative integer parser boundary. Focused coverage now distinguishes `-2147483648`, which parses through `std::stoi` and then fails Quadrable's non-negative raw-key guard with `quadb error: int range exceeded`, from `-2147483649suffix`, which fails at the `std::stoi` range boundary with `quadb error: stoi`. `examples/wordpress-quadb-proof-stdin-binary.php` reports `negativeUnderflowIntegerProofFailsStoi: true` alongside the existing direct integer proof command smoke fields. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 direct integer proof-command rework evidence: native PHP now has focused direct `quadb exportProof --int` command-output coverage rebased on accepted base `db5bdd8e4250f8521401888aaf85f61ca875905f`, in addition to the existing stdin integer proof path. The regression proves raw binary output matches `exportIntegerProofBytes()`, direct and stdin `--hex` output both match the existing `0x...` text proof form, direct and stdin `--dump` return matching upstream-shaped proof dump text before output encoding and before bad format validation, bad direct integer input returns `quadb error: stoi`, numeric-prefix direct keys such as `2suffix` are accepted through the shared `std::stoi`-style parser, trailing whitespace and suffix text after the numeric token are ignored by the same parser, leading-space/plus-sign integer input matches the same parsed keys, leading-zero 2147483647 and signed-zero raw key 0 are accepted, out-of-32-bit positive and plus-prefixed overflow input returns `quadb error: stoi`, negative and signed-minimum input returns `quadb error: int range exceeded`, and `--format=FullKeys` fails for raw integer proofs because no original key string is available. `examples/wordpress-quadb-proof-stdin-binary.php` now reports `directIntegerExportProofExitCode: 0`, `directIntegerBinaryProofMatchesStdin: true`, `directIntegerHexProofMatchesStdin: true`, `directIntegerDumpIgnoresBadFormat: true`, `stdinIntegerDumpMatchesDirect: true`, `numericPrefixIntegerExportProofExitCode: 0`, `trailingWhitespaceIntegerProofMatchesNumericPrefix: true`, `spacedPlusIntegerExportProofExitCode: 0`, `spacedPlusIntegerProofMatchesNumericPrefix: true`, `maxIntegerExportProofExitCode: 0`, `signedZeroIntegerExportProofExitCode: 0`, `emptyIntegerExportProofExitCode: 0`, `emptyIntegerHexProofHasPrefix: true`, `plusOverflowIntegerProofFailsStoi: true`, `minSignedIntegerProofFailsRange: true`, and fail-closed direct negative-key proof output for WordPress integer-key snapshot proof export. No new upstream oracle fixture was needed; this reuses the existing static upstream `quadb.cpp` mapping, command-level CLI integer parser, native proof transport, and QuadbStore command wrappers. Focused `QuadbStoreTest.php` passed 1 file, 998 assertions, and 0 failures. Root harness not run - isolated micro-slice.

Latest 2026-05-25 integer read/write WordPress smoke evidence: native PHP already maps upstream-shaped `quadb get --int`, `put --int`, and `del --int` command output for missing stores, `std::stoi`-style bad keys, numeric-prefix acceptance, missing-delete no-op, deletes, and post-delete missing-key reads. This slice adds `examples/wordpress-quadb-int-read-write-guard.php` so the user-visible WordPress integer snapshot single-row path is covered by a local smoke. No new upstream oracle fixture was needed; this reuses the existing static upstream `quadb.cpp` mapping, CLI integer parser, command-output wrappers, and file-backed store components. Focused `QuadbStoreTest.php` passed 1 file, 921 assertions, and 0 failures; focused full-lane verification passed 10 files, 135 behavior tests, 3,110 assertions, and 0 failures; the new example smoke passed. Root harness not run - isolated micro-slice.

Latest 2026-05-25 plain import/export command evidence: native PHP now has focused command-output coverage for plain `quadb import`/`export` separator validation in addition to the existing missing-store, empty export, successful string-key row import/export, and malformed-line cases. The slice added `examples/wordpress-quadb-import-export.php` to exercise WordPress option/post string-key snapshot rows through the same command-output wrappers, including fail-closed missing stores and empty `--sep` errors. No new upstream oracle fixture was needed; this reuses the existing static upstream `quadb.cpp` mapping, file-backed command opener, separator parser, and store components. Focused `QuadbStoreTest.php` passed 1 file, 911 assertions, and 0 failures; focused full-lane verification passed 10 files, 135 behavior tests, 3,100 assertions, and 0 failures. Root harness not run - isolated micro-slice.

## Runner Status

The upstream test runner is `make test`, which applies AddressSanitizer flags, cleans build outputs, builds `check.cpp`, creates `testdb/`, and runs the resulting `./check` binary.

Initial 2026-05-22 evidence found missing runner dependencies. This lane then installed the directly required OS packages:

- `sudo -n dnf install -y gcc-c++ lmdb-devel libb2-devel libasan`
- `git submodule update --init --depth 1 external/hoytech-cpp external/lmdbxx external/docopt.cpp`

After that, `make -r test` passed all upstream scenarios on 2026-05-22, and was reconfirmed after the tracked fork/branch node-id slice, after the bounded tracked diff node-id slice, after the native memStore-range tracked-node slice, after the named-head MemStore fork guard slice, after the tracked diff reconstruction slice, after the imported sync shadow node-id slice, after the bounded upstream-MT19937 sync-fuzz slice, after the composite integer/hash key slice, after the PHP-native integer range guard slice, after the native upstream-shaped `SyncFuzzer` helper slice, after the persisted tracked node-store snapshot slice, after the cached full proof-tree/full native fuzzer slice, after the imported-partial sub-proof export slice, after the partial range sub-proof export slice, after the FullKeys proof transport/key-enumeration slice, after the shared empty witness proof slice, after the full persisted tracked-node fuzzer evidence run, after the direct upstream `big proof test` PHP mapping, after the file-backed `quadb exportProof --hex`/`--format=FullKeys`/`--int` PHP mapping, after the file-backed `quadb importProof --hex`/`mergeProof --hex` proof-head persistence mapping, after the file-backed proof-head write/delete/fork replay mapping, after the ordered proof/update event-history slice for merging updated-root proofs after persisted proof-head writes, after the raw integer proof-backed head update/import slice, after the upstream-shaped `quadb gc` full-head reachability sweep slice, after the upstream-shaped `quadb stats` output slice, after the upstream-shaped proof dump plus unauthenticated `quadb importProof` output slice, after the upstream-shaped `quadb dumpTree` output slice, after the upstream-shaped binary/stdin `quadb exportProof`/`importProof`/`mergeProof` slice, after the upstream-shaped `quadb --noTrackKeys` export/diff/dump/proof slice, after the file-backed composite integer+hash line/proof slice for upstream `Key::fromIntegerAndHash()`, after the proof-backed LMDB bucket layout projection slice, after the independent proof-import LMDB allocation-order slice, after the retained `mergeProof` import garbage plus `quadb gc` slice, after the divergent proof-backed fork update storage-sharing slice, again on 2026-05-23 after capturing the exact upstream `big proof test` transport-byte digest fixture, again on 2026-05-23 after the numeric-head LMDB string-key PHP mapping, again on 2026-05-23 after the binary LMDB value/tracked-key and raw cursor-ordering PHP mapping, again on 2026-05-23 after the upstream LMDB cursor oracle fixture mapping, again on 2026-05-23 after broadening that fixture to retained/pruned `mergeProof` plus `quadb gc` buckets, again on 2026-05-23 after extending that fixture to a detached proof-head raw cursor oracle, again on 2026-05-23 after adding noTrackKeys full/proof raw cursor oracle buckets, again on 2026-05-23 after adding portable dump/restore verification for mixed full, named proof-backed, detached proof-backed, and noTrackKeys PHP stores, again on 2026-05-23 after the focused named raw-restored `mergeProof`/`quadb gc` dump/load oracle slice, again on 2026-05-23 after the detached raw-restored `mergeProof`/`quadb gc` dump/load oracle slice, again on 2026-05-23 after the noTrack raw-restored `mergeProof`/`quadb gc` dump/load oracle slice, again on 2026-05-23 after the larger mixed tracked/noTrack/full/proof/detached raw-restored detached-head `mergeProof`/`quadb gc` dump/load oracle slice, again on 2026-05-23 after the mixed raw-restored named proof-head `mergeProof`/`quadb gc` oracle slice, again on 2026-05-23 after the mixed raw-restored noTrack active-current proof-head `mergeProof`/`quadb gc` oracle slice, again on 2026-05-23 after the sequential multi-merge raw-restored proof-head oracle slice, and again on 2026-05-23 after the upstream-shaped `quadb put`/`del` command-output slice. `-r` disables GNU Make built-in rules; plain `make test` now gets past the prior missing tooling, but this shell selects the built-in C++ compile rule and omits the repository `-Iinclude -Iexternal -Iexternal/hoytech-cpp -Iexternal/docopt.cpp` include flags, so it fails on `#include "quadrable.h"`. With built-ins disabled, the upstream Makefile's own pattern rule is used and the upstream test binary reports `All tests OK`.

Latest 2026-05-23 `mineHash` evidence: upstream `quadb.cpp` implements `mineHash <prefix>` by repeatedly hashing decimal candidate strings with `quadrable::Key::hash(std::to_string(r))`, checking the leading bits with `Key::getBit()`, and printing `<candidate> -> <hex hash>` without a `0x` prefix when a match is found. Native PHP maps this predicate deterministically for reproducible fixtures: `Key::mineHashPrefix('101010', 1, 200)` returns candidate `146`, hash `aba72397aa8d459aaf3190fd24625ca5cf09fe3127aa1fb40325eb13c57f1c89`, and upstream-shaped `QuadbStore::mineHashText()` output. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed.

Latest 2026-05-23 proof-backed partial export evidence: upstream `quadb.cpp` handles `export` by walking the current tree, printing tracked leaf keys when present, raw integer keys when `--int` is used and no tracked key is available, and `quadrable::renderUnknown()` placeholders for unknown key hashes or witness-leaf value hashes. Native PHP now maps that behavior for file-backed proof heads: `QuadbStore::exportLines()` and `exportIntegerLines()` emit proven partial-tree leaves plus upstream-shaped `H(?)=0x...` witness placeholders instead of rejecting proof-backed heads. `examples/wordpress-quadb-partial-export.php` demonstrates a delegated WordPress proof head exported before and after `mergeProof`. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed.

Latest 2026-05-23 init/length command evidence: upstream `quadb.cpp` prints `quadb: init'ing directory: <db>/` to stdout on first `init`, prints `quadb: Directory '<db>/' already init'ed. Doing nothing.` to stderr when the backing database already exists, and advertises `length` in the docopt usage without handling it in the command branch. A targeted upstream `quadb` run showed `length` exits successfully with empty stdout/stderr both before and after a write. Native PHP now maps this command boundary with `QuadbStore::initCommandOutput()` returning separate stdout/stderr strings and `QuadbStore::lengthText()` returning an empty string. `examples/wordpress-quadb-init-length.php` demonstrates WordPress snapshot-store bootstrap and confirms the no-op length output around imported records. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed.

Latest 2026-05-23 root command startup evidence: upstream `quadb.cpp` rejects non-init commands before opening LMDB when the database directory itself is missing, emitting `quadb error: Could not access directory '<db>/': No such file or directory` and exit code 1, but accepts an already existing empty directory and initializes the LMDB payload before executing `root`. Native PHP now maps that command boundary with `QuadbStore::openForCommand()` and `QuadbStore::rootCommandOutput()`, returning upstream-shaped stdout/stderr/exit-code triples while keeping the stricter library `open()` unchanged. `examples/wordpress-quadb-root-command-guard.php` demonstrates WordPress snapshot root checks that fail closed for missing stores and allow precreated empty stores to bootstrap. Targeted upstream probes were run against a locally built `quadb` binary; this shallow/cache checkout has no tags, so `git describe --tags` printed `fatal: No names found, cannot describe anything.` while compiling an empty `QUADRABLE_VERSION`. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed.

Latest 2026-05-23 help/version command evidence: upstream `quadb.cpp` delegates `--help` and `--version` to docopt before opening any database. Targeted upstream `./quadb --help` exited 0 with the usage block that starts with a leading newline and ends with a trailing blank line; `./quadb --version` exited 0 with `quadb ` in this tagless cache checkout because `QUADRABLE_VERSION` compiles as an empty string. Native PHP now maps those metadata-only command boundaries with `QuadbStore::usageText()`, `helpCommandOutput()`, and `versionCommandOutput()`. `examples/wordpress-quadb-cli-metadata.php` demonstrates a WordPress snapshot CLI preflight that can show metadata without opening or creating a store. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed.

Latest 2026-05-23 no-argument/get command evidence: upstream `docopt::docopt()` handles a bare `./quadb` before opening any database, exiting 255, printing the usage block to stdout with an extra leading newline, and printing `Arguments did not match expected patterns` to stderr without a trailing newline. Upstream `quadb get <key>` opens or creates the LMDB payload for an already existing store directory, prints a found value to stdout with a trailing newline, throws `key not found in db` for absent keys, and still fails before opening LMDB when the store directory itself is missing. Native PHP now maps those command boundaries with `QuadbStore::noArgumentCommandOutput()` and `getCommandOutput()`. `examples/wordpress-quadb-get-guard.php` demonstrates a WordPress option-read guard that distinguishes usage errors, missing stores, absent options, and found option values without shelling out. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed.

Latest 2026-05-23 put/del command evidence: targeted upstream `quadb put`/`quadb del` probes against a locally built upstream binary showed both commands fail before opening LMDB when the store directory is missing, emitting `quadb error: Could not access directory '<dir>/': No such file or directory` with exit code 1. For an existing empty directory, `put` initializes the backing store, writes without stdout/stderr, overwrites without stdout/stderr, and `del` returns success with empty stdout/stderr for both existing and missing keys; post-delete `get` reports `quadb error: key not found in db`. Native PHP now maps those command boundaries with `QuadbStore::putCommandOutput()` and `deleteCommandOutput()`. `examples/wordpress-quadb-put-del-guard.php` demonstrates a WordPress snapshot write guard that distinguishes missing stores, empty-stream writes, overwrites, missing-delete no-ops, and post-delete missing-key output without shelling out. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed.

Latest 2026-05-23 import --int command evidence: upstream `quadb.cpp` handles `import --int` by parsing the integer key with `std::stoi` before calling `Key::fromInteger()`. Targeted upstream probes against a locally built `quadb` binary showed a missing store fails before opening LMDB, `abc` and `2147483648` fail with `quadb error: stoi`, `-1` fails with `quadb error: int range exceeded`, and `1x` succeeds by importing integer key `1` because `std::stoi` accepts a leading numeric prefix. Native PHP now maps this command boundary with `QuadbStore::importIntegerCommandOutput()` and a CLI integer parser shared by `importIntegerLines()` and `exportIntegerProofBytesFromKeyLines()`. `examples/wordpress-quadb-import-int-guard.php` demonstrates a WordPress snapshot import guard that preserves those stdout/stderr/exit-code shapes without shelling out. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed.

Latest 2026-05-23 invalid proof command evidence: upstream `quadb.cpp` validates `exportProof --format` only when not dumping a proof, strips ASCII whitespace before `from_hex()` for `importProof --hex` and `mergeProof --hex`, strips only lowercase `0x`, pads odd-length hex with a leading zero, and surfaces malformed proof decode errors before mutating the store. Targeted upstream probes against a locally built `quadb` binary showed `exportProof --format=Bad` fails with `quadb error: unknown proof format`; empty input, `0x`, and `00` fail with `quadb error: proof ends prematurely`; odd `abc` becomes bytes `0a bc` and fails with `quadb error: unexpected proof encoding type: 10`; `zz` fails with `quadb error: unexpected character in from_hex: 122`; uppercase `0X00` fails with `quadb error: unexpected character in from_hex: 88`; `0001` decodes to an empty proof and fails with `quadb error: empty proof`; and truncated FullKeys varints fail with `quadb error: premature end of varint`. Native PHP now maps this command boundary with `QuadbStore::exportProofCommandOutput()`, `importProofHexCommandOutput()`, `mergeProofHexCommandOutput()`, and command-level `from_hex` decoding while keeping the stricter library `importProofHex()` API intact. `examples/wordpress-quadb-proof-input-guard.php` demonstrates a WordPress delegated-proof input guard with upstream-shaped missing-store, invalid-format, malformed-hex, empty-proof, and trusted import behavior. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed.

Latest 2026-05-25 isolated proof-command follow-up: native PHP now routes both direct `QuadbStore::exportProofCommandOutput()` and newline-driven `exportProofStdinCommandOutput()` through one command-format gate, preserving upstream dump-before-format precedence for debug proof dumps. Focused PHP evidence covers noTrack `FullKeys` command-output failure, successful noTrack hashed proof export, and stdin `--dump` still producing proof dump text when an otherwise invalid format is supplied. `examples/wordpress-quadb-proof-input-guard.php` now includes the stdin dump-precedence smoke field for delegated WordPress proof input guards. No upstream rerun was needed for this isolated slice; it stays within the command behavior already inventoried above.

Latest 2026-05-25 isolated hex proof-dump follow-up: native PHP now has focused regression evidence for `quadb importProof --hex --dump` precedence before `--root` validation. The test proves a valid hex proof with an invalid uppercase `0X` root still dumps `ITEMS (...)`, emits no stderr, and leaves the target head at the empty root. `examples/wordpress-quadb-proof-input-guard.php` now reports `hexDumpIgnoresInvalidRoot: true` alongside the existing binary dump-before-root and direct/stdin export dump-precedence guards. No upstream rerun was needed for this isolated slice; it reuses the existing command-level hex decoder, proof transport, and WordPress proof-input guard example.

Latest 2026-05-25 isolated direct integer proof-command rework: native PHP preserves the rebased direct `quadb exportProof --int` evidence on the current accepted base. Focused coverage now proves raw binary output, direct/stdin `--hex` parity, `--dump` precedence before invalid format validation, `std::stoi` invalid/range failures, numeric-prefix acceptance, leading-space/plus-sign acceptance, negative `int range exceeded`, and FullKeys failure when raw integer proofs cannot emit original key strings. `examples/wordpress-quadb-proof-stdin-binary.php` now reports the direct integer proof command fields alongside the existing stdin and binary delegated-proof smoke. No new support component is needed; this reuses the bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 isolated direct integer proof-command signed-zero follow-up: native PHP now keeps the rebased direct `quadb exportProof --int` slice additive on accepted HEAD by covering `std::stoi` signed-zero behavior for direct proof keys. Focused coverage proves `-0suffix` and `+0` parse to raw integer key `0` and emit the same binary proof bytes as `exportIntegerProofBytes([0])`; `examples/wordpress-quadb-proof-stdin-binary.php` reports `signedZeroIntegerExportProofExitCode` and signed-zero binary proof byte length for the WordPress integer snapshot proof smoke. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 isolated direct integer proof-command C-whitespace refresh: native PHP keeps the stale-patch rework additive on accepted HEAD by extending direct `quadb exportProof --int` command coverage to carriage-return-prefixed keys. Focused coverage proves `\r+2suffix` and `\r4` are parsed through the same `std::stoi`-style C whitespace gate as space, tab, newline, form-feed, and vertical-tab input, while NUL-prefixed keys still fail with `quadb error: stoi`. `examples/wordpress-quadb-proof-stdin-binary.php` reports `carriageReturnWhitespaceIntegerProofMatchesNumericPrefix: true` for the WordPress integer snapshot proof smoke. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 isolated direct integer proof-command empty-key follow-up: native PHP keeps the direct `quadb exportProof --int` rework additive on accepted HEAD by covering the no-key command path. Focused coverage proves direct integer proof export with an empty key list exits 0, emits the same binary bytes as `exportIntegerProofBytes([])`, and emits a `0x...` proof through direct `--hex`; `examples/wordpress-quadb-proof-stdin-binary.php` reports `emptyIntegerExportProofExitCode: 0` and `emptyIntegerHexProofHasPrefix: true` for the WordPress integer snapshot proof smoke. Focused `QuadbStoreTest.php` passed 1 file, 978 assertions, and 0 failures. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 isolated direct integer proof-command max-boundary follow-up: native PHP keeps the direct `quadb exportProof --int` rework additive on accepted HEAD `a301ac7cf7bb351327f549f663cf3f7d65d6f8b1` by covering accepted 32-bit maximum integer proof keys with leading-zero and plus-sign `std::stoi`-style command input. Focused coverage proves `0002147483647suffix` and `+0000000002` emit the same binary proof bytes as `exportIntegerProofBytes([2147483647, 2])`; `examples/wordpress-quadb-proof-stdin-binary.php` reports `maxIntegerExportProofExitCode: 0` and max-boundary binary proof byte length for the WordPress integer snapshot proof smoke. Focused `QuadbStoreTest.php` passed 1 file, 981 assertions, and 0 failures. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 isolated direct integer proof-command rebase refresh: the direct `quadb exportProof --int` rework has been regenerated on accepted HEAD `5690d6f1dc3c11c016e4940a385d1915cd527dd1` with one additive stdin dump parity check. The focused evidence remains the same direct/stdin parity cluster: raw binary output, direct/stdin `--hex`, empty-key binary and hex proof output, direct/stdin `--dump` before bad-format validation, numeric-prefix and leading-space/plus-sign `std::stoi` acceptance, signed-zero raw key `0`, accepted 32-bit maximum input, out-of-range `stoi`, negative `int range exceeded`, and FullKeys failure for raw integer proofs. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the existing bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 isolated direct integer proof-command whitespace refresh: native PHP keeps the rebased direct `quadb exportProof --int` slice additive on accepted HEAD `3c3c4d3aa423591a516e2926be9d6a7e146e73b6` by extending command regression evidence from spaces to tab/newline-prefixed direct integer arguments. Focused coverage proves `"\t+2suffix"` and `"\n4"` parse through the same `std::stoi`-style whitespace/numeric-prefix path and emit the same binary proof bytes as raw integer keys `[2, 4]`; `examples/wordpress-quadb-proof-stdin-binary.php` reports `verticalWhitespaceIntegerProofMatchesNumericPrefix: true` for WordPress integer snapshot proof export. No new support component is needed; this reuses the bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-25 isolated direct integer proof-command C-whitespace refresh: native PHP keeps the rebased direct `quadb exportProof --int` slice additive on accepted HEAD `7d2ccd908fe40ab900c442e8098cfe42003ead5b` by tightening the command integer parser to trim only C `std::stoi` whitespace instead of PHP's default NUL-trimming behavior. Focused coverage proves form-feed and vertical-tab-prefixed direct integer arguments parse through the accepted whitespace/numeric-prefix path, while a NUL-prefixed direct integer argument fails with `quadb error: stoi`. `examples/wordpress-quadb-proof-stdin-binary.php` reports `formFeedWhitespaceIntegerProofMatchesNumericPrefix: true` and `nulPrefixedIntegerProofFailsStoi: true` for WordPress integer snapshot proof export. Focused `QuadbStoreTest.php` passed 1 file, 990 assertions, and 0 failures. Root harness not run - isolated micro-slice. No new support component is needed; this reuses the bounded proof transport, command-level CLI integer parser, QuadbStore command wrappers, and WordPress proof stdin/binary example.

Latest 2026-05-23 `importProof --root` command evidence: upstream `quadb.cpp` decodes the proof first, then decodes `--root` with the same `from_hex()` helper used for proof hex. Targeted upstream probes against a locally built `quadb` binary showed lowercase `0x` is stripped, uppercase hex digits are accepted, uppercase `0X` is not stripped and fails with `quadb error: unexpected character in from_hex: 88`, `zz` fails with `unexpected character in from_hex: 122`, short and odd roots such as `0x00` and `abc` decode to non-empty non-32-byte values and then fail with `quadb error: proof invalid`, and empty roots (`--root=` or `--root=0x`) import successfully with empty stdout/stderr rather than printing the unauthenticated warning. Native PHP now maps that command boundary in `QuadbStore::importProofHexCommandOutput()` while leaving `QuadbStore::importProofHex()` strict for library callers. `examples/wordpress-quadb-proof-input-guard.php` now covers delegated WordPress proof root guards for uppercase prefixes, short roots, uppercase accepted roots, empty roots, and trusted roots. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed. Focused quadrable PHP tests pass with 10 files, 125 behavior tests, 2,956 assertions, and 0 failures. A final repo-wide root harness sample passed with 200 files, 22,964 assertions, and 0 failures.

Latest 2026-05-23 binary `importProof` command evidence: upstream binary `quadb importProof --root=0X<root>` against a valid FullKeys proof exits 1 with `quadb error: unexpected character in from_hex: 88`, while `quadb importProof --dump --root=0X<root>` exits 0, prints the proof dump beginning with `ITEMS (1):`, and does not parse the invalid root because `quadb.cpp` dumps immediately after decoding the proof. Native PHP now maps this with `QuadbStore::importProofCommandOutput()`, sharing the command-level root decoder used by the hex command while preserving dump-before-root precedence, trusted root imports, empty-root imports with no warning text, and unauthenticated imports with the upstream root warning. `examples/wordpress-quadb-proof-input-guard.php` now checks binary dump precedence and trusted binary proof import for delegated WordPress snapshot proofs. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed. Focused quadrable PHP tests pass with 10 files, 126 behavior tests, 2,976 assertions, and 0 failures. The required duplicate-root gate found active root harness PID 2656523 owned by `claude`, so no duplicate repo-wide root run was started for this slice.

Latest 2026-05-23 binary `mergeProof` command evidence: upstream binary `quadb mergeProof` opens the store before proof decoding, so a missing database directory fails with `quadb error: Could not access directory '<dir>/': No such file or directory`. Targeted upstream probes against a locally built `quadb` binary showed binary `not a proof` input fails with `quadb error: unexpected proof encoding type: 110`, a valid FullKeys proof against an empty full head fails with `quadb error: different roots, unable to merge proofs`, and merging a second FullKeys proof into an imported proof-backed head exits 0 with empty stdout/stderr and makes the delegated key readable. Native PHP now maps this with `QuadbStore::mergeProofCommandOutput()` while keeping `mergeProofBytes()` as the strict library API. `examples/wordpress-quadb-proof-input-guard.php` now checks binary merge expansion for a delegated WordPress `home` option proof after importing a trusted binary `siteurl` proof. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed. Focused quadrable PHP tests pass with 10 files, 127 behavior tests, 2,985 assertions, and 0 failures. A repo-wide root harness sample passed with 202 files, 23,334 assertions, and 0 failures after a clear duplicate-root gate.

Latest 2026-05-23 `checkout`/`fork` command evidence: upstream `quadb.cpp` mutates `quadrable_quadb_state` for `checkout [<head>]` and `fork [<head>] [--from=<from>]` without printing output on success. Targeted upstream probes against a locally built `quadb` binary showed missing stores fail before creating a directory, `checkout <head>` creates an empty current named head until the first write, bare `checkout` detaches to the empty root instead of copying the current head, `fork <head>` copies the current root into a named head, and `fork <head> --from=missing-head` creates a named empty head. Native PHP now maps this with `QuadbStore::checkoutCommandOutput()` and `QuadbStore::forkCommandOutput()`. `examples/wordpress-quadb-checkout-fork-guard.php` demonstrates WordPress preview-head guards for missing-store failure, silent named checkout, named fork, detached checkout, and missing-source-head import review forks. The upstream runner was reconfirmed with `make -r test` after this slice; all 34 scenarios passed. Focused quadrable PHP tests pass with 10 files, 128 behavior tests, 3,004 assertions, and 0 failures. A repo-wide root harness sample passed with 204 files, 23,577 assertions, and 0 failures after a clear duplicate-root gate.

The native file-backed `QuadbStore` slice used a precise static upstream mapping instead of shelling out to `quadb`: `quadb.cpp` first reloads `detachedHead`, then `currHead`, and otherwise checks out `master`; `checkout <head>` persists `currHead`, bare `checkout` persists a detached node id at command commit, `fork <head>` copies the current node id into a named head and makes it current, and `import --int` / `export --int` use separator-delimited integer-key lines. The latest integer-import command mapping covers `std::stoi` quirks for `quadb import --int`: missing separators throw `couldn't find separator in input line`, nonnumeric and out-of-32-bit-range keys throw `stoi`, negative parsed keys throw `int range exceeded`, and numeric-prefix keys such as `1x` import as integer key `1`. A later focused mapping covers tracked string-key `put`/`del`/`get`, plain `import`/`export`, `diff <head>` patch-line emission, and `patch` input: `diff` compares another head to the current head, emits `-key<sep>old` and `+key<sep>new` lines for changed leaves, keeps tracked string keys when available, falls back to upstream-shaped `H(?)=0x...` unknown-key rendering for untracked raw keys, and `patch` skips `#` comments while rejecting empty, malformed, or unexpected operation lines. The proof export mapping covers `exportProof --hex`, `--format=FullKeys`, `--int`, and `--stdin`: file-backed current heads are converted into native sparse proof trees without shelling out, newline-delimited stdin key lists map to string or raw integer proofs, encoded proof bytes are available in upstream's default raw binary form or `0x` hex text shape, tracked string keys support FullKeys enumeration, raw integer proof bytes authenticate `Key::fromInteger()` records, and FullKeys encoding still fails when raw keys have no original key string. The composite-key mapping follows upstream `Key::fromIntegerAndHash()`: file-backed composite import/export lines persist the integer prefix and 23-31 byte suffix metadata, direct get/put/delete helpers address the raw composite key, and delegated HashedKeys proof lines authenticate composite postmeta rows plus proven absences. The `--noTrackKeys` mapping follows `quadb.cpp` and `Quadrable::trackKeys`: string writes still address leaves by the original key hash, active no-track mode makes export/diff/dump render `H(?)=0x...`, suppresses newly written key strings even after reopening with tracking enabled, keeps HashedKeys proofs usable, rejects FullKeys encoding when no original key string is available, and now matches upstream raw LMDB cursors with empty `quadrable_key` buckets for full and proof-backed noTrack heads. The proof dump/import mapping follows `quadb.cpp` and `quadrable::dumpProof`: export/import dump paths print upstream-shaped `ITEMS` and `CMDS` sections without mutating the current head, binary and hex proof input both decode through the native transport, and importing without `--root` persists the partial proof head while emitting `Imported UNAUTHENTICATED proof. Root: 0x...`. The proof import mapping covers `importProof --hex --root=<root>`, binary `importProof`, `mergeProof --hex`, and binary `mergeProof`: proof-backed heads can only import into an empty current head, persist/reopen encoded proof fragments in the lane-local JSON state, expose upstream-shaped root/status/head output from the imported partial root, merge additional proofs only when roots match, export delegated proofs from the expanded partial tree, and now export proof-backed partial-tree records through `quadb export`-style line output with witness placeholders for unknown keys or values. Later focused mappings cover the README partial-tree update/fork promise for file-backed proof heads: string-key `put` and `del` operations on imported heads replay after persisted proofs, updated partial nodes allocate memStore-range node ids, proof-backed named heads can be forked without copying, updated partial heads can export new delegated proofs, reopened partial heads can merge additional proofs when they authenticate the current updated root, and raw `Key`/integer reads, writes, deletes, and `import --int`-style batches work against proof-backed heads. The `quadb gc` mapping follows `quadb.cpp`: it marks all persisted full heads plus the current full detached head, sweeps unreachable tracked leaf/branch node records, prints `Collected X/Y nodes`, deletes garbage records, and keeps surviving named/detached roots readable after reopen. The `quadb stats` mapping follows `quadrable::dumpStats`: it prints `numNodes`, `numLeafNodes`, `numBranchNodes`, `numWitnessNodes`, `maxDepth`, and `numBytes` in upstream field order; full heads count C++ raw node byte sizes (`72 + value bytes` for leaves and `48` for branches), and proof-backed heads also count imported witness and witness-leaf nodes. The `quadb dumpTree` mapping follows `quadrable::dumpDb`: it prints separator-delimited, abbreviated root/node-id lines for empty trees, full branch/leaf trees, raw-key leaves rendered as `H(?)=0x...`, and proof-backed partial trees with witness and witness-leaf lines. The full-head LMDB bucket mapping follows `Quadrable::init`, `BuiltNode`, `ParsedNode`, `leafKeys`, and `quadb.cpp` state handling: native PHP exposes `quadrable_head`, `quadrable_nodesLeaf`, `quadrable_nodesInterior`, `quadrable_key`, and `quadrable_quadb_state` buckets with raw leaf records (`NodeType::Leaf`, node hash, key hash, value), raw branch records (packed branch type/child word, node hash, right-child padding), tracked key records by leaf node id, and current/detached head state bytes. The raw-entry mapping follows upstream `dbi.put(txn, lmdb::to_sv<uint64_t>(nodeId), ...)` plus text-key `quadrable_head`/`quadrable_quadb_state` writes: native PHP now exposes cursor-style key/value byte pairs, with little-endian uint64 node-id keys for `MDB_INTEGERKEY` buckets and literal string keys for head/state buckets. The numeric-head mapping covers a PHP-specific boundary in that upstream LMDB treats head names such as `20260523` and `404` as string keys, while PHP arrays coerce those keys to integers; native snapshots now cast head keys back to text when reopening JSON state and when exporting raw LMDB entries for full and proof-backed heads. The binary persistence mapping covers upstream's raw `std::string` leaf values and tracked leaf keys: native JSON state now base64-encodes only non-UTF-8 string fields for full leaves, tracked keys, and proof-backed update events, keeps legacy JSON states readable, and preserves raw bytes in `lmdbRawEntrySnapshot()` while string-key buckets retain LMDB cursor ordering such as `10`, `2`, `a-preview`, `master`. The proof-backed LMDB bucket projection follows `BuiltNode::newWitnessLeaf`, `BuiltNode::newWitness`, `BuiltNode::newBranch`, `ParsedNode`, `leafKeys`, and upstream `writeNodeToDb` allocation order: persisted proof/update event histories are reconstructed into imported partial trees, independent proof imports receive separate projected LMDB leaf/interior ids in import order, forks of the same imported proof head share the imported root id, divergent proof-backed fork updates reuse unchanged imported proof nodes while allocating fresh nodes for edited leaves, and projected records expose regular LMDB-range raw `NodeType::Leaf`, `NodeType::WitnessLeaf`, `NodeType::Witness`, and branch bytes plus proof-head and `currHead`/`detachedHead` state bytes. The README command-line sections for heads, detached heads, `checkout`, `fork`, `import`, `export`, `diff`, `patch`, stats, proof export, proof import, proof merge, partial-tree updates, integer-key APIs, noTrackKeys, and garbage collection corroborate that command surface. Native PHP maps that subset with a JSON-backed directory state file rather than shelling out to `quadb`.

The explicit heavy native parity probe `SyncFuzzer::run(500, 0)` now passes after caching immutable full proof trees during sync-fragment export. The 2026-05-22 run completed 500 upstream-shaped trials in 167.914s, with max 9 round trips, 25,537 requests, and 22,591 responses. The probe is intentionally not part of the fast root suite. This lane now also maps a persisted tracked-node variant with `SyncFuzzer::runWithPersistedTrackedSnapshots()`: the fast PHP suite runs 2 upstream-shaped trials from seed 0, persists and reloads named local/remote tracked heads from JSON, verifies the restored head node ids and roots, checks unchanged shared node ids survive across the fork, and then runs the same sync request/response convergence checks. The full persisted probe now also passes outside the fast suite: `SyncFuzzer::runWithPersistedTrackedSnapshots(500, 0)` completed 500 trials in 236.484s, with max 9 round trips, 25,537 requests, 22,591 responses, max snapshot 273,206 bytes, first root `243e1afd0c3d6219af774e7a07c76642d53690837d29c27f59db4fe59a6321d8`, and last root `2da6576693c86c3fc00fa9e5ea833ab3da838fd0fe6b6ee48404461c0c94b9a8`. The latest watchdog summary fields add deterministic `rootDigest`, `maxDiffs`, `maxScanDiffs`, and `maxShadowNodeId` aggregates, and `watchdogReport()` can validate an expected root digest separately from numeric budget limits, so a manual 500-trial run can detect changed convergence roots without storing every trial row.

Latest 2026-05-23 upstream dump/load evidence: `notes/upstream-lmdb-dump-restore-fixture.cpp` creates one real C++ LMDB database containing tracked full heads, a noTrack full head, a named tracked proof head, a named noTrack proof head, and a detached tracked proof head. The source database was exported with `mdb_dump -a`, restored with `mdb_load -a`, and dumped again through the same raw cursor reader. Fedora LMDB 0.9.34 printed `db_pagesize` metadata warnings during load, but the restored raw cursor entries exactly match the source entries. The resulting fixture is `fixtures/upstream-lmdb-dump-restore-oracle.json`: 7 `quadrable_head` entries, 15 leaf-node entries, 55 interior-node entries, 10 tracked-key entries, 1 state entry, and raw-entry digest `808456bc2e5ab3be32cba5eec511755964e9c4fd8b9fda2bae878eca27ada8ea`.

Latest 2026-05-23 raw-restored merge evidence: `notes/upstream-lmdb-raw-restored-merge-fixture.cpp` creates upstream C++ proof-backed LMDB stores, restores them through `mdb_dump -a` and `mdb_load -a`, applies a delegated WordPress `siteurl` write, merges an updated-root proof for `home`, runs `quadb gc`, and dumps raw cursor bytes before the write, before GC, and after GC. Fedora LMDB 0.9.34 printed the same `db_pagesize` metadata warnings during load. The named fixture is `fixtures/upstream-lmdb-raw-restored-merge-oracle.json`: before GC it has 2 head entries, 6 leaf-node entries, 11 interior-node entries, 6 tracked-key entries, and 1 state entry; `quadb gc` reports `Collected 7/17 nodes`; after GC it has 2 head entries, 5 leaf-node entries, 5 interior-node entries, 5 tracked-key entries, and 1 state entry. The detached fixture is `fixtures/upstream-lmdb-detached-raw-restored-merge-oracle.json`: before GC it has 1 full-head entry plus detached state, 6 leaf-node entries, 11 interior-node entries, 6 tracked-key entries, and 1 state entry; `quadb gc` also reports `Collected 7/17 nodes`; after GC it has 1 full-head entry plus detached state, 5 leaf-node entries, 5 interior-node entries, 5 tracked-key entries, and 1 state entry. The noTrack fixture is `fixtures/upstream-lmdb-notrack-raw-restored-merge-oracle.json`: before GC it has 2 head entries, 6 leaf-node entries, 11 interior-node entries, 0 tracked-key entries, and 1 state entry; `quadb gc` reports `Collected 7/17 nodes`; after GC it has 2 head entries, 5 leaf-node entries, 5 interior-node entries, 0 tracked-key entries, and 1 state entry. The sequential fixture `fixtures/upstream-lmdb-sequential-raw-restored-merge-oracle.json` starts from the same named raw-restored proof-head shape, applies the `siteurl` write, merges same-updated-root `home` and `wp_posts:1` proofs in sequence, and then runs `quadb gc`: after the first merge it has 2 head entries, 6 leaf-node entries, 11 interior-node entries, 6 tracked-key entries, and 1 state entry; before GC after the second merge it has 2 head entries, 7 leaf-node entries, 17 interior-node entries, 7 tracked-key entries, and 1 state entry; `quadb gc` reports `Collected 14/24 nodes`; after GC it has 2 head entries, 6 leaf-node entries, 4 interior-node entries, 6 tracked-key entries, and 1 state entry. The mixed detached fixture `fixtures/upstream-lmdb-mixed-raw-restored-merge-oracle.json`, generated by `notes/upstream-lmdb-mixed-raw-restored-merge-fixture.cpp`, starts from one dump/load-restored database with tracked full heads, noTrack full and proof heads, a named tracked proof head, and an active detached tracked proof head. It applies a detached binary option write, merges an updated-root `wp_posts:1` proof, and runs `quadb gc`: before update it has 7 head entries, 15 leaf-node entries, 55 interior-node entries, 10 tracked-key entries, and 1 state entry; before GC it has 7 head entries, 17 leaf-node entries, 68 interior-node entries, 12 tracked-key entries, and 1 state entry; `quadb gc` reports `Collected 38/85 nodes`; after GC it has 7 head entries, 13 leaf-node entries, 34 interior-node entries, 9 tracked-key entries, and 1 state entry. The mixed named fixture `fixtures/upstream-lmdb-mixed-named-raw-restored-merge-oracle.json` starts from the same dump/load-restored database but checks out the named `binary-proof` delegated proof head before the update. It applies the same binary option write, merges the same updated-root `wp_posts:1` proof, leaves the former detached proof nodes orphaned for the sweep, and runs `quadb gc`: before update it has 7 head entries, 15 leaf-node entries, 55 interior-node entries, 10 tracked-key entries, and 1 current-head state entry; before GC it has 7 head entries, 17 leaf-node entries, 68 interior-node entries, 12 tracked-key entries, and 1 state entry; `quadb gc` reports `Collected 49/85 nodes`; after GC it has 7 head entries, 12 leaf-node entries, 24 interior-node entries, 8 tracked-key entries, and 1 state entry. The mixed noTrack fixture `fixtures/upstream-lmdb-mixed-notrack-raw-restored-merge-oracle.json` starts from the same dump/load-restored database with current `private-proof`. It applies a noTrack private option write, merges an updated-root private post proof, keeps the private key out of `quadrable_key`, leaves stale tracked-key rows that upstream noTrack GC does not delete, and runs `quadb gc`: before update it has 7 head entries, 15 leaf-node entries, 55 interior-node entries, 10 tracked-key entries, and 1 current-head state entry; before GC it has 7 head entries, 17 leaf-node entries, 62 interior-node entries, 10 tracked-key entries, and 1 state entry; `quadb gc` reports `Collected 43/79 nodes`; after GC it has 7 head entries, 11 leaf-node entries, 25 interior-node entries, 10 tracked-key entries, and 1 state entry.

Latest raw-entry import slice: native PHP now restores mixed full/proof-backed LMDB cursor dumps without the PHP portable `state` payload through `QuadbStore::restoreRawEntryDump()`. The importer decodes upstream raw `quadrable_head`, `quadrable_nodesLeaf`, `quadrable_nodesInterior`, `quadrable_key`, and `quadrable_quadb_state` entries, validates leaf, witness-leaf, witness, and branch hashes, verifies tracked-key hashes against leaf key hashes, classifies heads with witness descendants as proof-backed partial projections, preserves unreferenced raw proof-import nodes in that projection, supports current, detached, tracked, and noTrack proof-backed heads, and re-emits the same raw cursor entries after restore and reopen. Focused tests cover a native WordPress full/detached dump, a full-head-only slice extracted from the real upstream `mdb_dump`/`mdb_load` oracle, and the full mixed upstream oracle with tracked full heads, named proof-backed heads, noTrack full/proof heads, a detached proof-backed head, and raw-entry digest `808456bc2e5ab3be32cba5eec511755964e9c4fd8b9fda2bae878eca27ada8ea`. The latest tests write proven delegated keys on raw-restored proof heads, confirm the updated roots survive reopen, export FullKeys proofs only when key tracking is available, then merge updated-root proofs into named, detached, noTrack, mixed detached-current, mixed named-current, mixed noTrack-current, and sequential multi-merge raw-restored proof heads while preserving existing raw node ids and retaining imported proof projection nodes for `quadb gc`-style pruning; the focused noTrack case keeps `quadrable_key` empty, the mixed noTrack case preserves surrounding tracked key rows but suppresses the private key and retains stale tracked-key rows left by upstream noTrack GC, the sequential case reports upstream `Collected 14/24 nodes`, the mixed detached case reports upstream `Collected 38/85 nodes`, the mixed named case reports upstream `Collected 49/85 nodes`, and the mixed noTrack case reports upstream `Collected 43/79 nodes`. Raw-restored proof-backed heads can now read, inspect, re-export byte-for-byte before edits, accept proven-key writes, merge same-root/update-root proofs without the original proof event history, perform two sequential same-root proof merges, and match focused upstream named, detached, noTrack, sequential, mixed detached-current, mixed named-current, and mixed noTrack-current raw-restored merge/GC oracles byte-for-byte. The latest import --int command-output slice reconfirmed the upstream C++ runner with `make -r test`; all 34 scenarios passed. Current Quadrable-only PHP tests pass with 10 test files, 124 behavior tests, 2,914 assertions, and 0 failures. The duplicate-root gate found active root harness PID 2399793 owned by `claude`, so no duplicate repo-wide root run was started for this slice.

Latest 2026-05-23 status/head/stats/gc/dumpTree command evidence: upstream `quadb.cpp` opens the store before these inspection and cleanup commands, so missing database directories fail before LMDB creation with `quadb error: Could not access directory '<dir>/': No such file or directory`. Targeted upstream probes showed a precreated empty store bootstraps and returns `Head: master` status, empty `head` output, zero stats, `Collected 0/0 nodes`, and the empty-tree dump. Populated probes showed `status`, `head`, `stats`, and `dumpTree` print the current preview head, sorted heads, current tree stats, and tree rendering; `head rm` succeeds silently; and `gc` prints `Collected X/Y nodes`. Native PHP maps this with `QuadbStore::statusCommandOutput()`, `headCommandOutput()`, `headRemoveCommandOutput()`, `statsCommandOutput()`, `garbageCollectCommandOutput()`, and `dumpTreeCommandOutput()`. Focused lane tests now pass 10 test files, 129 behavior tests, 3,031 assertions, and 0 failures. `make -r test` reconfirmed all 34 upstream scenarios. The duplicate-root gate found active root harness PID 2895371 owned by `claude`, so no duplicate repo-wide root run was started for this slice.

Latest 2026-05-23 diff/patch command evidence: upstream `quadb.cpp` opens the store before `diff` and `patch`, so missing database directories fail before LMDB creation with `quadb error: Could not access directory '<dir>/': No such file or directory`. Targeted upstream probes showed `diff <head>` on a precreated empty store exits 0 with empty stdout/stderr, populated `diff master --sep='|'` emits tree-walk ordered patch lines from `Quadrable::diff()` (`-wp_posts:1`, `+wp_posts:1`, `-wp_options:home`, `+wp_posts:2` in the WordPress probe), successful `patch --sep='|'` exits 0 with empty stdout/stderr, comments are skipped, and malformed patch input returns upstream-shaped `empty line in patch`, `unexpected line in patch`, or `couldn't find separator in input line` errors without applying changes. Native PHP maps this with `QuadbStore::diffCommandOutput()`, `patchCommandOutput()`, and a tree-walk native `diffLines()` order that mirrors `include/quadrable/impl/diff.h` instead of sorting keys. Focused lane tests now pass 10 test files, 130 behavior tests, 3,046 assertions, and 0 failures. `make -r test` reconfirmed all 34 upstream scenarios. The duplicate-root gate was clear and the repo-wide root harness passed with 207 test files, 23,995 assertions, and 0 failures.

Static denominator from `check.cpp`:

- 34 top-level `test("...")` scenarios.
- 29 `equivHeads("...")` equivalence subcases.
- 136 `verify(...)` checks.
- 20 `verifyThrow(...)` error checks.
- 10 proof/range scenarios touch proof export/import/sizing.
- 5 scenarios touch iterator behavior (`back up start of iterator window`, `re-use leafs`, `integer proofs`, `iterators basic`, `iterators full`).
- 1 scenario covers sync fuzz.

Top-level scenarios:

1. basic put/get
2. zero-length keys
3. overwriting updates before apply
4. saves nodeId
5. integer round-trips
6. empty heads
7. batch insert
8. getMulti
9. del
10. del bubble
11. mix del and put
12. del non-existent
13. leaf splitting while deleting/updating split leaf
14. bunch of strings
15. large mixed update/del
16. back up start of iterator window
17. fork
18. re-use leafs
19. basic proof
20. use same empty node for multiple keys
21. more proofs
22. big proof test
23. sub-proof test
24. no unnecessary empty witnesses
25. update proof
26. integer proofs
27. proof sizing
28. iterators basic
29. iterators full
30. range proofs
31. memStore basic
32. memStore forking from lmdb
33. memStore-only env
34. sync fuzz

Current PHP mapping:

- `HashTreeTest.php` maps upstream BLAKE2s-256 key/value hash vectors, sparse empty root, leaf hash domain separation, branch hashing, and path bit ordering.
- `KeyTest.php` maps the `integer round-trips` scenario, PHP-native overflow rejection for upstream's `int range exceeded` boundary, most-significant-bit key addressing, prefix retention, deterministic `quadb mineHash <prefix>` hash-bit matching, integer-format rejection, and the upstream `Key::fromIntegerAndHash` 23-31 byte truncated hash overlay used for composite WordPress post-meta keys.
- `SparseTreeTest.php` maps focused in-memory get/put/delete/update semantics from `basic put/get`, `zero-length keys`, `empty heads`, `batch insert`, `getMulti`, upstream raw-key `getMultiRaw` for integer keys, overwrite-before-apply, delete bubbling, missing deletes, cached full proof-tree invalidation after later updates, raw integer WordPress option/post records, and an exact upstream-digest WordPress snapshot root.
- `IteratorTest.php` maps the externally visible lower-bound and upper-bound raw-key behavior from `iterators basic` plus representative `iterators full` sweeps.
- `ProofTest.php` maps `proofRoundtrip`-style compact transport encode/decode, `FullKeys` proof transport with tracked string keys, imported partial-tree enumeration of proven string keys, `basic proof`, upstream `big proof test` behavior over a 1000-key tree and -500..499 string-key getMulti window, exact upstream C++ `big proof test` proof transport bytes via `fixtures/upstream-big-proof-oracle.json`, `use same empty node for multiple keys`, `no unnecessary empty witnesses`, `integer proofs`/`proof sizing`, `range proofs`, and partial-tree range sub-proof slices. Imported proofs build a partial tree that validates the expected root, returns authenticated values or proven absences, reuses a shared empty witness for multiple absent keys in the same empty subtree, enumerates proven full-key leaves when `FullKeys` transport was used, exports narrower range proofs when enough witness material is present, and throws on unauthenticated witness subtrees.
- `ProofUpdateTest.php` maps focused `update proof` subcases: mutating proven leaves, rejecting updates through opaque witness branches, updating two proven leaves at different levels, splitting a proven leaf, upgrading and mutating witness leaves, splitting a witness leaf, deletion bubbling, the upstream `can't bubble a witness node` guard, and a WordPress raw-key post update from a narrow proof.
- `ProofMergeTest.php` maps focused `mergeProof` behavior from upstream proof/sub-proof semantics: separately imported proofs with the same root expand witness branches, witness leaves can be upgraded to full leaves, different roots are rejected, imported partial trees can export narrower sub-proofs like upstream `sub-proof test`, and merged/delegated WordPress option/post proofs can authenticate separate partial reads.
- `SyncTest.php` maps the transport and in-memory proof-fragment boundary from upstream `sync fuzz`: `SyncRequest` encode/decode, response proof length-prefix encode/decode, bounded root and later witness proof fragments, witness-leaf expansion, imported shadow roots, imported proof-fragment leaf node ids in the memStore range, upstream path-order/same-path fragment request rejection, matching-hash witness skips during diff, scan-time diff callbacks, WordPress scan/final diff equivalence, deterministic small randomized multi-round sync convergence with scan/final node-id parity, raw-key diff reconstruction, a pure-PHP `std::mt19937`-compatible seed-zero generator, the first two upstream-shaped sync-fuzz trials using upstream RNG dimensions and request/response byte budgets, a reusable native `SyncFuzzer` helper parameterized for the upstream 500-trial shape, a fast four-trial CI mapping of upstream RNG dimensions and budgeted request/response counts, persisted tracked-node snapshot reloads inside a two-trial upstream-shaped sync-fuzz loop, deterministic watchdog summary digests and pressure maxima, and path-projected diffs over partial shadow witness branches. Full native `SyncFuzzer::run(500, 0)` and `SyncFuzzer::runWithPersistedTrackedSnapshots(500, 0)` probes now pass outside the fast suite with matching request/response budgets.
- `SyncTest.php` also now exposes the upstream-shaped `nodeIdShadow` checkpoint for native PHP sync sessions: imported partial roots report memStore-range root node ids, every imported shadow node id stays in the memStore range, and expanding later proof fragments produces fresh shadow root node ids while preserving the authenticated remote root.
- `NodeIdTest.php` maps focused `saves nodeId`, `fork`, `re-use leafs`, `memStore basic`, `memStore forking from lmdb`, `memStore-only env`, and `Sync::diff` leaf node-id behavior for a native in-memory tracked tree: new leaf writes return non-zero node ids, duplicate unchanged puts return zero, changed puts return fresh ids, deletes return the removed leaf id, missing deletes return zero, saved branch head ids can be checked out to restore old and new trees, named heads persist head node ids, tracked node-store snapshots restore named heads and branch node ids across JSON persistence, copy-on-write updates reuse unchanged branch child ids after reload, reused leaves preserve their original leaf node ids across a fresh checkout, memStore-mode writes allocate from upstream's `firstMemStoreNodeId` range, detached overlays can add volatile records while the base tree remains unchanged, named heads reject memStore node persistence until an explicit fork detaches the head, changed/added diffs report the target leaf node id, deleted diffs report the source leaf node id, bounded scan callbacks match final diff node ids, tracked diffs can be applied to reconstruct changed randomized forks, and WordPress snapshot compact rebuild/fork/diff/reconstruct/memStore overlay/named-head/persisted-reload examples keep authenticated old and updated roots addressable by node id.
- `QuadbStoreTest.php` maps native no-argument docopt output, `quadb mineHash <prefix>` success and invalid-prefix stdout/stderr/exit-code behavior, advertised no-op `quadb length` command output for missing, empty, and populated stores, `quadb get` found/missing-key/missing-store stdout/stderr/exit-code behavior, `quadb put` and `quadb del` missing-store errors plus empty-stream success/overwrite/delete/missing-delete behavior, `quadb import`/`export` command output for missing stores, empty existing stores, sparse-order tracked string-key rows, malformed import lines, and empty separators, `quadb diff <head>` and `quadb patch` missing-store output, empty-store diff bootstrap, upstream tree-walk ordered patch lines, silent patch success, comment skipping, and malformed patch input errors, `quadb import --int` missing-store output plus `std::stoi`-style numeric-prefix, nonnumeric, negative, and out-of-range integer input behavior, `quadb init` stdout/stderr, `quadb root` missing-directory errors plus empty-existing-directory command bootstrap behavior, and a native `quadb` persistence subset for WordPress/Playground snapshots: a file-backed store initializes with the default `master` current head, reopens the current named head, keeps checkout-created heads empty until writes, persists named fork heads without copying unchanged leaf node ids, supports `fork --from`-style named copies, preserves detached fork state across reopen, round-trips `import --int` / `export --int` separator-delimited integer-key lines, tracks string keys for plain import/export, imports/exports/proves composite integer+hash lines backed by upstream `Key::fromIntegerAndHash()`, emits upstream-shaped `diff <head>` patch lines, applies `patch` lines back onto a fork, skips comment lines, rejects empty/malformed patch input, emits upstream-shaped `exportProof --hex` proof text for hashed, FullKeys, and raw integer keys, validates those proof bytes by decoding/importing them into native partial trees, maps `exportProof --stdin` newline-delimited string and raw integer key lists, consumes binary proof bytes for trusted `importProof` and same-root `mergeProof`, maps proof-backed partial `export`/`export --int` line output with `H(?)=0x...` witness placeholders, maps `--noTrackKeys` runtime behavior for export/diff/dump, HashedKeys proofs, FullKeys rejection, raw LMDB key-bucket suppression, and reopening after no-track writes, emits upstream-shaped `dumpProof` text for proof export/import debugging, imports FullKeys hex proofs with and without a trusted root, returns the upstream unauthenticated import warning/root line when no root is supplied, persists and reopens that proof-backed head, merges a second same-root proof after reopen, retains the second imported proof's unreferenced LMDB-shaped nodes until `quadb gc`, rejects different-root merges without mutating persisted state, delegates another proof from the merged partial head, persists proof-backed string-key writes and deletes across reopen, rejects stale original-root proof merges after a partial write, merges updated-root proofs after persisted proof-backed writes by replaying ordered proof/update events, imports and mutates raw integer proof-backed heads across reopen, forks proof-backed heads, exports delegated proofs from updated partial heads, shares unchanged imported proof storage across a divergent proof-backed fork update, formats upstream-shaped `quadb stats` output for full and proof-backed heads, exposes full-head LMDB bucket layout snapshots with raw leaf/interior node bytes and head/key/state records, exposes proof-backed LMDB bucket layout projections with raw `NodeType::Leaf`, `NodeType::WitnessLeaf`, `NodeType::Witness`, branch, head, key, and state records, exposes raw LMDB entry keys and values for integer-key and string-key buckets, preserves numeric-looking full and proof-backed head names as LMDB string keys across reopen, preserves non-UTF-8 tracked keys and binary leaf values across full/proof-backed JSON reopen while keeping raw LMDB cursor ordering, matches actual upstream C++ LMDB cursor oracle fixtures for binary tracked keys/values, string-sorted numeric heads, named and detached proof-backed delegated updates, noTrackKeys full/proof heads with empty `quadrable_key` buckets, retained/pruned mergeProof buckets, and a mixed database restored through `mdb_dump`/`mdb_load`, adds portable raw-entry digests to native dumps and validates them on restore, restores native portable dumps for mixed full, named proof-backed, detached proof-backed, and noTrackKeys stores while verifying raw LMDB cursor bytes and current head status, restores full-head raw LMDB cursor entries without portable state while preserving detached/current head reads, restores a full-head-only slice extracted from the upstream mixed dump/load oracle, restores the full mixed upstream raw-entry oracle including named and detached proof-backed projections without portable state, writes a proven binary delegated key on a raw-restored proof head and reopens/exports a FullKeys proof for the updated root, matches upstream named, detached, noTrack, sequential multi-merge, mixed detached-current, mixed named-current, and mixed noTrack-current raw-restored `mergeProof`/`quadb gc` cursor oracles byte-for-byte while preserving empty noTrack key buckets and named/full heads, distinguishes independent proof imports from forks in projected LMDB allocation order, emits upstream-shaped `quadb dumpTree` output for empty/full/raw-key/proof-backed heads, and garbage-collects unreachable full-head and retained proof-import node records while preserving named and current detached roots.

The native PHP hash primitive now uses a lane-local pure-PHP BLAKE2s-256 implementation matching upstream `Key::hash`, including multi-block values, and the key primitive now includes upstream-compatible integer, composite integer/hash, and deterministic `mineHash` behavior. Root/proof bytes for the mapped in-memory and proof surfaces are digest-compatible with upstream, including the exact C++ `big proof test` proof-byte oracle in `fixtures/upstream-big-proof-oracle.json`. The tracked/file-backed tree now covers node-id reuse, proof/range export/import, proof-backed updates and merges, sync fragments and upstream-shaped sync-fuzz helpers, `quadb` command output for no-arg/help/version/mineHash/root/get/put/del/import/export/import --int/init/length/checkout/fork/status/head/head rm/stats/gc/dumpTree/diff/patch, proof import/export/merge including command-level `exportProof --stdin` string and integer key input, `importProof --root` root decoding, binary `importProof --dump` precedence, and binary `mergeProof` command-output parity, noTrackKeys, LMDB bucket/raw-entry projection, raw-entry-only restore, portable dumps, and named/detached/noTrack raw-restored merge/GC cursor oracle parity. The latest `head rm` command-output slice exercises the native command wrapper for missing stores, current named-head reset, explicit named-head removal, detached-head reset, and follow-on `gc` cleanup while preserving upstream-shaped empty stdout/stderr success. `examples/wordpress-quadb-head-cleanup.php` now removes a discarded WordPress preview head through `QuadbStore::headRemoveCommandOutput()` and runs `garbageCollectCommandOutput()` as the smoke path. Focused QuadbStore verification passed 1 file, 935 assertions, and 0 failures; focused full-lane verification passed 10 files, 3,124 assertions, and 0 failures. Root harness not run for this isolated micro-slice. Dependency closure: no new support component is needed; this reuses the existing bounded file-backed `QuadbStore` command wrapper, head-state persistence, and tracked-node garbage collection components. Remaining native parity work is deciding whether the slow 500-trial sync-fuzzer probes belong in optional CI or identifying any remaining low-risk CLI boundary not already covered.
