{
    "schemaVersion": 1,
    "lane": "quadrable",
    "priority": 7,
    "upstream": {
        "name": "hoytech/quadrable",
        "url": "https://github.com/hoytech/quadrable",
        "commit": "4f44437dc9b951a91986ad69e2856938387be614",
        "license": "BSD-2-Clause",
        "architecture": "Header-only C++ sparse binary Merkle tree, compact proofs, sync, copy-on-write LMDB-backed store, MemStore, and quadb CLI."
    },
    "benchmarkDenominator": {
        "status": "cloned-upstream-runner-pass",
        "total": "55 tracked upstream paths; check.cpp has 34 top-level scenarios, 29 equivHeads subcases, 136 verify checks, and 20 verifyThrow checks; `make -r test` passes all 34 upstream scenarios after shallow submodule initialization",
        "mapped": 55,
        "source": "Local cloned upstream inventory of check.cpp, Makefile, include/quadrable headers, docs, and shallow-initialized submodules at commit 4f44437dc9b951a91986ad69e2856938387be614.",
        "runner": "make test",
        "runnerStatus": "passed 2026-05-22 with `make -r test` after `sudo -n dnf install -y gcc-c++ lmdb-devel libb2-devel libasan` and `git submodule update --init --depth 1 external/hoytech-cpp external/lmdbxx external/docopt.cpp`; reconfirmed 2026-05-22 after the tracked fork/branch node-id slice, bounded tracked diff node-id slice, native memStore-range tracked-node slice, named-head memStore fork guard slice, tracked diff reconstruction slice, imported sync shadow node-id slice, bounded upstream-MT19937 sync-fuzz slice, composite integer/hash key slice, sync shadow root node-id slice, sync request guard slice, PHP-native integer range guard slice, native upstream-shaped SyncFuzzer helper slice, persisted tracked node-store snapshot slice, cached full proof-tree slice, full native `SyncFuzzer::run(500, 0)` parity probe, persisted tracked-node sync-fuzz fast slice, imported-partial sub-proof export slice, partial range sub-proof export slice, raw-key `getMultiRaw` slice, FullKeys proof transport/key-enumeration slice, shared empty witness proof slice, and full native `SyncFuzzer::runWithPersistedTrackedSnapshots(500, 0)` parity probe. Plain `make test` now gets past missing tooling but this GNU Make environment selects the built-in C++ rule without the repo include flags, so `-r` was used to disable built-in rules while running the upstream test target",
        "inventoryPath": "lanes/quadrable/notes/upstream-inventory.md",
        "warning": "The upstream C++ runner passes locally, but native PHP parity is still partial. Native PHP tests map upstream-compatible BLAKE2s-256 hashing/key behavior, PHP-native integer key overflow rejection matching upstream's `int range exceeded` boundary, focused in-memory sparse tree update/get/delete scenarios, upstream raw-key `getMultiRaw` batch lookups for integer keys, FullKeys proof transport with tracked string-key enumeration from imported partial trees, iterator lower/upper-bound windows, composite integer/hash keys from `Key::fromIntegerAndHash`, compact proof transport encoding/decoding, selected proof import/export, shared empty witness proof reuse for multiple absent keys, integer proof sizing, range proofs, range sub-proof export from imported partial trees, proof-backed updates on imported partial trees, mergeProof expansion of separately imported proofs, sub-proof export from imported partial trees matching upstream `sub-proof test`, bounded sync proof-fragment transport/import with raw-key diff reconstruction, upstream-shaped sync fragment path-order/same-path guards, upstream-shaped scan-time diff callbacks, imported proof-fragment shadow leaf node ids that match scan-time and final sync diffs, exposed sync shadow root node ids in the memStore range, deterministic small sync-fuzz convergence, a pure-PHP `std::mt19937`-compatible RNG with the upstream seed-zero vector, full native `SyncFuzzer::run(500, 0)` convergence using upstream RNG dimensions and request/response byte budgets, full native `SyncFuzzer::runWithPersistedTrackedSnapshots(500, 0)` convergence using the same request/response byte budgets plus persisted named-head JSON reloads, cached full proof-tree reuse with update invalidation, path-projected diffs over partial shadow witness branches, leaf node-id output/reuse semantics from `saves nodeId` and `re-use leafs`, saved branch-head checkout from `fork`, persisted tracked node-store snapshots that restore named heads and branch node ids across JSON persistence, unchanged-branch copy-on-write node id reuse after reload, tracked leaf diff node ids matching upstream `Sync::diff` Added/Deleted/Changed callbacks, bounded scan/final diff node-id equality, tracked diff application/reconstruction across randomized forks, native memStore-range tracked node allocation for volatile detached overlays, the named-head memStore fork guard from `memStore forking from lmdb`, delegated WordPress sub-proofs and range sub-proofs, and an exact WordPress snapshot root under the upstream digest. Remaining parity gaps include broader full-scale proof query coverage such as upstream `big proof test`, native LMDB persistence, and quadb CLI behavior."
    },
    "nativeImplementation": {
        "language": "PHP",
        "shellOutsAllowedForProgress": false,
        "currentSlice": "Pure-PHP BLAKE2s-256 hash/key primitives plus PHP-native integer overflow guards, in-memory sparse tree get/put/delete, update batching, path-independent roots, getMulti, raw-key getMultiRaw, string-key tracking for upstream FullKeys proof transport and partial-tree enumeration, empty-head restoration, delete bubbling equivalence, cached full proof-tree reuse with update invalidation, raw-key iterator windows, composite integer/hash keys from `Key::fromIntegerAndHash`, compact proof transport encoding/decoding, selected proof import/export, shared empty witness proof reuse for multiple absent keys, proof-size checks, raw-key range proofs, range sub-proof export from imported partial trees, proof-backed updates against imported partial trees, mergeProof expansion between separately imported proofs, sub-proof export from imported partial trees, sync request/response transport, bounded sync proof fragments, upstream-compatible sync fragment path-order/same-path guards, witness expansion, raw-key diff reconstruction, scan-time diff callbacks, imported proof-fragment shadow leaf node ids, exposed sync shadow root node ids, deterministic small and full 500-trial sync-fuzz convergence with scan/final node-id parity, full persisted tracked-node snapshot fuzzer convergence, pure-PHP `std::mt19937`-compatible sync-fuzz dimensions, path-projected partial-shadow diffs that skip hash-equal witnesses, in-memory tracked leaf node-id output/reuse semantics, branch child identity, checkout/fork by saved head node id, persisted tracked node-store export/import with named-head and branch node-id continuity, copy-on-write unchanged branch reuse after reload, tracked diff callbacks carrying upstream-shaped leaf node ids, tracked diff application/reconstruction across randomized forks, memStore-range tracked node allocation for volatile detached overlays, and named-head MemStore write rejection until an explicit fork detaches the head."
    },
    "wordpressScenario": "Authenticated local-first state sync for Playground snapshots and content databases; current fixtures/examples map ordered WordPress option/post records through raw integer keys, raw-key batch lookups without rehashing encoded IDs, FullKeys proof transport that preserves original WordPress option/post string keys for partial-tree enumeration, PHP-native large record-id boundary rejection, exact upstream-compatible BLAKE2s snapshot roots, iterator windows, compact authenticated range proofs, delegated sub-range proofs from imported partial snapshot proofs, proof-backed updates, merged partial proofs, delegated sub-proofs from imported partial snapshot proofs, sync diffs that reconstruct changed post/meta snapshots, malformed overlapping sync proof-fragment request rejection, scan-time diff callbacks that match final authenticated diffs, imported proof-fragment shadow leaf node ids that match final sync diffs, exposed sync shadow root node ids for authenticated partial imports, deterministic upstream-shaped full-fuzzer preflight, persisted tracked-head sync-fuzz preflights that reload named local/remote heads from JSON before convergence, compact rebuilds that preserve reused leaf node ids for unchanged records, saved branch-head fork checkout for old/new snapshots, persisted tracked-node JSON reload with the same published head/root/leaf node ids, tracked scan/final diffs that report identical leaf node ids for changed/deleted/added WordPress records, tracked diff application that reconstructs changed WordPress snapshots, volatile memStore-range preview overlays for unsaved Playground edits, and named published snapshot heads that reject volatile memStore writes until a detached preview fork is created.",
    "nextTask": "Map upstream `big proof test` more directly by exporting the 1000-key proof, importing it, and validating the -500..499 `getMulti` result set against upstream semantics."
}
