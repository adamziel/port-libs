{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 55,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 87 PHP behavior tests map upstream-compatible BLAKE2s hashing/key behavior, PHP-native integer overflow rejection, in-memory sparse tree updates, raw-key `getMultiRaw`, FullKeys proof transport and partial enumeration, iterator windows, composite integer/hash keys, compact proof transport, proof/range import/export, proof-backed partial-tree updates, mergeProof expansion, bounded sync proof fragments, tracked node-id persistence, native file-backed `quadb` current-head/detached-head persistence, integer and string-key import/export/diff/patch lines, root/status/head/head-rm output, proof export/import/merge persistence, proof-head writes/deletes/forks, ordered proof/update replay for merging updated-root proofs after persisted proof-head writes, and raw integer proof-backed head imports/puts/deletes across reopen.",
    "phpPass": 87,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered WordPress option/post records, raw integer keys, raw-key batch lookups, FullKeys proof transport, composite post-meta keys, exact upstream-compatible BLAKE2s snapshot roots, iterator windows, range/sub proofs, proof-backed post updates, merged partial proofs, sync diffs and scan callbacks, tracked node-id reuse, memStore preview overlays, native file-backed `quadb` preview-head reloads, tracked patch lines, proof import/merge/update examples, `wordpress-large-proof-window.php`, `wordpress-quadb-proof-merge-after-update.php` for merging an updated-root delegated proof after a persisted partial-head edit, and `wordpress-quadb-integer-proof-update.php` for persisted raw integer delegated preview edits.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices, now including ordered proof/update event history and raw integer proof-backed head updates",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` reconfirmed all 34 scenarios after this batch. Quadrable-only PHP tests pass with 87 behavior tests, 2317 assertions, and 0 failures. Required repo-wide `php tools/run-tests.php` passes with 140 test files, 12447 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus a native file-backed `QuadbStore`. The latest slice extends proof-backed heads to accept raw `Key` and integer get/put/delete operations, lets `importIntegerLines()` batch `quadb import --int`-style updates into partial proof heads, persists those raw updates in the ordered proof/update event history, and verifies updated-root proof merging plus delegated raw integer proof export after reopening.",
    "blocker": "No current quadrable-specific upstream runner, native digest, quadrable-only PHP, or repo-wide PHP blocker. Upstream `make -r test` passes all 34 scenarios; quadrable-only PHP tests pass with 87 behavior tests, 2317 assertions, and 0 failures; repo-wide PHP passes with 140 test files, 12447 assertions, and 0 failures. Remaining quadrable gaps: broader LMDB persistence internals, raw/composite-key CLI ergonomics beyond the native API, and quadb CLI dump/statistics/garbage-collection behavior.",
    "latestCommit": "5b313d9 Port quadrable raw integer proof heads",
    "nextTask": "Map broader LMDB persistence internals or remaining quadb CLI behavior such as importProof/mergeProof dump output, statistics/output formatting, or garbage collection; capture byte-level C++ oracle bytes for exact big-proof transport fixture parity if exact proof transport becomes the next priority."
}
