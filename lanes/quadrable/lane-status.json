{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 56,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 88 PHP behavior tests map upstream-compatible BLAKE2s hashing/key behavior, PHP-native integer overflow rejection, in-memory sparse tree updates, raw-key `getMultiRaw`, FullKeys proof transport and partial enumeration, iterator windows, composite integer/hash keys, compact proof transport, proof/range import/export, proof-backed partial-tree updates, mergeProof expansion, bounded sync proof fragments, tracked node-id persistence, native file-backed `quadb` current-head/detached-head persistence, integer and string-key import/export/diff/patch lines, root/status/head/head-rm output, proof export/import/merge persistence, proof-head writes/deletes/forks, ordered proof/update replay for merging updated-root proofs after persisted proof-head writes, raw integer proof-backed head imports/puts/deletes across reopen, and upstream-shaped `quadb gc` reachability sweeps for discarded full heads while preserving named and detached roots.",
    "phpPass": 88,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered WordPress option/post records, raw integer keys, raw-key batch lookups, FullKeys proof transport, composite post-meta keys, exact upstream-compatible BLAKE2s snapshot roots, iterator windows, range/sub proofs, proof-backed post updates, merged partial proofs, sync diffs and scan callbacks, tracked node-id reuse, memStore preview overlays, native file-backed `quadb` preview-head reloads, tracked patch lines, proof import/merge/update examples, `wordpress-large-proof-window.php`, `wordpress-quadb-proof-merge-after-update.php` for merging an updated-root delegated proof after a persisted partial-head edit, `wordpress-quadb-integer-proof-update.php` for persisted raw integer delegated preview edits, and `wordpress-quadb-head-cleanup.php` for removing and garbage-collecting discarded WordPress preview heads without changing the approved preview root.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices, now including ordered proof/update event history, raw integer proof-backed head updates, and upstream-shaped `quadb gc` full-head reachability sweeps",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` reconfirmed all 34 scenarios after this batch. Quadrable-only PHP tests pass with 88 behavior tests, 2335 assertions, and 0 failures. Required repo-wide `php tools/run-tests.php` passes with 142 test files, 12668 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus a native file-backed `QuadbStore`. The latest slice maps upstream `quadb gc`: named full heads are marked, the current full detached head is preserved, unreachable tracked leaf/branch records are swept, node-id counters are reset to the remaining high-water marks, stale tracked key names are pruned, and `garbageCollectText()` emits `Collected X/Y nodes` like the CLI.",
    "blocker": "No current quadrable-specific upstream runner, native digest, quadrable-only PHP, or repo-wide PHP blocker. Upstream `make -r test` passes all 34 scenarios; quadrable-only PHP tests pass with 88 behavior tests, 2335 assertions, and 0 failures; repo-wide PHP passes with 142 test files, 12668 assertions, and 0 failures. Remaining quadrable gaps: broader LMDB persistence internals, raw/composite-key CLI ergonomics beyond the native API, and quadb CLI dump/statistics/importProof/mergeProof output behavior.",
    "latestCommit": "pending current lane batch",
    "nextTask": "Map broader LMDB persistence internals or remaining quadb CLI behavior such as importProof/mergeProof dump output and statistics/output formatting; capture byte-level C++ oracle bytes for exact big-proof transport fixture parity if exact proof transport becomes the next priority."
}
