{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 29,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 55 PHP behavior tests mapped to upstream-compatible BLAKE2s hashing/key behavior, in-memory sparse tree updates, raw-key iterator windows, composite integer/hash keys from `Key::fromIntegerAndHash`, compact proof transport, selected proof import/export, integer proof sizing, range proofs, proof-backed partial-tree updates, mergeProof expansion, bounded sync proof fragments with diff reconstruction, scan-time diff callbacks, imported proof-fragment shadow node ids, deterministic small sync-fuzz convergence with scan/final node-id parity, pure-PHP `std::mt19937` seed-zero sync-fuzz dimensions and upstream byte budgets, path-projected partial-shadow diffs over witness branches, leaf and branch node-id behavior, checkout/fork by saved head node id, tracked diff leaf node ids, bounded scan/final diff node-id equality, randomized tracked diff application/reconstruction, native memStore-range tracked writes/overlay forks, named-head memStore fork guards, and an exact WordPress snapshot root",
    "phpPass": 55,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; current fixtures/examples map ordered WordPress option/post records through raw integer keys, composite post-meta keys using `Key::fromIntegerAndHash`, exact upstream-compatible BLAKE2s snapshot roots, iterator windows, compact authenticated range proofs, proof-backed post updates from a narrow raw-key proof, merged partial proofs for separate option/post reads, sync diffs that reconstruct changed post/meta snapshots, scan-time diff callbacks that match final authenticated diffs, imported proof-fragment shadow node ids that match final WordPress sync diffs, upstream-MT19937-shaped sync-fuzz dimensions and byte budgets, tracked leaf node-id reuse for compact unchanged-record rebuilds, saved branch-head fork checkout for old/new WordPress snapshots, tracked scan/final diffs with identical leaf node ids for changed/deleted/added WordPress records, tracked diff application that reconstructs changed WordPress snapshots from final diffs, volatile memStore-range preview overlays for unsaved Playground edits, and named published snapshot heads that reject volatile writes until an explicit preview fork detaches the head.",
    "phase": "upstream runner pass plus upstream-compatible BLAKE2s, key, composite integer/hash key, sparse tree, iterator window, compact proof/range, proof-backed update, mergeProof, bounded sync, scan-diff, imported sync shadow node-id, deterministic small sync-fuzz, bounded upstream-MT19937 sync-fuzz, tracked leaf/branch node-id, bounded tracked diff node-id, randomized tracked diff reconstruction, memStore-range tracked write, and named-head memStore fork guard slices",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` reconfirmed passing after the composite integer/hash key slice; native PHP parity is still partial, now uses pure-PHP BLAKE2s-256 for upstream-compatible root/proof digests and maps focused composite `Key::fromIntegerAndHash` behavior, bounded sync proof-fragment, scan-time diff callback, imported proof-fragment shadow node ids, deterministic small sync-fuzz with scan/final node-id parity, pure-PHP `std::mt19937` seed-zero sync-fuzz dimensions, path-projected partial-shadow diffs over witness branches, diff reconstruction, saved leaf node-id outputs, leaf reuse behavior, saved branch-head checkout, unchanged-branch copy-on-write reuse, tracked diff leaf node ids, bounded scan/final diff node-id equality, randomized tracked diff application/reconstruction, memStore-range detached writes/overlay forks, and the named-head LMDB/MemStore guard",
    "currentWork": "Pure-PHP BLAKE2s-256 hash/key primitives plus in-memory sparse tree get/put/delete, update batching, path-independent roots, getMulti, empty-head restoration, delete bubbling equivalence, raw-key iterator lower/upper-bound windows, composite integer/hash keys from `Key::fromIntegerAndHash`, compact proof encode/decode, selected proof import/export, integer proof sizing, raw-key range proofs, proof-backed updates, mergeProof expansion, sync request/response transport, bounded sync proof fragments, witness expansion, raw-key diff reconstruction, scan-time diff callbacks, imported proof-fragment shadow node ids, deterministic small sync-fuzz convergence with scan/final node-id parity, pure-PHP `std::mt19937` seed-zero sync-fuzz dimensions and upstream byte budgets, path-projected partial-shadow diffs that skip hash-equal witnesses, tracked leaf node-id output/reuse, branch child identity, checkout/fork by saved head node id, copy-on-write unchanged branch reuse, tracked diff callbacks with upstream-shaped leaf node ids, tracked diff application/reconstruction across randomized forks, memStore-range node allocation for volatile tracked overlays, named-head memStore write rejection until fork, and exact WordPress snapshot root verification.",
    "blocker": "No current quadrable upstream runner, digest, quadrable-only PHP, or repo-wide PHP blocker: `make -r test` passes after installing gcc-c++, lmdb-devel, libb2-devel, and libasan and shallow-initializing the three upstream submodules; native PHP implements BLAKE2s-256 directly; quadrable-only tests pass with 55 behavior tests, 953 assertions, and 0 failures; and repo-wide `php tools/run-tests.php` passes with 100 test files, 6727 assertions, and 0 failures. Plain `make test` still selects the built-in C++ rule in this GNU Make environment and omits repo include flags unless `-r` disables built-ins. Full upstream-scale 500-trial proof-fragment sync fuzz with persisted LMDB/imported shadow branch node-id parity remains unported.",
    "latestCommit": "b2b9aa4",
    "nextTask": "Broaden the bounded two-trial upstream-MT19937 sync-fuzz slice into the full upstream 500-trial proof-fragment sync fuzz, including persisted LMDB-style branch node-id parity."
}
