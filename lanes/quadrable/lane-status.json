{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 23,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 50 PHP behavior tests mapped to upstream-compatible BLAKE2s hashing/key behavior, in-memory sparse tree updates, raw-key iterator windows, compact proof transport, selected proof import/export, integer proof sizing, range proofs, proof-backed partial-tree updates, mergeProof expansion, bounded sync proof fragments with diff reconstruction, scan-time diff callbacks, deterministic small sync-fuzz convergence, leaf and branch node-id behavior, checkout/fork by saved head node id, tracked diff leaf node ids, bounded scan/final diff node-id equality, native memStore-range tracked writes/overlay forks, and an exact WordPress snapshot root",
    "phpPass": 50,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; current fixtures/examples map ordered WordPress option/post records through raw integer keys, exact upstream-compatible BLAKE2s snapshot roots, iterator windows, compact authenticated range proofs, proof-backed post updates from a narrow raw-key proof, merged partial proofs for separate option/post reads, sync diffs that reconstruct changed post/meta snapshots, scan-time diff callbacks that match final authenticated diffs, tracked leaf node-id reuse for compact unchanged-record rebuilds, saved branch-head fork checkout for old/new WordPress snapshots, tracked scan/final diffs with identical leaf node ids for changed/deleted/added WordPress records, and volatile memStore-range preview overlays for unsaved Playground edits.",
    "phase": "upstream runner pass plus upstream-compatible BLAKE2s, key, sparse tree, iterator window, compact proof/range, proof-backed update, mergeProof, bounded sync, scan-diff, deterministic small sync-fuzz, tracked leaf/branch node-id, bounded tracked diff node-id, and memStore-range tracked write slices",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` reconfirmed passing after the native memStore-range tracked-node slice; native PHP parity is still partial, now uses pure-PHP BLAKE2s-256 for upstream-compatible root/proof digests and maps focused bounded sync proof-fragment, scan-time diff callback, deterministic small sync-fuzz, diff reconstruction, saved leaf node-id outputs, leaf reuse behavior, saved branch-head checkout, unchanged-branch copy-on-write reuse, tracked diff leaf node ids, bounded scan/final diff node-id equality, and memStore-range detached writes/overlay forks",
    "currentWork": "Pure-PHP BLAKE2s-256 hash/key primitives plus in-memory sparse tree get/put/delete, update batching, path-independent roots, getMulti, empty-head restoration, delete bubbling equivalence, raw-key iterator lower/upper-bound windows, compact proof encode/decode, selected proof import/export, integer proof sizing, raw-key range proofs, proof-backed updates, mergeProof expansion, sync request/response transport, bounded sync proof fragments, witness expansion, raw-key diff reconstruction, scan-time diff callbacks, deterministic small sync-fuzz convergence, tracked leaf node-id output/reuse, branch child identity, checkout/fork by saved head node id, copy-on-write unchanged branch reuse, tracked diff callbacks with upstream-shaped leaf node ids, memStore-range node allocation for volatile tracked overlays, and exact WordPress snapshot root verification.",
    "blocker": "No current quadrable upstream runner, digest, or root-suite blocker: `make -r test` passes after installing gcc-c++, lmdb-devel, libb2-devel, and libasan and shallow-initializing the three upstream submodules; native PHP implements BLAKE2s-256 directly; quadrable-only tests pass; and repo-wide `php tools/run-tests.php` passes. Plain `make test` still selects the built-in C++ rule in this GNU Make environment and omits repo include flags unless `-r` disables built-ins. Full upstream-scale 500-trial randomized sync fuzz parity, imported shadow node-id equality, and the named-head LMDB/MemStore guard from upstream `memStore forking from lmdb` remain unported.",
    "latestCommit": "0075b27",
    "nextTask": "Broaden bounded tracked scan/diff node-id parity into full upstream 500-trial randomized sync fuzz with imported shadow node ids, then port the remaining named-head LMDB/MemStore guard from upstream `memStore forking from lmdb`."
}
