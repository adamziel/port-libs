{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 96,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 125 PHP behavior tests now map upstream-compatible BLAKE2s/key semantics, deterministic `quadb mineHash` bit-prefix mining, sparse tree updates, iterator windows, proofs/ranges, proof-backed updates/mergeProof, proof-backed partial `quadb export`/`export --int` lines with witness placeholders, bounded sync, tracked node ids, file-backed `quadb` command surfaces including no-argument docopt output, help/version metadata output, init stdout/stderr, advertised length no-op output, `root` missing/empty-directory startup behavior, `get` success/missing-key/missing-store output, `put`/`del` missing-store plus empty-stream write/overwrite/delete/missing-delete behavior, `import --int` missing-store plus `std::stoi`-style numeric-prefix/invalid input behavior, proof command output for invalid `exportProof --format`, upstream `from_hex` odd/empty/uppercase-prefix behavior, malformed proof decoding, empty proofs, premature varints, and `importProof --root` command root decoding; also upstream-shaped LMDB buckets/raw cursor entries, raw-entry-only restore, named/detached/noTrack raw-restored merge/GC oracles, a sequential multi-merge raw-restored named proof-head oracle, and mixed tracked/noTrack/full/proof/detached raw-restored merge/GC oracles for detached-current, named tracked-current, and named noTrack-current proof heads.",
    "phpPass": 125,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered records, proofs/ranges, sync diffs, tracked node ids, deterministic `mineHash` prefix fixtures via `wordpress-quadb-minehash-prefix.php`, file-backed preview heads, upstream-shaped `quadb --help`/`--version` metadata preflight via `wordpress-quadb-cli-metadata.php`, upstream-shaped `quadb init`/`length` bootstrap checks via `wordpress-quadb-init-length.php`, upstream-shaped `quadb root` startup guards via `wordpress-quadb-root-command-guard.php`, upstream-shaped no-argument/get read guards via `wordpress-quadb-get-guard.php`, upstream-shaped put/del write guards via `wordpress-quadb-put-del-guard.php`, upstream-shaped import --int guards via `wordpress-quadb-import-int-guard.php`, proof import/merge/update flows, proof input and root guards via `wordpress-quadb-proof-input-guard.php`, proof-backed partial export manifests via `wordpress-quadb-partial-export.php`, noTrackKeys privacy behavior, upstream-shaped LMDB bucket/raw-entry inspection, raw-entry-only restore, sequential raw backup named-head merge verification via `wordpress-quadb-sequential-raw-merge.php`, mixed raw backup detached-head merge verification via `wordpress-quadb-mixed-raw-merge.php`, mixed raw backup named-head merge verification via `wordpress-quadb-mixed-named-raw-merge.php`, mixed raw backup noTrack private-head merge/privacy verification via `wordpress-quadb-mixed-notrack-raw-merge.php`, portable mixed dump restore, numeric/date-shaped preview heads, and retained/pruned delegated proof-import raw cursor keys.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices; latest slice maps upstream `importProof --root` command decoding for lowercase `0x`, uppercase hex digits, rejected uppercase `0X`, short/odd root proof-invalid handling, and empty root values that import without unauthenticated warning text.",
    "audit": "audited 2026-05-23T09:21Z: upstream C++ `make -r test` passes all 34 scenarios after the `importProof --root` command-root slice. Targeted upstream probes showed `--root=0x<uppercase hex>` succeeds, `--root=` and `--root=0x` import with empty stdout/stderr, uppercase `0X` root prefixes fail with `quadb error: unexpected character in from_hex: 88`, `zz` fails with `unexpected character in from_hex: 122`, and short/odd roots such as `0x00` and `abc` fail with `proof invalid`. Quadrable-only PHP tests pass with 10 files, 125 behavior tests, 2956 assertions, and 0 failures. Example `examples/wordpress-quadb-proof-input-guard.php` now records WordPress delegated-proof and root input guards with upstream-shaped missing-store, invalid-format, malformed-hex, invalid-root, empty-root, uppercase-root, and trusted import behavior. A final repo-wide root harness sample passed with 200 test files, 22964 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus native file-backed `QuadbStore`; latest work adds command-level upstream `from_hex` root decoding for `importProof --root` while keeping the stricter library root API unchanged.",
    "blocker": "No current quadrable-specific upstream runner, quadrable-only PHP, or repo-wide root harness blocker. Full 500-trial sync-fuzzer probes remain outside the fast suite by design.",
    "latestCommit": "8d345b5e - Advance quadrable proof command parity",
    "nextTask": "Map another low-risk CLI boundary such as binary `importProof --root` parity/dump precedence, or decide whether the slow 500-trial sync-fuzzer probes should become optional CI evidence."
}
