{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 15,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 36 PHP tests mapped to hashing/key, in-memory sparse tree updates, raw-key iterator windows, compact proof transport, selected proof import/export, integer proof sizing, range proofs, proof-backed partial-tree updates, mergeProof expansion, and bounded sync proof fragments with diff reconstruction",
    "phpPass": 36,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; current fixtures/examples map ordered WordPress option/post records through raw integer keys, iterator windows, compact authenticated range proofs, proof-backed post updates from a narrow raw-key proof, merged partial proofs for separate option/post reads, and sync diffs that reconstruct changed post/meta snapshots.",
    "phase": "upstream runner pass plus key, sparse tree, iterator window, compact proof/range, proof-backed update, mergeProof, and bounded sync slices",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` passes; native PHP parity is still partial, uses a SHA-256 hash surrogate, and now maps focused bounded sync proof-fragment and diff reconstruction behavior",
    "currentWork": "Native hash/key primitives plus in-memory sparse tree get/put/delete, update batching, path-independent roots, getMulti, empty-head restoration, delete bubbling equivalence, raw-key iterator lower/upper-bound windows, compact proof encode/decode, selected proof import/export, integer proof sizing, raw-key range proofs, proof-backed updates, mergeProof expansion, sync request/response transport, bounded sync proof fragments, witness expansion, and raw-key diff reconstruction.",
    "blocker": "No current upstream runner blocker: `make -r test` passes after installing gcc-c++, lmdb-devel, libb2-devel, and libasan and shallow-initializing the three upstream submodules. Plain `make test` still selects the built-in C++ rule in this GNU Make environment and omits repo include flags unless `-r` disables built-ins. PHP HashTree still uses a SHA-256 surrogate because this PHP build lacks BLAKE2s, so roots/proof bytes are not digest-compatible with upstream yet. Full randomized sync fuzz parity, scan/diff node-id equivalence, and persisted LMDB node-id behavior remain unported.",
    "latestCommit": "pending",
    "nextTask": "Broaden sync fuzz parity with deterministic randomized multi-round sync cases, scan/diff equivalence, and persisted LMDB node-id behavior."
}
