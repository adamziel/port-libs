{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 40,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 69 PHP behavior tests mapped to upstream-compatible BLAKE2s hashing/key behavior, PHP-native integer overflow rejection for upstream's `int range exceeded` boundary, in-memory sparse tree updates, upstream raw-key `getMultiRaw` batch lookups, raw-key iterator windows, composite integer/hash keys from `Key::fromIntegerAndHash`, compact proof transport, selected proof import/export, integer proof sizing, range proofs, range sub-proof exports from imported partial trees, proof-backed partial-tree updates, mergeProof expansion, sub-proof exports from imported partial trees, bounded sync proof fragments with diff reconstruction, sync fragment path-order/same-path guards, scan-time diff callbacks, imported proof-fragment shadow leaf node ids, exposed sync shadow root node ids, deterministic small sync-fuzz convergence with scan/final node-id parity, pure-PHP `std::mt19937` seed-zero sync-fuzz dimensions and upstream byte budgets, full native `SyncFuzzer::run(500, 0)` parity evidence, bounded persisted tracked-node snapshot reloads inside the upstream-shaped sync-fuzz loop, cached full proof-tree reuse/invalidation, path-projected partial-shadow diffs over witness branches, leaf and branch node-id behavior, checkout/fork by saved head node id, persisted tracked node-store snapshots with named-head and branch node-id reload, tracked diff leaf node ids, bounded scan/final diff node-id equality, randomized tracked diff application/reconstruction, native memStore-range tracked writes/overlay forks, named-head memStore fork guards, delegated WordPress sub-proofs and range sub-proofs, and an exact WordPress snapshot root",
    "phpPass": 69,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; current fixtures/examples map ordered WordPress option/post records through raw integer keys, raw-key batch lookups without rehashing encoded IDs, PHP-native large record-id boundary rejection, composite post-meta keys using `Key::fromIntegerAndHash`, exact upstream-compatible BLAKE2s snapshot roots, iterator windows, compact authenticated range proofs, delegated narrower range proofs from imported partial snapshot proofs, proof-backed post updates from a narrow raw-key proof, merged partial proofs for separate option/post reads, delegated narrower sub-proofs from imported partial snapshot proofs, sync diffs that reconstruct changed post/meta snapshots, malformed overlapping sync proof-fragment request rejection, scan-time diff callbacks that match final authenticated diffs, imported proof-fragment shadow leaf node ids that match final WordPress sync diffs, exported sync shadow root node ids in the memStore range, upstream-MT19937-shaped sync-fuzz dimensions and byte budgets, deterministic full-fuzzer preflight for Playground snapshot sync, persisted tracked-head sync-fuzz preflights that reload named local/remote heads from JSON before convergence, tracked leaf node-id reuse for compact unchanged-record rebuilds, saved branch-head fork checkout for old/new WordPress snapshots, persisted tracked-node JSON reload with the same published head/root/leaf node ids, tracked scan/final diffs with identical leaf node ids for changed/deleted/added WordPress records, tracked diff application that reconstructs changed WordPress snapshots from final diffs, volatile memStore-range preview overlays for unsaved Playground edits, and named published snapshot heads that reject volatile writes until an explicit preview fork detaches the head.",
    "phase": "upstream runner pass plus upstream-compatible BLAKE2s, key, PHP-native integer range guard, composite integer/hash key, sparse tree, raw-key getMultiRaw, iterator window, compact proof/range, proof-backed update, mergeProof, sub-proof export, range sub-proof export, bounded sync, sync request guard, scan-diff, imported sync shadow leaf node-id, sync shadow root node-id, deterministic small sync-fuzz, bounded and full upstream-MT19937 sync-fuzz, persisted tracked-node sync-fuzz fast slice, cached full proof-tree optimization, tracked leaf/branch node-id, persisted tracked-node store, bounded tracked diff node-id, randomized tracked diff reconstruction, memStore-range tracked write, and named-head memStore fork guard slices",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` reconfirmed passing after the raw-key getMultiRaw slice; native PHP parity is still partial, now uses pure-PHP BLAKE2s-256 for upstream-compatible root/proof digests and maps focused integer overflow rejection, raw-key `getMultiRaw` behavior, composite `Key::fromIntegerAndHash` behavior, sub-proof and range sub-proof export from imported partial trees, bounded sync proof-fragment transport, upstream same-path fragment request rejection, scan-time diff callback, imported proof-fragment shadow leaf node ids, exposed sync shadow root node ids, deterministic small sync-fuzz with scan/final node-id parity, pure-PHP `std::mt19937` seed-zero sync-fuzz dimensions, full 500-trial upstream-shaped SyncFuzzer convergence, persisted tracked-node JSON reload checks inside a two-trial upstream-shaped sync-fuzz loop, path-projected partial-shadow diffs over witness branches, diff reconstruction, saved leaf node-id outputs, leaf reuse behavior, saved branch-head checkout, persisted named-head/branch node-id reload, unchanged-branch copy-on-write reuse after reload, tracked diff leaf node ids, bounded scan/final diff node-id equality, randomized tracked diff application/reconstruction, memStore-range detached writes/overlay forks, and the named-head LMDB/MemStore guard",
    "currentWork": "Pure-PHP BLAKE2s-256 hash/key primitives plus PHP-native integer overflow rejection, in-memory sparse tree get/put/delete, update batching, path-independent roots, getMulti, raw-key getMultiRaw, empty-head restoration, delete bubbling equivalence, raw-key iterator lower/upper-bound windows, composite integer/hash keys from `Key::fromIntegerAndHash`, compact proof encode/decode, selected proof import/export, integer proof sizing, raw-key range proofs, range sub-proof exports from imported partial trees, proof-backed updates, mergeProof expansion, sub-proof exports from imported partial trees, sync request/response transport, bounded sync proof fragments, sync fragment path-order/same-path guards, witness expansion, raw-key diff reconstruction, scan-time diff callbacks, imported proof-fragment shadow leaf node ids, exposed sync shadow root node ids, deterministic small and full upstream-shaped sync-fuzz convergence with scan/final node-id parity, persisted tracked-node snapshot reload checks inside a bounded upstream-shaped sync-fuzz loop, cached full proof-tree reuse with update invalidation, path-projected partial-shadow diffs that skip hash-equal witnesses, tracked leaf node-id output/reuse, branch child identity, checkout/fork by saved head node id, persisted tracked node-store export/import, copy-on-write unchanged branch reuse after reload, tracked diff callbacks with upstream-shaped leaf node ids, tracked diff application/reconstruction across randomized forks, memStore-range node allocation for volatile tracked overlays, named-head memStore write rejection until fork, delegated WordPress sub-proof and range sub-proof verification, and exact WordPress snapshot root verification.",
    "blocker": "No current quadrable upstream runner, native digest, quadrable-only PHP, or repo-wide PHP blocker. Upstream `make -r test` passes all 34 upstream scenarios after installing gcc-c++, lmdb-devel, libb2-devel, and libasan and shallow-initializing the three upstream submodules; plain `make test` still selects the GNU Make built-in C++ rule and omits repo include flags unless `-r` disables built-ins. Native PHP implements BLAKE2s-256 directly; quadrable-only tests pass with 69 behavior tests, 1143 assertions, and 0 failures; repo-wide `php tools/run-tests.php` passes with 117 test files, 9011 assertions, and 0 failures. Full native `SyncFuzzer::run(500, 0)` passes in 167.914s with max 9 round trips, 25537 requests, and 22591 responses; bounded `SyncFuzzer::runWithPersistedTrackedSnapshots(2, 0)` passes in the fast suite and the matching WordPress example reports restored local/remote head continuity. Remaining quadrable evidence gap: full 500-trial persisted tracked-node fuzzer timing is not yet run outside the fast suite.",
    "latestCommit": "0173a46 (quadrable partial proof and range sub-proof exports)",
    "nextTask": "Run the full 500-trial `SyncFuzzer::runWithPersistedTrackedSnapshots(500, 0)` probe outside the fast suite and compare its timing/budget envelope with the existing in-memory full-fuzzer evidence."
}
