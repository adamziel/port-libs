{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 70,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 104 PHP behavior tests map upstream-compatible BLAKE2s/key semantics, sparse tree updates, iterator windows, proofs/ranges, proof-backed updates/mergeProof, bounded sync, tracked node ids, file-backed `quadb` command surfaces, upstream-shaped full/proof-backed LMDB bucket values, raw LMDB entry key/value byte snapshots, numeric/date-shaped head names preserved as LMDB string keys, binary/non-UTF-8 leaf values plus tracked keys preserved through JSON-backed reopen, and an upstream C++ LMDB cursor oracle for binary tracked-key/proof-head raw bytes.",
    "phpPass": 104,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered records, proofs/ranges, sync diffs, tracked node ids, file-backed preview heads, proof import/merge/update flows, noTrackKeys privacy behavior, upstream-shaped LMDB bucket inspection via `wordpress-quadb-lmdb-layout.php`, raw LMDB backup manifests with binary serialized option payloads via `wordpress-quadb-lmdb-raw-entries.php`, and numeric/date-shaped WordPress preview head names via `wordpress-quadb-numeric-heads.php`.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices; latest slice captures an upstream C++ LMDB cursor oracle and verifies native raw-entry bytes for binary tracked keys, string-sorted numeric heads, and proof-backed delegated updates.",
    "audit": "audited 2026-05-23: upstream C++ `make -r test` passes all 34 scenarios. Quadrable-only PHP tests pass with 104 behavior tests, 2555 assertions, and 0 failures. Focused `QuadbStoreTest` passes with the upstream LMDB cursor oracle comparison. `php lanes/quadrable/examples/wordpress-quadb-lmdb-raw-entries.php` runs successfully. Required repo-wide `php tools/run-tests.php` passes with 174 test files, 16311 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus native file-backed `QuadbStore`; latest work locks `lmdbRawEntrySnapshot()` to a generated upstream C++ LMDB cursor oracle for binary WordPress serialized keys/values and delegated proof-head updates.",
    "blocker": "No current quadrable-specific upstream runner, quadrable-only PHP, example, or repo-wide PHP blocker. Remaining quadrable gap: broader byte-for-byte LMDB dump/restore parity across mergeProof garbage retention, gc pruning, and larger multi-head stores.",
    "latestCommit": "this upstream LMDB cursor oracle batch; root PHP harness passed before commit",
    "nextTask": "Broaden the upstream LMDB cursor oracle to cover mergeProof followed by quadb gc, then compare native retained/pruned proof-import buckets byte-for-byte against that upstream dump."
}
