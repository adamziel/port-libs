{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 62,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 94 PHP behavior tests map upstream-compatible BLAKE2s hashing/key behavior, PHP-native integer overflow rejection, in-memory sparse tree updates, raw-key `getMultiRaw`, FullKeys proof transport and partial enumeration, iterator windows, composite integer/hash keys, compact proof transport, proof/range import/export, proof-backed partial-tree updates, mergeProof expansion, bounded sync proof fragments, tracked node-id persistence, native file-backed `quadb` current-head/detached-head persistence, integer and string-key import/export/diff/patch lines, file-backed composite integer+hash import/export/proof lines for upstream `Key::fromIntegerAndHash()`, root/status/head/head-rm/stats output, proof export/import/merge persistence, stdin key proof export, raw binary proof import/merge, noTrackKeys export/diff/dump/proof behavior, proof dump text, unauthenticated importProof output, proof-head writes/deletes/forks, ordered proof/update replay for merging updated-root proofs after persisted proof-backed writes, raw integer proof-backed head imports/puts/deletes across reopen, upstream-shaped `quadb gc` reachability sweeps for discarded full heads, and upstream-shaped `quadb dumpTree` output for empty, full, raw-key, and proof-backed heads.",
    "phpPass": 94,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered WordPress option/post records, raw integer keys, raw-key batch lookups, FullKeys proof transport, composite post-meta keys, exact upstream-compatible BLAKE2s snapshot roots, iterator windows, range/sub proofs, proof-backed post updates, merged partial proofs, sync diffs and scan callbacks, tracked node-id reuse, memStore preview overlays, native file-backed `quadb` preview-head reloads, tracked patch lines, proof import/merge/update examples, `wordpress-large-proof-window.php`, `wordpress-quadb-proof-merge-after-update.php` for merging an updated-root delegated proof after a persisted partial-head edit, `wordpress-quadb-integer-proof-update.php` for persisted raw integer delegated preview edits, `wordpress-quadb-composite-meta.php` for delegated postmeta proofs keyed by upstream composite integer+hash keys, `wordpress-quadb-head-cleanup.php` for removing and garbage-collecting discarded WordPress preview heads without changing the approved preview root, `wordpress-quadb-stats.php` for reporting full-head and delegated proof-head stats, `wordpress-quadb-unauthenticated-proof-import.php` for debugging a delegated proof and surfacing the upstream unauthenticated import warning/root before trusting preview data, `wordpress-quadb-dump-tree.php` for inspecting full and delegated proof tree witnesses during preview debugging, `wordpress-quadb-proof-stdin-binary.php` for newline-delimited delegated proof keys with raw binary proof import/merge, and `wordpress-quadb-notrackkeys.php` for privacy-preserving snapshot exports that authenticate through hashed proofs while suppressing original key strings.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices, now including ordered proof/update event history, raw integer proof-backed head updates, file-backed composite integer+hash line/proof helpers, upstream-shaped `quadb gc` full-head reachability sweeps, upstream-shaped `quadb stats` output for full and proof-backed heads, upstream-shaped proof dump plus unauthenticated importProof output, upstream-shaped `quadb dumpTree` output, upstream-shaped `quadb exportProof --stdin` plus binary `importProof`/`mergeProof` input, and upstream-shaped `quadb --noTrackKeys` export/diff/dump/proof behavior",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` reconfirmed all 34 scenarios after this batch. Quadrable-only PHP tests pass with 94 behavior tests, 2415 assertions, and 0 failures. The new `wordpress-quadb-composite-meta.php` example runs successfully. The required repo-wide `php tools/run-tests.php` still needs this batch's final rerun; earlier status recorded an unrelated dirty-worktree Pandoc failure in `lanes/pandoc/tests/MarkdownReaderTest.php`.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus a native file-backed `QuadbStore`. The latest slice maps file-backed composite integer+hash keys using upstream `Key::fromIntegerAndHash()` semantics: import/export lines persist the integer and 23-31 byte suffix metadata, direct put/get/delete helpers address the raw composite key, and delegated HashedKeys proofs authenticate WordPress postmeta rows plus proven absences.",
    "blocker": "No current quadrable-specific upstream runner, native digest, or quadrable-only PHP blocker. Upstream `make -r test` passes all 34 scenarios; quadrable-only PHP tests pass with 94 behavior tests, 2415 assertions, and 0 failures. Current repo-wide PHP status is pending this batch's final rerun in a dirty multi-lane checkout. Remaining quadrable gaps: broader LMDB persistence internals and exact LMDB storage-layout parity.",
    "latestCommit": "current quadrable batch committed after green root suite; final worker report carries the git hash",
    "nextTask": "Map broader LMDB persistence internals or capture byte-level C++ oracle bytes for exact big-proof transport fixture parity if exact proof transport becomes the next priority."
}
