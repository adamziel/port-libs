{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 13,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 33 PHP tests mapped to hashing/key, in-memory sparse tree updates, raw-key iterator windows, compact proof transport, selected proof import/export, integer proof sizing, range proofs, proof-backed partial-tree updates, and mergeProof expansion",
    "phpPass": 33,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; current fixtures/examples map ordered WordPress option/post records through raw integer keys, iterator windows, compact authenticated range proofs, proof-backed post updates from a narrow raw-key proof, and merged partial proofs for separate option/post reads.",
    "phase": "upstream runner pass plus key, sparse tree, iterator window, compact proof/range, proof-backed update, and mergeProof slices",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` passes; native PHP parity is still partial, uses a SHA-256 hash surrogate, and now maps focused mergeProof behavior",
    "currentWork": "Native hash/key primitives plus in-memory sparse tree get/put/delete, update batching, path-independent roots, getMulti, empty-head restoration, delete bubbling equivalence, raw-key iterator lower/upper-bound windows, compact proof encode/decode, selected proof import/export, integer proof sizing, raw-key range proofs, proof-backed updates, and mergeProof expansion between separately imported partial proofs.",
    "blocker": "No current upstream runner blocker: `make -r test` passes after installing gcc-c++, lmdb-devel, libb2-devel, and libasan and shallow-initializing the three upstream submodules. Plain `make test` still selects the built-in C++ rule in this GNU Make environment and omits repo include flags unless `-r` disables built-ins. PHP HashTree still uses a SHA-256 surrogate because this PHP build lacks BLAKE2s, so roots/proof bytes are not digest-compatible with upstream yet. Sync request/response proof fragments and persisted LMDB node-id behavior remain unported.",
    "latestCommit": "4c57af7",
    "nextTask": "Port sync request/response proof fragments and diff reconstruction from upstream sync fuzz scenarios."
}
