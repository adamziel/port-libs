{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 76,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 105 PHP behavior tests map upstream-compatible BLAKE2s/key semantics, sparse tree updates, iterator windows, proofs/ranges, proof-backed updates/mergeProof, bounded sync, tracked node ids, file-backed `quadb` command surfaces, upstream-shaped full/proof-backed LMDB bucket values, raw LMDB entry key/value byte snapshots, numeric/date-shaped head names preserved as LMDB string keys, binary/non-UTF-8 leaf values plus tracked keys preserved through JSON-backed reopen, an upstream C++ LMDB cursor oracle for binary tracked-key named/detached proof-head raw bytes, noTrackKeys full/proof heads with empty `quadrable_key` buckets, mergeProof-retained and post-`quadb gc` pruned proof-import buckets, and portable dump/restore verification for mixed full, named proof-backed, detached proof-backed, and noTrackKeys stores.",
    "phpPass": 105,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered records, proofs/ranges, sync diffs, tracked node ids, file-backed preview heads, proof import/merge/update flows, noTrackKeys privacy behavior with empty raw LMDB key buckets via `wordpress-quadb-notrackkeys.php`, upstream-shaped LMDB bucket inspection via `wordpress-quadb-lmdb-layout.php`, raw LMDB backup manifests with binary serialized option payloads via `wordpress-quadb-lmdb-raw-entries.php`, portable mixed full/proof/detached/noTrack dump restore via `wordpress-quadb-portable-dump-restore.php`, numeric/date-shaped WordPress preview head names via `wordpress-quadb-numeric-heads.php`, and retained/pruned delegated proof-import raw cursor keys via `wordpress-quadb-proof-merge-gc.php`.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices; latest slice adds portable dump/restore for mixed full heads, named proof-backed heads, detached proof-backed heads, and noTrackKeys heads with raw LMDB cursor-byte verification after restore.",
    "audit": "audited 2026-05-23: upstream C++ `make -r test` passes all 34 scenarios. Quadrable-only PHP tests pass with 105 behavior tests, 2601 assertions, and 0 failures. Focused portable dump/restore coverage verifies mixed full, named proof-backed, detached proof-backed, and noTrackKeys raw LMDB cursor entries after restore. Required repo-wide `php tools/run-tests.php` passes with 178 test files, 17211 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus native file-backed `QuadbStore`; latest work adds `exportPortableDump()` and `restorePortableDump()` so WordPress backup/migration tooling can restore JSON-backed state and prove the restored upstream-shaped raw LMDB cursor bytes match the dump.",
    "blocker": "No current quadrable-specific upstream runner, quadrable-only PHP, or repo-wide PHP harness blocker. Remaining quadrable gap: upstream-backed byte-for-byte dump/restore oracle evidence against a larger real C++ LMDB database beyond the native portable dump verifier.",
    "latestCommit": "pending lane commit after portable dump/restore batch; quadrable-only PHP tests, upstream C++ runner, and root PHP harness passed",
    "nextTask": "Capture an upstream C++ LMDB dump/restore oracle for a larger mixed tracked, noTrackKeys, named proof, and detached proof store."
}
