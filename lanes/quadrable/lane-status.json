{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 65,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 99 PHP behavior tests map upstream-compatible BLAKE2s hashing/key behavior, PHP-native integer overflow rejection, in-memory sparse tree updates, raw-key `getMultiRaw`, FullKeys proof transport and partial enumeration, iterator windows, composite integer/hash keys, compact proof transport, proof/range import/export, proof-backed partial-tree updates, mergeProof expansion, retained mergeProof import garbage projection plus `quadb gc` pruning, bounded sync proof fragments, tracked node-id persistence, native file-backed `quadb` current-head/detached-head persistence, integer and string-key import/export/diff/patch lines, file-backed composite integer+hash import/export/proof lines for upstream `Key::fromIntegerAndHash()`, root/status/head/head-rm/stats output, proof export/import/merge persistence, stdin key proof export, raw binary proof import/merge, noTrackKeys export/diff/dump/proof behavior, proof dump text, unauthenticated importProof output, proof-head writes/deletes/forks, ordered proof/update replay for merging updated-root proofs after persisted proof-backed writes, raw integer proof-backed head imports/puts/deletes across reopen, upstream-shaped `quadb gc` reachability sweeps for discarded full heads and retained proof-import nodes, upstream-shaped `quadb dumpTree` output for empty, full, raw-key, and proof-backed heads, full-head LMDB bucket layout snapshots, proof-backed LMDB bucket layout projections with upstream raw leaf/witness/branch buckets, independent proof-import LMDB allocation order with forked proof-head root sharing, and divergent proof-backed fork update storage sharing for unchanged imported proof nodes.",
    "phpPass": 99,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered WordPress option/post records, raw integer keys, raw-key batch lookups, FullKeys proof transport, composite post-meta keys, exact upstream-compatible BLAKE2s snapshot roots, iterator windows, range/sub proofs, proof-backed post updates, merged partial proofs, sync diffs and scan callbacks, tracked node-id reuse, MemStore preview overlays, native file-backed `quadb` preview-head reloads, tracked patch lines, proof import/merge/update examples, `wordpress-large-proof-window.php`, `wordpress-quadb-proof-merge-after-update.php` for merging an updated-root delegated proof after a persisted partial-head edit, `wordpress-quadb-integer-proof-update.php` for persisted raw integer delegated preview edits, `wordpress-quadb-proof-fork-sharing.php` for sharing unchanged imported proof storage across a delegated base/preview fork, `wordpress-quadb-composite-meta.php` for delegated postmeta proofs keyed by upstream composite integer+hash keys, `wordpress-quadb-head-cleanup.php` for removing and garbage-collecting discarded WordPress preview heads without changing the approved preview root, `wordpress-quadb-stats.php` for reporting full-head and delegated proof-head stats, `wordpress-quadb-lmdb-layout.php` for inspecting full-head and delegated proof-head upstream-shaped LMDB buckets, including independent delegated preview imports versus shared proof-head forks before WordPress snapshot backup/migration, `wordpress-quadb-unauthenticated-proof-import.php` for debugging a delegated proof and surfacing the upstream unauthenticated import warning/root before trusting preview data, `wordpress-quadb-dump-tree.php` for inspecting full and delegated proof tree witnesses during preview debugging, `wordpress-quadb-proof-stdin-binary.php` for newline-delimited delegated proof keys with raw binary proof import/merge, and `wordpress-quadb-notrackkeys.php` for privacy-preserving snapshot exports that authenticate through hashed proofs while suppressing original key strings.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices, now including ordered proof/update event history, raw integer proof-backed head updates, file-backed composite integer+hash line/proof helpers, upstream-shaped `quadb gc` full-head reachability sweeps plus retained proof-import node sweeps after `mergeProof`, upstream-shaped `quadb stats` output for full and proof-backed heads, upstream-shaped proof dump plus unauthenticated importProof output, upstream-shaped `quadb dumpTree` output, upstream-shaped `quadb exportProof --stdin` plus binary `importProof`/`mergeProof` input, upstream-shaped `quadb --noTrackKeys` export/diff/dump/proof behavior, upstream-shaped full-head plus proof-backed LMDB bucket layout snapshots, independent proof-import LMDB allocation order with forked proof-head root sharing, and divergent proof-backed fork update storage sharing",
    "audit": "audited 2026-05-23: upstream C++ `make -r test` reconfirmed all 34 scenarios after this batch. Quadrable-only PHP tests pass with 99 behavior tests, 2500 assertions, and 0 failures. `php lanes/quadrable/examples/wordpress-quadb-proof-fork-sharing.php` runs successfully and reports the unchanged `wp_options:home` proof leaf stored once across delegated base/preview heads. Required repo-wide `php tools/run-tests.php` passes with 166 test files, 15392 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus a native file-backed `QuadbStore`. The latest slice shares projected LMDB storage for common proof-event prefixes so a delegated proof head and its divergent preview fork reuse unchanged imported proof nodes while allocating fresh nodes for edited leaves.",
    "blocker": "No current quadrable-specific upstream runner, native digest, quadrable-only PHP, or repo-wide PHP blocker. Upstream `make -r test` passes all 34 scenarios, quadrable-only PHP tests pass with 99 behavior tests, 2500 assertions, and 0 failures, and repo-wide `php tools/run-tests.php` passes with 166 test files, 15392 assertions, and 0 failures. Remaining quadrable gaps: broader byte-for-byte LMDB persistence internals and exact big-proof oracle bytes if proof transport becomes the next priority.",
    "latestCommit": "pending implementation commit for this lane batch; root PHP harness passed before commit",
    "nextTask": "Capture byte-level C++ oracle bytes for exact big-proof transport fixture parity or broaden byte-for-byte LMDB persistence internals."
}
