{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 74,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 104 PHP behavior tests map upstream-compatible BLAKE2s/key semantics, sparse tree updates, iterator windows, proofs/ranges, proof-backed updates/mergeProof, bounded sync, tracked node ids, file-backed `quadb` command surfaces, upstream-shaped full/proof-backed LMDB bucket values, raw LMDB entry key/value byte snapshots, numeric/date-shaped head names preserved as LMDB string keys, binary/non-UTF-8 leaf values plus tracked keys preserved through JSON-backed reopen, and an upstream C++ LMDB cursor oracle for binary tracked-key named/detached proof-head raw bytes, noTrackKeys full/proof heads with empty `quadrable_key` buckets, plus mergeProof-retained and post-`quadb gc` pruned proof-import buckets.",
    "phpPass": 104,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered records, proofs/ranges, sync diffs, tracked node ids, file-backed preview heads, proof import/merge/update flows, noTrackKeys privacy behavior with empty raw LMDB key buckets via `wordpress-quadb-notrackkeys.php`, upstream-shaped LMDB bucket inspection via `wordpress-quadb-lmdb-layout.php`, raw LMDB backup manifests with binary serialized option payloads via `wordpress-quadb-lmdb-raw-entries.php`, numeric/date-shaped WordPress preview head names via `wordpress-quadb-numeric-heads.php`, and retained/pruned delegated proof-import raw cursor keys via `wordpress-quadb-proof-merge-gc.php`.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices; latest slice extends the upstream C++ LMDB cursor oracle to noTrackKeys full and proof-backed heads and verifies native raw bucket bytes, including empty `quadrable_key` tables across JSON reopen.",
    "audit": "audited 2026-05-23: upstream C++ `make -r test` passes all 34 scenarios. Quadrable-only PHP tests pass with 104 behavior tests, 2575 assertions, and 0 failures. Focused `QuadbStoreTest` passes with 31 tests, 407 assertions, and upstream LMDB cursor oracle comparisons for binary named proof heads, detached proof heads, noTrackKeys full/proof heads, and mergeProof before/after `quadb gc`. Required repo-wide `php tools/run-tests.php` passes with 177 test files, 16976 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus native file-backed `QuadbStore`; latest work adds upstream raw cursor evidence that noTrackKeys full and proof-backed heads omit `quadrable_key` entries while retaining authenticated hashed proof reads and binary values.",
    "blocker": "No current quadrable-specific upstream runner or quadrable-only PHP blocker. Remaining quadrable gap: broader byte-for-byte LMDB dump/restore parity across larger mixed tracked, noTrackKeys, named proof, and detached proof stores.",
    "latestCommit": "pending lane commit after noTrackKeys LMDB cursor oracle batch; root PHP harness passed",
    "nextTask": "Combine upstream-backed full, proof-backed, detached proof, and noTrackKeys heads into one larger dump/restore scenario."
}
