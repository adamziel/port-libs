{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 98,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 130 PHP behavior tests now map upstream-compatible BLAKE2s/key semantics, deterministic `quadb mineHash` bit-prefix mining, sparse tree updates, iterator windows, proofs/ranges, proof-backed updates/mergeProof, proof-backed partial `quadb export`/`export --int` lines with witness placeholders, bounded sync, tracked node ids, and file-backed `quadb` command surfaces including no-argument docopt output, help/version metadata, init/length/root/get/put/del/import/proof commands, checkout/fork transitions, status/head/head rm/stats/gc/dumpTree inspection, and the latest diff/patch command-output wrappers with upstream tree-walk delta order. The lane also maps upstream-shaped LMDB buckets/raw cursor entries, raw-entry-only restore, named/detached/noTrack raw-restored merge/GC oracles, a sequential multi-merge raw-restored named proof-head oracle, and mixed tracked/noTrack/full/proof/detached raw-restored merge/GC oracles for detached-current, named tracked-current, and named noTrack-current proof heads.",
    "phpPass": 130,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered records, proofs/ranges, sync diffs, tracked node ids, deterministic `mineHash` prefix fixtures via `wordpress-quadb-minehash-prefix.php`, file-backed preview heads, upstream-shaped CLI guards for metadata/init/root/get/put/del/checkout/fork/import/proof/status/head/stats/dumpTree/diff/patch commands, proof import/merge/update flows, proof-backed partial export manifests, noTrackKeys privacy behavior, upstream-shaped LMDB bucket/raw-entry inspection, raw-entry-only restore, sequential and mixed raw backup merge verification, portable mixed dump restore, numeric/date-shaped preview heads, retained/pruned delegated proof-import raw cursor keys, `wordpress-quadb-inspection-command-guard.php` for status/head/stats/dumpTree inspection plus `head rm`/`gc` cleanup of discarded preview heads, and the updated `wordpress-quadb-patch.php` scenario for fail-closed command-style WordPress preview diff/patch replication.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices; latest slice maps `quadb diff <head>` and `quadb patch` command-output wrappers, including missing-store guards, empty-store diff bootstrap, upstream tree-walk ordered deltas, silent patch success, comment skipping, and malformed patch errors.",
    "audit": "audited 2026-05-23T10:19Z: upstream C++ `make -r test` passes all 34 scenarios after the diff/patch command-output slice. Targeted upstream probes showed missing `diff`/`patch` commands fail before creating a directory; precreated empty stores bootstrap for `diff`; populated `diff <head>` emits upstream tree-walk ordered deltas; `patch` succeeds silently; and empty, malformed-operation, and missing-separator patch lines return upstream-shaped errors. Quadrable-only PHP tests pass with 10 files, 130 behavior tests, 3,046 assertions, and 0 failures. Example `examples/wordpress-quadb-patch.php` passed and records WordPress preview diff/patch replication through command wrappers. The required duplicate-root gate was clear, and repo-wide `php tools/run-tests.php` passed with 207 test files, 23,995 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus native file-backed `QuadbStore`; latest work adds command-output parity for `quadb diff <head>` and `quadb patch`, with upstream tree-walk delta ordering and patch input guards.",
    "blocker": "No current quadrable-specific upstream runner, focused PHP, or repo-wide harness blocker. Full 500-trial sync-fuzzer probes remain outside the fast suite by design.",
    "latestCommit": "35aa3bed - Advance quadrable CLI command parity",
    "nextTask": "Decide whether the slow 500-trial sync-fuzzer probes should become optional CI evidence, or map another remaining low-risk CLI boundary if one is identified."
}
