{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 78,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 106 PHP behavior tests map upstream-compatible BLAKE2s/key semantics, sparse tree updates, iterator windows, proofs/ranges, proof-backed updates/mergeProof, bounded sync, tracked node ids, file-backed `quadb` command surfaces, upstream-shaped full/proof-backed LMDB bucket values, raw LMDB entry key/value byte snapshots, numeric/date-shaped head names preserved as LMDB string keys, binary/non-UTF-8 leaf values plus tracked keys preserved through JSON-backed reopen, upstream C++ LMDB cursor oracles for binary tracked-key named/detached proof-head raw bytes, noTrackKeys full/proof heads with empty `quadrable_key` buckets, mergeProof-retained and post-`quadb gc` pruned proof-import buckets, native portable raw-entry digests, portable dump/restore verification, and an upstream `mdb_dump`/`mdb_load` restored oracle for one mixed tracked/noTrack/full/proof/detached WordPress-shaped database.",
    "phpPass": 106,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered records, proofs/ranges, sync diffs, tracked node ids, file-backed preview heads, proof import/merge/update flows, noTrackKeys privacy behavior with empty raw LMDB key buckets via `wordpress-quadb-notrackkeys.php`, upstream-shaped LMDB bucket inspection via `wordpress-quadb-lmdb-layout.php`, raw LMDB backup manifests with binary serialized option payloads via `wordpress-quadb-lmdb-raw-entries.php`, portable mixed full/proof/detached/noTrack dump restore with raw-entry digests via `wordpress-quadb-portable-dump-restore.php`, numeric/date-shaped WordPress preview head names via `wordpress-quadb-numeric-heads.php`, and retained/pruned delegated proof-import raw cursor keys via `wordpress-quadb-proof-merge-gc.php`.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices; latest slice adds portable raw-entry digests and an upstream LMDB dump/load oracle for a mixed tracked full-head, noTrack full-head, named proof-head, noTrack proof-head, and detached proof-head store.",
    "audit": "audited 2026-05-23: upstream C++ `make -r test` passes all 34 scenarios. Quadrable-only PHP tests pass with 106 behavior tests, 2612 assertions, and 0 failures. Focused dump/load coverage verifies native raw cursor entries and portable raw-entry digest against `fixtures/upstream-lmdb-dump-restore-oracle.json`, generated from a real upstream C++ LMDB database restored through `mdb_dump -a` and `mdb_load -a`. Latest required repo-wide `php tools/run-tests.php` rerun passes with 182 test files, 17593 assertions, and 0 failures after one immediately previous moving-worktree sample reported 181 files, 17545 assertions, and 1 unrelated failure.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus native file-backed `QuadbStore`; latest work adds deterministic `rawEntryDigest` values to portable dumps, validates them on restore, and compares a mixed WordPress-shaped store to upstream C++ LMDB raw cursor entries after an actual `mdb_dump`/`mdb_load` cycle.",
    "blocker": "No current quadrable-specific upstream runner, quadrable-only PHP, or repo-wide PHP harness blocker. Remaining quadrable gap: native restore from raw LMDB cursor dumps is not implemented; current portable restore still requires the PHP JSON-backed state plus raw-entry verification.",
    "latestCommit": "current batch: quadrable dump/load oracle and portable raw-entry digest slice; quadrable-only PHP tests, upstream C++ runner, and root PHP harness passed",
    "nextTask": "Implement a native raw LMDB cursor dump importer for tracked full-head buckets, then extend it to proof-backed projected buckets."
}
