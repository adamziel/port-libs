{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 98,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 128 PHP behavior tests now map upstream-compatible BLAKE2s/key semantics, deterministic `quadb mineHash` bit-prefix mining, sparse tree updates, iterator windows, proofs/ranges, proof-backed updates/mergeProof, proof-backed partial `quadb export`/`export --int` lines with witness placeholders, bounded sync, tracked node ids, file-backed `quadb` command surfaces including no-argument docopt output, help/version metadata output, init stdout/stderr, advertised length no-op output, `root` missing/empty-directory startup behavior, `get` success/missing-key/missing-store output, `put`/`del` missing-store plus empty-stream write/overwrite/delete/missing-delete behavior, `import --int` missing-store plus `std::stoi`-style numeric-prefix/invalid input behavior, proof command output for invalid `exportProof --format`, upstream `from_hex` odd/empty/uppercase-prefix behavior, malformed proof decoding, empty proofs, premature varints, `importProof --root` command root decoding, binary `importProof --root`/`--dump` precedence, binary `mergeProof` missing-store/malformed/full-head/success output, and `checkout`/`fork [--from]` missing-store/silent-success/detached-empty/missing-source-head behavior; also upstream-shaped LMDB buckets/raw cursor entries, raw-entry-only restore, named/detached/noTrack raw-restored merge/GC oracles, a sequential multi-merge raw-restored named proof-head oracle, and mixed tracked/noTrack/full/proof/detached raw-restored merge/GC oracles for detached-current, named tracked-current, and named noTrack-current proof heads.",
    "phpPass": 128,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered records, proofs/ranges, sync diffs, tracked node ids, deterministic `mineHash` prefix fixtures via `wordpress-quadb-minehash-prefix.php`, file-backed preview heads, upstream-shaped `quadb --help`/`--version` metadata preflight via `wordpress-quadb-cli-metadata.php`, upstream-shaped `quadb init`/`length` bootstrap checks via `wordpress-quadb-init-length.php`, upstream-shaped `quadb root` startup guards via `wordpress-quadb-root-command-guard.php`, upstream-shaped no-argument/get read guards via `wordpress-quadb-get-guard.php`, upstream-shaped put/del write guards via `wordpress-quadb-put-del-guard.php`, upstream-shaped checkout/fork preview guards via `wordpress-quadb-checkout-fork-guard.php`, upstream-shaped import --int guards via `wordpress-quadb-import-int-guard.php`, proof import/merge/update flows, proof input/root/binary dump and binary merge guards via `wordpress-quadb-proof-input-guard.php`, proof-backed partial export manifests via `wordpress-quadb-partial-export.php`, noTrackKeys privacy behavior, upstream-shaped LMDB bucket/raw-entry inspection, raw-entry-only restore, sequential raw backup named-head merge verification via `wordpress-quadb-sequential-raw-merge.php`, mixed raw backup detached-head merge verification via `wordpress-quadb-mixed-raw-merge.php`, mixed raw backup named-head merge verification via `wordpress-quadb-mixed-named-raw-merge.php`, mixed raw backup noTrack private-head merge/privacy verification via `wordpress-quadb-mixed-notrack-raw-merge.php`, portable mixed dump restore, numeric/date-shaped preview heads, and retained/pruned delegated proof-import raw cursor keys.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices; latest slice maps `quadb checkout` and `quadb fork [--from]` command output and state transitions.",
    "audit": "audited 2026-05-23T09:53Z: upstream C++ `make -r test` passes all 34 scenarios after the checkout/fork command-output slice. Targeted upstream probes showed missing stores fail before creation, `checkout <head>`, bare `checkout`, `fork <head>`, detached fork, and `fork <head> --from=missing-head` all succeed with empty stdout/stderr on existing stores; bare checkout detaches to the empty root, and a missing source head forks an empty named head. Quadrable-only PHP tests pass with 10 files, 128 behavior tests, 3,004 assertions, and 0 failures. Example `examples/wordpress-quadb-checkout-fork-guard.php` records WordPress preview-head guards for silent success and fail-closed missing-store behavior. Repo-wide `php tools/run-tests.php` passed with 204 test files, 23,577 assertions, and 0 failures after a clear duplicate-root gate.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus native file-backed `QuadbStore`; latest work adds command-output parity for `quadb checkout` and `quadb fork [--from]`, preserving upstream silent success and fail-closed missing-store behavior.",
    "blocker": "No current quadrable-specific upstream runner, quadrable-only PHP, or repo-wide harness blocker. Full 500-trial sync-fuzzer probes remain outside the fast suite by design.",
    "latestCommit": "a2741b0b - Advance quadrable checkout fork command parity",
    "nextTask": "Map another low-risk CLI boundary such as status/head/gc/dumpTree command-output wrappers, or decide whether the slow 500-trial sync-fuzzer probes should become optional CI evidence."
}
