{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 12,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 29 PHP tests mapped to hashing/key, in-memory sparse tree updates, raw-key iterator windows, compact proof transport, selected proof import/export, integer proof sizing, range proofs, and proof-backed partial-tree updates",
    "phpPass": 29,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; current fixtures/examples map ordered WordPress option/post records through raw integer keys, iterator windows, compact authenticated range proofs, and proof-backed post updates from a narrow raw-key proof.",
    "phase": "upstream runner pass plus key, sparse tree, iterator window, compact proof/range, and proof-backed update slices",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` passes; native PHP parity is still partial and uses a SHA-256 hash surrogate",
    "currentWork": "Native hash/key primitives plus in-memory sparse tree get/put/delete, update batching, path-independent roots, getMulti, empty-head restoration, delete bubbling equivalence, raw-key iterator lower/upper-bound windows, compact proof encode/decode, selected proof import/export, integer proof sizing, raw-key range proofs, and proof-backed updates against imported partial trees.",
    "blocker": "No current upstream runner blocker: `make -r test` passes after installing gcc-c++, lmdb-devel, libb2-devel, and libasan and shallow-initializing the three upstream submodules. Plain `make test` still selects the built-in C++ rule in this GNU Make environment and omits repo include flags unless `-r` disables built-ins. PHP HashTree still uses a SHA-256 surrogate because this PHP build lacks BLAKE2s, so roots/proof bytes are not digest-compatible with upstream yet. mergeProof/sync and persisted LMDB node-id behavior remain unported.",
    "latestCommit": "b1dd9cf",
    "nextTask": "Port mergeProof expansion between separately imported partial proofs, then map sync request/response proof exchange scenarios."
}
