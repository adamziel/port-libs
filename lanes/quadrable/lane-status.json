{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 72,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 104 PHP behavior tests map upstream-compatible BLAKE2s/key semantics, sparse tree updates, iterator windows, proofs/ranges, proof-backed updates/mergeProof, bounded sync, tracked node ids, file-backed `quadb` command surfaces, upstream-shaped full/proof-backed LMDB bucket values, raw LMDB entry key/value byte snapshots, numeric/date-shaped head names preserved as LMDB string keys, binary/non-UTF-8 leaf values plus tracked keys preserved through JSON-backed reopen, and an upstream C++ LMDB cursor oracle for binary tracked-key/proof-head raw bytes plus mergeProof-retained and post-`quadb gc` pruned proof-import buckets.",
    "phpPass": 104,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered records, proofs/ranges, sync diffs, tracked node ids, file-backed preview heads, proof import/merge/update flows, noTrackKeys privacy behavior, upstream-shaped LMDB bucket inspection via `wordpress-quadb-lmdb-layout.php`, raw LMDB backup manifests with binary serialized option payloads via `wordpress-quadb-lmdb-raw-entries.php`, numeric/date-shaped WordPress preview head names via `wordpress-quadb-numeric-heads.php`, and retained/pruned delegated proof-import raw cursor keys via `wordpress-quadb-proof-merge-gc.php`.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices; latest slice captures an upstream C++ LMDB cursor oracle for mergeProof followed by `quadb gc` and verifies native retained/pruned proof-import bucket bytes, including preserved surviving LMDB node ids after GC.",
    "audit": "audited 2026-05-23: upstream C++ `make -r test` passes all 34 scenarios. Quadrable-only PHP tests pass with 104 behavior tests, 2561 assertions, and 0 failures. Focused `QuadbStoreTest` passes with upstream LMDB cursor oracle comparisons for binary proof heads and mergeProof before/after `quadb gc`. `php lanes/quadrable/examples/wordpress-quadb-proof-merge-gc.php` runs successfully. Required repo-wide `php tools/run-tests.php` passes with 174 test files, 16490 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus native file-backed `QuadbStore`; latest work persists post-GC proof projection metadata so `lmdbRawEntrySnapshot()` matches upstream retained node ids before and after mergeProof garbage pruning.",
    "blocker": "No current quadrable-specific upstream runner, quadrable-only PHP, example, or repo-wide PHP blocker. Remaining quadrable gap: broader byte-for-byte LMDB dump/restore parity across larger multi-head stores and detached proof-head combinations.",
    "latestCommit": "pending lane commit after mergeProof/quadb-gc LMDB cursor oracle batch; root PHP harness passed",
    "nextTask": "Extend upstream-backed LMDB byte parity to a larger multi-head dump/restore scenario with full heads, proof-backed heads, detached proof heads, and noTrackKeys buckets."
}
