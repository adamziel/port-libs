{
    "schemaVersion": 1,
    "library": "quadrable",
    "estimatedProgress": 58,
    "suiteProgress": "cloned upstream runner passes with `make -r test`; 90 PHP behavior tests map upstream-compatible BLAKE2s hashing/key behavior, PHP-native integer overflow rejection, in-memory sparse tree updates, raw-key `getMultiRaw`, FullKeys proof transport and partial enumeration, iterator windows, composite integer/hash keys, compact proof transport, proof/range import/export, proof-backed partial-tree updates, mergeProof expansion, bounded sync proof fragments, tracked node-id persistence, native file-backed `quadb` current-head/detached-head persistence, integer and string-key import/export/diff/patch lines, root/status/head/head-rm/stats output, proof export/import/merge persistence, proof dump text, unauthenticated importProof output, proof-head writes/deletes/forks, ordered proof/update replay for merging updated-root proofs after persisted proof-head writes, raw integer proof-backed head imports/puts/deletes across reopen, and upstream-shaped `quadb gc` reachability sweeps for discarded full heads while preserving named and detached roots.",
    "phpPass": 90,
    "phpFail": 0,
    "wordpressScenarios": "Authenticated local-first state sync for Playground snapshots and content databases; examples cover ordered WordPress option/post records, raw integer keys, raw-key batch lookups, FullKeys proof transport, composite post-meta keys, exact upstream-compatible BLAKE2s snapshot roots, iterator windows, range/sub proofs, proof-backed post updates, merged partial proofs, sync diffs and scan callbacks, tracked node-id reuse, memStore preview overlays, native file-backed `quadb` preview-head reloads, tracked patch lines, proof import/merge/update examples, `wordpress-large-proof-window.php`, `wordpress-quadb-proof-merge-after-update.php` for merging an updated-root delegated proof after a persisted partial-head edit, `wordpress-quadb-integer-proof-update.php` for persisted raw integer delegated preview edits, `wordpress-quadb-head-cleanup.php` for removing and garbage-collecting discarded WordPress preview heads without changing the approved preview root, `wordpress-quadb-stats.php` for reporting full-head and delegated proof-head stats, and `wordpress-quadb-unauthenticated-proof-import.php` for debugging a delegated proof and surfacing the upstream unauthenticated import warning/root before trusting preview data.",
    "phase": "upstream runner pass plus native BLAKE2s/key/sparse-tree/proof/range/sync/tracked-node/file-backed-quadb slices, now including ordered proof/update event history, raw integer proof-backed head updates, upstream-shaped `quadb gc` full-head reachability sweeps, upstream-shaped `quadb stats` output for full and proof-backed heads, and upstream-shaped proof dump plus unauthenticated importProof output",
    "audit": "audited 2026-05-22: upstream C++ `make -r test` reconfirmed all 34 scenarios after this batch. Quadrable-only PHP tests pass with 90 behavior tests, 2365 assertions, and 0 failures. Required repo-wide `php tools/run-tests.php` passes with 147 test files, 13096 assertions, and 0 failures.",
    "currentWork": "Pure-PHP Quadrable data structures and proof transport plus a native file-backed `QuadbStore`. The latest slice maps upstream `dumpProof` and unauthenticated `quadb importProof`: `Proof::dumpText()` emits upstream-shaped ITEMS/CMDS sections, `exportProofDumpText()` and `importProofHexDumpText()` expose non-mutating debug output, and `importProofHexOutputText()` returns the upstream `Imported UNAUTHENTICATED proof. Root: 0x...` line when no trusted root is supplied.",
    "blocker": "No current quadrable-specific upstream runner, native digest, quadrable-only PHP, or repo-wide PHP blocker. Upstream `make -r test` passes all 34 scenarios; quadrable-only PHP tests pass with 90 behavior tests, 2365 assertions, and 0 failures; repo-wide PHP passes with 147 test files, 13096 assertions, and 0 failures. Remaining quadrable gaps: broader LMDB persistence internals, raw/composite-key CLI ergonomics beyond the native API, binary/stdin CLI proof ergonomics, dumpTree output, and exact LMDB storage-layout parity.",
    "latestCommit": "current run: Port quadrable proof dump and unauthenticated import output",
    "nextTask": "Map binary/stdin CLI proof ergonomics, dumpTree output, or broader LMDB persistence internals; capture byte-level C++ oracle bytes for exact big-proof transport fixture parity if exact proof transport becomes the next priority."
}
