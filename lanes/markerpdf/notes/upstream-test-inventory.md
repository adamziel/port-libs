# markerPDF Upstream Test Inventory

Inventory source: shallow clone of `https://github.com/sddai/markerPDF` at `da6a2f5c9a7b1e92c82d85fbcf3680a79dd28a34` in `.upstream-cache/markerpdf`.

The manifest denominator now keeps `benchmarkDenominator.total` numeric at `78`; the long inventory explanation is recorded separately as `inventorySummary`. The clone check on 2026-05-24 19:13 UTC confirmed `git ls-tree -r --name-only HEAD | wc -l` returns `78`, with `0` committed Python unit test files found and the full upstream benchmark runner still blocked on Poetry plus heavy PDF/model dependencies.

## Reduced Handoff 2026-06-02 05:39 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-text-rendering-mode-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted JavaScript action-chain current base.
- Text rendering mode clipping slice: `PdfTextExtractor` now tracks the PDF `Tr` operator through text extraction, styled-span extraction, naive text output, and `q`/`Q` graphics-state save/restore. Non-painting modes `3` and `7` are excluded from visible text and marked-content `/ActualText` or `/Alt` replacement, while visible clipping modes `4`, `5`, and `6` remain extractable before WordPress paragraph rendering.
- Source truth: upstream `marker/pdf/extract_text.py::naive_get_text` delegates per-page text extraction to pypdfium/PDFium. PDF text rendering mode semantics make modes `3` and `7` non-painting text modes, while modes `4`, `5`, and `6` still paint glyphs and add their outlines to the clipping path. This native slice ports that visibility boundary without loading pypdfium, pdftext, Python models, or external PDF tools.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, and `php -l lanes/markerpdf/examples/wordpress-pdf-text-rendering-mode-import.php` passed; `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 file, 309 assertions, and 0 failures; `php lanes/markerpdf/examples/wordpress-pdf-text-rendering-mode-import.php` emitted six visible Gutenberg paragraphs while excluding invisible OCR text, clipping-only text, hidden `ActualText`, and scoped hidden text; `php tools/run-tests.php lanes/markerpdf/tests` passed with 56 files, 2066 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed.
- Counters: `phpPass` moves 393 -> 394 and mapped focused source/dependency semantics move 248 -> 249 / 78.
- Dependency closure: no new support component is needed; this reuses the existing native PDF content tokenizer, marked-content property handling, styled-span path, CMap/font lookup, and graphics-state stack. Full upstream benchmarks remain dependency-gated on Poetry plus pdftext, pypdfium2, Surya, tabled-pdf, Texify, Torch, Nougat comparison tooling, and model downloads.

## Reduced Handoff 2026-06-02 03:30 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-optional-content-layer-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted LZW DecodeParms, DCT/CMYK, and AcroForm handoff.
- Optional content group layer visibility slice: `PdfTextExtractor` now derives default view visibility from catalog `/OCProperties` `/D` configuration, applies `/BaseState`, `/ON`, and `/OFF` states for listed `/OCGs`, resolves page/form/appearance `/Resources /Properties` names, strips hidden `/OC ... BDC ... EMC` marked-content blocks, and skips hidden stream/Form XObject/annotation `/OC` entries before native WordPress paragraph extraction.
- Source truth: upstream `marker/pdf/extract_text.py::naive_get_text` delegates per-page text extraction to `pypdfium2`/PDFium text pages; PDFium `CPDF_OCContext` checks page-object marked content named `OC`, loads OCG state from `/OCProperties` default configuration, honors `/BaseState`, `/ON`, and `/OFF`, and treats groups missing from `/OCGs` as visible. This native slice ports the bounded default-view layer visibility behavior without loading pypdfium or external PDF tools.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, and `php -l lanes/markerpdf/examples/wordpress-pdf-optional-content-layer-import.php` passed; `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 file, 203 assertions, and 0 failures; `php lanes/markerpdf/examples/wordpress-pdf-optional-content-layer-import.php` emitted `Base Visible Text`, `Layer Visible Text`, and `Visible Form Text` while excluding hidden layer/Form XObject text; `php tools/run-tests.php lanes/markerpdf/tests` passed with 51 files, 1301 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed.
- Counters: `phpPass` moves 331 -> 332 and mapped focused source/dependency semantics move 206 -> 207 / 78.
- Dependency closure: no new support component is needed; this reuses the existing native PDF object parser, page-resource lineage lookup, Form XObject expansion, annotation appearance extraction, and content-token parser. Full upstream benchmarks remain dependency-gated on Poetry plus pdftext, pypdfium2, Surya, tabled-pdf, Texify, Torch, Nougat comparison tooling, and model downloads.

## Reduced Handoff 2026-06-02 00:07 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-identity-cmap-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted PDF page and structure handoffs.
- Type0 Identity-H/V font CMap slice: `PdfTextExtractor` now uses `/Encoding /Identity-H` and `/Encoding /Identity-V` code-space widths when no `/ToUnicode` CMap is present, so two-byte CID hex strings, literal strings, and `TJ` array strings tokenize before fallback Unicode decoding instead of rendering raw NUL bytes in WordPress paragraphs.
- Focused evidence after supervisor rebase: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-identity-cmap-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-identity-cmap-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 74 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 1026 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and CMap code-space fallback path.

## Reduced Handoff 2026-06-02 00:10 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-xobject-form-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted PDF page, structure, and Identity-H/V handoffs.
- Form XObject invocation slice: `PdfTextExtractor` now expands page resource `/XObject` entries only when the page content stream invokes a `/Subtype /Form` object with the `Do` operator, preserving execution order and keeping unreferenced form streams excluded before native WordPress paragraph extraction.
- Focused evidence after supervisor rebase: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-xobject-form-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-xobject-form-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 79 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 1031 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing native PDF object parser, stream decoder, and content-token parser.

## Reduced Handoff 2026-05-25 23:43 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-winansi-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted PDF font/CMap handoffs.
- Simple-font WinAnsiEncoding slice: `PdfTextExtractor` now decodes `/Encoding /WinAnsiEncoding` high-bit punctuation for simple fonts without `/ToUnicode`, including curly quotes, en/em dashes, bullets, ellipsis, trademark, Euro, and common Latin ligature characters before native WordPress paragraph extraction.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-winansi-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-winansi-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 63 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 996 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and adds only a small local WinAnsi encoding table. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-25 17:39 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-encoding-differences-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted PDF text/CMap handoffs.
- Simple-font Encoding Differences slice: `PdfTextExtractor` now decodes single-byte `/Encoding << /Differences [...] >>` glyph names when no `/ToUnicode` CMap is present, including ordinary Adobe glyph names and bounded `uniXXXX`/`uXXXX` forms before native WordPress paragraph extraction.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-encoding-differences-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-encoding-differences-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 61 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 994 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and adds only a small local glyph-name mapping table. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-25 06:05 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-cmap-codespace-fallback-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted CMap and literal-string handoffs.
- ToUnicode codespacerange fallback slice: `PdfTextExtractor` now chooses source-code widths from `begincodespacerange` before applying mappings, so an unmapped two-byte CID is not split by an unrelated one-byte mapping before native WordPress paragraph extraction.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-cmap-codespace-fallback-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-cmap-codespace-fallback-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 59 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 992 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and ToUnicode CMap lookup. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-25 03:19 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-cmap-usecmap-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted stream-filter and ToUnicode handoffs.
- ToUnicode usecmap slice: `PdfTextExtractor` now collects named CMap streams and resolves `/BaseName usecmap` inheritance before local ToUnicode entries, so derived CMaps can reuse base glyph mappings and still supply local overrides before native WordPress paragraph extraction.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-cmap-usecmap-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-cmap-usecmap-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 50 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 983 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and ToUnicode CMap lookup. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-25 02:36 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-cmap-bfrange-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted stream-filter and ToUnicode handoffs.
- ToUnicode bfrange-array slice: `PdfTextExtractor` now parses explicit destination arrays in `beginbfrange` CMap blocks, so encoded glyph IDs can map to non-sequential Unicode strings before native WordPress paragraph extraction.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-cmap-bfrange-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-cmap-bfrange-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 48 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 981 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and ToUnicode CMap lookup. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-25 01:28 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-escaped-name-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted stream-filter and ToUnicode handoffs.
- PDF name escape slice: `PdfTextExtractor` now decodes `#XX` escapes in PDF name tokens for stream filter names and font resource names, so escaped names such as `/Fl#61teDecode` and `/F#31` resolve before native stream decoding and ToUnicode text lookup.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-escaped-name-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-escaped-name-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 46 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 979 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser, stream-filter chain, and ToUnicode lookup. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-25 01:02 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-flate-predictor-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted stream-filter handoffs.
- Flate predictor slice: `PdfTextExtractor` now applies `/FlateDecode` `/DecodeParms` predictors after inflation, including TIFF Predictor 2 horizontal differencing, PNG Predictor 10-15 row filters 0-4, `Columns`/`Colors`/`BitsPerComponent` row sizing, and malformed predictor row rejection before native text-token parsing.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-flate-predictor-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-flate-predictor-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 43 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 976 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and stream-filter chain. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-25 00:25 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-runlength-filter-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted ASCIIHex, literal-escape, indirect Filter/DecodeParms, and ASCII85 handoffs.
- RunLength stream-filter slice: `PdfTextExtractor` now decodes `/RunLengthDecode` and `/RL` content streams, including literal runs, repeated-byte runs, EOD termination, malformed truncation rejection, and ordered `/RunLengthDecode` then `/FlateDecode` filter arrays before native text-token parsing. This keeps another ordinary encoded content-stream path on the native `naive_get_text` boundary used by WordPress import examples.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-runlength-filter-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-runlength-filter-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 40 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 973 assertions, and 0 failures, and `git diff --check -- lanes/markerpdf` passed.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and stream-filter chain. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-06-02 01:04 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, and `wordpress-pdf-lzw-filter-import.php` on top of the accepted pdftext dictionary-core handoff.
- LZW stream-filter slice: `PdfTextExtractor` now decodes `/LZWDecode` content streams before native text-token parsing, covering clear/end codes, variable code widths, and the KwKwK dictionary edge so ordinary LZW-compressed PDF text imports into WordPress without Python, pdftext, pypdfium, Poppler, Ghostscript, or external PDF tools.
- Focused evidence after supervisor rebase: `php -l` passed for the changed extractor, focused test, and example; `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 file, 107 assertions, and 0 failures; `php lanes/markerpdf/examples/wordpress-pdf-lzw-filter-import.php` emitted `LZW PDF Import` and `Native Blocks`; full markerPDF lane passed with 47 files, 1076 assertions, and 0 failures; `git diff --check --cached -- lanes/markerpdf` passed.
- Dependency closure: no new support component is needed; this reuses the native PDF object parser, stream-filter dispatch, and text-token parser.

## Reduced Handoff 2026-05-24 23:37 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-ascii85-filter-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted ASCIIHex, literal-escape, and indirect Filter/DecodeParms handoffs.
- ASCII85 stream-filter slice: `PdfTextExtractor` now decodes `/ASCII85Decode` and `/A85` content streams, including optional `<~ ~>` delimiters, whitespace, `z` zero groups, partial final groups, and ordered `/ASCII85Decode` then `/FlateDecode` filter arrays before native text-token parsing. This keeps another ordinary encoded content-stream path on the native `naive_get_text` boundary used by WordPress import examples.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-ascii85-filter-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-ascii85-filter-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 36 assertions, and 0 failures, and `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 969 assertions, and 0 failures.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and stream-filter chain. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-24 23:20 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-indirect-filter-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted ASCIIHex and literal-escape handoffs.
- Indirect filter/DecodeParms slice: `PdfTextExtractor` now resolves `/Filter` names and filter arrays through indirect objects, resolves direct and indirect `/DecodeParms` entries, accepts `null` or `/Predictor 1` parameters, and rejects unsupported predictor parameters instead of silently decoding incorrectly. This keeps ordinary indirect encoded content streams on the native text extraction boundary used by WordPress import examples.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-indirect-filter-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-indirect-filter-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 32 assertions, and 0 failures, and `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 965 assertions, and 0 failures.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser and object inventory. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-24 22:58 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-literal-escape-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted ASCIIHex stream-filter handoff.
- Literal-string escape slice: `PdfTextExtractor` now decodes PDF literal escapes in `Tj` and `TJ` text operands, including escaped delimiters, octal bytes, CRLF line continuations, nested literal strings in `TJ` arrays, and unknown escaped bytes before native line assembly. This keeps escaped content-stream text on the same native `naive_get_text` boundary that upstream reaches through pypdfium text pages.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-literal-escape-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-literal-escape-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 29 assertions, and 0 failures, and `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 962 assertions, and 0 failures.
- Acceptance blocker remains integrator/root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. Dependency closure: no new support component is needed; this reuses the existing bounded native `PdfTextExtractor` content-stream parser. Broader searchable PDF dictionary extraction should reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens.

## Reduced Handoff 2026-05-24 20:08 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-asciihex-filter-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted `Tm` horizontal-scale handoff.
- ASCIIHex stream-filter slice: `PdfTextExtractor` now decodes `/ASCIIHexDecode` content streams and applies ordered filter arrays for `/ASCIIHexDecode` then `/FlateDecode` before native text-token parsing. This keeps encoded content streams on the same native `naive_get_text`-style extraction boundary that upstream reaches through pypdfium text pages.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-asciihex-filter-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-asciihex-filter-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 24 assertions, and 0 failures, and `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 957 assertions, and 0 failures.
- Accepted integration evidence for this reduced slice is `5e46840f`, with focused markerPDF checks green and the serialized no-argument root harness passing `338` test files, `48339` assertions, and `0` failures without a lock-wait notice. Full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates remain blockers for broader markerPDF claims. The dependency checkpoint for broader PDF text extraction is to reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens; no new support component is activated by this stream-filter slice.

## Reduced Handoff 2026-05-24 19:52 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-tm-horizontal-scale-import.php`, and lane-owned manifest/status/notes refreshes on top of the accepted text-positioning handoff.
- Tm horizontal-scale slice: `PdfTextExtractor` now multiplies a non-identity `Tm` matrix horizontal scale into the native end-X estimate used by later same-line `Tm` gap decisions. Widened matrix text keeps suffixes joined (`Import Profiles`), while compressed matrix text can still create a readable next-word gap (`SiteMap Index`).
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-tm-horizontal-scale-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-tm-horizontal-scale-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 22 assertions, and 0 failures, and `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 955 assertions, and 0 failures.
- Integrator acceptance: focused PHP checks passed, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 22 assertions, and 0 failures, `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 955 assertions, and 0 failures, and integration-owned `php tools/run-tests.php` passed with 341 test files, 50811 assertions, and 0 failures. No lock-wait message was printed. Remaining blockers are full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates for broader markerPDF claims. The dependency checkpoint for broader PDF text extraction is to reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens; no new support component is activated by this Tm horizontal-scale slice.

## Reduced Handoff 2026-05-24 19:30 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-tj-positioning-import.php`, and lane-owned manifest/status/notes refreshes on top of the prior reduced text-position handoff.
- TJ positioning slice: `PdfTextExtractor` now parses `TJ` array string and numeric elements. Numeric adjustments feed the same native end-X estimate used by later same-line `Tm` gap decisions, so negative adjustments can keep a suffix joined (`Import Profiles`) while positive adjustments can preserve a readable next-word gap (`SiteMap Index`).
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-tj-positioning-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-tj-positioning-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 19 assertions, and 0 failures, and `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 952 assertions, and 0 failures.
- Acceptance blocker remains root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates. The dependency checkpoint for broader PDF text extraction is to reuse the existing `pdf-text-dictionary-core` row only when that accepted rich slice opens; no new support component is activated by this TJ slice.

## Reduced Handoff 2026-05-24 19:13 UTC

- Scope adds only `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-graphics-state-import.php`, and lane-owned manifest/status/notes refreshes on top of the prior reduced text-position handoff.
- Graphics-state slice: `q` and `Q` now save and restore the native text-state fields used by same-line positioning (`Tf`, `TL`, `Tc`, `Tw`, and `Tz`). A scoped `Tc` change no longer leaks into later positioned fragments such as `Import`/`Tool`.
- Focused evidence: `php -l lanes/markerpdf/src/PdfTextExtractor.php`, `php -l lanes/markerpdf/tests/PdfTextExtractorTest.php`, `php -l lanes/markerpdf/examples/wordpress-pdf-graphics-state-import.php`, `php lanes/markerpdf/examples/wordpress-pdf-graphics-state-import.php`, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 16 assertions, and 0 failures, and `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 949 assertions, and 0 failures.
- Acceptance blocker remains root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates.

## Reduced Handoff 2026-05-24 19:03 UTC

- Scope is intentionally limited to `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-text-state-spacing-import.php`, this manifest/status/notes refresh, and no root coordination files.
- Foundation batch: minimal text-position state for `BT`/`ET`, `Tf`, `TL`, `Td`/`TD`, `Tm`, `T*`, and a simple text-end X estimate before same-line `Tm` gap decisions. Evidence: `uses text advance before same-line Tm gap decisions for WordPress paragraph rendering`.
- Spacing slice: `Tc`, `Tw`, `Tz`, and double-quote showing operands feed that text-end estimate so fragments such as `Data`/`base`, `Import Profile`/`s`, and `Media Import`/`er` stay joined while larger same-line gaps still become readable spaces.
- Focused evidence: `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 test file, 14 assertions, and 0 failures; `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 947 assertions, and 0 failures. The WordPress example emitted Gutenberg paragraphs for `Database`, `Intro`, `Import Profiles`, and `Media Importer` without Python, pdftext, pypdfium, Poppler, Ghostscript, or external PDF tools.
- Acceptance blocker remains root aggregate verification plus full upstream runner parity and the inactive `pdf-text-dictionary-core`, `layout-ocr-result-core`, and `table-geometry-core` gates.

## Counted Denominator

- Repository files inventoried with `git ls-tree`: 78.
- Targeted lane-relevant workflow/benchmark/example/source/runtime/support paths inspected with `git ls-tree` and targeted `git show`: 78.
- Python files: 47, including 39 files under `marker/`.
- Committed Python unit tests: 0 found.
- CI benchmark workflow: 1 integration workflow, `.github/workflows/tests.yml`.
- Benchmark/scoring scripts inspected: `benchmarks/overall.py`, `marker/benchmark/scoring.py`, `marker/benchmark/table.py`, and `scripts/verify_benchmark_scores.py`. The upstream `benchmarks/overall.py` PDF enumeration, reference loading, method conversion loop, optional Markdown-output write path, and report aggregation shape are now mapped natively for supplied conversion text.
- Benchmark scoring functions inspected: 6 (`chunk_text`, `overlap_score`, `score_text`, `split_to_cells`, `align_rows`, and `score_table`).
- Cleaner/postprocessor/source utilities inspected: 7 cleaner modules with 17 functions under `marker/cleaners/`, 8 functions in `marker/postprocessors/markdown.py`, `marker/layout/layout.py`, `marker/layout/order.py`, `marker/pdf/extract_text.py`, `marker/pdf/utils.py`, `marker/schema/page.py`, `marker/debug/data.py`, `marker/debug/render.py`, `marker/ocr/heuristics.py`, `marker/ocr/utils.py`, `marker/ocr/recognition.py`, `marker/equations/equations.py`, `marker/equations/inference.py`, `marker/tables/table.py`, `marker/tables/utils.py`, `marker/models.py`, `marker/utils.py`, `marker/logger.py`, `marker_app.py`, `run_marker_app.py`, `marker_server.py`, top-level `convert.py`, top-level `convert_single.py`, top-level `chunk_convert.py`, and top-level `chunk_convert.sh`.
- Locked table helper dependency inspected: `tabled-pdf==0.1.4` from `poetry.lock`; the source tarball `tabled_pdf-0.1.4.tar.gz` was downloaded to `/tmp/markerpdf-tabled-src` for targeted inspection only (28,889 bytes, sha256 `23af02dd04f64fcbda3c5fe67fbee740ab7cf12277e3fd949c40000c5b63a30d`). `tabled/inference/detection.py::merge_boxes` and `merge_tables` are now mapped because `marker/tables/table.py::get_table_boxes` imports that helper for adjacent table boxes. The table-recognition boundary also inspected `tabled/inference/recognition.py::{get_cells,recognize_tables}`, `tabled/assignment.py::{assign_rows_columns,merge_multiline_rows,handle_rowcol_spans}`, `tabled/heuristics/cells.py::{cluster_coords,find_column_separators,assign_cells_to_columns}`, `tabled/formats/__init__.py::formatter`, `tabled/formats/markdown.py::markdown_format`, and `tabled/formats/common.py::{sort_cells,sort_within_cell}`.
- Cleaner functions mapped: `marker/cleaners/bullets.py::replace_bullets`, `marker/cleaners/text.py::cleanup_text`, `marker/cleaners/fontstyle.py::find_bold_italic`, five focused functions from `marker/cleaners/headers.py` (`filter_common_elements`, `filter_header_footer`, `replace_leading_trailing_digits`, `find_overlap_elements`, and `filter_common_titles`), four focused functions from `marker/cleaners/code.py` (`is_code_linelen`, `comment_count`, `identify_code_blocks`, and `indent_blocks`), all three functions from `marker/cleaners/headings.py` (`split_heading_blocks`, `bucket_headings`, and `infer_heading_levels`), and both functions from `marker/cleaners/toc.py` (`get_pdf_toc` and `compute_toc`).
- Markdown postprocessor functions mapped: 8 of 8 (`escape_markdown`, `surround_text`, `merge_spans`, `block_surround`, `line_separator`, `block_separator`, `merge_lines`, and `get_full_text`). `MarkdownPostProcessor::mergeSpans` now maps the full upstream page-level wrapper, including skipped empty-span lines, lowercased font capture, first/last-span emphasis guards, next-substantial-span lookahead, block metadata preservation, and the empty-page Text fallback. `block_surround` title casing now preserves Python-style hyphen capitalization for headings such as `Perspective-Free`.
- Layout utilities mapped: all three functions from `marker/layout/layout.py` (`get_batch_size`, the supplied-prediction boundary of `surya_layout`, and `annotate_block_types`), plus all three functions from `marker/layout/order.py` (`get_batch_size`, the supplied-prediction boundary of `surya_order`, and `sort_blocks_in_reading_order`) and `marker/pdf/utils.py::sort_block_group`, including layout and ordering batch-size overrides/multiplier truncation, supplied layout/order prediction assignment with upstream `zip` semantics, `ORDER_MAX_BBOXES` planning before ordering, layout-label assignment by maximum bbox overlap, closest annotated-block fallback, default Text fallback, consecutive same-layout block merging, order-position assignment by maximum bbox overlap, vertical-bucket/horizontal row sorting, fallback ordering for unpositioned blocks, and header/footer pinning.
- Debug data export mapped: `marker/debug/data.py::dump_bbox_debug_data` is represented by `DebugDataExporter`, including the upstream `DEBUG` gate, document-base `_bbox.json` file naming, top-level `images` exclusion, `layout.segmentation_map` exclusion, `text_lines.heatmap`/`affinity_map` exclusion, and bbox/layout/text-line JSON persistence for WordPress review metadata. `marker/debug/data.py::draw_page_debug_images` and `draw_layout_page_debug_images` are represented by `DebugPageImagePlanner`, including DEBUG gating, document-base debug folder naming, `layout_page_N.png` and `pdf_page_N.png` artifact names, text-line image sizing, PDF-line text overlays, detector-line boxes, layout labels, order labels, and block overlays. Debug render overlay planning is mapped from `marker/debug/render.py::render_on_image` by `DebugRenderPlanner`, including integer bbox coercion, `draw_bbox`, scalar/per-box colors, label offsets, white label backgrounds, and the upstream zero-size text guard at a supplied text-size boundary. Native raster drawing, font download, and PIL image mutation remain unported.
- PDF utility and dictionary conversion mapped: all 3 functions from `marker/pdf/utils.py` (`find_filetype`, `font_flags_decomposer`, and `sort_block_group`) plus all 4 focused functions from `marker/pdf/extract_text.py` (`pdftext_format_to_blocks`, the supplied-data boundary for `get_text_blocks`, `naive_get_text`, and `get_length_of_text`). `find_filetype` is represented by magic-MIME detection that maps PDF-like MIME types to `pdf`, maps configured non-PDF MIME types through `SUPPORTED_FILETYPES`, and returns `other` for unknown or unsupported WordPress uploads. The pdftext dictionary conversion covers PDF font flag suffix construction, trailing newline/carriage-return trimming, `-\n` span cleanup, sequential `pnum_spanid` assignment, one Marker block per pdftext line, negative-area line skipping after span IDs are consumed, rotation-aware page bbox normalization, and original char-block preservation. The supplied `get_text_blocks` boundary covers `start_page`, `max_pages`, over-long max-page truncation, falsey `max_pages=0`, relative span-id enumeration over the selected page range, original document page numbers, and PDF TOC metadata handoff without loading pdftext/pypdfium. The native `naive_get_text`/`get_length_of_text` boundary appends one newline per page-text stream, trims before length counting, and supports `convert.py` min-length gates before a WordPress import job queues the heavier model stack. The latest reduced handoff adds six focused pdftext-boundary behaviors: text-end advance before same-line `Tm` gap decisions, `Tc`/`Tw`/`Tz`/double-quote spacing in that advance estimate, `q`/`Q` scoped text-state restore for those positioning fields, `TJ` array numeric positioning adjustments before later same-line `Tm` gap decisions, non-identity `Tm` horizontal scaling in the same end-X path, and `ASCIIHexDecode` stream-filter decoding before text-token parsing.
- OCR heuristic utilities mapped: all four focused functions from `marker/ocr/heuristics.py` (`should_ocr_page`, `detect_bad_ocr`, `no_text_found`, and `detected_line_coverage`) plus `marker/ocr/utils.py::alphanum_ratio`.
- OCR language utilities mapped: `marker/ocr/lang.py::replace_langs_with_codes`, `marker/ocr/lang.py::validate_langs`, and `marker/ocr/lang.py::langs_to_ids`, backed by `marker/ocr/tesseract.py`'s 93-entry Tesseract language map, the 94-entry Surya language map, and the locked Surya recognition tokenizer constants from `surya-ocr==0.6.13`. `langs_to_ids` is represented natively at the language-token boundary without loading the Surya model stack.
- OCR detection orchestration mapped from `marker/ocr/detection.py`: `get_batch_size` and the `surya_detection` prediction-assignment boundary are represented natively with supplied Surya line detections. The native boundary computes the same detector batch-size/multiplier value and applies upstream Python `zip` semantics when writing predictions to `page.text_lines`; the Surya detector model call remains unported.
- OCR recognition orchestration mapped from `marker/ocr/recognition.py`: `get_batch_size`, the `run_ocr` page-selection/statistics/replacement boundary, and the pure supplied-boundary half of `surya_recognition` are represented natively. The new Surya slice scales detector polygons from `SURYA_DETECTOR_DPI` to `SURYA_OCR_DPI`, drops zero-area polygons before recognition, computes recognition batch size with the upstream multiplier, and reconstructs Marker Page/Block/Line/Span arrays from supplied OCR text-line results. The Surya model call and Tesseract/OCRmyPDF recognition bodies remain unported because upstream loads `surya.ocr`, pypdfium2 rendering, OCRmyPDF/Tesseract subprocess integration, and model processors.
- Block span filtering mapped from `marker/schema/block.py` and the `marker/convert.py` cleanup order: `Block.filter_spans` removes known header/footer span IDs and drops newly empty lines, while `Block.filter_bad_span_types` clears text spans from configured bad block types such as `Page-header`, `Page-footer`, and `Picture` without discarding the block metadata that later WordPress media review can still use. Both native filters now also mirror upstream line-list compaction when every span is kept, so already-empty extracted lines do not survive into WordPress block rendering.
- Page helper inspection mapped from `marker/schema/page.py`: `get_all_lines`, `get_nonblank_lines`, `get_nonblank_spans`, `get_font_sizes`, `get_line_heights`, and `prelim_text` are represented by `PageInspector` for native Page arrays before WordPress review metadata is emitted.
- Late `convert_single_pdf` finalization mapped from `marker/convert.py`: `ConversionFinalizer` now applies the native span-filter cleanup order, code-block cleanup, supplied equation replacement after span/code cleanup, supplied image payload insertion after bad-span filtering, heading split/level inference, font-style marking, computed TOC generation, `merge_spans`, `merge_lines`, common-title filtering, `get_full_text`, `cleanup_text`, `replace_bullets`, and `images_to_dict` over supplied pages after the upstream OCR/layout/table/equation model boundaries.
- Early `convert_single_pdf` orchestration mapped from `marker/convert.py`: `CorePdfConverter` now applies metadata language override, OCR language normalization/validation, `OCR_ALL_PAGES` folding, magic-MIME filetype metadata, unsupported-filetype short-circuiting, supplied page/TOC metadata, start-page-aware low-resolution image planning, and supplied downstream pipeline handoff without loading pdftext, pypdfium2, Surya, Texify, tabled, or Torch.
- Document-level supplied conversion mapped from `marker/convert.py`: `SuppliedDocumentConverter` now composes supplied pdftext dictionaries, layout predictions, ordering predictions, recognized table dictionaries, table Markdown insertion, supplied equation result dictionaries, supplied image payloads, upstream-shaped no-OCR `ocr_stats` metadata, the upstream zero-extracted-block short-circuit after OCR, and the late finalizer into a BenchmarkRunner-ready native callback. The supplied `switch_trans.pdf` Contents page slice maps one upstream committed Marker table structure through this path, replacing extracted TOC text with one recognized Markdown table while preserving computed heading metadata. The newest forced-OCR table slice maps the upstream `OCR_ALL_PAGES`/`detect_boxes` table-cell redetection boundary by routing supplied detector cells and OCR text through `get_cells` and `recognize_tables` before Markdown table insertion. This covers the convert-single-pdf pipeline order over supplied model/data boundaries while still explicitly excluding live pdftext, pypdfium2, Surya, tabled, Texify, Torch, and Nougat execution.
- Image insertion helpers mapped from `marker/images/save.py`, `marker/images/extract.py`, `marker/schema/block.py`, and `marker/schema/bbox.py`: deterministic image filenames, page image dictionary export, nearest-block fallback insertion, Figure/Picture layout-region matching, intersecting text-span clearing, and Markdown image-span insertion. Native PHP does not raster-render PDF crops yet; upstream uses `pypdfium2` and PIL for that part.
- Marker app image preview helpers mapped from `marker_app.py`: `img_to_html` converts supplied PNG bytes into base64 `data:image/png` `<img>` tags, and `markdown_insert_images` replaces Marker's Markdown image references while preserving missing or unsupported paths. Native PHP accepts supplied image bytes and does not run Streamlit or pypdfium page-preview rendering.
- Marker app PDF upload preview helpers mapped from `marker_app.py`: `open_pdf`, `page_count`, and `get_page_image` are represented by `MarkerAppPreview`, including uploaded PDF byte parsing, catalog-order page counting, inherited/direct `MediaBox` handling, upstream one-based UI page numbers to zero-based `page_indices`, `dpi / 72` scale, RGB conversion, and pypdfium default annotation behavior. Native PHP emits a review plan and still does not rasterize page pixels through pypdfium/PIL.
- Marker API server helpers mapped from `marker_server.py`: `CommonParams` default request fields, `convert_pdf` local-mode option guard, `convert_pdf_from_upload` application/pdf validation plus temporary upload cleanup, `convert_pdf_local` success/error response shape with base64 image payloads, and `convert_pdf_remote` Datalab form payload plus original `request_check_url` polling are represented by `MarkerServerAdapter`. Native PHP accepts supplied local/remote callbacks and does not run FastAPI, Uvicorn, requests, or upstream model loading.
- PDF image render planning mapped from `marker/pdf/images.py`: `render_image`'s `dpi / 72` scale, `draw_annots=False`, RGB output boundary, and `render_bbox_image`'s page-bbox-to-rendered-image crop scaling through `rescale_bbox`. Native PHP still does not raster-render or crop pixels; it emits the deterministic crop plan that downstream WordPress media workflows can store as review metadata.
- BBox geometry helpers mapped from `marker/schema/bbox.py`: adjacent-box merge checks, merged box extents, strict intersection checks, box1-area intersection percentage, multiple-box intersection, 0-1000 normalized box expansion, center calculation, rescaling between detected image and PDF page coordinate spaces, and BboxElement-style height/width/start/area/distance validation metrics.
- Equation replacement helpers mapped from `marker/equations/equations.py`, `marker/equations/inference.py`, `marker/schema/block.py`, and `marker/schema/bbox.py`: Formula layout-region discovery, bbox rescaling, intersecting line removal, nearest-block fallback insertion, Texify model token-limit guards, insertion-point adjustment, LaTeX prediction validation, Formula block insertion, original-text fallback on failed predictions, converted span capture, and document-level equation metadata. Native PHP accepts supplied LaTeX predictions and uses a deterministic token-count boundary; upstream Texify tokenizer/model inference and equation crop rendering remain unported.
- Table crop planning, recognition handoff, and formatting mapped from `marker/tables/table.py`: `get_table_boxes` now maps high-resolution table crop planning from supplied Table layout boxes, locked `tabled-pdf` adjacent-box merge semantics, tiny-box filtering, page/document index accounting, text-line duplication per table, and null text-line payloads for OCRed pages. `TableRecognizer` maps the pure-Python boundary around `get_cells`, `recognize_tables`, `assign_rows_columns`, `merge_multiline_rows`, `handle_rowcol_spans`, `tabled.heuristics.cells` DBSCAN-style column separator clustering for heuristic fallback, and `formatter("markdown", ...)` with supplied text-line cells filtered to each table bbox, supplied detector cells filtered to positive-area bboxes before OCR, `needs_ocr` flags, supplied OCR text, row/column geometry, assigned-column multiline continuation row merging, multi-column header spans, row-spanning side labels, cleanup, Unicode character-width padding matching upstream `tabulate`, and Markdown output. The post-recognition branch of `format_tables` now consumes the same merged table regions as `get_table_boxes`, maps Table layout-region bbox rescaling for intersection checks, removes all intersecting source Table blocks, calculates upstream insertion points, constructs Markdown table blocks from supplied recognition output, and skips recognized tables when no Table block intersects. Native PHP still does not run Surya/tabled neural cell detection or recognition; it accepts supplied model-boundary inputs for now.
- Table utility cleanup mapped from `marker/tables/utils.py`: `sort_table_blocks` vertical midpoint bucket ordering with left-to-right row sorting, `replace_dots` long dot-leader cleanup, and `replace_newlines` table cell newline cleanup before WordPress table rendering.
- Output artifact helpers mapped from `marker/output.py`: `get_subfolder_path`, `get_markdown_filepath`, `markdown_exists`, and `save_markdown`, covering per-document output folders, converted Markdown paths, metadata JSON, and PNG image artifact writes for downstream WordPress import handoff.
- Top-level batch conversion mapped from `convert.py`: `BatchConverter` represents input-folder file discovery, `ceil(len(files) / num_chunks)` chunk sizing, chunk-index slicing, post-chunk `--max` limiting, `--metadata_file` JSON loading, basename metadata lookup from that metadata file, existing Markdown skip, `min_length` filetype/text-length preflight, non-empty output persistence through `OutputWriter`, empty-output skip, and nonfatal conversion error reporting. Native PHP accepts a supplied converter callback instead of loading Python models or multiprocessing workers.
- Top-level single-document conversion mapped from `convert_single.py`: `SingleDocumentConverter` represents comma-separated `--langs` parsing, `max_pages`/`start_page`/`batch_multiplier` option handoff, basename output saving through `OutputWriter`, tuple or named supplied conversion result normalization, elapsed-time reporting, and empty-output persistence. Native PHP accepts a supplied converter callback instead of loading models with `load_all_models()`.
- Top-level chunk conversion mapped from `chunk_convert.py` and `chunk_convert.sh`: `ChunkConversionPlanner` represents required `NUM_DEVICES`, `NUM_WORKERS`, input-folder, and output-folder validation, one Marker job per device, `CUDA_VISIBLE_DEVICES`/`DEVICE_NUM` environment assignment, `--num_chunks`, `--chunk_idx`, `--workers`, optional `--metadata_file`, optional `--min_length`, and the five-second launch-delay boundary. Native PHP emits queueable job data and does not execute the upstream `marker` subprocess.
- Settings defaults and computed helpers mapped from `marker/settings.py`: conversion defaults, supported PDF MIME mapping, image extraction/pagination/OCR flags, page separator and bad span type settings, `TORCH_DEVICE_MODEL` CPU fallback, CUDA/dtype helpers represented without importing Torch, pydantic-style typed environment overrides, `extra=ignore`, and the `OCR_ENGINE` literal boundary.
- Model setup utility boundaries mapped from `marker/models.py` and `marker/utils.py`: `ModelPipelinePlanner` records the upstream `PYTORCH_ENABLE_MPS_FALLBACK` import-time environment flag, seven model helper/load functions (`setup_table_rec_model`, `setup_recognition_model`, `setup_detection_model`, `setup_texify_model`, `setup_layout_model`, `setup_order_model`, and `load_all_models`), model/processor loader names, checkpoint/device/dtype argument shapes, processor attachment, the `convert_single_pdf` model-list order, and the CUDA-only `flush_cuda_memory` empty-cache boundary without importing Torch, Surya, Texify, or tabled.
- Runtime utility boundaries mapped from `marker/logger.py`, `run_marker_app.py`, and `marker_app.py`: `MarkerRuntimePlanner` records `configure_logging` root/logger/warning suppression, the Streamlit command and environment overlay (`IN_STREAMLIT=true`, `PDFTEXT_CPU_WORKERS=1`) that upstream would pass to `subprocess.run`, and the Marker app import-time environment setup including `PYTORCH_ENABLE_MPS_FALLBACK=1`. Native PHP records this as WordPress worker preflight metadata and does not launch Streamlit or Python model code.
- Published benchmark documents in the README accuracy table: 6 (`multicolcnn.pdf`, `switch_trans.pdf`, `thinkpython.pdf`, `thinkos.pdf`, `thinkdsp.pdf`, `crowd.pdf`).
- CI benchmark archive inspected: `.github/workflows/tests.yml` downloads `benchmark_data_short.zip` from Google Drive ID `1NHrdYatR1rtqs2gPVfdvO0BAvocH8CJi`; the downloaded archive was 6,212,657 bytes with sha256 `c7511a4f5055e949a7a7c293be5541942433059d7841965f056d7f9b441a41ad`.
- Actual external CI benchmark pairs mapped from that archive: `benchmark_data/pdfs/multicolcnn.pdf` (851,968 bytes, sha256 `2b0e8314ff2c2680dd309ce46a49d740084d66eb39549337d2daa91215c426f8`) with `benchmark_data/references/multicolcnn.md` (30,542 bytes, sha256 `3ff96757b43e82595410f0fa50643945fec7c5c51f7c3d7562edefbd555aaa96`), and `benchmark_data/pdfs/switch_trans.pdf` (1,304,157 bytes, sha256 `f340f6ace31abf7d0730ef461404279f40d3c890e9cc2daeb7068b3304afdbd6`) with `benchmark_data/references/switch_trans.md` (82,456 bytes, sha256 `74f8f6cbe23873304aa182b3a46edc8df896d35542aeade95bb93254823d58e1`).
- CI score assertions: 3 thresholds in `scripts/verify_benchmark_scores.py` (`multicolcnn.pdf > 0.34`, `switch_trans.pdf > 0.40`, and average table report score `>= 0.7`), now represented natively by `BenchmarkReportVerifier`. The `benchmarks/overall.py` `files`, `avg_score`, `time_per_page`, and `time_per_doc` report aggregation is represented by `BenchmarkReportBuilder`, and the outer benchmark loop is represented by `BenchmarkRunner` over supplied native conversion callbacks.
- Committed markdown examples: 4 marker outputs and 4 nougat outputs under `data/examples`.

The lane manifest now counts all 78 tracked upstream repository paths as the static cloned inventory and 78 targeted upstream lane-relevant paths for dashboard purposes: workflows, benchmark/scoring scripts, committed example outputs, cleaner/postprocessor/layout/OCR/image/PDF-image-render/equation/table/output/settings/schema/debug/source/model/runtime utility modules, `marker/convert.py`, `marker/models.py`, `marker/utils.py`, `marker/logger.py`, `marker_app.py`, `run_marker_app.py`, `marker_server.py`, top-level `convert.py`, top-level `convert_single.py`, top-level `chunk_convert.py`, top-level `chunk_convert.sh`, and support scripts, plus 6 README benchmark document identifiers. This is a static inventory, not upstream pass parity.

## Mapped Benchmark Pairs

- `fixtures/upstream-ci-benchmark-short.php` records the actual CI archive metadata and focused excerpts for the two external `benchmark_data` PDF/reference pairs. `BenchmarkReportVerifierTest` scores committed Marker output excerpts against the external references and verifies the upstream marker thresholds; `examples/wordpress-benchmark-report.php` turns the same fixture into a WordPress import quality gate.
- `fixtures/upstream-multicolcnn-surrogate.php` records a small excerpt pair from the README-linked committed outputs for `multicolcnn.pdf`: `data/examples/marker/multicolcnn.md` as Marker output and `data/examples/nougat/multicolcnn.md` as a reference-like surrogate.
- `fixtures/upstream-multicolcnn-supplied-document.php` records a fuller supplied-dictionary excerpt for `multicolcnn.pdf` with pdftext/layout/order payloads for the title, authors, abstract, and introduction. `SuppliedDocumentConverter` converts it to block-ready Markdown with upstream no-OCR stats and scores `0.9778095238095238` against the committed Nougat surrogate reference threshold of `0.80`.
- `fixtures/upstream-switch-transformers-surrogate.php` records a small excerpt pair from the README-linked committed outputs for `switch_trans.pdf`: `data/examples/marker/switch_transformers.md` as Marker output and `data/examples/nougat/switch_transformers.md` as a reference-like surrogate.
- `fixtures/upstream-switch-transformers-supplied-document.php` records a fuller supplied-dictionary excerpt for `switch_trans.pdf` with pdftext/layout/order payloads for the title, authors, abstract, and introduction. `SuppliedDocumentConverter` converts it to block-ready Markdown, preserves styled span emphasis for `different`, and scores `0.8827096774193548` against the committed Nougat surrogate reference threshold of `0.75`.
- `fixtures/upstream-thinkpython-surrogate.php` records a small excerpt pair from the README-linked committed outputs for `thinkpython.pdf`: `data/examples/marker/thinkpython.md` as Marker output and `data/examples/nougat/thinkpython.md` as a reference-like surrogate.
- `fixtures/upstream-thinkos-surrogate.php` records a focused Preface excerpt pair from the README-linked committed outputs for `thinkos.pdf`: `data/examples/marker/thinkos.md` as Marker output and `data/examples/nougat/thinkos.md` as a reference-like surrogate.
- `examples/upstream-surrogate-score.php` scores all four pairs with the native `BenchmarkScorer`. Current scores are about `0.978` for `multicolcnn.pdf` against a `0.80` threshold, `0.954` for `switch_trans.pdf` against a `0.75` surrogate threshold while also clearing the upstream CI `switch_trans.pdf > 0.40` boundary, `0.844` for `thinkpython.pdf` against a `0.78` surrogate threshold, and `0.986` for the focused `thinkos.pdf` Preface excerpt against a `0.95` surrogate threshold.
- The README-linked Marker/Nougat pairs remain intentionally labeled surrogates. They complement, but do not replace, the two actual external CI benchmark pairs now mapped from `benchmark_data_short.zip`.

## Mapped Native Semantics

- PDF content stream text operators feed Marker-style Markdown conversion.
- PDF text movement operators define block-ready line boundaries before WordPress import.
- `marker/pdf/utils.py` magic-MIME filetype detection is mapped by `FiletypeDetector`, including the upstream `pdf` substring shortcut, settings-backed non-PDF MIME mapping, and `other` fallback for unknown or unsupported uploads.
- `marker/postprocessors/markdown.py` hyphenated line dewrapping is mapped by `MarkdownPostProcessor::mergeLines`.
- `marker/postprocessors/markdown.py` heading/list/text wrapping and hash escaping are mapped by `MarkdownPostProcessor::surroundBlock`.
- `marker/pdf/utils.py` font flag decomposition is mapped by `PdfTextBlockConverter::fontFlagsDecomposer`, preserving Marker's ordered fixed-pitch/serif/symbolic/script/non-symbolic/italic/caps/bold/use-external-attributes suffixes.
- `marker/pdf/extract_text.py` pdftext dictionary conversion is mapped by `PdfTextBlockConverter::pdftextFormatToPage`, including span text cleanup, font suffixes, span IDs, one block per line, rotation-aware page bboxes, and char-block retention before later layout annotation.
- `marker/pdf/extract_text.py::get_text_blocks` page-range assembly is mapped by `PdfTextDocumentExtractor::getTextBlocks` for supplied pdftext dictionaries, including `start_page` validation, `max_pages` truncation, falsey zero handling, selected-range span-id enumeration, original page numbers, and PDF TOC metadata for partial WordPress imports.
- `marker/pdf/extract_text.py::naive_get_text` and `get_length_of_text` are mapped by `PdfTextExtractor::naiveGetText` and `PdfTextExtractor::getLengthOfText`, including upstream newline suffixing per page-text boundary and trim-before-length behavior for `convert.py --min_length` preflight.
- `marker/postprocessors/markdown.py` span assembly is mapped by `MarkdownPostProcessor::mergeSpans`, including Page/Block/Line/Span to MergedBlock conversion, lowercased font lists, skipped empty-span lines, first/last-span emphasis guards, next-substantial-span lookahead, and empty-page Text fallback blocks.
- `marker/postprocessors/markdown.py` block merging, block separators, and full-text assembly are mapped by `MarkdownPostProcessor::mergeBlocks`, `MarkdownPostProcessor::blockSeparator`, and `MarkdownPostProcessor::getFullText`.
- `marker/postprocessors/markdown.py` continuation geometry is mapped by `MarkdownPostProcessor::mergeBlocks` for equal-height, same-x lines whose vertical gap is below the upstream `max_block_gap`.
- `marker/pdf/utils.py` vertical-bucket and horizontal-row sorting is mapped by `LayoutOrderer::sortBlockGroup`.
- `marker/layout/layout.py::get_batch_size` is mapped by `LayoutAnnotator::batchSize`, including `LAYOUT_BATCH_SIZE` override handling, the upstream default six-page batch boundary, and `batch_multiplier` truncation.
- `marker/layout/layout.py::surya_layout` is mapped at the supplied-output boundary by `LayoutAnnotator::runWithSuppliedLayouts`, including collection of page text-line detections for the model call plan and upstream `zip` semantics when assigning supplied layout predictions to pages. Native PHP still does not load the Surya layout detector.
- `marker/layout/layout.py` layout annotation is mapped by `LayoutAnnotator::annotateBlockTypes`, including maximum-overlap layout-label assignment, nearest annotated-block fallback for unassigned blocks, default Text fallback, and consecutive same-layout block merging before Markdown assembly.
- `marker/layout/order.py` ordering-model preflight is mapped by `LayoutOrderer::batchSize` and `LayoutOrderer::runWithSuppliedOrder`, including upstream default/override/multiplier batch-size behavior, layout bbox collection capped by `ORDER_MAX_BBOXES`, supplied order-result assignment with Python `zip` semantics, and unchanged unpaired pages.
- `marker/layout/order.py` reading-order assignment is mapped by `LayoutOrderer::sortBlocksInReadingOrder`, including maximum bbox-overlap position assignment, fallback positions for unordered blocks, `sort_block_group` tie sorting, and Page-header/Page-footer/Footnote pinning.
- `marker/debug/data.py::dump_bbox_debug_data` is mapped by `DebugDataExporter::dumpBboxDebugData`, including disabled-debug no-op behavior, document-base `_bbox.json` path construction, heavy model/debug field exclusion, and JSON output over supplied Page arrays. The upstream print side effect is intentionally represented by returning the written path for PHP callers.
- `marker/debug/data.py::draw_page_debug_images` and `draw_layout_page_debug_images` are mapped by `DebugPageImagePlanner::drawPageDebugImagePlans`, including the upstream `DEBUG` guard, document-base debug folder, `layout_page_N.png` and `pdf_page_N.png` names, text-line image dimensions from `page.text_lines.image_bbox`, rescaled PDF line overlays, detector-line rectangles, layout label overlays, green order labels, block label overlays, and copied-PDF-image overlay sequencing. The native slice emits a deterministic artifact plan for WordPress review tooling instead of mutating PIL images.
- `marker/debug/render.py::render_on_image` is mapped by `DebugRenderPlanner::renderOnImagePlan`, including bbox integer casting, optional bbox rectangle drawing, shared or per-box color selection, label position calculation from `label_offset`, white label background rectangles, text overlay operations, and zero-size label skipping. The native slice emits a deterministic overlay operation plan for WordPress review previews instead of mutating a PIL image or downloading a font file.
- `marker/ocr/utils.py` alphanumeric quality scoring is mapped by `OcrHeuristics::alphanumRatio`, including the upstream behavior that removes spaces and newlines before counting alphanumeric characters.
- `marker/ocr/heuristics.py` bad-OCR detection is mapped by `OcrHeuristics::detectBadOcr`, including empty text, whitespace/newline runs, garbled low-alphanumeric text, and repeated replacement-character checks.
- `marker/ocr/heuristics.py` whole-document no-text detection is mapped by `OcrHeuristics::noTextFound`.
- `marker/ocr/heuristics.py` detected-line coverage is mapped by `OcrHeuristics::detectedLineCoverage`, including Surya image-bbox to page-bbox rescaling and extracted line intersection coverage.
- `marker/ocr/heuristics.py` OCR fallback triage is mapped by `OcrHeuristics::shouldOcrPage`, including no-text, bad-OCR, line-coverage, force-OCR, and zero-detected-line behavior.
- `marker/ocr/lang.py` language normalization, validation, and language-token IDs are mapped by `OcrLanguage::replaceLangsWithCodes`, `OcrLanguage::validateLangs`, `OcrLanguage::normalizeAndValidate`, and `OcrLanguage::langTokenIds`. The Surya branch title-cases names before mapping to Surya codes and preserves already-coded inputs; the OCRmyPDF/Tesseract branch maps exact upstream language names to Tesseract codes, defaults missing languages to `English`, and rejects invalid engine-specific codes before OCR handoff. The native tokenizer boundary de-duplicates Surya codes and applies Surya's locked `TOTAL_TOKENS=65536` plus `TOKEN_OFFSET=3` language-token offset.
- `marker/ocr/detection.py` detector batch sizing is mapped by `OcrDetection::batchSize`, including configured `DETECTOR_BATCH_SIZE`, the upstream default of 4, and Python-style integer truncation after applying a batch multiplier.
- `marker/ocr/detection.py` Surya detection assignment is mapped by `OcrDetection::runWithSuppliedPredictions`, including supplied prediction arrays, writing each prediction to `page.text_lines`, ignoring extra predictions, and leaving unpaired pages unchanged like upstream `zip(pages, predictions)`.
- `marker/ocr/recognition.py` batch sizing, `run_ocr` orchestration, and the pure supplied-boundary half of `surya_recognition` are mapped by `OcrRecognition`. The native boundary computes OCR page indexes with `no_text_found`/`should_ocr_page`, honors the upstream no-op `none` engine branch, accepts supplied Surya/OCRmyPDF result pages, counts successful versus failed recognized text with `detect_bad_ocr`, replaces only successful pages before WordPress block rendering, scales detector polygons for Surya OCR, skips zero-area polygons, and rebuilds recognized OCR lines into Marker Page arrays.
- `marker/schema/block.py::bbox_from_lines`, `split_block_lines`, and `Block.get_min_line_start` are mapped by `BlockStructure`, including upstream line-bbox extent calculation, zero-index and overlong-index split guards, minimal split Block-shaped output with recomputed bboxes/page numbers, and first-span x-coordinate review metadata for WordPress block splitting.
- `marker/schema/block.py::filter_spans` and `filter_bad_span_types` are mapped by `BlockSpanFilter`, including the upstream conversion cleanup order that removes known bad span IDs first, compacts already-empty lines even when all spans are retained, clears configured bad block-type text spans, and keeps Picture/image block metadata available for Gutenberg media review.
- `marker/images/save.py` deterministic image filename and image dictionary helpers are mapped by `ImageExtractor::getImageFilename` and `ImageExtractor::imagesToDict`.
- `marker/schema/block.py` nearest-block fallback insertion is mapped by `ImageExtractor::findInsertBlock`.
- `marker/images/extract.py` Figure/Picture region discovery is mapped by `ImageExtractor::findImageBlocks`, including layout image-bbox to page-bbox rescaling.
- `marker/images/extract.py` image placeholder insertion is mapped by `ImageExtractor::insertImagePlaceholders`, including intersecting text-span clearing, existing-line insertion, newly-created image lines, and upstream Markdown image syntax. The supplied-document path now threads supplied page image payloads through this boundary after `BAD_SPAN_TYPES` text has been removed, so Picture OCR text stays out of WordPress paragraphs while `0_image_N.png` entries are exported in the image dictionary. Raster image rendering remains unported because upstream uses `pypdfium2` and PIL.
- `marker_app.py` `img_to_html` and `markdown_insert_images` are mapped by `MarkdownImageEmbedder`, including the upstream Markdown image regex, optional title text consumption, repeated replacement, base64 PNG data URI output, and missing-image preservation for WordPress preview HTML.
- `marker_app.py` `open_pdf`, `page_count`, and `get_page_image` are mapped by `MarkerAppPreview`, including uploaded PDF byte validation, Page-tree traversal that excludes the `/Pages` container, inherited/direct page boxes, pypdfium-style zero-based page index planning, `dpi / 72` scale calculation, RGB output metadata, and the marker_app render call's default annotation behavior.
- `marker_server.py` API/upload helpers are mapped by `MarkerServerAdapter`, including `CommonParams` defaults, local-mode `extract_images=True`/`paginate=False` guard, upload `application/pdf` validation, temporary upload write and cleanup, local conversion success/error response shape, PNG image base64 encoding, Datalab `X-API-Key` form payload construction, and polling the original `request_check_url` until completion.
- `marker/pdf/images.py` render planning is mapped by `PdfImageRenderer`, including `dpi / 72` scale calculation, no-annotation rendering intent, RGB output boundary, and `render_bbox_image` crop bbox scaling from PDF page coordinates into rendered-image pixel coordinates.
- `marker/schema/bbox.py` geometry helpers are mapped by `BboxGeometry`, including upstream adjacent-box merge tolerances, strict edge-touch intersection behavior, intersection percentage as a fraction of the first box area, normalized coordinate expansion, detected-image to page bbox rescaling, center/distance calculations, and BboxElement-style bbox validation.
- `marker/equations/equations.py` Formula region discovery is mapped by `EquationReplacer::findEquationBlocks`, including `Formula` layout label filtering, layout-image bbox to page-bbox rescaling, line intersection using the upstream `0.7` threshold, removal of intersecting text lines, nearest-block fallback when no text line overlaps, and the Texify model token cap before replacement.
- `marker/equations/equations.py` insertion shifting and block insertion are mapped by `EquationReplacer::insertLatexBlocks`, including insertion before a block, after a block, or inside a split text block, plus later insertion-point adjustment after new Formula blocks are inserted.
- `marker/equations/equations.py` LaTeX validation is mapped by `EquationReplacer::insertLatexBlocks`, including predicted-token limit, non-empty output, predicted text length greater than 70% of the original equation text, original-text fallback when validation fails, and converted-span capture when validation succeeds.
- `marker/equations/equations.py` document-level equation stats are mapped by `EquationReplacer::replaceEquations`, returning `successful_ocr`, `unsuccessful_ocr`, and `equations` metadata in the same shape used by Marker's README output metadata.
- `marker/equations/inference.py::get_total_texify_tokens` is represented by `EquationReplacer::totalTexifyTokens` as a native deterministic token-count boundary. This is intentionally not full Texify tokenizer parity because the upstream tokenizer/model stack is not part of the native PHP slice.
- `marker/equations/inference.py::get_batch_size` and `get_latex_batched` are represented by `EquationReplacer::texifyBatchSize` and `getLatexBatchedFromSuppliedOutputs`, including CPU/GPU/default batch sizing, `TEXIFY_BATCH_SIZE`, `TEXIFY_TOKEN_BUFFER`, per-batch dynamic `max_tokens`, and blanking supplied run-on outputs that reach the upstream max-length sentinel before WordPress math block insertion.
- `marker/postprocessors/markdown.py` Formula display-math handling is covered after equation replacement: accepted LaTeX spans remain fenced with `$$...$$`, and `MarkdownPostProcessor` preserves those fences for WordPress math/HTML block rendering.
- `benchmarks/overall.py` report aggregation is mapped by `BenchmarkReportBuilder`, including per-method file score/time/page records, average score, upstream `total_pages` denominator behavior for `time_per_page`, and per-document timing.
- `benchmarks/overall.py` benchmark orchestration is mapped by `BenchmarkRunner`, including `.pdf` input-folder filtering, basename-derived reference Markdown loading, supplied `marker` and comparison method callbacks, pypdfium-style supplied page-count callbacks, optional `md_out_path` writes as `{method}_{document}.md`, and handoff into the upstream report shape.
- `marker/benchmark/scoring.py` chunking, local-window overlap, and text score aggregation are mapped by `BenchmarkScorer`.
- `marker/benchmark/table.py` pipe-cell splitting, best-row fuzzy alignment, and aggregate table scoring are mapped by `TableScorer`.
- `marker/tables/table.py` post-recognition table replacement is mapped by `TableFormatter::formatTables`, including supplied Markdown table insertion after intersecting Table layout blocks are removed. The formatter now derives merged table regions consistently with `get_table_boxes`, so adjacent PDF table fragments consume one recognized Markdown table instead of one per raw layout box.
- `tabled.inference.recognition.py::get_cells` and `recognize_tables` are mapped by `TableRecognizer` at the supplied-boundary level: text-line table blocks are filtered to the current table bbox and used when present, detector cells are required when text lines are absent, filtered empty, or `detect_boxes` is forced, zero-area detector bboxes are dropped before OCR, `needs_ocr` flags are preserved, and supplied OCR text is zipped back onto detector cells before recognition output is formatted.
- `tabled.assignment.py::assign_rows_columns` is mapped by `TableRecognizer`, including initial row/column intersection assignment, `merge_multiline_rows` continuation-row merging by assigned row/column IDs, overlapping-row/column rescue, unassigned-cell closest-row/column fallback, a bounded heuristic row layout fallback when most cells remain unassigned, row/column span expansion for cells whose geometry covers otherwise-open row or column bands, and the upstream table-recognition batch-size override/default boundary.
- `tabled/heuristics/cells.py` is mapped by `TableRecognizer::heuristicColumnSeparators` and the fallback branch of `assignRowsColumns`, including repeated normalized left/right/center coordinate filtering, one-dimensional DBSCAN clustering with the locked `eps=.01` and `min_samples=max(2,row_count//4)` boundary, separator-family selection by largest cluster count, and assignment through upstream-style separator keys before Markdown table rendering.
- `tabled.formats.markdown.py::markdown_format` is mapped by `TableRecognizer::markdownFormat`, including row/column/order sorting, dot-leader and newline cleanup, pipe/hyphen escaping, and GitHub-style Markdown table output for Gutenberg table rendering.
- `marker/tables/utils.py` table cell ordering and text cleanup are mapped by `TableUtils::sortTableBlocks`, `TableUtils::replaceDots`, and `TableUtils::replaceNewlines`.
- `marker/output.py` output artifact persistence is mapped by `OutputWriter::getSubfolderPath`, `OutputWriter::getMarkdownFilepath`, `OutputWriter::markdownExists`, and `OutputWriter::saveMarkdown`, including the upstream final-extension filename split, per-document output folder, converted Markdown file, `_meta.json` metadata, and image artifact writes.
- Top-level `convert.py` batch processing is mapped by `BatchConverter::loadMetadataFile`, `BatchConverter::planTasks`, `BatchConverter::processFile`, and `BatchConverter::processFolder`, including metadata-file JSON loading, chunk planning, metadata lookup by basename, `min_length` skip gates, existing-output skip gates, non-empty conversion persistence, and summary counts for bulk WordPress import jobs.
- Top-level `chunk_convert.py` and `chunk_convert.sh` processing is mapped by `ChunkConversionPlanner::planFromEnvironment` and `ChunkConversionPlanner::planDeviceJobs`, including required environment/input validation, per-device Marker job fanout, chunk index/count arguments, worker count propagation, optional metadata/min-length flags, and a WordPress queue-friendly plan that does not execute the upstream subprocess.
- Top-level `convert_single.py` processing is mapped by `SingleDocumentConverter::parseLanguages` and `SingleDocumentConverter::convert`, including Python-style comma language splitting without trimming, option handoff to the supplied converter callback, basename output layout, tuple/named conversion result normalization, Marker output artifact persistence, and the important upstream difference from batch mode: empty single-document output is still saved.
- `marker/settings.py` settings defaults and computed fields are mapped by `MarkerSettings`, including supported filetype checks, environment-style override coercion, unknown extra ignore behavior, OCR engine literal validation, CPU fallback device handling, native dtype labels, page separators, and bad-span/image/pagination/OCR flags.
- `marker/models.py` model setup and `marker/utils.py::flush_cuda_memory` are mapped by `ModelPipelinePlanner`, including the import-time MPS fallback environment flag, upstream setup helper loader/processor attachment semantics, Texify/layout checkpoint arguments, explicit device/dtype propagation, `load_all_models` load sequence versus returned model-list order, and CUDA-only empty-cache planning. Native PHP intentionally stops at a deterministic plan and does not load Torch, Surya, Texify, or tabled models.
- `scripts/verify_benchmark_scores.py` table-score threshold is covered by `examples/wordpress-table-score.php`, which scores an OCR-noisy WordPress table import above `0.7`.
- `marker/schema/page.py` helper properties are mapped by `PageInspector`, including all-line flattening, nonblank line/span filtering, font-size extraction from nonblank spans, line-height extraction from nonblank line bboxes, and page-level `prelim_text` assembly.
- `marker/cleaners/bullets.py` bullet glyph normalization is mapped by `TextCleaner::replaceBullets`.
- `marker/cleaners/text.py` excessive whitespace and non-breaking-space cleanup is mapped by `TextCleaner::cleanupText`.
- `marker/cleaners/fontstyle.py` font-name and font-weight emphasis detection is mapped by `FontStyleCleaner::markBoldItalicSpans`.
- `marker/postprocessors/markdown.py` `surround_text` and the styled-span branch of `merge_spans` are mapped by `FontStyleCleaner::mergeStyledLine`, including the upstream first/last-span guard and Markdown `*`/`**` markers.
- `marker/cleaners/headers.py` repeated edge-line detection is mapped by `HeaderFooterCleaner::findCommonEdgeLines` and `HeaderFooterCleaner::removeCommonEdgeLines`, including the upstream minimum of three pages before common elements are filtered.
- `marker/cleaners/headers.py` repeated title filtering is mapped by `HeaderFooterCleaner::replaceLeadingTrailingDigits`, `HeaderFooterCleaner::findOverlapElements`, and `HeaderFooterCleaner::filterCommonTitles`.
- `marker/cleaners/code.py` line-length and comment-prefix detection are mapped by `CodeBlockDetector::isCodeLineLength` and `CodeBlockDetector::commentCount`.
- `marker/cleaners/code.py` code block classification is mapped by `CodeBlockDetector::isCodeBlock` and `CodeBlockDetector::identifyCodeBlocks`, including the upstream `> 3` line minimum, indentation/comment majority threshold, and optional small-font checks.
- `marker/cleaners/code.py` indentation reconstruction is mapped by `CodeBlockDetector::indentBlock`, including line-geometry-derived prefixes and repeated blank-line suppression.
- `marker/convert.py` code cleanup integration is mapped by `ConversionFinalizer`, including the upstream placement of `identify_code_blocks`/`indent_blocks` before heading/Markdown assembly, document-level mean font-size and median line-height checks, Code block type mutation, single-span code replacement, and `span_id` values such as `0_fix_code`.
- `marker/convert.py` early `convert_single_pdf` metadata and filetype orchestration is mapped by `CorePdfConverter`, including metadata-provided language override, engine-specific OCR language normalization, `OCR_ALL_PAGES` flag folding, `filetype` metadata, `other` filetype early return before model callbacks, `pdf_toc`/page-count metadata, and low-resolution render-count planning after upstream-style `start_page` document trimming.
- `marker/cleaners/headings.py` heading line splitting is mapped by `HeadingCleaner::splitHeadingBlocks`, including the upstream `Title`/`Section-header` labels and the `0.7` bounding-box intersection threshold.
- `marker/cleaners/headings.py` line-height bucketing and heading-level inference are mapped by `HeadingCleaner::bucketHeadings` and `HeadingCleaner::inferHeadingLevels`, including the upstream four-level/default-level behavior.
- `marker/cleaners/toc.py` PDF outline metadata import is mapped by `HeadingCleaner::getPdfToc`, including the upstream `doc.get_toc(max_depth)` call boundary and ordered `title`, `level`, and `page_index` projection.
- `marker/cleaners/toc.py` computed table-of-contents metadata is mapped by `HeadingCleaner::computeToc`, emitting title, level, and page for `Title` and `Section-header` blocks.
- README-linked committed Marker/Nougat `multicolcnn`, `switch_transformers`, `thinkpython`, and `thinkos` markdown outputs are mapped as upstream-derived surrogate benchmark pairs.
- `scripts/verify_benchmark_scores.py` marker and table report threshold checks are mapped by `BenchmarkReportVerifier`, including the upstream `<=` failure boundary for marker scores and `< 0.7` failure boundary for average table reports.
- `benchmark_data_short.zip` `multicolcnn.pdf` and `switch_trans.pdf` PDF/reference pairs are mapped as actual external benchmark evidence with recorded archive, PDF, and reference hashes.
- `examples/wordpress-pdf-outline-import.php` maps `marker/cleaners/toc.py::get_pdf_toc` into a WordPress bookmark import path. It accepts a pypdfium-style `get_toc(max_depth)` adapter, preserves upstream title/level/page metadata, and emits a Gutenberg list with review attributes before OCR/layout model work begins.
- `examples/wordpress-paginated-import.php` uses the full block-merge path to preserve upstream-style page-start markers as reviewable Gutenberg separators while emitting headings, paragraphs, and list blocks.
- `examples/wordpress-ocr-triage.php` maps upstream OCR heuristics into a WordPress import preflight that sends garbled/scanned pages to OCR before block rendering while leaving clean extracted text alone.
- `examples/wordpress-image-import.php` maps upstream image insertion metadata into a Gutenberg image-block import path, preserving Marker-style `![page_image](page_image.png)` filenames while avoiding a Python/pypdfium raster-render dependency.
- `examples/wordpress-markdown-image-preview.php` maps `marker_app.py::markdown_insert_images` into a WordPress review preview. It takes Marker-style Markdown image spans and supplied PNG bytes, embeds the image as upstream-style data URI HTML, and emits a core HTML block without loading Streamlit or pypdfium.
- `examples/wordpress-marker-app-preview.php` maps `marker_app.py::page_count` and `get_page_image` into a WordPress upload review preflight. It reports page count, selected one-based page number, pypdfium-style zero-based page index, render scale, RGB conversion, pypdfium default annotation mode, and rendered preview size without loading Streamlit or rasterizing through pypdfium/PIL.
- `examples/wordpress-marker-api-upload.php` maps `marker_server.py` into a WordPress import endpoint path. It validates an uploaded PDF MIME type, writes a temporary upload for a supplied native converter, returns the upstream local API response shape, base64-encodes images when present, and confirms the temporary upload is removed without running FastAPI, Uvicorn, requests, or Python models.
- `examples/wordpress-pdf-image-crop.php` maps `marker/pdf/images.py::render_bbox_image` into a WordPress media review path. It records the upstream render scale and crop bbox that a raster backend would use, without shelling out to pypdfium/PIL.
- `examples/wordpress-bbox-geometry-import.php` maps `marker/schema/bbox.py` into a WordPress geometry preflight. It merges adjacent PDF span boxes into one block-ready paragraph bbox, preserves that bbox as review metadata, and only emits a related core image block when the supplied image region intersects the merged paragraph box under Marker's strict bbox semantics.
- `examples/wordpress-equation-import.php` maps upstream equation replacement into a WordPress math import path. It removes the source equation text line, inserts a supplied LaTeX Formula block, emits paragraph blocks around it, renders the equation as a constrained core HTML block, and reports Marker-style equation metadata for review.
- `examples/wordpress-table-format-import.php` maps the post-recognition half of `marker/tables/table.py::format_tables` into a Gutenberg table import path. It replaces the source PDF Table block with a supplied Markdown table and renders it as a core table block while leaving the upstream Surya/tabled recognition stack explicitly out of the native slice.
- `examples/wordpress-merged-table-import.php` maps the merged-region `format_tables` boundary into a Gutenberg table import path. It accepts adjacent PDF Table layout fragments, removes both source fragments, consumes one supplied recognized Markdown table, and renders one core table block.
- `examples/wordpress-table-box-plan.php` maps `marker/tables/table.py::get_table_boxes` into a WordPress review path. It merges adjacent supplied Table layout boxes, emits high-resolution crop bbox metadata, duplicates supplied text-line detections for non-OCR pages, and marks OCRed pages for table-cell redetection before Gutenberg table rendering.
- `examples/wordpress-table-recognition-handoff.php` maps the next supplied table-recognition boundary into a WordPress import path. It accepts supplied tabled-style cells/rows/columns, applies native row/column assignment, preserves row/column span metadata for reviewer tooling, and renders column spans as table-cell attributes while still feeding the existing `format_tables` replacement path without running Surya/tabled models.
- `examples/wordpress-table-detector-filter.php` maps the detector-fallback half of `tabled.inference.recognition.py::get_cells` into a WordPress table import preflight. It drops zero-width and zero-height detector boxes before OCR text assignment, reports the dropped count as review metadata, and renders the remaining OCR-backed cells as a core table block.
- `examples/wordpress-table-heuristic-columns.php` maps the locked `tabled.heuristics.cells` fallback into a WordPress table import path. It records DBSCAN-derived column separator metadata for reviewer tooling, assigns rows and columns without model row/column boxes, and renders the result as a Gutenberg table.
- `examples/wordpress-table-cleanup-import.php` maps `marker/tables/utils.py` into a Gutenberg table cleanup path. It orders cell blocks into rows, removes dot leaders used as visual table fillers, collapses embedded cell newlines, and renders the cleaned cells as a core table block.
- `examples/wordpress-output-artifact.php` maps `marker/output.py` into a WordPress import artifact handoff. It writes converted block Markdown, review metadata, and image artifacts to Marker's per-document output layout without shelling out to Python or PIL.
- `examples/wordpress-debug-bbox-export.php` maps `marker/debug/data.py::dump_bbox_debug_data` into a WordPress review workflow. It writes a Marker-style bbox JSON artifact, preserves layout labels and text-line boxes for reviewer tooling, and confirms large model fields such as `segmentation_map` and `heatmap` are omitted.
- `examples/wordpress-debug-render-plan.php` maps `marker/debug/render.py::render_on_image` into a WordPress review workflow. It emits the bbox and label overlay operations an admin-side preview renderer can draw over imported PDF page images without requiring PIL, font downloads, or Python model code.
- `examples/wordpress-debug-page-image-plan.php` maps `marker/debug/data.py::draw_page_debug_images` into a WordPress review workflow. It emits the document debug folder, `layout_page_0.png` and `pdf_page_0.png` artifact paths, image dimensions, and overlay operation counts so an admin preview can render Marker-style debug images without PIL, font downloads, or Python model code.
- `examples/wordpress-batch-convert-import.php` maps top-level `convert.py` into a WordPress bulk import path. It plans a folder of PDFs, applies basename metadata, runs native min-length preflight, writes Marker-style per-document output artifacts, and reports converted/skipped/error counts without loading Python model workers.
- `examples/wordpress-chunk-convert-queue.php` maps top-level `chunk_convert.py` plus `chunk_convert.sh` into a WordPress sharded import queue. It validates the same device/worker environment, emits one queue item per device with the upstream chunk arguments and optional metadata/min-length flags, and intentionally avoids invoking the upstream `marker` subprocess.
- `examples/wordpress-single-convert-import.php` maps top-level `convert_single.py` into a WordPress single-upload import path. It passes `max_pages`, `start_page`, `langs`, and `batch_multiplier` to a supplied native converter, writes Marker's per-document Markdown/metadata layout, and reports the same option state a WordPress import UI would need for review.
- `examples/wordpress-settings-preflight.php` maps `marker/settings.py` into a WordPress import preflight. It accepts PDF uploads, rejects unsupported MIME types, applies shared-hosting overrides for image extraction and pagination, and exposes the same page separator and bad-span defaults that later conversion stages consume.
- `examples/wordpress-model-preflight.php` maps `marker/models.py` and `marker/utils.py` into a WordPress import worker preflight. It reports the upstream model load sequence, returned model-list order consumed by `convert_single_pdf`, deferred Python loader names, checkpoint/device/dtype arguments, MPS fallback environment variable, and CUDA cache cleanup plan without importing or executing the Python model stack.
- `examples/wordpress-filetype-preflight.php` maps `marker/pdf/utils.py::find_filetype` into a WordPress upload preflight. It accepts a PDF by magic bytes even when extension text is not trusted, and rejects a ZIP-like extension-spoofed payload before the heavier pdftext/pypdfium/Surya stack is reached.
- `examples/wordpress-benchmark-runner.php` maps the outer `benchmarks/overall.py` loop into a WordPress import quality gate. It writes temporary supplied PDF/reference pairs from the actual CI benchmark excerpts, runs a supplied native marker conversion callback, emits upstream-style `marker_*.md` outputs, builds the report, and verifies the upstream marker thresholds without loading Poetry or ML model dependencies.
- `examples/wordpress-supplied-document-benchmark.php` maps the composed supplied-document conversion path into a WordPress import quality gate. It accepts supplied pdftext/layout/order/table/image dictionaries, emits block-ready Markdown, preserves supplied-boundary metadata such as table and image counts, and builds an upstream-shaped benchmark report for editorial review.
- `examples/wordpress-supplied-equation-import.php` maps the composed supplied-document path with a Formula layout region and supplied Texify-style equation result dictionary. It replaces the extracted equation text after span/code cleanup, emits a Gutenberg HTML math block, and records upstream-shaped equation stats without loading Texify or rendering equation crops.
- `examples/wordpress-texify-equation-batch-preflight.php` maps the pure `get_latex_batched` control-flow boundary into a WordPress import worker preflight. It plans equation recognition batches, calculates upstream-style `max_tokens`, and drops run-on supplied Texify outputs before they can be inserted as math blocks.
- `examples/wordpress-multicolcnn-supplied-benchmark.php` maps a fuller upstream `multicolcnn.pdf` supplied-dictionary excerpt into a WordPress Data Liberation quality gate. It converts the supplied pdftext/layout/order payloads without live model downloads, preserves no-OCR `ocr_stats`, emits block-preview metadata, and reports a native score of `0.9778095238095238` against the committed surrogate threshold.
- `examples/wordpress-switch-transformers-supplied-benchmark.php` maps a fuller upstream `switch_trans.pdf` supplied-dictionary excerpt into a WordPress Data Liberation quality gate. It converts supplied pdftext/layout/order payloads without live model downloads, preserves styled emphasis from pdftext spans, emits block-preview metadata, and reports a native score of `0.8827096774193548` against the committed surrogate threshold.
- `examples/wordpress-switch-transformers-toc-table-import.php` maps the upstream committed Marker `switch_trans.pdf` Contents table page slice into a Gutenberg TOC table preview. It accepts supplied pdftext/layout/order/recognized-table dictionaries, runs the native table-recognition and table-formatting replacement path, records one inserted table, and scores `0.527` against the inspected Marker output excerpt without loading Python, pypdfium, Surya, tabled, or model weights.
- `examples/wordpress-switch-transformers-table1-import.php` maps the upstream committed Marker `switch_trans.pdf` Table 1 benchmark slice into a Gutenberg benchmark table preview. It accepts supplied pdftext/layout/order/recognized-table/caption dictionaries, preserves Unicode `Speed (↑)` and `Not achieved†` cells with upstream-style Markdown padding, records one inserted table, and scores `0.9122169059011164` against the inspected Marker output excerpt without loading Python, pypdfium, Surya, tabled, or model weights.
- `examples/wordpress-forced-ocr-table-import.php` maps an upstream `OCR_ALL_PAGES` table-recognition boundary into a shared-hosting WordPress import preflight. It forces supplied detector cells through supplied OCR text before Markdown table formatting, records `needs_ocr` metadata, and renders the Gutenberg-ready table without loading Surya, Tesseract, tabled, Torch, or pypdfium.
- `examples/wordpress-benchmark-report.php` maps `benchmarks/overall.py` plus `scripts/verify_benchmark_scores.py` into a WordPress quality gate. It builds the upstream benchmark report shape from imported block-content excerpts, scores those excerpts against the actual upstream CI benchmark references, and rejects a report before editorial review if either Marker document score misses the upstream threshold.
- `examples/wordpress-ocr-language-preflight.php` maps `marker/ocr/lang.py` into a multilingual WordPress OCR metadata preflight. It normalizes imported PDF metadata such as `["Spanish", "Russian"]` to OCRmyPDF/Tesseract codes such as `["spa", "rus"]`, and applies Marker's default English language when metadata omits languages.
- `examples/wordpress-ocr-recognition-handoff.php` maps `marker/ocr/recognition.py::run_ocr` into a WordPress OCR handoff. It sends a garbled PDF page through the native OCR selection boundary, accepts a supplied recognized page, emits Marker's `ocr_stats`, and renders the successful OCR text as a Gutenberg paragraph without loading Surya or Tesseract.
- `examples/wordpress-surya-ocr-recognition-import.php` maps the pure supplied-boundary half of `marker/ocr/recognition.py::surya_recognition` into a WordPress import path. It records the scaled detector polygon count that would be sent to Surya, drops a zero-area detection box before recognition, reconstructs a Marker page from supplied OCR text lines, and emits the recovered text as a Gutenberg paragraph without loading the Surya model.
- `examples/wordpress-bad-span-filter-import.php` maps `marker/schema/block.py::filter_spans` and `filter_bad_span_types` into a WordPress import cleanup path. It removes a repeated header span and OCR text over a Picture block before Markdown rendering while preserving the image filename and bbox metadata for a core image block.
- `examples/wordpress-block-line-split-import.php` maps `marker/schema/block.py::split_block_lines` into a WordPress import review path. It splits one extracted PDF text block at a reviewer/model boundary, recomputes upstream-style bboxes, then emits a Gutenberg heading plus paragraph without loading Python models.
- `examples/wordpress-conversion-finalizer-import.php` maps the late `marker/convert.py::convert_single_pdf` cleanup and assembly order into a WordPress import handoff. It removes bad header spans, marks bold body spans, generates computed TOC and image metadata, normalizes bullet list markers, and emits heading/paragraph/list blocks without running pdftext, pypdfium, Surya, Texify, or tabled models.
- `examples/wordpress-core-convert-preflight.php` maps the early `marker/convert.py::convert_single_pdf` boundary into a WordPress import preflight. It carries supplied page and PDF TOC metadata through the core conversion context, emits the low-resolution render plan that Surya detection would consume, normalizes language metadata, and hands the supplied pages to the native finalizer without loading Python models.
- `examples/wordpress-finalizer-code-block-import.php` maps the integrated `identify_code_blocks`/`indent_blocks` finalizer step into a WordPress import handoff. It keeps ordinary PDF prose as a paragraph, records `block_stats.code`, and emits an upstream-indented Gutenberg code block from supplied PDF line geometry without running Python models.
- `examples/wordpress-ocr-detection-preflight.php` maps `marker/ocr/detection.py::surya_detection` into a WordPress preflight. It accepts supplied Surya text-line boxes, writes them to `page.text_lines` with upstream zip semantics, checks line coverage through the existing OCR heuristics, and records the detection boxes as review metadata before deciding whether a page needs OCR.
- `examples/wordpress-layout-detection-preflight.php` maps the supplied-prediction half of `marker/layout/layout.py::surya_layout` into a WordPress import preflight. It records the upstream layout plan, attaches supplied Surya layout labels with `zip` semantics, then feeds `annotate_block_types` before emitting heading and paragraph blocks.
- `examples/wordpress-layout-annotation-import.php` maps `marker/layout/layout.py::annotate_block_types` into a WordPress import preflight. It assigns Title/Text/Picture labels from supplied layout boxes, merges split title lines from the same layout region, filters bad span types with `MarkerSettings::badSpanTypes()`, and renders only block-ready heading and paragraph output.
- `examples/wordpress-span-merge-import.php` maps `marker/postprocessors/markdown.py::merge_spans` into the WordPress paragraph path. It carries styled PDF spans through Marker's MergedBlock shape, preserves lowercased font metadata for downstream analysis, emits upstream-style emphasis markers, and renders them as `<strong>`/`<em>` in a core paragraph block.
- `examples/wordpress-pdftext-block-import.php` maps `marker/pdf/extract_text.py::pdftext_format_to_blocks` into the WordPress paragraph path. It converts supplied pdftext dictionary output into Marker's native page/block/line/span shape, normalizes pdftext hyphenated span text, preserves font flag suffixes, and feeds the existing merge-spans path without loading pdftext, pypdfium, or Surya.
- `examples/wordpress-pdftext-page-range-import.php` maps the supplied-data half of `marker/pdf/extract_text.py::get_text_blocks` into a partial WordPress import path. It selects a requested page range from supplied pdftext dictionaries, preserves upstream relative span-id enumeration and original document page numbers, carries PDF TOC metadata, and emits Gutenberg paragraphs only for the selected pages.
- `examples/wordpress-text-length-preflight.php` maps `marker/pdf/extract_text.py::get_length_of_text` into a WordPress queue preflight. It counts native extracted text with upstream trim semantics and records whether the PDF should be queued for heavier conversion work under a min-length policy.
- `examples/wordpress-page-inspection-preflight.php` maps `marker/schema/page.py` helper properties into WordPress review metadata. It records nonblank line/span counts, font-size and line-height distributions, and the upstream `prelim_text` page view before a document import is sent to editorial review.
- `examples/wordpress-pdf-literal-utf16-import.php` maps a bounded native PDF string-decoding edge inside the text extraction path. It decodes UTF-16BE and UTF-16LE BOM literal strings after PDF literal escape handling, then emits block-ready WordPress paragraphs without external PDF tooling.

## Runner Status

The full upstream runner was not executed. The CI workflow's Google Drive archive was downloaded and inspected because it is small, giving two actual PDF/reference pairs for static evidence. The locked `tabled-pdf` source tarball was downloaded only to inspect focused table detection, recognition, assignment, multiline-row merge, heuristic column layout, and formatting helpers used by Marker's table path; no Python model stack was installed or executed. Running upstream `benchmarks/overall.py` still requires Poetry dependencies including `torch`, `surya-ocr`, `pdftext`, `pypdfium2`, `tabled-pdf`, Texify model tooling, Nougat comparison tooling, and model downloads. The native `BenchmarkRunner` now covers that runner's filesystem/method/report loop for supplied conversion callbacks, and `SuppliedDocumentConverter` now gives that callback a native document-level path over supplied pdftext/layout/order/table/image/equation dictionaries. This slice includes four supplied-dictionary excerpts: `multicolcnn.pdf` scores `0.9778095238095238` against the committed Nougat surrogate reference, the `switch_trans.pdf` introduction slice scores `0.8827096774193548`, the `switch_trans.pdf` Contents table slice scores `0.527`, and the `switch_trans.pdf` Table 1 benchmark table/caption slice scores `0.9122169059011164` against the inspected committed Marker output excerpt; it also includes one forced-OCR table-routing fixture, a supplied Formula layout/equation-result fixture that threads through the late `replace_equations` boundary, and a pure Texify batch/output-filter preflight for supplied equation model outputs. This is still not full upstream runner parity because it does not load pdfium, Marker models, Nougat, Torch, tabled, or Texify. Under the lane resource constraints, the defensible denominator for now is the cloned static inventory above, the two hash-identified external CI benchmark pairs, the natively ported filetype detection, pdftext dictionary conversion, supplied page-range assembly, native text-length preflight, benchmark scoring/runner/report-building/report-verifier/cleaner/postprocessor/merge-spans/block-structure/block-span-filtering/page-inspection/conversion-core-preflight/supplied-document-conversion/forced-OCR-table-routing/conversion-finalizer code cleanup/supplied-equation-replacement/supplied-equation-batch-preflight/supplied-image-extraction/batch-converter/chunk-conversion-planner/single-document-converter/model-pipeline-planner/marker-runtime-planner/marker-server-api metadata-file/heading/PDF-outline/TOC/layout-detection-supplied-boundary/layout-ordering-supplied-boundary/reading-order/OCR/OCR-language/OCR-language-token-id/OCR-detection/OCR-recognition-orchestration/Surya-recognition-supplied-boundary/image-insertion/marker-app-image-preview/marker-app-upload-preview/PDF-image-render-planning/bbox-geometry/equation-replacement/table-box-planning/table-recognition-handoff/detector-positive-area-filter/multiline-row-merge/table-heuristic-column/table-formatting/table-utility/debug-bbox-output/debug-render-plan/debug-page-image-plan/output/settings functions, the four committed README-linked surrogate pairs, the four supplied-document excerpts, one forced-OCR table-routing fixture, and one supplied Formula fixture, not local full runner parity.

## Latest Reduced Handoff

2026-05-25 05:08 UTC: implemented the PDF literal UTF-16 BOM slice. `PdfTextExtractor` now routes decoded literal-string bytes through the same BOM-aware PDF string decoder used by hex strings, so UTF-16BE and UTF-16LE literal operands render as Unicode text before WordPress paragraph output. Focused evidence: PHP syntax checks for the extractor, focused test, and example; local example smoke for `wordpress-pdf-literal-utf16-import.php`; focused `PdfTextExtractorTest`; markerPDF-only focused harness; and `git diff --check -- lanes/markerpdf`. Dependency closure: no new support component is needed; the slice reuses the existing bounded native `PdfTextExtractor` parser/string decoder. Broader searchable PDF dictionary work remains gated to the inactive `pdf-text-dictionary-core` support row only if that future rich slice opens.

2026-05-25 06:05 UTC: implemented the ToUnicode codespacerange fallback slice. `PdfTextExtractor` now uses `begincodespacerange` widths to choose the source-code segment before mapping or fallback decoding, preventing a one-byte mapping from splitting an unmapped two-byte CID. Focused evidence: PHP syntax checks for the extractor, focused test, and example; local example smoke for `wordpress-pdf-cmap-codespace-fallback-import.php`; focused `PdfTextExtractorTest`; markerPDF-only focused harness; and `git diff --check -- lanes/markerpdf`. Dependency closure: no new support component is needed; the slice reuses the existing bounded native `PdfTextExtractor` parser and ToUnicode CMap lookup. Broader searchable PDF dictionary work remains gated to the inactive `pdf-text-dictionary-core` support row only if that future rich slice opens.

2026-06-02 00:12 UTC: integrated the document-structure boundary slice on top of the accepted page `/Contents` text extraction handoff. `DocumentStructureBoundary` resolves layout regions in page coordinates so Table regions protect nested Formula and Picture/Figure regions before supplied equation replacement or image insertion. This preserves the upstream `convert_single_pdf` stage priority where recognized table replacement owns nested content and prevents duplicate math/image blocks in WordPress output. Focused evidence: `php -l` for the new helper, changed extractors, focused tests, and example; `php tools/run-tests.php lanes/markerpdf/tests/ImageExtractorTest.php lanes/markerpdf/tests/EquationReplacerTest.php lanes/markerpdf/tests/SuppliedDocumentConverterTest.php` passed with 3 test files, 161 assertions, and 0 failures; `php lanes/markerpdf/examples/wordpress-structure-boundary-import.php` emitted heading/table/paragraph Gutenberg blocks with no separate math/image duplicate; `php tools/run-tests.php lanes/markerpdf/tests` passed with 47 test files, 1020 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses existing supplied layout/table/equation/image arrays and a local page-coordinate structure helper.

2026-06-02 00:34 UTC: integrated the page resource inheritance and PDF object-stream/xref parser slices together on current `origin/main`. `PdfTextExtractor` now carries page-specific font maps through catalog-ordered `/Contents` extraction, resolves inherited `/Resources` dictionaries root-to-page, expands PDF 1.5 `/ObjStm` member objects, and honors xref-stream type-2 membership so stale unlisted compressed objects are ignored. Focused evidence: `php -l` for `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-resource-inheritance-import.php`, and `wordpress-pdf-object-stream-xref-import.php`; focused `PdfTextExtractorTest.php` passed with 1 file, 89 assertions, and 0 failures; both example smokes emitted the expected Gutenberg paragraphs; full markerPDF lane passed with 47 files, 1041 assertions, and 0 failures. Dependency closure: no new support component is needed; the slices reuse the native PDF object inventory, stream decoder, page-tree walker, and CMap/font lookup paths.

2026-06-02 00:46 UTC: integrated three bounded PDF text-extraction hardening slices on current `origin/main`. `PdfTextExtractor` now chooses exact mapped ToUnicode source-key widths when a minimal CMap omits `begincodespacerange`, falls back to a font's own `/Encoding` map when a referenced `/ToUnicode` CMap stream cannot be decoded, and skips `/Subtype /Image` XObject streams in the stream-only fallback before text-token parsing. Focused evidence: `php -l` passed for `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, `wordpress-pdf-cmap-source-width-fallback-import.php`, `wordpress-pdf-malformed-cmap-filter-import.php`, and `wordpress-pdf-image-xobject-boundary-import.php`; the three example smokes emitted the expected Gutenberg paragraphs; focused `PdfTextExtractorTest.php` passed with 1 file, 102 assertions, and 0 failures; full markerPDF lane passed with 47 files, 1054 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slices reuse the native PDF object parser, stream decoder, CMap lookup, font Encoding fallback maps, and content-token parser.

2026-06-02 00:55 UTC: integrated the pdftext dictionary-core boundary on current `origin/main`. `PdfTextBlockConverter` now validates real pdftext dictionary shape, preserves null font names as `None`, carries char offsets, `chars`, and rotation metadata, and `PdfTextDocumentExtractor` records `pdftext_options` for selected ranges. Focused evidence: `php -l` passed for the changed converter, document extractor, focused tests, and `wordpress-pdftext-dictionary-core-import.php`; focused `PdfTextBlockConverterTest.php` plus `PdfTextDocumentExtractorTest.php` passed with 2 files, 53 assertions, and 0 failures; the example smoke emitted dictionary-core metadata and a Gutenberg paragraph; full markerPDF lane passed with 47 files, 1071 assertions, and 0 failures; `git diff --check --cached -- lanes/markerpdf` passed. Dependency closure: bounded supplied-dictionary mode; no Python pdftext, pypdfium, or model stack is executed.

2026-06-02 23:00 UTC: isolated current-base pdftext dictionary keep_chars boundary. `PdfTextDocumentExtractor` now mirrors upstream pdftext `dictionary_output(..., keep_chars=False)` sanitation by stripping non-core block/line keys and raw span `chars` before storing `char_blocks`, while preserving char offsets and selected-range metadata. `PdfTextBlockConverter` now rejects missing or non-string span text instead of silently casting malformed dictionaries. Focused evidence: `php -l` passed for changed source, tests, and `wordpress-pdftext-dictionary-core-import.php`; focused `PdfTextBlockConverterTest.php` plus `PdfTextDocumentExtractorTest.php` passed with 2 files, 62 assertions, and 0 failures; the example smoke emitted `raw_chars_present:false`, `char_blocks_raw_chars_present:false`, and the expected Gutenberg paragraph; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; this reuses bounded supplied-dictionary mode without executing Python pdftext, pypdfium, or model workers.

2026-06-02 01:04 UTC: integrated the LZWDecode stream-filter boundary on current `origin/main`. `PdfTextExtractor` now decodes `/LZWDecode` content streams before native text-token parsing, including variable-width code growth and dictionary reset/end-code handling. Focused evidence: `php -l` passed for `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, and `wordpress-pdf-lzw-filter-import.php`; focused `PdfTextExtractorTest.php` passed with 1 file, 107 assertions, and 0 failures; the example smoke emitted `LZW PDF Import` and `Native Blocks`; full markerPDF lane passed with 47 files, 1076 assertions, and 0 failures; `git diff --check --cached -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses the native stream-filter dispatcher and content-token parser.

2026-06-02 01:13 UTC: integrated the native PDF outline and trailer `/Info` metadata boundary on current `origin/main`. `PdfTextExtractor::extractOutlineMetadata()` now reuses the object-stream-aware PDF object inventory and page-tree order to emit upstream-shaped `pdf_toc` rows with `title`, `level`, and zero-based `page`, while also decoding document-info strings for WordPress review metadata. Focused evidence: `php -l` passed for `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, and `wordpress-pdf-outline-import.php`; focused `PdfTextExtractorTest.php` passed with 1 file, 113 assertions, and 0 failures; the example smoke emitted three Gutenberg list items and document info for `WP PDF Import`; full markerPDF lane passed with 47 files, 1082 assertions, and 0 failures; `git diff --check --cached -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses the native PDF object inventory, page-tree walker, and PDF string decoder.

2026-06-02 01:20 UTC: integrated the native PDF named-destination outline boundary on current `origin/main`. `PdfOutlineExtractor` now resolves `/Outlines` entries through direct destination arrays, `/A /GoTo` actions, catalog `/Names /Dests` name trees with `/Kids`, legacy catalog `/Dests` dictionaries, PDF name escapes, UTF-16BE title strings, and stale-destination exclusion before emitting upstream-shaped TOC rows. Focused evidence: `php -l` passed for `PdfOutlineExtractor.php`, `PdfOutlineExtractorTest.php`, and `wordpress-pdf-named-destinations-import.php`; focused `PdfOutlineExtractorTest.php` plus `HeadingCleanerTest.php` passed with 2 files, 19 assertions, and 0 failures; the example smoke emitted three destination-linked Gutenberg list items; full markerPDF lane passed with 48 files, 1088 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice uses a bounded native PDF outline/name-tree parser and does not run Python, pdftext, pypdfium, or external PDF tools.

2026-06-02 01:27 UTC: integrated the PDF link annotation URI boundary on current `origin/main`. `PdfLinkAnnotationExtractor` walks catalog-ordered pages, extracts page-scoped `/Annots` entries whose subtype is `/Link` and action is safe `/S /URI`, excludes text annotations, GoTo destinations, unsafe URI schemes, and unreferenced annotations, then applies matching link URIs to overlapping supplied pdftext spans before `MarkdownPostProcessor` emits Markdown links. Focused evidence: `php -l` passed for `PdfLinkAnnotationExtractor.php`, `MarkdownPostProcessor.php`, `PdfLinkAnnotationExtractorTest.php`, and `wordpress-pdf-link-annotation-import.php`; focused `PdfLinkAnnotationExtractorTest.php` plus `MarkdownPostProcessorTest.php` passed with 2 files, 48 assertions, and 0 failures; the example smoke emitted one linked Gutenberg paragraph and excluded a text annotation; full markerPDF lane passed with 49 files, 1107 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses native PDF page/object parsing, supplied pdftext bboxes, and the existing Markdown span merge path.

2026-06-02 01:30 UTC: integrated the DCTDecode filter boundary on current `origin/main`. `PdfTextExtractor` now treats `/DCTDecode` and `/DCT` streams as JPEG/image-only payloads and skips them before text-token parsing, preventing raster bytes that contain PDF-looking text operators from leaking into imported WordPress paragraphs. Focused evidence: `php -l` passed for `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, and `wordpress-pdf-dctdecode-filter-import.php`; focused `PdfTextExtractorTest.php` passed with 1 file, 119 assertions, and 0 failures; the example smoke emitted `DCT PDF Import` and `Clean Paragraphs` while excluding JPEG noise; full markerPDF lane passed with 49 files, 1113 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses the native PDF object parser, stream-filter dispatcher, and content-token parser.

2026-06-02 01:36 UTC: integrated the CCITTFaxDecode filter boundary on current `origin/main`. `PdfTextExtractor` now treats `/CCITTFaxDecode` and `/CCF` streams as fax/image-only payloads and skips them before text-token parsing, including direct and indirect `DecodeParms` cases, preventing scanner bytes that contain PDF-looking text operators from leaking into imported WordPress paragraphs. Focused evidence: `php -l` passed for `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, and `wordpress-pdf-ccitt-fax-filter-import.php`; focused `PdfTextExtractorTest.php` passed with 1 file, 127 assertions, and 0 failures; the example smoke emitted `CCITT Boundary` and `Native Import` while excluding fax noise; full markerPDF lane passed with 49 files, 1121 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses the native PDF object parser, stream-filter dispatcher, DecodeParms resolver, and content-token parser.

2026-06-02 01:45 UTC: integrated the catalog `/PageLabels` number-tree boundary on current `origin/main`. `PdfTextExtractor` now parses page-label number trees from the catalog, follows `/Kids`, accepts direct and indirect `/Nums` label dictionaries, formats decimal, roman, and alphabetic page labels with `/P` prefixes and `/St` starts, falls back to one-based labels when metadata is absent, and exposes labeled page text entries aligned with catalog page `/Contents` extraction. Focused evidence: `php -l` passed for `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, and `wordpress-pdf-page-labels-import.php`; focused `PdfTextExtractorTest.php` passed with 1 file, 135 assertions, and 0 failures; the example smoke emitted `front-ii`, `Body 1`, and `App-AA` page-break labels plus Gutenberg paragraphs; full markerPDF lane passed with 49 files, 1129 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses the native PDF object parser, page-tree ordering, string decoder, and page `/Contents` extraction path.

2026-06-02 01:48 UTC: integrated the catalog XMP metadata boundary on current `origin/main`. `PdfMetadataExtractor` now resolves the catalog `/Metadata` stream, decodes common PDF stream filters, extracts Dublin Core and XMP fields, records XMP-over-Info source precedence, falls back to trailer `/Info` when XMP is missing or malformed, and keeps metadata streams out of visible text extraction. Focused evidence: `php -l` passed for `PdfMetadataExtractor.php`, `PdfMetadataExtractorTest.php`, and `wordpress-pdf-xmp-metadata-import.php`; focused `PdfMetadataExtractorTest.php` passed with 1 file, 25 assertions, and 0 failures; the example smoke emitted XMP-preferred document metadata and `Visible PDF Body`; full markerPDF lane passed with 50 files, 1154 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses native PDF object parsing, stream decoding, and PDF string fallback paths.

2026-06-02 01:51 UTC: integrated the CIDFont width-metrics boundary on current `origin/main`. `PdfTextExtractor` now parses descendant-font `/W` arrays and `/DW` default widths, attaches those metrics to the active ToUnicode CMap, and uses them for text-advance grouping so tightly packed CID words stay together while narrow advances still insert spaces. Focused evidence: `php -l` passed for `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, and `wordpress-pdf-cidfont-widths-import.php`; focused `PdfTextExtractorTest.php` passed with 1 file, 140 assertions, and 0 failures; the example smoke emitted `WideBlock` and `Thin Text`; full markerPDF lane passed with 50 files, 1159 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses the native font dictionary parser, CMap source tokenizer, and content-token text positioning path.

2026-06-02 01:57 UTC: integrated the inline image boundary on current `origin/main`. `PdfTextExtractor` now skips `BI ... ID ... EI` inline image payloads before text-token parsing, preventing raster bytes that contain PDF-looking text operators from leaking into WordPress paragraphs. Focused evidence: `php -l` passed for `PdfTextExtractor.php`, `PdfTextExtractorTest.php`, and `wordpress-pdf-inline-image-boundary-import.php`; focused `PdfTextExtractorTest.php` passed with 1 file, 146 assertions, and 0 failures; the example smoke emitted `Visible Before Image` and `Visible After Image` while excluding inline image noise; full markerPDF lane passed with 50 files, 1165 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; the slice reuses the native content-token parser and page `/Contents` extraction path.

2026-06-02 02:02 UTC isolated lane: implemented the nested Form XObject resource-boundary slice on current base. Source truth is the PDF resource model used before Marker's `marker/pdf/extract_text.py::naive_get_text` page text boundary: invoked Form XObjects carry their own `/Resources`, and nested forms must not resolve text fonts through the parent page's resource names. `PdfTextExtractor` now aliases each invoked form's local font resources while recursively expanding `/Subtype /Form` XObjects, so a page, parent form, and child form can all use `/F1` with distinct font maps before WordPress paragraph extraction. Red-first evidence: the new focused test initially failed because `Parent Form` and `Child Form` decoded as `A` and `B` through the page font map. After the fix, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 file, 152 assertions, and 0 failures; `php tools/run-tests.php lanes/markerpdf/tests` passed with 50 files, 1171 assertions, and 0 failures; `php lanes/markerpdf/examples/wordpress-pdf-nested-xobject-form-import.php` emitted the expected four Gutenberg paragraphs with parent/child resource-scope flags set. Dependency closure: no new support component is needed; the slice reuses the native PDF object parser, form invocation expander, font-resource maps, CMap lookup, and content-token parser without Python, pdftext, pypdfium, or external PDF tools.

2026-06-02 02:55 UTC isolated lane: implemented the CIDSet subset glyph default-width slice on current base. `PdfTextExtractor` now resolves descendant CIDFont `/FontDescriptor` dictionaries, decodes compressed `/CIDSet` streams, parses high-order-bit CID membership, and uses present subset CIDs as evidence for the CIDFont default `/DW 1000` advance when explicit `/W` widths are absent. Red-first evidence: before the fix, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` failed because the CIDSet fixture emitted `Wide Block`; after the fix, the same focused test passed with 1 file, 158 assertions, and 0 failures. `php lanes/markerpdf/examples/wordpress-pdf-cidset-glyph-widths-import.php` emitted `WideBlock` with `executes_python_or_models=false` and `executes_external_pdf_tools=false`. Dependency closure: no new support component is needed; the slice reuses the native PDF object parser, stream decoder, descendant font map, CMap/font metrics path, and text-positioning grouping logic. Full upstream Python/model benchmark parity remains dependency-gated.

2026-06-02 03:21 UTC isolated lane: implemented the marked-content ActualText/Alt replacement slice on current base. Source truth is Marker upstream `marker/pdf/extract_text.py::naive_get_text` / `get_text_blocks` text extraction at the pdftext/pypdfium boundary, plus PDF marked-content property-list semantics where `/ActualText` replaces marked-content glyph text and `/Alt` supplies alternate text. `PdfTextExtractor` now parses inline `<<...>>` property dictionaries in content streams, resolves named page `/Resources /Properties` property lists, emits `/ActualText` once with `/Alt` fallback, and suppresses original glyph/image noise inside the marked sequence. Red-first evidence: focused `PdfTextExtractorTest` initially failed with raw `Glyph Noise`, `Resource Glyph Noise`, and `Inline Alt Glyphs`; after the fix it passed with 1 file, 204 assertions, and 0 failures. Full markerPDF lane passed with 51 files, 1302 assertions, and 0 failures. `wordpress-pdf-marked-content-actualtext-import.php` emitted five Gutenberg paragraphs and recorded native-only flags. Dependency closure: no new support component is needed; the slice reuses the native PDF object parser, page resource dictionary lookup, PDF string decoder, content-token parser, and stream/image-filter boundaries. Full upstream Python/model benchmark parity remains dependency-gated.

2026-06-02 03:55 UTC supervisor integration: accepted marked-content ActualText/Alt replacement, optional content group visibility, and Type3 CharProc width handling on current base. Focused `PdfTextExtractorTest` passed with 1 file, 251 assertions, and 0 failures; full `php tools/run-tests.php lanes/markerpdf/tests` passed with 51 files, 1446 assertions, and 0 failures. The OCG merge was adjusted to preserve visible `BDC`/`BMC`/`EMC` operators so ActualText/Alt and StructTree MCID behavior remain available while hidden optional-content groups are filtered. `wordpress-pdf-type3-charproc-widths-import.php` emits `WideBlock` and `Thin Text` using `/CharProcs` d0/d1 widths without Python, pdftext, pypdfium, Poppler, Ghostscript, or external PDF tools.

2026-06-02 04:10 UTC supervisor integration: accepted trailer `/ID` fingerprint metadata, catalog language/viewer-preference metadata, and catalog OpenAction safety-review metadata on current base. `PdfMetadataExtractor` now exposes stable trailer document fingerprints plus catalog `/Lang` and `/ViewerPreferences`; `PdfOutlineExtractor` classifies catalog OpenAction `URI`, `Launch`, local `GoTo`, and remote `GoToR` rows as non-executing review metadata before WordPress import. Focused evidence: `PdfMetadataExtractorTest` passed with 1 file, 74 assertions, and 0 failures after the trailer/catalog merge; `PdfOutlineExtractorTest` passed with 1 file, 36 assertions, and 0 failures; the outline/heading gate passed with 2 files, 49 assertions, and 0 failures; `wordpress-pdf-openaction-safety-review.php` emitted three review-only action rows; the full markerPDF lane passed with 51 files, 1497 assertions, and 0 failures. Dependency closure: no new support component is needed; the slice reuses the native PDF object parser, string decoder, metadata extractor, name/destination resolver, and outline action classifier.

2026-06-02 04:25 UTC supervisor integration: accepted length-bounded ASCIIHex/RunLength stream filters, destination Fit/XYZ page-view metadata, XFA packet review metadata, and EmbeddedFiles name-tree attachment metadata on current base. `PdfTextExtractor` now honors direct or indirect `/Length` before applying stream filters and skips `/Type /EmbeddedFile` payload streams during fallback text extraction; `PdfOutlineExtractor` exposes catalog `/PageMode`, `/PageLayout`, `/OpenAction`, and outline destination `/Fit*`/`/XYZ` view metadata; `PdfAcroFormExtractor` emits review-only XFA packet metadata; `PdfEmbeddedFileExtractor` emits attachment rows from catalog `/Names /EmbeddedFiles` name trees. Focused evidence: `PdfTextExtractorTest` passed with 1 file, 254 assertions, and 0 failures; `PdfOutlineExtractorTest` passed with 1 file, 55 assertions, and 0 failures; `PdfAcroFormExtractorTest` passed with 1 file, 121 assertions, and 0 failures; `PdfEmbeddedFileExtractorTest` plus `PdfTextExtractorTest` passed with 2 files, 291 assertions, and 0 failures; the full markerPDF lane passed with 52 files, 1589 assertions, and 0 failures. Dependency closure: no new support component is needed; these slices reuse the native PDF object parser, stream decoder, outline/name-tree parser, AcroForm parser, and review metadata paths without Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 04:15 UTC supervisor integration: accepted rich media/screen annotation review metadata, text-markup QuadPoints review metadata, and AcroForm SubmitForm/ResetForm action review metadata on current base. `PdfRichMediaAnnotationExtractor` emits non-executing review rows for `/Screen` and `/RichMedia` annotations while `PdfTextExtractor` keeps their media appearance streams out of visible paragraph extraction; `PdfMarkupAnnotationExtractor` maps text-markup `/QuadPoints` onto supplied pdftext spans for editorial review markup; `PdfAcroFormExtractor` reports `/SubmitForm` and `/ResetForm` actions without executing PDF actions. Focused evidence: `PdfRichMediaAnnotationExtractorTest` plus `PdfTextExtractorTest` passed with 2 files, 282 assertions, and 0 failures; `PdfMarkupAnnotationExtractorTest` passed with 1 file, 40 assertions, and 0 failures; `PdfAcroFormExtractorTest` passed with 1 file, 148 assertions, and 0 failures; the three WordPress example smokes emitted review-only metadata; the full markerPDF lane passed with 54 files, 1684 assertions, and 0 failures. Dependency closure: no new support component is needed; these slices reuse the native PDF object parser, page annotation traversal, supplied pdftext span metadata, AcroForm parser, and review metadata paths without Python, pdftext, pypdfium, Poppler, Ghostscript, media execution, or PDF action execution.

2026-06-02 04:20 UTC supervisor integration: accepted catalog associated Filespec `/AFRelationship` metadata, JavaScript action safety-review metadata, and Base14/simple font width metrics on current base. `PdfEmbeddedFileExtractor` now emits catalog `/AF` associated-file rows with relationship, description, MIME, size, and payload-review metadata; `PdfJavaScriptActionInspector` inventories document name-tree, catalog, page, annotation, and chained JavaScript actions without executing scripts; `PdfTextExtractor` applies Base14 and explicit simple-font widths before same-line gap decisions. Focused evidence: `PdfEmbeddedFileExtractorTest` passed with 1 file, 61 assertions, and 0 failures; `PdfJavaScriptActionInspectorTest` plus `PdfLinkAnnotationExtractorTest` plus `PdfTextExtractorTest` passed with 3 files, 312 assertions, and 0 failures; the three WordPress example smokes emitted review-only/native text output; the full markerPDF lane passed with 55 files, 1747 assertions, and 0 failures. Dependency closure: no new support component is needed; these slices reuse native PDF object parsing, Filespec/EF stream decoding, name-tree/action traversal, link safety filtering, and text-positioning paths without JavaScript execution, Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 04:24 UTC supervisor integration: accepted xref stream `/Index` plus zero-width `/W` handling, cyclic ToUnicode `usecmap` plus codespace-count guards, and annotation border/color/opacity/popup review metadata on current base. `PdfTextExtractor` now honors explicit xref stream index ranges and zero-width field defaults before page extraction, prevents mutual CMap inheritance cycles, and ignores extra codespace rows beyond the declared count; `PdfAnnotationExtractor` emits review metadata for `/Border`, `/BS`, `/C`, `/IC`, `/CA`, and `/Popup` dictionaries while suppressing duplicate popup rows. Focused evidence: `PdfTextExtractorTest` passed with 1 file, 272 assertions, and 0 failures; `PdfAnnotationExtractorTest` passed with 1 file, 61 assertions, and 0 failures; the three WordPress example smokes emitted native/review output; the full markerPDF lane passed with 56 files, 1819 assertions, and 0 failures. Dependency closure: no new support component is needed; these slices reuse native PDF object parsing, stream/xref parsing, ToUnicode CMap parsing, page annotation traversal, and text extraction paths without Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 04:27 UTC supervisor integration: accepted catalog `/Threads` bead reading-order metadata on current base. `PdfTextExtractor` now follows catalog `/Threads`, each thread `/F` bead chain, `/N` links, page `/P` references, and bead `/R` rectangles so article text can be emitted before raw column/page content order when the PDF supplies thread structure. Focused evidence: `PdfTextExtractorTest` passed with 1 file, 277 assertions, and 0 failures; `wordpress-pdf-thread-bead-reading-order-import.php` emitted four Gutenberg paragraphs in bead order; the full markerPDF lane passed with 56 files, 1824 assertions, and 0 failures. Dependency closure: no new support component is needed; this slice reuses native PDF object parsing, page text-position parsing, font maps, and content-token extraction without Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 04:33 UTC supervisor integration: accepted the object-generation free-entry reuse guard on current base. `PdfTextExtractor` now respects explicit xref entries while expanding `/ObjStm` members: matching compressed entries are accepted, while current free, direct, or mismatched entries suppress recovered members so stale generation-reuse pages cannot leak into WordPress text extraction. Focused evidence: `PdfTextExtractorTest` passed with 1 file, 284 assertions, and 0 failures; `wordpress-pdf-object-generation-free-entry-import.php` emitted only `Current object generation page` and excluded the stale freed object-stream member; the full markerPDF lane passed with 56 files, 1831 assertions, and 0 failures. Dependency closure: no new support component is needed; this slice reuses native object inventory, xref table/stream parsing, object-stream decoding, page-tree walking, and content-token parsing without Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 04:38 UTC supervisor integration: accepted text-markup annotation border/popup review metadata on current base. `PdfMarkupAnnotationExtractor` now carries legacy `/Border` arrays, `/BS` border-style dictionaries, and direct or indirect `/Popup` annotations into extracted markup rows and overlapping supplied pdftext review spans. Focused evidence: `PdfMarkupAnnotationExtractorTest` passed with 1 file, 50 assertions, and 0 failures; `wordpress-pdf-highlight-review-import.php` emitted dashed border and popup review metadata; the full markerPDF lane passed with 56 files, 1841 assertions, and 0 failures. Dependency closure: no new support component is needed; this slice reuses native PDF object parsing, page annotation traversal, PDF string/name decoding, supplied pdftext span geometry, and existing review metadata paths without Python, pdftext, pypdfium, Poppler, Ghostscript, or annotation rendering.

2026-06-02 04:58 UTC isolated lane: implemented the ToUnicode CMap comment-stripping edge on current base. Source truth is the upstream `marker/pdf/extract_text.py` text boundary at `sddai/markerPDF` `da6a2f5`, which delegates PDF text decoding to pdftext/pypdfium; PDF/PostScript CMap `%` comments must not become glyph mappings. `PdfTextExtractor` now strips line comments outside literal and hex tokens before CMapName/usecmap/codespace/bfchar/bfrange parsing. Red-first evidence: the new focused test initially failed with expected `ACleanDE` and actual `NoiseCleanXY`. After the fix, `php tools/run-tests.php lanes/markerpdf/tests/PdfTextExtractorTest.php` passed with 1 file, 298 assertions, and 0 failures, `php lanes/markerpdf/examples/wordpress-pdf-cmap-comment-import.php` emitted only `ACleanDE`, and `php tools/run-tests.php lanes/markerpdf/tests` passed with 56 files, 1855 assertions, and 0 failures. Dependency closure: no new support component is needed; this reuses native `PdfTextExtractor` CMap/font parsing, stream decoding, and text-token extraction without Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 05:18 UTC supervisor integration: accepted indirect name-tree destination parsing on current base. `PdfOutlineExtractor` now resolves indirect `/Names /Dests` string keys and indirect outline/action destination labels through the object table before TOC, destination-view, and catalog OpenAction review metadata. Focused evidence: `PdfOutlineExtractorTest` passed with 1 file, 110 assertions, and 0 failures; `wordpress-pdf-indirect-nametree-destinations-import.php` emitted two resolved destination list items plus `/FitBH`, `/FitR`, and OpenAction review metadata; the full markerPDF lane passed with 56 files, 2012 assertions, and 0 failures. Dependency closure: no new support component is needed; this reuses native PDF object parsing, name-tree traversal, PDF string/name decoding, destination resolution, and review metadata paths without Python, pdftext, pypdfium, Poppler, Ghostscript, or external PDF tools.

2026-06-02 05:24 UTC supervisor integration: accepted page annotation geometry review metadata on current base. `PdfAnnotationExtractor` now extracts geometry for line, ink, polygon, polyline, square, circle, and FreeText callout annotations, including line endings, leader lines, caption offsets, rect differences, derived shape rectangles, centers, radii, vertices, paths, and bounding boxes. Focused evidence: `PdfAnnotationExtractorTest` passed with 1 file, 92 assertions, and 0 failures; `wordpress-pdf-annotation-geometry-import.php` emitted six review-only geometry rows while confirming no action execution or appearance rendering; the full markerPDF lane passed with 56 files, 2043 assertions, and 0 failures. Dependency closure: no new support component is needed; this reuses native PDF object parsing, page annotation traversal, PDF array/name decoding, and review metadata paths without Python, pdftext, pypdfium, Poppler, Ghostscript, rendering, or action execution.

2026-06-02 06:28 UTC supervisor integration: accepted ICCBased image color-space and soft-mask metadata on current base. `PdfImageRenderer` now records direct/indirect ICC profile metadata, `/Alternate`, `/Range`, `/SMask`, `/Matte`, `/SMask /None`, matte-unblend requirements, and RGB preview intent before any future raster backend runs. Focused evidence: `php -l` passed for `PdfImageRenderer.php`, `PdfImageRendererTest.php`, and `wordpress-pdf-icc-softmask-image-review.php`; focused `PdfImageRendererTest.php` passed with 1 file, 62 assertions, and 0 failures; the example smoke emitted ICC profile and soft-mask review metadata with `executes_pypdfium_or_pil=false`; full markerPDF lane passed with 56 files, 2153 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; this remains a native review/planning boundary for Marker's upstream `marker/pdf/images.py` RGB output behavior while live pypdfium/PIL raster execution remains dependency-gated.

2026-06-02 06:47 UTC supervisor integration: accepted Standard encryption permission metadata on current base. `PdfMetadataExtractor` now resolves trailer and xref-stream `/Encrypt` dictionaries and emits review-only Standard security-handler metadata for `/V`, `/R`, `/Length`, `/EncryptMetadata`, `/StmF`, `/StrF`, `/EFF`, `/CF`, signed/unsigned `/P` permission flags, allow/deny labels, print quality, and hashed `/Perms` without password validation, decryption, or raw owner/user key exposure. Focused evidence: `php -l` passed for `PdfMetadataExtractor.php`, `PdfMetadataExtractorTest.php`, and `wordpress-pdf-encryption-permission-metadata-import.php`; focused `PdfMetadataExtractorTest.php` passed with 1 file, 106 assertions, and 0 failures; the example smoke emitted `encrypted_text_blocked=true`, permission hex `FFFF0A14`, and no raw key exposure; full markerPDF lane passed with 56 files, 2185 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; this reuses native PDF object/trailer parsing and encrypted-PDF preflight while full decryption remains intentionally unimplemented.

2026-06-02 07:04 UTC supervisor integration: accepted embedded-file `/Params /CheckSum` review metadata on current base. `PdfEmbeddedFileExtractor` now decodes `/CheckSum` as a binary-safe PDF byte string, normalizes MD5 checksums to lowercase hex, computes the current payload MD5, and reports `checksum_algorithm`, `computed_checksum`, and `checksum_matches` without dropping mismatched attachments. Focused evidence: `php -l` passed for `PdfEmbeddedFileExtractor.php`, `PdfEmbeddedFileExtractorTest.php`, and `wordpress-pdf-embedded-files-import.php`; focused `PdfEmbeddedFileExtractorTest.php` passed with 1 file, 119 assertions, and 0 failures; the example smoke emitted matching declared/computed MD5 and `checksum_matches=true`; full markerPDF lane passed with 56 files, 2217 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; this reuses native PDF object parsing, byte-string/literal escape decoding, stream decoding, Filespec/EF parsing, and attachment review metadata paths.

2026-06-02 07:16 UTC supervisor integration: accepted XMP and trailer Info date timezone normalization on current base. `PdfMetadataExtractor` now preserves raw `created_at`, `modified_at`, and `metadata_date` values while adding `*_utc` fields only when XMP ISO-8601 or PDF `/Info` `D:` dates include an explicit timezone; timezone-free dates remain raw-only. Focused evidence: `php -l` passed for `PdfMetadataExtractor.php`, `PdfMetadataExtractorTest.php`, and `wordpress-pdf-xmp-metadata-import.php`; focused `PdfMetadataExtractorTest.php` passed with 1 file, 123 assertions, and 0 failures; the example smoke emitted `timezone_normalized_to_utc=true` with raw and UTC date fields; full markerPDF lane passed with 56 files, 2234 assertions, and 0 failures; `git diff --check -- lanes/markerpdf` passed. Dependency closure: no new support component is needed; this reuses native PDF object parsing, XMP XML parsing, PDF string decoding, and metadata merge paths.

2026-06-02 06:11 UTC isolated lane: implemented the stream-filter error-boundary slice on current base. `PdfTextExtractor` now fails closed for declared but unsupported, corrupt, unresolved indirect, or stacked unknown stream filters instead of treating filtered bytes as raw content streams. Red-first evidence: before the fix, a `/Filter /Crypt` stream emitted `Unsupported filter leak`; after the fix, `PdfTextExtractorTest` passed with 1 file, 344 assertions, and 0 failures, the full markerPDF lane passed with 56 files, 2295 assertions, and 0 failures, and `wordpress-pdf-stream-filter-error-boundary-import.php` emitted only `Filter Boundary Visible` and `Filter Boundary Tail` with all error-boundary exclusion flags true. Dependency closure: no new support component is needed; this reuses the native PDF object parser, stream decoder, filter dispatcher, and content-token parser without Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 06:35 UTC isolated lane: implemented the PDFDocEncoding string metadata slice on current base. Source truth is the upstream markerPDF metadata/text boundary plus PDF text-string parser behavior: non-BOM PDF text strings use PDFDocEncoding, while BOM-prefixed strings use UTF-16. `PdfMetadataExtractor` now decodes trailer `/Info`, catalog, and OutputIntent PDF string bytes through the PDFDocEncoding map after preserving existing UTF-16BE/UTF-16LE BOM handling. Red-first evidence: the new focused test initially failed with expected `WordPress• PDF ﬁﬂ Import €` and actual `NULL`; after the fix, `PdfMetadataExtractorTest` passed with 1 file, 131 assertions, and 0 failures, and `wordpress-pdf-pdfdocencoding-metadata-import.php` emitted decoded title/authors/keywords with `metadata_not_visible_text=true`. Dependency closure: no new support component is needed; this reuses native PDF object parsing, literal/hex string readers, metadata merging, and PHP UTF-8 conversion without Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 06:55 UTC isolated lane: mapped the stream `/Filter` name-array indirect current-base edge. Source truth is the upstream markerPDF PDF text extraction boundary (`marker/pdf/extract_text.py::naive_get_text` / `get_text_blocks`) plus PDF stream filter-array semantics. `PdfTextExtractor` already resolves `/Filter [ 2 0 R null 3 0 R ]` on current base, so this handoff adds focused regression coverage and a WordPress smoke that decode ASCIIHex then Flate through indirect filter-name objects, ignore `null`, and fail closed on a cyclic filter-name reference. Focused evidence: `php -l` passed for `PdfTextExtractorTest.php` and `wordpress-pdf-filter-name-array-indirect-import.php`; `jq empty lanes/markerpdf/UPSTREAM_TEST_MANIFEST.json lanes/markerpdf/lane-status.json` passed; the WordPress smoke emitted `Name Array Indirect Filter` and `Block Ready Import`; focused `PdfTextExtractorTest.php` passed with 1 file, 398 assertions, and 0 failures; full markerPDF lane passed with 58 files, 2423 assertions, and 0 failures. Dependency closure: no new support component is needed; this reuses native PDF object parsing, stream-filter dispatch, and content-token parsing without Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 08:10 UTC isolated lane: implemented the image `/Decode` and `/ImageMask` stencil preview boundary on current base. Source truth is upstream `marker/pdf/images.py::render_image` / `render_bbox_image`, which renders through pypdfium, disables annotations for extraction crops, and converts the result to RGB before `marker/images/extract.py::extract_page_images` inserts image Markdown. `PdfImageRenderer` now records base image `/Decode` arrays before RGB preview conversion, keeps mismatched component counts reviewable, applies valid Decode arrays to decoded samples, and maps `/ImageMask true` stencil `/Decode` arrays, defaulting to `[0 1]`, into zero/one sample opacity. Red-first evidence: the focused renderer test initially failed with missing `image_decode` and `image_mask` metadata; after the fix, `php tools/run-tests.php lanes/markerpdf/tests/PdfImageRendererTest.php` passed with 1 file, 85 assertions, and 0 failures, and the full markerPDF lane passed with 59 files, 2619 assertions, and 0 failures. Dependency closure: no new support component is needed; this reuses the native PDF value parser and renderer review planner while full raster parity remains gated on pypdfium2/PIL or a native raster backend.

2026-06-02 08:19 UTC supervisor integration: accepted annotation appearance Form XObject resource boundaries on current base. Source truth is upstream markerPDF's `marker/pdf/extract_text.py` pdftext/PDFium boundary plus PDF appearance stream semantics: selected page annotation `/AP /N` streams are Form XObjects with their own `/Resources`, `/Matrix`, and `/BBox`. `PdfTextExtractor` now passes selected annotation appearance Form XObject `/Matrix` and `/BBox` into the existing Form XObject expansion path, clipping out-of-BBox appearance text while preserving top-level and nested appearance-local `/F1` font scopes. Red-first evidence from the worker showed `BBox Noise` leaking before the fix; current-base verification will be recorded in the supervisor status update after focused and full gates complete. Dependency closure: no new support component is needed; this reuses native PDF object parsing, page annotation traversal, stream decoding, Form XObject expansion, CMap/font maps, matrix handling, and BBox clipping without Python, pdftext, pypdfium, Poppler, Ghostscript, or model downloads.

2026-06-02 16:08 UTC isolated lane: implemented encrypted PDF permission-handler review on current base. `PdfMetadataExtractor` now records Standard `/P` reserved-bit well-formedness metadata, and `PdfSecurityPreflight` emits `permission_handler_review` so malformed Standard permission words and unsupported non-Standard handlers are review-only instead of being treated as reliable copy/extract grants. Red-first evidence: `PdfSecurityPreflightTest.php` failed with missing `permission_handler_review` and both malformed/unsupported fixtures reported as `copy_extract_allowed_after_decryption`; after the fix, `PdfSecurityPreflightTest.php` passed with 1 file, 236 assertions, and 0 failures, and `PdfMetadataExtractorTest.php` passed with 1 file, 308 assertions, and 0 failures. The WordPress smoke emitted malformed and unsupported handler review policies while blocking encrypted text and exposing no raw owner/user/recipient bytes, decryption, permission enforcement, Python/models, or external PDF tools. Counters: `phpPass` moves 534 -> 536 and mapped focused source/dependency semantics move 381 -> 382 / 78. Dependency closure: no new support component is needed; this reuses native PDF object/trailer parsing and encrypted-PDF preflight while decryption, public-key recipient permission decoding, signature validation, and permission enforcement remain intentionally unimplemented.

2026-06-02 16:40 UTC isolated lane: implemented image ColorKey `/Mask` plus `/Decode` review on current base. Source truth is upstream `marker/pdf/images.py::render_image` / `render_bbox_image`, which delegates raster behavior to PDFium and returns RGB images before `marker/images/extract.py::extract_page_images` inserts Markdown image spans; color-key PDF masks compare raw component samples before image Decode. `PdfImageRenderer` now emits `color_key_mask` metadata, flags component mismatches, and exposes `colorKeyMaskSamplePreview()` so matching raw samples return alpha `0.0` while nonmatching samples return alpha `1.0` and surviving samples still receive Decode-adjusted RGB review components. Red-first evidence: the focused renderer test failed with missing `color_key_mask` metadata; after the fix, `PdfImageRendererTest.php` passed with 1 file, 297 assertions, and 0 failures. The WordPress smoke emitted raw-sample-before-Decode mask metadata with pypdfium/PIL/external tooling disabled. Counters: `phpPass` moves 564 -> 565 and mapped image semantics move 404 -> 405 / 78. Dependency closure: no new support component is needed; this reuses native PDF dictionary/value parsing and renderer review planning while full raster parity remains gated on pypdfium2/PIL or a future native raster backend.

2026-06-02 18:36 UTC isolated lane: implemented page annotation reply-thread review on current base. Source truth is the upstream markerPDF pdftext/PDFium extraction boundary plus PDF page annotation dictionary behavior: page `/Annots` defines current annotation membership, `/IRT` points to the annotation being replied to, `/RT` distinguishes ordinary replies from grouped annotations, and `/State` plus `/StateModel` carry review state. `PdfAnnotationExtractor` now emits additive `reply_thread` metadata on annotation rows, page-level `annotation_threads` summaries for current-page reply roots, and `detached_annotation_thread_replies` for `/IRT` targets outside the page annotation array. Focused evidence: `PdfPageAnnotationThreadCurrentBaseTest.php` passed with 1 file, 66 assertions, and 0 failures; `PdfAnnotationExtractorTest.php` plus the new current-base test passed with 2 files, 329 assertions, and 0 failures; the WordPress smoke emitted root object 6, reply objects 7 and 8, reply/group labels, Accepted/Marked review states, detached reply object 9 targeting object 90, and visible-text exclusion flags. Counters: `phpPass` moves 645 -> 646 and mapped page annotation semantics move 471 -> 472 / 78. Dependency closure: no new support component is needed; this reuses native PDF object parsing, page annotation traversal, popup nesting, dictionary/name/string decoders, and text extraction boundaries without Python, pdftext, pypdfium/PDFium execution, Poppler, Ghostscript, models, action execution, or annotation rendering.

2026-06-02 18:44 UTC isolated lane: implemented inline ImageMask packed-sample preview rows on current base. Source truth is upstream `marker/pdf/images.py::render_image` / `render_bbox_image`, which renders through pypdfium, disables annotations, converts to RGB, and then `marker/images/extract.py::extract_page_images` inserts image Markdown spans; native PHP keeps this as parser-side review metadata without raster execution. `PdfImageRenderer::inlineImageMaskPreviewRows()` now expands inline image abbreviations, decodes native inline payload filters, unpacks packed stencil samples, maps `/ImageMask` `/Decode` values to opacity, reports incomplete sample data, and keeps inline payload text out of WordPress paragraphs. Red-first evidence: the focused test failed with missing `inlineImageMaskPreviewRows()`; after the fix, the image-focused gate passed with 3 files, 521 assertions, and 0 failures, and the WordPress smoke emitted `visible_text_imported=true`, `excluded_inline_mask_payload_text=true`, four opacity preview pixels, and no Python/PIL/external-tool execution. Counters: `phpPass` moves 648 -> 650 and mapped image semantics move 474 -> 475 / 78. Dependency closure: no new support component is needed; this reuses native PDF dictionary parsing, filter decoding, packed sample reading, and ImageMask `/Decode` opacity mapping while full raster parity remains gated on pypdfium2/PIL or a future native raster backend.

2026-06-02 21:18 UTC isolated lane: implemented current trailer ID plus top-level catalog language/viewer-preference precedence on current base. Source truth is upstream markerPDF `marker/pdf/extract_text.py`, which delegates PDF page text extraction to pdftext/PDFium, plus `marker/output.py`, which writes metadata separately from Markdown output; at the parser boundary, catalog `/Lang`, `/PageLayout`, `/PageMode`, and `/ViewerPreferences` are top-level catalog keys, not nested review-dictionary keys. `PdfMetadataExtractor` now reads those catalog fields through the top-level dictionary parser and resolves viewer-preference operands from top-level preference entries, while keeping the latest xref-stream trailer `/ID` authoritative over stale textual trailers. Red-first probe selected `stale-nested-lang` and nested viewer preferences before the fix; after the fix, `PdfMetadataTrailerIdLangViewerPreferenceCurrentBaseTest.php` passed with 1 file, 20 assertions, and 0 failures, the metadata family gate passed with 2 files, 858 assertions, the broader `PdfMetadata*.php` gate passed with 9 files, 1120 assertions, and 0 failures, and `wordpress-pdf-metadata-trailer-id-lang-viewer-preference-currentbase.php` emitted `current_id_selected=true`, `nested_catalog_decoy_excluded=true`, `language=en-US`, and the current Gutenberg paragraph. Counters: `phpPass` moves 837 -> 838 and mapped metadata semantics move 587 -> 588 / 78. Dependency closure: no new support component is needed; this reuses native object/xref parsing, top-level dictionary parsing, PDF string/name decoding, metadata merging, and the WordPress smoke path while full upstream runtime parity remains dependency-gated.

2026-06-04 UTC isolated lane: implemented the CCITT Fax DecodeParms fail-closed review boundary on accepted base `d74bad6c88fb561dfd80595abb30cd894a59e542`. Source truth is upstream markerPDF's pdftext/PDFium text extraction boundary plus image rendering handoff: CCITTFaxDecode/CCF raster streams stay out of visible text, while PDF CCITT DecodeParms remain review metadata. `PdfTextExtractor` now distinguishes malformed or unresolved CCITT operands from absent/default operands by adding `valid_decode_parms=false`, ordered `invalid_decode_parms_fields`, and `decode_parms_review=invalid_ccitt_decodeparms_fail_closed` only for invalid rows. Red-first probe showed `/K /TwoD`, `/Columns -4`, `/Rows 99 0 R`, and `/BlackIs1 /Maybe` collapsed into null-ish/default-looking review values; after the fix, `PdfCcittFaxFilterBoundaryCurrentBaseTest.php` passed with 1 file, 63 assertions, and 0 failures, and `wordpress-pdf-ccitt-fax-decodeparms-failclosed-currentbase.php` emitted invalid-field metadata while excluding fax payload bytes and running no Python/models/external PDF tools. Counters: `phpPass` moves 1127 -> 1128, WordPress scenarios move 1120 -> 1121, and mapped CCITT current-base semantics add `pdfCcittFaxDecodeParmsFailClosedCurrentBase`. Dependency closure: no new support component is needed; full CCITT raster parity remains gated on pypdfium2/PDFium/PIL or a future native raster backend.

2026-06-05 UTC isolated lane: implemented the named-destination indirect view-operand boundary on accepted base `e25ad1f2ac6eaccdea2f6b6dc8a510504a91892b`. Source truth is upstream markerPDF's pdftext/PDFium destination/navigation boundary plus PDF explicit destination arrays: view mode names and numeric view parameters may be indirect objects. `PdfNamedDestinationExtractor` now resolves indirect `/FitH`, `/XYZ`, `/FitR`, and coordinate operands before emitting standalone catalog `/Names /Dests` metadata, while keeping page-reference generation validation and name-tree `/Limits` pruning intact. Red-first focused evidence dropped all indirect-operand destination rows; after the fix, `PdfNamedDestinationExtractorTest.php` passed with 1 file, 53 assertions, and 0 failures, and `wordpress-pdf-named-destinations-import.php` emitted `destination_count=6` plus `indirect_destination_operands_resolved=true` without Python/models/external PDF tools. Counters: `phpPass` moves 1205 -> 1206, WordPress scenarios move 1185 -> 1186, and mapped named-destination current-base semantics move 3 -> 4. Dependency closure: no new support component is needed; this reuses the native PDF object scanner, name-tree walker, exact-generation resolver, and destination normalizer.

2026-06-05 UTC isolated lane: implemented the pdftext dictionary character/font core-key boundary on accepted base `b3e31fcd254cdb56827a13df0f383fc8e9fe2950`. Source truth is upstream markerPDF's `get_text_blocks()` storage of pdftext dictionary blocks as Marker `char_blocks`, plus pdftext `keep_chars` character dictionaries shaped as `char`, `bbox`, `rotation`, `font`, and `char_idx`. `PdfTextDocumentExtractor` now strips legacy `c` aliases and non-core span/character font payload keys before WordPress review metadata. Red-first focused evidence retained `c` in the character key list; after the fix, `PdfTextDictionaryCoreBoundaryCurrentBaseTest.php` passed with 1 file, 38 assertions, and 0 failures, and the adjacent pdftext gate passed with 3 files, 218 assertions, and 0 failures. `wordpress-pdftext-dictionary-char-core-boundary-currentbase.php` emitted `legacy_c_alias_excluded=true`, `font_payload_excluded=true`, and visible text `Character dictionary import` without Python/pdftext/model/external PDF tool execution. Counters: `phpPass` moves 1273 -> 1274, WordPress scenarios move 1237 -> 1238, and mapped pdftext dictionary core semantics move 1 -> 2. Dependency closure: no new support component is needed; this reuses the native supplied pdftext dictionary sanitizer and WordPress smoke path.

2026-06-05 UTC isolated lane: implemented the pdftext dictionary script-style span boundary on accepted base `9cee22bf103c37b2f933968fe17170dd7125153b`. Source truth is upstream markerPDF's `get_text_blocks()` delegation to pdftext dictionaries plus current pdftext/Marker span metadata where `superscript` and `subscript` are boolean span flags and Marker trims script span text before storing `has_superscript` / `has_subscript`. `PdfTextDocumentExtractor` now preserves only boolean script flags through sanitized `char_blocks`, rejects non-boolean adapter values, and `PdfTextBlockConverter` maps true script flags into rendered span review metadata while excluding private payload keys. Red-first focused evidence showed missing `has_superscript` and no exception for `superscript="yes"`; after the fix, `PdfTextDictionaryCoreBoundaryCurrentBaseTest.php` passed with 1 file, 54 assertions, and 0 failures, and the adjacent pdftext/converter/Markdown gate passed with 4 files, 283 assertions, and 0 failures. `wordpress-pdftext-dictionary-script-style-currentbase.php` emitted `superscript_preserved=true`, `subscript_preserved=true`, `char_blocks_script_flags_preserved=true`, `script_text_trimmed=true`, and no Python/pdftext/model/external PDF tool execution. Counters: `phpPass` moves 1335 -> 1337, WordPress scenarios move 1283 -> 1284, and mapped pdftext dictionary script-style current-base semantics adds 1. Dependency closure: no new support component is needed; this reuses the native supplied pdftext dictionary sanitizer and WordPress smoke path.

2026-06-05 UTC isolated lane: implemented the convert.py worker cleanup runtime preflight boundary on accepted base `e9c085ca25b4381207b8f1a9c219c75c995052d4`. Source truth is upstream markerPDF `convert.py`, where `pool._worker_handler.terminate = worker_exit` and `del model_lst` run after `pool.imap(process_single_pdf, task_args)`. `BatchConverter` now records that cleanup as review-only `worker_pool.pool_cleanup` metadata, including the worker-exit global `model_refs` deletion, parent `model_lst` deletion, CUDA/CPU versus MPS parent-model-list state, and the `pool-process-count-failed` block when zero/negative process counts prevent Pool creation. Red-first focused evidence failed with missing `worker_pool.pool_cleanup`; after the fix, `php tools/run-tests.php lanes/markerpdf/tests/MarkerRuntimePreflightBoundaryCurrentBaseTest.php` passed with 1 file, 613 assertions, and 0 failures. `php lanes/markerpdf/examples/wordpress-marker-runtime-preflight-boundary-currentbase.php` emitted `runtime_pool_cleanup_reached=true`, `runtime_worker_exit_assignment=pool._worker_handler.terminate = worker_exit`, `runtime_model_list_delete_reached=true`, and `zero_worker_cleanup_blocked_by=pool-process-count-failed` while running no Python, Torch multiprocessing, model workers, or external PDF tools. Counters: `phpPass` moves 1374 -> 1375; the existing WordPress runtime preflight smoke is updated without adding a new scenario file. Dependency closure: no new support component is needed; this reuses native runtime planning metadata while live upstream model execution remains intentionally out of scope.

2026-06-05 UTC isolated lane: implemented the convert.py process_single_pdf text-length exception preflight boundary on accepted base `53dec48565a675bc0cc37c451daad9c43f45b44d`. Source truth is upstream markerPDF `convert.py`, where the `try/except Exception` covers `get_length_of_text()` before `convert_single_pdf()` and returns Python `None` after printing the error and traceback. `BatchConverter` now records `preflight-exception-print-return-none` rows with `error_stage=get_length_of_text`, no converter invocation, and no Markdown writes. Red-first focused evidence failed with the text-length exception escaping; after the fix, `php tools/run-tests.php lanes/markerpdf/tests/MarkerRuntimePreflightBoundaryCurrentBaseTest.php` passed with 1 file, 635 assertions, and 0 failures. `php lanes/markerpdf/examples/wordpress-marker-runtime-preflight-boundary-currentbase.php` emitted `text_length_error_boundary=preflight-exception-print-return-none`, `text_length_error_converter_called=false`, and `text_length_error_writes_markdown=false` while running no Python, Torch multiprocessing, model workers, or external PDF tools. Counters: `phpPass` moves 1405 -> 1406 and mapped runtime main preflight behavior coverage moves 3 -> 4. Dependency closure: no new support component is needed.

## Next Task

Choose the next bounded markerPDF/PDF extraction gap on current base, favoring AcroForm value dictionaries, page/action metadata, annotation geometry, object-stream/xref edges, Base14/font flag metrics, parser, object, resource, remaining filter, and supplied-dictionary edges that can ship with focused and full markerPDF PHP evidence. Keep OCR/model, live benchmark, and runtime launch work out of accepted parser-hardening slices unless the handoff supplies a bounded native boundary and verification.
