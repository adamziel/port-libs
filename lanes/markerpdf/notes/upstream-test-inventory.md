# markerPDF Upstream Test Inventory

Inventory source: shallow clone of `https://github.com/sddai/markerPDF` at `da6a2f5c9a7b1e92c82d85fbcf3680a79dd28a34` in `.upstream-cache/markerpdf`.

## Counted Denominator

- Repository files inventoried with `git ls-tree`: 78.
- Targeted lane-relevant workflow/benchmark/example/source/support paths inspected with `git ls-tree` and targeted `git show`: 69.
- Python files: 47, including 39 files under `marker/`.
- Committed Python unit tests: 0 found.
- CI benchmark workflow: 1 integration workflow, `.github/workflows/tests.yml`.
- Benchmark/scoring scripts inspected: `benchmarks/overall.py`, `marker/benchmark/scoring.py`, `marker/benchmark/table.py`, and `scripts/verify_benchmark_scores.py`. The upstream `benchmarks/overall.py` report aggregation shape is now mapped natively for supplied conversion text.
- Benchmark scoring functions inspected: 6 (`chunk_text`, `overlap_score`, `score_text`, `split_to_cells`, `align_rows`, and `score_table`).
- Cleaner/postprocessor/source utilities inspected: 7 cleaner modules with 17 functions under `marker/cleaners/`, 8 functions in `marker/postprocessors/markdown.py`, `marker/layout/layout.py`, `marker/layout/order.py`, `marker/pdf/extract_text.py`, `marker/pdf/utils.py`, `marker/schema/page.py`, `marker/debug/data.py`, `marker/debug/render.py`, `marker/ocr/heuristics.py`, `marker/ocr/utils.py`, `marker/ocr/recognition.py`, `marker/equations/equations.py`, `marker/equations/inference.py`, `marker/tables/table.py`, `marker/tables/utils.py`, and top-level `convert.py`.
- Locked table helper dependency inspected: `tabled-pdf==0.1.4` from `poetry.lock`; the source tarball `tabled_pdf-0.1.4.tar.gz` was downloaded to `/tmp/markerpdf-tabled-src` for targeted inspection only (28,889 bytes, sha256 `23af02dd04f64fcbda3c5fe67fbee740ab7cf12277e3fd949c40000c5b63a30d`). `tabled/inference/detection.py::merge_boxes` and `merge_tables` are now mapped because `marker/tables/table.py::get_table_boxes` imports that helper for adjacent table boxes. The table-recognition boundary also inspected `tabled/inference/recognition.py::{get_cells,recognize_tables}`, `tabled/assignment.py::{assign_rows_columns,merge_multiline_rows,handle_rowcol_spans}`, `tabled/heuristics/cells.py::{cluster_coords,find_column_separators,assign_cells_to_columns}`, `tabled/formats/__init__.py::formatter`, `tabled/formats/markdown.py::markdown_format`, and `tabled/formats/common.py::{sort_cells,sort_within_cell}`.
- Cleaner functions mapped: `marker/cleaners/bullets.py::replace_bullets`, `marker/cleaners/text.py::cleanup_text`, `marker/cleaners/fontstyle.py::find_bold_italic`, five focused functions from `marker/cleaners/headers.py` (`filter_common_elements`, `filter_header_footer`, `replace_leading_trailing_digits`, `find_overlap_elements`, and `filter_common_titles`), four focused functions from `marker/cleaners/code.py` (`is_code_linelen`, `comment_count`, `identify_code_blocks`, and `indent_blocks`), all three functions from `marker/cleaners/headings.py` (`split_heading_blocks`, `bucket_headings`, and `infer_heading_levels`), and both functions from `marker/cleaners/toc.py` (`get_pdf_toc` and `compute_toc`).
- Markdown postprocessor functions mapped: 8 of 8 (`escape_markdown`, `surround_text`, `merge_spans`, `block_surround`, `line_separator`, `block_separator`, `merge_lines`, and `get_full_text`). `MarkdownPostProcessor::mergeSpans` now maps the full upstream page-level wrapper, including skipped empty-span lines, lowercased font capture, first/last-span emphasis guards, next-substantial-span lookahead, block metadata preservation, and the empty-page Text fallback.
- Layout utilities mapped: all three functions from `marker/layout/layout.py` (`get_batch_size`, the supplied-prediction boundary of `surya_layout`, and `annotate_block_types`), plus `marker/pdf/utils.py::sort_block_group` and `marker/layout/order.py::sort_blocks_in_reading_order`, including layout batch-size overrides/multiplier truncation, supplied layout prediction assignment with upstream `zip` semantics, layout-label assignment by maximum bbox overlap, closest annotated-block fallback, default Text fallback, consecutive same-layout block merging, order-position assignment by maximum bbox overlap, vertical-bucket/horizontal row sorting, fallback ordering for unpositioned blocks, and header/footer pinning.
- Debug data export mapped: `marker/debug/data.py::dump_bbox_debug_data` is represented by `DebugDataExporter`, including the upstream `DEBUG` gate, document-base `_bbox.json` file naming, top-level `images` exclusion, `layout.segmentation_map` exclusion, `text_lines.heatmap`/`affinity_map` exclusion, and bbox/layout/text-line JSON persistence for WordPress review metadata. `marker/debug/render.py` was inspected as the PIL/font-download image drawing half of the same debug module family, but native raster drawing remains unported.
- PDF utility and dictionary conversion mapped: all 3 functions from `marker/pdf/utils.py` (`find_filetype`, `font_flags_decomposer`, and `sort_block_group`) plus 2 focused functions from `marker/pdf/extract_text.py` (`pdftext_format_to_blocks` and the supplied-data boundary for `get_text_blocks`). `find_filetype` is represented by magic-MIME detection that maps PDF-like MIME types to `pdf`, maps configured non-PDF MIME types through `SUPPORTED_FILETYPES`, and returns `other` for unknown or unsupported WordPress uploads. The pdftext dictionary conversion covers PDF font flag suffix construction, trailing newline/carriage-return trimming, `-\n` span cleanup, sequential `pnum_spanid` assignment, one Marker block per pdftext line, negative-area line skipping after span IDs are consumed, rotation-aware page bbox normalization, and original char-block preservation. The supplied `get_text_blocks` boundary covers `start_page`, `max_pages`, over-long max-page truncation, falsey `max_pages=0`, relative span-id enumeration over the selected page range, original document page numbers, and PDF TOC metadata handoff without loading pdftext/pypdfium.
- OCR heuristic utilities mapped: all four focused functions from `marker/ocr/heuristics.py` (`should_ocr_page`, `detect_bad_ocr`, `no_text_found`, and `detected_line_coverage`) plus `marker/ocr/utils.py::alphanum_ratio`.
- OCR language utilities mapped: `marker/ocr/lang.py::replace_langs_with_codes` and `marker/ocr/lang.py::validate_langs`, backed by `marker/ocr/tesseract.py`'s 93-entry Tesseract language map and the 94-entry Surya language map from the locked `surya-ocr==0.6.13` wheel hash recorded in `poetry.lock`. `langs_to_ids` remains unported because it calls the Surya recognition tokenizer.
- OCR detection orchestration mapped from `marker/ocr/detection.py`: `get_batch_size` and the `surya_detection` prediction-assignment boundary are represented natively with supplied Surya line detections. The native boundary computes the same detector batch-size/multiplier value and applies upstream Python `zip` semantics when writing predictions to `page.text_lines`; the Surya detector model call remains unported.
- OCR recognition orchestration mapped from `marker/ocr/recognition.py`: `get_batch_size`, the `run_ocr` page-selection/statistics/replacement boundary, and the pure supplied-boundary half of `surya_recognition` are represented natively. The new Surya slice scales detector polygons from `SURYA_DETECTOR_DPI` to `SURYA_OCR_DPI`, drops zero-area polygons before recognition, computes recognition batch size with the upstream multiplier, and reconstructs Marker Page/Block/Line/Span arrays from supplied OCR text-line results. The Surya model call and Tesseract/OCRmyPDF recognition bodies remain unported because upstream loads `surya.ocr`, pypdfium2 rendering, OCRmyPDF/Tesseract subprocess integration, and model processors.
- Block span filtering mapped from `marker/schema/block.py` and the `marker/convert.py` cleanup order: `Block.filter_spans` removes known header/footer span IDs and drops newly empty lines, while `Block.filter_bad_span_types` clears text spans from configured bad block types such as `Page-header`, `Page-footer`, and `Picture` without discarding the block metadata that later WordPress media review can still use.
- Page helper inspection mapped from `marker/schema/page.py`: `get_all_lines`, `get_nonblank_lines`, `get_nonblank_spans`, `get_font_sizes`, `get_line_heights`, and `prelim_text` are represented by `PageInspector` for native Page arrays before WordPress review metadata is emitted.
- Late `convert_single_pdf` finalization mapped from `marker/convert.py`: `ConversionFinalizer` now applies the native span-filter cleanup order, heading split/level inference, font-style marking, computed TOC generation, `merge_spans`, `merge_lines`, common-title filtering, `get_full_text`, `cleanup_text`, `replace_bullets`, and `images_to_dict` over supplied pages after the upstream OCR/layout/table/equation model boundaries.
- Image insertion helpers mapped from `marker/images/save.py`, `marker/images/extract.py`, `marker/schema/block.py`, and `marker/schema/bbox.py`: deterministic image filenames, page image dictionary export, nearest-block fallback insertion, Figure/Picture layout-region matching, intersecting text-span clearing, and Markdown image-span insertion. Native PHP does not raster-render PDF crops yet; upstream uses `pypdfium2` and PIL for that part.
- PDF image render planning mapped from `marker/pdf/images.py`: `render_image`'s `dpi / 72` scale, `draw_annots=False`, RGB output boundary, and `render_bbox_image`'s page-bbox-to-rendered-image crop scaling through `rescale_bbox`. Native PHP still does not raster-render or crop pixels; it emits the deterministic crop plan that downstream WordPress media workflows can store as review metadata.
- BBox geometry helpers mapped from `marker/schema/bbox.py`: adjacent-box merge checks, merged box extents, strict intersection checks, box1-area intersection percentage, multiple-box intersection, 0-1000 normalized box expansion, center calculation, rescaling between detected image and PDF page coordinate spaces, and BboxElement-style height/width/start/area/distance validation metrics.
- Equation replacement helpers mapped from `marker/equations/equations.py`, `marker/equations/inference.py`, `marker/schema/block.py`, and `marker/schema/bbox.py`: Formula layout-region discovery, bbox rescaling, intersecting line removal, nearest-block fallback insertion, Texify model token-limit guards, insertion-point adjustment, LaTeX prediction validation, Formula block insertion, original-text fallback on failed predictions, converted span capture, and document-level equation metadata. Native PHP accepts supplied LaTeX predictions and uses a deterministic token-count boundary; upstream Texify tokenizer/model inference and equation crop rendering remain unported.
- Table crop planning, recognition handoff, and formatting mapped from `marker/tables/table.py`: `get_table_boxes` now maps high-resolution table crop planning from supplied Table layout boxes, locked `tabled-pdf` adjacent-box merge semantics, tiny-box filtering, page/document index accounting, text-line duplication per table, and null text-line payloads for OCRed pages. `TableRecognizer` maps the pure-Python boundary around `get_cells`, `recognize_tables`, `assign_rows_columns`, `merge_multiline_rows`, `handle_rowcol_spans`, `tabled.heuristics.cells` DBSCAN-style column separator clustering for heuristic fallback, and `formatter("markdown", ...)` with supplied text-line cells filtered to each table bbox, supplied detector cells filtered to positive-area bboxes before OCR, `needs_ocr` flags, supplied OCR text, row/column geometry, assigned-column multiline continuation row merging, multi-column header spans, row-spanning side labels, cleanup, and Markdown output. The post-recognition branch of `format_tables` now consumes the same merged table regions as `get_table_boxes`, maps Table layout-region bbox rescaling for intersection checks, removes all intersecting source Table blocks, calculates upstream insertion points, constructs Markdown table blocks from supplied recognition output, and skips recognized tables when no Table block intersects. Native PHP still does not run Surya/tabled neural cell detection or recognition; it accepts supplied model-boundary inputs for now.
- Table utility cleanup mapped from `marker/tables/utils.py`: `sort_table_blocks` vertical midpoint bucket ordering with left-to-right row sorting, `replace_dots` long dot-leader cleanup, and `replace_newlines` table cell newline cleanup before WordPress table rendering.
- Output artifact helpers mapped from `marker/output.py`: `get_subfolder_path`, `get_markdown_filepath`, `markdown_exists`, and `save_markdown`, covering per-document output folders, converted Markdown paths, metadata JSON, and PNG image artifact writes for downstream WordPress import handoff.
- Top-level batch conversion mapped from `convert.py`: `BatchConverter` represents input-folder file discovery, `ceil(len(files) / num_chunks)` chunk sizing, chunk-index slicing, post-chunk `--max` limiting, `--metadata_file` JSON loading, basename metadata lookup from that metadata file, existing Markdown skip, `min_length` filetype/text-length preflight, non-empty output persistence through `OutputWriter`, empty-output skip, and nonfatal conversion error reporting. Native PHP accepts a supplied converter callback instead of loading Python models or multiprocessing workers.
- Settings defaults and computed helpers mapped from `marker/settings.py`: conversion defaults, supported PDF MIME mapping, image extraction/pagination/OCR flags, page separator and bad span type settings, `TORCH_DEVICE_MODEL` CPU fallback, CUDA/dtype helpers represented without importing Torch, pydantic-style typed environment overrides, `extra=ignore`, and the `OCR_ENGINE` literal boundary.
- Published benchmark documents in the README accuracy table: 6 (`multicolcnn.pdf`, `switch_trans.pdf`, `thinkpython.pdf`, `thinkos.pdf`, `thinkdsp.pdf`, `crowd.pdf`).
- CI benchmark archive inspected: `.github/workflows/tests.yml` downloads `benchmark_data_short.zip` from Google Drive ID `1NHrdYatR1rtqs2gPVfdvO0BAvocH8CJi`; the downloaded archive was 6,212,657 bytes with sha256 `c7511a4f5055e949a7a7c293be5541942433059d7841965f056d7f9b441a41ad`.
- Actual external CI benchmark pairs mapped from that archive: `benchmark_data/pdfs/multicolcnn.pdf` (851,968 bytes, sha256 `2b0e8314ff2c2680dd309ce46a49d740084d66eb39549337d2daa91215c426f8`) with `benchmark_data/references/multicolcnn.md` (30,542 bytes, sha256 `3ff96757b43e82595410f0fa50643945fec7c5c51f7c3d7562edefbd555aaa96`), and `benchmark_data/pdfs/switch_trans.pdf` (1,304,157 bytes, sha256 `f340f6ace31abf7d0730ef461404279f40d3c890e9cc2daeb7068b3304afdbd6`) with `benchmark_data/references/switch_trans.md` (82,456 bytes, sha256 `74f8f6cbe23873304aa182b3a46edc8df896d35542aeade95bb93254823d58e1`).
- CI score assertions: 3 thresholds in `scripts/verify_benchmark_scores.py` (`multicolcnn.pdf > 0.34`, `switch_trans.pdf > 0.40`, and average table report score `>= 0.7`), now represented natively by `BenchmarkReportVerifier`. The `benchmarks/overall.py` `files`, `avg_score`, `time_per_page`, and `time_per_doc` report aggregation is represented by `BenchmarkReportBuilder`.
- Committed markdown examples: 4 marker outputs and 4 nougat outputs under `data/examples`.

The lane manifest now counts all 78 tracked upstream repository paths as the static cloned inventory and 69 targeted upstream lane-relevant paths for dashboard purposes: workflows, benchmark/scoring scripts, committed example outputs, cleaner/postprocessor/layout/OCR/image/PDF-image-render/equation/table/output/settings/schema/debug/source modules, `marker/convert.py`, top-level `convert.py`, and support scripts, plus 6 README benchmark document identifiers. This is a static inventory, not upstream pass parity.

## Mapped Benchmark Pairs

- `fixtures/upstream-ci-benchmark-short.php` records the actual CI archive metadata and focused excerpts for the two external `benchmark_data` PDF/reference pairs. `BenchmarkReportVerifierTest` scores committed Marker output excerpts against the external references and verifies the upstream marker thresholds; `examples/wordpress-benchmark-report.php` turns the same fixture into a WordPress import quality gate.
- `fixtures/upstream-multicolcnn-surrogate.php` records a small excerpt pair from the README-linked committed outputs for `multicolcnn.pdf`: `data/examples/marker/multicolcnn.md` as Marker output and `data/examples/nougat/multicolcnn.md` as a reference-like surrogate.
- `fixtures/upstream-switch-transformers-surrogate.php` records a small excerpt pair from the README-linked committed outputs for `switch_trans.pdf`: `data/examples/marker/switch_transformers.md` as Marker output and `data/examples/nougat/switch_transformers.md` as a reference-like surrogate.
- `fixtures/upstream-thinkpython-surrogate.php` records a small excerpt pair from the README-linked committed outputs for `thinkpython.pdf`: `data/examples/marker/thinkpython.md` as Marker output and `data/examples/nougat/thinkpython.md` as a reference-like surrogate.
- `fixtures/upstream-thinkos-surrogate.php` records a focused Preface excerpt pair from the README-linked committed outputs for `thinkos.pdf`: `data/examples/marker/thinkos.md` as Marker output and `data/examples/nougat/thinkos.md` as a reference-like surrogate.
- `examples/upstream-surrogate-score.php` scores all four pairs with the native `BenchmarkScorer`. Current scores are about `0.978` for `multicolcnn.pdf` against a `0.80` threshold, `0.954` for `switch_trans.pdf` against a `0.75` surrogate threshold while also clearing the upstream CI `switch_trans.pdf > 0.40` boundary, `0.844` for `thinkpython.pdf` against a `0.78` surrogate threshold, and `0.986` for the focused `thinkos.pdf` Preface excerpt against a `0.95` surrogate threshold.
- The README-linked Marker/Nougat pairs remain intentionally labeled surrogates. They complement, but do not replace, the two actual external CI benchmark pairs now mapped from `benchmark_data_short.zip`.

## Mapped Native Semantics

- PDF content stream text operators feed Marker-style Markdown conversion.
- PDF text movement operators define block-ready line boundaries before WordPress import.
- `marker/pdf/utils.py` magic-MIME filetype detection is mapped by `FiletypeDetector`, including the upstream `pdf` substring shortcut, settings-backed non-PDF MIME mapping, and `other` fallback for unknown or unsupported uploads.
- `marker/postprocessors/markdown.py` hyphenated line dewrapping is mapped by `MarkdownPostProcessor::mergeLines`.
- `marker/postprocessors/markdown.py` heading/list/text wrapping and hash escaping are mapped by `MarkdownPostProcessor::surroundBlock`.
- `marker/pdf/utils.py` font flag decomposition is mapped by `PdfTextBlockConverter::fontFlagsDecomposer`, preserving Marker's ordered fixed-pitch/serif/symbolic/script/non-symbolic/italic/caps/bold/use-external-attributes suffixes.
- `marker/pdf/extract_text.py` pdftext dictionary conversion is mapped by `PdfTextBlockConverter::pdftextFormatToPage`, including span text cleanup, font suffixes, span IDs, one block per line, rotation-aware page bboxes, and char-block retention before later layout annotation.
- `marker/pdf/extract_text.py::get_text_blocks` page-range assembly is mapped by `PdfTextDocumentExtractor::getTextBlocks` for supplied pdftext dictionaries, including `start_page` validation, `max_pages` truncation, falsey zero handling, selected-range span-id enumeration, original page numbers, and PDF TOC metadata for partial WordPress imports.
- `marker/postprocessors/markdown.py` span assembly is mapped by `MarkdownPostProcessor::mergeSpans`, including Page/Block/Line/Span to MergedBlock conversion, lowercased font lists, skipped empty-span lines, first/last-span emphasis guards, next-substantial-span lookahead, and empty-page Text fallback blocks.
- `marker/postprocessors/markdown.py` block merging, block separators, and full-text assembly are mapped by `MarkdownPostProcessor::mergeBlocks`, `MarkdownPostProcessor::blockSeparator`, and `MarkdownPostProcessor::getFullText`.
- `marker/postprocessors/markdown.py` continuation geometry is mapped by `MarkdownPostProcessor::mergeBlocks` for equal-height, same-x lines whose vertical gap is below the upstream `max_block_gap`.
- `marker/pdf/utils.py` vertical-bucket and horizontal-row sorting is mapped by `LayoutOrderer::sortBlockGroup`.
- `marker/layout/layout.py::get_batch_size` is mapped by `LayoutAnnotator::batchSize`, including `LAYOUT_BATCH_SIZE` override handling, the upstream default six-page batch boundary, and `batch_multiplier` truncation.
- `marker/layout/layout.py::surya_layout` is mapped at the supplied-output boundary by `LayoutAnnotator::runWithSuppliedLayouts`, including collection of page text-line detections for the model call plan and upstream `zip` semantics when assigning supplied layout predictions to pages. Native PHP still does not load the Surya layout detector.
- `marker/layout/layout.py` layout annotation is mapped by `LayoutAnnotator::annotateBlockTypes`, including maximum-overlap layout-label assignment, nearest annotated-block fallback for unassigned blocks, default Text fallback, and consecutive same-layout block merging before Markdown assembly.
- `marker/layout/order.py` reading-order assignment is mapped by `LayoutOrderer::sortBlocksInReadingOrder`, including maximum bbox-overlap position assignment, fallback positions for unordered blocks, `sort_block_group` tie sorting, and Page-header/Page-footer/Footnote pinning.
- `marker/debug/data.py::dump_bbox_debug_data` is mapped by `DebugDataExporter::dumpBboxDebugData`, including disabled-debug no-op behavior, document-base `_bbox.json` path construction, heavy model/debug field exclusion, and JSON output over supplied Page arrays. The upstream print side effect is intentionally represented by returning the written path for PHP callers.
- `marker/ocr/utils.py` alphanumeric quality scoring is mapped by `OcrHeuristics::alphanumRatio`, including the upstream behavior that removes spaces and newlines before counting alphanumeric characters.
- `marker/ocr/heuristics.py` bad-OCR detection is mapped by `OcrHeuristics::detectBadOcr`, including empty text, whitespace/newline runs, garbled low-alphanumeric text, and repeated replacement-character checks.
- `marker/ocr/heuristics.py` whole-document no-text detection is mapped by `OcrHeuristics::noTextFound`.
- `marker/ocr/heuristics.py` detected-line coverage is mapped by `OcrHeuristics::detectedLineCoverage`, including Surya image-bbox to page-bbox rescaling and extracted line intersection coverage.
- `marker/ocr/heuristics.py` OCR fallback triage is mapped by `OcrHeuristics::shouldOcrPage`, including no-text, bad-OCR, line-coverage, force-OCR, and zero-detected-line behavior.
- `marker/ocr/lang.py` language normalization and validation are mapped by `OcrLanguage::replaceLangsWithCodes`, `OcrLanguage::validateLangs`, and `OcrLanguage::normalizeAndValidate`. The Surya branch title-cases names before mapping to Surya codes and preserves already-coded inputs; the OCRmyPDF/Tesseract branch maps exact upstream language names to Tesseract codes, defaults missing languages to `English`, and rejects invalid engine-specific codes before OCR handoff.
- `marker/ocr/detection.py` detector batch sizing is mapped by `OcrDetection::batchSize`, including configured `DETECTOR_BATCH_SIZE`, the upstream default of 4, and Python-style integer truncation after applying a batch multiplier.
- `marker/ocr/detection.py` Surya detection assignment is mapped by `OcrDetection::runWithSuppliedPredictions`, including supplied prediction arrays, writing each prediction to `page.text_lines`, ignoring extra predictions, and leaving unpaired pages unchanged like upstream `zip(pages, predictions)`.
- `marker/ocr/recognition.py` batch sizing, `run_ocr` orchestration, and the pure supplied-boundary half of `surya_recognition` are mapped by `OcrRecognition`. The native boundary computes OCR page indexes with `no_text_found`/`should_ocr_page`, honors the upstream no-op `none` engine branch, accepts supplied Surya/OCRmyPDF result pages, counts successful versus failed recognized text with `detect_bad_ocr`, replaces only successful pages before WordPress block rendering, scales detector polygons for Surya OCR, skips zero-area polygons, and rebuilds recognized OCR lines into Marker Page arrays.
- `marker/schema/block.py::filter_spans` and `filter_bad_span_types` are mapped by `BlockSpanFilter`, including the upstream conversion cleanup order that removes known bad span IDs first, removes empty lines, clears configured bad block-type text spans, and keeps Picture/image block metadata available for Gutenberg media review.
- `marker/images/save.py` deterministic image filename and image dictionary helpers are mapped by `ImageExtractor::getImageFilename` and `ImageExtractor::imagesToDict`.
- `marker/schema/block.py` nearest-block fallback insertion is mapped by `ImageExtractor::findInsertBlock`.
- `marker/images/extract.py` Figure/Picture region discovery is mapped by `ImageExtractor::findImageBlocks`, including layout image-bbox to page-bbox rescaling.
- `marker/images/extract.py` image placeholder insertion is mapped by `ImageExtractor::insertImagePlaceholders`, including intersecting text-span clearing, existing-line insertion, newly-created image lines, and upstream Markdown image syntax. Raster image rendering remains unported because upstream uses `pypdfium2` and PIL.
- `marker/pdf/images.py` render planning is mapped by `PdfImageRenderer`, including `dpi / 72` scale calculation, no-annotation rendering intent, RGB output boundary, and `render_bbox_image` crop bbox scaling from PDF page coordinates into rendered-image pixel coordinates.
- `marker/schema/bbox.py` geometry helpers are mapped by `BboxGeometry`, including upstream adjacent-box merge tolerances, strict edge-touch intersection behavior, intersection percentage as a fraction of the first box area, normalized coordinate expansion, detected-image to page bbox rescaling, center/distance calculations, and BboxElement-style bbox validation.
- `marker/equations/equations.py` Formula region discovery is mapped by `EquationReplacer::findEquationBlocks`, including `Formula` layout label filtering, layout-image bbox to page-bbox rescaling, line intersection using the upstream `0.7` threshold, removal of intersecting text lines, nearest-block fallback when no text line overlaps, and the Texify model token cap before replacement.
- `marker/equations/equations.py` insertion shifting and block insertion are mapped by `EquationReplacer::insertLatexBlocks`, including insertion before a block, after a block, or inside a split text block, plus later insertion-point adjustment after new Formula blocks are inserted.
- `marker/equations/equations.py` LaTeX validation is mapped by `EquationReplacer::insertLatexBlocks`, including predicted-token limit, non-empty output, predicted text length greater than 70% of the original equation text, original-text fallback when validation fails, and converted-span capture when validation succeeds.
- `marker/equations/equations.py` document-level equation stats are mapped by `EquationReplacer::replaceEquations`, returning `successful_ocr`, `unsuccessful_ocr`, and `equations` metadata in the same shape used by Marker's README output metadata.
- `marker/equations/inference.py::get_total_texify_tokens` is represented by `EquationReplacer::totalTexifyTokens` as a native deterministic token-count boundary. This is intentionally not full Texify tokenizer parity because the upstream tokenizer/model stack is not part of the native PHP slice.
- `marker/postprocessors/markdown.py` Formula display-math handling is covered after equation replacement: accepted LaTeX spans remain fenced with `$$...$$`, and `MarkdownPostProcessor` preserves those fences for WordPress math/HTML block rendering.
- `benchmarks/overall.py` report aggregation is mapped by `BenchmarkReportBuilder`, including per-method file score/time/page records, average score, upstream `total_pages` denominator behavior for `time_per_page`, and per-document timing.
- `marker/benchmark/scoring.py` chunking, local-window overlap, and text score aggregation are mapped by `BenchmarkScorer`.
- `marker/benchmark/table.py` pipe-cell splitting, best-row fuzzy alignment, and aggregate table scoring are mapped by `TableScorer`.
- `marker/tables/table.py` post-recognition table replacement is mapped by `TableFormatter::formatTables`, including supplied Markdown table insertion after intersecting Table layout blocks are removed. The formatter now derives merged table regions consistently with `get_table_boxes`, so adjacent PDF table fragments consume one recognized Markdown table instead of one per raw layout box.
- `tabled.inference.recognition.py::get_cells` and `recognize_tables` are mapped by `TableRecognizer` at the supplied-boundary level: text-line table blocks are filtered to the current table bbox and used when present, detector cells are required when text lines are absent, filtered empty, or `detect_boxes` is forced, zero-area detector bboxes are dropped before OCR, `needs_ocr` flags are preserved, and supplied OCR text is zipped back onto detector cells before recognition output is formatted.
- `tabled.assignment.py::assign_rows_columns` is mapped by `TableRecognizer`, including initial row/column intersection assignment, `merge_multiline_rows` continuation-row merging by assigned row/column IDs, overlapping-row/column rescue, unassigned-cell closest-row/column fallback, a bounded heuristic row layout fallback when most cells remain unassigned, row/column span expansion for cells whose geometry covers otherwise-open row or column bands, and the upstream table-recognition batch-size override/default boundary.
- `tabled/heuristics/cells.py` is mapped by `TableRecognizer::heuristicColumnSeparators` and the fallback branch of `assignRowsColumns`, including repeated normalized left/right/center coordinate filtering, one-dimensional DBSCAN clustering with the locked `eps=.01` and `min_samples=max(2,row_count//4)` boundary, separator-family selection by largest cluster count, and assignment through upstream-style separator keys before Markdown table rendering.
- `tabled.formats.markdown.py::markdown_format` is mapped by `TableRecognizer::markdownFormat`, including row/column/order sorting, dot-leader and newline cleanup, pipe/hyphen escaping, and GitHub-style Markdown table output for Gutenberg table rendering.
- `marker/tables/utils.py` table cell ordering and text cleanup are mapped by `TableUtils::sortTableBlocks`, `TableUtils::replaceDots`, and `TableUtils::replaceNewlines`.
- `marker/output.py` output artifact persistence is mapped by `OutputWriter::getSubfolderPath`, `OutputWriter::getMarkdownFilepath`, `OutputWriter::markdownExists`, and `OutputWriter::saveMarkdown`, including the upstream final-extension filename split, per-document output folder, converted Markdown file, `_meta.json` metadata, and image artifact writes.
- Top-level `convert.py` batch processing is mapped by `BatchConverter::loadMetadataFile`, `BatchConverter::planTasks`, `BatchConverter::processFile`, and `BatchConverter::processFolder`, including metadata-file JSON loading, chunk planning, metadata lookup by basename, `min_length` skip gates, existing-output skip gates, non-empty conversion persistence, and summary counts for bulk WordPress import jobs.
- `marker/settings.py` settings defaults and computed fields are mapped by `MarkerSettings`, including supported filetype checks, environment-style override coercion, unknown extra ignore behavior, OCR engine literal validation, CPU fallback device handling, native dtype labels, page separators, and bad-span/image/pagination/OCR flags.
- `scripts/verify_benchmark_scores.py` table-score threshold is covered by `examples/wordpress-table-score.php`, which scores an OCR-noisy WordPress table import above `0.7`.
- `marker/schema/page.py` helper properties are mapped by `PageInspector`, including all-line flattening, nonblank line/span filtering, font-size extraction from nonblank spans, line-height extraction from nonblank line bboxes, and page-level `prelim_text` assembly.
- `marker/cleaners/bullets.py` bullet glyph normalization is mapped by `TextCleaner::replaceBullets`.
- `marker/cleaners/text.py` excessive whitespace and non-breaking-space cleanup is mapped by `TextCleaner::cleanupText`.
- `marker/cleaners/fontstyle.py` font-name and font-weight emphasis detection is mapped by `FontStyleCleaner::markBoldItalicSpans`.
- `marker/postprocessors/markdown.py` `surround_text` and the styled-span branch of `merge_spans` are mapped by `FontStyleCleaner::mergeStyledLine`, including the upstream first/last-span guard and Markdown `*`/`**` markers.
- `marker/cleaners/headers.py` repeated edge-line detection is mapped by `HeaderFooterCleaner::findCommonEdgeLines` and `HeaderFooterCleaner::removeCommonEdgeLines`, including the upstream minimum of three pages before common elements are filtered.
- `marker/cleaners/headers.py` repeated title filtering is mapped by `HeaderFooterCleaner::replaceLeadingTrailingDigits`, `HeaderFooterCleaner::findOverlapElements`, and `HeaderFooterCleaner::filterCommonTitles`.
- `marker/cleaners/code.py` line-length and comment-prefix detection are mapped by `CodeBlockDetector::isCodeLineLength` and `CodeBlockDetector::commentCount`.
- `marker/cleaners/code.py` code block classification is mapped by `CodeBlockDetector::isCodeBlock` and `CodeBlockDetector::identifyCodeBlocks`, including the upstream `> 3` line minimum, indentation/comment majority threshold, and optional small-font checks.
- `marker/cleaners/code.py` indentation reconstruction is mapped by `CodeBlockDetector::indentBlock`, including line-geometry-derived prefixes and repeated blank-line suppression.
- `marker/cleaners/headings.py` heading line splitting is mapped by `HeadingCleaner::splitHeadingBlocks`, including the upstream `Title`/`Section-header` labels and the `0.7` bounding-box intersection threshold.
- `marker/cleaners/headings.py` line-height bucketing and heading-level inference are mapped by `HeadingCleaner::bucketHeadings` and `HeadingCleaner::inferHeadingLevels`, including the upstream four-level/default-level behavior.
- `marker/cleaners/toc.py` PDF outline metadata import is mapped by `HeadingCleaner::getPdfToc`, including the upstream `doc.get_toc(max_depth)` call boundary and ordered `title`, `level`, and `page_index` projection.
- `marker/cleaners/toc.py` computed table-of-contents metadata is mapped by `HeadingCleaner::computeToc`, emitting title, level, and page for `Title` and `Section-header` blocks.
- README-linked committed Marker/Nougat `multicolcnn`, `switch_transformers`, `thinkpython`, and `thinkos` markdown outputs are mapped as upstream-derived surrogate benchmark pairs.
- `scripts/verify_benchmark_scores.py` marker and table report threshold checks are mapped by `BenchmarkReportVerifier`, including the upstream `<=` failure boundary for marker scores and `< 0.7` failure boundary for average table reports.
- `benchmark_data_short.zip` `multicolcnn.pdf` and `switch_trans.pdf` PDF/reference pairs are mapped as actual external benchmark evidence with recorded archive, PDF, and reference hashes.
- `examples/wordpress-pdf-outline-import.php` maps `marker/cleaners/toc.py::get_pdf_toc` into a WordPress bookmark import path. It accepts a pypdfium-style `get_toc(max_depth)` adapter, preserves upstream title/level/page metadata, and emits a Gutenberg list with review attributes before OCR/layout model work begins.
- `examples/wordpress-paginated-import.php` uses the full block-merge path to preserve upstream-style page-start markers as reviewable Gutenberg separators while emitting headings, paragraphs, and list blocks.
- `examples/wordpress-ocr-triage.php` maps upstream OCR heuristics into a WordPress import preflight that sends garbled/scanned pages to OCR before block rendering while leaving clean extracted text alone.
- `examples/wordpress-image-import.php` maps upstream image insertion metadata into a Gutenberg image-block import path, preserving Marker-style `![page_image](page_image.png)` filenames while avoiding a Python/pypdfium raster-render dependency.
- `examples/wordpress-pdf-image-crop.php` maps `marker/pdf/images.py::render_bbox_image` into a WordPress media review path. It records the upstream render scale and crop bbox that a raster backend would use, without shelling out to pypdfium/PIL.
- `examples/wordpress-bbox-geometry-import.php` maps `marker/schema/bbox.py` into a WordPress geometry preflight. It merges adjacent PDF span boxes into one block-ready paragraph bbox, preserves that bbox as review metadata, and only emits a related core image block when the supplied image region intersects the merged paragraph box under Marker's strict bbox semantics.
- `examples/wordpress-equation-import.php` maps upstream equation replacement into a WordPress math import path. It removes the source equation text line, inserts a supplied LaTeX Formula block, emits paragraph blocks around it, renders the equation as a constrained core HTML block, and reports Marker-style equation metadata for review.
- `examples/wordpress-table-format-import.php` maps the post-recognition half of `marker/tables/table.py::format_tables` into a Gutenberg table import path. It replaces the source PDF Table block with a supplied Markdown table and renders it as a core table block while leaving the upstream Surya/tabled recognition stack explicitly out of the native slice.
- `examples/wordpress-merged-table-import.php` maps the merged-region `format_tables` boundary into a Gutenberg table import path. It accepts adjacent PDF Table layout fragments, removes both source fragments, consumes one supplied recognized Markdown table, and renders one core table block.
- `examples/wordpress-table-box-plan.php` maps `marker/tables/table.py::get_table_boxes` into a WordPress review path. It merges adjacent supplied Table layout boxes, emits high-resolution crop bbox metadata, duplicates supplied text-line detections for non-OCR pages, and marks OCRed pages for table-cell redetection before Gutenberg table rendering.
- `examples/wordpress-table-recognition-handoff.php` maps the next supplied table-recognition boundary into a WordPress import path. It accepts supplied tabled-style cells/rows/columns, applies native row/column assignment, preserves row/column span metadata for reviewer tooling, and renders column spans as table-cell attributes while still feeding the existing `format_tables` replacement path without running Surya/tabled models.
- `examples/wordpress-table-detector-filter.php` maps the detector-fallback half of `tabled.inference.recognition.py::get_cells` into a WordPress table import preflight. It drops zero-width and zero-height detector boxes before OCR text assignment, reports the dropped count as review metadata, and renders the remaining OCR-backed cells as a core table block.
- `examples/wordpress-table-heuristic-columns.php` maps the locked `tabled.heuristics.cells` fallback into a WordPress table import path. It records DBSCAN-derived column separator metadata for reviewer tooling, assigns rows and columns without model row/column boxes, and renders the result as a Gutenberg table.
- `examples/wordpress-table-cleanup-import.php` maps `marker/tables/utils.py` into a Gutenberg table cleanup path. It orders cell blocks into rows, removes dot leaders used as visual table fillers, collapses embedded cell newlines, and renders the cleaned cells as a core table block.
- `examples/wordpress-output-artifact.php` maps `marker/output.py` into a WordPress import artifact handoff. It writes converted block Markdown, review metadata, and image artifacts to Marker's per-document output layout without shelling out to Python or PIL.
- `examples/wordpress-debug-bbox-export.php` maps `marker/debug/data.py::dump_bbox_debug_data` into a WordPress review workflow. It writes a Marker-style bbox JSON artifact, preserves layout labels and text-line boxes for reviewer tooling, and confirms large model fields such as `segmentation_map` and `heatmap` are omitted.
- `examples/wordpress-batch-convert-import.php` maps top-level `convert.py` into a WordPress bulk import path. It plans a folder of PDFs, applies basename metadata, runs native min-length preflight, writes Marker-style per-document output artifacts, and reports converted/skipped/error counts without loading Python model workers.
- `examples/wordpress-settings-preflight.php` maps `marker/settings.py` into a WordPress import preflight. It accepts PDF uploads, rejects unsupported MIME types, applies shared-hosting overrides for image extraction and pagination, and exposes the same page separator and bad-span defaults that later conversion stages consume.
- `examples/wordpress-filetype-preflight.php` maps `marker/pdf/utils.py::find_filetype` into a WordPress upload preflight. It accepts a PDF by magic bytes even when extension text is not trusted, and rejects a ZIP-like extension-spoofed payload before the heavier pdftext/pypdfium/Surya stack is reached.
- `examples/wordpress-benchmark-report.php` maps `benchmarks/overall.py` plus `scripts/verify_benchmark_scores.py` into a WordPress quality gate. It builds the upstream benchmark report shape from imported block-content excerpts, scores those excerpts against the actual upstream CI benchmark references, and rejects a report before editorial review if either Marker document score misses the upstream threshold.
- `examples/wordpress-ocr-language-preflight.php` maps `marker/ocr/lang.py` into a multilingual WordPress OCR metadata preflight. It normalizes imported PDF metadata such as `["Spanish", "Russian"]` to OCRmyPDF/Tesseract codes such as `["spa", "rus"]`, and applies Marker's default English language when metadata omits languages.
- `examples/wordpress-ocr-recognition-handoff.php` maps `marker/ocr/recognition.py::run_ocr` into a WordPress OCR handoff. It sends a garbled PDF page through the native OCR selection boundary, accepts a supplied recognized page, emits Marker's `ocr_stats`, and renders the successful OCR text as a Gutenberg paragraph without loading Surya or Tesseract.
- `examples/wordpress-surya-ocr-recognition-import.php` maps the pure supplied-boundary half of `marker/ocr/recognition.py::surya_recognition` into a WordPress import path. It records the scaled detector polygon count that would be sent to Surya, drops a zero-area detection box before recognition, reconstructs a Marker page from supplied OCR text lines, and emits the recovered text as a Gutenberg paragraph without loading the Surya model.
- `examples/wordpress-bad-span-filter-import.php` maps `marker/schema/block.py::filter_spans` and `filter_bad_span_types` into a WordPress import cleanup path. It removes a repeated header span and OCR text over a Picture block before Markdown rendering while preserving the image filename and bbox metadata for a core image block.
- `examples/wordpress-conversion-finalizer-import.php` maps the late `marker/convert.py::convert_single_pdf` cleanup and assembly order into a WordPress import handoff. It removes bad header spans, marks bold body spans, generates computed TOC and image metadata, normalizes bullet list markers, and emits heading/paragraph/list blocks without running pdftext, pypdfium, Surya, Texify, or tabled models.
- `examples/wordpress-ocr-detection-preflight.php` maps `marker/ocr/detection.py::surya_detection` into a WordPress preflight. It accepts supplied Surya text-line boxes, writes them to `page.text_lines` with upstream zip semantics, checks line coverage through the existing OCR heuristics, and records the detection boxes as review metadata before deciding whether a page needs OCR.
- `examples/wordpress-layout-detection-preflight.php` maps the supplied-prediction half of `marker/layout/layout.py::surya_layout` into a WordPress import preflight. It records the upstream layout plan, attaches supplied Surya layout labels with `zip` semantics, then feeds `annotate_block_types` before emitting heading and paragraph blocks.
- `examples/wordpress-layout-annotation-import.php` maps `marker/layout/layout.py::annotate_block_types` into a WordPress import preflight. It assigns Title/Text/Picture labels from supplied layout boxes, merges split title lines from the same layout region, filters bad span types with `MarkerSettings::badSpanTypes()`, and renders only block-ready heading and paragraph output.
- `examples/wordpress-span-merge-import.php` maps `marker/postprocessors/markdown.py::merge_spans` into the WordPress paragraph path. It carries styled PDF spans through Marker's MergedBlock shape, preserves lowercased font metadata for downstream analysis, emits upstream-style emphasis markers, and renders them as `<strong>`/`<em>` in a core paragraph block.
- `examples/wordpress-pdftext-block-import.php` maps `marker/pdf/extract_text.py::pdftext_format_to_blocks` into the WordPress paragraph path. It converts supplied pdftext dictionary output into Marker's native page/block/line/span shape, normalizes pdftext hyphenated span text, preserves font flag suffixes, and feeds the existing merge-spans path without loading pdftext, pypdfium, or Surya.
- `examples/wordpress-pdftext-page-range-import.php` maps the supplied-data half of `marker/pdf/extract_text.py::get_text_blocks` into a partial WordPress import path. It selects a requested page range from supplied pdftext dictionaries, preserves upstream relative span-id enumeration and original document page numbers, carries PDF TOC metadata, and emits Gutenberg paragraphs only for the selected pages.
- `examples/wordpress-page-inspection-preflight.php` maps `marker/schema/page.py` helper properties into WordPress review metadata. It records nonblank line/span counts, font-size and line-height distributions, and the upstream `prelim_text` page view before a document import is sent to editorial review.

## Runner Status

The full upstream runner was not executed. The CI workflow's Google Drive archive was downloaded and inspected because it is small, giving two actual PDF/reference pairs for static evidence. The locked `tabled-pdf` source tarball was downloaded only to inspect focused table detection, recognition, assignment, multiline-row merge, heuristic column layout, and formatting helpers used by Marker's table path; no Python model stack was installed or executed. Running `benchmarks/overall.py` still requires Poetry dependencies including `torch`, `surya-ocr`, `pdftext`, `pypdfium2`, `tabled-pdf`, Texify model tooling, and model downloads. Under the lane resource constraints, the defensible denominator for now is the cloned static inventory above, the two hash-identified external CI benchmark pairs, the natively ported filetype detection, pdftext dictionary conversion and supplied page-range assembly, benchmark scoring/report-building/report-verifier/cleaner/postprocessor/merge-spans/block-span-filtering/page-inspection/conversion-finalizer/batch-converter metadata-file/heading/PDF-outline/TOC/layout-detection-supplied-boundary/reading-order/OCR/OCR-language/OCR-detection/OCR-recognition-orchestration/Surya-recognition-supplied-boundary/image-insertion/PDF-image-render-planning/bbox-geometry/equation-replacement/table-box-planning/table-recognition-handoff/detector-positive-area-filter/multiline-row-merge/table-heuristic-column/table-formatting/table-utility/debug-bbox-output/output/settings functions, and the four committed README-linked surrogate pairs, not local full runner parity.
