{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 31,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 106 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, assign-semantics class fields, derived `super(...)` insertion, expression/return/throw/if/switch/for control-statement `super(...)` splitting, initialized private class-field insertion, selected TypeScript auto-accessor marker/type erasure, and explicit resource management `using` rejection boundaries.",
    "phpPass": 106,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, computed-super controllers, conditional/lazy `super(...)` constructors, comma-expression controllers, return-super controllers, control-statement switch-test/for-initializer controllers, private-settings controllers, auto-accessor block controllers, and typed `using` disposable asset handles without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps selected `TestTSUsing` multiline declaration, `await using`, missing-initializer, and `export await using` boundaries plus the prior auto-accessor marker/type erasure slice.",
    "audit": "audited 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 106 behavior tests, 547 assertion calls, and 0 failures, not full upstream parity. Required root `php tools/run-tests.php` passes with 139 test files, 12328 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now keeps comma-continued multiline `using` and `await using` declarations together, erases their type annotations, validates initialized declarators, rejects upstream-invalid `export using`/`export await using`, and preserves the ASI split for `using` followed by a line break. The WordPress disposable asset fixture now covers multiline typed preview/worker handles before registration without Node.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 106 behavior tests, 547 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` passes with 139 test files, 12328 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "cefb22f",
    "nextTask": "Map explicit resource management lowering for older targets, starting with upstream `using x = null`/`undefined` optimization and helper boundaries, then continue target-version-specific static field lowering, broader auto-accessor lowering for older JS targets, broader TypeScript class side-effect-order matrices, and namespace/class/enum merge matrices."
}
