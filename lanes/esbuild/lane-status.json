{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 31,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 108 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization and top-level helper lowering, assign-semantics class fields, derived `super(...)` insertion, expression/return/throw/if/switch/for control-statement `super(...)` splitting, initialized private class-field insertion, and selected TypeScript auto-accessor marker/type erasure.",
    "phpPass": 108,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, computed-super controllers, conditional/lazy `super(...)` constructors, comma-expression controllers, return-super controllers, control-statement switch-test/for-initializer controllers, private-settings controllers, auto-accessor block controllers, and typed `using` disposable asset handles without invoking Node; the disposable asset fixture also verifies opt-in legacy helper lowering with native `__using`/`__callDispose` cleanup.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps selected upstream `TestUsing` nullish-initializer optimization plus top-level unsupported-target helper lowering for explicit resource management.",
    "audit": "audited 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 108 behavior tests, 569 assertion calls, and 0 failures, not full upstream parity. Required root `php tools/run-tests.php` passes with 140 test files, 12447 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now parses top-level `using` declarators into structured declarations, prints `undefined` as `void 0`, converts all-nullish `using` declarations to ordinary declarations in minify-syntax mode, preserves mixed cleanup declarations as `using`, and can opt into a bounded unsupported-target helper wrapper around top-level `using` statements. The WordPress disposable asset fixture now covers both modern `using` syntax and legacy helper-lowered cleanup.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 108 behavior tests, 569 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` passes with 140 test files, 12447 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets. Remaining native explicit-resource-management gaps include nested scopes, `for`/`for-of` using declarations, full `await using` lowering, and symbol collision avoidance.",
    "latestCommit": "pending lane commit",
    "nextTask": "Continue explicit resource management lowering beyond the bounded top-level helper slice: nested function/block scopes, `for`/`for-of` using declarations, full `await using` lowering, symbol collision avoidance, then target-version-specific static field lowering and broader auto-accessor lowering."
}
