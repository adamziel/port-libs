{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 9,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` now passes in a detached runner worktree with Nix Go/Node/OpenSSL; lexer, import/export, import-attribute, import.meta, new URL asset-reference, TypeScript import-equals/type-only import-export, and TypeScript namespace metadata slices mapped",
    "phpPass": 22,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; current fixtures preflight a WordPress block view script importing @wordpress/dom-ready, block metadata via `./block.json` with `{ type: \"json\" }`, relative stylesheet/worker assets via `new URL(..., import.meta.url)`, and a TypeScript block script that keeps erased @wordpress type imports out of runtime dependency lists while recording namespace-backed block registration metadata.",
    "phase": "cloned static inventory plus upstream make test runner evidence and lexer/import-export/import-attribute/import-meta/new-url/typescript import-export/namespace slices",
    "audit": "audited 2026-05-22: upstream make test passes locally; native PHP smoke coverage now maps 22 tests and 133 esbuild assertions, not full upstream parity",
    "currentWork": "Native JavaScript lexer plus module analyzer for static imports, expression/direct-string dynamic imports, exports, re-exports, import assertions/attributes, import.meta ESM markers, new URL(import.meta.url) asset references, TypeScript import-equals/export-equals/type-only import/export forms, TypeScript namespace/export namespace metadata, package/relative source classification, WordPress package imports, and upstream `make test` runner evidence from a detached hydrated worktree.",
    "blocker": "No current esbuild implementation blocker. The core upstream runner blocker is resolved via detached runner worktree plus `nix-shell -p go nodejs openssl make test`; remaining release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets. Root `php tools/run-tests.php` passes in the current dirty shared worktree.",
    "latestCommit": "5778c7a",
    "nextTask": "Map TypeScript import-use elision/tree-shaking behavior from `TestTSImportEquals` and adjacent import pruning cases, including side-effect-only downgrade for unused runtime imports."
}
