{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 76,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Go/Node/OpenSSL evidence; native PHP now maps 238 focused behavior tests. Current reduced handoff covers analyzer string/template/conditional import records, dead branch pruning, relative glob pattern records, bounded native glob fixture expansion/matching, bounded package/node_modules main-field resolution, package `imports`, bounded package `exports` root/subpath/condition/pattern resolution, package-local browser object root/subpath/disabled remapping, containing-package browser object package remaps for imports inside package files, bounded Node builtin externalization/browser-map shim behavior, and bounded tsconfig `baseUrl`/`paths` alias resolution including relative `extends` with overridden `baseUrl`. The previous broad unaccepted esbuild pile remains preserved as stash `port-esbuild-broad-unaccepted-20260524T2011Z` (`6a72921b`).",
    "phpPass": 238,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments. Current reduced analyzer preflight recognizes WordPress preview chunks, conditional dynamic preview assets, conditional legacy CommonJS preview assets, block metadata, resolved worker paths, worker assets, bounded relative glob asset patterns, and package dependency paths when sources are written as no-substitution template literals, string concatenations, template substitutions, or package names. `GlobImportResolver` expands relative glob records against a bounded WordPress fixture graph. `PackageResolver` now resolves `@wordpress/interactivity`, a local block runtime package, package subpath helpers, package self-imports, node/browser main-field differences, bad-main/index fallback, package `imports`, package `exports` root conditions, import-vs-require branches, wildcard subpaths, slash-prefix subpaths, exact export targets, invalid export target/path traversal exclusions, browser-platform package-local browser object remaps/disabled entries, containing-package browser object package remaps for imports inside package files, and containing-package browser-map shims/disabled entries for Node builtins such as `path`, `node:path`, and `crypto` against a bounded `node_modules` fixture without invoking Node/npm. Existing examples still cover block view assets, block.json imports, TypeScript namespace/lowering fixtures, decorators, explicit resource management, and async-generator previews without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence; reduced analyzer/resolver source-discovery slice. Targeted upstream reads inspected glob handling plus resolver default main-field ordering, package.json parsing, package `exports` condition/subpath/pattern semantics, package `imports`, selected package `browser` object-map behavior, and esbuild's `checkForBuiltInNodeModules` resolver boundary. Bounded native PHP now records relative `commonjs-require-glob`/`dynamic-glob` patterns, resolves them against local fixtures, resolves package imports through parent `node_modules`, applies package `exports` root/subpath/condition/pattern maps before main-field fallback, resolves package `imports`, applies browser-platform package-local browser object root/subpath/disabled remapping, applies containing-package browser object package remaps for imports inside package files, and avoids node-platform file resolution for Node builtins while allowing browser-map shims/disabled builtin entries. Yarn PnP, plugin resolution, explicit external metadata records, and full npm condition handling remain out of scope.",
    "audit": "verified 2026-05-24 22:06 UTC plus 2026-05-24 current lane run: PHP syntax checks pass for `lanes/esbuild/src/PackageResolver.php`, `lanes/esbuild/src/TsConfigPathResolver.php`, `lanes/esbuild/src/TsConfigPathResolution.php`, `lanes/esbuild/tests/PackageResolverTest.php`, `lanes/esbuild/tests/TsConfigPathResolverTest.php`, and `lanes/esbuild/examples/wordpress-asset-preflight.php`. Focused tsconfig resolver verification passes `php tools/run-tests.php lanes/esbuild/tests/TsConfigPathResolverTest.php` with 1 selected test file, 11 assertions, and 0 failures. Focused esbuild lane verification passes `php tools/run-tests.php lanes/esbuild/tests` with 6 selected test files, 1,783 assertions, and 0 failures; direct PASS count is 238. `git diff --check -- lanes/esbuild` passes. The WordPress preflight example reports package resolver browser/node checks, package exports map resolution, package browser object maps, containing package browser maps, Node builtin browser maps, and tsconfig paths aliases as `yes` along with existing no-substitution, conditional, dead expression, glob pattern, and glob fixture checks. Root no-argument verification was not run by this lane worker by assignment.",
    "currentWork": "Extended the bounded native `TsConfigPathResolver` for selected upstream `TestTsconfigPathsOverriddenBaseURL`, `TestTsconfigPathsOverriddenBaseURLDifferentDir`, `TestTsconfigBaseURLExtendsPaths`, and `TestTsconfigPathsExtendsBaseURL` semantics. It reads the nearest `tsconfig.json`, follows relative string/array `extends` entries with cycle protection, inherits `paths`, tracks whether `baseUrl` and `paths` came from different config files, applies an overriding child `baseUrl` to inherited path aliases, matches exact and single-star aliases, respects ordered fallback target arrays, probes configured extensions and `index`, accepts absolute-looking virtual aliases such as `/virtual/*`, and rejects relative/imports/node builtin/unsafe target cases. The WordPress block fixture now keeps path aliases in an extended config while the active config supplies `baseUrl`, covering `@blocks/*`, `@shared/*`, `shared-config`, `@theme/*`, `wordpress-runtime`, and `/virtual/*` aliases plus focused tests and example smoke output.",
    "blocker": "Aggregate root verification and integrator acceptance remain pending for this reduced esbuild handoff, and release-extra upstream `make test-all` coverage remains static-only. Focused lane tests, PHP syntax checks, upstream-targeted static reads, and example smoke are green. The shared `js-package-resolution-core` dependency row is referenced for reuse but remains deferred, not activated, until an accepted resolver gate requires cross-lane support.",
    "latestCommit": "uncommitted reduced port-esbuild analyzer/resolver batch for no-substitution template literal, conditional dynamic import, direct CommonJS require/require.resolve, conditional require branch, dead if/logical/constant-conditional require/import omission, relative glob require/dynamic import pattern source handling, bounded native glob fixture expansion, bounded package/node_modules main-field resolution, bounded package imports/exports map resolution, bounded package-local browser object-map remapping, bounded containing-package browser object package remapping, bounded Node builtin externalization/browser-map shim behavior, bounded tsconfig baseUrl/paths alias resolution, and bounded tsconfig relative extends/overridden baseUrl resolution; broad unaccepted esbuild pile preserved as stash `port-esbuild-broad-unaccepted-20260524T2011Z` (`6a72921b`)",
    "nextTask": "After integrator/root acceptance, the next best lane task is either tsconfig package-based `extends` resolution through node_modules/package exports or a fuller Node builtin externalization record model if the bundler slice needs explicit external metadata instead of the current no-file-resolution boundary. Dependency-closure checkpoint: reference existing high-priority `js-package-resolution-core` row; activation gate is `esbuild-bundler-resolution-next-or-lightningcss-css-package-resolution-next`; reuse opportunity is shared package metadata parsing, condition matching, builtin classification, tsconfig alias matching, and path normalization with LightningCSS; denominator expectation is esbuild resolver/package.json/tsconfig tests plus relevant Node package-resolution spec fixtures; mapped fixtures should include malformed package.json, conditional imports/browser maps, tsconfig package extends/baseUrl overrides/path traversal, generated local fixture packages, and full relevant upstream/spec runner evidence where safe. This resolver slice stays lane-local and does not activate the shared support component until the current batch is accepted and the next resolver gate requires it."
}
