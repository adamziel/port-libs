{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 28,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 93 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration erasure, assign-semantics class fields, derived `super(...)` insertion, top-level comma-expression `super(...)` splitting, and initialized private class-field insertion after `super(...)`",
    "phpPass": 93,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, conditional/lazy `super(...)` constructors, comma-expression controllers, and a private-settings controller whose `#settings` initializer is moved after `super(metadata)` before WordPress dependency assignments without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps initialized private class-field insertion after `super(...)` from upstream `TestTSSuperCall`",
    "audit": "audited 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 93 behavior tests, 468 assertion calls, and 0 failures, not full upstream parity. Required root `php tools/run-tests.php` now passes with 124 test files, 10969 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now handles assign-semantics initialized private class fields in derived classes. It preserves the `#field;` declaration, moves the initializer into the constructor immediately after the direct `super(...)` call, reorders private declarations after the injected constructor when needed, and covers a WordPress private `#settings` block-controller fixture.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 93 behavior tests, 468 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs; required root `php tools/run-tests.php` passes with 124 test files, 10969 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "244b829",
    "nextTask": "Continue the remaining derived-constructor insertion matrix such as return, throw, switch-test, and for-init `super(...)` placements, then broaden `TestTSClassSideEffectOrder` for target-version-specific static field lowering, auto-accessors, and namespace/class/enum merge matrices while keeping analyzer metadata separate from broader JS printing."
}
