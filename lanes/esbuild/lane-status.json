{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 62,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Go/Node/OpenSSL evidence; native PHP now maps 193 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, switch-case using diagnostics, block-wrapped switch-case helper cleanup, switch-clause for-using helper cleanup, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor/decorator boundaries, class-statement/class-expression decorator placement, opt-in class-decorator helper lowering for named/exported/default class statements, ES2021 after-class static assignments, private accessor/method type erasure, static private field retention, namespace function-scoped `using`/`await using` annotation erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 193,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, private/static/accessor controller metadata, decorated controller diagnostics, opt-in legacy decorator helper lowering for named and default-exported block controllers, explicit-resource-management asset cleanup, switch-case disposable asset diagnostics and helper cleanup, case-level for-using asset-loop cleanup, async-generator preview asset streams, auto-accessor block controllers, namespace-scoped disposable preview cleanup, and a namespace-backed async preview registration helper that preserves `await using` while erasing `AsyncDisposable` annotations without Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps a bounded unsupported-target `TestTSDecorators` class-decorator helper path for named/exported/default class statements, including decorator-array emission, anonymous default binding generation, empty decorator context names, `__decoratorStart`/`__decorateElement`/`__runInitializers` calls, type-argument erasure, generated `_init` collision avoidance, and explicit member-decorator helper rejection.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally. Focused esbuild lane verification passes 4 test files, 193 behavior tests, 1,373 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript legacy decorator helper bytes: 613`, `WordPress TypeScript default legacy decorator helper bytes: 577`, `WordPress TypeScript namespace async using bytes: 633`, `WordPress TypeScript switch case using diagnostic: yes`, and `WordPress TypeScript decorator boundary diagnostic: yes`. Required duplicate-root guard was clear, so this worker ran `php tools/run-tests.php`; it passed with 198 test files, 21,891 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now extends the opt-in class-decorator helper path to exported class statements and anonymous default-exported decorated classes. Anonymous default classes get a generated binding, `_class_decorators`, and an empty decorator context name, and a new WordPress default block-controller fixture preflights legacy decorator output without Node.",
    "blocker": "No current esbuild implementation blocker. Esbuild focused verification, the WordPress example, and the root harness are green. Remaining upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths.",
    "latestCommit": "current esbuild decorator helper lane batch; focused and root tests green",
    "nextTask": "Continue unsupported-target decorator helper lowering for member decorators, helper-backed private-static target lowering for older runtimes, remaining auto-accessor target interactions, broader namespace function type erasure, and upstream-style switch printer formatting against upstream."
}
