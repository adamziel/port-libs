{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 10,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` now passes in a detached runner worktree with Nix Go/Node/OpenSSL; lexer, import/export, import-attribute, import.meta, new URL asset-reference, TypeScript import-equals/type-only import-export, TypeScript namespace metadata, TypeScript import pruning, namespace import-equals lowering, top-level TypeScript import/export-equals module lowering, selected TypeScript type annotation/cast erasure, selected runtime enum, same-file enum access inlining, and non-exported const enum erasure slices mapped",
    "phpPass": 39,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; current fixtures preflight a WordPress block view script importing @wordpress/dom-ready, block metadata via `./block.json` with `{ type: \"json\" }`, relative stylesheet/worker assets via `new URL(..., import.meta.url)`, and TypeScript block scripts that keep erased @wordpress type imports out of pruned runtime dependency lists while preserving runtime `@wordpress/dom-ready`, `./block.json`, and legacy `wp.blocks` import-equals dependencies. The namespace lowerer maps a WordPress `export import blocks = wp.blocks` namespace alias into a namespace property assignment, and the module lowerer maps CommonJS `export = registerBlock`, typed edit callbacks, `satisfies BlockConfiguration`, runtime enum config, and non-exported const enum config into JavaScript without invoking Node.",
    "phase": "cloned static inventory plus upstream make test runner evidence and lexer/import-export/import-attribute/import-meta/new-url/typescript import-export/namespace/import-pruning/namespace-lowering/module-lowering/type-erasure/runtime-enum/enum-access/const-enum slices",
    "audit": "audited 2026-05-22: upstream make test passes locally; esbuild lane-only PHP verification passes 4 test files, 39 behavior tests, 203 assertions, and 0 failures, not full upstream parity. Required root `php tools/run-tests.php` passes with 76 test files, 4335 assertions, and 0 failures.",
    "currentWork": "Native JavaScript lexer plus module analyzer for static imports, expression/direct-string dynamic imports, exports, re-exports, import assertions/attributes, import.meta ESM markers, new URL(import.meta.url) asset references, TypeScript import-equals/export-equals/type-only import/export forms, TypeScript namespace/export namespace metadata, package/relative source classification, WordPress package imports, TypeScript import pruning metadata, a narrow TypeScript namespace import-equals lowerer, a narrow top-level TypeScript module lowerer for `import =`, `export import =`, `export =`, selected type annotation/cast erasure forms, selected runtime enum declarations, same-file enum member access substitution, non-exported const enum erasure, and upstream `make test` runner evidence from a detached hydrated worktree.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane-only verification passes with 4 test files, 39 behavior tests, 203 assertions, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` also runs. Required root `php tools/run-tests.php` passes with 76 test files, 4335 assertions, and 0 failures. The core upstream runner blocker remains resolved via detached runner worktree plus `nix-shell -p go nodejs openssl make test`; remaining release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "8a155d4",
    "nextTask": "Map the next bounded TypeScript printer slice, such as namespace value export lowering or broader enum constant folding for computed enum member expressions, while keeping analyzer metadata separate from broader JS printing."
}
