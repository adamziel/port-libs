{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 50,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 160 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, unsupported-target async-generator runtime lowering including multiple same-statement async-generator expressions, bounded await operands with nested commas, and bounded await unary/binary/logical/conditional precedence, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 160,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, derived `super(...)` ordering, explicit-resource-management asset cleanup, and async-generator preview asset streams. The registry fixture now lowers two same-statement preview streams in one array, preserves awaited multi-argument `queue.openNext(...)` calls plus parenthesized fallback preparation, keeps awaited logical-OR fallback handle selection outside the `__await(...)` operand, and keeps object-map and later callback-argument streams native without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded unsupported-target async-generator await operands with nested commas plus unary/binary/logical/conditional precedence boundaries on top of prior same-statement async-generator expression lowering and explicit-resource-management cleanup paths.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 160 behavior tests, 1122 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports the expanded async-generator registry helper output. Required root `php tools/run-tests.php` now passes: 183 test files, 17837 assertions, and 0 failures. An earlier exact root attempt in this run was red from `/tmp` disk-quota write failures in other lanes, but the final exact rerun is green.",
    "currentWork": "Native TypeScript module lowering now scans async-generator `await` operands instead of using a comma-fragile regex. Awaited calls such as `openAsset(queue, metadata, {...})`, parenthesized comma expressions such as `(prepare(metadata), openAsset(...))`, and `yield await renderAsset(asset, fallback)` lower through native `yield new __await(...)` without truncating nested comma arguments. The scanner also stops before top-level binary/logical/conditional operators, so `await loadAsset() + metadata.version`, `await queue.ready && queue.hasMore()`, and `await getChoice() ? a : b` preserve upstream await precedence while grouped and unary operands remain inside `__await(...)`. The WordPress registry fixture now exercises multi-argument preview asset resolution, fallback preparation, and logical-OR handle selection in same-statement async-generator streams.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and the final exact root `php tools/run-tests.php` pass. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths.",
    "latestCommit": "current lane batch commit: Port esbuild async generator await precedence; previous accepted commit bc50a28",
    "nextTask": "Continue unsupported-target async-generator runtime lowering for optional chaining/nullish await precedence, remaining expression contexts, and helper hygiene, then continue target-version-specific static field lowering, broader auto-accessor lowering, and namespace `await using` diagnostics against upstream."
}
