{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 28,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 95 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration erasure, assign-semantics class fields, derived `super(...)` insertion, top-level expression/return/throw comma-expression `super(...)` splitting, and initialized private class-field insertion after `super(...)`",
    "phpPass": 95,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, conditional/lazy `super(...)` constructors, comma-expression controllers, a return-super controller that returns `this` only after WordPress settings/dependency assignments, and a private-settings controller whose `#settings` initializer is moved after `super(metadata)` before WordPress dependency assignments without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps return/throw comma-expression insertion around `super(...)` from upstream `TestTSSuperCall` internals",
    "audit": "audited 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 95 behavior tests, 481 assertion calls, and 0 failures, not full upstream parity. Required root `php tools/run-tests.php` now passes with 126 test files, 11229 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now splits top-level return/throw comma expressions around one direct derived-constructor `super(...)` call. Pre-super side effects run before base initialization, parameter-property and assign-semantics field assignments run immediately after `super(...)`, post-super return/throw expressions keep their order, and the WordPress return-super controller fixture lowers without invoking the `__super` helper or Node.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 95 behavior tests, 481 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs; required root `php tools/run-tests.php` passes with 126 test files, 11229 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "3e48e53",
    "nextTask": "Continue the remaining derived-constructor insertion matrix such as switch-test and for-init `super(...)` placements, then broaden `TestTSClassSideEffectOrder` for target-version-specific static field lowering, auto-accessors, and namespace/class/enum merge matrices while keeping analyzer metadata separate from broader JS printing."
}
