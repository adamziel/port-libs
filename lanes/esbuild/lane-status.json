{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 63,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Go/Node/OpenSSL evidence; native PHP now maps 195 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, switch-case using diagnostics, block-wrapped switch-case helper cleanup, switch-clause for-using helper cleanup, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor/decorator boundaries, class-statement/class-expression decorator placement, opt-in class-decorator helper lowering for named/exported/default class statements, bounded instance-method decorator helper lowering, ES2021 after-class static assignments, private accessor/method type erasure, static private field retention, namespace function-scoped `using`/`await using` annotation erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 195,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, private/static/accessor controller metadata, decorated controller diagnostics, opt-in legacy decorator helper lowering for named/default-exported block controllers and simple method-decorated block registration classes, explicit-resource-management asset cleanup, switch-case disposable asset diagnostics and helper cleanup, case-level for-using asset-loop cleanup, async-generator preview asset streams, auto-accessor block controllers, namespace-scoped disposable preview cleanup, and a namespace-backed async preview registration helper that preserves `await using` while erasing `AsyncDisposable` annotations without Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps a bounded unsupported-target `TestTSDecorators` instance-method decorator helper path for simple named methods, including decorator-array emission, type-argument erasure, generated `_init` collision avoidance, newline-separated decorator/member parsing, constructor instance-initializer insertion, `__decorateElement(..., 1, name, ...)`, and `__decoratorMetadata` calls.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally. Focused esbuild lane verification passes 4 test files, 195 behavior tests, 1,394 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript legacy decorator helper bytes: 613`, `WordPress TypeScript default legacy decorator helper bytes: 577`, `WordPress TypeScript method legacy decorator helper bytes: 609`, `WordPress TypeScript namespace async using bytes: 633`, `WordPress TypeScript switch case using diagnostic: yes`, and `WordPress TypeScript decorator boundary diagnostic: yes`. The first root harness attempt exited 1 with 198 test files, 22,146 assertions, and 1 failure but the captured output truncated before the failing test name; a duplicate-root guard check was clear, the rerun waited on the repo test lock, and the latest `php tools/run-tests.php` passed with 198 test files, 22,151 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now extends opt-in unsupported-target decorator helper lowering to simple instance methods. It keeps decorators attached to the following member across line breaks, erases method return/type-only decorator syntax, inserts `__runInitializers(_init, 5, this)` for instance initializers, emits method-level `__decorateElement`/`__decoratorMetadata`, and adds a WordPress method-decorated registration fixture.",
    "blocker": "No current esbuild implementation blocker. Esbuild focused verification, the WordPress example, and the latest root harness are green. Remaining upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths; remaining decorator helper gaps are fields, accessors, static members, derived classes, mixed class/member decorators, and existing-constructor insertion.",
    "latestCommit": "current esbuild method decorator helper lane batch; focused and latest root tests green",
    "nextTask": "Continue unsupported-target decorator helper lowering for fields, accessors, static members, derived classes, and classes with existing constructors; then helper-backed private-static target lowering for older runtimes, remaining auto-accessor target interactions, broader namespace function type erasure, and upstream-style switch printer formatting against upstream."
}
