{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 35,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 118 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level helper lowering, bounded function-body and block-scoped helper lowering, per-iteration helper lowering for simple `for`/`for await` `using` loops, function-scope `using` annotation erasure with async-boundary validation, for/for-await `using` header type erasure and invalid for-in/initializer rejection, assign-semantics class fields, derived `super(...)` insertion, expression/return/throw/if/switch/for control-statement `super(...)` splitting, initialized private class-field insertion, and selected TypeScript auto-accessor marker/type erasure.",
    "phpPass": 118,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, computed-super controllers, conditional/lazy `super(...)` constructors, comma-expression controllers, return-super controllers, control-statement switch-test/for-initializer controllers, private-settings controllers, auto-accessor block controllers, typed top-level `using` disposable asset handles, function-scoped disposable preview asset handles with opt-in function-local helper cleanup, block-scoped disposable preview asset cleanup under native helper lowering, and `for (using ... of ...)` block asset loops without invoking Node; the top-level, function-scoped, block-scoped, and loop-scoped disposable asset fixtures verify opt-in legacy helper lowering with native `__using`/`__callDispose` cleanup.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps selected upstream `TestLowerUsing` for-loop explicit-resource-management helper lowering for simple `for`/`for await` loops, including per-iteration cleanup and async disposal awaits.",
    "audit": "verified 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 118 behavior tests, 659 assertion calls, and 0 failures. Required root `php tools/run-tests.php` passes with 149 test files, 13523 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now helper-lowers simple `for` and `for await` loops whose loop variable uses `using` or `await using` when `lowerUsingDeclarations` is enabled. It rewrites the loop value through a temporary, creates a per-iteration `__using` stack, preserves the loop body order, awaits `__callDispose` for async disposables, and adds a WordPress block asset loop helper-cleanup scenario.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 118 behavior tests, 659 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` passes with 149 test files, 13523 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "pending",
    "nextTask": "Cover explicit resource management helper symbol collision avoidance, broader async/control-flow `await using` lowering, target-version-specific static field lowering, and broader auto-accessor lowering."
}
