{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 70,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Go/Node/OpenSSL evidence; native PHP now maps 221 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor/decorator boundaries, class-statement/class-expression decorator placement, opt-in class/member decorator helper lowering, existing-constructor, bounded derived member decorator helper insertion, computed field/accessor/method/getter/setter decorator helper insertion, private field, private auto-accessor, private getter/setter, named/default/exported mixed class/member decorator helper insertion, ES2021 after-class static assignments, private accessor/method type erasure, static private field retention, namespace function-scoped `using`/`await using` helper cleanup, and WordPress block asset preflight fixtures.",
    "phpPass": 221,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, decorated controller diagnostics, opt-in legacy decorator helper lowering for named/default-exported block controllers plus instance/static method-decorated, constructor-backed and derived method-decorated, static field-decorated, private-field-decorated, private-auto-accessor-decorated, private-getter-decorated, accessor-decorated, computed-field-decorated, computed-method-decorated, computed-getter/setter-decorated, named mixed class-plus-method-decorated block registration classes, and anonymous default-exported mixed decorator block registration classes, explicit-resource-management asset cleanup, switch-case asset cleanup, async-generator preview asset streams, auto-accessor block controllers, namespace-scoped disposable preview cleanup, and namespace-backed async preview registration with helper-backed disposal without Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded `TestLowerUsingInsideTSNamespace` function-body explicit-resource-management helper cleanup.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; this slice used targeted local upstream esbuild CLI oracle checks for `namespace ns { export async function f() { await using x = y; z(x) } }`, non-async namespace `await using` rejection, namespace top-level `await using` rejection, and exported namespace `await using` rejection using `.upstream-cache/esbuild-runner/esbuild --loader=ts --target=es2020/2022`. `php -l lanes/esbuild/src/TypeScriptNamespaceLowerer.php` and `php -l lanes/esbuild/tests/TypeScriptNamespaceLowererTest.php` pass. Focused esbuild lane verification passes 4 test files, 1,677 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript namespace async using bytes: 2422`. The required duplicate-root guard found no active root harness, so this worker ran `php tools/run-tests.php`; aggregate root verification passes with 218 test files, 25,145 assertions, and 0 failures.",
    "currentWork": "Native TypeScript namespace lowering now wraps namespace function bodies that contain `using` or async-function `await using` declarations in a function-local `_stack`, `try`/`catch`/`finally`, `__using`, and `__callDispose` cleanup scope. Async namespace functions await the disposal promise, exported namespace values are still rewritten to namespace properties inside acquisition and registration statements, top-level namespace `await using` remains rejected, and non-async namespace functions still reject `await using`. The WordPress namespace async preview fixture now emits helper-backed cleanup instead of preserving explicit-resource-management syntax.",
    "blocker": "No current esbuild implementation blocker. Focused esbuild verification, the WordPress example, and aggregate root verification are green. Remaining upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths; remaining esbuild gaps include broader derived constructor/super decorator interactions, helper-backed private-static/private-accessor member-use rewriting for older runtimes, auto-accessor target interactions, and deeper namespace using nesting/stack-collision hygiene.",
    "latestCommit": "port-esbuild lane batch verified with focused and aggregate root PHP tests; see git log for commit hash",
    "nextTask": "Broaden decorator helper lowering for deeper derived constructor/super interactions, then helper-backed private-static/private-accessor member-use rewriting for older runtimes; continue auto-accessor target interactions and deeper namespace using helper nesting/stack-collision hygiene."
}
