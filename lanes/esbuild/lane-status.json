{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 29,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 99 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration erasure, assign-semantics class fields, derived `super(...)` insertion, expression/return/throw/if/switch/for control-statement `super(...)` splitting, and initialized private class-field insertion after `super(...)`",
    "phpPass": 99,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, computed-super controllers, conditional/lazy `super(...)` constructors, comma-expression controllers, return-super controllers, control-statement switch-test/for-initializer controllers, and private-settings controllers without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps `if`/`switch` test and `for` initializer insertion around `super(...)` from upstream `insertStmtsAfterSuperCall` internals",
    "audit": "audited 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 99 behavior tests, 509 assertion calls, and 0 failures, not full upstream parity. Required root `php tools/run-tests.php` passes with 129 test files, 11605 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now splits top-level `if`/`switch` test expressions and `for` initializer expressions around one direct derived-constructor `super(...)` call. Pre-super side effects run before base initialization, parameter-property and assign-semantics field assignments run immediately after `super(...)`, and the resumed control statement keeps post-super order without forcing the constructor through the `__super` helper.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 99 behavior tests, 509 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs; required root `php tools/run-tests.php` passes with 129 test files, 11605 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "pending lane commit",
    "nextTask": "Continue target-version-specific static field lowering, auto-accessors, broader TypeScript class side-effect-order matrices, and namespace/class/enum merge matrices while keeping analyzer metadata separate from broader JS printing."
}
