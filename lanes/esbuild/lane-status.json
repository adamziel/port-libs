{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 44,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 140 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level/function/block/control-flow/loop helper lowering, top-level helper hoisting for module boundaries and exportable declarations, helper-symbol collision avoidance in the module using runtime including for-loop `using` call sites, namespace-scoped `using` helper cleanup with helper-symbol collision avoidance, assign-semantics class fields, derived `super(...)` insertion, selected TypeScript auto-accessor marker/type erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 140,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, derived `super(...)` ordering, private-settings controllers, auto-accessor block controllers, top-level/function/block/loop scoped disposable asset cleanup, ordinary `while` asset-queue cleanup with `await using`, for-loop asset cleanup with helper-name collision avoidance, namespace-scoped disposable preview asset cleanup, and namespace cleanup helper-name collision avoidance inside WordPress block namespace IIFEs without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded explicit-resource-management cleanup for ordinary `while`, `for`, and `for await` block bodies on top of the prior top-level/function/if-block/for-using/namespace cleanup paths.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 140 behavior tests, 889 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs and reports the while-scoped asset helper output. Required root `php tools/run-tests.php` passes with 170 test files, 15764 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now applies the existing block-scope explicit-resource-management helper to ordinary `while`, `for`, and `for await` bodies, so `using`/`await using` declarations inside those control-flow blocks are disposed per iteration instead of being left as TypeScript syntax. A WordPress asset-queue fixture keeps `import metadata from \"./block.json\" with { type: \"json\" }` and an exported async registration function while lowering `while (queue.hasMore()) { await using asset ... }` through native `__using`/`__callDispose` cleanup.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and the required root PHP harness pass. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream's custom Go/browser/WASM build path.",
    "latestCommit": "f36366e",
    "nextTask": "Broaden async-generator `await using` lowering, target-version-specific static field lowering, broader auto-accessor lowering, and tighten namespace `await using` rejection diagnostics against upstream."
}
