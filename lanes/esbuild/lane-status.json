{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 42,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 134 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level/function/block/loop helper lowering, top-level helper hoisting for module boundaries and exportable declarations, helper-symbol collision avoidance in the module using runtime, namespace-scoped `using` helper cleanup, assign-semantics class fields, derived `super(...)` insertion, selected TypeScript auto-accessor marker/type erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 134,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, derived `super(...)` ordering, private-settings controllers, auto-accessor block controllers, top-level/function/block/loop scoped disposable asset cleanup, and namespace-scoped disposable preview asset cleanup inside a WordPress block namespace IIFE without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded upstream `TestLowerUsingInsideTSNamespace` namespace-local `using` declarations through native `__using`/`__callDispose` helper cleanup.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 134 behavior tests, 829 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs and reports namespace using helper output. Required root `php tools/run-tests.php` passes with 166 test files, 15399 assertions, and 0 failures.",
    "currentWork": "Native TypeScript namespace lowerer now detects namespace-local `using` declarations, erases TypeScript annotations, wraps namespace IIFE bodies with `_stack`/`try`/`catch`/`finally` cleanup, emits the native explicit-resource-management helper runtime, preserves exported namespace property assignments around the cleanup scope, and rejects `export using`, `export await using`, and unsupported namespace `await using` boundaries. Added a WordPress namespace preview asset fixture and example coverage.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification and the required root PHP harness pass. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream's custom Go/browser/WASM build path.",
    "latestCommit": "pending local commit",
    "nextTask": "Broaden async/control-flow `await using` lowering, target-version-specific static field lowering, broader auto-accessor lowering, namespace `await using` support, and namespace helper-symbol collision avoidance."
}
