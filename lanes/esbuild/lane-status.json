{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 38,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 124 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level helper lowering, bounded function-body and block-scoped helper lowering, per-iteration helper lowering for simple `for`/`for await` `using` loops, bounded top-level helper hoisting for directive/import/export module statements, simple local `export const`/`export class` declarations, and top-level function declarations, function-scope `using` annotation erasure with async-boundary validation, for/for-await `using` header type erasure and invalid for-in/initializer rejection, assign-semantics class fields, derived `super(...)` insertion, expression/return/throw/if/switch/for control-statement `super(...)` splitting, initialized private class-field insertion, and selected TypeScript auto-accessor marker/type erasure.",
    "phpPass": 124,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, computed-super controllers, conditional/lazy `super(...)` constructors, comma-expression controllers, return-super controllers, control-statement switch-test/for-initializer controllers, private-settings controllers, auto-accessor block controllers, typed top-level `using` disposable asset handles, imported `block.json` plus top-level `using` disposable preview asset cleanup with module import/export hoisting, exported `settings` plus registration classes around top-level disposable preview cleanup, an exported registration function around top-level disposable preview cleanup, function-scoped disposable preview asset handles with opt-in function-local helper cleanup, block-scoped disposable preview asset cleanup under native helper lowering, and `for (using ... of ...)` block asset loops without invoking Node; the top-level, imported-top-level, exported-local, exported-function, function-scoped, block-scoped, and loop-scoped disposable asset fixtures verify opt-in legacy helper lowering with native `__using`/`__callDispose` cleanup.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded upstream `TestLowerUsingHoisting` function-hoist cases so plain function declarations, direct `export function`, and `export default function` declarations remain outside top-level explicit-resource-management cleanup scopes while `using` declarations still lower to helper-wrapped `var` bindings.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 124 behavior tests, 714 assertion calls, and 0 failures. Required root `php tools/run-tests.php` passes with 157 test files, 14270 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now hoists top-level function declarations during `lowerUsingDeclarations`: plain functions, direct `export function`, and `export default function` stay before the generated helper scope, while top-level `using` declarations remain helper-wrapped `var` bindings inside the cleanup `try` block. Added a WordPress `block.json` fixture that exports a registration function closing over a disposable preview asset without trapping the export inside the cleanup scope.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 124 behavior tests, 714 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` passes with 157 test files, 14270 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "c102047",
    "nextTask": "Broaden explicit resource management helper hoisting for destructuring/default exports and helper symbol collision avoidance, then cover broader async/control-flow `await using` lowering, target-version-specific static field lowering, and broader auto-accessor lowering."
}
