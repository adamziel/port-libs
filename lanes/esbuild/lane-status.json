{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 37,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 122 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level helper lowering, bounded function-body and block-scoped helper lowering, per-iteration helper lowering for simple `for`/`for await` `using` loops, bounded top-level helper hoisting for directive/import/export module statements plus simple local `export const`/`export class` declarations, function-scope `using` annotation erasure with async-boundary validation, for/for-await `using` header type erasure and invalid for-in/initializer rejection, assign-semantics class fields, derived `super(...)` insertion, expression/return/throw/if/switch/for control-statement `super(...)` splitting, initialized private class-field insertion, and selected TypeScript auto-accessor marker/type erasure.",
    "phpPass": 122,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, computed-super controllers, conditional/lazy `super(...)` constructors, comma-expression controllers, return-super controllers, control-statement switch-test/for-initializer controllers, private-settings controllers, auto-accessor block controllers, typed top-level `using` disposable asset handles, imported `block.json` plus top-level `using` disposable preview asset cleanup with module import/export hoisting, exported `settings` plus registration classes around top-level disposable preview cleanup, function-scoped disposable preview asset handles with opt-in function-local helper cleanup, block-scoped disposable preview asset cleanup under native helper lowering, and `for (using ... of ...)` block asset loops without invoking Node; the top-level, imported-top-level, exported-local, function-scoped, block-scoped, and loop-scoped disposable asset fixtures verify opt-in legacy helper lowering with native `__using`/`__callDispose` cleanup.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps a bounded upstream `TestLowerUsingHoisting` local-export case so simple `export const` and `export class` declarations remain inside top-level explicit-resource-management cleanup scopes as runtime declarations while their export clauses are emitted after disposal.",
    "audit": "verified 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 122 behavior tests, 698 assertion calls, and 0 failures. Required root `php tools/run-tests.php` passes with 154 test files, 14015 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now rewrites simple top-level local exports during `lowerUsingDeclarations`: `export const`/`export let`/`export var` become runtime `var` declarations inside the cleanup `try` block, `export class` becomes a class expression assigned inside that block, and matching `export { ... }` clauses are emitted after `__callDispose`. Added a WordPress `block.json` fixture that exports block settings and a preview registration class while disposing preview assets without Node.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 122 behavior tests, 698 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` passes with 154 test files, 14015 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "pending-current-esbuild-local-export-hoist",
    "nextTask": "Broaden explicit resource management helper hoisting for destructuring/default exports and exported functions, cover helper symbol collision avoidance, broader async/control-flow `await using` lowering, target-version-specific static field lowering, and broader auto-accessor lowering."
}
