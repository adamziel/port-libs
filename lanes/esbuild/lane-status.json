{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 46,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 146 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level/function/block/control-flow/loop/class-method/object-method helper lowering, top-level helper hoisting for module boundaries and exportable declarations, helper-symbol collision avoidance in the module using runtime including for-loop `using` call sites, namespace-scoped `using` helper cleanup with helper-symbol collision avoidance, assign-semantics class fields, derived `super(...)` insertion, selected TypeScript auto-accessor marker/type erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 146,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, derived `super(...)` ordering, private-settings controllers, auto-accessor block controllers, top-level/function/block/loop scoped disposable asset cleanup, ordinary `while` asset-queue cleanup with `await using`, class and object-literal async-generator preview asset cleanup with `await using`, for-loop asset cleanup with helper-name collision avoidance, namespace-scoped disposable preview asset cleanup, and namespace cleanup helper-name collision avoidance inside WordPress block namespace IIFEs without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded object-literal async-generator method `await using` erasure and helper cleanup on top of the prior top-level/function/block/control-flow/for-using/class-method/namespace cleanup paths.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 146 behavior tests, 954 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs and reports the object async-generator asset helper output. Required root `php tools/run-tests.php` passes with 174 test files, 16300 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now detects object-literal method bodies that contain `using` or `await using`, erases method parameter/return and resource type annotations when preserving modern syntax, rejects `await using` in non-async object generator methods, and helper-lowers object async-generator method cleanup through native `__using`/`__callDispose` when requested. A WordPress preview asset object fixture keeps `import metadata from \"./block.json\" with { type: \"json\" }`, an exported `previewAssets` object, and an exported registration helper while lowering `async *assets(queue) { await using asset ... }` without Node/npm.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification and the WordPress example pass. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream's custom Go/browser/WASM build path; full unsupported-target async-generator runtime lowering remains unported.",
    "latestCommit": "pending lane-local commit after this status update; previous c8de659",
    "nextTask": "Broaden full unsupported-target async-generator runtime lowering, target-version-specific static field lowering, broader auto-accessor lowering, and tighten namespace `await using` rejection diagnostics against upstream."
}
