{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 66,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Go/Node/OpenSSL evidence; native PHP now maps 210 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor/decorator boundaries, class-statement/class-expression decorator placement, opt-in class/member decorator helper lowering, existing-constructor, bounded derived member decorator helper insertion, computed field/accessor/method decorator helper insertion, private field decorator helper insertion, ES2021 after-class static assignments, private accessor/method type erasure, static private field retention, namespace function-scoped `using`/`await using` annotation erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 210,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, decorated controller diagnostics, opt-in legacy decorator helper lowering for named/default-exported block controllers plus instance/static method-decorated, constructor-backed and derived method-decorated, static field-decorated, private-field-decorated, accessor-decorated, computed-field-decorated, and computed-method-decorated block registration classes, explicit-resource-management asset cleanup, switch-case asset cleanup, async-generator preview asset streams, auto-accessor block controllers, namespace-scoped disposable preview cleanup, and namespace-backed async preview registration without Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps a bounded unsupported-target `TestTSDecorators` private field decorator path for simple instance/static private fields plus derived base metadata capture.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; this slice used targeted local upstream esbuild CLI oracle checks for `class Foo { @dec #x = y }`, static private fields, and a derived private field. `php -l lanes/esbuild/src/TypeScriptModuleLowerer.php`, `php -l lanes/esbuild/tests/TypeScriptModuleLowererTest.php`, and `php -l lanes/esbuild/examples/wordpress-asset-preflight.php` pass. Focused esbuild lane verification passes 4 test files, 210 behavior tests, 1,523 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript private field legacy decorator helper bytes: 736` alongside existing decorator/using/async-generator/namespace preflight metrics. The duplicate-root guard was clear, so this worker ran `php tools/run-tests.php`; aggregate root verification passed with 206 test files, 23,904 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now handles a bounded private field decorator helper path. It allocates WeakMap storage, passes that storage to `__decorateElement(..., 21|29, \"#field\", ...)`, initializes instance/static private storage through `__privateAdd(... __runInitializers(...))`, and preserves derived class base capture before `__decoratorStart(base)`. A WordPress fixture now covers a hidden decorated `#settings` block metadata field lowered for legacy decorator targets without invoking Node.",
    "blocker": "No current esbuild implementation blocker. Focused esbuild verification, the WordPress example, and the aggregate root harness are green. Remaining upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths; remaining decorator helper gaps are private accessor decorators, computed getter/setter decorators, mixed class/member decorators, broader derived constructor/super interactions, and helper-backed private-static target lowering.",
    "latestCommit": "pending local esbuild private field decorator helper lane commit; focused tests, WordPress example, and root harness green",
    "nextTask": "Continue unsupported-target decorator helper lowering for private accessor decorators, computed getter/setter decorators, mixed class/member decorators, broader derived constructor/super interactions, and helper-backed private-static target lowering for older runtimes; then remaining auto-accessor target interactions, broader namespace function type erasure, and upstream-style switch printer formatting against upstream."
}
