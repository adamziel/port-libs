{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 56,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 181 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor/decorator boundaries, class-statement and class-expression decorator placement with ES2021 after-class static assignments, private accessor/method type erasure, static private field retention, and WordPress block asset preflight fixtures.",
    "phpPass": 181,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields including ES2021 after-class public static metadata assignment, private static block-settings caches that remain class-local, private accessor block settings with erased `BlockConfiguration` getter/setter annotations, anonymous default-exported static block controllers with computed worker asset keys, class-expression block controllers with comma-expression static metadata lowering, decorated class-expression block controllers with decorators attached to the class temp before after-class static metadata assignment, decorated block-controller classes with erased decorator type arguments plus after-class static settings, derived `super(...)` ordering, explicit-resource-management asset cleanup, async-generator preview asset streams, and auto-accessor block controllers.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps class-expression decorator preservation and ES2021 after-class static field interactions from upstream `TestTSDecorators`/`TestTSClassSideEffectOrder`.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally. Focused esbuild lane verification passes 4 test files, 181 behavior tests, 1,267 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports the decorated class-expression controller preflight. Required duplicate-root guard found no exact active root run before starting `php tools/run-tests.php`; root verification passed with 190 test files, 20,660 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now keeps decorators attached to class expressions when TypeScript member lowering or ES2021 after-class static assignment lowering is needed, erases class-expression decorator type arguments, restores semicolons for decorated class-expression statements, and wraps static public field assignments around the decorated class temp instead of emitting invalid `@decorator (...)` syntax. A WordPress decorated class-expression controller fixture covers typed `@blockController<BlockConfiguration>(metadata)` settings and computed worker assets without Node.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and the root PHP harness are green. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths. Unsupported-target decorator helper lowering, helper-backed private-static lowering for older runtimes, remaining auto-accessor target interactions, and namespace `await using` diagnostics remain future slices.",
    "latestCommit": "17c50db9",
    "nextTask": "Continue unsupported-target decorator lowering, helper-backed private-static target lowering for older runtimes, remaining auto-accessor target interactions, and namespace `await using` diagnostics against upstream."
}
