{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 9,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` now passes in a detached runner worktree with Nix Go/Node/OpenSSL; lexer, import/export, import-attribute, import.meta, new URL asset-reference, TypeScript import-equals/type-only import-export, TypeScript namespace metadata, TypeScript import pruning, namespace import-equals lowering, top-level TypeScript import/export-equals module lowering, and selected TypeScript type annotation/cast erasure slices mapped",
    "phpPass": 33,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; current fixtures preflight a WordPress block view script importing @wordpress/dom-ready, block metadata via `./block.json` with `{ type: \"json\" }`, relative stylesheet/worker assets via `new URL(..., import.meta.url)`, and TypeScript block scripts that keep erased @wordpress type imports out of pruned runtime dependency lists while preserving runtime `@wordpress/dom-ready`, `./block.json`, and legacy `wp.blocks` import-equals dependencies. The namespace lowerer maps a WordPress `export import blocks = wp.blocks` namespace alias into a namespace property assignment, and the module lowerer maps a CommonJS-style `export = registerBlock` runtime plus typed edit callbacks and `satisfies BlockConfiguration` metadata checks into JavaScript without invoking Node.",
    "phase": "cloned static inventory plus upstream make test runner evidence and lexer/import-export/import-attribute/import-meta/new-url/typescript import-export/namespace/import-pruning/namespace-lowering/module-lowering/type-erasure slices",
    "audit": "audited 2026-05-22: upstream make test passes locally; esbuild lane-only PHP verification passes 33 tests and 178 assertions, not full upstream parity; required root `php tools/run-tests.php` passes 70 test files and 3924 assertions",
    "currentWork": "Native JavaScript lexer plus module analyzer for static imports, expression/direct-string dynamic imports, exports, re-exports, import assertions/attributes, import.meta ESM markers, new URL(import.meta.url) asset references, TypeScript import-equals/export-equals/type-only import/export forms, TypeScript namespace/export namespace metadata, package/relative source classification, WordPress package imports, TypeScript import pruning metadata, a narrow TypeScript namespace import-equals lowerer, a narrow top-level TypeScript module lowerer for `import =`, `export import =`, `export =`, and selected type annotation/cast erasure forms, and upstream `make test` runner evidence from a detached hydrated worktree.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane-only verification passes with 4 test files, 178 assertions, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` also runs. Required root `php tools/run-tests.php` passes with 70 test files, 3924 assertions, and 0 failures. The core upstream runner blocker is resolved via detached runner worktree plus `nix-shell -p go nodejs openssl make test`; remaining release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "pending-esbuild-type-erasure-slice",
    "nextTask": "Map the next bounded TypeScript printer slice, such as selected enum/namespace value export lowering, while keeping analyzer metadata separate from broader JS printing."
}
