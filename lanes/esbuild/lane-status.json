{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 64,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Go/Node/OpenSSL evidence; native PHP now maps 197 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, switch-case using diagnostics, block-wrapped switch-case helper cleanup, switch-clause for-using helper cleanup, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor/decorator boundaries, class-statement/class-expression decorator placement, opt-in class-decorator helper lowering for named/exported/default class statements, bounded instance/static method decorator helper lowering, ES2021 after-class static assignments, private accessor/method type erasure, static private field retention, namespace function-scoped `using`/`await using` annotation erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 197,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, private/static/accessor controller metadata, decorated controller diagnostics, opt-in legacy decorator helper lowering for named/default-exported block controllers plus instance and static method-decorated block registration classes, explicit-resource-management asset cleanup, switch-case disposable asset diagnostics and helper cleanup, case-level for-using asset-loop cleanup, async-generator preview asset streams, auto-accessor block controllers, namespace-scoped disposable preview cleanup, and a namespace-backed async preview registration helper that preserves `await using` while erasing `AsyncDisposable` annotations without Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps a bounded unsupported-target `TestTSDecorators` static method decorator helper path for simple named static methods, including decorator-array emission, type-argument erasure, generated `_init` collision avoidance, existing-constructor preservation, `__decorateElement(..., 9, name, ...)`, `__decoratorMetadata`, and `__runInitializers(_init, 3, ClassName)` calls.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally. Focused esbuild lane verification passes 4 test files, 197 behavior tests, 1,410 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript legacy decorator helper bytes: 613`, `WordPress TypeScript default legacy decorator helper bytes: 577`, `WordPress TypeScript method legacy decorator helper bytes: 609`, `WordPress TypeScript static method legacy decorator helper bytes: 637`, `WordPress TypeScript namespace async using bytes: 633`, `WordPress TypeScript switch case using diagnostic: yes`, and `WordPress TypeScript decorator boundary diagnostic: yes`. The duplicate-root guard was clear before the aggregate run, and `php tools/run-tests.php` passed with 198 test files, 22,371 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now extends opt-in unsupported-target decorator helper lowering to simple static methods. It keeps decorators attached to the following static method, erases method return/type-only decorator syntax, avoids adding instance initializer constructors for static-only decorator classes, emits `__decorateElement(..., 9, ...)`, `__decoratorMetadata`, and static initializer calls, and adds a WordPress static method-decorated registration fixture.",
    "blocker": "No current esbuild implementation blocker. Esbuild focused verification, the WordPress example, and the latest root harness are green. Remaining upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths; remaining decorator helper gaps are fields, accessors, static fields/accessors, derived classes, mixed class/member decorators, and instance-member classes with existing constructors.",
    "latestCommit": "current esbuild static method decorator helper lane batch; focused and latest root tests green",
    "nextTask": "Continue unsupported-target decorator helper lowering for fields, accessors, static fields/accessors, derived classes, mixed class/member decorators, and instance-member classes with existing constructors; then helper-backed private-static target lowering for older runtimes, remaining auto-accessor target interactions, broader namespace function type erasure, and upstream-style switch printer formatting against upstream."
}
