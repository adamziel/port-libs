{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 32,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 112 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level helper lowering, function-scope `using` annotation erasure with async-boundary validation, for/for-await `using` header type erasure and invalid for-in/initializer rejection, assign-semantics class fields, derived `super(...)` insertion, expression/return/throw/if/switch/for control-statement `super(...)` splitting, initialized private class-field insertion, and selected TypeScript auto-accessor marker/type erasure.",
    "phpPass": 112,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, computed-super controllers, conditional/lazy `super(...)` constructors, comma-expression controllers, return-super controllers, control-statement switch-test/for-initializer controllers, private-settings controllers, auto-accessor block controllers, typed top-level `using` disposable asset handles, function-scoped disposable preview asset handles, and `for (using ... of ...)` block asset loops without invoking Node; the top-level disposable asset fixture also verifies opt-in legacy helper lowering with native `__using`/`__callDispose` cleanup.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps selected upstream `TestUsing` for/for-await explicit-resource-management loop header printing, type erasure, and invalid for-in/initializer boundaries.",
    "audit": "audited 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 112 behavior tests, 595 assertion calls, and 0 failures, not full upstream parity. Required root `php tools/run-tests.php` passes with 144 test files, 12904 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now recognizes `using` and `await using` declarations in `for`/`for await` loop headers. It erases `Disposable`/`AsyncDisposable` annotations without swallowing the `of` clause, rejects `using` in `for-in`, rejects `await using` in classic `for` headers, rejects initialized for-of `using` declarations, and adds a WordPress asset-registration loop fixture.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 112 behavior tests, 595 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` passes with 144 test files, 12904 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets. Remaining native explicit-resource-management gaps include block-scoped helper lowering, full `await using` helper lowering, and symbol collision avoidance.",
    "latestCommit": "925e515",
    "nextTask": "Continue explicit resource management lowering beyond the bounded top-level/function-scope/for-loop printer slices: block-scoped helper lowering, full `await using` helper lowering, symbol collision avoidance, then target-version-specific static field lowering and broader auto-accessor lowering."
}
