{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 55,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 176 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor/decorator boundaries, private accessor/method type erasure, ES2021 after-class public static field assignment for named/default-exported classes and class expressions, static private field retention, and WordPress block asset preflight fixtures.",
    "phpPass": 176,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields including ES2021 after-class public static metadata assignment, private static block-settings caches that remain class-local, private accessor block settings with erased `BlockConfiguration` getter/setter annotations, anonymous default-exported static block controllers with computed worker asset keys, class-expression block controllers with comma-expression static metadata lowering, derived `super(...)` ordering, explicit-resource-management asset cleanup, async-generator preview asset streams, and auto-accessor block controllers.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps private accessor/method type annotation erasure and valid private-name spacing from upstream `TestTSPrivateIdentifiers`.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally. Focused esbuild lane verification passes 4 test files, 176 behavior tests, 1,242 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports the private accessor controller preflight. Required root-harness guard found no exact active root run before this worker started `php tools/run-tests.php`; final root verification passed with 187 test files, 20,272 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now detects TypeScript parameter/return annotations inside private getters, setters, static private accessors, computed accessors, and private methods, erases those annotations, and preserves required `get #name`/`set [expr]` spacing. A WordPress private-accessor block-controller fixture covers typed private block settings without Node.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and the root PHP harness are green. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths. Helper-backed private-static lowering for older runtimes, unsupported-target decorator lowering, broader nested class-expression contexts, and namespace `await using` diagnostics remain future slices.",
    "latestCommit": "pending lane commit after root-green verification",
    "nextTask": "Continue full helper-backed private-static target lowering, unsupported-target decorator lowering, broader nested class-expression contexts, and namespace `await using` diagnostics against upstream."
}
