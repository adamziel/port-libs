{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 40,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 129 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level helper lowering, bounded function-body and block-scoped helper lowering, per-iteration helper lowering for simple `for`/`for await` `using` loops, bounded top-level helper hoisting for directive/import/export module statements, simple and destructured local `export const`/`export let`/`export var` declarations, simple `export class` declarations with class self-reference rewriting, top-level function declarations, default class/expression export hoisting with class self-reference rewriting, helper-symbol collision avoidance for the using runtime, function-scope `using` annotation erasure with async-boundary validation, for/for-await `using` header type erasure and invalid for-in/initializer rejection, assign-semantics class fields, derived `super(...)` insertion, expression/return/throw/if/switch/for control-statement `super(...)` splitting, initialized private class-field insertion, and selected TypeScript auto-accessor marker/type erasure.",
    "phpPass": 129,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, computed-super controllers, conditional/lazy `super(...)` constructors, comma-expression controllers, return-super controllers, control-statement switch-test/for-initializer controllers, private-settings controllers, auto-accessor block controllers, typed top-level `using` disposable asset handles, imported `block.json` plus top-level `using` disposable preview asset cleanup with module import/export hoisting, exported and destructured `settings` plus registration classes around top-level disposable preview cleanup, an exported registration function and a self-referencing default-exported block controller around top-level disposable preview cleanup, function-scoped disposable preview asset handles with opt-in function-local helper cleanup, block-scoped disposable preview asset cleanup under native helper lowering, and `for (using ... of ...)` block asset loops without invoking Node; the top-level, imported-top-level, exported-local, destructured-export, exported-function, default-controller, function-scoped, block-scoped, and loop-scoped disposable asset fixtures verify opt-in legacy helper lowering with native `__using`/`__callDispose` cleanup.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded upstream `TestLowerUsingHoisting` default class/expression export cases, exported/default class self-reference rewriting, and helper-symbol collision behavior so class exports are rewritten to cleanup-scope variables with upstream-style private class self-bindings when needed, default exports are emitted after disposal, and using helper runtime symbols are renamed when source identifiers collide.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 129 behavior tests, 787 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` was rerun and passes with 162 test files, 14874 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now rewrites top-level `export class`, `export default class`, and `export default <expr>` statements that occur while explicit-resource-management helper lowering is active. Exported/default classes drop unused class-expression names and rewrite used class self-references to `_Name` bindings, matching the focused upstream `TestLowerUsingHoisting` snapshots. The using helper runtime allocates non-colliding `__knownSymbol`/`__typeError`/`__using`/`__callDispose` names when source identifiers already use those helper names. The WordPress default-exported block controller fixture now self-references its controller class while preview assets are acquired and disposed without trapping the default export inside the cleanup scope.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 129 behavior tests, 787 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` passes with 162 test files, 14874 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "42b311d",
    "nextTask": "Broaden async/control-flow `await using` lowering, target-version-specific static field lowering, broader auto-accessor lowering, and local class declaration hoisting inside top-level using helper scopes."
}
