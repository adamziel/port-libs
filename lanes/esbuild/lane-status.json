{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 43,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 138 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level/function/block/loop helper lowering, top-level helper hoisting for module boundaries and exportable declarations, helper-symbol collision avoidance in the module using runtime including for-loop `using` call sites, namespace-scoped `using` helper cleanup with helper-symbol collision avoidance, assign-semantics class fields, derived `super(...)` insertion, selected TypeScript auto-accessor marker/type erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 138,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, derived `super(...)` ordering, private-settings controllers, auto-accessor block controllers, top-level/function/block/loop scoped disposable asset cleanup, for-loop asset cleanup with helper-name collision avoidance, namespace-scoped disposable preview asset cleanup, and namespace cleanup helper-name collision avoidance inside WordPress block namespace IIFEs without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded explicit-resource-management helper-symbol collision avoidance for `for (using ... of ...)` loop helper call sites on top of the prior top-level/function/block/namespace cleanup paths.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 138 behavior tests, 865 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs and reports for-loop and namespace using collision helper output. Required root `php tools/run-tests.php` passes with 170 test files, 15617 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now routes `for (using ... of ...)` and `for (await using ... of ...)` acquisition call sites through the allocated explicit-resource-management helper name instead of hardcoding `__using`. WordPress asset-loop fixtures keep colliding WordPress globals intact while generated cleanup helpers use renamed symbols such as `__using2`/`__callDispose2`.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and the required root PHP harness pass. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream's custom Go/browser/WASM build path.",
    "latestCommit": "pending local commit",
    "nextTask": "Broaden async-generator/control-flow `await using` lowering, target-version-specific static field lowering, broader auto-accessor lowering, and tighten namespace `await using` rejection diagnostics against upstream."
}
