{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 61,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 190 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, switch-case using diagnostics, block-wrapped switch-case helper cleanup, switch-clause for-using helper cleanup, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor/decorator boundaries, class-statement and class-expression decorator placement with ES2021 after-class static assignments, private accessor/method type erasure, static private field retention, namespace function-scoped `using`/`await using` annotation erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 190,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, private/static/accessor controller metadata, decorated controller diagnostics, explicit-resource-management asset cleanup, switch-case disposable asset diagnostics and helper cleanup, case-level for-using asset-loop cleanup, async-generator preview asset streams, auto-accessor block controllers, namespace-scoped disposable preview cleanup, and a namespace-backed async preview registration helper that preserves `await using` while erasing `AsyncDisposable` annotations without Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps selected upstream `TestTSUsing` function-scope semantics inside TypeScript namespace function bodies, including `using` annotation erasure, async `await using` preservation, exported namespace value rewrites, and non-async `await using` diagnostics.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally. Focused esbuild lane verification passes 4 test files, 190 behavior tests, 1,342 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript namespace async using bytes: 633`, `WordPress TypeScript switch case using diagnostic: yes`, and `WordPress TypeScript decorator boundary diagnostic: yes`. Required duplicate-root guard was clear, so this worker ran the root harness once; `php tools/run-tests.php` exited 0 with 197 test files, 21,650 assertions, and 0 failures.",
    "currentWork": "Native TypeScript namespace lowering now has a using-aware function declaration printer. It erases resource type annotations from namespace function-body `using` declarations, preserves `await using` only inside async namespace functions, rewrites exported namespace values such as `settings` to namespace properties inside those function bodies, and rejects non-async namespace functions that contain `await using`. The WordPress preflight example now includes an exported async namespace registration helper for preview assets.",
    "blocker": "No current esbuild implementation blocker. Esbuild focused verification, the WordPress example, and the root harness are green. Remaining upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths.",
    "latestCommit": "0fab7309 (root green)",
    "nextTask": "Continue unsupported-target decorator helper lowering, helper-backed private-static target lowering for older runtimes, remaining auto-accessor target interactions, broader namespace function type erasure, and upstream-style switch printer formatting against upstream."
}
