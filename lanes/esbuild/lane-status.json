{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 56,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 178 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor/decorator boundaries, class-statement decorator placement with ES2021 after-class static assignments, private accessor/method type erasure, static private field retention, and WordPress block asset preflight fixtures.",
    "phpPass": 178,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields including ES2021 after-class public static metadata assignment, private static block-settings caches that remain class-local, private accessor block settings with erased `BlockConfiguration` getter/setter annotations, anonymous default-exported static block controllers with computed worker asset keys, class-expression block controllers with comma-expression static metadata lowering, decorated block-controller classes with erased decorator type arguments plus after-class static settings, derived `super(...)` ordering, explicit-resource-management asset cleanup, async-generator preview asset streams, and auto-accessor block controllers.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps class-statement decorator placement and ES2021 after-class static field interactions from upstream `TestTSDecorators`/`TestTSClassSideEffectOrder`.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally. Focused esbuild lane verification passes 4 test files, 178 behavior tests, 1,254 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports the decorated controller preflight. Required root-harness guard found no exact active root run before this worker started `php tools/run-tests.php`; final root verification passed with 189 test files, 20,467 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now treats decorated class statements as class statements when the class body requires TypeScript lowering, normalizes `export @x class Foo` to upstream-style `@x export class Foo`, erases class decorator type arguments, rejects double decorator placement, and keeps ES2021 public static field assignments after the decorated class. A WordPress decorated block-controller fixture covers typed `@blockController<BlockConfiguration>(metadata)` settings without Node.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and the root PHP harness are green. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths. Unsupported-target decorator helper lowering, helper-backed private-static lowering for older runtimes, decorated class-expression target interactions, remaining auto-accessor target interactions, and namespace `await using` diagnostics remain future slices.",
    "latestCommit": "10cdf74f",
    "nextTask": "Continue unsupported-target decorator lowering, helper-backed private-static target lowering, decorated class-expression target interactions, remaining auto-accessor target interactions, and namespace `await using` diagnostics against upstream."
}
