{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 28,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 91 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration erasure, assign-semantics class fields, derived `super(...)` insertion, and the new top-level comma-expression `super(...)` split slice",
    "phpPass": 91,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, conditional/lazy `super(...)` constructors, and a comma-expression controller that preloads assets before `super(metadata)` while injecting WordPress settings assignments immediately after `super(...)` without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps top-level comma-expression derived-super assignment insertion from upstream `TestTSSuperCall`",
    "audit": "audited 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 91 behavior tests, 455 assertion calls, and 0 failures, not full upstream parity. Required root `php tools/run-tests.php` passes with 123 test files, 10675 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now splits a supported constructor expression statement of the form `before(), super(...), after()` when a derived assign-semantics class needs parameter-property or field assignment injection. It emits pre-super side effects, the direct `super(...)` call, generated `this.*` assignments, and post-super comma-chain side effects without taking the `__super` helper path. A WordPress comma-super block-controller fixture and example preflight line cover the shared-hosting asset tooling scenario.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 91 behavior tests, 455 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` passes with 123 test files, 10675 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets.",
    "latestCommit": "pending esbuild comma-super lane commit",
    "nextTask": "Continue the remaining derived-constructor insertion matrix such as return, throw, switch-test, and for-init `super(...)` placements, then broaden `TestTSClassSideEffectOrder` for target-version-specific static field lowering, private fields, auto-accessors, and namespace/class/enum merge matrices while keeping analyzer metadata separate from broader JS printing."
}
