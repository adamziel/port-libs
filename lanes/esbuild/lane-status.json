{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 50,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 163 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, unsupported-target async-generator runtime lowering including multiple same-statement async-generator expressions, bounded await operands with nested commas, bounded await unary/binary/logical/conditional precedence, optional-chain/nullish fallback precedence, async-helper collision avoidance after prior statements, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 163,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, derived `super(...)` ordering, explicit-resource-management asset cleanup, and async-generator preview asset streams. The registry fixture lowers two same-statement preview streams in one array, preserves awaited multi-argument `queue.openNext(...)` calls plus parenthesized fallback preparation, keeps awaited logical-OR and optional-chain/nullish fallback handle selection outside the `__await(...)` operand, and keeps object-map and later callback-argument streams native without invoking Node. The new helper-collision fixture lowers a WordPress interactive asset stream after existing `wp.interactivity` helper globals, renaming generated `__await`/`__asyncGenerator`/`__yieldStar`/`__forAwait` helpers without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded unsupported-target async-generator declarations after prior statements plus generated async-helper collision avoidance on top of prior await precedence and expression-context coverage.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 163 behavior tests, 1165 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports the async-generator helper-collision fixture output. Required root `php tools/run-tests.php` passes: 183 test files, 18198 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now recognizes `async function*` declarations that appear after earlier top-level statements and routes generated async-generator runtime names through the existing collision allocator. Focused coverage includes prior statements that bind `__knownSymbol`, `__typeError`, `__await`, `__asyncGenerator`, `__yieldStar`, and `__forAwait`, plus a WordPress `wp.interactivity` helper-global fixture.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and the exact root `php tools/run-tests.php` pass. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths.",
    "latestCommit": "eb10f46314588c02718f668e19ff87c5aae3d69a records current esbuild async-generator helper collision slice; previous accepted commit bc50a28",
    "nextTask": "Continue unsupported-target async-generator runtime lowering for remaining expression contexts and broader helper hygiene, then continue target-version-specific static field lowering, broader auto-accessor lowering, and namespace `await using` diagnostics against upstream."
}
