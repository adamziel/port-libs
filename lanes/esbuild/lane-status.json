{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 51,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 168 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, unsupported-target async-generator runtime lowering, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor normalization/erasure, decorated auto-accessor type erasure, decorator type-argument erasure and invalid decorator-call chain diagnostics from upstream `TestTSDecorators`, ES2021 after-class static field assignment from upstream `TestTSClassSideEffectOrder`, and WordPress block asset preflight fixtures.",
    "phpPass": 168,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields including ES2021 after-class static metadata assignment, derived `super(...)` ordering, explicit-resource-management asset cleanup, async-generator preview asset streams, and a block controller with plain static/instance plus decorated typed/computed/private auto-accessors.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps upstream `TestTSClassSideEffectOrder` ES2021 target behavior for assign-semantics static fields by moving static field assignments after named classes when class static blocks are unavailable.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane-only PHP verification passes 4 test files, 168 behavior tests, 1188 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports the ES2021 static-field preflight. A full root harness was accidentally started via `php tools/run-tests.php lanes/esbuild/tests` before the required pgrep guard because the root runner ignores path arguments; it completed green with 183 test files, 19088 assertions, and 0 failures. A later `pgrep -af '^php tools/run-tests\\.php( |$)'` check initially showed no active root harness, but the final pre-close check showed other root harnesses active: PID 704601 (`php tools/run-tests.php lanes/rclone/tests/DeletePlanningTest.php`) and PID 704613 (`php tools/run-tests.php`), both owned by `claude`, so no duplicate root run was started.",
    "currentWork": "Native TypeScript module lowering now accepts a `targetYear` option. With `useDefineForClassFields=false` and `targetYear < 2022`, public static field assignments are collected outside the class and rewritten from `this.*`/`this[...]` to the named class while preserving computed key side-effect ordering. The WordPress class-field fixture now verifies `CardBlockController.metadata = metadata` for ES2021-style preflight output without invoking Node.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and the latest completed root harness are green. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths. Broader target-specific static-field coverage still needs anonymous/default exports and private static fields.",
    "latestCommit": "lane-scoped decorator plus ES2021 static-field target batch committed by this worker; exact commit hash is reported in the lane worker final report",
    "nextTask": "Continue broader static-field target coverage for anonymous/default exports and private static fields, unsupported-target decorator lowering, remaining auto-accessor target interactions, and namespace `await using` diagnostics against upstream."
}
