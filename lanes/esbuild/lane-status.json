{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 32,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 110 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level helper lowering, function-scope `using` annotation erasure with async-boundary validation, assign-semantics class fields, derived `super(...)` insertion, expression/return/throw/if/switch/for control-statement `super(...)` splitting, initialized private class-field insertion, and selected TypeScript auto-accessor marker/type erasure.",
    "phpPass": 110,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, computed-super controllers, conditional/lazy `super(...)` constructors, comma-expression controllers, return-super controllers, control-statement switch-test/for-initializer controllers, private-settings controllers, auto-accessor block controllers, typed top-level `using` disposable asset handles, and function-scoped disposable preview asset handles without invoking Node; the top-level disposable asset fixture also verifies opt-in legacy helper lowering with native `__using`/`__callDispose` cleanup.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps selected upstream `TestUsing` function-scope declaration printing and async-boundary validation for explicit resource management.",
    "audit": "audited 2026-05-22: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 110 behavior tests, 580 assertion calls, and 0 failures, not full upstream parity. Required root `php tools/run-tests.php` passes with 143 test files, 12722 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowerer now recognizes function-scope `using` declarations in function declarations, function expressions, and async arrow bodies. It erases `Disposable`/type annotations, preserves `await using` in async bodies, rejects `await using` in non-async function bodies, and adds a WordPress exported block-registration helper fixture that disposes preview asset handles without requiring Node.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification passes with 4 test files, 110 behavior tests, 580 assertion calls, and 0 failures; `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs. Required root `php tools/run-tests.php` passes with 143 test files, 12722 assertions, and 0 failures. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, and Yarn PnP targets. Remaining native explicit-resource-management gaps include block-scoped helper lowering, `for`/`for-of` using declarations, full `await using` helper lowering, and symbol collision avoidance.",
    "latestCommit": "8ee25ae",
    "nextTask": "Continue explicit resource management lowering beyond the bounded top-level/function-scope printer slice: block-scoped helper lowering, `for`/`for-of` using declarations, full `await using` helper lowering, symbol collision avoidance, then target-version-specific static field lowering and broader auto-accessor lowering."
}
