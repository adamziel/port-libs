{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 48,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 150 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, multiline `using`/`await using` declaration normalization, explicit resource management nullish-initializer optimization, top-level/function/block/control-flow/loop/class-method/object-method helper lowering, opt-in unsupported-target async-generator runtime helper lowering for declarations, expressions, class/object methods, `yield*`, `await`, and `for await`, top-level helper hoisting for module boundaries and exportable declarations, helper-symbol collision avoidance in the module using runtime including for-loop `using` call sites, namespace-scoped `using` helper cleanup with helper-symbol collision avoidance, assign-semantics class fields, derived `super(...)` insertion, selected TypeScript auto-accessor marker/type erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 150,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, computed class-field asset keys, derived `super(...)` ordering, private-settings controllers, auto-accessor block controllers, top-level/function/block/loop scoped disposable asset cleanup, ordinary `while` asset-queue cleanup with `await using`, class and object-literal async-generator preview asset cleanup with `await using`, exported async-generator function preview asset streams lowered through native `__asyncGenerator`/`__await`/`__yieldStar`, for-loop asset cleanup with helper-name collision avoidance, namespace-scoped disposable preview asset cleanup, and namespace cleanup helper-name collision avoidance inside WordPress block namespace IIFEs without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps bounded unsupported-target async-generator runtime lowering for function declarations/expressions and class/object methods on top of prior explicit-resource-management cleanup paths.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 150 behavior tests, 1002 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` runs and reports the async-generator function asset helper output. An intermediate root sample was red in another lane at `lanes/pandoc/tests/MarkdownReaderTest.php` / `maps upstream markdown reader more rectangular grid tables` with `Call to a member function attr() on null`; a focused Pandoc rerun passed, and the final required root `php tools/run-tests.php` passes with 174 test files, 16538 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now has an opt-in `lowerAsyncGenerators` path that lowers selected upstream `TestLowerAsyncGenerator` shapes to native runtime helpers: `async function*` declarations/expressions become ordinary functions returning `__asyncGenerator`, `yield*` routes through `__yieldStar`, `await` in generator bodies becomes `yield new __await(...)`, and simple `for await` plus `for await (await using ...)` bodies lower through `__forAwait` with generator-safe disposal. Class and object async-generator methods can now choose the same runtime lowering instead of only erasing `await using` annotations. A WordPress preview asset stream fixture keeps `import metadata from \"./block.json\" with { type: \"json\" }`, exports `streamPreviewAssets`, and lowers disposable async asset streaming without Node/npm.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and the required root test harness pass. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream's custom Go/browser/WASM build path. Remaining async-generator gaps include arrow/nested expression coverage, default-export edge cases, broader await-expression parsing, full helper hygiene parity, and complete unsupported-target async-function lowering.",
    "latestCommit": "b2682d7",
    "nextTask": "Broaden unsupported-target async-generator runtime lowering to arrow/default-export/nested expression cases, then continue target-version-specific static field lowering, broader auto-accessor lowering, and namespace `await using` diagnostics against upstream."
}
