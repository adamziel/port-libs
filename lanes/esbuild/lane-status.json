{
    "schemaVersion": 1,
    "library": "esbuild",
    "estimatedProgress": 50,
    "suiteProgress": "cloned static inventory: 2,567 counted upstream test entry points; upstream `make test` passes in a detached runner worktree with Nix Go/Node/OpenSSL; native PHP now maps 158 focused behavior tests across lexer, module analysis, TypeScript import/export forms, namespace/module lowering, enum/type/declaration/using erasure, explicit-resource-management helper lowering, unsupported-target async-generator runtime lowering including multiple same-statement async-generator expressions, assign-semantics class fields, derived `super(...)` insertion, TypeScript auto-accessor erasure, and WordPress block asset preflight fixtures.",
    "phpPass": 158,
    "phpFail": 0,
    "wordpressScenarios": "Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments; fixtures preflight WordPress block view assets, `block.json` metadata imports, `new URL(..., import.meta.url)` stylesheet/worker assets, type-only WordPress imports, namespace-backed registrations, CommonJS `export =`, runtime/const enum block configs, ambient declarations, constructor-property controllers, assign-semantics class fields, derived `super(...)` ordering, explicit-resource-management asset cleanup, and async-generator preview asset streams. The registry fixture now lowers two same-statement preview streams in one array plus object-map and later callback-argument streams without invoking Node.",
    "phase": "cloned static inventory plus upstream make-test runner evidence and native lexer/module-analysis/typescript-lowering slices; current slice maps same-statement unsupported-target async-generator expression lowering on top of prior explicit-resource-management cleanup paths.",
    "audit": "verified 2026-05-23: upstream make test previously passed locally; esbuild lane PHP verification passes 4 test files, 158 behavior tests, 1105 assertion calls, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports the expanded async-generator registry helper output. Required root `php tools/run-tests.php` passes with 180 test files, 17439 assertions, and 0 failures.",
    "currentWork": "Native TypeScript module lowering now transforms every supported `async function*` expression in a statement instead of stopping after the first match. Arrays with multiple preview streams, repeated call arguments, and object-literal property values all lower through native `__asyncGenerator`, `__await`, and `__yieldStar` helpers while preserving existing `await using` cleanup through `__using`/`__callDispose`. The WordPress registry fixture now includes two same-statement preview streams, each with disposable asset cleanup, plus the existing object-map and callback-argument streams.",
    "blocker": "No current esbuild implementation blocker. Esbuild lane verification, the WordPress example, and required root `php tools/run-tests.php` pass. Remaining esbuild upstream release-extra coverage is `make test-all` with Deno, WASM browser/node, typecheck, Yarn PnP targets, and upstream custom Go/browser/WASM build paths. Remaining native gaps include broader await-expression parsing, helper hygiene parity, target-version-specific static field lowering, broader auto-accessor lowering, namespace `await using` diagnostics, and full unsupported-target async-function lowering.",
    "latestCommit": "bc50a28 (same-statement async-generator lowering implementation batch)",
    "nextTask": "Broaden unsupported-target async-generator runtime lowering for wider await-expression parsing and helper hygiene, then continue target-version-specific static field lowering, broader auto-accessor lowering, and namespace `await using` diagnostics against upstream."
}
