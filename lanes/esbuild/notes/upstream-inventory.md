# esbuild Upstream Inventory

- Upstream: `evanw/esbuild`
- Commit: `6a794dff68e6a43539f6da671e3080efdf11ca70`
- License: MIT
- Inventory method: shallow blob-filtered clone in `.upstream-cache/esbuild` with no checkout; counted paths with `git ls-tree` and hydrated only targeted metadata/test files through `git show`.

## Counted Denominator

- 349 repository paths.
- 63 test-related paths.
- 33 Go `_test.go` files.
- 1,391 top-level Go `func Test*` functions.
- 9,950 Go helper invocation subcases such as lexer/parser/printer expectations.
- 1,059 bundler `expectBundled` cases.
- 14 bundler snapshot files.
- 12 Node/browser test scripts.
- 331 `scripts/js-api-tests.js` async API cases.
- 97 `scripts/plugin-tests.js` async plugin cases.
- 748 `scripts/end-to-end-tests.js` CLI cases.

The lane denominator is the 2,567 counted upstream test entry points made from 1,391 Go tests, 331 JS API cases, 97 plugin cases, and 748 end-to-end CLI cases. The larger helper and snapshot counts are tracked as subcase coverage targets, not runner pass parity.

## Runner Evidence

The original inventory cache remains a blob-filtered/no-checkout cache with deletion status, so the runner was executed from a separate detached worktree instead of destructively restoring that cache:

```text
git worktree add --detach ../esbuild-runner HEAD
```

The first runner pass installed the missing Go/Node toolchain through Nix and let the Makefile install its locked `scripts/node_modules` dependencies:

```text
nix-shell -p go nodejs --run 'go version && node -v && npm -v && make test'
```

That passed, but `scripts/end-to-end-tests.js` skipped HTTPS cases because `openssl` was not on PATH. The runner was then rerun with OpenSSL available:

```text
nix-shell -p go nodejs openssl --run 'go version && node -v && openssl version && make test'
```

Result: passed. The run covered Go tests and vet plus the Node source-map, end-to-end, JS API, plugin, register, node-unref, and decorator targets. Environment evidence:

- Go: `go1.24.10`
- Node: `v22.20.0`
- npm: `10.9.3`
- OpenSSL: `OpenSSL 3.4.3`

Deno is also available through Nix for the later release-extra lane:

```text
nix-shell -p deno --run 'deno --version'
deno 2.2.12
```

`make test-all` was not executed in this slice. It adds Deno tests, WASM node/browser tests, TypeScript typechecks, and Yarn PnP coverage and would also build upstream's custom Go 1.26.1 compiler plus browser/WASM artifacts. Treat the current evidence as core upstream `make test` parity, not release-extra coverage.

Focused update for the current slice: targeted reads of `internal/bundler_tests/bundler_lower_test.go` and `internal/bundler_tests/snapshots/snapshots_lower.txt` inspected upstream `TestLowerUsing` function-body, block-scope, `for`/`for await` loop helper output, and `TestLowerUsingHoisting` module-boundary/local-export/function-hoist/default-export/local-class output. No broad `git grep` or full checkout hydration was used. The native slice maps simple function-body cases where unsupported-target `using` and `await using` declarations are wrapped in a function-local stack, `try`/`catch`/`finally`, `__using`, and `__callDispose` cleanup, nested block cases with their own per-block cleanup scope, simple `for`/`for await` loop cases with per-iteration disposal stacks and awaited cleanup for async disposables, a bounded top-level hoisting case that keeps directive prologue strings, ES import declarations, export-star/export-from declarations, trailing export clauses, and top-level plain/exported/default function declarations outside the generated top-level `try`/`catch` helper scope, bounded local-export cases that rewrite simple and destructured `export var`/`export let`/`export const` declarations plus simple `export class` declarations into runtime declarations inside the cleanup scope plus export clauses after disposal, bounded ordinary local class cases that rewrite top-level `class Name` declarations inside the cleanup scope to `var Name = class ...` assignments, bounded exported/default/local class self-reference cases that drop unused class-expression names and rewrite used class self-references through `_Name` bindings, bounded default class/expression export cases that become cleanup-scope variables with post-disposal default export clauses, and a helper-symbol collision case that renames generated `__knownSymbol`/`__typeError`/`__using`/`__callDispose` helpers when source identifiers collide.

Focused update for the namespace `using` slice: targeted reads of the same upstream files inspected `TestLowerUsingInsideTSNamespace` and the corresponding snapshot for `namespace ns { export let a = b; using c = d; export let e = f }`. No broad `git grep` or full upstream suite was used. The native slice maps non-`await` namespace-local `using` declarations to `const name = __using(_stack, initializer)` inside a namespace IIFE `try`/`catch`/`finally` cleanup wrapper, preserves exported namespace assignments before and after the disposable acquisition, erases TypeScript annotations on the namespace-local declaration, and rejects unsupported namespace `await using`.

Focused update for the namespace helper collision slice: reusing the targeted `TestLowerUsingHoisting` helper-name collision boundary and the namespace `using` lowering shape above, the native namespace lowerer now allocates unique names for generated `__knownSymbol`, `__typeError`, `__using`, and `__callDispose` helpers when those identifiers already appear in the source. Namespace-local `using` calls and cleanup `finally` blocks use the renamed helper names. No additional upstream checkout hydration or broad runner was needed.

Focused update for the namespace function `await using` slice: targeted reads reused upstream `TestTSUsing` function-scope printer expectations and the already inspected namespace emission shape from `TestLowerUsingInsideTSNamespace`. No broad checkout hydration or upstream rerun was needed. The native namespace lowerer now erases TypeScript resource annotations from `using` declarations inside namespace function bodies and from `await using` declarations inside async namespace functions, rewrites referenced exported namespace values such as `settings` to namespace properties, and rejects `await using` inside non-async namespace functions.

Focused update for the for-loop helper collision slice: targeted reads reused upstream `TestLowerUsing` `for (using ... of ...)` / `for (await using ... of ...)` helper-lowering snapshots and the `TestLowerUsingHoisting` helper-name collision boundary. The native module lowerer now routes per-iteration acquisition calls through the allocated helper symbol, so colliding source globals such as `__using` do not hijack generated `for`/`for await` resource acquisition. No additional upstream checkout hydration or broad runner was needed.

Focused update for the control-flow block-body cleanup slice: targeted reads reused upstream `TestLowerUsing` block-scope and loop helper snapshots from `internal/bundler_tests/bundler_lower_test.go` plus `internal/bundler_tests/snapshots/snapshots_lower.txt`. The native module lowerer now applies the existing block-scope `__using`/`__callDispose` cleanup wrapper to ordinary `while`, `for`, and `for await` bodies that contain `using` or `await using` declarations, giving each iteration its own disposal stack. No additional upstream checkout hydration or broad runner was needed.

Focused update for the class async-generator cleanup slice: targeted reads of `internal/bundler_tests/bundler_lower_test.go` inspected upstream `TestLowerAsyncGenerator`, and targeted reads of `internal/bundler_tests/snapshots/snapshots_lower.txt` inspected its class-method output shape. The native module lowerer now erases `await using` type annotations inside class async generator methods and, when explicit-resource-management helper lowering is requested, wraps those method bodies in native `__using`/`__callDispose` cleanup while preserving native `async *method()` syntax. This is a bounded class-method slice only; full unsupported-target async-generator runtime lowering remains future work. No additional upstream checkout hydration or broad runner was needed.

Focused update for the object-literal async-generator cleanup slice: targeted reads of the same upstream `TestLowerAsyncGenerator` input and snapshot inspected the `foo = { async *bar() { await using ... } }` object-method boundary. The native module lowerer now recognizes object-literal async generator methods inside runtime statements, erases method parameter/return and resource type annotations, rejects `await using` in non-async object generator methods, and helper-lowers method-local `await using` cleanup through native `__using`/`__callDispose` while preserving native `async *method()` syntax. This remains a bounded parser/printer slice and does not claim full unsupported-target async-generator runtime lowering.

Focused update for the async-generator runtime slice: targeted reads of upstream `TestLowerAsyncGenerator` and `TestLowerAsyncGeneratorNoAwait` snapshots inspected the runtime lowering shape for `async function*`, function expressions, object methods, class methods, `yield*`, `await using`, and simple `for await` loops. The native module lowerer now exposes an opt-in `lowerAsyncGenerators` path that emits native `__await`, `__asyncGenerator`, `__yieldStar`, and `__forAwait` helpers, lowers selected async-generator declarations/expressions/methods to ordinary functions returning `__asyncGenerator`, rewrites `yield*` through `__yieldStar`, rewrites `await` in generator bodies to `yield new __await(...)`, and uses generator-safe disposal with `yield new __await(_promise)`. This is still bounded: arrow async generators, arbitrary nested expression contexts, default-export edge cases, and broad await-expression parsing remain future slices.

Focused update for the parenthesized async-generator expression slice: targeted reads reused upstream `TestLowerAsyncGenerator` declaration/expression helper output and the same snapshot runtime helpers. The native module lowerer now recognizes non-default parenthesized async-generator function expressions in larger statements such as exported constants and call arguments, lowers them through the existing `__asyncGenerator`/`__await`/`__yieldStar` path, preserves surrounding statement prefixes/suffixes, and keeps TypeScript `AsyncGenerator`/`AsyncDisposable` annotations erased. No additional upstream checkout hydration or broad runner was needed.

Focused update for the nested async-generator expression slice: targeted reads again reused upstream `TestLowerAsyncGenerator` and `TestLowerAsyncGeneratorNoAwait` snapshot output for the helper shape, then applied that shape to native PHP parser contexts that previously rejected nested expression positions. The native module lowerer now recognizes async-generator function expressions in array elements and later call arguments, lowers them through the existing `__asyncGenerator`/`__await`/`__yieldStar` path, preserves surrounding statement prefixes/suffixes, and avoids an extra space after `[` in generated arrays. No additional upstream checkout hydration or broad runner was needed.

Focused update for the object-property async-generator expression slice: targeted reads reused the same upstream `TestLowerAsyncGenerator` helper shape and extended the native nested-expression gate to object-literal property values, including computed property names and object-literal callback arguments. The WordPress registry fixture now includes a `previewStreamMap` keyed by `metadata.name`, and that object-property stream lowers through the existing `__asyncGenerator`/`__await`/`__yieldStar` plus `__using`/`__callDispose` path without invoking Node. Multiple async-generator expressions in one statement remained a future slice at this checkpoint.

Focused update for the multiple async-generator expression slice: targeted reads reused upstream `TestLowerAsyncGenerator` and `TestLowerAsyncGeneratorNoAwait` snapshot helper output, then replaced the native single-match statement lowering path with a bounded statement transformer. The native module lowerer now lowers every supported `async function*` expression in the same statement for array elements, repeated call arguments, and object-property values instead of only lowering the first expression and leaving the suffix untouched. The WordPress registry fixture now exercises two same-statement preview stream entries in one array, each with its own disposable asset cleanup. Full unsupported-target async-function lowering remained a future slice at this checkpoint.

Focused update for the async-generator await operand slice: targeted reads reused upstream `TestLowerAsyncGenerator` helper snapshots that lower awaited values through `yield new __await(...)`. No broad checkout hydration or upstream rerun was needed. The native module lowerer now replaces the previous regex-based await rewrite with a bounded operand scanner, so awaited calls with nested comma arguments, parenthesized comma expressions, and `yield await` operands keep the full operand instead of being truncated at inner commas or left as `await(...)`. The WordPress registry fixture now resolves preview assets with multi-argument `queue.openNext(...)` calls and a parenthesized fallback-preparation expression before block registration. Full unsupported-target async-function lowering remained a future slice at this checkpoint.

Focused update for the async-generator await precedence slice: targeted reads reused upstream `TestLowerAsyncGenerator` helper snapshots for the `__await` runtime shape and `internal/js_parser/js_parser_test.go` `TestAwait` expectations for grouped/unary await operands. No broad checkout hydration or upstream rerun was needed. The native await operand scanner now stops before top-level binary, logical, conditional, `in`, and `instanceof` operators while still preserving grouped and unary operands inside `__await(...)`. The WordPress registry fixture now uses awaited logical-OR fallback handle selection so PHP preflight preserves upstream `await queue.resolveHandle(...) || metadata.viewScript` order before block registration. Full unsupported-target async-function lowering remains a future slice.

Focused update for the optional-chain/nullish async-generator await precedence slice: targeted reads reused the same upstream `TestLowerAsyncGenerator` helper shape plus `TestAwait` operand precedence expectations. No broad checkout hydration or upstream rerun was needed. The native await operand scanner now treats `?` explicitly so optional calls/properties such as `await queue.resolveHandle?.(...)` and `await queue.assets?.[name]` stay inside `yield new __await(...)`, while top-level `??` and `||` fallback expressions remain outside the awaited operand. The WordPress registry fixture now resolves a preview handle through `await queue.resolveHandle?.(metadata.viewScript) ?? metadata.viewScript` before opening a disposable asset, preserving fallback order without invoking Node.

Focused update for the async-generator helper-hygiene slice: targeted reads reused upstream `TestLowerAsyncGenerator`/`TestLowerAsyncGeneratorNoAwait` helper output and the helper-name collision boundary already inspected from `TestLowerUsingHoisting`. No broad checkout hydration or upstream rerun was needed. The native module lowerer now treats a statement-start `async function*` declaration as independent of earlier top-level statements, so declarations after imports/constants still lower. Generated async runtime helpers `__await`, `__asyncGenerator`, `__yieldStar`, and `__forAwait` now have focused collision coverage alongside `__knownSymbol` and `__typeError`. The WordPress helper-collision fixture preserves existing `wp.interactivity` helper globals while lowering a later `streamInteractiveAssets()` async generator through renamed generated helpers.

Focused update for the plain auto-accessor slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSAutoAccessors` expectations for `accessor`, `accessor x`, `static accessor x`, computed accessors, private accessors, and initialized accessors. No broad checkout hydration or upstream rerun was needed. The native module lowerer now normalizes these plain auto-accessor forms through the TypeScript class printer even when there are no optional markers or type annotations, while preserving the already-mapped marker/type erasure and malformed accessor diagnostics. The WordPress auto-accessor controller fixture now includes plain static and instance accessors for block controller metadata in addition to typed settings and computed worker asset accessors.

Focused update for the decorated auto-accessor slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` cases for `@x accessor y: any`, `@x static y: any`, `@x accessor #y: any`, and the existing `TestTSAutoAccessors` marker/type erasure boundaries. No broad checkout hydration or upstream rerun was needed. The native module lowerer now ends decorator expressions before decorated class members instead of treating the following member as part of the decorator expression, while preserving dotted decorator expressions such as `@blockTypes.declaration(...)`. Decorated auto-accessors preserve the decorator and runtime `accessor` syntax while erasing TypeScript-only optional markers and type annotations. The WordPress auto-accessor controller fixture now decorates the typed block settings accessor and keeps the decorator in the lowered runtime output.

Focused update for the decorator type-argument slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` expectations for `@x<{}>`, `@x<{}>()`, chained decorator type arguments, and dotted decorator type arguments on class members. No broad checkout hydration or upstream rerun was needed. The native module lowerer now skips TypeScript-only decorator type-argument lists while preserving decorator calls, chained decorators, dotted decorator expressions, and spacing before the decorated member. The WordPress auto-accessor controller fixture now uses a typed `@trackAccessor<BlockConfiguration>()` decorator factory and lowers it to runtime `@trackAccessor()` syntax.

Focused update for the decorator call-chain diagnostic slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` for the malformed `@x<{}>().y<[], () => {}>() z: any` case. No broad checkout hydration or upstream rerun was needed. The native module lowerer now rejects property access after a decorator call expression instead of incorrectly preserving `@x().y()` syntax.

Focused update for the ES2021 static-field target slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSClassSideEffectOrder`, specifically the `expectPrintedAssignSemanticsTargetTS(t, 2021, ...)` case that cannot use a class `static {}` block. No broad checkout hydration or upstream rerun was needed. The native module lowerer now accepts a `targetYear` option and, for `targetYear: 2021` with assign-semantics class fields, emits static public field assignments after the named class while preserving computed key side-effect ordering. The WordPress class-field assign-semantics fixture now has an ES2021 preflight path that emits `CardBlockController.metadata = metadata` instead of a class static block for shared hosts targeting older JavaScript runtimes.

Focused update for the private static field assign-semantics slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSPrivateIdentifiers` and its note that static private field initializers must remain inside the class body at the TypeScript parser/printer boundary because private names cannot be assigned from after-class output. No broad checkout hydration or upstream rerun was needed. The native module lowerer now keeps initialized `static #field` declarations inside assign-semantics classes while still allowing public static fields to move to after-class assignments for `targetYear: 2021`. The new WordPress private-static cache fixture verifies a private `#settings` block configuration remains class-local while public static metadata uses ES2021-compatible after-class assignment.

Focused update for the private accessor/method annotation slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSPrivateIdentifiers` private getter, setter, static private accessor, and private method printer expectations. No broad checkout hydration or upstream rerun was needed. The native module lowerer now recognizes parameter and return annotations inside private accessors/methods as TypeScript syntax, erases those annotations, and preserves required `get #foo`/`set [foo]` spacing. The new WordPress private-accessor controller fixture verifies `BlockConfiguration` annotations are erased from private block-settings getters/setters while registration still uses `this.#blockSettings`.

Focused update for the default-export static field slice: targeted upstream oracle checks with the detached esbuild runner inspected ES2021 assign-semantics output for `export default class Foo { static ... }`, anonymous `export default class { static ... }`, computed static keys, and generated default-name collisions. No broad checkout hydration or upstream rerun was needed. The native module lowerer now rewrites default-exported classes with after-class static assignments to a local class binding plus a trailing `export { ... as default }` clause, gives anonymous default classes a generated collision-safe binding, and preserves computed static key caching before the class.

Focused update for the class-expression static field slice: targeted upstream oracle checks with the detached esbuild runner inspected ES2021 assign-semantics output for `const Foo = class { static ... }`, named class expressions, computed static keys, call-argument class expressions, and derived class expressions with computed keys. No broad checkout hydration or upstream rerun was needed. The native module lowerer now wraps class-expression after-class static assignments in upstream-style comma expressions, caches computed keys before the class temp, and keeps derived class base lookup before computed field-key side effects. The WordPress class-expression controller fixture verifies static block metadata and computed worker asset fields lower without invoking Node.

Focused update for the decorated class-statement target slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` class-decorator placement cases such as `@x class Foo`, `export @x class Foo`, and the malformed double-decorator placement boundary, plus the existing `TestTSClassSideEffectOrder` ES2021 after-class static-field assignment shape. No broad checkout hydration or upstream rerun was needed. The native module lowerer now treats decorated class statements as class statements when the class body needs TypeScript lowering, moves `export @x class` to upstream-style `@x export class`, erases TypeScript-only class decorator type arguments, rejects `@x export @y class`, and keeps ES2021 public static field assignments after the decorated class instead of rewriting the class as an invalid decorated class expression. The WordPress decorated controller fixture verifies `@blockController<BlockConfiguration>(metadata)` becomes `@blockController(metadata)` while typed static `BlockConfiguration` settings move to after-class assignments without invoking Node.

Focused update for the decorated class-expression target slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` class-expression cases such as `_ = @x @y class {}`, `export default (@x class Foo {})`, and decorator type-argument erasure, then combined that with the existing `TestTSClassSideEffectOrder` ES2021 after-class static-field assignment shape. No broad checkout hydration or upstream rerun was needed. The native module lowerer now treats leading decorators as part of class expressions when TypeScript member lowering or ES2021 static-field lowering is needed, preserves class-expression decorator spacing, erases TypeScript-only decorator type arguments, restores semicolons for decorated class-expression statements, and wraps after-class static public assignments around the decorated class expression instead of producing invalid `@decorator (...)` output. The WordPress decorated class-expression controller fixture verifies `@blockController<BlockConfiguration>(metadata)` stays attached to the class temp while typed static settings and computed worker assets move after the class without invoking Node.

Verification update for the decorated class-expression target slice: a lane-only esbuild test invocation passes 4 test files with 181 behavior tests, 1,267 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports the decorated class-expression controller preflight. The required root-harness gate found no exact active root run before this worker started `php tools/run-tests.php`; the final root run passed with 190 test files, 20,660 assertions, and 0 failures.

Focused update for the switch-case `using` diagnostic slice: targeted reads of `internal/js_parser/js_parser_test.go` inspected upstream `TestUsing` switch-case diagnostics for direct `using` and `await using` declarations under `case` and `default` clauses. No broad checkout hydration or upstream rerun was needed. The native module lowerer now rejects those direct switch-clause declarations while allowing block-wrapped cases, and the WordPress preflight example records that a direct disposable preview-asset acquisition under a `case` clause is diagnosed instead of emitted as invalid runtime code.

Verification update for the switch-case `using` diagnostic slice: a lane-only esbuild test invocation passes 4 test files with 183 behavior tests, 1,271 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript switch case using diagnostic: yes`.

Focused update for the switch-case block helper slice: targeted reads of `internal/js_parser/js_parser_test.go` reused the upstream `TestUsing` block-wrapped switch-case expectations for `{ using y = z }` and `{ await using y = z }` under `case` clauses, then mapped the unsupported-target helper-lowering boundary already used by the lane's bounded `TestLowerUsing` block-body slices. No broad checkout hydration or upstream rerun was needed. The native module lowerer now rewrites block-wrapped `using`/`await using` declarations in switch case/default bodies through native `__using`/`__callDispose` cleanup while preserving the existing direct-clause diagnostic.

Verification update for the switch-case block helper slice: a lane-only esbuild test invocation passes 4 test files with 185 behavior tests, 1,300 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript switch using asset helper bytes: 2571` plus `WordPress TypeScript switch case using diagnostic: yes`.

Root harness gate for the switch-case block helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` check found active root run PID 1467716. Owner evidence from `ps -o pid,user,ppid,etime,stat,command -p 1467716` recorded `1467716 claude 1467715 00:22 R php tools/run-tests.php`, so this worker did not start a duplicate root harness and left root verification pending for the supervisor/integrator.

Focused update for the switch-clause `for using` helper slice: a tiny upstream oracle run against `.upstream-cache/esbuild-runner/esbuild` confirmed that `switch (x) { case 0: for (using y of z) body(y) }` and `for (await using q of r)` inside switch clauses lower through per-iteration `__using`/`__callDispose` helper scopes when `--supported:using=false` is set. The native module lowerer now scans switch case/default clauses for top-level `for (using ... of ...)` and `for (await using ... of ...)` statements and rewrites those loops through the same native per-iteration cleanup path already used outside switch clauses. The WordPress switch-mode fixture now includes a bulk asset case that disposes collected mode-specific block assets without invoking Node.

Verification update for the switch-clause `for using` helper slice: a lane-only esbuild test invocation passes 4 test files with 186 behavior tests, 1,320 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript switch using asset helper bytes: 2952` plus `WordPress TypeScript switch case using diagnostic: yes`.

Root harness gate for the switch-clause `for using` helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` check found active root run PID 1516193. Owner evidence from `ps -o pid,user,ppid,etime,stat,command -p 1516193` recorded `1516193 claude 1516192 00:10 R php tools/run-tests.php`, so this worker did not start a duplicate root harness and left root verification pending for the supervisor/integrator.

Latest duplicate-root guard sample after lane verification found active root run PID 1534970. Owner evidence from `ps -o pid,user,ppid,etime,stat,command -p 1534970` recorded `1534970 claude 1501779 00:21 Rs php tools/run-tests.php`; this worker still did not start a duplicate root harness.

Focused update for the decorator-boundary diagnostic slice: targeted reads of `internal/js_parser/js_parser_test.go` inspected upstream decorator parse diagnostics for parameter decorators, computed/optional-chain decorator expressions, and `@new Function() class Foo {}`. Targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` diagnostics for decorators before `enum`, `namespace`, and `function` declarations. No broad checkout hydration or upstream rerun was needed. The native module lowerer now validates decorator positions before printing so invalid decorators are rejected instead of being emitted as non-upstream JavaScript, while existing class-statement, class-expression, and class-member decorators remain supported.

Verification update for the decorator-boundary diagnostic slice: a lane-only esbuild test invocation passes 4 test files with 188 behavior tests, 1,328 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript decorator boundary diagnostic: yes` alongside the existing switch using diagnostics.

Root harness result for this slice: the required duplicate-root guard was clear after a transient focused syncthing test PID exited, so this worker ran `php tools/run-tests.php` once. It exited 1 with 196 test files, 21,502 assertions, and 1 unrelated difftastic failure in `lanes/difftastic/tests/TokenDifferTest.php` (`maps upstream side by side novel spans with ansi colors`), where the side-by-side ANSI renderer colors only `legacy` instead of the expected contiguous `legacy-card` span. No esbuild lane failures were reported.

Focused update for the class-decorator helper slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` unsupported-feature output for `@dec class Foo {}` and decorator type-argument erasure cases. No broad checkout hydration or upstream rerun was needed. The native module lowerer now has an opt-in class-decorator helper path that emits decorator arrays plus `__decoratorStart`, `__decorateElement`, and `__runInitializers` calls for named class statements, erases TypeScript-only decorator type arguments, avoids `_init` source-name collisions, and explicitly rejects member-decorator helper lowering until that upstream slice is ported.

Verification update for the class-decorator helper slice: a lane-only esbuild test invocation passes 4 test files with 192 behavior tests, 1,361 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript legacy decorator helper bytes: 613` plus the existing switch/decorator diagnostics.

Root harness gate for the class-decorator helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` check found active root run PID 2059800. Owner evidence from `ps -o pid,user,ppid,etime,stat,command -p 2059800` recorded `2059800 claude 2059799 00:25 R php tools/run-tests.php`, so this worker did not start a duplicate root harness and left root verification pending for the supervisor/integrator.

Focused update for the default/exported class-decorator helper slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` export/default decorator placement and unsupported-feature helper output. A bounded upstream CLI oracle run in `.upstream-cache/esbuild-runner` with local `go run ./cmd/esbuild --loader=ts --target=es2021 --supported:decorators=false` confirmed the helper shape for `@dec export class Foo {}`, `export @dec class Foo {}`, named default decorated classes, and anonymous `export default @dec class {}` forms. The native module lowerer now maps exported class decorators and anonymous default decorated classes through the existing `__decoratorStart`/`__decorateElement`/`__runInitializers` path; anonymous defaults get a generated default binding, `_class_decorators`, and an empty decorator context name, while member-decorator helper lowering remains explicitly future work.

Verification update for the default/exported class-decorator helper slice: a lane-only esbuild test invocation passes 4 test files with 193 behavior tests, 1,373 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript default legacy decorator helper bytes: 577`, `WordPress TypeScript legacy decorator helper bytes: 613`, `WordPress TypeScript namespace async using bytes: 633`, `WordPress TypeScript switch case using diagnostic: yes`, and `WordPress TypeScript decorator boundary diagnostic: yes`.

Root harness result for this slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` duplicate-root guard was clear, so this worker ran `php tools/run-tests.php`. It exited 0 with 198 test files, 21,891 assertions, and 0 failures.

Focused update for the instance-method decorator helper slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` unsupported-feature output for `class Foo { @dec x() {} }`, and targeted reads of `internal/bundler_tests/snapshots/snapshots_lower.txt` cross-checked the method decorator helper shape against the lower snapshots. No broad checkout hydration or upstream rerun was needed. The native module lowerer now maps simple named instance method decorators behind the opt-in decorator helper flag: decorator arrays are emitted before the class, newline-separated decorators remain attached to the following method, TypeScript-only decorator type arguments and method return annotations are erased, a constructor instance-initializer call is inserted when no constructor is present, `__decorateElement(_init, 1, "method", ...)` and `__decoratorMetadata` are emitted after the class, and generated `_init` names avoid source collisions. Fields, accessors, static members, derived classes, mixed class/member decorators, and existing-constructor insertion remain intentionally future slices.

Verification update for the instance-method decorator helper slice: a lane-only esbuild test invocation passes 4 test files with 195 behavior tests, 1,394 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript method legacy decorator helper bytes: 609` alongside the existing legacy/default decorator helper, namespace async using, switch using diagnostic, and decorator boundary diagnostic checks.

Root harness result for the instance-method decorator helper slice: the required duplicate-root guard was clear before the first aggregate run. That run exited 1 with 198 test files, 22,146 assertions, and 1 failure, but the captured output truncated before the failing test name. A second duplicate-root guard check was clear; the rerun waited on `.upstream-cache/run-tests.lock` and then exited 0 with 198 test files, 22,151 assertions, and 0 failures, so the latest aggregate evidence is green.

Focused update for the static-method decorator helper slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` unsupported-feature output for `class Foo { @dec static x() {} }`, and targeted reads of `internal/bundler_tests/snapshots/snapshots_lower.txt` cross-checked the static method decorator helper shape against the lower snapshots. No broad checkout hydration or upstream rerun was needed. The native module lowerer now maps simple named static method decorators behind the opt-in decorator helper flag: decorator arrays are emitted before the class, TypeScript-only decorator type arguments and method return annotations are erased, existing constructors remain untouched for static-only decorator classes, `__decorateElement(_init, 9, "method", ...)`, `__decoratorMetadata`, and `__runInitializers(_init, 3, ClassName)` are emitted after the class, and generated `_init` names avoid source collisions. Fields, accessors, static field/accessor decorators, derived classes, mixed class/member decorators, and instance-member classes with existing constructors remain intentionally future slices.

Verification update for the static-method decorator helper slice: a lane-only esbuild test invocation passes 4 test files with 197 behavior tests, 1,410 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript static method legacy decorator helper bytes: 637` alongside the existing legacy/default/instance-method decorator helper, namespace async using, switch using diagnostic, and decorator boundary diagnostic checks.

Root harness result for the static-method decorator helper slice: the required duplicate-root guard was clear before the aggregate run, so this worker ran `php tools/run-tests.php`. It exited 0 with 198 test files, 22,371 assertions, and 0 failures.

Focused update for the public-field decorator helper slice: targeted reads of `internal/js_parser/js_parser_test.go` and `internal/js_parser/ts_parser_test.go` inspected upstream `TestDecorators`/`TestTSDecorators` unsupported-feature output for `class Foo { @dec x }`, `class Foo { @dec x = y }`, and `class Foo { @dec static x = y }`. Targeted local upstream CLI oracle checks with `.upstream-cache/esbuild-runner/esbuild --loader=ts --target=es2015` confirmed initializer placement for decorated public fields without rerunning the full upstream suite. The native module lowerer now maps simple public identifier field decorators behind the opt-in decorator helper flag: instance fields are omitted from the class body and initialized in an inserted constructor through `__publicField(this, "x", __runInitializers(_init, 8, this[, value])), __runInitializers(_init, 11, this)`, while static fields emit `__decorateElement(_init, 13, ...)`, `__decoratorMetadata`, and after-class `__publicField(ClassName, "x", __runInitializers(_init, 8, ClassName[, value])), __runInitializers(_init, 11, ClassName)`. Accessors, private fields, computed fields, derived classes, mixed class/member decorators, and instance-member classes with existing constructors remain intentionally future slices.

Verification update for the public-field decorator helper slice: a lane-only esbuild test invocation passes 4 test files with 199 behavior tests, 1,424 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript static field legacy decorator helper bytes: 797` alongside the existing legacy/default/instance-method/static-method decorator helper, namespace async using, switch using diagnostic, and decorator boundary diagnostic checks.

Root harness gate for the public-field decorator helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` check found active root run PID 2536973. Owner evidence from `ps -o pid,user,ppid,etime,stat,command -p 2536973` recorded `2536973 claude 2509052 00:15 Rs php tools/run-tests.php`, so this worker did not start a duplicate root harness and left root verification pending for the supervisor/integrator.

Focused update for the auto-accessor decorator helper slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` unsupported-feature output for `class Foo { @dec accessor x }` and `class Foo { @dec static accessor x }`. Targeted reads of `internal/bundler_tests/bundler_lower_test.go` and `internal/bundler_tests/snapshots/snapshots_lower.txt` inspected the bundled issue-3768 accessor decorator output, including WeakMap storage, `__decorateElement(..., 4|12, ..., storage)`, and `__privateAdd(... __runInitializers(_init, 8|11, ...))` initialization order. No broad checkout hydration or full upstream rerun was needed. The native module lowerer now maps simple public identifier auto-accessor decorators behind the opt-in decorator helper flag for instance and static accessors, while private accessors, mixed class/member decorators, and instance-member classes with existing constructors remained future slices at that checkpoint.

Verification update for the auto-accessor decorator helper slice: a lane-only esbuild test invocation passes 4 test files with 201 behavior tests, 1,440 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript accessor legacy decorator helper bytes: 750` alongside the existing legacy/default/instance-method/static-method/static-field decorator helper, namespace async using, switch using diagnostic, and decorator boundary diagnostic checks.

Root harness result for the auto-accessor decorator helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` check found no active root harness before this worker started `php tools/run-tests.php`. The aggregate root harness exited 1 with 200 test files, 22,711 assertions, and 13 failures, so this esbuild lane batch remains uncommitted under the project's passing-root requirement.

Focused update for the existing-constructor decorator helper slice: targeted reads of `internal/js_parser/ts_parser_test.go` reused upstream `TestTSDecorators` unsupported-feature output for instance method, public field, and auto-accessor decorator helpers. Small local upstream CLI oracle checks with `.upstream-cache/esbuild-runner/esbuild --loader=ts --supported:decorators=false` confirmed the existing-constructor insertion order for `constructor(value: any) { ... }`, public field initializers, auto-accessor WeakMap initializers, and TypeScript constructor-parameter-property ordering. No broad checkout hydration or upstream suite rerun was needed. The native module lowerer now injects opt-in decorator helper instance initializers into existing non-derived constructors instead of rejecting them. Ordinary user constructor statements run after method/field/accessor decorator initializers, while generated TypeScript constructor-parameter-property assignments remain before method decorator initializers. The WordPress constructor-backed method decorator fixture covers a registration class that stores `wp.blocks` in its constructor and decorates `register()` for legacy targets.

Verification update for the existing-constructor decorator helper slice: a lane-only esbuild test invocation passes 4 test files with 202 behavior tests, 1,453 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript method constructor decorator helper bytes: 689` alongside the existing decorator, using, async-generator, and namespace preflight metrics.

Root harness guard for the existing-constructor decorator helper slice: the required duplicate-root check first found active root harness PID `2631258` owned by `claude`, and a later final guard found active root harness PID `2650409` owned by `claude` (`php tools/run-tests.php`, PPID `2618963`, elapsed `20s` at sampling), so this worker did not start another aggregate root run. Root aggregate verification remains pending for the supervisor/integrator.

Focused update for the derived member decorator helper slice: targeted local upstream CLI oracle checks with `.upstream-cache/esbuild-runner/esbuild --loader=ts --supported:decorators=false` inspected derived class member decorator output for instance methods, existing constructors, conditional `super(...)` paths, public fields, and public auto-accessors. No broad checkout hydration or upstream suite rerun was needed. The native module lowerer now maps simple derived member decorators by capturing the base class in the `extends` expression, initializing decorator arrays in that expression, calling `__decoratorStart(_base)`, inserting synthetic derived constructors when needed, and injecting instance method/field/accessor initializers immediately after direct `super(...)` calls or through the existing `__super` helper for conditional/multiple `super(...)` paths. The WordPress derived registration fixture covers a block controller subclass that preserves constructor-injected `wp.blocks` state while lowering a decorated `register()` method for legacy targets.

Verification update for the derived member decorator helper slice: a lane-only esbuild test invocation passes 4 test files with 204 behavior tests, 1,481 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript derived method decorator helper bytes: 817` alongside the existing decorator, using, async-generator, and namespace preflight metrics.

Root harness result for the derived member decorator helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` duplicate-root guard was clear before the aggregate run, so this worker ran `php tools/run-tests.php`. The first aggregate run exited 1 with 202 test files, 23,277 assertions, and 2 failures, but the very large streamed output did not retain the failing test names. A second duplicate-root guard check was clear, so this worker ran a filtered aggregate rerun (`php tools/run-tests.php 2>&1 | grep -E '^(FAIL|[0-9]+ test files)'`); it exited 0 and reported `202 test files, 23287 assertions, 0 failures`. The latest aggregate evidence is green.

Focused update for the computed field/accessor decorator helper slice: targeted local upstream CLI oracle checks with `.upstream-cache/esbuild-runner/esbuild --loader=ts --supported:decorators=false` inspected unsupported-target decorator output for `class Foo { @dec [x] = y }`, `class Foo { @dec static [x] = y }`, `class Foo { @dec accessor [x] = y }`, and the corresponding derived-class computed-key shapes. No broad checkout hydration or upstream suite rerun was needed. The native module lowerer now maps simple public computed field and auto-accessor member decorators by caching the computed key in a generated temp, initializing the decorator array during that key evaluation, passing the temp to `__decorateElement`, and routing instance/static field or accessor initialization through `__publicField`/`__privateAdd` plus `__runInitializers` in upstream order. A WordPress computed-field decorator fixture covers a block registration class whose decorated computed settings key is lowered for legacy targets without invoking Node.

Verification update for the computed field/accessor decorator helper slice: a lane-only esbuild test invocation passes 4 test files with 206 behavior tests, 1,492 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript computed field legacy decorator helper bytes: 786` alongside the existing decorator, using, async-generator, and namespace preflight metrics.

Root harness gate for the computed field/accessor decorator helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` duplicate-root check found active root run PID `2719365`. Owner evidence from `ps -o pid,user,ppid,etime,stat,command -p 2719365` recorded `2719365 claude 2719364 00:15 R php tools/run-tests.php`, so this worker did not start a duplicate aggregate root harness and left root verification pending for the supervisor/integrator.

Focused update for the computed method decorator helper slice: targeted local upstream CLI oracle checks with `.upstream-cache/esbuild-runner/esbuild --loader=ts --supported:decorators=false` inspected unsupported-target decorator output for `class Foo { @dec [x](): any {} }`, `class Foo { @dec static [x](): any {} }`, and `class Foo extends Bar { @dec [x](): any {} }`. No broad checkout hydration or upstream suite rerun was needed. The native module lowerer now maps simple public computed method decorators by emitting the decorator array assignment inside the computed method key, passing the generated key temp to `__decorateElement(..., 1|9, key, ...)`, inserting instance initializers when needed, running static initializers for static methods, and capturing derived class bases before `__decoratorStart(base)`. A WordPress computed-method decorator fixture covers a block registration class whose decorated computed `methodKey("register")` method is lowered for legacy targets without invoking Node.

Verification update for the computed method decorator helper slice: `php -l lanes/esbuild/src/TypeScriptModuleLowerer.php` passes. A lane-only esbuild test invocation passes 4 test files with 208 behavior tests, 1,506 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript computed method legacy decorator helper bytes: 716` alongside the existing decorator, using, async-generator, and namespace preflight metrics.

Root harness gate for the computed method decorator helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` duplicate-root check found active root run PID `2834808`. Owner evidence from `ps -o pid,user,ppid,etime,stat,command -p 2834808` recorded `2834808 claude 2834807 00:06 R php tools/run-tests.php`, so this worker did not start a duplicate aggregate root harness and left root verification pending for the supervisor/integrator.

Focused update for the private field decorator helper slice: targeted local upstream CLI oracle checks with `.upstream-cache/esbuild-runner/esbuild --loader=ts --supported:decorators=false` inspected unsupported-target decorator output for `class Foo { @dec #x = y }`, `class Foo { @dec static #x = y }`, and the derived-class private-field shape. No broad checkout hydration or upstream suite rerun was needed. The native module lowerer now maps simple private field decorators by allocating WeakMap storage, calling `__decorateElement(..., 21|29, "#x", decorators, storage)`, initializing instance/static private storage through `__privateAdd(... __runInitializers(_init, 8|11, ...))`, and preserving derived-class decorator array initialization before `__decoratorStart(base)`. A WordPress private-field decorator fixture covers a block registration class whose hidden decorated `#settings` metadata field is lowered for legacy decorator targets without invoking Node.

Verification update for the private field decorator helper slice: `php -l lanes/esbuild/src/TypeScriptModuleLowerer.php`, `php -l lanes/esbuild/tests/TypeScriptModuleLowererTest.php`, and `php -l lanes/esbuild/examples/wordpress-asset-preflight.php` pass. A lane-only esbuild test invocation passes 4 test files with 210 behavior tests, 1,523 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript private field legacy decorator helper bytes: 736` alongside existing decorator/using/async-generator/namespace preflight metrics.

Root harness result for the private field decorator helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` duplicate-root guard was clear before the aggregate run, so this worker ran `php tools/run-tests.php`. It exited 0 with 206 test files, 23,904 assertions, and 0 failures.

Focused update for the private auto-accessor decorator helper slice: targeted reads of `internal/js_parser/ts_parser_test.go` inspected upstream `TestTSDecorators` coverage for `class Foo { @x accessor #y: any }`, and targeted local upstream CLI oracle checks with `.upstream-cache/esbuild-runner/esbuild --loader=ts --supported:decorators=false` inspected unsupported-target helper output for `class Foo { @dec accessor #x = y }`, `class Foo { @dec static accessor #x = y }`, and a derived `class Foo extends Bar { @dec accessor #x = y }` shape. No broad checkout hydration or upstream suite rerun was needed. The native module lowerer now maps simple private auto-accessor decorators behind the opt-in decorator helper flag by allocating private WeakMap storage plus a WeakSet brand, calling `__decorateElement(..., 20|28, "#x", decorators, brand, storage)`, capturing generated `get`/`set` functions from the decorator result, initializing instance/static brand plus storage in upstream order, and preserving derived-class decorator array initialization before `__decoratorStart(base)`. A WordPress private-accessor decorator fixture covers a block registration class whose decorated private `accessor #settings: BlockConfiguration = metadata` metadata accessor is lowered for legacy decorator targets without invoking Node.

Verification update for the private auto-accessor decorator helper slice: `php -l lanes/esbuild/src/TypeScriptModuleLowerer.php`, `php -l lanes/esbuild/tests/TypeScriptModuleLowererTest.php`, and `php -l lanes/esbuild/examples/wordpress-asset-preflight.php` pass. A lane-only esbuild test invocation passes 4 test files with 212 behavior tests, 1,541 assertions, and 0 failures. `php lanes/esbuild/examples/wordpress-asset-preflight.php` passes and reports `WordPress TypeScript private accessor legacy decorator helper bytes: 958` alongside existing decorator/using/async-generator/namespace preflight metrics.

Root harness result for the private auto-accessor decorator helper slice: the required `pgrep -af '^php tools/run-tests\.php( |$)'` duplicate-root guard was clear before the aggregate run, so this worker ran `php tools/run-tests.php`. It exited 0 with 209 test files, 24,115 assertions, and 0 failures.

## Current Native Mapping

- `TestComment`: unterminated block comments now raise a native PHP lexer error.
- `TestHashbang`: file-start hashbangs produce a `hashbang` token.
- `TestNumericLiteral`: binary, octal, hexadecimal-with-separators, decimal-leading-dot, exponent, and trailing-dot number literals produce `number` tokens with numeric values.
- `TestImport`/export forms: selected static imports, dynamic direct-string imports, named/default/namespace imports, and named/star/default exports are analyzed.
- `TestImportAssertions`: selected static import/export `assert { type: "json" }` clauses are preserved as module metadata.
- `TestImportAttributes`: selected static import/export `with { ... }` clauses and direct-string dynamic import options are preserved, with duplicate keys and non-string values rejected.
- `TestES6Syntax`/`TestImportMeta*`: `import.meta` is tracked as an ESM marker, and `import.meta.url`/`import.meta.path` property reads are preserved as native metadata.
- `TestNew`/`TestCall`: `new URL("path", import.meta.url)` references are mapped for plain asset references, `new Worker(new URL(...))`, and `import(new URL(...))`.
- Dynamic imports with expression arguments are accepted without being counted as direct string import records, while malformed `import()` and `import(...a)` calls are rejected.
- `TestTSImportEquals`/`TestTSExportEquals`: `import x = require("...")`, qualified `import x = foo.bar`, and `export = value` are classified as TypeScript/CommonJS forms and do not mark a file as ESM by themselves.
- `TestTSTypeOnlyImport`: top-level `import type`, mixed `import { value, type Type }`, runtime `import type from "bar"` ambiguity, and malformed combined type default imports are distinguished.
- `TestTSTypeOnlyExport`: `export type { ... } from`, mixed `export { value, type Type }`, type-only star exports, and invalid local `export type { default }` are distinguished.
- `TestTSNamespaceExports`: namespace declarations, `export namespace`, exported namespace members, declared namespace exports, nested namespace names, and namespace-local import-equals aliases are recorded separately from top-level module exports.
- `TestTSImport`: selected TypeScript import pruning behavior is mapped as analyzer metadata: unused value imports are omitted, live default/named/namespace specifiers are retained, and imports referenced only from dead `if (false)` control flow are downgraded to side-effect-only imports.
- `TestTSImportEquals`: selected fixed-point pruning behavior is mapped: unused qualified-reference aliases are removed, `require(...)` aliases are retained for side effects, and chains such as `a -> b -> c` are retained when a later live use depends on them.
- `TestTSImportEqualsInNamespace`: selected printer behavior is mapped by a native namespace lowerer for namespace-local import-equals elision, runtime `const` aliases, exported namespace property assignments, exported alias reference rewriting, and invalid nested/import-form rejection.
- `TestTSNamespace`/`TestTSNamespaceExports`: selected printer behavior is mapped by the native namespace lowerer for exported `var`/`let`/`const` namespace property assignments, initialized multi-declarator exports, later exported value reference rewriting, and namespace parameter renaming when an exported value shares the namespace name.
- `TestTSNamespaceExports`: selected printer behavior is mapped by the native namespace lowerer for exported `function` and `class` declarations inside namespaces, including local declaration emission, namespace property assignment, and namespace parameter renaming when the export shares the namespace name.
- `TestTSNamespaceExports`: selected nested namespace printer behavior is mapped for exported nested namespace property initializers, non-exported local nested namespaces, exported namespace functions, namespace-local runtime enums, and simple `export declare const` variable reference rewrites.
- `TestTSNamespace`: dot nested namespace syntax is mapped for `namespace foo.bar { ... }`, `module foo.bar { ... }`, and namespace/module alias combinations, emitting nested namespace IIFEs with `bar = foo.bar || (foo.bar = {})` initialization. Analyzer metadata now records the parent namespace plus exported child namespace instead of flattening the qualified name.
- `TestTSNamespaceExports`: a bounded declared binding-pattern case is mapped for `export declare let [[L2 = x, { [y]: L3 }]]`, recording `L2` and `L3` as declared namespace members and rewriting later value references without treating default-initializer or computed-key identifiers as exports.
- `TestTSNamespaceDestructuring`: a bounded exported runtime binding-pattern case is mapped for `export var [a, [, b = c, ...d], { [x]: [[y]] = z, ...o }] = ref`, lowering binding identifiers to namespace-object assignment targets while leaving computed property names and default initializer expressions alone.
- `TestTSNamespace`: a bounded namespace/value merge slice is mapped for runtime namespaces that merge with preceding function/class/enum declarations, repeated runtime namespace blocks, later enum declarations that provide the hoisted `var`, and `var`/`let`/`const` plus function-after-namespace collision errors.
- `TestTSExportEquals`: selected top-level printer behavior is mapped by a native module lowerer that lowers `export = value` to `module.exports = value` and appends the assignment after ordinary statements.
- `TestTSImportEquals`: selected top-level printer behavior is mapped by the module lowerer for `import x = require("...")`, `import x = foo.bar`, and `export import x = ...` to runtime `const`/`export const` declarations, with malformed targets rejected.
- `TestTSTypes`: selected printer behavior is mapped by the module lowerer for variable annotations, function parameter/return annotations, arrow callback annotations, `as` casts, `satisfies` checks, and type-only imports.
- `TestTSUsing`: selected printer/error behavior is mapped for top-level explicit resource management declarations. Type annotations are erased from `using x: any = y`, multi-declarator `using x: any = y, z: any = _`, comma-continued multiline `using x: any = y,\n z: any = _`, and `await using x: any = y,\n z: any = _`; `using \n x = y` keeps the upstream ASI split, missing initializers are rejected, and `export using`/`export await using` are rejected at top level and inside namespaces.
- `TestUsing`: direct `using` and `await using` declarations under `case` or `default` clauses are rejected unless wrapped in a block, matching the upstream explicit-resource-management parser boundary.
- `TestUsing`: the upstream function has 85 direct `expect*` checks, including 15 focused nullish-initializer optimization checks. The native lowerer now maps the selected `undefined` to `void 0` print boundary, minify-syntax conversion of all-nullish `using` declarations to ordinary declarations, mixed nullish/non-nullish preservation as `using`, a bounded unsupported-target helper path that wraps top-level `using` declarations in native `__using`/`__callDispose` cleanup code, a bounded `TestLowerUsing` helper-lowering slice for function-body and block-scoped `using`/`await using` declarations inside simple functions and `if`/block bodies plus ordinary `while`, `for`, `for await`, block-wrapped switch case/default bodies, and top-level `for (using ... of ...)`/`for (await using ... of ...)` statements inside switch clauses, simple `for`/`for await` loop helper lowering with per-iteration disposal stacks, selected function-scope printer semantics for function declarations, function expressions, async arrow bodies, namespace function bodies, and non-async `await using` rejection, selected for-loop printer/error semantics for `using`/`await using` declarations in `for`/`for await` headers, bounded `TestLowerUsingHoisting` top-level module-boundary/local-export/function-hoist/default-export/local-class slices that keep directive strings, ES import declarations, export-star/export-from declarations, trailing export clauses, simple and destructured local export clauses, top-level plain/exported/default function declarations, exported/default/local class self-references, ordinary local class declarations, and default class/expression export clauses outside the generated `try`/`catch` helper scope while avoiding helper-name collisions including for-loop acquisition call sites, and a bounded `TestLowerUsingInsideTSNamespace` slice that wraps namespace-local `using` declarations in an IIFE-local cleanup scope with helper-name collision avoidance.
- `TestLowerAsyncGenerator`/`TestAwait`: bounded class-method and object-literal method slices still map `async *method() { await using ... }` by erasing TypeScript method/resource annotations when preserving modern syntax, and an opt-in unsupported-target runtime path now maps selected `async function*` declarations/function expressions/default exports/parenthesized expressions/nested array elements/later call arguments/object-property values/class methods/object methods to ordinary functions returning `__asyncGenerator`, including multiple supported async-generator expressions in one array, call, or object-literal statement and async-generator declarations that follow earlier top-level statements. It routes `yield*` through `__yieldStar`, rewrites `await` through `yield new __await(...)` for simple operands, calls with nested comma arguments, parenthesized comma expressions, grouped arithmetic, unary operands, optional-chain property/call/index operands, and optional-chain operands before nullish/logical fallbacks, stops before top-level binary/logical/conditional operators, lowers simple `for await` loops through `__forAwait`, renames generated async helpers when source identifiers collide, and keeps async disposal inside generator bodies using `yield new __await(_promise)`. Non-async class/object generator methods with `await using` are rejected.
- `TestTSEnum`: selected printer behavior is mapped by the module lowerer for normal and exported runtime enum objects, including numeric auto-increment, string literal members, `void 0` after non-numeric members, upstream-style missing separator errors, split enum block emission, enum member constant aliases, additive member-reference folding, use-before-declaration direct member substitution, and same-file dot/bracket enum member inlining while leaving optional enum access un-inlined. The focused upstream `TestTSEnum` function has 29 print/error expectations; this slice maps the inspected split enum, shadowed enum member alias, current-enum member arithmetic, and cross-enum member reference cases without claiming the broader `TestTSEnumConstantFolding` operator matrix.
- Same-file non-exported `const enum` behavior is mapped for a bounded slice: the declaration is erased and direct dot/bracket member accesses are substituted with numeric comments.
- `TestTSDeclare`: selected ambient declaration behavior is mapped by the module lowerer for `declare var`/`let`/`const`/`function`/`class`/`enum`/`namespace`/`module`/`global` erasure, `declare` line-break ASI boundaries, `export as namespace` erasure, and malformed dotted or unterminated `export as namespace` rejection.
- `TestTSDeclare`/`TestTSExperimentalDecorator`: selected ambient class boundaries are mapped for `declare abstract class`, `export declare abstract class`, decorated ambient class declarations, decorators inside erased ambient class bodies, and private/accessor tokens in erased ambient declarations.
- `TestTSDeclare`: selected class member declare behavior is mapped for plain, public, override, static, static-declare, accessor, and initialized declared fields. The PHP lowerer erases those members while preserving later runtime class members, and rejects the upstream invalid private identifier, index signature, method, getter, setter, and decorated-declare-field boundaries.
- `TestTSClass`: selected abstract class behavior is mapped for erased `abstract` class headers, erased abstract method signatures, ordinary class method return-type erasure, and ASI-sensitive `abstract` line breaks that remain runtime statements.
- `TestTSClass`/`TestTSSuperCall`: selected constructor parameter property behavior is mapped for public/protected/private/readonly/override parameter properties, lowering them to ordinary parameters, `this.<name> = <name>` assignments, and matching class field declarations, with destructured parameter properties and missing access-modifier names rejected. The assign-semantics subset now omits the extra field declarations under `useDefineForClassFields=false`, inserts simple derived-constructor parameter-property assignments after a direct `super(...)` call, wraps multiple or conditional derived `super(...)` paths in an upstream-style `__super` helper, normalizes one-line derived constructors before injecting assign-semantics class-field assignments after `super(...)`, wraps initialized-field logical-assignment `super(...)` calls such as `y ||= super(1)`, preserves upstream's dead `if (false) __super(...)` branch shape while assigning after the live direct `super(...)` call, splits top-level comma-expression statements, return/throw comma-expression statements, `if`/`switch` tests, and `for` initializers around one direct `super(...)` call so pre-super side effects, injected assignments, and post-super side effects retain upstream order without using the helper path, and maps the initialized private-field cases where `#x = value` becomes a constructor assignment after `super(...)` while retaining the `#x;` declaration after the constructor when injection is needed.
- `TestTSClass`: selected method type-parameter behavior is mapped for identifier and computed method names, including optional method markers and parameter/return type erasure. Definite-assignment markers on methods such as `foo!()`, `*foo!()`, `get foo!()`, `set foo!(x)`, `async foo!()`, and `foo!<T>()` are rejected.
- `TestTSPrivateIdentifiers`: selected private getter, setter, static private accessor, computed accessor, and private method printer behavior is mapped so TypeScript parameter/return annotations are erased while required private-name spacing is preserved.
- `TestTSClass`: a bounded assign-semantics class-field mode is mapped behind `useDefineForClassFields=false`. It erases uninitialized public typed fields and index signatures, moves initialized optional/definite/string/computed public fields into constructor `this.*` assignments, lowers bounded static typed field initializers into static blocks or ES2021 after-class public assignments, and keeps initialized static private fields inside the class to avoid invalid after-class private-name writes.
- `TestTSClass`: a focused computed-key extension is mapped behind `useDefineForClassFields=false`. Public computed instance/static field keys are evaluated once into class-scope temporaries before constructor/static assignment emission, and uninitialized computed fields preserve key side effects even though the field declaration is erased. This maps the simple computed field expectations from `TestTSClass`.
- `TestTSSuperCall`: a focused derived-class computed-key ordering slice is mapped behind `useDefineForClassFields=false`. When computed assign-semantics fields need class-scope key evaluation, the lowerer rewrites `extends Base` into `extends (_base = Base, _key = key(), _base)` so base lookup side effects run before computed field-key side effects and field assignments still land after `super(...)`.
- `TestTSClassSideEffectOrder`/`TestTSPrivateIdentifiers`: a bounded assign-semantics side-effect-order slice is mapped for computed methods interleaved with erased/initialized computed fields. Pending erased field key expressions are folded into the next computed method key, initialized field keys are cached into temporaries before constructor/static assignments use them, trailing erased field key side effects are folded back into the previous computed method key while preserving that method name, static assignment blocks stay before later class members for modern targets, `targetYear: 2021` emits public static field assignments after named/default-exported classes and bounded class expressions instead of using a static block, anonymous default classes get a generated default binding before the trailing default export clause, and initialized `static #private` fields remain inside the class.
- `TestTSAutoAccessors`/`TestTSDecorators`: selected printer behavior is mapped for TypeScript auto-accessors and decorated class members. Plain `accessor`, identifier, static identifier, computed, private, and initialized accessor members normalize through the class printer; optional and definite assignment markers plus type annotations are erased for identifier, computed, and private accessor names; `readonly accessor` preserves runtime accessor syntax; decorated auto-accessors such as `@x accessor y: any`, `@x static accessor y?: any`, and `@x accessor #y: any` preserve decorators while erasing TypeScript-only syntax; decorator type arguments such as `@x<{}>`, `@x<{}>()`, `@x<{}> @y<[], () => {}>`, and `@x<{}>.y<[], () => {}>` are erased while preserving runtime decorator syntax; the malformed call-chain decorator `@x<{}>().y<[], () => {}>()` is rejected; malformed `accessor x<T>`, `accessor x<T>()`, `accessor declare x`, and `accessor readonly x` forms are rejected; and auto-accessor members inside type literals are erased from variable declarations.
- `TestTSDecorators`: selected class-statement and class-expression decorator placement behavior is mapped for decorated classes that also need TypeScript class lowering. Decorators before class statements are preserved, `export @x class Foo` is normalized to `@x export class Foo`, TypeScript-only decorator type arguments are erased, `@x export @y class Foo` is rejected, decorated class expressions keep decorators attached through ES2021 after-class static field assignment lowering, and decorated class-expression statements regain trailing semicolons.
- `TestTSDecorators`: bounded unsupported-target decorator helper slices are mapped behind an opt-in flag. Named class statements such as `@dec class Foo {}` lower to an upstream-style decorator array, `__decoratorStart(null)`, `__decorateElement(..., 0, "Foo", ...)`, and `__runInitializers(..., 1, Foo)`, with generated `_init` names avoiding source collisions. Simple named instance method decorators such as `class Foo { @dec x(): any {} }` erase TypeScript-only syntax, insert or reuse an instance initializer constructor, and emit `__decorateElement(..., 1, "x", ...)` plus `__decoratorMetadata`. Simple named static method decorators such as `class Foo { @dec static x(): any {} }` erase TypeScript-only syntax, keep existing constructors untouched for static-only decorator classes, and emit `__decorateElement(..., 9, "x", ...)`, `__decoratorMetadata`, and `__runInitializers(..., 3, Foo)`. Simple public identifier field decorators such as `class Foo { @dec x: any = y }` and `class Foo { @dec static x: any = y }` erase TypeScript-only syntax, emit `__decorateElement(..., 5|13, "x", ...)`, and route instance/static field initializers through `__publicField` plus `__runInitializers(..., 8|11, ...)` in the upstream order. Simple private field decorators such as `class Foo { @dec #x: any = y }` and `class Foo { @dec static #x: any = y }` allocate WeakMap storage, emit `__decorateElement(..., 21|29, "#x", decorators, storage)`, and route private storage initialization through `__privateAdd` plus `__runInitializers(..., 8|11, ...)`. Simple private auto-accessor decorators such as `class Foo { @dec accessor #x: any = y }` and `class Foo { @dec static accessor #x: any = y }` allocate private WeakMap storage plus a WeakSet brand, emit `__decorateElement(..., 20|28, "#x", decorators, brand, storage)`, capture generated get/set functions, and initialize instance/static brand plus storage in upstream order. Simple public identifier auto-accessor decorators such as `class Foo { @dec accessor x: any = y }` and `class Foo { @dec static accessor x: any = y }` allocate WeakMap storage, emit `__decorateElement(..., 4|12, "x", ..., storage)`, and route initialization through `__privateAdd` plus `__runInitializers(..., 8|11, ...)`. Simple public computed field/accessor decorators cache the computed key in a temp, initialize decorator arrays during key evaluation, pass that temp to `__decorateElement`, and use it for field/accessor initialization. Simple public computed method decorators initialize decorator arrays inside the computed method key, pass the generated key temp to `__decorateElement(..., 1|9, key, ...)`, and capture derived class bases before `__decoratorStart(base)`. Existing non-derived constructors now receive instance method/field/accessor decorator initializers in upstream order, including the TypeScript constructor-parameter-property ordering boundary. Simple derived class member decorators now capture the base class in the `extends` expression, initialize decorator arrays there, call `__decoratorStart(_base)`, insert synthetic derived constructors when needed, and inject instance initializers after direct `super(...)` calls or through the existing `__super` helper for conditional/multiple `super(...)` paths. Private getter/setter decorators, computed getter/setter decorators, mixed class/member decorators, private-accessor member-use rewriting, and broader derived constructor/super interactions remain future slices.
- `TestTSDecorators`: selected parser diagnostic boundaries are mapped for invalid decorator positions and syntax. Parameter decorators are rejected, decorators before `enum`/`namespace`/`function` declarations are rejected, and invalid decorator expressions such as computed property access, optional chaining, and `new`-started decorator expressions are rejected at the native PHP boundary instead of being emitted.
- WordPress fixtures: a block view script using `@wordpress/dom-ready`, a `block.json` import with `with { type: "json" }`, and relative stylesheet/worker `new URL(..., import.meta.url)` references tokenizes and analyzes without Node/npm. A TypeScript block fixture additionally separates pruned runtime `@wordpress/dom-ready`, `./block.json`, and legacy `wp.blocks` dependencies from erased `@wordpress/blocks`/`@wordpress/element` type imports plus namespace-backed block registration metadata. WordPress namespace-lowering scenarios map `export import blocks = wp.blocks`, exported namespace block settings, exported registration functions/classes, nested exported namespace enum settings, a dot-qualified `PortLibs.CardBlock` runtime namespace, destructured block settings, a function/namespace merged `registerBlock.settings` block runtime, namespace-scoped disposable preview asset cleanup, namespace async disposable preview registration, and namespace helper-name collision avoidance to namespace property assignments/IIFEs with later reference rewrites and no duplicate merged-symbol `var`, a CommonJS-style WordPress block runtime maps `export = registerBlock` to `module.exports = registerBlock`, a typed block edit callback fixture erases `BlockEditProps`, `WPElement`, and `satisfies BlockConfiguration` syntax, a runtime enum block configuration fixture lowers a `DisplayMode` enum, an enum alias configuration fixture folds `DisplayMode.Default`/`DisplayMode.Wide` member references, a const enum block configuration fixture erases non-exported display-mode enum declarations, an ambient declarations fixture erases `@wordpress/blocks` module declarations, global `wp` declarations, and `export as namespace wp`, an ambient exported controller fixture erases decorated `export declare abstract class` plus private accessor declarations, a block-controller class fixture erases declared `BlockConfiguration` metadata fields, a constructor-parameter-property controller fixture lowers public/private WordPress block-controller dependencies into runtime assignments after `super()`, an assign-semantics constructor controller path omits duplicate field declarations, a class-field assign-semantics controller fixture lowers typed `BlockConfiguration` fields plus a generic registration method into constructor/static assignments, an anonymous default static controller fixture lowers `BlockConfiguration` static settings and computed worker asset fields to ES2021 after-class assignments plus a trailing default export clause, a computed-class-field fixture caches generated asset keys for WordPress block metadata before runtime registration, a computed-super controller fixture evaluates `resolveBaseController(metadata)` before the computed `assetKey("settings")` field key while still assigning settings after `super(metadata)`, a conditional-super controller fixture wraps preview/default base-controller paths in a native `__super` helper, a lazy-super controller fixture wraps `ready ||= super(metadata)` so WordPress block settings are assigned immediately after the base controller initializes without invoking Node, a comma-super controller fixture splits preloaded asset side effects before `super(metadata)` from hydration side effects after assignment injection, a return-super controller fixture splits pre-super side effects before `super(metadata)` and returns `this` after block settings/dependency assignments, a control-statement controller fixture splits `switch` tests and `for` initializers around `super(metadata)` without the helper path, a private-settings controller fixture keeps `#settings;` while moving typed WordPress block settings initialization into the constructor after `super(metadata)`, a typed-decorator auto-accessor controller fixture preserves runtime settings/worker asset accessors and lowers `@trackAccessor<BlockConfiguration>()` to runtime `@trackAccessor()` while erasing WordPress package type metadata, a top-level `using` disposable asset fixture erases typed WordPress asset-handle declarations before block registration and can opt into a legacy helper-lowered path that disposes preview/worker handles through `__callDispose`, an imported `block.json` plus top-level `using` fixture keeps the metadata import and trailing `export { settings }` outside the generated cleanup `try` block while preserving disposable preview-asset cleanup, an exported-local `block.json` plus top-level `using` fixture keeps `settings` and `PreviewRegistration` exportable after helper-lowered preview-asset cleanup, a destructured-export `block.json` plus top-level `using` fixture keeps destructured `blockName` and `settings` exportable after helper-lowered preview-asset cleanup, an exported-function `block.json` plus top-level `using` fixture keeps the exported registration function outside the cleanup scope while it closes over the helper-lowered preview asset, a default-exported block controller fixture keeps the default export outside the cleanup scope while preview assets are helper-disposed, a local exported block controller fixture rewrites the controller class to a cleanup-scope class expression while keeping its export clause after preview-asset disposal, a function-scoped `using` disposable asset fixture erases typed preview handles inside an exported block-registration helper and can opt into function-local helper cleanup, a block-scoped disposable preview asset fixture helper-lowers conditional cleanup through native `__using`/`__callDispose`, a for-loop `using` asset fixture preserves `for (using asset of collectBlockAssets(metadata))` after erasing `Disposable` annotations and can opt into per-iteration native helper disposal with helper-name collision avoidance, an async asset-queue fixture preserves `export async function registerQueuedAssets(queue)` while helper-lowering `while (queue.hasMore()) { await using asset ... }` per iteration, an exported async-generator function fixture lowers `streamPreviewAssets()` through native `__asyncGenerator`/`__await`/`__yieldStar`, a default-exported async-generator fixture lowers a parenthesized default stream, an exported async-generator constant fixture lowers `previewAssetStream` while keeping it exportable for registration code, an async-generator registry fixture lowers two same-statement array-element preview streams plus object-map and later call-argument preview streams for registration code, a class async generator asset-queue fixture preserves an exported `PreviewAssetQueue` class while helper-lowering its `async *assets()` method cleanup before block registration, and an object-literal async generator asset-queue fixture preserves an exported `previewAssets` object while helper-lowering its `async *assets()` method cleanup before block registration.
