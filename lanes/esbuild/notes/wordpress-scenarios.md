# esbuild WordPress Scenario

Node-free asset tooling for shared hosting, Playground, PHAR tools, and browser-backed PHP environments.

## Current Native Slice

Native JavaScript lexer for identifiers, esbuild-style numeric literal forms, strings, hashbangs, comments, decorators, private identifiers, and punctuators. A small module analyzer maps static imports, expression/direct-string dynamic imports, exports, re-exports, import assertions/attributes, `import.meta` ESM markers, `new URL(..., import.meta.url)` asset references, TypeScript import-equals/export-equals/type-only import/export forms, TypeScript namespace/export namespace metadata including dot-qualified namespace chains and declared binding-pattern exports, `export as namespace` type-only metadata, TypeScript import pruning metadata, and package-vs-relative import classification. A narrow namespace lowerer maps selected `TestTSImportEqualsInNamespace` printer behavior for import-equals aliases inside namespace blocks plus selected `TestTSNamespace`/`TestTSNamespaceExports` behavior for exported `var`/`let`/`const` namespace values, exported `function`/`class` declarations, nested exported namespaces, dot-qualified namespace declarations, namespace-local runtime enums, simple `export declare const` reference rewrites, declared binding-pattern reference rewrites, runtime namespace destructuring export assignment targets, namespace/value merge declaration rules, namespace-scoped `using` helper cleanup, namespace function-scoped `using`/`await using` helper cleanup, and reference rewrites. A narrow module lowerer maps selected top-level `TestTSExportEquals`/`TestTSImportEquals` printer behavior for `export =`, `import =`, and `export import =` declarations, selected `TestTSTypes` erasure for type annotations, `as`, `satisfies`, and type-only imports, selected `TestTSUsing` erasure/rejection behavior for explicit resource management declarations including comma-continued multiline `using`/`await using` forms, missing-initializer boundaries, nullish-initializer minify optimization, opt-in top-level `__using`/`__callDispose` helper lowering for unsupported targets, bounded function-body and block-scoped helper lowering for simple functions and `if`/block bodies, function-scope `using` annotation erasure with async-boundary validation, for/for-await `using` header type erasure plus invalid for-in/initializer rejection, bounded top-level helper export hoisting for simple and destructured local exports, exported/default/local class self-reference rewriting, ordinary local class declaration hoisting inside cleanup scopes, default class/expression exports, and helper-symbol collision avoidance, selected `TestTSEnum` runtime enum declarations, split enum block emission, enum member constant aliases, additive member-reference folding, same-file enum member access substitution, non-exported const enum erasure, selected ambient declaration/export-as-namespace erasure, decorated/exported/abstract ambient class erasure, selected class member `declare` field erasure plus invalid private/index/method/decorated field boundaries, selected `TestTSClass` abstract class/member erasure, constructor parameter property lowering, assign-semantics constructor parameter property field omission, simple, multiple, conditional, and logical-assignment derived `super()` assignment ordering through an upstream-style `__super` helper, dead `if (false) super(...)` branch handling, top-level expression/return/throw comma-expression, `if`/`switch` test, and `for` initializer derived `super()` splitting before assignment injection, initialized private class-field assignment insertion after `super(...)`, initialized static private fields retained inside assign-semantics classes, private getter/setter/method type annotation erasure, selected class method type-parameter erasure plus definite-assignment method rejection, bounded opt-in class-field assign-semantics lowering, computed public class-field key caching in assign-semantics mode, derived-class computed field key ordering through the `extends` expression, a bounded `TestTSClassSideEffectOrder` slice for computed methods interleaved with erased/initialized computed fields including ES2021 after-class public static field assignment emission, and selected `TestTSAutoAccessors`/`TestTSDecorators` plain and decorated auto-accessor normalization, decorator type-argument erasure, invalid decorator-call chain diagnostics, marker/type erasure, and malformed accessor rejection.

Current reduced analyzer/resolver update: WordPress preview chunks, conditional dynamic preview assets, conditional legacy CommonJS preview assets, block metadata, resolved worker paths, worker asset references, and bounded relative glob asset patterns can now use no-substitution template literal, string-concatenation, and template-substitution path sources without requiring Node. The preflight covers ``import(`./preview.js`)``, ``import(metadata.viewScript ? `./view-preview.js` : `./editor-preview.js`)``, `import metadata = require(\`./block.json\`)`, `require(metadata.viewScript ? \`./legacy-preview.js\` : \`./legacy-editor.js\`)`, `require.resolve(\`./preview-worker.js\`)`, omitted dead debug-preview `require(...)`/`import(...)` calls from `if (false)`, `false &&`, `true ||`, `true ??`, and constant ternary branches, live fallback preview chunks/workers in those same expression forms, ``new URL(`./preview-worker.js`, import.meta.url)``, ``require(`./blocks/${metadata.name}/view.js`)``, `import('./variations/' + metadata.variation + '.js')`, and `require('./styles/' + metadata.style + '.css')`. `GlobImportResolver` expands the relative glob records against `fixtures/wordpress-glob-assets`, returning sorted matches for block `view.js`, variation JS, and CSS files while excluding editor JS, SVG, and root-view non-matches. The example reports `WordPress no-substitution template literal sources: yes`, `WordPress conditional CommonJS template sources: yes`, `WordPress conditional dynamic import sources: yes`, `WordPress dead expression asset pruning: yes`, `WordPress glob asset sources: yes`, and `WordPress glob fixture matches: yes`. This handoff intentionally does not include package/node_modules resolution, source-map/data-URL/metafile classes, TypeScript phase-import/top-level-await work, conditional `require.resolve(...)` branch records that upstream does not emit at this commit, or accumulated fixtures from the preserved stash.

Current module-boundary helper update: unsupported-target lowering now keeps top-level directive strings, ES import declarations, export-star/export-from declarations, trailing export clauses, simple and destructured local `export const`/`export let`/`export var` declarations, simple local `export class` declarations, top-level plain/exported/default function declarations, and default class/expression export clauses outside the generated top-level `using` cleanup `try` block. Local variable/class/default exports are rewritten as export clauses after disposal, ordinary local class declarations inside the cleanup scope are rewritten to `var Name = class ...` assignments, exported/default/local class self-references are preserved through `_Name` class-expression bindings, and function declarations stay hoisted before the helper scope. The helper runtime also renames generated `__knownSymbol`, `__typeError`, `__using`, and `__callDispose` symbols when source identifiers collide, including per-iteration `for (using ... of ...)` acquisition call sites. This maps the bounded upstream `TestLowerUsingHoisting` and `TestLowerUsing` loop-helper shapes needed for WordPress block scripts that import `block.json`, acquire disposable preview assets, and still export metadata or registration helpers for a later native bundling step.

Current namespace helper update: namespace-local `using` declarations now erase TypeScript annotations and lower through an IIFE-local `_stack`, `__using`, `try`/`catch`/`finally`, and `__callDispose` cleanup wrapper. Generated namespace cleanup helpers are renamed when the source already binds `__knownSymbol`, `__typeError`, `__using`, or `__callDispose`. This maps the bounded upstream `TestLowerUsingInsideTSNamespace` shape plus helper-symbol collision boundaries for WordPress block scripts that keep block settings on a namespace object while acquiring a disposable preview asset during namespace initialization.

Current namespace async using update: namespace function bodies now erase resource type annotations from `using` and `await using` declarations, wrap the function body in a function-local `_stack`, `__using`, `try`/`catch`/`finally`, and `__callDispose` cleanup scope, and await async disposal promises when `await using` is present. `await using` is accepted only in async namespace functions and rejected in non-async namespace functions, and top-level namespace `await using` remains rejected. This maps a bounded `TestLowerUsingInsideTSNamespace` function-body helper slice for WordPress block namespaces that expose async preview-asset registration helpers while keeping exported settings rewrites on the namespace object.

Current control-flow helper update: unsupported-target lowering now applies the existing block-scope cleanup wrapper to ordinary `while`, `for`, and `for await` bodies that contain `using` or `await using` declarations. Each loop iteration gets a fresh native `_stack`, `__using`, `try`/`catch`/`finally`, and `__callDispose` cleanup scope. This maps a bounded `TestLowerUsing` control-flow block-body shape for WordPress block scripts that process queued preview or worker assets asynchronously without leaving TypeScript `await using` syntax in the output.

Current class async-generator helper update: class async generator methods now erase `await using` type annotations when preserving modern syntax, and helper-lower method-local disposable acquisitions through native `__using`/`__callDispose` cleanup when explicit-resource-management lowering is enabled. This maps a bounded upstream `TestLowerAsyncGenerator` class-method shape for WordPress preview asset queues that expose async iterable asset metadata without leaving TypeScript `AsyncDisposable` syntax in the output.

Current object async-generator helper update: object-literal async generator methods now erase method parameter/return and `await using` resource type annotations when preserving modern syntax, reject `await using` in non-async object generator methods, and helper-lower method-local disposable acquisitions through native `__using`/`__callDispose` cleanup when explicit-resource-management lowering is enabled. This maps the bounded upstream `TestLowerAsyncGenerator` object-method shape for WordPress preview asset queues exported as plain objects instead of classes.

Current async-generator runtime update: the module lowerer now has an opt-in unsupported-target path for selected upstream `TestLowerAsyncGenerator` shapes. Exported `async function*` asset streams, function expressions, direct and parenthesized default-export async-generator functions, non-default parenthesized async-generator expressions in constants/call arguments, nested array-element streams, later call-argument streams, object-property value streams, class methods, object methods, multiple supported async-generator expressions in one array/call/object statement, and async-generator declarations after prior statements lower to ordinary functions returning native `__asyncGenerator`. `await` in generator bodies becomes `yield new __await(...)`, optional-chain calls/properties stay inside the awaited operand, top-level `??`/`||` fallbacks stay outside it, `yield*` routes through `__yieldStar`, and simple `for await` plus `for await (await using ...)` loops route through `__forAwait` with generator-safe disposal. Generated async helpers are renamed when source identifiers already bind `__await`, `__asyncGenerator`, `__yieldStar`, or `__forAwait`. This lets WordPress block preflight code stream preview assets on older shared-hosting browsers without leaving `async function*` syntax in the generated runtime slice or clobbering existing helper globals.

Current default static controller update: ES2021 assign-semantics lowering now rewrites `export default class` declarations with static public field assignments to a local class binding, after-class static assignments, and a trailing default export clause. Anonymous default classes get a generated collision-safe binding, and computed static keys are cached before the class. This lets WordPress block controllers default-exported from TypeScript modules keep `BlockConfiguration` static metadata and computed worker asset keys inspectable without leaving class static blocks or requiring Node.

Current class expression controller update: ES2021 assign-semantics lowering now rewrites class expressions with static public field assignments to upstream-style comma expressions. Variable-initializer and call-argument class expressions get a class temp, computed static keys are cached once, and derived class expressions preserve base-controller lookup before computed asset-key side effects. This lets WordPress controller factories expose block controller classes while keeping static `BlockConfiguration` metadata and worker asset keys compatible with older JavaScript targets without invoking Node.

Current decorated controller update: decorated class statements that also need TypeScript lowering now stay class statements instead of being mistaken for decorated class expressions. The lowerer normalizes `export @x class` to upstream-style `@x export class`, erases TypeScript-only decorator type arguments, and leaves ES2021 after-class public static assignments after the decorated class. This lets WordPress block-controller decorators such as `@blockController<BlockConfiguration>(metadata)` remain inspectable while typed static block settings are emitted without `static {}` blocks or Node.

Current decorated class expression controller update: decorated class expressions that also need TypeScript lowering now keep decorators attached to the class expression while ES2021 public static field assignments wrap the decorated class temp in an upstream-style comma expression. The lowerer erases TypeScript-only decorator type arguments, restores semicolons for decorated class-expression statements, and avoids invalid `@decorator (...)` output. This lets WordPress controller factories export decorated classes with typed static block settings and computed worker asset fields without invoking Node.

Current method decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple instance method decorators. The lowerer keeps newline-separated decorators attached to the following method, erases WordPress-only type arguments and method return annotations, inserts or reuses an instance-initializer constructor, emits `__decorateElement(..., 1, "register", ...)` plus `__decoratorMetadata`, and still leaves private method decorator helper shapes for future slices. This lets a WordPress registration class preflight a method-level controller decorator for older runtimes without invoking Node.

Current public field decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple public identifier field decorators. Instance fields initialize through an inserted `__publicField(this, ..., __runInitializers(_init, 8, this, ...))` constructor path, and static fields initialize after `__decoratorMetadata` through `__publicField(ClassName, ..., __runInitializers(_init, 8, ClassName, ...))` without adding an instance constructor. This lets a WordPress registration class preflight a decorated static `BlockConfiguration` settings field for older runtimes without invoking Node.

Current private field decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple private identifier field decorators. Instance and static private fields allocate WeakMap storage, call `__decorateElement(..., 21|29, "#settings", decorators, storage)`, and initialize hidden metadata through `__privateAdd(... __runInitializers(_init, 8|11, ...))` in upstream order. The fixture in `fixtures/wordpress-block-private-field-decorator-legacy-controller.ts` preflights a decorated private `#settings: BlockConfiguration` metadata field for older runtimes without invoking Node.

Current private auto-accessor decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple private identifier auto-accessor decorators. Instance and static private accessors allocate private WeakMap storage plus a WeakSet brand, call `__decorateElement(..., 20|28, "#settings", decorators, brand, storage)`, capture generated get/set functions, and initialize brand plus storage in upstream order. The fixture in `fixtures/wordpress-block-private-accessor-decorator-legacy-controller.ts` preflights a decorated private `accessor #settings: BlockConfiguration = metadata` metadata accessor for older runtimes without invoking Node.

Current private getter decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple private getter/setter decorators. Instance and static private accessors move the getter/setter body into a generated function, allocate a WeakSet brand, call `__decorateElement(..., 18|19|26|27, "#settings", decorators, brand, fn)`, assign the decorated result back to the generated function, and initialize instance/static brands plus extra initializers in upstream order. The fixture in `fixtures/wordpress-block-private-getter-decorator-legacy-controller.ts` preflights a decorated private `get #settings(): BlockConfiguration` used by controller metadata decorators for older runtimes without invoking Node.

Current accessor decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple public identifier auto-accessor decorators. Instance and static accessors are removed from the class body, receive collision-safe WeakMap storage, call `__decorateElement(..., 4|12, ..., storage)`, and initialize through `__privateAdd(... __runInitializers(_init, 8|11, ...))` in the upstream order. This lets a WordPress registration class preflight a decorated `accessor settings: BlockConfiguration = metadata` member for older runtimes without invoking Node.

Current computed field decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple public computed field and auto-accessor decorators. The lowerer caches the computed key in a generated temp, initializes the decorator array during that key evaluation, passes the temp to `__decorateElement`, and initializes field/accessor storage through the same upstream-ordered helper calls used for identifier members. The fixture in `fixtures/wordpress-block-computed-field-decorator-legacy-controller.ts` uses a computed `assetKey("settings")` block settings property, erases `BlockConfiguration`, and preserves registration through the computed key without invoking Node.

Current computed method decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple public computed method decorators. The lowerer initializes the decorator array inside the computed method key, passes the generated key temp to `__decorateElement`, inserts instance initializer constructors when needed, runs static initializers for static computed methods, and keeps derived class decorator metadata tied to the captured base class. The fixture in `fixtures/wordpress-block-computed-method-decorator-legacy-controller.ts` uses a computed `methodKey("register")` registration method, erases `BlockConfiguration`, and preserves legacy-target block registration without invoking Node.

Current computed accessor decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple public getter and setter decorators, including computed and static accessor names. The lowerer erases getter return and setter parameter annotations, emits `__decorateElement(..., 2|3|10|11, key, ...)`, evaluates computed decorator arrays inside computed accessor keys, preserves derived class base metadata, and avoids duplicate instance extra-initializer calls when paired accessors share a class. The fixture in `fixtures/wordpress-block-computed-accessor-decorator-legacy-controller.ts` uses computed `accessorKey("settings")` get/set accessors for block settings, erases `BlockConfiguration`, and preserves legacy-target block registration without invoking Node.

Current existing-constructor decorator helper update: opt-in unsupported-target decorator helper lowering now injects simple instance method, public field, and public auto-accessor decorator initializers into existing non-derived constructors. The inserted initializer runs before ordinary constructor body statements, while generated TypeScript constructor-parameter-property assignments remain before method decorator initializers. This lets a WordPress registration class keep constructor-injected `wp.blocks` state and still lower a decorated `register()` method for older runtimes without invoking Node.

Current derived member decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple derived class member decorators. The lowerer captures the base class in the `extends` expression, initializes decorator arrays there, calls `__decoratorStart(_base)`, inserts synthetic derived constructors when needed, and injects instance method/field/accessor initializers immediately after direct `super(...)` calls or through the existing `__super` helper for conditional/multiple `super(...)` paths. This lets a WordPress block controller subclass preserve base-controller initialization and constructor-injected `wp.blocks` state while lowering a decorated `register()` method without invoking Node.

Current mixed decorator helper update: opt-in unsupported-target decorator helper lowering now handles simple named classes with both class and member decorators. Class and member decorators share one `_init` sequence, member decorators run before the class decorator, static member initializers run after class decoration, class initializers run last, derived classes keep member decorator arrays in the `extends` expression, and mixed class/member output omits explicit `__decoratorMetadata` because the class decorator call records metadata. This lets WordPress block registration controllers combine class-level block metadata decorators with method-level registration decorators for older runtimes without invoking Node.

Current default mixed decorator helper update: opt-in unsupported-target decorator helper lowering now handles exported named classes and anonymous default-exported classes with both class and member decorators. Anonymous defaults receive a generated class binding before member decorators are lowered, class decorators keep the upstream empty default-export context name, member decorators still run before the class decorator, and explicit member metadata remains omitted because the class decorator call records it. The fixture in `fixtures/wordpress-block-default-mixed-decorator-legacy-controller.ts` uses `export default @blockController<BlockConfiguration>(metadata) class { @methodController<BlockConfiguration>(metadata) register(...) { ... } }` to preflight a default-exported block registration controller for older runtimes without invoking Node.

Current decorator-boundary diagnostic update: decorators are now validated before the native printer emits output. Parameter decorators, decorators before non-class declarations, computed decorator property access, optional-chain decorator expressions, and `new`-started decorator expressions are rejected. The preflight example records that a mistyped WordPress `@blockController<BlockConfiguration>(metadata)` decorator on a registration function is diagnosed locally instead of producing JavaScript that does not match upstream esbuild.

Current switch-case disposable diagnostic update: direct `using` and `await using` declarations immediately under a `case` or `default` clause are rejected instead of being emitted as invalid JavaScript. The preflight example now checks a WordPress preview-asset acquisition inside a `switch (metadata.viewScript)` case and reports the diagnostic, which points block tooling toward wrapping disposable case bodies in `{ ... }` before future helper lowering.

Current switch-case disposable helper update: block-wrapped `using` and `await using` declarations under `case`/`default` clauses now lower through native `__using`/`__callDispose` cleanup. The fixture in `fixtures/wordpress-block-switch-using-assets.ts` models a WordPress block choosing view/editor preview assets from a `switch (mode)` statement, preserving block metadata imports and async cleanup without invoking Node.

Current switch-case asset loop update: top-level `for (using ... of ...)` and `for (await using ... of ...)` statements inside switch `case`/`default` clauses now lower through per-iteration native cleanup helpers. The switch-mode fixture uses this for a bulk block-asset registration branch that disposes each collected mode-specific preview asset before the next asset is registered, again without invoking Node.

The fixture in `fixtures/wordpress-block-view.js` represents a block view script that can be preflighted on shared hosting without Node/npm and classified as importing `@wordpress/dom-ready`, a relative `block.json` metadata import with `with { type: "json" }`, and relative stylesheet/worker assets. The fixture in `fixtures/wordpress-block-view-types.ts` represents a TypeScript block script that imports erased WordPress package types from `@wordpress/blocks` and `@wordpress/element`, keeps `@wordpress/dom-ready`, `./block.json`, and a legacy `import wpBlocks = wp.blocks` alias as pruned runtime dependencies, and maps the alias without shelling out to Node.
Current terminal import path rewrite update: the bounded WordPress package output smoke now rewrites retained `./block.css` and `./block.json` imports to output-relative `./src/block.css` and `./src/block.json` paths while removing the bundled local JavaScript import. This gives shared-hosting/Playground preflight a native PHP preview of where terminal block assets will be loaded from without invoking Node/npm or claiming broader CSS/JSON bundling.
It also records the `CardBlock` namespace and its runtime exported `name`/`register` members so a PHP preflight can distinguish namespace-backed block registration code from erased TypeScript-only imports.
The namespace-lowering scenario maps a WordPress `export import blocks = wp.blocks` alias to `CardBlockRuntime.blocks = wp.blocks` and rewrites later alias uses to the namespace property, which is the shape needed before a future native printer can emit shared-hosting-compatible block registration code.
The fixture in `fixtures/wordpress-block-namespace-export.ts` adds exported namespace block settings. The namespace lowerer emits `CardBlockRuntime.settings` and `CardBlockRuntime.viewMode` property assignments and rewrites later block registration references to those namespace properties without invoking Node.
The fixture in `fixtures/wordpress-block-namespace-runtime.ts` adds exported namespace runtime declarations. The namespace lowerer emits a local `PreviewController` class and `register` function, assigns both back to `CardBlockRuntime`, and rewrites exported settings and `wp.blocks` alias references inside the registration function without invoking Node.
The fixture in `fixtures/wordpress-block-nested-namespace-enum.ts` adds a nested `CardBlockRuntime.Supports` namespace with an exported runtime `DisplayMode` enum and exported settings object. The namespace lowerer emits nested namespace initialization, enum member assignments, and block registration against `Supports.settings` without invoking Node.
The fixture in `fixtures/wordpress-block-dot-namespace.ts` represents a library-style `PortLibs.CardBlock` namespace used by shared WordPress block tooling. The namespace lowerer emits the parent `PortLibs` namespace and child `CardBlock` initializer with `CardBlock = PortLibs.CardBlock || (PortLibs.CardBlock = {})`, then rewrites exported `blocks` and `settings` references before registration without invoking Node.
The fixture in `fixtures/wordpress-block-destructured-settings.ts` represents WordPress block metadata/config unpacked through a namespace destructuring export. The namespace lowerer rewrites destructured `blockName`, `settings`, and `viewMode` bindings to `CardBlockRuntime` properties and rewrites later spread/object references without invoking Node.
The fixture in `fixtures/wordpress-block-function-namespace.ts` represents the TypeScript pattern where a callable `registerBlock` function is merged with a namespace carrying static WordPress block settings. The namespace lowerer preserves the function, attaches `registerBlock.settings` through the namespace IIFE without emitting a duplicate `var registerBlock`, and preserves the final registration call without invoking Node.
The fixture in `fixtures/wordpress-block-namespace-using-preview.ts` represents a WordPress block namespace that exports settings, acquires a disposable preview asset during namespace initialization, exports the derived preview URL, and registers the block. The namespace lowerer wraps the namespace IIFE body in native `__using`/`__callDispose` cleanup, rewrites exported settings and preview references to namespace properties, and erases WordPress package type imports without invoking Node.
The fixture in `fixtures/wordpress-block-namespace-using-helper-collision.ts` adds WordPress globals that already bind `__knownSymbol`, `__typeError`, `__using`, and `__callDispose` next to the namespace disposable preview asset flow. The namespace lowerer keeps those globals intact, emits renamed cleanup helpers such as `__using2`/`__callDispose2`, and routes the namespace-local acquisition and cleanup through the renamed helpers without invoking Node.
The fixture in `fixtures/wordpress-block-namespace-await-using-preview.ts` represents a namespace-backed async block registration helper that opens a preview asset with `await using` inside an exported async namespace function. The namespace lowerer erases the `AsyncDisposable` annotation, rewrites `settings` to `CardBlockRuntime.settings`, helper-lowers acquisition and awaited disposal through native `__using`/`__callDispose`, and assigns the exported helper back to the namespace without invoking Node.
The fixture in `fixtures/wordpress-block-commonjs-export.ts` represents a legacy TypeScript block runtime that uses `export = registerBlock`. The module lowerer maps that to `module.exports = registerBlock`, giving PHP preflight tools a native CommonJS boundary without shelling out to esbuild or Node.
The fixture in `fixtures/wordpress-block-typed-callback.ts` represents a typed block edit callback using `BlockEditProps`, `WPElement`, and `satisfies BlockConfiguration`. The module lowerer erases those type-only imports and annotations while preserving the runtime `wp.element.createElement` and `wp.blocks.registerBlockType` calls.
The fixture in `fixtures/wordpress-block-enum-config.ts` represents a block runtime that uses a TypeScript `DisplayMode` enum in registration metadata. The module lowerer emits the runtime enum object and erases the WordPress package type import plus `satisfies BlockConfiguration`, preserving a Node-free registration path.
The fixture in `fixtures/wordpress-block-enum-alias-config.ts` represents a block runtime that aliases enum members such as `Default = Card` and `Wide = DisplayMode.Grid` before using those aliases in registration metadata. The module lowerer folds those member references and keeps the generated config JavaScript available to PHP preflight code without invoking Node.
The fixture in `fixtures/wordpress-block-const-enum-config.ts` represents the same WordPress configuration style with a non-exported `const enum`. The module lowerer erases the enum declaration and substitutes `DisplayMode.Card`, `DisplayMode.Grid`, and `DisplayMode['List']` with inline numeric comments before the block registration call.
The fixture in `fixtures/wordpress-block-ambient-types.ts` represents ambient WordPress package/global typings colocated with runtime block code. The module lowerer erases `declare module '@wordpress/blocks'`, `declare global`, and `export as namespace wp` declarations while preserving the runtime `wp.blocks.registerBlockType` call.
The fixture in `fixtures/wordpress-block-ambient-exports.ts` represents generated or third-party WordPress block typings that include decorated exported ambient classes and private accessors near runtime registration code. The module lowerer erases the decorated `export declare abstract class` and private/accessor declarations while preserving the runtime `wp.blocks.registerBlockType` call.
The fixture in `fixtures/wordpress-block-class-declare-settings.ts` represents a runtime block controller class with declared `BlockConfiguration` metadata fields emitted by TypeScript tooling. The module lowerer erases those declared fields while preserving the controller's runtime `wp.blocks.registerBlockType` call and constructor use without invoking Node.
The fixture in `fixtures/wordpress-block-constructor-properties.ts` represents a TypeScript block controller that extends an abstract base controller and captures WordPress runtime dependencies through constructor parameter properties. The module lowerer erases the abstract method signature, lowers public/private parameter properties to runtime assignments after `super()`, emits class fields only in define-semantics mode, erases the `BlockConfiguration` type import, and preserves the `wp.blocks.registerBlockType` call without invoking Node.
The fixture in `fixtures/wordpress-block-class-fields-assign.ts` represents a TypeScript block controller with typed public/static `BlockConfiguration` fields and a generic registration method under assign-semantics class fields. The module lowerer erases the WordPress package type import, moves initialized instance fields into constructor assignments, emits a bounded static assignment block on modern targets, emits `CardBlockController.metadata = metadata` after the class for an ES2021 preflight target, erases method type parameters/parameter annotations, and preserves the `wp.blocks.registerBlockType` call without invoking Node.
The fixture in `fixtures/wordpress-block-private-static-cache.ts` represents a block controller with a private static `#settings` cache and public static metadata under an ES2021 preflight target. The module lowerer erases the `BlockConfiguration` type, keeps the private static initializer inside the class so it is not emitted as invalid after-class private-name syntax, moves the public static metadata assignment after the class, and preserves registration through `CachedBlockController.#settings` without invoking Node.
The fixture in `fixtures/wordpress-block-default-static-controller.ts` represents an anonymous default-exported block controller with typed static settings and a computed worker asset field under an ES2021 preflight target. The module lowerer erases `BlockConfiguration`, caches the worker asset key, emits static metadata as after-class assignments on the generated default binding, and preserves a trailing `export { defaultExport as default }` clause without invoking Node.
The fixture in `fixtures/wordpress-block-class-expression-controller.ts` represents a WordPress block controller held in a class expression that extends a base controller, stores typed static settings, computes a worker asset key, and exports the controller through a named export. The module lowerer erases `BlockConfiguration`, caches the base controller and worker asset key in upstream order, lowers static metadata through a class temp comma expression for ES2021 targets, and preserves the export clause without invoking Node.
The fixture in `fixtures/wordpress-block-decorated-class-expression-controller.ts` represents a WordPress block controller exported through a decorated class expression with typed static settings and a computed worker asset key. The module lowerer erases `BlockConfiguration` and the decorator type argument, keeps `@blockController(metadata)` attached to the class temp, emits static metadata and worker asset assignments after the class expression for ES2021 targets, and preserves block registration through the exported controller binding without invoking Node.
The fixture in `fixtures/wordpress-block-computed-class-fields.ts` represents a block asset controller that derives instance and static field names through computed keys. The module lowerer caches those keys in temporaries before constructor/static assignment emission so generated WordPress asset key helpers run once during native preflight instead of per instance or after registration code, while still erasing package-only `BlockConfiguration` types.
The fixture in `fixtures/wordpress-block-computed-super-controller.ts` represents a derived block controller whose base controller and computed settings key both have pre-constructor side effects. The module lowerer caches the base-controller lookup before evaluating the computed settings key, then injects the settings assignment after `super(metadata)` so shared-hosting preflight preserves upstream initialization order without invoking Node.
The fixture in `fixtures/wordpress-block-conditional-super-controller.ts` represents a derived block controller with preview/default base-controller initialization paths. The module lowerer wraps the conditional `super(...)` calls in a native `__super` helper so WordPress block settings and constructor parameter properties are assigned immediately after whichever base path runs, without invoking Node.
The fixture in `fixtures/wordpress-block-lazy-super-controller.ts` represents a derived block controller that lazily initializes its base controller through `ready ||= super(metadata)`. The module lowerer wraps that logical-assignment `super(...)` call in a native `__super` helper so erased type-only imports stay out of the output while WordPress block settings and runtime dependencies are assigned immediately after the base controller initializes.
The fixture in `fixtures/wordpress-block-comma-super-controller.ts` represents a derived block controller that preloads preview assets, calls `super(metadata)`, then hydrates asset metadata in one comma-expression constructor statement. The module lowerer splits that statement around the direct `super(...)` call so pre-super side effects stay before base initialization, WordPress constructor properties and settings are assigned immediately after `super(...)`, and hydration side effects stay after those assignments without invoking Node.
The fixture in `fixtures/wordpress-block-return-super-controller.ts` represents a derived block controller that returns a top-level comma expression after preloading preview assets. The module lowerer splits pre-super side effects before `super(metadata)`, injects WordPress constructor properties and settings immediately after base initialization, then returns `this` without invoking the `__super` helper or Node.
The fixture in `fixtures/wordpress-block-control-super-controller.ts` represents derived WordPress block controllers that hide `super(metadata)` inside a `switch` test and a `for` initializer. The module lowerer splits pre-super preview/asset queue side effects before base initialization, injects block settings and dependencies immediately after `super(metadata)`, and resumes the `switch(resolveMode())` or `for(asset = nextAsset(); ...)` statement without invoking the `__super` helper or Node.
The fixture in `fixtures/wordpress-block-private-settings-controller.ts` represents a derived block controller with typed private `#settings` metadata. The module lowerer keeps the private declaration for runtime access while moving the settings initializer after `super(metadata)` and before WordPress dependency assignments, preserving shared-hosting execution order without invoking Node.
The fixture in `fixtures/wordpress-block-private-accessor-controller.ts` represents a block controller that hides block settings behind private getter/setter accessors. The module lowerer erases `BlockConfiguration` return and parameter annotations from those private accessors, keeps `get #blockSettings`/`set #blockSettings` runtime syntax valid, and preserves registration through `this.#blockSettings` without invoking Node.
The fixture in `fixtures/wordpress-block-auto-accessor-controller.ts` represents a block controller using TypeScript auto-accessors for a plain static controller version, a plain instance asset handle, a typed decorator-factory runtime block settings accessor, a computed worker asset key, and a private block name. The module lowerer normalizes plain and decorated auto-accessors and erases `BlockConfiguration`, optional/definite markers, type annotations, and decorator type arguments while preserving `@trackAccessor()` plus auto-accessor runtime syntax, and the same decorator parser now rejects invalid chained decorator factories after a call instead of emitting non-upstream syntax. PHP preflight can inspect the settings/worker asset path without invoking Node.
The fixture in `fixtures/wordpress-block-decorated-controller.ts` represents a WordPress block controller decorated with `@blockController<BlockConfiguration>(metadata)` and typed static block settings. The module lowerer emits `@blockController(metadata) class DecoratedBlockController`, moves public static `settings` to an ES2021-compatible after-class assignment, erases WordPress package type imports, and preserves registration through `DecoratedBlockController.settings` without invoking Node.
The fixture in `fixtures/wordpress-block-decorator-legacy-controller.ts` represents a WordPress block controller that needs decorator syntax lowered for older runtimes. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, turns `@blockController<BlockConfiguration>(metadata)` into `_DecoratedRegistration_decorators = [blockController(metadata)]`, emits `__decoratorStart`, `__decorateElement`, and `__runInitializers` calls, and preserves the registration call without invoking Node.
The fixture in `fixtures/wordpress-block-default-decorator-legacy-controller.ts` represents a default-exported anonymous WordPress block controller that also needs decorator syntax lowered for older runtimes. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, generates a default class binding, routes `export default @blockController<BlockConfiguration>(metadata) class { ... }` through `_class_decorators`, emits an empty decorator context name for the anonymous class, and keeps the default export inspectable without invoking Node.
The fixture in `fixtures/wordpress-block-method-decorator-legacy-controller.ts` represents a WordPress block registration class whose `register()` method is decorated for older runtimes. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, turns `@methodController<BlockConfiguration>(metadata)` into `_register_dec = [methodController(metadata)]`, inserts the method initializer constructor, emits `__decorateElement(_init, 1, "register", ...)` and `__decoratorMetadata`, and preserves the registration call without invoking Node.
The fixture in `fixtures/wordpress-block-mixed-decorator-legacy-controller.ts` represents a WordPress block registration class with both `@blockController<BlockConfiguration>(metadata)` on the class and `@methodController<BlockConfiguration>(metadata)` on `register()`. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, emits `_MixedDecoratedRegistration_decorators` and `_register_dec`, calls `__decorateElement` for `register` before the class decorator, omits explicit member metadata, and preserves the registration call without invoking Node.
The fixture in `fixtures/wordpress-block-default-mixed-decorator-legacy-controller.ts` represents an anonymous default-exported WordPress block registration class with both a class-level block metadata decorator and a method-level registration decorator. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, generates a `defaultExport` class binding, emits `_class_decorators` and `_register_dec`, calls `__decorateElement` for `register` before decorating the default class with an empty context name, omits explicit member metadata, and keeps the default export inspectable without invoking Node.
The fixture in `fixtures/wordpress-block-method-decorator-constructor-legacy-controller.ts` represents a WordPress block registration class that stores `wp.blocks` in an existing constructor before a decorated `register()` method uses that state. With opt-in decorator helper lowering, the module lowerer injects `__runInitializers(_init, 5, this)` into the constructor before the stored block registry assignment, erases `BlockConfiguration`, emits `__decorateElement(_init, 1, "register", ...)`, and preserves the registration call without invoking Node.
The fixture in `fixtures/wordpress-block-derived-method-decorator-legacy-controller.ts` represents a derived WordPress block registration class whose constructor calls `super(metadata)` before storing `wp.blocks`. With opt-in decorator helper lowering, the module lowerer captures `BaseBlockController` in the `extends` expression, initializes `_register_dec` there, injects `__runInitializers(_init, 5, this)` immediately after `super(metadata)`, erases `BlockConfiguration`, emits `__decorateElement(_init, 1, "register", ...)`, and preserves the registration call without invoking Node.
The fixture in `fixtures/wordpress-block-static-method-decorator-legacy-controller.ts` represents a WordPress block registration class whose static `register()` method is decorated for older runtimes. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, turns `@methodController<BlockConfiguration>(metadata)` into `_register_dec = [methodController(metadata)]`, preserves static registration without inserting an instance initializer constructor, emits `__decorateElement(_init, 9, "register", ...)`, `__decoratorMetadata`, and `__runInitializers(_init, 3, StaticMethodDecoratedRegistration)`, and preserves the static registration call without invoking Node.
The fixture in `fixtures/wordpress-block-static-field-decorator-legacy-controller.ts` represents a WordPress block registration class whose static `settings: BlockConfiguration` field is decorated for older runtimes. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, turns `@fieldController<BlockConfiguration>(metadata)` into `_settings_dec = [fieldController(metadata)]`, emits `__decorateElement(_init, 13, "settings", ...)` and `__decoratorMetadata`, initializes the static settings field through `__publicField(... __runInitializers(_init, 8, StaticFieldDecoratedRegistration, metadata))`, and preserves static block registration without invoking Node.
The fixture in `fixtures/wordpress-block-accessor-decorator-legacy-controller.ts` represents a WordPress block registration class whose `settings: BlockConfiguration` auto-accessor is decorated for older runtimes. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, turns `@accessorController<BlockConfiguration>(metadata)` into `_settings_dec = [accessorController(metadata)]`, allocates `_settings = new WeakMap()`, emits `__decorateElement(_init, 4, "settings", ..., _settings)` and `__decoratorMetadata`, initializes the accessor through constructor-local `__privateAdd`, and preserves registration through `this.settings` without invoking Node.
The fixture in `fixtures/wordpress-block-computed-field-decorator-legacy-controller.ts` represents a WordPress block registration class whose settings are stored behind a decorated computed property key. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, evaluates `assetKey("settings")` once into a generated temp while initializing the decorator array, emits `__decorateElement(_init, 5, _a, ...)`, initializes the instance field through `__publicField(this, _a, ...)`, and preserves registration through `this[assetKey("settings")]` without invoking Node.
The fixture in `fixtures/wordpress-block-computed-method-decorator-legacy-controller.ts` represents a WordPress block registration class whose registration method is selected by a computed key. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, evaluates `methodKey("register")` inside the computed method key while initializing the decorator array, emits `__decorateElement(_init, 1, _a, ...)`, inserts the instance decorator initializer constructor, and preserves `new ComputedMethodDecoratedRegistration()[methodKey('register')]()` without invoking Node.
The fixture in `fixtures/wordpress-block-computed-accessor-decorator-legacy-controller.ts` represents a WordPress block registration class whose settings are read and written through decorated computed accessor keys. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, evaluates `accessorKey("settings")` inside each computed accessor key while initializing each decorator array, emits `__decorateElement(_init, 2|3, _a, ...)`, inserts only one instance extra-initializer constructor call for the accessor pair, and preserves block registration through `this[accessorKey("settings")]` without invoking Node.
The fixture in `fixtures/wordpress-block-private-field-decorator-legacy-controller.ts` represents a WordPress block registration class that keeps framework metadata in a decorated private `#settings` field. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, allocates private WeakMap storage, emits `__decorateElement(_init, 21, "#settings", ...)`, initializes private storage through `__privateAdd(this, _settings, __runInitializers(...))`, and preserves block registration without invoking Node.
The fixture in `fixtures/wordpress-block-private-accessor-decorator-legacy-controller.ts` represents a WordPress block registration class that keeps framework metadata in a decorated private auto-accessor. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, allocates private WeakMap storage plus a WeakSet brand, emits `__decorateElement(_init, 20, "#settings", ..., _settings_instances, _settings)`, captures generated `settings_get`/`settings_set` functions, initializes brand plus storage in the constructor, and preserves block registration without invoking Node.
The fixture in `fixtures/wordpress-block-private-getter-decorator-legacy-controller.ts` represents a WordPress block registration class with a decorated private settings getter for older runtimes. With opt-in decorator helper lowering, the module lowerer erases `BlockConfiguration`, moves the private getter body to `settings_get = function() { ... }`, allocates a WeakSet brand, emits `__decorateElement(_init, 18, "#settings", ..., _settings_instances, settings_get)`, initializes the brand in the constructor, and preserves registration through the public settings object without invoking Node.
The fixture in `fixtures/wordpress-block-using-disposable.ts` represents an explicit resource management block script that acquires preview and worker asset handles before registration using a comma-continued multiline `using` declaration. The module lowerer keeps the declaration together, erases typed `Disposable` annotations and the `BlockConfiguration` check, preserves runtime disposal boundaries for Node-free preflight, and can opt into a legacy helper-lowered path that disposes preview and worker asset handles through `__callDispose`.
The fixture in `fixtures/wordpress-block-using-import-hoist.ts` represents a TypeScript block script that imports `./block.json` with an import attribute, acquires a disposable preview asset at top level, registers the block, and exports the computed settings. With helper lowering enabled, the module lowerer keeps the `block.json` import and trailing `export { settings }` at module scope instead of trapping them inside the cleanup `try` block, while still erasing `Disposable` and `BlockConfiguration` type syntax.
The fixture in `fixtures/wordpress-block-using-export-local.ts` represents a TypeScript block script that imports `./block.json`, acquires a disposable preview asset, exports computed block settings, and exports a registration class. With helper lowering enabled, the module lowerer keeps `settings` and `PreviewRegistration` exportable after the cleanup `finally` block while running the preview asset acquisition and registration in the native `__using`/`__callDispose` scope.
The fixture in `fixtures/wordpress-block-using-destructured-export.ts` represents a TypeScript block script that destructures `metadata.name` into an exported `blockName` next to exported block settings while a disposable preview asset is active. With helper lowering enabled, the module lowerer keeps both `blockName` and `settings` exportable after the cleanup `finally` block while running preview asset acquisition and registration in the native `__using`/`__callDispose` scope.
The fixture in `fixtures/wordpress-block-using-export-function.ts` represents a TypeScript block script that imports `./block.json`, acquires a disposable preview asset at top level, and exports a registration function that closes over that asset. With helper lowering enabled, the module lowerer keeps the exported function outside the cleanup `try` block while the preview asset is acquired and disposed through native `__using`/`__callDispose`.
The fixture in `fixtures/wordpress-block-using-default-controller.ts` represents a TypeScript block script that imports `./block.json`, acquires a disposable preview asset at top level, and default-exports a self-referencing controller class that registers the block. With helper lowering enabled, the module lowerer keeps the default export clause outside the cleanup `try` block while the preview asset is acquired and disposed through native `__using`/`__callDispose`, and rewrites the controller self-reference through the upstream-style class-expression binding.
The fixture in `fixtures/wordpress-block-using-local-controller.ts` represents a TypeScript block script that imports `./block.json`, acquires a disposable preview asset at top level, defines a local self-referencing controller class, registers it, and exports the class through a trailing export clause. With helper lowering enabled, the module lowerer rewrites the controller to a cleanup-scope class expression, rewrites the class self-reference through the upstream-style private binding, and leaves the export clause after disposal.
The fixture in `fixtures/wordpress-block-function-using-disposable.ts` represents a block registration helper that acquires a preview asset handle inside an exported function. The module lowerer erases the function-scope `Disposable` annotation while preserving the scoped `using` declaration and runtime `wp.blocks.registerBlockType` call without invoking Node, and its helper-lowered path wraps the function body in a native `__using`/`__callDispose` cleanup scope for legacy targets.
The fixture in `fixtures/wordpress-block-block-using-disposable.ts` represents a conditional block asset preflight that acquires a disposable preview asset only when block metadata references a view script. With helper lowering enabled, the module lowerer wraps that block in a native `__using`/`__callDispose` stack so shared-hosting code can preserve cleanup order without invoking Node.
The fixture in `fixtures/wordpress-block-for-using-assets.ts` represents a block asset registration loop that acquires disposable asset handles with `for (using asset: Disposable of collectBlockAssets(metadata))`. The module lowerer erases the `Disposable` annotation while preserving the `of` clause and registration body without invoking Node, and its helper-lowered path disposes each loop asset before the next collected asset is registered. The switch-mode fixture now covers the same per-iteration cleanup when the asset loop appears directly under a switch case.
The fixture in `fixtures/wordpress-block-for-using-helper-collision.ts` adds WordPress globals that already bind `__knownSymbol`, `__typeError`, `__using`, and `__callDispose` next to the loop-scoped asset registration flow. The module lowerer keeps those globals intact, emits renamed cleanup helpers such as `__using2`/`__callDispose2`, and routes the per-iteration asset acquisition and cleanup through the renamed helpers without invoking Node.
The fixture in `fixtures/wordpress-block-while-using-assets.ts` represents an exported async block asset queue that imports `./block.json`, repeatedly opens queued preview assets with `await using` inside `while (queue.hasMore())`, registers each asset, and then registers the block. The module lowerer keeps the metadata import and exported async function intact while helper-lowering the while body to dispose every queued asset before the next iteration without invoking Node.
The fixture in `fixtures/wordpress-block-async-generator-function-assets.ts` represents an exported `streamPreviewAssets()` async generator function that opens a queued preview asset, yields metadata, delegates additional assets with `yield*`, and lets a registration helper consume the stream before registering the block. With async-generator runtime lowering enabled, the module lowerer keeps the `block.json` import and exported registration helper intact while lowering the asset stream through native `__asyncGenerator`, `__await`, `__yieldStar`, `__using`, and `__callDispose` helpers without invoking Node.
The fixture in `fixtures/wordpress-block-default-async-generator-assets.ts` represents a default-exported parenthesized async generator asset stream that imports `./block.json`, opens a queued preview asset, yields metadata, and delegates additional assets with `yield*`. With async-generator runtime lowering enabled, the module lowerer keeps the metadata import intact while lowering the default-exported stream through native `__asyncGenerator`, `__await`, `__yieldStar`, `__using`, and `__callDispose` helpers without invoking Node.
The fixture in `fixtures/wordpress-block-exported-async-generator-constant.ts` represents an exported `previewAssetStream` constant initialized from a parenthesized async generator function expression. With async-generator runtime lowering enabled, the module lowerer keeps the constant export and `block.json` metadata import intact while lowering disposable asset streaming through native `__asyncGenerator`, `__await`, `__yieldStar`, `__using`, and `__callDispose` helpers without invoking Node.
The fixture in `fixtures/wordpress-block-async-generator-registry-assets.ts` represents a registry array with two same-statement preview asset streams, awaited logical-OR and optional-chain/nullish fallback handle selection, a computed object map keyed by `metadata.name`, and a later callback argument that wraps an existing stream for registration. With async-generator runtime lowering enabled, the module lowerer keeps the `block.json` metadata import and exported registration function intact while lowering both array-element streams plus the object-property and callback-argument async-generator expressions through native `__asyncGenerator`, `__await`, `__yieldStar`, `__using`, and `__callDispose` helpers without invoking Node, and it preserves `await queue.resolveHandle(...) || metadata.viewScript` plus `await queue.resolveHandle?.(...) ?? metadata.viewScript` precedence instead of awaiting the whole fallback expression.
The fixture in `fixtures/wordpress-block-async-generator-helper-collision.ts` represents WordPress interactivity code that already binds helper-like globals before declaring an async generator asset stream. With async-generator runtime lowering enabled, the module lowerer preserves those globals, lowers the later `streamInteractiveAssets()` declaration, and emits renamed generated `__await2`, `__asyncGenerator2`, `__yieldStar2`, and `__forAwait2` helpers without invoking Node.
The fixture in `fixtures/wordpress-block-async-generator-assets.ts` represents an exported `PreviewAssetQueue` class whose `async *assets()` method opens a queued preview asset with `await using`, yields asset metadata, and lets a registration helper consume the async generator before registering the block. The module lowerer keeps `block.json` imports and exported class/function declarations intact, erases `AsyncDisposable` and `BlockConfiguration` syntax, and helper-lowers the class async generator method cleanup without invoking Node.
The fixture in `fixtures/wordpress-block-object-async-generator-assets.ts` represents the same queued preview asset flow exported as a plain `previewAssets` object with an `async *assets()` method. The module lowerer keeps `block.json` imports and the exported object/function declarations intact, erases `AsyncDisposable` and `BlockConfiguration` syntax, and helper-lowers the object-literal async generator method cleanup without invoking Node.

Current package resolver update: the fixture in `fixtures/wordpress-package-assets` represents a WordPress-oriented block asset preflight that imports `@wordpress/interactivity`, a local block runtime package, a package subpath helper, a package self import, a server-oriented package, a package with a missing `main` field, an `exports`-mapped runtime package, package `imports`, a browser-object-mapped package, and a package that itself remaps package imports through its containing `browser` map. `PackageResolver` resolves those package records against a bounded `node_modules` fixture with browser and node main-field order, scoped package names, subpath lookup, package self-import lookup, extension/index fallback, bad-main fallback, package `imports`, package `exports` root condition maps, import-vs-require condition branches, wildcard feature subpaths, slash-prefix legacy subpaths, exact export targets, unsafe export target rejection, package-local browser object root/subpath remapping, containing-package package-to-relative and package-to-package remapping, false browser-map disabling, and bounded Node builtin behavior without invoking Node/npm. The Node builtin slice records node-platform `path`, `node:path`, and `fs/promises` as explicit external records even when a fixture `node_modules/path` package exists, while browser-platform containing package maps can shim `path`/`node:path` to a local browser file or disable `crypto`. The latest bounded package `exports` update adds `conditional-array-pkg` to prove invalid array targets are skipped before browser/default targets are selected, and that a custom `wordpress` condition can choose a WordPress-specific package runtime path. It also keeps unshimmed browser/neutral `node:path` and `node:fs` imports from resolving through fixture `node_modules` packages, which preserves a no-polyfill WordPress preflight boundary unless a package explicitly provides a browser map shim.

Current tsconfig paths update: the fixture in `fixtures/wordpress-tsconfig-assets` represents a TypeScript WordPress block entry that imports block view/style assets, shared settings/config, theme code, a runtime through an ordered fallback alias `wordpress-runtime`, a virtual absolute-looking alias `/virtual/*`, aliases inherited from a package-exported `@wordpress/tsconfig/block` config, a baseUrl-only block view import `blocks/card/view`, and a `@legacy-fallback/*` alias inherited from a package subpath JSON fallback when package `exports` does not expose the config. `TsConfigPathResolver` reads the nearest `tsconfig.json`, follows relative and package-exported `extends` entries through bounded `node_modules` package resolution, falls back to package-root `tsconfig.json` or package subpath `.json` configs inside the package boundary, merges `compilerOptions.paths` across multiple extended configs, applies the active config's overridden `compilerOptions.baseUrl`, matches exact and single-star `paths` keys by specificity, probes TypeScript/JavaScript/CSS/JSON extensions and `index`, rejects unsafe target escapes, and returns native path-resolution evidence without invoking Node/npm. The latest focused slice adds bounded package-extends fallback behavior for a package whose `exports` map blocks the config while preserving the existing `TestTsconfigBaseURL` fallback behavior. This is still a lane-local resolver slice: explicit external metadata records, Yarn PnP, plugin callbacks, broader tsconfig diagnostics, and full npm condition handling remain future work. The existing gated `js-package-resolution-core` backlog row is referenced for reuse but not activated until this batch is accepted and the next resolver gate requires cross-lane support.

## Next Task

Focused update 2026-05-25 for supervisor-next-20260525T004721Z: targeted the tsconfig package `extends` fallback cluster. Code changes are limited to `TsConfigPathResolver`, the focused tsconfig fixture/test, the WordPress smoke example, and lane metadata/notes. Verification evidence: `php tools/run-tests.php lanes/esbuild/tests/TsConfigPathResolverTest.php` passes with 1 selected test file, 37 assertions, and 0 failures; full focused lane verification passes with 6 selected test files, 1,833 assertions, and 0 failures; the local WordPress example reports `WordPress tsconfig paths aliases: yes`; `git diff --check -- lanes/esbuild` passes. Root no-argument harness was not run by assignment.

Focused update 2026-05-25 for supervisor-next-20260525T010040Z: targeted the package `exports` array/custom-condition cluster. Code changes are limited to the package resolver fixture/test, the WordPress smoke example, and lane metadata/notes. Verification evidence: `php tools/run-tests.php lanes/esbuild/tests/PackageResolverTest.php` passes with 1 selected test file, 89 assertions, and 0 failures; full focused lane verification passes with 6 selected test files, 1,838 assertions, and 0 failures; the local WordPress example reports `WordPress package exports condition arrays: yes`; `git diff --check -- lanes/esbuild` passes. Root no-argument harness was not run by assignment. Dependency closure: no new support component is needed; this reuses the bounded lane-local `PackageResolver` condition/array fallback machinery, while `js-package-resolution-core` remains a deferred shared component only if a later accepted cross-lane resolver gate needs it.

Focused update 2026-05-25 for supervisor-next-20260525T012109Z: targeted node-builtin external propagation into a bounded import-record surface. Code changes are limited to a lane-local `ImportRecord` value object, `PackageResolver::importRecords()`, the focused package resolver test, the WordPress smoke example, and lane metadata/notes. Verification evidence: PHP syntax checks pass for changed PHP files; `php tools/run-tests.php lanes/esbuild/tests/PackageResolverTest.php` passes with 1 selected test file, 95 assertions, and 0 failures; full focused lane verification passes with 6 selected test files, 1,844 assertions, and 0 failures; the local WordPress example reports `WordPress node builtin external records: yes` and `WordPress node builtin import-record propagation: yes`; `git diff --check -- lanes/esbuild` passes. Root no-argument harness was not run by assignment. Dependency closure: no new support component is needed; this reuses the bounded lane-local `PackageResolver` and adds only a lane-local import-record DTO, while `js-package-resolution-core` remains deferred unless a later accepted cross-lane resolver gate needs it.

After integrator/root acceptance of this reduced resolver handoff, the next best WordPress-visible task is first bundler graph assembly over relative/package import records, or broader tsconfig diagnostics/condition handling. Dependency-closure checkpoint: no new support component is needed for this slice because it reuses the existing bounded lane-local `TsConfigPathResolver` and `PackageResolver` and adds no shared dependency; keep the high-priority `js-package-resolution-core` row deferred behind `esbuild-bundler-resolution-next-or-lightningcss-css-package-resolution-next`. Activation would require cross-lane resolver reuse evidence, an esbuild resolver/package.json/tsconfig denominator, relevant Node package-resolution spec fixtures, malformed package.json/imports/browser-map/tsconfig/path-traversal cases, generated local fixture packages, PHP pass/fail evidence, and as much real upstream/spec runner coverage as can be run without Node/npm shell-out progress credit.
Focused update for rearmer-20260525T020009Z bundler graph assembly: the WordPress package fixture now includes `src/node-entry.js`, `src/local-preview.js`, and `src/missing-entry.js` to smoke a first native graph walk. `BundlerGraphBuilder` starts at a WordPress package entry, follows resolved package imports such as `@wordpress/interactivity`, package self imports, package `imports`, and local JavaScript files, preserves a disabled browser-map edge as a non-fatal missing edge, and records node-platform builtins such as `path` and `node:crypto` as external graph edges without probing fixture `node_modules` packages or invoking Node/npm.
Focused update for supervisor-rearm-20260525T023451Z loader-aware graph edges: the WordPress package fixture now includes `src/loader-entry.js`, `src/block.css`, and `src/block.json` to smoke terminal asset dependencies in the graph. `BundlerGraphBuilder` records resolved `.css` and `.json` imports with `css`/`json` loader labels while leaving those assets as terminal edges, and continues to traverse the adjacent JavaScript preview module. Verification evidence: PHP syntax checks pass for changed PHP files; `php tools/run-tests.php lanes/esbuild/tests/BundlerGraphBuilderTest.php` passes with 1 selected test file, 29 assertions, and 0 failures; `php tools/run-tests.php lanes/esbuild/tests` passes with 7 selected test files, 1,873 assertions, and 0 failures; the local WordPress example reports `WordPress loader-aware graph assets: yes`; `git diff --check -- lanes/esbuild` passes. Root no-argument harness was not run by assignment. Dependency closure: no new support component is needed; this reuses the lane-local analyzer/resolver/graph objects and adds only a bounded loader label on graph edges.

Focused update for rearmer-20260525T025706Z unsupported loader diagnostics: the WordPress package fixture now includes `src/unsupported-loader-entry.js` and `src/asset.bin` to smoke a local asset import with no configured loader. `BundlerGraphBuilder` records the resolved `.bin` import in `unsupportedEdges`, leaves it out of the JavaScript module traversal, and still follows the adjacent local preview module. This gives WordPress block tooling a native preflight surface for "configure a loader for this asset" diagnostics without shelling out to esbuild or Node. Dependency closure: no new support component is needed; this reuses the lane-local analyzer/resolver/graph objects.

Verification update for rearmer-20260525T025706Z unsupported loader diagnostics: focused graph and lane test runs pass, and the WordPress preflight example reports `WordPress unsupported loader diagnostics: yes`.

Focused update for rearmer-20260525T032124Z graph metafile diagnostics: added a bounded `BundlerMetafile` summary over the existing native graph. It reports entry-relative inputs with byte counts, per-import kind/external/loader/missing/unsupported metadata, and external/missing/unsupported diagnostic buckets for WordPress asset preflight tooling. The slice intentionally does not add linker output, output byte accounting, source maps, plugins, data URLs, code splitting, or broad metafile parity. Dependency closure: no new support component is needed; this reuses the lane-local analyzer/resolver/graph objects and adds only a lane-local summary class.

Focused update for watchdog-next-20260525T045348Z bounded JS output bytes: added a tiny `BundlerOutput` surface over already-resolved graph modules. It emits a deterministic entry-first concatenated JavaScript preview with source path comments, reports output/input byte counts, skips terminal CSS/JSON and unsupported assets, and preserves external/missing/unsupported diagnostics through the existing metafile summary. This is a WordPress block asset preflight smoke for shared hosting and Playground environments, not a full linker: it does not rewrite imports, perform tree shaking, generate source maps, split chunks, lower module formats, or execute plugins. Dependency closure: no new support component is needed; this reuses the lane-local analyzer/resolver/graph/metafile objects and adds only a bounded lane-local output summary class.

Focused update for watchdog-next-20260525T052405Z static bundled-JS import elision: extended the bounded `BundlerOutput` smoke so the WordPress block preview removes only static import declarations whose JavaScript target is already included in the graph. Terminal CSS/JSON imports stay visible for later loader/output slices, unsupported assets remain diagnostics-only, and per-input accounting now records the number of bundled static imports removed. This is still not a full linker: broad import-path rewriting, tree shaking, source maps, code splitting, output format conversion, CSS/JSON bundling, and plugin execution remain out of scope. Dependency closure: no new support component is needed; this continues to reuse lane-local analyzer/resolver/graph/metafile objects inside the existing bounded `BundlerOutput` class.

Focused update for watchdog-next-20260525T055536Z metafile output-byte propagation: extended the bounded `BundlerMetafile` surface to optionally accept the existing `BundlerOutput` summary and report an `outputs` map with output bytes, per-input bytes-in-output, and total/per-input static import removal counts. The WordPress smoke now checks that the block output preview contributes entry/local-preview bytes while terminal CSS/JSON assets stay out of the bundled-JS output map. This remains a diagnostic/preflight surface only: broader linker printing, import-path rewriting, tree shaking, source maps, code splitting, CSS/JSON bundling, and plugin execution remain out of scope. Dependency closure: no new support component is needed; this reuses lane-local analyzer/resolver/graph/output objects inside the existing bounded `BundlerMetafile` summary class.

Focused update for watchdog-next-20260525T070533Z node output external imports: extended the bounded `BundlerOutput`/`BundlerMetafile` surfaces so node-platform WordPress package previews preserve and account for external builtin imports such as `path` and `node:crypto`. The output input record now reports per-input external import counts/lists, and the metafile output map aggregates those externals back to the contributing input while still leaving them unbundled. This remains a diagnostic/preflight surface only: broader linker printing, tree shaking, source maps, output format conversion, external chunk generation, code splitting, CSS/JSON bundling, and plugin execution remain out of scope. Verification evidence: PHP syntax checks pass for changed PHP files; `php tools/run-tests.php lanes/esbuild/tests/BundlerGraphBuilderTest.php` passes with 1 selected test file, 98 assertions, and 0 failures; `php tools/run-tests.php lanes/esbuild/tests` passes with 7 selected test files, 1,942 assertions, and 0 failures; the local WordPress example reports `WordPress node output external imports: yes` alongside the existing bounded output/metafile smoke lines; `git diff --check -- lanes/esbuild` passes. Root no-argument harness was not run by assignment. Dependency closure: no new support component is needed; this reuses lane-local analyzer/resolver/graph/output/metafile objects and keeps `js-package-resolution-core` deferred unless a later accepted cross-lane resolver gate needs it.

Focused update for support-refill-20260525T173942Z current-base rework: added `src/output-static-entry.js` to the WordPress package fixture as a rebase-safe regression for bundled static import elision. The fixture imports the local preview module twice using different static import forms and also imports terminal CSS/JSON assets. The bounded output smoke proves shared-hosting/Playground preflight output removes the already-bundled JavaScript imports while keeping CSS/JSON asset references rewritten for a nested output path. Dependency closure: no new support component is needed; this reuses the lane-local analyzer, resolver, graph, output, and metafile code.

Focused update for continuous-dev-20260525T224833Z named re-export output preview: added `src/output-reexport-entry.js` to the WordPress package fixture. The fixture keeps a terminal CSS asset import, re-exports a local preview symbol, and re-exports a package helper symbol. The bounded output smoke proves the PHP preflight follows both re-export sources into the graph, includes their JavaScript modules in the preview, rewrites the named re-export clauses to source-less `export { ... }` clauses, and retains the CSS import rewritten for a nested output path. This keeps WordPress shared-hosting/Playground output inspection moving without invoking Node/npm. Dependency closure: no new support component is needed; this reuses the lane-local analyzer, package resolver, graph, output, and metafile code, while `js-package-resolution-core` remains deferred.
